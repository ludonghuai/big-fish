# Bigfish 已知问题与排查

> 面向用户自助排查 + 开发者记录。持续更新。
>
> 源码与反馈：<https://gitee.com/ludonghuai/big-fish>——这里没写到的故障，欢迎在仓库里提 Issue。

## 问题 1：启动弹窗「Failed to start the DeepSeek Harness backend: Timed out ...」

**严重程度**：高（重大）
**记录时间**：2026-08-15（当天已出现 3 次）

### 症状

双击后弹出错误框，提示后端启动超时（`Timed out waiting for the backend at http://127.0.0.1:xxxxx`），程序进不去。

### 根因

用户使用「创造模式」让 AI **安装 / 删除插件**时，dsh 的配置被改坏：

- `~/.dsh/profiles/web/cordis.patch.yml`、`package.json` 被改出语法错误；
- 或插件本体删了，但配置里还残留指向它的引用。

后端启动加载这些坏配置时卡死，最终超时。

### 解决方案

重置 dsh 配置（见下方「自助重置步骤」）。

### 自助重置步骤（Windows）

1. `Ctrl+Shift+Esc` 打开任务管理器，结束所有 `Bigfish` 和 `node` 进程。
2. `Win+R` 输入 `%USERPROFILE%`，回车。
3. 顶部「查看」勾选「隐藏的项目」，找到 `.dsh` 文件夹。
4. **想保留会话/工程**：先把 `sessions` 和 `storages` 两个文件夹复制到别处备份。
5. 删除（或重命名成 `.dsh-backup`）整个 `.dsh`。
6. 重新打开 Bigfish，走新手引导、重新填 API Key。
7. （可选）完全退出后，把备份的 `sessions`、`storages` 拷回新的 `.dsh`。

### 代价

- 会丢失：会话记录、预设、API Key（需重新填写）。
- 不受影响：本地磁盘上的代码、文档、图片等工程文件。
- 回收站里若有 `.dsh`，会话/工程可能还能还原（只还原 `sessions`、`storages`，别还原 `profiles`）。

### 代码侧修复

- 托盘菜单已提供「重置数据」入口。
- 已改为两档：**重置配置（保留会话和工程）** 与 **彻底恢复出厂**。
- ✅ 已实现：后端启动失败时**自动弹窗引导**「重置插件配置并重试 / 彻底恢复出厂 / 退出」，
  选重置后自动清理、重启、重试（v0.0.1）。

---

## 问题 2：双击无窗口 / 只有后台进程

**症状**：双击后任务管理器有进程，但没有窗口、没有托盘图标。

**根因**：多为未签名应用被杀毒软件 / Windows SmartScreen 拦截（主程序 `Bigfish.exe` 或打包内的 `node.exe` 被拦）。

**排查**：

1. 右键 exe → 属性 → 勾选「解除锁定」。
2. SmartScreen 拦截 → 点「更多信息」→「仍要运行」。
3. 杀毒软件（360 / 火绒 / 腾讯管家 / 华硕管家 / Windows Defender）加白名单，或去「隔离区」恢复。
4. 华硕管家、ONE-FOX 工具箱等进程管控组件先退出再试。

**根本方案**：给 exe / 安装包做代码签名（OV / EV 证书），并向杀毒平台报白。

---

## 问题 3：本地源码运行「连进程都没启动」

**根因**：本地编译环境没准备好。常见：Node 版本 < 22、只 `npm install` 根目录而漏装 `dsh-bundle` 依赖、依赖拉不下来。

**正确步骤**：

```bash
git clone https://gitee.com/ludonghuai/big-fish.git       # 源码在 Gitee
cd big-fish
node -v                                   # 必须 >= 22
npm config set registry https://registry.npmmirror.com/   # 或用华为云镜像
npm install                               # 根目录：electron 等；会自动带上 dsh-bundle 后端依赖
npm start
```

> 自 v0.0.1 起，后端依赖（`dsh-bundle/`）由 `scripts/ensure-deps.js` 自动补齐：
> `npm install` 的 `postinstall` 与 `npm start` 的 `prestart` 都会检查并安装缺失的那一份，
> 所以不再需要手动 `cd dsh-bundle && npm install --omit=dev`。依赖齐全时该检查只做两次
> 文件存在性判断，不联网、不额外耗时；确实想跳过可设 `BIGFISH_SKIP_ENSURE_DEPS=1`。

**两个容易卡住的新坑**（均出现在 npm 12）：

1. **`npm error code EALLOWREMOTE`**（`Fetching packages of type "remote" have been disabled`）。
   npm 12 起 `allow-remote` 默认为 `none`：只允许下载「与当前配置的 registry 同主机」的
   tarball。本仓库的锁文件 `resolved` 指向 `registry.npmmirror.com`，若你的 registry 是别的
   （例如默认的 `registry.npmjs.org`），安装会直接失败。仓库里已放好两份 `.npmrc`
   （根目录 + `dsh-bundle/`）写入 `replace-registry-host=always`，会把锁文件里的主机改写成
   你自己配置的 registry。若你见过这个报错，请确认用仓库自带的 `.npmrc`（别覆盖它），
   或临时用 `npm install --allow-remote=all` 绕过。

2. **Electron 二进制没装上**（`node_modules/electron/dist` 是空的，或报
   `'electron' 不是内部或外部命令`）。Electron 的二进制靠 `postinstall` 下载，被拦（npm 11.6+
   的 `install-scripts` 白名单、杀毒软件、网络中断）就会留下一个「装了但没二进制」的目录。
   国内网络先设镜像再重装：

   ```bash
   set ELECTRON_MIRROR=https://registry.npmmirror.com/-/binary/electron/
   npm install
   ```

   若 npm 提示安装脚本被拦，用 `npm install-scripts approve electron` 放行后重试；
   实在下不动可以手动把 npm 缓存里的 `electron-v<版本>-win32-x64.zip`（缓存在
   `%LOCALAPPDATA%\electron\Cache`）解压到 `node_modules/electron/dist`，并在
   `node_modules/electron/` 下写一个内容为 `electron.exe` 的 `path.txt`。
