# Bigfish 需求池 / 技术待办（docs/TODO.md）

> 本档是本仓的**需求池**与**技术待办**台账。一条一行，不展开任务细节。
> - 需求池条目（用户需求点）挂「需求档节 + 批次档 §2」；技术待办挂「归属档节 + 最小证据行（file:line + 症状）」。
> - `status` 只取六态：**待讨论 / 待设计 / 在途 / 待核销**（活文件只留未决四态）；**已核销 / 已废弃**为归档态，勾销后逐条移入 `docs/TODO-archive.md`。
> - 技术待办每条带一种触发：`触发=归批（<批名>）` / `触发=条件（<条件句>）` / `触发=认账不排期`；无触发者进「待处置」清单，行龄超 30 天标「老化」。
> - **行宽约定（项目声明，2026-09-17 用户裁定 A）**：本档（台账）与 `docs/README.md`（文档地图）的**表格行不受「无 >300 字符单行」约束**——「一条一行、指针化、不展开细节」是本档的根本形态；该行宽约束适用于需求档 / 设计档 / 批次档等叙述性文档。

---

## 一、需求池（用户需求点）

| # | 条目 | 需求档节 | 批次档 §2 | status | 触发 |
|---|---|---|---|---|---|
| R1 | 桌宠拖拽跟手修复（Windows） | `docs/requirements/PET.md` §三 US-1…US-8 / §四 NFR-1…NFR-4 | `docs/batches/B01-pet-drag-follow.md` §2 | 在途 | 归批（B01） |
| R2 | 自动更新：App 本体（含内置技能/插件随包更新）+ Harness（npm latest）+ 已装插件（注册表版本对比）；更新源统一迁 Gitee | `docs/requirements/UPDATE.md` §一 / §三 US-1…US-9 | `docs/batches/B02-auto-update.md` §2 + `docs/batches/B04-harness-activate-fix.md` §2 | 待核销 | 归批（B02 实施 + B04 AC9 修复轮）——真机复测 AC9 通过（B04 §6.2）；剩 AC5/AC10/AC11/AC12 为发布门项（见 T8） |
| R4 | 桌宠多屏几何（多屏丢失 / 跨屏尺寸与抓取点 / 落点校正） | `docs/requirements/PET.md` §三 US-9…US-14 / §四 NFR-5…NFR-8 | `docs/batches/B03-pet-multimonitor.md` §2 | 在途 | 归批（B03 实机验收——13 轮修正均机检结，但实机面未跑，见 T9） |
| R11 | **接手项目规范化**：「把仓库风格统一成工程模式（Thincoder）风格」——① 规范载体（`AGENTS.md` / 代码规范档 / `.editorconfig` / lint-format 取舍 / `CHANGELOG.md` 起步）、② 文档补齐（探针脚本与杂项未入档、用户文档定位、地图入口）、③ 代码体检与优化改进空间（按证据排序逐项走全流程）。**本项属工程/流程类需求（非产品板块需求）**——落点由 B11 设计裁定 | `AGENTS.md`（B11 产出；本仓现无项目规范档——B03 评审当时即声明该局限） | `docs/batches/B11-conventions.md` §2（① 规范载体，已落）+ B14 / B15 / B16 / B17 各批批次档 §2（② ③ 分批，未立案） | 在途 | 归批（**① 规范载体已收口（2026-09-17）**：B11 轮 1 pass → 修正轮 1–4 → 独立复核 pass → 清理轮；**AC1–AC6 机检全绿**（核销证据见 `docs/batches/B11-conventions.md` §6）；**②③ 待 B14 / B15 / B16 / B17**；序 = B11 规范基建 → B14 文档事实收口 → B15 仓库卫生 → B16 测试与门禁 → B17+ 代码优化串） |
| R12 | 回合**等待用户确认**时增设一条提醒（B09 设计期 U-13 提出）：现判定下「回合在途 ⇒ 不提醒」正确（避开语义错误的「已完成」），但用户可能确实需要一个「该你确认了」的信号 | `docs/requirements/SHELL.md` §三（待落——归批时由 eng-designer 落） | B09 设计档 `docs/design/SHELL-UX.md` §2.2.12 的 **L-B09-1** + B09 批次档 §2.7 待裁定项 | 待讨论 | 归批（**待立批**——用户 2026-09-17 裁定 **A：不并入 B09**，另立需求点） |
| R10 | 任务完成提醒**迟滞太久**——「早就做完了结果才发消息」；机制 = 末次写入后**空闲 30 s** 才提醒（`main.js:49` `IDLE_NOTIFY_MS = 30 * 1000`）+ 判据 `now - lastBusyAt > IDLE_NOTIFY_MS`（`shell-notify.js:132`），忙信号只看 `~/.dsh` 下非 `profiles`/`node_modules` 的写入 ⇒ 提醒时刻与真实完成时刻脱钩 | `docs/requirements/SHELL.md` §三（待落——归批时由 eng-designer 落；本批 US-11 明文锁定「阈值 30 s 不改」⇒ **新需求点**） | `docs/batches/B09-notify-latency.md` §2 | 待核销 | 归批（**B09——实施已交付**：评审轮 1 changes-required → 修正轮 1–3 → 实施 → 收口轮；**§6 已核销**（AC17–AC19 **绿**；**AC20 真机时延量化未跑** ⇒ 真机项，不记已验）；**归档条件 = 用户真机秒表验收通过**（秒表测「末次写入 → 提醒」+ 目视判「显著更快」+ `grep "completion gate unavailable" bigfish.log`）） |
| R13 | **跨平台可用性（win / linux / mac）**：本仓需在 Windows 之外**建立 linux（及 mac）的可使用性**——构建面已有目标（`electron-builder --win/--mac/--linux` = nsis / dmg / AppImage+deb，`package.json` scripts 齐备），但**可用性未建立**：发布 / 更新通道为 **win32 单平台**（`latest.json`）、无「安装 → 启动 → 托盘 / 桌宠 / 通知 / 更新」的跨平台实测面 | （待落——归批时由 eng-designer 落） | （待立批：候选 = **并入 B13 打包链与发布面**（同域）或独立批） | 待讨论 | 归批（**待立批**——用户 2026-09-17 提出；**平台集合已裁定（用户 2026-09-17）= A**：win + linux 先做透（Linux 验 AppImage / deb 实跑 + 沙箱 / 托盘 / 通知 / 路径差异），**mac 后置**（需 Apple 开发者证书做签名 / 公证；无证书则首启需手动放行）） |
| R14 | **外部项目样本 / 移植参考目录（完全解耦）**：新建顶层目录供用户放置**他人项目**（用途 = 移植功能 / 借鉴样本）；**硬约束**：本目录**不被任何代码引用 · 不参与构建 · 不打进发行版**——含「整体不入仓」（git-ignore ⇒ 第三方代码不进历史、规避许可与污染）与「构建面零登记」（`build.files` / `extraResources` 均为**显式白名单**，不登记即天然排除）；**移植本体另立需求点，走四步流程** | （待落——归批时由 eng-designer 落） | 归批（**父侧已落部分（2026-09-17）**：`samples/` 目录 + `.gitignore` 条目 + 地图落点行；**余项** = 「不打包 / 不引用」的机械守卫（`build.files` / `extraResources` 禁列 + 全仓 require 引用面零命中）随 **B15 仓库卫生 / B16 门禁**） | 待讨论 | 归批（**待立批**——用户 2026-09-17 交付样本 `samples/dsh-pet` 并要求「分析 + 设计功能植入」；详见下条 R15） |
| R15 | **外部项目能力移植（源 = `samples/dsh-pet`，PC2005-cloud，v0.2.11，MIT 代码）**：候选面 = ① **动画链引擎**（权重链 + 双缓冲交叉淡入 + 事件档位 + 档内随机轮换）② **物理手感**（阻尼弹簧拖拽 / 甩抛抛物线 / 边缘反弹 / 落地摩擦 / Q 弹挤压）③ **会话事件联动**（工作状态六档 / 余额档位 + 气泡 / 碎碎念 / 对话 + 记忆持久化）④ **多实例与 pet pack**（多宠物、新种类）⑤ **素材生成链**（提示词 → 绿幕源视频 → 抠像 → 归一化 → 转码）⑥ **无障碍与平台兼容**（`prefers-reduced-motion` / headless Linux 检测 / Safari mov 回退）。**不做** = 其 DSH 插件架构 / cordis+React+tsdown 工具链 / Electron 运行时下载（本仓是**宿主应用**、非插件；B11 规范亦不引入 lint/构建大改）。**许可硬约束**：其**代码 MIT**（可适配，但**必须署名** —— 落 `THIRD-PARTY-NOTICES.md`，已在 `build.files` 清单内）；其**动画素材 / 提示词 / 源视频「允许开源使用、禁止商用」** ⇒ **用户 2026-09-17 裁定「本仓不商用」⇒ 素材可用于本仓**（须署名 `THIRD-PARTY-NOTICES.md` + 发布面，且**带商用化消解路径**：转商用 ⇒ 换自产资产或取得授权）；**体积无上界**（同日裁定「素材无预算」） | `docs/requirements/PET.md` §三 US-15…US-19 / §四 NFR-9…NFR-13（B18 段已落；其余面待立批时落） | `docs/batches/B18-pet-animation-chain.md` §1 / §2（A 面已立案；B / D1 / D2 / C / E 各面待立批） | 在途 | 归批（**A 面已收口（2026-09-17）**：B18 评审 pass → 实施（桩测 **121/121** · **O5 探针 pass** · 几何零 diff）→ 收口轮 → **§6 核销**；序 = **A 动画链 → B 物理（+ O10 位移接管合并）→ D1 工作状态 → D2 余额/碎碎念/对话 → C 素材链 → E 多实例**）；**用户 2026-09-18 裁定（B20 面）**：① **物理参数取 B**——`restitution` 0.78 → **≈0.55**（玩偶感：弹 1–2 下、典型抛掷 ≈3.9 s 停；用户原话「先做成 B 吧，不行再调整手感」）⇒ U-2 由「照搬样本」改为「**本仓调**」；**须同步**设计档参数表 + §2.2.12/AC11/NFR-19 的静止时长判据（口径随参数变）+ 与样本差异登记；② **ffmpeg 取法照 `node-runtime/` 先例**（官方入口，落 `tools/`，不入库、不进包）。**两项均在 B20 设计解冻后落档（口径变化 ⇒ 需复评）**。**用户 2026-09-17 四项裁定**：① **不商用** ⇒ 素材可用（见左）；② **素材无预算**（不设体积上界）；③ **LLM 走用户自己的 API**（项目零成本）；④ **O10 位移接管 = 与 B 面合并**（立批时先定位移仲裁层 + 重跑 B03 判据，依据见 `docs/batches/B18-pet-animation-chain.md` §1.9） |
| R16 | **动作解锁规则（本仓原创需求，非移植面）**：桌宠动作池随**时间**与**陪伴**生长——① **时节门**：系统日期落在「节前 7 天 → 节后 3 天」窗口内才解锁该时节动作（**其余时间锁死**；农历节日需**建表**）② **饭点门**：三餐窗口内解锁「吃早餐/午餐/晚餐」，一天一次、过点不补 ③ **等级门**：好感度等级解锁动作（**越丰富越高等级**，10 级对 10 档）。**+ 动作图鉴**（2026-09-18 用户增补：与喂食**同弹窗的第二标签卡**——列 动作 / 解锁条件 / **解锁百分比**；原「另批」改为本批一并做）；**不做**：抽卡/成就、网络校时 | （待落——归批时由 eng-designer 落：`docs/requirements/PET.md`） | `docs/batches/B23-pet-action-unlock.md` §1（立案含勘察 + 硬约束九条 + 用户已裁定 D-1…D-3 含 10 级映射表） | 在途 | 归批（**B23 已立案** 2026-09-18；设计待派——**排在 B21 之后**（两者同碰 `pet-chain-core.js` 选段面，实施串行）） |
| R17 | **工作状态六档（事件桥）**：样本六档中的「等待批准 / 回合成功 / 回合失败」在本仓只读落盘面**不可观测**（B19 段实测：事件名存于 `dsh-agent-presets` 的 `SessionEventMap`，`dsh-user-approval` 明写 log-only ⇒ 不进投影行）⇒ 用户 2026-09-18 裁定「做6种」⇒ 需建**事件桥**（后端插件订阅 `turn/end` / `tool/call` / `approval` / `asked` 并落盘可读面）；B19 已预留扩展面（池段可增 3 键 + 判定门可扩档 ⇒ **增量补齐、不返工**；素材已在仓未引用） | （待落——归批时由 eng-designer 落） | （待立批：**B22**——先技术预研后端插件 / ACP 订阅面） | 待讨论 | 归批（**B22 待立批**；序 = 排在已立案批次之后） |
| R18 | **动作生产 / 导入（台账原为「用户面」；2026-09-18 用户裁定改向）**：① **现形态（本批 = B24）= 维护者工具链、不进 app**——用户指定源视频 ⇒ 一条命令跑通 抠像 → 归一化 → VP9-alpha 编码 → alpha 校验 → 入 `assets/pet-anim/webm/` + 写 `pool.json`（工具链不进发行包；ffmpeg 按 `node-runtime/` 先例取，不入库）；② **后置 = 用户面导入**（app 内入口 + 自动解析成动作；**「管理员入口」不可行**——桌面应用里隐藏 ≠ 限制，用户拥有本机程序）。原话「给用户一个入口添加视频之后，是不是可以解析成动作呀？」+「…只有我才可以添加动作，**或者就不坐在 app 里面，我指定哪个视频直接让你解析**」 | （待落——归批时由 eng-designer 落） | `docs/batches/B24-pet-import-action.md` §1（含 **§1.8 范围改向裁定** 与待决 U-1…U-6 备查） | 在途 | 归批（**B24 已立案，范围已改向**；设计待派） |
| R19 | **工作档可见性：自主随机小动作应在工作档期间让位（2026-09-18 用户实机观察）**：用户原话「工作模式的时候会**穿插其它模式的动作**，导致动作不明显，交替起来看不出来在工作」✗。**取证（主 agent 亲验）**：设计档 §2.8.4 优先级表 + 共同门（`shell-pet-work.js:173-178`）的让位名单 = `drag ∨ walk/run ∨ happy ∨ eat ∨ read ∨ starry ∨ scared`；而 `shell-pet.js:122` 的「待机时随机表演小动作（看书 / 星星眼 / 惊吓 / 开心）」**恰好全在名单内** ✗ ⇒ 自主小动作不断打断工作档 ✗。**设计自相矛盾**：DD-23 已裁定「工作档期间不启动散步」（理由 = 「干活」>「溜达」）⇒ **同一理由应覆盖自主小动作** ✗（`shell-pet.js:287` 的起步门只对散步加了 ✗）。**修法方向** = 工作档期间闸住**自主随机**小动作（与散步同口径 ✓）；**保留**用户触发交互（点击 / 喂食）压过工作档 ✓（响应性不变 ✓）。连带面 = 设计档 §2.8.4 优先级表 + 共同门口径 + AC/TC 各一条 + 需求档 US-21 一句 | `docs/requirements/PET.md`（US-21 面）/ `docs/design/PET-ANIMATION.md` §2.8.4 / `shell-pet.js:122` · `:283-287` | 实测：`shell-pet.js:122`（自主小动作）+ `:287`（只给散步加门）+ `shell-pet-work.js:173-178`（共同门含 `read/starry/scared/happy`）；同源观察 = 设计档 §2.8.4 L624 优先级表 / DD-23 | 在途 | 归批（**B26** —— 用户 2026-09-18 裁定「修」✓ 已并入桌宠观感修正批；设计在飞）· **2026-09-18 主 agent 代裁（用户授权「先听你的」）**：F3 = **按档节流**（每档各自 ≥ 30 s）+ **全局下限 10 s**（防「思考→工具→收尾」连弹三条 ✗）⇒ F3 纳入 B26 ✓；用户实操后再看效果，不满意可一处常量回正 ✓ |
| R20 | **弹跳落点与「可见边框」差一段（2026-09-18 用户实机）**：用户原话「重力模式弹跳不真实，**距离边框有一点的距离**，我觉得应该**完全等于边框**，这才真实」✗。**取证（主 agent 亲验）**：物理边界 = **窗口矩形口径**（设计档 §2.2.4 明写；`shell-pet-physics.js:119` 取 `petWorkAreaBounds` + `:272` 以 `bounds.maxY` 收口 + `pet-physics-core.js:232-233` `y = b.maxY` 落地）⇒ **窗口下沿**贴地 ✓；但**可见精灵**在窗口内内缩 ✗ —— `pet.html` 的 `#pet-stage { 640×360; overflow:hidden }` + `#pet { position:absolute; bottom:26px; height:200px }` ⇒ 脚底离窗口下沿还有 **26px（×几何缩放）** ✗ ⇒ 观感 = 悬在地面上方一小段 ✗。**性质 = 设计缺口**（DD-6 / U-8 只定义「窗口矩形口径」，未考虑可见面内缩 ✗；非实现 bug ✓）。**修法方向（三选一，交设计者）**：① 物理地面按**可见脚底口径**补偿（常数 / 按池帧内容盒实测 ✓）② 改 `#pet` 的 `bottom` 使可见脚底 = 窗口下沿（**碰渲染层** ✗）③ 起飞时**一次性实测**该内缩量（校准式，与 `petCalibrateSize` 同族 ✓）。**连带面** = 设计档 §2.2.4 边界口径 + AC/TC 各一条；走 ② 则含 `pet.html` | `docs/requirements/PET.md`（US-24 面）/ `docs/design/PET-MOVEMENT.md` §2.2.4 / `pet.html`（`#pet` / `#pet-stage`）| 实测：`pet.html` `#pet { bottom: 26px }`（脚底内缩）+ `shell-pet-physics.js:119` · `:272`（边界 = 窗口矩形）+ `pet-physics-core.js:232-233`（`y = b.maxY`）| 在途 | 归批（**B26** —— 与 R19 同批；用户 2026-09-18 裁定「修」✓）· **2026-09-18 主 agent 代裁**：`FEET_ALPHA_MARGIN_DIP` 取 **4**（= 实测 alpha 残差 ⇒ 可见脚底真踩实，对齐用户原话「完全等于边框」✓）；如实机观感过度（脚压进任务栏 ✗）⇒ **一处常量回正为 0** ✓ |

## 二、技术待办

| # | 条目 | 归属档节 | 最小证据（file:line + 症状） | status | 触发 |
|---|---|---|---|---|---|
| T4 | 无自动化测试基建 | `docs/design/PET-DRAG.md` §3.3 | `package.json:13-21` scripts 无 `test`（= start/pack/dist/dist:win/mac/linux/icons/make-latest）；现存 `tests/` 三文件（update-lib.test.js / update-stub.mjs / harness-store.test.js）为开发期工具、非仓门禁 | 待讨论 | 认账不排期 |
| T5 | 发版流程说明缺 `make-latest` 步骤——手工改 `latest.json` 会留空 sha256，客户端 fail-closed 拒装新版本 | `docs/design/AUTO-UPDATE.md` §2.2.8 | `latest.json:11-13` —— sha256 三平台为空串；`package.json` scripts 已有 `make-latest` | 待讨论 | 条件（下次 App 发版前） |
| T7 | 打包前置 `node-runtime/` 已落档 + 本机备齐：README §打包 增补三步准备法（Node 22 zip 取 `node.exe`；`pnpm@10` 取 `dist/pnpm.cjs` + 一行 ESM 壳 `pnpm.mjs`；npmmirror 镜像环境变量）；本机 `node-runtime` = Node v22.20.0 + pnpm 10.34.5（2026-09-16），`dist:win` 产出 `Bigfish.Setup.0.0.1.exe`（157.2MB）冒烟通过（内置 node 拉起 dsh 后端、内置 pnpm 跑通 harness 自更新安装）；`node-runtime` 维持不入库（平台二进制） | `README.md` §打包 | `node-runtime/node.exe` + `node-runtime/pnpm/{pnpm.mjs,pnpm.cjs}` 实测可用；`dist/win-unpacked` 冒烟：`node-runtime\node.exe …\dsh\lib\bin.js --profile web` 存活 | 待核销 | 条件（用户复核 README 步骤 / 下次换机打包复现） |
| T8 | 首次真实发版演练清单：`make-latest.js` 正例（AC11）· 真实安装+自动拉起（AC5）· 市场更新徽标（AC10）· 更新前后 `~/.dsh` 关键面快照（AC12） | `docs/design/AUTO-UPDATE.md` §2.2.8 / §3.1 | `docs/batches/B04-harness-activate-fix.md` §6.3 —— 四项均为发布门项，本地构建不可闭环；⚠️ 禁用本地构建产物跑 `make-latest.js` 覆盖 `latest.json`（sha256 会与已发布实附件不符 → 用户拒装） | 待讨论 | 条件（首次真实发版时） |
| T9 | **跨屏瞬间「上下突跳」的实机验证**（第 13 轮已修，仅机检） | `docs/design/PET-MULTIMONITOR.md` §3.1 S8 · §3.2 TC-31 · `docs/batches/B03-pet-multimonitor.md` §6.3-1 / §6.4 | `main.js:1250` —— 尺寸写入已改 `setBounds({ width, height })`（不传位置）+ 位置校验 / 条件回写；桩测 189/189 结，但**本机无混合 DPI 三屏硬件，实机未验**；判据 = 尺寸型 `geom-fix` 行的 `\|pos-after − pos\| ≤ 1 DIP`；另 E8（partial 矩形语义）为文档明证未实机取证 | 待核销 | 条件（用户三屏实机验收时，兼 B03 的 TC-23/24/27/29/30/31） |
| T10 | harness bundle 落后 5 个 rc（`0.1.0-rc.6` vs 注册表最新 `0.1.5-rc.1`，2026-09-10 发布）。**原条目后半段「dev 的 `dshBinPath()` 固定走 bundle ⇒ harness 自更新在 dev 不生效」已被 B06 F5 覆盖**——现 `shell-backend.js` 活跃指针优先、dev 与打包同口径，且已实测生效（`updater.log` 2026-09-17：dev 侧完成 0.1.0-rc.6 → 0.1.5-rc.1 安装 + 激活 ok=1） | `docs/design/AUTO-UPDATE.md` §2.2.1（路径口径） | `dsh-bundle/package.json:7`（**精确钉 `0.1.5-rc.1`**，B08 于 2026-09-17 刷新——旧值 `0.1.0-rc.6`）；`shell-backend.js:52`（活跃指针优先）；registry 实查 = `0.1.5-rc.1` | 待核销 | 归批（**B08 实施已交付**：钉版 `0.1.5-rc.1` 与锁三值一致（主 agent 亲读）· `bundle:refresh` / `bundle:check` 两入口在场 · `--check` 主 agent 亲跑 `action=none`（与注册表 latest 同步）；**§6 已核销**；归档条件 = 打包态双态冒烟 / `npm ci` 全量复现由用户定是否补跑）；**2026-09-18 用户裁定「刷 bundle」（实跑 `npm run bundle:refresh`）**：`pin=0.1.5-rc.1 → 0.1.5-rc.2`（tag=latest、registry=npmmirror）· 冒烟过 ✓ · `bundle:check` = `action=none` ✓ · 变更面 = `dsh-bundle/package.json` + `package-lock.json`；**动因** = 用户「离线版应=最新版，没有新版就不该提示」（实测启动弹 `update-available latest=rc.2 current=rc.1`）；**残余** = 运行时那份（`~/.dsh` 的 active harness）仍为 rc.1 ⇒ 如仍提示，点一次它自己的更新即归位（归 T41 同面） |
| T13 | **行宽债清理（非豁免面实测 65 行——修正原登记的 3 行）**：明细 = `B03-pet-multimonitor.md` **40**（§2 3 / §5 37）· `PET-MULTIMONITOR.md` **18** · `AUTO-UPDATE.md` **3**（`:57` 366 / `:829` 309 / `:1034` 321（as-of 2026-09-17 B08 修正轮 1 后；行号随文档增删**持续漂移**——历史 `:53/:698/:853` → `:796/:993` → 现值，**以 `>300` 计数为准、行号只作 as-of 参考**））· `B01` §2 1 · `B05` §6 1 · `B06` §5 1 · `PET.md:214` 1（321，原未登记）；**豁免面**另 56 行（批次档 §3 34 / 台账 16 / 地图 6）不动。①②③（跨档一致性）已由 B07 收口 | 见上各档 | 主 agent 2026-09-17 全仓实测（分段归属 + 不含 CR 口径）；与 B11 设计者独立计数一致（65） | 待讨论 | 条件（**消解路径**：**活文档 22 行**（`PET-MULTIMONITOR` 18 + `AUTO-UPDATE` 3 + `PET.md` 1）随 **B14** 修正；**批次档历史段 43 行**随该档下次被合法动笔时一并压行——父侧不代笔改写他人段；用户若要求立即清，另批授权）· **2026-09-18 B16 设计期复测（口径 = B16 附 A 的 A-check 扫描面）**：非豁免 **92 行 / 16 档** · 豁免面另计（**批次档 §3 = 121 行 · 表格行 = 45 行**）—— **与上行 65 / 56 的差异源于扫描面与豁免面口径不同**（本行为 B07 期口径）⇒ 两值并存，**以后续 B16 落地的机检基线口径为准** |
| T21 | B12 设计期发现的**跨档前置条件**：`AUTO-UPDATE.md` §2.2.5 / §3.1 AC10 与 `UPDATE.md` US-7 的「更新判定面」在本批后新增了前置（插件标识入参门）——两档均未记该前置（写权不在 B12 设计者） | `docs/design/AUTO-UPDATE.md` §2.2.5 / §3.1；`docs/requirements/UPDATE.md` US-7 | B12 设计档 O21（`docs/design/SHELL-UX.md` §2.2.14 与 §3.1 AC26–AC31 的描述面） | 待讨论 | 归批（**B14 文档事实收口**——与其余跨档不一致同轮处理） |
| T22 | 内置插件「**一键安装**」路径**当前不可达**（用户可见功能缺陷）：`normalizePlugin` 只从 `install` 字段提 `add …` 形态，而内置条目 `install` = 字面量 `builtin:<name>` ⇒ `installSpec` 为 `undefined`；`market.js:275` 的「一键安装」为**死码** | `docs/design/SHELL-UX.md`（B12 O22 已落档） | `market.js:99-103`（提字段）· `:275`（死码按钮）· 对照：`shell-plugins.js` 的 `builtin:` 分支已实现 | 待讨论 | 归批（**待定**——B17 代码优化串可承载，或用户指定**快车道**单点全流程；修复面小但属行为修正） |
| T23 | 插件市场面同族两项（B12 设计期发现，本批不改）：① `market.js:254-258` 把注册表字段 `p.url` 直赋 `<a href>`，**中键/新窗与导航守卫未设**（与「更新 URL 冒主白名单」同族）；② `shell-market.js:127`/`:131` 为打日志而各扫一次目录 / 读一次 profile（扫描开销家族） | `docs/design/SHELL-UX.md`（B12 O20 / O23 已落档） | `market.js:254-258`（`href` 赋值）· `shell-market.js:127`·`:131` | 待讨论 | 归批（**B17 代码优化串**——低风险卫生项；① 可与「更新 URL 白名单」同轮） |
| T16 | 同源面一致性（设计档 §2.2.6 注 S1 ↔ `shell-backend.js` 头注释 ↔ 实测）：① 设计档函数行号 as-of 漂移（记 `:165`/`:105`，实测 `:197`/`:97`）；② 头注释 `dshHome` 序在 `getCurrentDshVersion` 前、而实测相反（`:286` vs `:278`）；③ `getPort` / `init` 枚举口径两档不同形；④ B07 §5.2 三处行号残差（`makeTee` 记 `:237-247` 实测 `:240-248`；A2 记 `:243` 实测 `:245-246`；not captured 记 `:254` 实测 `:257`） | `docs/design/SHELL-UX.md` §2.2.6（注 S1）；`docs/batches/B07-harness-auth-compat.md` §5.2 | 上列四项均已由 B07 收尾轮实测定位（eng-coder 报告 + 主 agent 亲扫） | 待讨论 | 归批（B08——或与下次动 `shell-backend.js` / §2.2.6 的轮次同轮；四项均属「同一清单的多面同形」类，一次对齐可全清） |
| T11 | 安装包二进制托管 = GitHub `ludonghuai/big-fish` Releases（Gitee 附件 100MB 上限装不下；0.0.1 实测 157.2MB）；建仓已完成（2026-09-16 公开 + main 双仓同步推送，git 已对 github.com 配 127.0.0.1:7890 代理）；安装包已构建 + `latest.json` sha256 已填（win32 单平台）；**剩用户手动步**：GitHub 网页建 release `v0.0.1` 传 `dist/Bigfish.Setup.0.0.1.exe` → Gitee 建 tag `v0.0.1` → 推送 `latest.json` | `docs/design/AUTO-UPDATE.md` §2.2.2 / §2.2.8 | `dist/Bigfish.Setup.0.0.1.exe`（157.2MB，2026-09-16 构建）；`make-latest.js` 上传清单三步输出 | 在途 | 条件（用户完成 GitHub release 上传后收口并推送 latest.json） |
| T24 | 仓库内固化 EOL 口径：新增 `.gitattributes`（`* text=auto eol=lf`）——现口径只依赖本机 `core.autocrlf=true` | `docs/CONVENTIONS.md` §六（已知局限） | 全仓无 `.gitattributes`；`git config core.autocrlf` = true（本机） | 待讨论 | 归批（**B15 仓库卫生**）/ 用户裁定 |
| T25 | 前世代 / 相对指针**文件头迁移**：**20 档** = 17 档无设计档指针 + 3 档相对指针（`update.js` · `make-latest.js` · `market-update.js`）；迁到 `docs/CONVENTIONS.md` §一 标准形 | `docs/CONVENTIONS.md` §1.1 | 逐名清单见 `docs/CONVENTIONS.md` §1.1（口径 = `git ls-files` 42 档三分；引用时以当次输出为准） | 待讨论 | 归批（随下次动档的批次 / **B17 代码优化串**） |
| T26 | 行宽 ≤300 / 行数 ≤500 **升为门禁句**（机检接入 CI；口径 = 不含行尾 CR；豁免面 = 台账 / 地图表格行 + 批次档 §3） | `docs/CONVENTIONS.md` §五 | 判据已成文（规范档 §五）；机检未接（`.github/workflows/build.yml` 无 lint / test 步骤） | 待讨论 | 归批（**B16 测试与门禁**） |
| T28 | `docs/design/SHELL-UX.md` **§2.2.6 依赖表的「注入面」列与实现不符**：该行把 `petSay` / `setPetState` 写作**顶层注入键**，实测（B10 修正轮 3）= **`pet` 对象的成员**——`init(deps)` 实键 = `getPetWindow` / `pet` / `setQuitting` / `APP_NAME`（与 `main.js:66` 同形）；载具或后续实现者照字面写会得到 `pet === undefined` ⇒ `petSay` 调用抛错 | `docs/design/SHELL-UX.md` §2.2.6 该行（B06 面措辞） | `main.js:66`（`init` 实参形态）· `shell-affinity.js:18-23`（读 `deps.pet`）· `:262`（导出 `handleAffinityView`，内部转发 `affinityView()`） | 待讨论 | 归批（**B14 文档事实收口** / 随下次动 §2.2.6 的批次） |
| T29 | **B03 dev 桩测工具的抽取源漂移**：`.thincoder/b03-pet-calibrate-stub.mjs:84` 硬绑 `main.js`，而几何 / 拖拽 / 散步实现已由 **B06 F6 拆分**迁出（`main.js:35-37` 只余 `require`）⇒ 现跑即抛 `marker not found: const PET_SIZE_DIP = {`（`:96` / `:118`），**0 条断言执行**（B03 的 189/189 为当时历史证据，as-of）；后果：任何「以该桩为几何零回退门禁」的判据**不可复现**（B18 §1.4-2 原判据已据此重写） | 该桩自身（`.thincoder/`，受 `.gitignore` 覆盖、不入仓） | `.thincoder/b03-pet-calibrate-stub.mjs:84` / `:96` / `:118` · `main.js:35-37` · `docs/batches/B03-pet-multimonitor.md:888` / `:915`（189/189 历史）/ `:1054`（路径与跑法） | 待讨论 | 归批（**需用时先修**：改绑 `shell-pet-geometry.js` / `shell-pet-drag.js` / `shell-pet.js`，纯 dev 工具；当前由 **B18 §1.4-2 ① 的仓级判据（几何面零 diff）** 承担该回归面） |
| T31 | **跳档「行锚 / 计数」漂移统一收口（承 B08 实施）**：`package.json` 因 B08 **+2 行**（scripts 10 → **12 项**）⇒ 多处跳档的 `package.json:NN` 行锚与计数陈述过期（B08 写域内 9 处已修，**写域外仍存**）：`docs/design/PET-ANIMATION.md` 5 处 · `docs/design/PET-MULTIMONITOR.md` 2 处 · `docs/design/SHELL-UX.md` 3 处 · `docs/requirements/PET.md:259` · `docs/batches/B02:190` / `:295` · `docs/batches/B03:340` · `docs/batches/B18:169` / `:190`；**另** `AGENTS.md:39` 的 `build.yml（75 行）` 实测 **74**（末行空行口径） | 各归属档节（逐处引现行值 + as-of 注） | B08 收口轮自查表（批次档 B08 §2.11）；实测：`build.yml` **74** 行 · `package.json` scripts **12** 项 | 待讨论 | 归批（**B14 文档事实收口**——本项即该批的典型面） |
| T30 | **`scripts/ensure-deps.js` 的陈旧进程清理通配对 Windows 路径不匹配**（B08 实施期实测发现，**非本批引入**）：其 PowerShell 清理通配对反斜杠路径不匹配 ⇒ **用户运行中的后端未被清**（实测）；后果 = 依赖变更后旧后端可能仍占端口 / 跑旧代码 | `scripts/ensure-deps.js` | B08 交付报告 §5.6 范围外注记（实测：用户进程未被清）；同档先例 `:57` 的 `shell: true` 拼接 | 待讨论 | 归批（**B17 代码优化串** / 随下次动该档的批次） |
| T32 | **卸载面 `builtin:<名>` 假成功**（B12 实施期发现，**与改前逐字同源、非本批回归**）：`shell-plugins.js` 卸载面**动作面用原始入参** ⇒ `uninstallPlugin('builtin:<名>')` 返回 `{ok:true,'已卸载 builtin:<名>…'}` **而实际未删未注销**；修法方向 = 动作面改用 `resolveInstalledName` 的解析结果（而非原始入参）⇒ 属 `pnpm remove` 实参语义变更（设计档行为差异表未覆盖该面） | `docs/design/SHELL-UX.md` §2.2.14（卸载面；B12 已登记为已知限制） | B12 交付报告 §5.6 残余 #4（主 agent 亲验）：`shell-plugins.js:412` / `:415` / `:421` / `:424` 用原始入参；与 **T22**（`builtin:` 一键安装死码）**同族**——同为 `builtin:` 形态在动作面的落点缺口 | 待讨论 | 归批（**B17 代码优化串** / 随下次动 `shell-plugins.js` 的批次；与 T22 同轮） |
| T33 | **B09 判定门与 B19 工作状态读面「同读一族落盘物、谓词重复」**（B19 设计期发现 O16）：两处各自读 `<DSH_HOME>/storages/session_projcache/sessions/*.json` 的 per-record 单元并各自维护同族谓词（新鲜度 / 回合边界）⇒ 建议抽**共享只读模块**（两处同时改，避免各自漂移） | `docs/design/PET-ANIMATION.md` §2.6.4 / **O16**；`docs/design/SHELL-UX.md` §2.2.12 | B19 设计报告 ⑤ / 设计档 O16（建议行，主 agent 登记）；实测两处读面：`shell-notify.js` 的 `completionGate()` 与 B19 新增读面 | 待讨论 | 归批（**B14 文档事实收口** 或随下次动该读面的批次） |
| T34 | **根目录散档归位（结构债；2026-09-18 用户裁定 A“结构调整”的实测面）**：根目录自研档**平铺 49 档**，其中**不进发行包**的 = 8 档 `probe-*`（`probe-displays.js` / `probe-displays2.js` / `probe-pet-media.js` / `probe-position-accuracy.js` / `probe-resizable-setsize.js` / `probe-settle-scan.js` / `probe-size-readback.js` / `probe-straddle-size.js`）+ 5 档构建脚本（`afterPack.js` / `download-electron.js` / `download-node.js` / `make-icons.js` / `make-latest.js`）⇒ 建议归位到 `scripts/`（已存在）或 `tools/`；**连带面实测** = ① 各探针内部 `require('./pet.js')` 等的**相对深度** ② `probe-pet-media.js` 硬绑 `main.js`（见 **T29**） ③ 探针的**设计档指针**（R11 ② 点名「探针脚本与杂项未入档」） | `docs/design/REPO-CONVENTIONS.md`（B11 规范面）/ 各探针文件头 | 实测：根目录 49 自研档；`package.json:41-84` `build.files` **零 probe 条目**（不进包 ⇒ 可安全搬迁）；`probe-pet-media.js:1-2` 的 `./pet.js` / `./pet-chain-core.js` 相对引用 | 待讨论 | 归批（**B15 仓库卫生**——与 T24 同批；**排 B19 / B20 之后**：两批在飞时不动档） |
| T35 | **`测试-更新功能/` 目录清点与处置**（中文名 + 疑似遗留）：根目录实测存在该目录（**仅 1 项**），用途与产出未入档 | 待定（清点后定落点） | 实测：`./测试-更新功能/` 存在（1 项）；不在 `build.files` 内 ⇒ 不进包 | 待讨论 | 归批（**B15 仓库卫生**；处置三选一 = 删 / 归位 `tests/` / 归档） |
| T36 | **【立场】发行包内的 43 档不搬**：`build.files` 白名单内的 43 条（`main.js` + `shell-*` 16 + `pet-*` 6 + `market-*` 4 + `exchange-*` 3 + `update-*`/`updater` 5 + `plugins.json` + `build/*` 3 + `harness-store.js` / `update-lib.js`）**留在根目录不动**：搬迁 = 连带改 43 条白名单 + 全部相对 require + 预载/渲染页路径 + `__dirname` 假设，**收益仅观感、风险为发版面** ⇒ 本仓**接受**「根目录平铺」这一形态（与 T34 的“只搬不进包面”互为口径） | `docs/design/REPO-CONVENTIONS.md` / `package.json` `build.files` | 实测：`build.files[0..41]` 逐条为**扁平路径**（非目录通配）；require 图：`main.js` 为枢纽（16 档 `shell-*`） | 待讨论 | **认账不排期**（除非将来出现非搬不可的硬理由） |
| T37 | **组装面机检缺失（B20 实机发现，2026-09-18）**：域模块的 `init(deps)` 接线**无任何机检面**——B20 实测：`main.js:75-88` 的 11 个模块逐一有 `init({...})`，**唯独 `physics` 漏调** ✗ ⇒ 模块收不到 5 个依赖 ⇒ 全链路死，而**批内 AC1–AC20 全绿** ✗（AC 集覆盖了「托盘有该项 / 设置落盘 / IPC 监听在场」，未覆盖「组合根是否真的调了它」）。修法方向 = 机检句「`main.js` 里每个被 require 的域模块**各有一处且仅一处 `init(` 调用**」 | `docs/CONVENTIONS.md`（开发期机检节）/ `main.js` 组装面 | B20 §4.1 D-1（主 agent 三段实证：接线区缺调用 · `userData/pet-physics.log` 不存在 · `settings.json` 开关已为 true）；`main.js:75-88` | 待讨论 | 归批（**B16 测试与门禁**——与 T26 同批；亦属 B25 架构文档的「进程/模块模型」面）；**2026-09-18 扩展（同日第二次实机故障暴露）**：B16 附A 的 D③ 只查 `<名>.init(` ✗ ⇒ `shell-ipc.js` 走 `register()` 被列为**免检档** ✗ ⇒ 与 B20 同类的「**注册函数未被组合根调用**」在新门禁下**仍会隐身** ✗✗ ⇒ **判据扩充**：每个导出注册面（`init` / `register` / 同类）的模块，在组合根**各有一处且仅一处调用**（免检档必须**写明为什么安全**，不得默默免检 ✗）；**具体证据**：`shell-ipc.js:15` 的 `register()` 内含 `:17` 拖拽绑定与 `:26/27` 物理绑定，实机仅前者生效 ⇒ 该档曾被列为“免检”是隐患源 |
| T38 | **【结构债】组合根热点：`main.js` 被每个功能触碰 ⇒ 并行批次必然串行**（用户 2026-09-18 观察「任务不能划分模块时异步并不快」）：实测 B19 vs B20 的**共用写域 5 档**（`main.js` / `shell-pet.js` / `shell-settings.js` / `shell-tray.js` / `package.json`）⇒ 调度器只能串行，且**修偏被迫排在无关批次之后** ✗；候选方向 = ① 域模块自注册（新功能不碰 `main.js`）② 一张 wiring 表集中声明 + T37 的机检句 ③ 本仓**认账**（接受串行，只在排期上把「碰热点档的批」归为一组） | `docs/design/SHELL-UX.md` §2.2（15 模块拆分面）/ B25 架构文档（模块与依赖面） | 实测：B20 与 B19 共用写域 5 档；调度回报 `waiting for: eng-coder#12（域冲突 shell-pet.js）` 与 `#3 waiting for: #2（域冲突 main.js）` | 待讨论 | 归批（**B25 架构文档立档后评估**，或随 B17 代码优化串） |
| T39 | **结构判据三条（「高内聚低耦合」的可机检化）**（用户 2026-09-18 原则 + 主 agent 实测）：① **依赖无环** —— 自研档 require 图必须为 DAG；② **装配面唯一 + 域模块 fan-out ≤ 3** —— 超出者走 `init(deps)` 注入而非 `require`；③ **接线点唯一** —— 每个域模块在装配面各有一处且仅一处 `init(` 调用（= T37）。三条均**可静态机检** ⇒ 宜入 CI 门禁 | `docs/CONVENTIONS.md`（结构原则节，待落）/ `main.js` 装配面 / `docs/design/SHELL-UX.md` §2.2 | **实测（2026-09-18）**：① 依赖图 **无环 ✓ 现状通过**（基线守住）；② **现状违规（6 档）**：`shell-tray.js` fan-out **10** ✗ · `shell-ipc.js` **6** ✗ · `shell-window.js` **5** ✗ · `shell-pet.js` **5** ✗ · `shell-update.js` **5** ✗ · `shell-market.js` **4** ✗（域间 require 共 49 边；**2026-09-18 B16 设计期复测 = 6 档**，主 agent 初测漏 `shell-update.js`）；③ = T37 的实测缺口 | 待讨论 | 归批（**B16 测试与门禁**——与 T26 / T37 同批；**不在任何在飞批次里夹带重构** ✗） |
| T40 | **任务完成提醒在 app 启动后误报（2026-09-18 用户实机，主 agent 三段实证）**：重启 app（12:00:51）⇒ **后端自身启动写入** DSH_HOME（`storages/session_projcache/sessions/session-f098….json` + `sessions/…/session.v3.jsonl.zstd` + `storages/workspace.json`，实测 mtime **12:01:24**）⇒ 提醒模块把「启动写入 + 静默 8 s」判成任务完成 ⇒ 误报一句 ✗。**修法** = 把「本进程启动 / 后端自身启动窗口内的写入」排除在忙信号外（或记启动前 `lastBusyAt` 基线）；否则**每次重启 app 都会误报一次**。同族：**R10**（提醒时机与真实完成脱钩）· **R12**（「等待确认」另报）。**另**：台账 R10 行所述阈值「30 s」与实现不符（`main.js:53` `IDLE_NOTIFY_MS = 8 * 1000`）⇒ 文字同步（归 B14） | `docs/requirements/SHELL.md` §三（R10 / US-11 面）/ `docs/design/SHELL-UX.md` §2.2.12（判定规则） | 实测：`settings.json` `notifyOnComplete=true`；`~/.dsh` 最新写入 12:01:24 = 启动产生；`shell-notify.js:277` 阈值判据 + `:164`/`:171` 门回退；`main.js:53` 阈值 8 s | 待讨论 | 归批（**R10 / R12 同批**——B09 后续轮；或并入 B14 文字同步轮） |
| T41 | **`dsh-bundle` 缺 `@deepseek-ai/cordis-plugin-group`（2026-09-18 实测，非本批引入）**：app 启动时 `bigfish.log` 稳定出现 **2 行** `Error: Cannot find package '…/dsh-bundle/node_modules/@deepseek-ai/cordis-plugin-group/index.js'` ✗；后端**仍能起**（`backend web url captured` ✓）⇒ 疑为非致命插件载入失败。待查：该包是否应随 bundle 提供（`dsh-bundle/package.json` 依赖面）· 是否与 harness 版本相关（现测 current **0.1.5-rc.1** / 可用 **0.1.5-rc.2**）· 影响面（哪个插件/功能不可用）。**另**：本行顺带订正 **T10** 的过期前提（其行述「0.1.0-rc.6 vs 0.1.5-rc.1」已不成立）⇒ 归 B14 同步 | `docs/design/AUTO-UPDATE.md`（bundle 钉版面）/ `dsh-bundle/package.json` / `AGENTS.md` §三 | 实测：`%APPDATA%\Bigfish\bigfish.log`（2026-09-18 13:49 启动轮）两条 Error 行；`updater.log` 同轮 `update check type=harness result=update-available latest=0.1.5-rc.2 current=0.1.5-rc.1` | 待讨论 | 归批（**待定**——优先跟随下次动 bundle / harness 面的批次；或并入 B14） |
| T42 | **【静默失效家族·第 3 例】 IPC 处理器面缺两重保障（2026-09-18 B20 实机 D-2，主 agent 亲验）**：`shell-pet-physics.js` 的 `handleTrailEnd(reasonRaw)` 为**单参签名** ✗，而 `ipcMain` 派发形态 = `(event, reason)` ⇒ 把 IpcMainEvent 对象绑进 reasonRaw ⇒ 归一化正则恒假 ⇒ **静默清轨迹早退（零日志零异常）** ✗ ⇒ 用户看到的只是「毫无反应」✗，外部推断费了两轮。反例对照：同通道冻结面 `shell-pet-drag.js:176` 的 `drag.handlePetDragEnd(_e, reason)` 双参 ⇒ 一直正常 ✓。**两条判据方向**（可机检部分入 B16 门禁）：① **处理器签名一致性** —— 同一 IPC 通道的处理器签名形态一致（多参通道必须声明 event 位；`ipcMain.on` 监听器首个形参恒为 event）；② **早退分支必留取证行** —— 每个处理器在 debug 开关下至少产一行（本批已落地：`phys-trail-start` + `phys-stop reason=bad-reason`）；**家族同源**：T37（组装面漏接线）· 本条的「异常/早退被吞进 stderr ⇒ 日志面一片安静」为同一识别难点 | `docs/CONVENTIONS.md`（IPC 处理器面判据，待落）/ `docs/design/PET-MOVEMENT.md` §2.2.11（事件词表，本次已同步）/ `shell-ipc.js` 各绑定 | 实测（主 agent 亲验）：`shell-pet-physics.js:99` 修前单参 / 修后双参 ✓；离屏复现装置 `.thincoder/b20-repro-trail.cjs` 修复前 `log-lines 0` → 修复后全链（`phys-trail-start → phys-arm → phys-land → phys-rest`）✓；桩测 112/112 · 静态门 64/64 · 冻结面零 diff ✓ | 待讨论 | 归批（**B16 测试与门禁**——与 T26 / T37 / T39 同批的追加判据面；**不得在任何在飞批次里夹带重构** ✗） |
| T43 | **【结构债·文档面】`docs/design/PET-ANIMATION.md` 多批共用 ⇒ 设计链被迫串行**（2026-09-18 实测）：**B18 / B19 / B21 / B23 / B26 五个批次**的设计面全落此一档 ✗ ⇒ 调度器必然串行（实测：`eng-designer#17（B24）waiting for #16（域冲突 docs/requirements/pet.md）`、B21 的 §2.13 被 B26 评审冻结窗口挡住 ✗）；与 **T38**（组合根热点 · 代码面）同族 —— 只是搬到了文档层。**已用的缓解** = ① 分批排队（自动 ✓）② 冻结窗口内让写者先做别的档（#13 实测 ✓）③ 新主题开新档（B24 已按此 ✓）。**候选方向** = ① 一主题一档（把 B21 的面改落新档 ✗ —— 与「链机制须同档对照」相冲 ⇒ 需设计者判）② 档内**分段冻结**（评审只冻被审节 ✗ ⇒ 需工具支持 ✗）③ **认账**（多批共档时接受排期串行，与 T38 同口径 ✓） | `docs/design/PET-ANIMATION.md`（档体量与主题边界）/ `docs/README.md`（地图 · 主题归属）/ B25 架构文档（文档面关系） | 实测：今日 3 次撞档（#13 停令 / #15 排队 / #17 排队）；`PET-ANIMATION.md` 现 **1007 行** ✗ | 待讨论 | 归批（**B25 架构文档**——与「文档面关系」同批评估；或随 T38 的认账口径一并裁） |

---

## 三、待处置（无触发，待人工裁决）

（当前无条目）
