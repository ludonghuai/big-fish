# Bigfish 需求池 / 技术待办（docs/TODO.md）

> 本档是本仓的**需求池**与**技术待办**台账。一条一行，不展开任务细节。
> - 需求池条目（用户需求点）挂「需求档节 + 批次档 §2」；技术待办挂「归属档节 + 最小证据行（file:line + 症状）」。
> - `status` 只取六态：**待讨论 / 待设计 / 在途 / 待核销**（活文件只留未决四态）；**已核销 / 已废弃**为归档态，勾销后逐条移入 `docs/TODO-archive.md`。
> - 技术待办每条带一种触发：`触发=归批（<批名>）` / `触发=条件（<条件句>）` / `触发=认账不排期`；无触发者进「待处置」清单，行龄超 30 天标「老化」。
> - **行宽约定（项目声明，2026-09-17 用户裁定 A）**：本档（台账）与 `docs/README.md`（文档地图）的**表格行不受「无 >300 字符单行」约束**——「一条一行、指针化、不展开细节」是本档的根本形态；该行宽约束适用于需求档 / 设计档 / 批次档等叙述性文档。

---

## 一、需求池（用户需求点）

| # | 条目 | 需求档节 | 批次档 §2 | status | 触发 |
|---|---|---|---|---|---|
| R2 | 自动更新：App 本体（含内置技能/插件随包更新）+ Harness（npm latest）+ 已装插件（注册表版本对比）；更新源统一迁 Gitee | `docs/requirements/UPDATE.md` §一 / §三 US-1…US-9 | `docs/batches/B02-auto-update.md` §2 + `docs/batches/B04-harness-activate-fix.md` §2 | 待核销 | 归批（B02 实施 + B04 AC9 修复轮）——真机复测 AC9 通过（B04 §6.2）；剩 AC5/AC10/AC11/AC12 为发布门项（见 T8） |
| R4 | 桌宠多屏几何（多屏丢失 / 跨屏尺寸与抓取点 / 落点校正） | `docs/requirements/PET.md` §三 US-9…US-14 / §四 NFR-5…NFR-8 | `docs/batches/B03-pet-multimonitor.md` §2 | 在途 | 归批（B03 实机验收——13 轮修正均机检结；**T9 已按用户裁定关闭（2026-09-19）**，余项 = 混合 DPI 三屏实机（TC-1…TC-9）） |
| R11 | **接手项目规范化**：「把仓库风格统一成工程模式（Thincoder）风格」——① 规范载体（`AGENTS.md` / 代码规范档 / `.editorconfig` / lint-format 取舍 / `CHANGELOG.md` 起步）、② 文档补齐（探针脚本与杂项未入档、用户文档定位、地图入口）、③ 代码体检与优化改进空间（按证据排序逐项走全流程）。**本项属工程/流程类需求（非产品板块需求）**——落点由 B11 设计裁定 | `AGENTS.md`（B11 产出；本仓现无项目规范档——B03 评审当时即声明该局限） | `docs/batches/B11-conventions.md` §2（① 规范载体，已落）+ `docs/batches/B14-doc-facts.md` / `B15-repo-hygiene.md` / `B17-code-optimization.md` 各批批次档 §2（② ③ 分批；**B14 / B15 / B17 已立案 2026-09-19**） | 在途 | 归批（**① 规范载体已收口（2026-09-17）**：B11 轮 1 pass → 修正轮 1–4 → 独立复核 pass → 清理轮；**AC1–AC6 机检全绿**（核销证据见 `docs/batches/B11-conventions.md` §6）；**②③ 待 B14 / B15 / B16 / B17**；序 = B11 规范基建 → B14 文档事实收口 → B15 仓库卫生 → B16 测试与门禁 → B17+ 代码优化串）· **2026-09-19：③（测试与门禁面）已收口**（B16 §6 核销；T4 / T26 / T37 / T39 归档；余存量 fanout 消解转 **T45**） |
| R13 | **跨平台可用性（win / linux / mac）**：本仓需在 Windows 之外**建立 linux（及 mac）的可使用性**——构建面已有目标（`electron-builder --win/--mac/--linux` = nsis / dmg / AppImage+deb，`package.json` scripts 齐备），但**可用性未建立**：发布 / 更新通道为 **win32 单平台**（`latest.json`）、无「安装 → 启动 → 托盘 / 桌宠 / 通知 / 更新」的跨平台实测面 | （待落——归批时由 eng-designer 落） | （待立批：候选 = **并入 B13 打包链与发布面**（同域）或独立批） | 待讨论 | 归批（**B32 已立案 2026-09-19**——用户 2026-09-17 提出；**平台集合已裁定（用户 2026-09-17）= A**：win + linux 先做透（Linux 验 AppImage / deb 实跑 + 沙箱 / 托盘 / 通知 / 路径差异），**mac 后置**（需 Apple 开发者证书做签名 / 公证；无证书则首启需手动放行）） |
| R15 | **外部项目能力移植（源 = `samples/dsh-pet`，PC2005-cloud，v0.2.11，MIT 代码）**：候选面 = ① **动画链引擎**（权重链 + 双缓冲交叉淡入 + 事件档位 + 档内随机轮换）② **物理手感**（阻尼弹簧拖拽 / 甩抛抛物线 / 边缘反弹 / 落地摩擦 / Q 弹挤压）③ **会话事件联动**（工作状态六档 / 余额档位 + 气泡 / 碎碎念 / 对话 + 记忆持久化）④ **多实例与 pet pack**（多宠物、新种类）⑤ **素材生成链**（提示词 → 绿幕源视频 → 抠像 → 归一化 → 转码）⑥ **无障碍与平台兼容**（`prefers-reduced-motion` / headless Linux 检测 / Safari mov 回退）。**不做** = 其 DSH 插件架构 / cordis+React+tsdown 工具链 / Electron 运行时下载（本仓是**宿主应用**、非插件；B11 规范亦不引入 lint/构建大改）。**许可硬约束**：其**代码 MIT**（可适配，但**必须署名** —— 落 `THIRD-PARTY-NOTICES.md`，已在 `build.files` 清单内）；其**动画素材 / 提示词 / 源视频「允许开源使用、禁止商用」** ⇒ **用户 2026-09-17 裁定「本仓不商用」⇒ 素材可用于本仓**（须署名 `THIRD-PARTY-NOTICES.md` + 发布面，且**带商用化消解路径**：转商用 ⇒ 换自产资产或取得授权）；**体积无上界**（同日裁定「素材无预算」） | `docs/requirements/PET.md` §三 US-15…US-19 / §四 NFR-9…NFR-13（B18 段已落；其余面待立批时落） | `docs/batches/B18-pet-animation-chain.md` §1 / §2（A 面已立案；B / D1 / D2 / C / E 各面待立批） | 在途 | 归批（**A 面已收口（2026-09-17）**：B18 评审 pass → 实施（桩测 **121/121** · **O5 探针 pass** · 几何零 diff）→ 收口轮 → **§6 核销**；序 = **A 动画链 → B 物理（+ O10 位移接管合并）→ D1 工作状态 → D2 余额/碎碎念/对话 → C 素材链 → E 多实例**）；**用户 2026-09-18 裁定（B20 面）**：① **物理参数取 B**——`restitution` 0.78 → **≈0.55**（玩偶感：弹 1–2 下、典型抛掷 ≈3.9 s 停；用户原话「先做成 B 吧，不行再调整手感」）⇒ U-2 由「照搬样本」改为「**本仓调**」；**须同步**设计档参数表 + §2.2.12/AC11/NFR-19 的静止时长判据（口径随参数变）+ 与样本差异登记；② **ffmpeg 取法照 `node-runtime/` 先例**（官方入口，落 `tools/`，不入库、不进包）。**两项均在 B20 设计解冻后落档（口径变化 ⇒ 需复评）**。**用户 2026-09-17 四项裁定**：① **不商用** ⇒ 素材可用（见左）；② **素材无预算**（不设体积上界）；③ **LLM 走用户自己的 API**（项目零成本）；④ **O10 位移接管 = 与 B 面合并**（立批时先定位移仲裁层 + 重跑 B03 判据，依据见 `docs/batches/B18-pet-animation-chain.md` §1.9） |
| R16 | **动作解锁规则（本仓原创需求，非移植面）**：桌宠动作池随**时间**与**陪伴**生长——① **时节门**：系统日期落在「节前 7 天 → 节后 3 天」窗口内才解锁该时节动作（**其余时间锁死**；农历节日需**建表**）② **饭点门**：三餐窗口内解锁「吃早餐/午餐/晚餐」，一天一次、过点不补 ③ **等级门**：好感度等级解锁动作（**越丰富越高等级**，10 级对 10 档）。**+ 动作图鉴**（2026-09-18 用户增补：与喂食**同弹窗的第二标签卡**——列 动作 / 解锁条件 / **解锁百分比**；原「另批」改为本批一并做）；**不做**：抽卡/成就、网络校时 | （已落——`docs/requirements/PET.md` US-38…US-43 / NFR-27…NFR-29；设计档 `docs/design/PET-UNLOCK.md`） | `docs/batches/B23-pet-action-unlock.md` §1（立案含勘察 + 硬约束九条 + 用户已裁定 D-1…D-3 含 10 级映射表） | 待核销 | 归批（**B23 已实施** 2026-09-20——三条门禁全绿 + 桩测 106/106 + §6 验收核销；**待用户实机目视** ⇒ 目视通过后转已核销归档） |
| R17 | **工作状态六档（事件桥）**：样本六档中的「等待批准 / 回合成功 / 回合失败」在本仓只读落盘面**不可观测**（B19 段实测：事件名存于 `dsh-agent-presets` 的 `SessionEventMap`，`dsh-user-approval` 明写 log-only ⇒ 不进投影行）⇒ 用户 2026-09-18 裁定「做6种」⇒ 需建**事件桥**（后端插件订阅 `turn/end` / `tool/call` / `approval` / `asked` 并落盘可读面）；B19 已预留扩展面（池段可增 3 键 + 判定门可扩档 ⇒ **增量补齐、不返工**；素材已在仓未引用） | （待落——归批时由 eng-designer 落） | （**B22 已立案**（2026-09-19）：先技术预研后端插件 / ACP 订阅面） | 待讨论 | 归批（**B22 已立案 2026-09-19**；序 = 设计先行、实施排 B27 之后） |
| R18 | **动作生产 / 导入（台账原为「用户面」；2026-09-18 用户裁定改向）**：① **现形态（本批 = B24）= 维护者工具链、不进 app**——用户指定源视频 ⇒ 一条命令跑通 抠像 → 归一化 → VP9-alpha 编码 → alpha 校验 → 入 `assets/pet-anim/webm/` + 写 `pool.json`（工具链不进发行包；ffmpeg 按 `node-runtime/` 先例取，不入库）；② **后置 = 用户面导入**（app 内入口 + 自动解析成动作；**「管理员入口」不可行**——桌面应用里隐藏 ≠ 限制，用户拥有本机程序）。原话「给用户一个入口添加视频之后，是不是可以解析成动作呀？」+「…只有我才可以添加动作，**或者就不坐在 app 里面，我指定哪个视频直接让你解析**」 | （待落——归批时由 eng-designer 落） | `docs/batches/B24-pet-import-action.md` §1（含 **§1.8 范围改向裁定** 与待决 U-1…U-6 备查） | 在途 | 归批（**B24 已立案，范围已改向**；设计待派） |
| R24 | **右键弹窗重做 + 动作图鉴卡片化（用户 2026-09-20 提出）**：「右键喂养与动作图签做的太烂——弹窗大小调不了、布局没有精心设计；图鉴做高级一点；加用户屏蔽和喜欢（设定喜欢的动作组合 / 屏蔽某动作，都在图鉴里完成）；总览要好看，每个动作放最好看最有代表性的图做成精美标签卡，让玩家有收集卡片的感觉；加 10 级特权——10 级后可直接解锁节日吃饭等动作，无视时间限制、自由组合」。四面 = ① **窗口形态**：现 320×500 固定（`shell-affinity.js` 兑换窗 `resizable:false`）⇒ 可调整大小 + 喂养/图鉴两面布局重做 ② **图鉴升级视频卡片墙**：每动作一张卡（播对应 webm + 名称 + 解锁条件 + 状态徽标 + 喜欢/屏蔽钮）③ **喜欢/屏蔽体系**：喜欢 = 星标 + 权重提升 + 置顶 + 「只看喜欢」开关；屏蔽 = 全屏蔽（随机池 + 事件触发均排除；独占事件唯一段不可屏蔽）④ **Lv.10 特权**：满级后时节/饭点时间窗与「一天一次」全部解除 | （待落——归批时由 eng-designer 落 `docs/requirements/PET.md` §三 / §四） | `docs/batches/B35-pet-gallery-ui.md` §1（已立案：勘察 + 硬约束 + 用户裁定 D-1…D-4 + 待决 U 表） | 在途 | 归批（**B35 已立案 2026-09-20**；需求四条裁定已收（视频卡 / fav-full / block-all / lv10-all）；设计待派——评审发起权在用户） |

## 二、技术待办

| # | 条目 | 归属档节 | 最小证据（file:line + 症状） | status | 触发 |
|---|---|---|---|---|---|
| T5 | 发版流程说明缺 `make-latest` 步骤——手工改 `latest.json` 会留空 sha256，客户端 fail-closed 拒装新版本 | `docs/design/AUTO-UPDATE.md` §2.2.8 | `latest.json:11-13` —— sha256 三平台为空串；`package.json` scripts 已有 `make-latest` | 待讨论 | 条件（下次 App 发版前） |
| T7 | 打包前置 `node-runtime/` 已落档 + 本机备齐：README §打包 增补三步准备法（Node 22 zip 取 `node.exe`；`pnpm@10` 取 `dist/pnpm.cjs` + 一行 ESM 壳 `pnpm.mjs`；npmmirror 镜像环境变量）；本机 `node-runtime` = Node v22.20.0 + pnpm 10.34.5（2026-09-16），`dist:win` 产出 `Bigfish.Setup.0.0.1.exe`（157.2MB）冒烟通过（内置 node 拉起 dsh 后端、内置 pnpm 跑通 harness 自更新安装）；`node-runtime` 维持不入库（平台二进制） | `README.md` §打包 | `node-runtime/node.exe` + `node-runtime/pnpm/{pnpm.mjs,pnpm.cjs}` 实测可用；`dist/win-unpacked` 冒烟：`node-runtime\node.exe …\dsh\lib\bin.js --profile web` 存活 | 待核销 | 条件（用户复核 README 步骤 / 下次换机打包复现） |
| T8 | 首次真实发版演练清单：`make-latest.js` 正例（AC11）· 真实安装+自动拉起（AC5）· 市场更新徽标（AC10）· 更新前后 `~/.dsh` 关键面快照（AC12） | `docs/design/AUTO-UPDATE.md` §2.2.8 / §3.1 | `docs/batches/B04-harness-activate-fix.md` §6.3 —— 四项均为发布门项，本地构建不可闭环；⚠️ 禁用本地构建产物跑 `make-latest.js` 覆盖 `latest.json`（sha256 会与已发布实附件不符 → 用户拒装） | 待讨论 | 条件（首次真实发版时） |
| T10 | harness bundle 落后 5 个 rc（`0.1.0-rc.6` vs 注册表最新 `0.1.5-rc.1`，2026-09-10 发布）。**原条目后半段「dev 的 `dshBinPath()` 固定走 bundle ⇒ harness 自更新在 dev 不生效」已被 B06 F5 覆盖**——现 `shell-backend.js` 活跃指针优先、dev 与打包同口径，且已实测生效（`updater.log` 2026-09-17：dev 侧完成 0.1.0-rc.6 → 0.1.5-rc.1 安装 + 激活 ok=1） | `docs/design/AUTO-UPDATE.md` §2.2.1（路径口径） | `dsh-bundle/package.json:7`（**精确钉 `0.1.5-rc.1`**，B08 于 2026-09-17 刷新——旧值 `0.1.0-rc.6`）；`shell-backend.js:52`（活跃指针优先）；registry 实查 = `0.1.5-rc.1` | 待核销 | 归批（**B08 实施已交付**：钉版 `0.1.5-rc.1` 与锁三值一致（主 agent 亲读）· `bundle:refresh` / `bundle:check` 两入口在场 · `--check` 主 agent 亲跑 `action=none`（与注册表 latest 同步）；**§6 已核销**；归档条件 = 打包态双态冒烟 / `npm ci` 全量复现由用户定是否补跑）；**2026-09-18 用户裁定「刷 bundle」（实跑 `npm run bundle:refresh`）**：`pin=0.1.5-rc.1 → 0.1.5-rc.2`（tag=latest、registry=npmmirror）· 冒烟过 ✓ · `bundle:check` = `action=none` ✓ · 变更面 = `dsh-bundle/package.json` + `package-lock.json`；**动因** = 用户「离线版应=最新版，没有新版就不该提示」（实测启动弹 `update-available latest=rc.2 current=rc.1`）；**残余** = 运行时那份（`~/.dsh` 的 active harness）仍为 rc.1 ⇒ 如仍提示，点一次它自己的更新即归位（归 T41 同面） |
| T13 | **行宽债清理（非豁免面实测 65 行——修正原登记的 3 行）**：明细 = `B03-pet-multimonitor.md` **40**（§2 3 / §5 37）· `PET-MULTIMONITOR.md` **18** · `AUTO-UPDATE.md` **3**（`:57` 366 / `:829` 309 / `:1034` 321（as-of 2026-09-17 B08 修正轮 1 后；行号随文档增删**持续漂移**——历史 `:53/:698/:853` → `:796/:993` → 现值，**以 `>300` 计数为准、行号只作 as-of 参考**））· `B01` §2 1 · `B05` §6 1 · `B06` §5 1 · `PET.md:214` 1（321，原未登记）；**豁免面**另 56 行（批次档 §3 34 / 台账 16 / 地图 6）不动。①②③（跨档一致性）已由 B07 收口 | 见上各档 | 主 agent 2026-09-17 全仓实测（分段归属 + 不含 CR 口径）；与 B11 设计者独立计数一致（65） | 待讨论 | 条件（**消解路径**：**活文档 22 行**（`PET-MULTIMONITOR` 18 + `AUTO-UPDATE` 3 + `PET.md` 1）随 **B14** 修正；**批次档历史段 43 行**随该档下次被合法动笔时一并压行——父侧不代笔改写他人段；用户若要求立即清，另批授权）· **2026-09-18 B16 设计期复测（口径 = B16 附 A 的 A-check 扫描面）**：非豁免 **92 行 / 16 档** · 豁免面另计（**批次档 §3 = 121 行 · 表格行 = 45 行**）—— **与上行 65 / 56 的差异源于扫描面与豁免面口径不同**（本行为 B07 期口径）⇒ 两值并存，**以后续 B16 落地的机检基线口径为准** |
| T21 | B12 设计期发现的**跨档前置条件**：`AUTO-UPDATE.md` §2.2.5 / §3.1 AC10 与 `UPDATE.md` US-7 的「更新判定面」在本批后新增了前置（插件标识入参门）——两档均未记该前置（写权不在 B12 设计者） | `docs/design/AUTO-UPDATE.md` §2.2.5 / §3.1；`docs/requirements/UPDATE.md` US-7 | B12 设计档 O21（`docs/design/SHELL-UX.md` §2.2.14 与 §3.1 AC26–AC31 的描述面） | 待讨论 | 归批（**B14 文档事实收口**——**已立案 2026-09-19**；与其余跨档不一致同轮处理） |
| T16 | 同源面一致性（设计档 §2.2.6 注 S1 ↔ `shell-backend.js` 头注释 ↔ 实测）：① 设计档函数行号 as-of 漂移（记 `:165`/`:105`，实测 `:197`/`:97`）；② 头注释 `dshHome` 序在 `getCurrentDshVersion` 前、而实测相反（`:286` vs `:278`）；③ `getPort` / `init` 枚举口径两档不同形；④ B07 §5.2 三处行号残差（`makeTee` 记 `:237-247` 实测 `:240-248`；A2 记 `:243` 实测 `:245-246`；not captured 记 `:254` 实测 `:257`） | `docs/design/SHELL-UX.md` §2.2.6（注 S1）；`docs/batches/B07-harness-auth-compat.md` §5.2 | 上列四项均已由 B07 收尾轮实测定位（eng-coder 报告 + 主 agent 亲扫） | 待讨论 | 归批（**B14 已立案 2026-09-19**（原挂 B08 已收口 ⇒ 改挂）；或与下次动 `shell-backend.js` / §2.2.6 的轮次同轮；四项均属「同一清单的多面同形」类，一次对齐可全清） |
| T24 | 仓库内固化 EOL 口径：新增 `.gitattributes`（`* text=auto eol=lf`）——现口径只依赖本机 `core.autocrlf=true` | `docs/CONVENTIONS.md` §六（已知局限） | 全仓无 `.gitattributes`；`git config core.autocrlf` = true（本机） | 待讨论 | 归批（**B15 已立案 2026-09-19**）/ 用户裁定 |
| T25 | 前世代 / 相对指针**文件头迁移**：**20 档** = 17 档无设计档指针 + 3 档相对指针（`update.js` · `make-latest.js` · `market-update.js`）；迁到 `docs/CONVENTIONS.md` §一 标准形 | `docs/CONVENTIONS.md` §1.1 | 逐名清单见 `docs/CONVENTIONS.md` §1.1（口径 = `git ls-files` 42 档三分；引用时以当次输出为准） | 待讨论 | 归批（**B17 已立案 2026-09-19**） |
| T28 | `docs/design/SHELL-UX.md` **§2.2.6 依赖表的「注入面」列与实现不符**：该行把 `petSay` / `setPetState` 写作**顶层注入键**，实测（B10 修正轮 3）= **`pet` 对象的成员**——`init(deps)` 实键 = `getPetWindow` / `pet` / `setQuitting` / `APP_NAME`（与 `main.js:66` 同形）；载具或后续实现者照字面写会得到 `pet === undefined` ⇒ `petSay` 调用抛错 | `docs/design/SHELL-UX.md` §2.2.6 该行（B06 面措辞） | `main.js:66`（`init` 实参形态）· `shell-affinity.js:18-23`（读 `deps.pet`）· `:262`（导出 `handleAffinityView`，内部转发 `affinityView()`） | 待讨论 | 归批（**B14 文档事实收口**——**已立案 2026-09-19** / 随下次动 §2.2.6 的批次） |
| T29 | **B03 dev 桩测工具的抽取源漂移**：`.thincoder/b03-pet-calibrate-stub.mjs:84` 硬绑 `main.js`，而几何 / 拖拽 / 散步实现已由 **B06 F6 拆分**迁出（`main.js:35-37` 只余 `require`）⇒ 现跑即抛 `marker not found: const PET_SIZE_DIP = {`（`:96` / `:118`），**0 条断言执行**（B03 的 189/189 为当时历史证据，as-of）；后果：任何「以该桩为几何零回退门禁」的判据**不可复现**（B18 §1.4-2 原判据已据此重写） | 该桩自身（`.thincoder/`，受 `.gitignore` 覆盖、不入仓） | `.thincoder/b03-pet-calibrate-stub.mjs:84` / `:96` / `:118`（**该 dev 桩 as-of 2026-09-19 已不在盘**——`.thincoder/` 为不入仓 dev 目录；结论按当时实测保留） · `main.js:35-37` · `docs/batches/B03-pet-multimonitor.md:888` / `:915`（189/189 历史）/ `:1054`（路径与跑法） | 待讨论 | 归批（**需用时先修**：改绑 `shell-pet-geometry.js` / `shell-pet-drag.js` / `shell-pet.js`，纯 dev 工具；当前由 **B18 §1.4-2 ① 的仓级判据（几何面零 diff）** 承担该回归面） |
| T31 | **跳档「行锚 / 计数」漂移统一收口（承 B08 实施）**：`package.json` 因 B08 **+2 行**（scripts 10 → **12 项**）⇒ 多处跳档的 `package.json:NN` 行锚与计数陈述过期（B08 写域内 9 处已修，**写域外仍存**）：`docs/design/PET-ANIMATION.md` 5 处 · `docs/design/PET-MULTIMONITOR.md` 2 处 · `docs/design/SHELL-UX.md` 3 处 · `docs/requirements/PET.md:259` · `docs/batches/B02:190` / `:295` · `docs/batches/B03:340` · `docs/batches/B18:169` / `:190`；**另** `AGENTS.md:39` 的 `build.yml（75 行）` 实测 **74**（末行空行口径） | 各归属档节（逐处引现行值 + as-of 注） | B08 收口轮自查表（批次档 B08 §2.11）；实测：`build.yml` **74** 行 · `package.json` scripts **12** 项 | 待讨论 | 归批（**B14 文档事实收口**——**已立案 2026-09-19**；本项即该批的典型面） |
| T33 | **B09 判定门与 B19 工作状态读面「同读一族落盘物、谓词重复」**（B19 设计期发现 O16）：两处各自读 `<DSH_HOME>/storages/session_projcache/sessions/*.json` 的 per-record 单元并各自维护同族谓词（新鲜度 / 回合边界）⇒ 建议抽**共享只读模块**（两处同时改，避免各自漂移） | `docs/design/PET-ANIMATION.md` §2.6.4 / **O16**；`docs/design/SHELL-UX.md` §2.2.12 | B19 设计报告 ⑤ / 设计档 O16（建议行，主 agent 登记）；实测两处读面：`shell-notify.js` 的 `completionGate()` 与 B19 新增读面 | 待讨论 | 归批（**B14 已立案 2026-09-19**（文档面：两处互指注）；**抽共享模块 = 代码面归 B17**） |
| T34 | **根目录散档归位（结构债；2026-09-18 用户裁定 A“结构调整”的实测面）**：根目录自研档**平铺 49 档**，其中**不进发行包**的 = 8 档 `probe-*`（`probe-displays.js` / `probe-displays2.js` / `probe-pet-media.js` / `probe-position-accuracy.js` / `probe-resizable-setsize.js` / `probe-settle-scan.js` / `probe-size-readback.js` / `probe-straddle-size.js`）+ 5 档构建 / 发布脚本（`afterPack.js` / `make-icons.js` / `make-latest.js` + `scripts/ensure-deps.js` / `scripts/refresh-dsh-bundle.js`）——**2026-09-19 订正**：原记 `download-electron.js` / `download-node.js` 实测工作区 + git 全历史零命中（已不存在；历史引用见 B02/B04 的 as-of 记录）⇒ 建议归位到 `scripts/`（已存在）或 `tools/`；**连带面实测** = ① 各探针内部 `require('./pet.js')` 等的**相对深度** ② ~~`probe-pet-media.js` 硬绑 `main.js`~~ **已订正（B25 §2.5 复核，2026-09-19）：全仓 grep 零命中 `require('./main.js')`** —— 实际耦合 = require `pet-chain-core` / `shell-settings` / `shell-pet-geometry` / `shell-pet-drag` / `pet.js` 五档 + 复刻 `BIGFISH_USER_DATA` 隔离钩子（`probe-pet-media.js:22-28`） ③ 探针的**设计档指针**（R11 ② 点名「探针脚本与杂项未入档」） | `docs/design/REPO-CONVENTIONS.md`（B11 规范面）/ 各探针文件头 | 实测：根目录 49 自研档；`package.json:41-84` `build.files` **零 probe 条目**（不进包 ⇒ 可安全搬迁）；`probe-pet-media.js:1-2` 的 `./pet.js` / `./pet-chain-core.js` 相对引用 | 待讨论 | 归批（**B15 已立案 2026-09-19**——与 T24 同批；**排 B19 / B20 之后**：两批在飞时不动档） |
| T35 | **`测试-更新功能/` 目录清点与处置**（中文名 + 疑似遗留）：根目录实测存在该目录（**仅 1 项**），用途与产出未入档 | 待定（清点后定落点） | 实测：`./测试-更新功能/` 存在（1 项）；不在 `build.files` 内 ⇒ 不进包 | 待讨论 | 归批（**B15 已立案 2026-09-19**；处置三选一 = 删 / 归位 `tests/` / 归档） |
| T36 | **【立场】发行包内的 43 档不搬**：`build.files` 白名单内的 43 条（`main.js` + `shell-*` 16 + `pet-*` 6 + `market-*` 4 + `exchange-*` 3 + `update-*`/`updater` 5 + `plugins.json` + `build/*` 3 + `harness-store.js` / `update-lib.js`）**留在根目录不动**：搬迁 = 连带改 43 条白名单 + 全部相对 require + 预载/渲染页路径 + `__dirname` 假设，**收益仅观感、风险为发版面** ⇒ 本仓**接受**「根目录平铺」这一形态（与 T34 的“只搬不进包面”互为口径） | `docs/design/REPO-CONVENTIONS.md` / `package.json` `build.files` | 实测：`build.files[0..41]` 逐条为**扁平路径**（非目录通配）；require 图：`main.js` 为枢纽（16 档 `shell-*`） | 待讨论 | **认账不排期**（除非将来出现非搬不可的硬理由） |
| T38 | **【结构债】组合根热点：`main.js` 被每个功能触碰 ⇒ 并行批次必然串行**（用户 2026-09-18 观察「任务不能划分模块时异步并不快」）：实测 B19 vs B20 的**共用写域 5 档**（`main.js` / `shell-pet.js` / `shell-settings.js` / `shell-tray.js` / `package.json`）⇒ 调度器只能串行，且**修偏被迫排在无关批次之后** ✗；候选方向 = ① 域模块自注册（新功能不碰 `main.js`）② 一张 wiring 表集中声明 + T37 的机检句 ③ 本仓**认账**（接受串行，只在排期上把「碰热点档的批」归为一组） | `docs/design/SHELL-UX.md` §2.2（15 模块拆分面）/ B25 架构文档（模块与依赖面） | 实测：B20 与 B19 共用写域 5 档；调度回报 `waiting for: eng-coder#12（域冲突 shell-pet.js）` 与 `#3 waiting for: #2（域冲突 main.js）` | 待讨论 | 归批（**B17 已立案 2026-09-19**；B25 架构文档立档后评估） |
| T41 | **`dsh-bundle` 缺 `@deepseek-ai/cordis-plugin-group`（2026-09-18 实测，非本批引入）**：app 启动时 `bigfish.log` 稳定出现 **2 行** `Error: Cannot find package '…/dsh-bundle/node_modules/@deepseek-ai/cordis-plugin-group/index.js'` ✗；后端**仍能起**（`backend web url captured` ✓）⇒ 疑为非致命插件载入失败。待查：该包是否应随 bundle 提供（`dsh-bundle/package.json` 依赖面）· 是否与 harness 版本相关（现测 current **0.1.5-rc.1** / 可用 **0.1.5-rc.2**）· 影响面（哪个插件/功能不可用）。**另**：本行顺带订正 **T10** 的过期前提（其行述「0.1.0-rc.6 vs 0.1.5-rc.1」已不成立）⇒ 归 B14 同步 | `docs/design/AUTO-UPDATE.md`（bundle 钉版面）/ `dsh-bundle/package.json` / `AGENTS.md` §三 | 实测：`%APPDATA%\Bigfish\bigfish.log`（2026-09-18 13:49 启动轮）两条 Error 行；`updater.log` 同轮 `update check type=harness result=update-available latest=0.1.5-rc.2 current=0.1.5-rc.1` | 待讨论 | 归批（**条件：下次动 `dsh-bundle` 面的批次**——与 T10 同族；或并入 B14） |
| T42 | **【静默失效家族·第 3 例】 IPC 处理器面缺两重保障（2026-09-18 B20 实机 D-2，主 agent 亲验）**：`shell-pet-physics.js` 的 `handleTrailEnd(reasonRaw)` 为**单参签名** ✗，而 `ipcMain` 派发形态 = `(event, reason)` ⇒ 把 IpcMainEvent 对象绑进 reasonRaw ⇒ 归一化正则恒假 ⇒ **静默清轨迹早退（零日志零异常）** ✗ ⇒ 用户看到的只是「毫无反应」✗，外部推断费了两轮。反例对照：同通道冻结面 `shell-pet-drag.js:176` 的 `drag.handlePetDragEnd(_e, reason)` 双参 ⇒ 一直正常 ✓。**两条判据方向**（可机检部分入 B16 门禁）：① **处理器签名一致性** —— 同一 IPC 通道的处理器签名形态一致（多参通道必须声明 event 位；`ipcMain.on` 监听器首个形参恒为 event）；② **早退分支必留取证行** —— 每个处理器在 debug 开关下至少产一行（本批已落地：`phys-trail-start` + `phys-stop reason=bad-reason`）；**家族同源**：T37（组装面漏接线）· 本条的「异常/早退被吞进 stderr ⇒ 日志面一片安静」为同一识别难点 | `docs/CONVENTIONS.md`（IPC 处理器面判据，待落）/ `docs/design/PET-MOVEMENT.md` §2.2.11（事件词表，本次已同步）/ `shell-ipc.js` 各绑定 | 实测（主 agent 亲验）：`shell-pet-physics.js:99` 修前单参 / 修后双参 ✓；离屏复现装置 `.thincoder/b20-repro-trail.cjs` 修复前 `log-lines 0` → 修复后全链（`phys-trail-start → phys-arm → phys-land → phys-rest`）✓；桩测 112/112 · 静态门 64/64 · 冻结面零 diff ✓ | 待讨论 | 归批（**B17 已立案 2026-09-19**（原挂 B16 已收口 ⇒ 改挂）；与 T26 / T37 / T39 同批的追加判据面；**不得在任何在飞批次里夹带重构** ✗） |
| T43 | **【结构债·文档面】`docs/design/PET-ANIMATION.md` 多批共用 ⇒ 设计链被迫串行**（2026-09-18 实测）：**B18 / B19 / B21 / B23 / B26 五个批次**的设计面全落此一档 ✗ ⇒ 调度器必然串行（实测：`eng-designer#17（B24）waiting for #16（域冲突 docs/requirements/pet.md）`、B21 的 §2.13 被 B26 评审冻结窗口挡住 ✗）；与 **T38**（组合根热点 · 代码面）同族 —— 只是搬到了文档层。**已用的缓解** = ① 分批排队（自动 ✓）② 冻结窗口内让写者先做别的档（#13 实测 ✓）③ 新主题开新档（B24 已按此 ✓）。**候选方向** = ① 一主题一档（把 B21 的面改落新档 ✗ —— 与「链机制须同档对照」相冲 ⇒ 需设计者判）② 档内**分段冻结**（评审只冻被审节 ✗ ⇒ 需工具支持 ✗）③ **认账**（多批共档时接受排期串行，与 T38 同口径 ✓） | `docs/design/PET-ANIMATION.md`（档体量与主题边界）/ `docs/README.md`（地图 · 主题归属）/ B25 架构文档（文档面关系） | 实测：今日 3 次撞档（#13 停令 / #15 排队 / #17 排队）；`PET-ANIMATION.md` 现 **1007 行** ✗ | 待讨论 | 归批（**B17 已立案 2026-09-19**——与 T38 同族一并裁） |

| T44 | **设计档 `PET-ANIMATION.md` §2.2.3 的「`slot-rotate` 整机形态无消费路径」注与实测相抵**（2026-09-18 主 agent 观察，dev 实例调试日志）：该注称「主进程档位定时器先于段长把档位置回 idle ⇒ 轮换分支不触发（两轮长跑 `reason=slot-rotate` 零行）」；但 **B19 工作档会持续持有槽位**（工作档在途期不断重断言）⇒ 段结束时 `playing.slotKey === slot` 成立 ⇒ **轮换分支可达**（实测：`anim switch … reason=slot-rotate`，2026-09-18 `%APPDATA%\Bigfish\pet-anim.log`）⇒ 该注的「无消费路径」表述已过期（B18 期成立、B19 起不成立） | `docs/design/PET-ANIMATION.md` §2.2.3（`nextInSlot` 可达性注） | 实测行样本：`[2026-09-18T14:12:07.616Z] anim switch anim=深度思考碎碎念 from=工作状态-思考冒泡 … loop=0 reason=slot-rotate` | 待讨论 | 归批（**B14 文档事实收口**——**已立案 2026-09-19**；与其余跨档表述订正同轮） |

| T45 | **fanout 存量 6 档消解（≤3）（2026-09-19 B16 §6 派生）**：B16 把「域模块 fan-out ≤ 3」判据接入门禁，但**存量 6 档违例入基线冻结**（`scripts/gates/baseline.json` 的 `fanout`：`shell-tray.js` 10 · `shell-ipc.js` 6 · `shell-pet.js` 5 · `shell-update.js` 5 · `shell-window.js` 5 · `shell-market.js` 4）✗ —— 存量豁免已带消解期（`expires.fanout` = 「随 B17 代码优化串把 6 档降到 ≤3 ⇒ 全部删除」✓），但 T39 归档后**活文件无跟踪条目** ⇒ 本条目接管跟踪 | `docs/CONVENTIONS.md`（结构原则节）/ `scripts/gates/baseline.json`（`fanout` + `expires.fanout`）/ `main.js` 装配面 | 实测（B16 §5）：`CHECK fanout frozen=6`；六档当前 fan-out = 10/6/5/5/5/4；判据 = 超出者走 `init(deps)` 注入而非 `require` | 待讨论 | 归批（**B17 已立案 2026-09-19**；消解完成条件 = 六档全部 ≤3 ∧ `baseline.json` 的 `fanout` 节删除） |

| T46 | **「已安装」标签页漏列 AI 安装的未收录插件（B28 设计轮 O25，2026-09-19）**：`market.js:455-464` 的「已装未收录」兜底条目 `installSpec` 恒 `undefined` ⇒ 该页**过滤掉** AI 安装的未收录插件 ✗（与 `:452-453` 注释「完整列出并卸载」意图相抵）；修法 = `normalizePlugin` 认**裸名 `install`** 形态——**超 T22 的 `builtin:` 面**（B28 未动） | `docs/design/SHELL-UX.md` §2.2.15（B28 面）/ `market.js:452-464` | B28 设计轮交付报告 O25（设计者亲读）；实测 = 兜底条目 `installSpec === undefined` ⇒ 过滤 | 待讨论 | 归批（**B28 后续轮** / 随下次动 `market.js` 的批次） |
| T47 | **`PET-MULTIMONITOR.md` 的 T9 验证句随用户裁定失效**（用户 2026-09-19 裁定：跨屏上下突跳 = Windows 系统行为、非缺陷 ⇒ T9 关闭归档）——档内 §3.1 S8 / §3.2 TC-31 等句仍列「实机验证」待办 ✗ | `docs/design/PET-MULTIMONITOR.md` §3.1 / §3.2 | 台账 T9 归档行（2026-09-19 T9 → `docs/TODO-archive.md`） | 待讨论 | 归批（**B14 文档事实收口**——与 T21 同轮处理） |
| T50 | **B28 轮残余两处同族小项**（B28 评审轮 1 #5 派生 + O24/C46 收敛后的实际更正面）：① `docs/design/REPO-CONVENTIONS.md` `:436`（C 判据现状行）与 `:572`（B16 面 b12 行）记 `tests/b12-plugin-guards.test.js` = **491**，与 2026-09-19 实测 **492** 不符（B28 轮已把 `SHELL-UX.md` 侧统一为 492；同族面不在 B28 写域）✗；② `docs/batches/B08-dsh-bundle-version.md` §5.6 的历史注记含死指针（`scripts/ensure-deps.js`——历史段，按「不代笔改写」惯例不直接改） | `docs/design/REPO-CONVENTIONS.md` §五（C 判据面）/ `docs/batches/B08-dsh-bundle-version.md` §5.6 | B28 修正轮 1 报告（未落项 #1 / #2）+ B28 设计档 O24 / C46 收敛口径 | 待讨论 | 归批（**B29 收口轮 / B14 同族**）：① 随下次动 `REPO-CONVENTIONS.md` 的轮次；② 历史段补注随 **B14** 处理 |
| T51 | **E② 面边界：顶层 JSON 非对象未 fail-closed（B29 微修轮上浮观察，2026-09-19）**：`package.json` 根为 `[]` / `"str"` 等（`JSON.parse` 成功但整根非对象）⇒ `checkPackageSamples` 现零命中放行——判据 E② 只覆盖 `build` 键非对象（`checks.js:284-292`）；后果面 = 病态仓可通过 samples 判据 | `docs/design/REPO-CONVENTIONS.md` §四（判据 E② 面） | `scripts/gates/checks.js:280-292`（解析后未校验根形态）· B29 微修轮交付报告「未落/存疑 2」 | 待讨论 | 归批（**B29 收口轮 / 随下次动 `checks.js` 的轮次**） |
| T52 | **`pet-chain.js:246` 正常切换就绪回调只摘旧段 `onended`、不摘 `old.onerror`（B27 加固轮越界观察，2026-09-19）**：对比 hold 分支 `:208`/`:216` 摘两者；淡出窗暂停的旧元素仍挂 `onerror` ⇒ 罕见触发会以旧段名提前结束静默 ✗（B18/B21 既有、非 B27 引入；加固轮修复后该路径仍良构）。修法 = 与 hold 分支同形摘两 handler（属行为修正 ⇒ 需走流程） | `docs/design/PET-ANIMATION.md` §2.14.9（清除面 / 生命周期面） | `pet-chain.js:246` vs `:208`/`:216`；B27 加固轮（#58）交付报告「越界观察」 | 待讨论 | 归批（**随下次动 `pet-chain.js` 的轮次**） |
| T53 | **逃跑气泡「我跑！」时机偏早 + 拖中 → 起跑硬切观感**（B27 用户实机反馈派生，2026-09-19；B27 §6 核销时登记的派生项）——「我跑!」气泡出现时机偏早；拖拽中被「吓一跳」到松手后起跑之间为硬切、观感可再打磨。属行为 / 观感修正 ⇒ 需走流程（设计轮 → 评审 → 实施） | `docs/design/PET-ANIMATION.md`（链 / 气泡触发面 §2.14.x）/ `shell-pet.js`（doWander 换段）/ 桌宠气泡触发点 | B27 批次档 §6（验收核销 · 派生登记）+ 用户 2026-09-19 实机反馈 | 待讨论 | 归批（**B27 后续小轮**——与 T52 同轮；具体时机参数待用户定）· **2026-09-20 裁定（T60 轮）**：气泡时机 = 保持现状（选项 A）⇒「气泡偏早」半条**核销**；「拖中 → 起跑硬切」半条随 T59/T60（起跑窗 + 素材转身帧过渡）落地后待实机复核；**T61 起被抓 / 被扔全程同档（被吓一跳），硬切面再减一处** |
| T54 | **`CHANGELOG.md` 未随批次收口同步**（as-of 2026-09-19 实测：`[Unreleased]` 曾仅占位、种子 = B01–B07）——2026-09-19 已补 B25 / B27 / B28 / B29 四批条目 + 0.0.1 发布节（`CHANGELOG.md:34`）；**残余 = B08–B26 回溯补写**（各批 §6 收口行转一条一行） | `CHANGELOG.md`（体例节 + `[Unreleased]`）/ 各批批次档 §6 | 实测：`CHANGELOG.md:7`（`[Unreleased]` 现有实体条目）+ `:32`（六批核销同步注）+ `:34`（`[0.0.1]` 节）；地图口径 = 条目来源为各批 §6 收口行 | 待讨论 | 归批（**B14 文档事实收口同轮**——条目来源与 B14 同一批 §6 面；**写权 = 主 agent**✗ CHANGELOG 属索引/台账族，不进 designer 写域） |
| T55 | **B31 折账轮残留三处（B31 收口轮 #6 交付报告「未落 / 存疑」派生，2026-09-19）**：① **静态占位文案与 JS 渲染不一致**——`exchange.html:45`「1000 token = 1💴」与 `pet.html:117`「Lv.1 好感 0」（JS 渲染覆盖，刷新失败时可见旧值；B31 后汇率 = 10,000、满级 = `MAX`）；② **文档口径残留「15 档」**——`docs/design/ARCHITECTURE.md:50` / `:113` 与 `docs/batches/B25-architecture-docs.md:164`（B31 后绑定面 = **16 档**；B25 已收口 ⇒ 属存量面）；③ `scripts/gates/baseline.json` 的 `asOf` 字段仍为 **2026-09-18**（含 09-19 的 `assemblyExempt` 条目；纯元数据、非判据面） | ① `exchange.html` / `pet.html`；② `docs/design/ARCHITECTURE.md` / `docs/batches/B25-architecture-docs.md`；③ `scripts/gates/baseline.json` | B31 收口轮 #6 交付报告（未落 / 存疑 ①–④）+ `docs/batches/B31-affinity-balance.md` §2.8 | 待讨论 | 归批（① 随下次动桌宠 / 兑换屋两档的轮次；② **B14 文档事实收口**；③ 随下次动 `scripts/gates/` 的轮次——**不得夹带在飞批次** ✗） |
| T56 | **发布包托管的国内可达性（2026-09-20 主 agent 机检 AC11 时发现）**：`latest.json` 的**清单面在 Gitee raw 可达** ✓（实测 `https://gitee.com/ludonghuai/big-fish/raw/main/latest.json` 匿名 HTTP 取回成功、无代理 ✓）——**但包体 URL 指向 GitHub Releases** ✗（`urls.win32` = `https://github.com/ludonghuai/big-fish/releases/download/v0.0.1/Bigfish.Setup.0.0.1.exe`）⇒ **无代理的国内用户**：清单能取、**包下不动** ✗（本机之所以能装是因为有代理 10.2.2.112:3128）。**候选方向** = ① 包体同步托管 Gitee Releases（附件面）并在 `latest.json` 增国内镜像条目（客户端择优 / 回退）② 客户端实现「主源失败 ⇒ 备源」双 URL ③ 认账（注明需代理）。**证据行** = `latest.json`（`urls.win32`）+ 本机 `gitee/raw` 取回成功实测 | `docs/design/AUTO-UPDATE.md` §2.2.8（清单与源面）/ `latest.json` / `make-latest.js`（生成面） | 实测 2026-09-20：`cd <repo> && curl -s https://gitee.com/ludonghuai/big-fish/raw/main/latest.json` ⇒ 200 + 与仓内清单逐字一致 ✓；包体 URL = GitHub Releases ✗ | 待讨论 | 归批（**发布链族——T5 / T8 同族**；下次发版前处置 ✓） |
| T57 | **市场弹窗 `showModal` 覆盖 `modalResolve` ⇒ 先开的确认框 Promise 永久悬挂**（2026-09-20 实机排查中主 agent 读码发现）：`market.js:75-85` 只用**一个** `modalResolve` 变量；若前一个 `confirmModal` 的 Promise 尚未结算而再次进入（例：连续点两个「安装」、或前一个弹窗被遮住未察觉），后者的 resolve **直接覆盖**前者 ⇒ 前者的 `await` **永不结算**（无超时、无日志、无 toast）⇒ 症状与用户 2026-09-20 报告的「点安装没反应」**同形**（静默 / 零日志）。**修法方向** = ① 模态期间锁入口（`state.modalOpen` 守卫 ⇒ 二次调用忽略或排队）② `modalResolve` 改队列 / 数组 ③ 补行为判据（「模态打开时再点其它操作」）+ 桩测。**家族同源** = T42（IPC 早退无取证行）——同属「静默失效家族」⇒ 建议同批处置 | `market.js:75-95`（showModal / closeModal）/ `market.html:192-201`（模态 DOM） | 实测读码：`market.js:76-85`（单变量覆盖）+ `:91-95`（三入口 closeModal）；用户报告「点击安装没有反应」+ `bigfish.log` 无安装面行 + 无 toast 失败行 | **已核销（B33，2026-09-20）** | 归批 = **B33**——`showModal` 重入守卫落地（模态打开期间二次调用立即 `resolve(false)`，TC-112 桩测封口）；设计契约 = `docs/design/SHELL-UX.md` §2.2.16 条 5，验收 = `docs/batches/B33-market-tarball.md` §6 AC-B33-4 |
| T58 | **GitHub 镜像表可用性巡检**（B33 派生，2026-09-20）：`shell-plugin-fetch.js` 的 `GITHUB_MIRRORS` = 公共代理（易变资源，当日实测三镜像对 dsh-pet 仓库均 404、仅 codeload 直连可用）；若日后 codeload 直连也不可达，需换表 / 加源。巡检口径 = 抽 1 个 github-only 插件（如 `github:ysyyhhh/dsh-pet`）实装验证 | `shell-plugin-fetch.js`（`GITHUB_MIRRORS`）· `docs/design/SHELL-UX.md` §2.2.16 条 1 | B33 实施轮实测（2026-09-20）：codeload 直连 200 / 三镜像 404 | 待讨论 | 归批（**待定**——下次市场面批次或发版前抽验；换表 = 单行面改动，随批即可） |
| T60 | **逃跑位移节奏（变慢变长至摔倒点）+ 甩抛放行**（2026-09-20 用户实机反馈两条，同轮设计批准）：① **位移窗对齐素材奔跑段**——escape 腿时长由原「腿长 ÷ 0.45 px/ms」（0.67–1.33 s 就停）改为定值窗 `ESCAPE_TRIP_MS − ESCAPE_RUN_START_MS` = 4000−1750 = **2250 ms**，移速按腿长派生（≈0.13–0.27 px/ms，慢一半到三分之二；撞墙只慢不错节奏）；素材标定 = 1.75–3.9 s 奔跑 / 3.9–4.2 跑停站直 / 4.3–5.0 前扑摔倒 / 其后起身擦汗（逐帧采样 `原地左转奔跑.webm` 10.042 s）；`ESCAPE_LEG_SPEED` 退役。② **甩抛放行**——物理（托盘「甩抛物理手感」）开 ∧ 松手为甩（物理自己的 `armGate` 判 ok：同一阈值 500 px/s / 同一轨迹 / 同一 G11 静默判据，不自造第二份判定）⇒ 逃跑彩蛋不劫持、物理接管飞行；慢放 / 静默放置（≤5 px）/ 物理关 ⇒ 彩蛋逐字照旧（US-7 不回退）。实现 = `shell-pet-physics.js` 零副作用读面 `petWouldArmFlight()`（不动 G3 会话号 / 不清轨迹 / 零日志）+ `shell-pet-drag.js` 逃跑分支放行一行 + `main.js` deps 接线（零新 require 边） | `shell-pet.js`（常量区 + `doWander`）/ `shell-pet-physics.js` / `shell-pet-drag.js` / `main.js`；文档同步面 = `docs/requirements/PET.md` US-7 注记 + `docs/design/PET-MOVEMENT.md` §2.2.13 交接③ + `docs/design/PET-ANIMATION.md` §2.14.8 常量表 | 桩测 = `.thincoder/t59-escape-delay-stub.mjs` **8/8**（腿墙钟 = 窗定值 4000 ms、walk 零回退）+ `.thincoder/t60-throw-gate-stub.mjs` **9/9**（甩放行 / 慢放触发 / 静默触发 / 物理关触发 / 读面零副作用 / 接线标记）；门禁 = `test:full` 62/62 + `test:integration` 3/3 + lint 零新增违规 | **已核销（2026-09-20）** | 小微轮随用随修（设计 = 会话内提案 + 逐帧标定 + 机制证据，用户 2026-09-20 批准；气泡时机裁 = **A 保持现状** ⇒ **T53** 的「气泡偏早」半条随之核销、硬切半条待实机复核）；**建议行**（写权 = eng-designer）：US-7 修订注记（触发面新增「甩抛放行」= 语义变更）· `PET-MOVEMENT.md` §2.2.13 交接③ 优先级句改写（甩抛时物理优先）· §2.14.8 常量表补 `ESCAPE_RUN_START_MS` / `ESCAPE_TRIP_MS` 并退役 `ESCAPE_LEG_SPEED` |
| T61 | **甩抛飞行途中播「逃跑戏」（空中地面戏错位）**（2026-09-20 用户实机反馈）：T60 放行后物理弹跳本身正常，但飞行期按 DD-11 以 \|vx\| 切 walk-\*/run-\*（≥900 px/s ⇒ run-\*），而 run 池唯一段 = `原地左转奔跑`（与 escape 档段同源，B21 O23）⇒ 空中播「站立 → 转身 → 地面奔跑 → 摔倒」整套逃跑戏。修法（DD-11 修订；样本实证 = `startThrow` 飞行全程零换档）：飞行全程保持「被吓一跳」（drag 档——抓起惊吓 → 空中扑腾 → 落地 Q 弹 → 停稳回神），`armFlight` 一次 `setPetState('drag')`、`flightTick` 删按 \|vx\| 切档两行（`PHYS_RUN_SPEED` 消费点退役，core 常量导出保留）；停泊复位 idle 照旧；重抓抢占 / 退出 / 换屏收口各路径逐字不动 | `shell-pet-physics.js`（armFlight / flightTick）；文档同步面 = `docs/design/PET-MOVEMENT.md` DD-11 注记 + §2.2.6 常量表 `PHYS_RUN_SPEED` 行 | 桩测 `.thincoder/t61-flight-anim-stub.mjs` **6/6**（起飞 drag 恰 1 次 / 空中 60 tick 零换档 / atRest 复位 idle / 静态零换档面）；门禁 = lint **PASS（checks=7）** + `test:full` 62/62 + `test:integration` 3/3；回归 = t59 8/8 + t60 9/9 | **已核销（2026-09-20）** | 小微轮随用随修（设计 = 会话内提案 + 样本对照 + 素材逐帧证据，用户 2026-09-20 批准）；捎带 = **T53** 硬切半条再减一处（被抓 / 被扔全程同档）；**建议行**（写权 = eng-designer）：`PET-MOVEMENT.md` DD-11 修订注记（飞行期保持 drag 档；样本实证不换档）+ §2.2.6 常量表 `PHYS_RUN_SPEED` 消费面退役注记 |
| T59 | **逃跑位移先于素材起跑（站立帧平移）**（2026-09-20 用户实机反馈）：拖到墙角松手后，escape 段 `原地左转奔跑`（≈10.04 s）前 ≈1.75 s 为站立（0–1.3 s 正面）+ 转身帧，而 `doWander` 位移原与换段同 tick 起跑 ⇒ 站立姿态期间窗口已滑出 300–600 px（「站着平移」）。修法 = escape 腿位移时钟后移单点常量 `ESCAPE_RUN_START_MS`（1750，与 B27 `ESCAPE_LEG_*` 同区），素材起跑点与位移起跑对齐；walk/run 腿逐字不动、零新增定时器（偏移只做进既有 `moveTimer`） | `shell-pet.js`（常量区 + `doWander` escape 分支）/ `docs/design/PET-ANIMATION.md` §2.14.8（常量表） | 逐帧标定 = 探针采样 `原地左转奔跑.webm`（10.042 s）：t=0–1.2 站立 / 1.5–1.7 转身 / ≥1.75 侧跑；桩测 `.thincoder/t59-escape-delay-stub.mjs` **8/8**（escape [0,1750) 零位移 ∧ 首位移 ∈ [1750,1766] ∧ walk 零延迟）；`npm run test:full` 62/62 | **已核销（2026-09-20）** | 小微轮随用随修（设计 = 会话内提案 + 逐帧标定证据，用户 2026-09-20 批准）；与 **T53**（气泡时机 / 硬切）同族不同面——T53 仍在途；**建议行**：`docs/design/PET-ANIMATION.md` §2.14.8 常量表补 `ESCAPE_RUN_START_MS` 行（写权 = eng-designer，随下次动该档的轮次） |

---

## 三、待处置（无触发，待人工裁决）

（当前无条目）
