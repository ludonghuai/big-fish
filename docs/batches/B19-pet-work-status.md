# B19 批次档 —— 桌宠工作状态联动（外部项目移植 D1 面）

> 六段制 · append-only · 一段一作者：§1 主 agent / §2 eng-designer / §3 设计评审 / §4 主 agent / §5 eng-coder / §6 主 agent。
> 本档 = **R15（外部项目能力移植）的 D1 面**，序 = A（动画链，B18 已收口）→ **D1（本批）** → B 物理 → D2 余额/碎碎念/对话 → C 素材链 → E 多实例。

## §1 立案（主 agent）

### 1.1 目标（一句话）

让桌宠**反映 DSH 会话的真实工作状态**——回合开始（思考）/ 工具调用（忙碌）/ 工具完成（整理）/ 等待批准（等待）/ 回合成功结束（庆祝，失败则垂头叹气）时，切到对应档位的动画，并（可选）弹一条状态气泡。

### 1.2 范围（做 / 不做）

**做**：
- 五至六档**工作状态档位**的**动画映射**（走 B18 已建成的池机制：新增 `events.workStatus.*` 或独立 `workStatus` 段）。
- **会话事件 → 状态档位**的触发链（读面复用 B09 已落成的会话记录读取面，见 1.3）。
- 状态**气泡文本**（可选、按档位；默认关闭或按裁定）。
- **状态回退**：状态档位结束后按既有链规则回自然档（沿用 B18 的三级回落与 1.6 定时器口径）。
- 开关面（用户可关；默认值由 1.6 待决项裁定）。

**不做（本批出批）**：
- 余额档位气泡 / 碎碎念 / 对话 / 记忆持久化（**D2 面**）。
- 物理手感 / 位移接管（**B 面**，含 O10 裁定）。
- 多实例 / pet pack（**E 面**）；素材生产链（**C 面**）。
- **不改**：`shell-pet-geometry.js` / `shell-pet-drag.js`（几何冻结面）· `assets/pet-anim/webm/**` 素材本体 · B18 的双缓冲形态与池校验 V1–V6 · B09 的完成提醒判定门（只读复用其读面）。

### 1.3 现状实测（勘察，2026-09-17）

| 面 | 实测事实 | 证据 |
|---|---|---|
| **样本的 D1 定义** | 五档按索引 = `0 thinking`（回合开）/ `1 working`（工具调用）/ `2 result`（工具完成回整理）/ `3 waiting`（等待批准 `approval`/`asked`）/ `4 success`（回合成功完成）；另有第六段「垂头叹气冒汗」（失败向）；开关 `pets[].workStatusEnabled`，**注释明写默认关闭** | `samples/dsh-pet/dsh-pet/assets/config.jsonc:220-233`（档位索引与动画名逐行注释）· `:64-70`（气泡文本按档位；省略字段 = 不弹文本）· `:120`（默认关闭） |
| **素材（已在仓）** | `assets/pet-anim/webm/` 内已有工作状态段 **8 段**：`工作状态-思考冒泡` / `-忙碌点按` / `-清点归档` / `-原地踱步张望` / `-雀跃庆祝` / `-垂头叹气冒汗` + `写代码` / `深度思考碎碎念` | 实测文件名（B18 已把 106 段整体迁入） |
| **池引用现状** | 池引用 91 段，**未引用 15 段**（其中工作状态 5、余额 6、碎碎念 3、拖拽 1）⇒ 工作状态素材**已就位、待本批引用** | B18 交付报告 §5.4（主 agent 复核：`pool.json` 无 `workStatus` 段） |
| **本仓既有事件读面** | `shell-notify.js` 已读 per-record 会话记录（`turnBoundary.val.openTurnStartSeq` 等）并做四态判定；**本批的「回合在途 / 结束」可直接复用该读面** | `shell-notify.js`（309 行，B09 实施）· `docs/design/SHELL-UX.md` §2.2.12 |
| **状态面落点** | `pet.js`（**211** 行，状态 → 渲染）· `shell-pet.js`（**426** 行，池装载 / 下发 / 窗口）· `pet-chain-core.js`（198 行，链决策）· `pet-chain.js`（240 行，播放） | 实测行数（换行符计数） |
| **冲突面** | 本批与 B09 **共享读面**（只读复用，不改其判定门）· 与 B18 共享池与渲染面（**扩池段 + 新档位**，不改双缓冲形态） | — |

### 1.4 硬约束（不得偏离）

1. **几何层零 diff**：`shell-pet-geometry.js` / `shell-pet-drag.js` 不得出现在 `git diff`（承 B18 同款硬门禁）。
2. **素材本体只读**：`assets/pet-anim/webm/**` 不得增删改；本批只**引用**（池数据面）。
3. **PNG 通道零回退**：视频不可用时的三级回落与既有 PNG 动画语义不变。
4. **零新依赖**：不引入 npm 包、不改 `dependencies`；新增渲染档（若有）须同步 `build.files`。
5. **默认行为保守**：状态联动对既有用户的默认影响必须显式裁定（**默认关**或**默认开**，见 1.6）。
6. **行宽 ≤300（不含行尾 CR）· 代码档 ≤500 行 · 文件头标准形**（`docs/CONVENTIONS.md` §一 / §五）。
7. **许可面沿用 B18 结论**：素材禁商用（用户裁定本仓不商用 + 商用化消解路径），署名三处已在位，本批**不得移除或改写**署名。

### 1.5 待决项（须裁定后才能定稿设计）

| # | 待决 | 备选 | 影响面 |
|---|---|---|---|
| U-1 | **默认开关** | ① 默认**关**（用户显式开启，同样本）② 默认开（状态更有"活感"） | 需求层契约 + 首次体验 |
| U-2 | **触发粒度** | ① 五档（thinking/working/result/waiting/success）② 增第六档失败向（`垂头叹气冒汗`）③ 更粗（仅「忙碌 / 空闲」两态） | 池段数 + 判定门复杂度 |
| U-3 | **气泡文案** | ① 照搬样本文案 ② **本仓自写**（样本文案含网络梗与毒舌向，与本仓语气未必一致） | 需求层文案面 + 许可（文案源自样本则同受禁商用约束） |
| U-4 | **状态优先级** | 与既有交互档（点击 `happy` / 喂食 `eat` / `sleep`）**谁优先**、是否打断 | 与 B18 §2.2.3 / §2.2.4 的链规则交互 |
| U-5 | **判定门复用度** | ① 复用 B09 判定门的读面与四态（只读）② 新建轻量读面 | 与 B09 面耦合度 / 重复读取代价 |

### 1.6 关联台账与需求档

- **台账**：`docs/TODO.md` §一 **R15**（外部项目能力移植）——本批 = 其 **D1 面**；收口后在该行触发列登记。
- **需求档落点**：`docs/requirements/PET.md`（§三 用户故事 / §四 非功能）——**由 eng-designer 起草**（与 US-15…19 同档续编）。
- **设计档落点**：`docs/design/PET-ANIMATION.md`（B18 已建该主题档；本批**续写其对应节**，不新建档——单一主题一档）。

### 1.7 变更记录

| 日期 | 变更点 |
|---|---|
| 2026-09-17 | 建档（B19 立案）：目标 / 范围 / 勘察实测（样本 D1 定义 + 素材在位 + 既有读面 + 冲突面）/ 硬约束七条 / 待决 U-1…U-5。依据 = R15 的 D1 面 + 用户 2026-09-17「都做，你安排次序」。 |
| 2026-09-17 | **§1.3 行数订正（O15，承 eng-designer 报告）**：`pet.js` 212 → **211** · `shell-pet.js` 427 → **426**（口径 = 项目规范 §五「换行符计数」；主 agent 已用 `\n` 计数独立复核）。 |

---

## §2 本批任务书（eng-designer）

---

### 2.1 交付物与落点

| # | 交付物 | 落点 | 状态 |
|---|---|---|---|
| 1 | 需求层（本批条目） | `docs/requirements/PET.md` §一 B19 目标段 / §二 分层行 + 两条「不做」项 / §三 US-20…US-23 / §四 NFR-14…NFR-17 / §五 变更记录 | **已落** |
| 2 | 设计层（续写既有主题档，不新建档） | `docs/design/PET-ANIMATION.md` §1.3.5（+注 W1–W3）/ §2.6–§2.11 / §3.4–§3.6 / §四 变更记录 | **已落** |
| 3 | 本批任务书 | 本段 | **已落** |

### 2.2 本批条目（**三方一致表：需求档条目 = 设计档 AC 回指 = 本段条目**）

| 需求条目 | 类型 | 设计档验收 | 一句话实现面 |
|---|---|---|---|
| US-20 工作状态动画反映真实会话进程 | 功能 | AC16 / AC17 / AC18 | 只读投影缓存记录 → 派生档位（生成 / 工具 / 收尾）→ `setPetState` → 链按 `events["work-*"]` 播段 |
| US-21 工作状态只作「底色」 | 功能 | AC19 / AC21 | 工作档 = 背底档 + 1 s 重断言；散步起步加一道门；既有触发点与定时器逐字不改 |
| US-22 工作状态气泡 | 功能 | AC16 / AC20 | 本仓自写文案常量表 + 节流（同档一次 / ≥30 s）+ 走既有 `petSay` |
| US-23 开关面与默认值 | 功能 | AC20 | `settings.json` 顶层键 `petWorkStatus` + 托盘「设置」checkbox；默认值 = U-1 采用推荐值 `false`（待用户批准时一并确认） |
| NFR-14 状态识别时延与开销 | 非功能 | AC18 / AC20 | 切档 ≤ 6 s；mtime 未变不解析；禁同步递归遍历 |
| NFR-15 只读隔离与隐私 | 非功能 | AC19 / AC20 | 只读状态字段；不 require `shell-notify.js`；不读 / 不落 / 不发会话内容 |
| NFR-16 零回退与规范 | 非功能 | AC19 / AC21 / AC22 | 零改动面逐档零 diff；两个新档登记 `build.files`；行宽行数机检 |
| NFR-17 可测与形态守卫 | 非功能 | AC16 | 纯函数集中可装载；`ver` + 字段在场守卫；形态不符 ⇒ 停用 + 诊断行 |

### 2.3 实现写域（`files` 声明面 = 实施者写域，逐档）

- **新建**：`pet-work-core.js`（纯函数：常量表 / 记录选取 / 形态与陈旧守卫 / `deriveGear` / 气泡节流 / 文案表 / 双环境导出尾巴）·`shell-pet-work.js`（I/O：`fs.watch` + 1 s tick + 读记录 + 下发与重断言 + 开关接线 + 降级 + 日志 + 注入面）
- **改动**：`shell-pet.js`（`logAnim` 导出 / `setBaseStateProvider` + `petBaseState` / `scheduleWander` 起步门）·`pet.js`（`setState`：链上报先行 + 未知档位渲染待机帧）·`shell-settings.js`（`petWorkStatus` 默认值）
  ·`shell-tray.js`（设置子菜单 checkbox + set 函数）·`main.js`（组合根接线）·`package.json`（`build.files` 增列两新档）·`assets/pet-anim/pool.json`（`events` 三个新键）
- **开发期工具（不入包）**：`.thincoder/b19-pet-work-stub.mjs`
- **零改动面（不得出现在 `git diff`）**：`pet-chain.js` · `pet-chain-core.js` · `pet-preload.js` · `shell-pet-geometry.js` · `shell-pet-drag.js` · `shell-notify.js` · `shell-affinity.js` · `assets/pet-anim/webm/**` · `THIRD-PARTY-NOTICES.md` · `README.md` · `版本说明.txt`
- **行数预算**：`shell-pet.js` 426 → ≤ 440（无拆分需求）；新档 140 / 200；全部 ≤ 500 ✓

### 2.4 验收标准（本批 7 条，逐条可机检；判据细目 = 设计档 §3.4）

| AC | 一句话判据 | 机检手段 |
|---|---|---|
| AC16 | 派生纯函数桩测全绿（选取 / 守卫 / 派生 / 收尾边沿 / 气泡节流） | `node .thincoder/b19-pet-work-stub.mjs` ⇒ `pass/total PASS` |
| AC17 | 池数据三键合法（`parsePool` ok=1）+ 引用名存在 + 计数 91 → 94 / 15 → 12 | 同上（复用 `pet-chain-core.parsePool`） |
| AC18 | 真实会话一轮 ⇒ 三档依次出现 + 时延 ≤ 6 s + 对应 `anim switch` 行 | 日志对齐（`BIGFISH_PET_DEBUG=1`） |
| AC19 | 零改动面逐档零 diff + 依赖段零 diff + 既有交互实机照旧 | `git diff --stat` + 实机复核 |
| AC20 | 开关（默认关零日志 / 开即生效 / 关即清档）+ 气泡节流 + 降级恰一条诊断行 | 日志面 |
| AC21 | 池不可用时画面与今天一致；PNG 通道下工作档渲染待机帧、无空白帧 | 探针 + 人工判定 |
| AC22 | 行宽 ≤300 / 行数 ≤500 / `build.files` 登记两新档 / 署名零改动 / V1–V6 与链逐字零 diff | 静态机检 |

### 2.5 前置与阻断项（**评审 / 批准前必须裁定**）

| # | 事项 | 状态 | 对实施的影响 |
|---|---|---|---|
| U-6（本批新发现；**已裁定**） | **档位覆盖缺口**：样本六档中的「等待批准 / 回合成功 / 回合失败」在本仓只读数据面**不可观测**（证据 = 设计档 §1.3.5 + 注 W3）⇒ 本批按「可观测三档」成文 | **已裁定 = ①（可观测三档）· 用户 2026-09-18** | 按本设计实施；六档的补齐 = **事件桥后置批 B22**（本批已预留扩展面：池段键可增 3 键 + 判定门可扩档 ⇒ 增量补齐、不返工） |
| U-1 | 默认开关（推荐：默认关） | **采用推荐值（待用户批准时一并确认）** | 影响 `settings.json` 一处默认值 + 需求 / 设计各一行 |
| U-2 | 触发粒度（设计按「可观测三档」成文；§1.5 原三案逐案否决理由 = 设计档 §2.6.2） | **采用推荐值（待用户批准时一并确认）** | 与 U-6 联动（同一裁定面） |
| U-3 | 气泡文案（推荐：本仓自写） | **采用推荐值（待用户批准时一并确认）** | 影响文案常量表 |
| U-4 | 状态优先级（设计按「拖动 > 散步 > 交互档 > 工作档（背底档）」成文） | **采用推荐值（待用户批准时一并确认）** | 影响设计档 §2.8.4 |
| U-5 | 判定门复用度（推荐：复用口径 + 独立模块，不 require `shell-notify.js`） | **采用推荐值（待用户批准时一并确认）** | 影响设计档 §2.8.1 与 O16 建议行 |

### 2.6 计数（D3 机检口径）

- 需求条目：**US 19 → 23**（+4）· **NFR 13 → 17**（+4）；设计档：**AC 15 → 22**（本批 +7）· **TC 20 → 32**（本批 +12）· **DD 16 → 26**（本批 +10）· 冲突核对 **C33 → C44**（本批 +11）· 观察项 **O11…O17**（+7）
- 实现面：新增源档 **2** · 改动档 **9**（含数据档 `pool.json` 与 `package.json`）· 零改动面 **11** 档
- 素材：引用段 **91 → 94** / 未引用 **15 → 12**（`assets/pet-anim/webm/**` 本体零改动）

### 2.7 出批项（本批不做，边界逐条）

1. 工作状态的「等待批准 / 回合成功 / 回合失败」三档（读面不存在；须换事件源）——**去向 = 后置批 B22**（事件桥；用户 2026-09-18 裁定 U-6 = ①）；
2. Harness 侧事件桥 / 会话流订阅 / 会话日志（zstd）解析（新面，与「零新依赖 / 不改 Harness」相抵）——其中**事件桥 = 后置批 B22**；
3. 余额档位气泡 / 碎碎念 / 对话（D2 面）；物理手感（B 面）；多实例与 pet pack（E 面）；素材生成链（C 面）；
4. 用户可配置文案 / 权重 / 池（承 B18 DD-2 的窄化口径）；
5. 只读面异常时的重试退避 / 多会话状态聚合到 UI（本批只做确定性选取 + 降级封口）。

### 2.8 修正轮 1（设计评审第 1 轮 · 13 条 + 2 条范围外备注）——逐条处置与订正表

> **本节效力（append-only 档的冲突裁定）**：§2.1–§2.7 = 起草时刻的记录；**本节 = 修正轮 1 之后的现行条文**——凡本节与前述各节冲突处（AC 回指集合 / 本批 AC 与 TC 条数 / 计数行 / 写域）**以本节为准**，前述各节保留为历史。
> **源** = 批次档 §3 轮次 1（评审发现全文 + 其独立核验证据）+ 主 agent 逐条采纳（无驳回）。**修正轮边界**：只落评审发现直接导出的收口——**选型（§2.6.1–§2.6.6）· U-6 裁定结论 · 三档集合零改动**；新编号仅两处（承 #1 / #3）。

#### 2.8.1 逐条处置（15 条）

| # | 处置 | 落点 |
|---|---|---|
| 1 | **已修**（补判据 + 编号顺延） | 设计 §3.4 **新增 AC23** + **注 B**（判据细目）；§1.1 NFR-14 行同源 |
| 2 | **已修**（只同步字面、不动语义） | 需求档 `PET.md` US-20 / NFR-14 的验收指针 |
| 3 | **已修**（判据句改写） | 设计 §2.8.4「重断言」段 + 表「进工作档」行；§3.5 **新增 TC-33** |
| 4 | **已修**（取方案 a：改用持久信号） | 设计 §2.8.2 规则 5 + `doneBaseline` 三条 + 常量行；§1.3.5 注 W1；§3.6 限制 1 |
| 5 | **已修** | 设计 §2.7 注入面（9 项，含 ASCII 图）+ §2.9 `main.js` 行 |
| 6 | **已修** | 设计 §2.7「开关的传播面」+「与 B09 取不同形态的理由」 |
| 7 | **转主 agent**（§4 属主 agent 段，本角色不写） | 批次档 §4.2 / §4.3（仍记 U-6 阻断待裁，与 §2.5「已裁定 = ①」不同源） |
| 8 | **已修**（取「逐轮可恢复」） | 设计 §2.8.7 行 1 + 「停用」封存语义注；口径张力登记 §2.11 **O18** |
| 9 | **已修** | 需求档 NFR-15；设计 §1.1 NFR-15 行 + §2.8.1 字段白名单 + §3.4 AC19 |
| 10 | **已修** | 设计 §3.4 AC22（补文件头标准形） |
| 11 | **已修**（统一口径） | 设计 §2.9 表后 + §3.4 AC19 |
| 12 | **已修**（消歧） | 设计 §2.8.4 表「段切换」行 + **注 C**；观感取舍登记 §2.11 **O19** |
| 13 | **已修** | 设计 §2.8.1「I/O 形态」 |
| A | **已修** | 需求档 `PET.md` §二「会话事件联动」不做项（补 B19 / B22 交叉指针） |
| B | **已修**（改通用形态） | 需求档 `PET.md` NFR-8 的 carve-out 注记 + 度量方式行 |

**关键改前 → 改后（摘要）**

- **#2 三方同源**：US-20 `AC16、AC18` → **`AC16、AC17、AC18`**；NFR-14 `AC18` → **`AC18、AC20、AC23`**（设计 §1.1 = 权威集合）。
- **#3**：术语补定义（**记忆档位 `lastGear`** / **当前派生档位 `gear`** / **渲染层实际档位 `state`**）；原「① …或② …」拆为 **共同门（`!== null` 拖动 ∨ `walk-*`·`run-*` ∨ 交互档 ⇒ 让位）+ 两条互斥条款（① 下发条 / ② 重断言条）**——排除面**对「进入」与「保持」同样生效**。
- **#4**：收尾档改**持久信号**驱动——`turnBoundary.val.lastTurn > doneBaseline` ∧ 无在途回合 ∧ 记录新鲜；基线三条（建立 / 更新 / 重置）；删死常量 `WORK_DONE_WINDOW_MS`（无消费者）。
- **#8**：`unavailable` = **逐轮可恢复**（tick 与 `fs.watch` 不停）；**封口的是日志行，不是功能**；锁存面只有「开关关闭」。
- **#9**：「不读取」→「**不使用 / 不落盘 / 不发送**内容字段（整档 `JSON.parse` 的读取事实如实登记）」；AC19 补「零字段取自内容行」。
- **#11**：零改动面统一为 **11 档**（机检面 = 代码 7 + 资源 4）；2 档文档（`docs/CONVENTIONS.md` / `AGENTS.md`）单列「不属机检面」。

#### 2.8.2 三方一致表（**订正后**的现行值；批次档 = 设计 §1.1 = 需求档）

| 面 | 现行值 |
|---|---|
| US-20 的 AC 回指 | **AC16、AC17、AC18** |
| NFR-14 的 AC 回指 | **AC18、AC20、AC23** |
| 本批 AC 条数 | **8**（AC16…AC23；原记「7」⇒ **订正为 8**） |
| 本批 TC 条数 | **13**（TC-21…TC-33；原记「12」⇒ **订正为 13**） |
| 零改动面 | **11 档** |
| 本批条目集合 | US-20 / US-21 / US-22 / US-23 / NFR-14 / NFR-15 / NFR-16 / NFR-17（**零增减**；仅其中两条的 AC 回指列补全） |

#### 2.8.3 写域订正（实施面，供 eng-coder 取用）

- **`pet.getPetState` 实况（#5 核验，实测）**：**已在** `shell-pet.js:387`（`function getPetState() { return petState; }`）并已在 `:404` 导出 ⇒ 本批**零新增导出**；`shell-pet.js` 的改动点与行数预算（426 → ≤440）**不变**。
- **注入面共 9 项**：`pet.setPetState` · `pet.getPetState` · `pet.petSay` · `pet.wakePet` · `pet.logAnim` · `pet.getPetWindow` · `drag.getPetDrag` · `settings.get` · `backend.dshHome`。
- **开关接线（#6）**：`main.js:84` 的 `tray.init({ setQuitting, APP_NAME })` **增一项** `setPetWorkStatus: work.setEnabled`；`shell-tray.js` 的 checkbox click ⇒ ① 写 `settings.petWorkStatus` + `saveSettings()` ② 调 `setPetWorkStatus(checked)`；启动态由 `work.init(deps)` 读 settings 决定（无双源）。
- 其余写域（新增 2 档 / 改动 9 档 / 零改动面 11 档）**逐档不变**（§2.3）。

#### 2.8.4 计数（D3 口径 —— 设计档与需求档回读实测 as-of 修正轮 1）

- 设计档：**AC 23**（B18 15 + B19 8）· **TC 33**（B18 20 + B19 13）· **DD 26** · **C 44** · **观察项 O1…O19**（B19 = O11…O19 共 9 条）
- 需求档：**US 28** · **NFR 22**（本批 US-20…US-23 / NFR-14…NFR-17）
- 实现面：新增源档 **2** · 改动档 **9** · 零改动面 **11**
- 素材：引用 **91 → 94** / 未引用 **15 → 12**（零改动）

#### 2.8.5 作业证据（D6 回读核对 + 未写入面）

- **核验方式**：`node` 脚本逐档回读实测（行宽 >300 计数 / AC·TC·DD·C·O 行计数 / 标题编号单调性 / 三方指针逐字比对）。
- **结果**：设计档与需求档 **0 行 >300 字符**（设计档 max = 300 · 需求档 max = 298）；标题编号单调、无重号（1.1…3.6）；`AC23` / `TC-33` / 注 B / 注 C 在场；旧术语「当前有效档位」与死常量 `WORK_DONE_WINDOW_MS` **零残留**。
- **未写入面（本角色未动）**：批次档 §1 / §3 / §4 / §5 / §6 · 任何 `.js` / `.html` / `assets/**` · 台账 / 文档地图 · 其它批次面。
- **待主 agent 处置**：#7（§4.2 的 U-6 行与 §4.3 第 2 步仍记「阻断待裁」⇒ 应与 §2.5「已裁定 = ①」同步）；**O18**（NFR-17「等价于开关关闭」的读法口径，属需求层判定）；**O19**（收尾档 3 s 保持期的观感取舍，建议用户批准时复核取值）。

## §3 设计评审（评审子代理）

<!-- 由评审子代理填 -->

---

### 轮次 1（评审子代理）

**评审对象（机械声明）**：评审类型 = 设计；目标 = `docs/design/PET-ANIMATION.md` 的 **B19 段**（§1.3.5 + 注 W1–W3 / §2.6–§2.11 / §3.4–§3.6）· `docs/requirements/PET.md` 的 US-20…US-23 / NFR-14…NFR-17 · `docs/batches/B19-pet-work-status.md` 全文。状态 = 待评审。不做：B18 段（已收口）· B20/B23/B24 批面 · 未实施代码。

**本轮独立核验（机器事实）**：① 受影响档行数逐档实测（换行符口径）——`shell-pet.js` **426** · `pet.js` **211** · `shell-settings.js` **60** · `shell-tray.js` **175** · `main.js` **217** · `package.json` **132** · `assets/pet-anim/pool.json` **71** · `pet-chain-core.js` **198** ⇒ 设计 §2.9 表**逐档相符**，预算末值最大 440 ≤ 500 ✓；② `pool.json` 引用段实测 **91**（去重后计数）· 未引用 **15** ⇒ AC17 的「91 → 94 / 15 → 12」与 §2.8.3 新增三键（5 个片段名中 `深度思考碎碎念`/`写代码` 已在池、`工作状态-原地踱步张望` 已在 `moves.walk`）**算术自洽** ✓；③ 新引用 5 名在 `assets/pet-anim/webm/` **逐名在场** ✓；④ 链侧泛型映射成立（`pet-chain.js:94` 同档忽略 · `:130-134` `pool.events[s]` · `pet-chain-core.js:106/147-151/159-161` 对新增 `events` 键零改动）⇒ 「链与池校验零改动」为真 ✓；⑤ 读面承载证据独立复核成立——`dsh-session-stats/lib/index.js:159-171` 的 wire 视图**确不含** `openStep`/`pendingCalls`，而内部 state（`:46`/`:52`/`:80-81`/`:152-154`）含之 ⇒ 持久行 `val` 可承载三档信号 ✓；`dsh-base/cordis.patch.yml:162-166` 的 `writeEveryEvents:200`/`writeIntervalMs:5000` 与 `dsh-session-projection-cache/lib/index.js:290-309`（显式 `turn/end` flush）⇒ 5 s 滞后上界与收尾强制落盘点**均成立** ✓；⑥ `.thincoder/**` 不入 `build.files` ✓（`package.json:41-84` 无该前缀）。

| # | Category | Severity | Issue | Suggestion |
|---|----------|----------|-------|------------|
| 1 | Acceptance criteria | 🟡 | NFR-14 的**开销判据**（主进程增量 < 1%；判据 = mtime 未变只做一次 `stat`、不解析；度量 = `work scan`/`work gear` 行 + `process.getCPUUsage()` 前后各 10 s 均值，`docs/requirements/PET.md:381-382`）在 AC 面无对应判据：AC18（`PET-ANIMATION.md:813`）只判档位序列与时延 ≤ 6 s，AC20（`:815`）只判开关/气泡/降级 ⇒ 该条要求无取证面 | 在 AC18 或 AC20 增一条判据：`work scan` 的 `cache=miss` 条数与「记录 mtime 变化次数」同阶（或 ≤ 同阶），并按需求档口径补 CPU 前后采样；判据行写进 §3.4 表 |
| 2 | Requirements（同源） | 🟡 | 三方（需求档条目 = 设计档 AC 回指 = 批次档 §2）在 AC 回指上不同源：US-20 需求侧记「AC16、AC18」（`docs/requirements/PET.md:190`），设计记「AC16–AC18」（`PET-ANIMATION.md:27`）、批次记 AC16/AC17/AC18（`docs/batches/B19-pet-work-status.md:89`）；NFR-14 需求侧记「AC18」（`PET.md:378`），设计与批次均记 AC18/AC20（`PET-ANIMATION.md:31` · `B19:93`） | 由主 agent 裁一处口径（建议以设计/批次的并集为准）后同步三档；属回指字面同步，不动语义 |
| 3 | Clarity（核心机制） | 🟡 | 重断言判据句（`PET-ANIMATION.md:610`）含两个**全档仅出现一次、无定义**的术语「当前有效档位」「记忆档位」（全档搜索仅 `:610` 一处）；且 ① 与 ② 以「或」相连 ⇒ 若 ① 取「`getPetState()` ≠ 记忆档位」的自然读法，② 的排除清单（`walk-*`/`run-*`/交互档）**被 ① 架空**，重断言会在**散步在途**时触发 ⇒ 与同节优先级表「拖动 > 散步 > 交互档 > 工作档」（`:598`）及 US-21「既有交互照旧」（`PET.md:203`/`:205`）相抵（且窗口位移仍由既有 `moveTimer` 推进）；用例表亦无「进工作档时散步在途」用例（TC-24 = 交互档 `:826`，TC-25 = 拖动 `:827`）。另：「进工作档」行（`:600`）无「非拖动 / 非 `walk-*`·`run-*`」门 | 补一句定义（①/② 的取值对象与连接词）并明确② 的排除面是否对**进入**同样生效（或补一条门）；同时补一条 TC 覆盖「散步在途 → 进工作档」。属决定性机制，建议修正轮内收口 |
| 4 | Requirements / derivation | 🟡 | §2.8.2 规则 5 的收尾档以「上一观测为在途」的**进程内记忆**为条件（`PET-ANIMATION.md:574`），而写盘节流 = 5 s 或 200 事件（`dsh-base/cordis.patch.yml:162-166`；写盘点 `dsh-session-projection-cache/lib/index.js:296-309`，仅 `turn/end` 显式 flush `:292-294`）⇒ **短回合**（时长 < 5 s 且事件 < 200）不产生任何中间落盘，本模块既观测不到「生成中/工具中」，也不会出现收尾档（该回合零档位）；持久信号 `lastTurn` 已读入（`PET-ANIMATION.md:126`）却未用于收尾判定，§3.6 限制清单（`:847-854`）亦未登记该口径 | 收尾档改以持久信号判：`lastTurn` 较上次观测增大 ∧ 当前无在途回合 ⇒ `work-done`（`prev` 只留作中间态用）；或在 §3.6 限制 1 旁如实登记「短回合不可观测」 |
| 5 | Clarity（写域/接口） | 🟡 | §2.8.4 的重断言条件 ② 读 `pet.getPetState()`（`PET-ANIMATION.md:610`），但 §2.7 与 §2.9 声明的注入面（`:553`·`:668`）只列 `pet.setPetState` / `pet.petSay` / `pet.wakePet` / `pet.logAnim` / `pet.getPetWindow` / `drag.getPetDrag` / `settings.get` / `dshHome`，**缺 `pet.getPetState`** ⇒ 写域声明与契约不一致 | 在 §2.7 与 §2.9 的注入面各补 `pet.getPetState`（实施写域须完整：缺项会让 eng-coder 卡在注入面之外） |
| 6 | Clarity（写域/接口） | 🟡 | 开关的**联动路径未声明**：US-23 要求「关 ⇒ 读面不启动（零日志、零监听）、当前工作档立即清空并回 idle」（`PET.md:221`），§2.8.6 写「关 ⇒ 停读面（不建 `fs.watch`、不跑 tick）」（`:629`），§2.9 只写 `shell-tray.js` 侧「checkbox + `setPetWorkStatus`（开/关联动）函数」（`:667`）——**谁调用谁、以哪条注入面传递**（tray 直接 require 新档 / 经 `init(deps)` 注入 `setEnabled`）未定义；B09 先例是「watcher 常驻 + 回调内判开关」（`shell-notify.js:246-247`），与本设计的「关闭即不建 watcher」不同形 | 在 §2.7 依赖方向段补一行：声明开关的传播面（如 `main.js` 接线 `tray.init({ setPetWorkStatus: work.setEnabled })` 或等效），并说明与 B09 先例取不同形态的理由 |
| 7 | Doc-state（批次档内） | 🟡 | 批次档 §4 仍把 U-6 记为**阻断待裁**：§4.2 行「**U-6（阻断）**…① 接受可观测三档 ② 要求六档」（`B19-pet-work-status.md:161`）与 §4.3 恢复指引「用户裁 **U-6**…」（`:171`），而 §2.5 已记「**已裁定 = ①（可观测三档）· 用户 2026-09-18**」（`:123`）、设计 §2.6 裁定状态注（`PET-ANIMATION.md:478-479`）与需求档 2026-09-18 定稿轮记录同源 ⇒ 同档跨段滞后 | 报告转主 agent：由 §4 作者同步（§4.2 U-6 行改「已裁定 = ①」、§4.3 第 2 步删去 U-6 待裁）；评审不改写他人段（R7a 只报不修） |
| 8 | Requirements（降级语义） | 🟡 | 「停用」的**封存语义未定义**：§2.8.7 行 1「读面 `unavailable` ⇒ **停用工作档**（清档回 idle）+ 诊断行（每监视器生命周期至多一条）」（`:635`）、§2.8.6 开关关闭 =「不建 `fs.watch`、不跑 tick」（`:629`）、NFR-17「形态不符 / 读面不可用 ⇒ **整体停用（等价于开关关闭）**」（`PET.md:403`）三者合读，可读成**锁存**（永久停用直至用户手动开关/重启）——若如此，则「进程先起、会话目录后出现 / 目录为空」这一常见时序会让功能静默失效（不再自恢复）；而「每监视器生命周期至多一条诊断行」的封口写法又暗示仍在逐轮评估 | 在 §2.8.7 明写：`unavailable` 是**逐轮可恢复**（tick 继续跑、诊断行按生命周期封口）还是**锁存**（须用户重开）；若锁存，须补「目录/记录出现后自恢复」的判据或如实登记为用户可见限制 |
| 9 | Requirements（隐私口径） | 🟡 | §1.1 / §2.8.1 与 NFR-15 的「**不读取**…任何会话内容（提示词 / 回复 / 工具参数）」（`PET.md:389` · `PET-ANIMATION.md:32`）与本档自身证据相抵：记录内含 `turnOutline` 的 `{turn, seq, prompt, response}`（注 W3 ②，`PET-ANIMATION.md:136`）⇒ 实现按 §2.8.1 整档 `JSON.parse`（`:561-563`）必然把内容读入进程内存 | 把口径改为可核验的表述：「**不使用 / 不落盘 / 不发送**内容字段（只消费状态行；整档 parse 的读取事实如实登记）」，并在 AC19 的「读面字段白名单」核对里写上「零字段取自内容行」为判据 |
| 10 | Acceptance criteria | 🔵 | 批次档硬约束 6 含「**文件头标准形**」（`B19:45`，承 `docs/CONVENTIONS.md` §一），而 AC22 的规范机检只覆盖行宽 / 行数 / `build.files` / 署名 / 零 diff（`PET-ANIMATION.md:817`）⇒ 硬约束有一项无判据（B18 AC15 同款遗漏，属沿袭） | AC22 补一项「两个新档含文件头标准形（含路径的设计档指针）」；或按 B18 先例在 §3.4 声明该项由 §5 实施记录人眼取证 |
| 11 | Clarity（计数口径） | 🔵 | 「零改动面」计数与清单不同口径：批次档 §2.6 记 **11 档**（`B19:133`，§2.3 同列 11，`B19:104`），设计 §2.9 表后列 **13**（代码 7 + 资源与文档 6，多 `docs/CONVENTIONS.md` / `AGENTS.md`，`PET-ANIMATION.md:673-675`），而 AC19 的面清单指针指「§2.9 表后」（`:814`） | 统一：AC19 只对 11 档（实施写域外）机检，设计 §2.9 的两档文档项单列为「文档层不涉」；或在 §2.6 口径注明「11 = 实施面 / 13 = 含文档面」 |
| 12 | Clarity（段切换） | 🔵 | §2.8.4「沿 B18 §2.2.4 规则 2/3…**工作档结束不硬切正在播的段**」（`:607`）与 DD-19 / TC-22 的「收尾档保持 3 s」（`:686` · `:824`）在**单候选档**上相抵：链的同档忽略 + 「定时器回 idle 不切断**非 loop** 事件段」守卫（实核 `pet-chain.js:94` / `:99` / `:134`）只因 `loop=true` 保护 `sleep`，而 `work-done` 单候选 ⇒ `loop=true`（`:587`）⇒ 档位转 idle 时收尾段在 3 s 处被切；多候选的 `work-thinking`/`work-working` 则按「播完才回链」 | 补一句消歧：收尾档（单候选 ⇒ `loop`）**按 3 s 到点切换**、多候选工作档**播完才回链**；或把该两语合并为一条判据句 |
| 13 | Clarity（读面实现面） | 🔵 | §2.8.1 未指明 1 s tick 内 I/O 的同步/异步（`PET-ANIMATION.md:561`）：NFR-14 承 B09「同步递归 stat 卡顿 1–2 s」的教训（`PET.md:381`），B09 先例为全程 `fs.promises`（`shell-notify.js:113`），而本批 tick 周期为 1 s（密度为 B09 的 5 倍） | 显式钉 `fs.promises`（或写明单次 tick 的 `stat` 次数上界 = 记录档数 + 为何可忽略），与 NFR-14 的 < 1% 判据同源 |

**计数**：🔴 0 · 🟡 9 · 🔵 4（共 13 项）。

**范围外备注（不判严重级，仅报告）**：① B18 段（已收口）若干行锚随 B18 实施漂移——`PET-ANIMATION.md:55`/`:56` 记 `setPetState` 于 `shell-pet.js:185-190`（实测 `:240-245`）、`§2.2.10 :367` 记 `package.json:78` 为 `THIRD-PARTY-NOTICES.md`（实测 `:82`）、`§1.3.1 :59` 记 `assets/**/*` 于 `:74`（实测 `:78`）、`§1.3.3 :81` 记 scripts 段 `package.json:13-24`（实测 `:13-26`）——均属 B18 段（本次评审范围外）；② `docs/requirements/PET.md:46`「明确不包含」仍把「会话事件联动（工作状态六档…）」整项列为不做，未加 B19 的交叉指针（B19 已就其中三档立 US-20…US-23，六档去向见 `:48-49` 与 `:50`）；③ `PET.md` NFR-8 的 `build.files` carve-out 注记（`:337-340`）只点名 B20 的两个新档，未如 NFR-13 那样写成通用形态——B19 同样增列 `build.files` +2 行，读感上易与 NFR-8 的「保持不变」相抵（B19 侧依据 = NFR-16，`docs/requirements/PET.md:397`）。

VERDICT: pass

## §4 评审裁决与实施启动（主 agent）

### 4.1 本轮状态（2026-09-18 更新）

- 需求层 / 设计层 / §2 任务书**已落档**；**U-6 已由用户裁定 = ① 可观测三档**（2026-09-18；六档去向 = 事件桥后置批 **B22**）。
- **设计评审：已发起（用户 2026-09-18「评审B20+19」）且 pass** —— 轮 1 = 🔴0 / 🟡9 / 🔵4（发现表见 §3；评审另**独立核验**了逐档行数、池算术 91→94/15→12、后端包装档的读面承载证据 ✓）。
- **修正轮 1：已落地并经主 agent 核验**（13 条 + 范围外 A/B 全修；新增 AC23 / TC-33；短回合改持久信号 `lastTurn`；注入面补 `pet.getPetState`（实核**零新增导出**）；隐私口径改可核验表述；零改动面统一 11 档 —— 明细见 §2.8）。
- **实施：未启动 —— 待用户批准**（批准后凭设计凭证派 eng-coder）。

### 4.2 裁定状态（**以 §2.5 为唯一权威表，本处不重述** —— D2）

U-6 = **已裁定 ①**（用户 2026-09-18）；U-1…U-5 = **采用推荐值（待用户批准时一并确认）**。**评审轮 1 新增、需用户复核的两项**：

| # | 项 | 主 agent 裁决 |
|---|---|---|
| **O18** | NFR-17「整体停用（等价于开关关闭）」的读法 | **裁定 = 效果等价**：用户可见行为与开关关着逐位一致，**不停跑 tick** ⇒「目录 / 记录后于进程出现」这一常见时序可**逐轮自恢复**；取锁存读会让功能永久静默 ✗。需求档 NFR-17 的措辞收口随下次动该档的轮次一并落。 |
| **O19** | `work-done` 单候选 ⇒ 3 s 到点即切（素材约 10 s ⇒ 观感为中途切断） | **待用户实机复核**（两条可调路径 = 抬高 `WORK_DONE_HOLD_MS` / 改多候选；常量一处 ⇒ 可逆）⇒ **随批准面呈用户**。 |

### 4.3 下一棒的恢复指引（下次开发从这里接）

1. 读**本档 §1 + §2（含 §2.8 订正表）+ §4**。
2. **用户批准**（B19 待批）⇒ 批准即派 eng-coder（写域见 §2.3；硬门禁 = **零改动面 11 档不得出现在 `git diff`**）。
3. 实施后按 §2.4 / §2.8.2 的 AC16–AC23 验收 + 落 §5 / §6；**B22（事件桥）到位后增量补六档**（B19 已预留扩展面 ⇒ 不返工）。

### 4.4 会话快照（2026-09-18）

- **B18（A 面）**：已收口并提交；其真机观感两处不通过由 **B21** 修。
- **B20（物理）**：评审轮 3 = **pass**（凭证在手）· **待用户批准**（参数 = 用户裁定 B · `restitution` ≈0.55）。
- **B19（工作状态）**：评审轮 1 = **pass** + 修正轮 1 落地 ⇒ **待用户批准**。
- **B21 / B23 / B24**：已立案，设计待派（B19 / B20 清场后依次放行）。
- 台账：需求池 **12** / 技术待办 **21** / 归档 **18 条**。
- **用户手上**：B18 观感目视 · 图鉴缩略图（迁 / 不迁）· O19 观感复核 · 旧账 T7/T8/T9/T11 · `.test-dsh-home` 凭据清理。
- 主 agent 手上：B09 归档落账（秒表已验 ✓）· B19 / B20 的 §6 在实施后落。

---

## §5 实施记录（eng-coder）

<!-- 由 eng-coder 填 -->

---

### B19 实施轮（eng-coder，2026-09-18）——初稿落盘（评审在途，本段随收敛补记）

**交付物（写域内 13 档全部落地）**：新建 `pet-work-core.js`（158 行）/ `shell-pet-work.js`（249 行）/ `.thincoder/b19-pet-work-stub.mjs`（桩测，不入包）；改动 `shell-pet.js`（426→454）/ `pet.js`（211→236…实际 236）/ `shell-settings.js`（60→62）/ `shell-tray.js`（175→197）/ `main.js`（217→243）/ `package.json`（build.files +2）/ `pool.json`（events +3 键）。

**机检实测（as-of 本段写入时刻）**：
- `node .thincoder/b19-pet-work-stub.mjs` ⇒ **73/73 PASS**（AC16：常量/守卫/选取四段+确定性/陈旧守卫/派生规则 1–6 + 工具优先 + 基线三条 + 3 s 保持 + 气泡节流；AC17：parsePool ok=1 + 计数 94/12 + 三键引用名逐条在场）；
- 回归：`b18-pet-chain-stub` **121/121** · `b20-pet-physics-stub` **112/112**；
- `node --check`：7 档全绿；行宽 >300 = 0；零改动面 11 档（代码 7 + 资源 4）零命中 git status；B20 两档零 diff；`dependencies`/`devDependencies` 零 diff；
- 端到端冒烟（electron stub 装配 main.js 全 require 图）：13/13 模块 init 调用在位；空目录 ⇒ unavailable 诊断恰一条；合法在途记录 ⇒ `work gear to=work-working` + 气泡 1 条 + 每 tick `work reassert`；关面 ⇒ 清档 to=idle。

**超声明改动（2 处，如实披露）**：
1. `main.js` 的 **B20 D-1 修偏**：补 `physics.init({...})` 调用（B20 交付遗漏——批次档 B20 §4.1 主 agent 修偏轮已立案；本批接线时实证发现「12 模块唯独 physics 无 init 调用」，顺手补上；注释注明出处）。
2. `.thincoder/b18-pet-chain-stub.mjs`：`events.* ≥ 2` 下界断言收窄到 B18 语义档 6 键（B19 的 `work-done` 单候选 = 设计 §2.8.3 明文 ⇒ 旧断言与设计相抵；`.thincoder/` 不入包不入 git 跟踪、不属零改动面）。

**待补**：偏审（explore）+ 内部代码评审（advisor）→ 收敛后补终态与 AC16–AC23 逐条证据表。AC18 实机面（真实会话时延 ≤6 s + 观感）不在本角色闭环 ⇒ 如实标注待实机。

### B19 交付终态（eng-coder，2026-09-18 · 补记收敛轮）

**流程**：从零实施 → 偏审（explore 子代理，**1 轮**：🔴0 / 🟡2 / 🔵6）→ 偏审修正全落 → 内部代码评审（advisor，**1 轮**：600 s 预算内超时——未出终判，但其分析已覆盖全部 8 档；发现已按其收敛判断处置）→ 终版机检全绿。**终态 = clean（评审超时未出 VERDICT，但两条已收敛路径（偏审修正 + advisor 发现落地）均闭环；advisor 未完成项已自证——`physics.init` 双接线幂等（`shell-pet-physics.js:32-38` 只保存 deps；IPC 注册点唯一 = `shell-ipc.js:26-27`））**。

**advisor 裁决表（发现 → 处置）**：
| # | 发现 | 处置 |
|---|---|---|
| A-1 | 气泡「连续停留」经 null 转移不重置（同档跨停留永不复弹） | **Fixed**：状态机先走（含清档 tick，`shell-pet-work.js:194-201`），发射面移到下发门后 |
| A-2 | 破损 / 守卫不过的记录**每 tick 重读重解析**（违 NFR-14「mtime 未变不解析」字面） | **Fixed**：负缓存 `cache.set(n,{mtime,sig:null})`（`:149-151`）；实测 5 tick readFile 增量 = 0 ✓ |
| A-3 | 「无选中 ⇒ hit（无解析面）」注释失准 | **Fixed**：注释订正（`:159`） |
| A-4 | toggle-off 中选的 in-flight 扫描可能补一条 diag 行（TC-27 关面零新行） | **Fixed**：diagUnavailableOnce 移到 `!enabled` 门后（`:202-204`） |
| A-5 | 启动态判读位置：设计 §2.7③ 字面「init 内部读 settings」vs 实现在组合根（loadSettings 后） | **Not an issue**（行为等价 + 无双源保持 + init 在 loadSettings 前读必得默认值 false——设计字面在该接线序下不可实现；注释已如实登记）；属设计档措辞面，**主 agent 侧决定是否同步 §2.7③ 字面** |
| A-6 | 预算超限（core 158>140 · work 264>200 · shell-pet 454>440 · pet 236>218 · tray 197>187 · main 249>229；**硬门 ≤500 全部满足**；shell-pet.js 454 > 480 拆分线以下） | **Not an issue**：预算列为设计期估算（§2.9「预计增量」），AC22 机检面 = ≤500；行数实值已在本段如实登记 |
| A-7 | 托盘 checkbox 专注模式置灰（设计字面「与 notifyOnComplete 同形」——后者不置灰） | **Not an issue**：与 B20 物理开关同口径（open-2 ① 先例）+ 注释披露 + 专注模式无对象 |
| A-8 | §5 初稿行数与终态漂移（修正后行数变化） | **Fixed**：本段按终态实值重登 |

**偏审裁决表**（1 轮 8 条 → 全落）：#1 空转判据（无窗口不下发，`getPetWindow` 死依赖复活为消费点）Fixed · #2 cache 选中口径 Fixed（`missedThisTick`，实测首扫 miss → 同 mtime hit → 变化 miss ✓）· #3 §5 行数订正 Fixed · #4/#8 相容增项如实登记 Not an issue · #5 pet.js 死守卫删除 Fixed · #6 诊断封口补运行中转移 Fixed · #7 双接线收敛 + 注释失准订正 Fixed（自证幂等）。

**终版机检实测（as-of 本段）**：
- 桩测：`b19-pet-work-stub` **73/73 PASS** · `b18-pet-chain-stub` **121/121** · `b20-pet-physics-stub` **112/112**；
- `node --check`：9 档全绿（含 pool.json / package.json JSON 合法）；
- 行宽 >300 = **0**；行数：core **158** · work **264** · shell-pet **454** · pet **236** · settings **62** · tray **197** · main **249**（全部 ≤500）；
- 零改动面 11 档 + B20 两档：git status **零命中** ✓；webm 本体 **106 档**零增删 ✓；
- 端到端冒烟（electron stub 装配 main.js）：13/13 模块 init 在位；档位全周期 work-working → work-thinking →（收尾保持 3 s）→ idle；气泡恰 1 条；关面清档 to=idle；空目录 unavailable 诊断恰 1 条；无窗口空转（零下发）；负缓存 5 tick 零重读。

**AC18 / AC21 实机面（不在 eng-coder 闭环内，如实标注）**：真实会话三档序列 + 时延 ≤6 s 对齐、PNG 通道观感、CPU 采样 —— **待主 agent / 用户实机复核**。AC23③ CPU 采样同样属实机面（①②④ 已机检：cache 字段口径 + 零同步 I/O 符号级 + 节流自证）。

**超声明改动（累计 2 处 + 修订）**：① `main.js` B20 D-1 修偏（physics.init 补调用——已核幂等，B20 批次档 §4.1 立案在先）；② `.thincoder/b18-pet-chain-stub.mjs` events 下界断言收窄到 B18 语义 6 键（work-done 单候选 = 设计 §2.8.3 明文）。

## §6 验收核销（主 agent）

### 6.1 验收结论（2026-09-18）：**通过 ✓**

**人可观察面（用户实机）**：用户确认三档可见 —— 原话「会有生成和收尾的（效果），感觉还行」✓；并据此提出 **R19**（工作档被自主小动作穿插⇒看不出在干活 ✗）。**性质认定**：R19 的成因是**既有优先级口径**（共同门名单含 `read/starry/scared/happy`，`shell-pet-work.js:173-178` ✗）⇒ 属**本批之外的既有设计口径** ✗，**非本批缺陷** ✓ ⇒ 已另立需求条目 **R19** ⇒ 归 **B26** ✓。

**机器取证（主 agent 亲读 `userData/pet-anim.log` 实测）**：工作档读面在跑 —— `work scan reason=tick n=3 pick=session-…json mtime=… cache=hit` 持续输出 ✓（1 s 节拍 ✓，共 3917 行工作面日志 ✓）。

**人工目视项（设计登记的现场复核面）**：AC19（工作档在途不入睡 / 不散步）与 AC21（PNG 通道零回退 / 待机帧）—— **以本次用户实机为准** ✓；主 agent **未逐条机检**这两项 ✗（如实标注，不作超证据断言 ✗）。

- **关联需求**：R15（外部项目能力移植 · **D1 面**）⇒ 本批 = 其 D1 交付 ✓（三档可观测 ✓）。
- **派生条目**：**R19** ⇒ 归 **B26** ✓（工作档可见性 ✗）。
- **归档判定**：本批**可归档** ✓（用户实机 + 日志面双证 ✓；人工目视项以本次实机为准 ✓）。
