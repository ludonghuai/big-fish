# B26 桌宠观感修正 —— 批次档（六段制）

> 六段制 · 一段一作者（§1 / §4 / §6 = 主 agent · §2 = eng-designer · §3 = 评审子代理 · §5 = eng-coder）
> 依据：`AGENTS.md` §一 写权矩阵 + `docs/README.md`（本档为 B26 唯一权威记录）
> 用户批准（2026-09-18 15:05）：「应该修，你的建议可以的」

---

## §1 立案（主 agent）

### 1.1 目标（一句话）

**让「她正在干活」看得出来**，并**让落地踩实** —— 三条实机观感缺陷的最小修正批。

### 1.2 任务来源（用户原话）

> 「工作模式的时候会**穿插其它模式的动作**，导致动作不明显，交替起来看不出来在工作」（2026-09-18）

> 「重力模式弹跳不真实，**距离边框有一点的距离**，我觉得应该**完全等于边框**，这才真实」（2026-09-18）

> 「应该修，你的建议可以的」（2026-09-18 批准开工）

### 1.3 现状实测（主 agent 亲验，as-of 2026-09-18）

| # | 现状 | 证据 |
|---|---|---|
| 1 | 工作档让位名单含 `happy` / `eat` / `read` / `starry` / `scared` | `shell-pet-work.js:173-178`（`gateBlocked`）|
| 2 | 「待机随机小动作（看书 / 星星眼 / 惊吓 / 开心）」的档位**全在**上述名单内 | `shell-pet.js:122`（注释）+ 档位名逐条对照 |
| 3 | 散步**已有**工作档门（`petBaseState() === 'idle'` 才起步） | `shell-pet.js:287` |
| 4 | 物理边界 = **窗口矩形口径**（窗口下沿贴地 ✓） | `shell-pet-physics.js:119` · `:272` · `pet-physics-core.js:232-233` |
| 5 | 可见精灵在窗口内**内缩 26px**（×几何缩放）⇒ 脚底离窗口下沿有距离 | `pet.html` `#pet { bottom: 26px; height: 200px }` + `#pet-stage { 640×360; overflow: hidden }` |
| 6 | **勘误行（2026-09-18 评审 🔴 勘误）**：原记「工具在跑档无气泡」为假 —— 实测 `work-working` **有**文案两条、`work-done` **无**键（收尾档设计为不弹）；真因 = 气泡节流计数器**跨档全局** ⇒ 生成档必弹、工具档 30 s 内被压掉 | 实测 `pet-work-core.js:23-26` + `shell-pet-work.js:35`；详见 §2.5 / 设计档 §2.11 O21 |

### 1.4 本批做（三条，逐条可验收）

| # | 事项 | 台账 | 要点 |
|---|---|---|---|
| F1 | **工作档期间闸住「自主随机小动作」** | R19 | 与散步**同口径**（`petBaseState()` 式门）；**保留**用户触发交互（点击 / 喂食）压过工作档 ✗ |
| F2 | **落地踩实**：物理地面按「**可见脚底**」口径补偿 | R20 | 先量准内缩量（含精灵自身透明下边距 ✗）再补偿；**不动渲染层**（选项 ② 需显式论证）|
| F3 | **「工具在跑」档补气泡** | 本批新增 | 与生成 / 收尾**同形**（同档一次 / ≥30 s 节流 ✓）|

### 1.5 硬约束（七条）

1. **不对既有行为做无关改动** ✗：`shell-pet-geometry.js` / `shell-pet-drag.js` / `pet-chain.js` / `pet-chain-core.js` / `assets/**` = **冻结面**（逐档 `git diff` 为空）。
2. **零新增依赖** ✗（`dependencies` / `devDependencies` 零 diff）；不用新框架 / 不引第三方。
3. **行宽 ≤300**（不含行尾 CR）· 单档 ≤500 行 · 新增行不得把既有档推过上限。
4. **文档三层齐**：需求档（`docs/requirements/PET.md` 用户故事）+ 设计档（`docs/design/PET-ANIMATION.md` / `PET-MOVEMENT.md`）+ 本档 §2 任务书；**三方条目一致**。
5. **每条功能性需求 ≥1 用例**（正常 / 边界 / 异常），验收标准逐条回指。
6. **禁散文锚** ✗：断言只写行为面（业务可观察）与结构机检面（设计档 §2.8.4 同口径）。
7. **排期**：本批与 **B16（测试与门禁）** 写域**不重叠** ⇒ 可并行；与 `pet-chain-core.js` 相关的批次（B21 / B23）**不并行** ✗。

### 1.6 不做（范围外，逐条）

- **不动手感常数**（`restitution` 0.55 / 起飞门 T 等）—— 用户本次未提手感意见 ⇒ 维持现状；调 = 一处常量（既有「可逆变」设计 ✓）。
- **不做** 12 段未引用素材的接入（余额 / 碎碎念 / 拖拽悬空 / 工作状态 2 段）—— 归 **D2 面 / B22**（既有登记 O-17）。
- **不做** `pool.categories.weight` 的运行时消费面（是否真被选段消费 = **待核**，另立条目）。
- **不做** 六档工作状态（等待 / 成功 / 失败）—— 归 **B22 事件桥**（R17）。
- **不做** 时节的日期门 —— 归 **B23**（R16 时节门）。

### 1.7 待决（交设计者，U 项）

| # | 待决 | 备选 |
|---|---|---|
| U-1 | **F1 的门怎么加** | ① 随机小动作调度器加条件（与散步同口径 ✓）② 共同门侧排除自主触发 ③ 别的最小改动 |
| U-2 | **F2 的内缩量怎么来** | ① 常数补偿 ② 按池帧内容盒实测 ③ 起飞时一次性校准（承 `petCalibrateSize` 家族）④ 改 `#pet` 的 `bottom`（**须显式论证是否碰既有观感** ✗）|
| U-3 | **F3 气泡的文案与节流** | 与「生成 / 收尾」同表同节流；文案常量表落点（既有 US-22 面）|
| U-4 | **F1 与「交互档」的边界** | 用户触发（点击 / 喂食）保留 ✓；**自主触发**（随机小动作 / 碎碎念）让位 ✗ —— 边界以「是否由用户输入触发」为准 |

### 1.8 关联台账与排期

- 需求池：**R19**（工作档可见性）· **R20**（落地踩空） —— 本批落定后逐条勾销。
- 技术待办：无直接派生（若勘察发现组装面 / 结构面问题 ⇒ 按 T37 / T39 / T42 既有家族登记）。
- 台账联动行（收口时写）：本批 §6。

### 1.9 变更记录

| 日期 | 变更点 |
|---|---|
| 2026-09-18 | 建档（B26 立案）：来源 = 用户两句实机观察 + 批准开工（用户原话见 §1.2）；现状实测六条（§1.3）；范围三条（F1 R19 / F2 R20 / F3 气泡）；硬约束七条；不做五条；待决 U-1…U-4 |

---

## §2 本批任务书（eng-designer）

<!-- 由 eng-designer 填 -->

---

### 2.1 交付物与落点（本段 = B26 任务书；**本批尚未批准实施**）

- **前置（硬）**：本段 = **设计就绪待评审**。设计档更新已落档（`docs/design/PET-ANIMATION.md` §2.12 · `docs/design/PET-MOVEMENT.md` §2.2.13 · `docs/requirements/PET.md` 的 US-21 / US-24 B26 口径），**未经设计评审 + 用户批准 ⇒ 不得开工**；实施权在「评审通过 + 用户批准 + designToken 签发」之后。
- **前置（代码面）**：B19（工作档门 `petBaseState()`）与 B20（物理域）**已在仓**（`shell-pet-work.js` / `shell-pet-physics.js` 就位）⇒ 本批在其上追加起步门与地面补偿；**不新增依赖边 / 不新增注入面 / `main.js` 零 diff**。
- 落点（本设计者已落）：需求档（US-21 / US-24 各一条 B26 口径 + §二 一行 + 档头关联清单补全 + 变更记录 2 行）· 动画设计档（§2.12 + §2.8.4 注 D + AC24 + TC-34–36 + O20 / O21）· 位移设计档（§2.2.13 + §2.1 J 组 + DD-17 + AC21–22 + TC-38–40 + O-13 / O-14 + §3.1 注 A）。

### 2.2 本批条目（**三方一致**：批次档 §2 = 设计档验收回指 = 需求档条目）

| # | 本批条目 | 需求档条目 | 设计档验收（回指） | 状态 |
|---|---|---|---|---|
| F1 | 工作档在途期间**闸住自主随机小动作**（与散步**同口径**起步门）；用户输入触发的交互与闲聊台词照旧 | `docs/requirements/PET.md` §三 US-21（B26 口径） | `docs/design/PET-ANIMATION.md` §2.12 / §3.4 **AC24** / §3.5 **TC-34–TC-36** | 在批 |
| F2 | **落地踩实**：物理地面与散步 y 归位改按**可见脚底**口径补偿（`FEET_INSET_DIP`） | `docs/requirements/PET.md` §三 US-24（B26 口径） | `docs/design/PET-MOVEMENT.md` §2.2.13 / §3.1 **AC21–AC22** / §3.2 **TC-38–TC-40** | 在批 |
| F3 | 「工具在跑」档补气泡 | ——（US-22 口径待裁，本批不改） | ——（**未纳入本批**） | **打回（见 §2.5）** |

**计数（D3）**：本批条目 **2 在批 + 1 打回**；设计档验收 **AC 2 条（AC24 / AC21–AC22 = 3 条编号）+ TC 6 条（TC-34–36 / TC-38–40）**；需求档**无新增 US / NFR 条目**（US 28 / NFR 22 不变），仅两条 B26 口径。

### 2.3 实施面（改什么 / 怎么改）

- **F1（1 行门）**：`shell-pet.js` 的 `playIdleVariant()` 首行守卫加一个条件——`petBaseState() !== 'idle' ⇒ 直接返回`（与 `scheduleWander` 的起步门**同判据形态**、同常量来源）。
  - **保留面逐字不改**：点击 / 喂食 / 拖动 / 闲聊台词（90 s · 40%）/ 小动作时长 2.4 s / 11 档词表 / 共同门名单 / `main.js` 注入面。
  - **不做**：不新增日志行型与开关；`shell-pet-work.js` 与 `pet-work-core.js` **零改动**。
- **F2（三常量 + 一个纯函数 + 两处取值）**：
  - `pet-physics-core.js`：新增 `FEET_ANCHOR_INSET_DIP = 26` · `FEET_ALPHA_MARGIN_DIP = 0` · `FEET_INSET_DIP`（= 两者之和）+ 纯函数 `groundBounds(bounds)`（只抬 `maxY`，其余逐字不变）+ 导出。
  - `shell-pet-physics.js`：飞行 bounds 取 `core.groundBounds(geometry.petWorkAreaBounds(...))`；经 `module.exports` 暴露 `groundBounds`（供散步同口径取用，**不新增 require 边**）。
  - `shell-pet.js`：`doWander` 的 y 归位上界改用同一 `physics.groundBounds(bounds).maxY`——**必要配套**（否则落地后的 y 会被散步拉回未补偿区间 = 26 DIP 上跳）。
  - **不变面**：`throwStep` 的边界语义 / `armGate` 11 条 / 停泊序列（§2.2.6）/ `shell-pet-geometry.js` · `shell-pet-drag.js` · `pet-chain.js` · `pet-chain-core.js` · `assets/**` · `main.js` · `package.json` · 渲染层三档（`pet.html` / `pet.js` / `pet-preload.js`）。
- **实施顺序**：F2 先（常量与纯函数 → 物理域 → 散步门），F1 后（一行门 + 两处自检）；两件均可用同一开发期桩测收口（见 §2.6）。

### 2.4 受影响文件全清单（现状行数 = 换行符口径，实测 as-of 2026-09-18；**file 级**）

| # | 文件 | 现状行数 | 动作 | 预计增量 | 说明 |
|---|---|---|---|---|---|
| 1 | `pet-physics-core.js` | 334 | 改 | +14 ± 6 | F2：三常量 + `groundBounds` + 导出（纯函数，双环境导出尾巴不动） |
| 2 | `shell-pet-physics.js` | 323 | 改 | +6 ± 3 | F2：飞行 bounds 改取 `groundBounds`；导出 `groundBounds` |
| 3 | `shell-pet.js` | 454 | 改 | +4 / −1 | F1：`playIdleVariant` 守卫 +1 条件；F2：`doWander` 的 y 上界 +1 行取值 +1 行 |
| 4 | `.thincoder/b26-pet-feel-stub.mjs` | —— | **新建**（gitignored） | +60 ± 20 | 开发期桩测（装载真实实现）：常量等式 + `groundBounds` 纯函数 + 冻结面零 diff（**非交付物**，收口按纪律判退役） |
| 5 | `docs/requirements/PET.md` | 481 | 改（**本设计者**） | +16 | US-21 / US-24 各一节 + §二 一行 + 档头清单 + 变更记录 |
| 6 | `docs/design/PET-ANIMATION.md` | 949 | 改（**本设计者**） | +58 | §2.12 / §2.8.4 注 D / §2.11 O20–O21 / §3.4 AC24 / §3.5 TC-34–36 / §四 |
| 7 | `docs/design/PET-MOVEMENT.md` | 672 | 改（**本设计者**） | +56 | §2.2.13 / §2.1 J 组 / §2.2.3 常量 / §2.2.4+§2.2.6 订正 / DD-17 / O-13–O-14 / AC21–22 + 注 A / TC-38–40 / §四 |

**零改动面（机检 = 零 diff）**：`shell-pet-geometry.js`（437）· `shell-pet-drag.js`（239）· `pet-chain.js`（240）· `pet-chain-core.js`（198）· `assets/**` · `main.js`（249）· `package.json` · `pet.html`（125）· `pet.js` · `pet-preload.js` · `pet-work-core.js`（158）· `shell-pet-work.js`（264）。
**贴线档（≥480 行）**：本批写域内**无**（最大 = `shell-pet.js` 454 → ≈458；三个代码档均 < 500）⇒ **无需拆分计划**。
**行数上限口径**：`docs/CONVENTIONS.md` §五 的「单档 ≤ 500 行」**适用范围 = `.js` / `.mjs`** ⇒ 本批两个设计档（.md，949 / 672 行）不受该上限约束；行宽 ≤300 对两档**照常适用**（本批新增行已逐行核对）。

### 2.5 打回项：F3「工具在跑」档补气泡（**前提与实现冲突 + 需需求层口径裁定**）

**发现（对实现一手核对，逐条证据）**：

1. **文案已在位**：`pet-work-core.js:23-26` 的 `WORK_QUOTES` **已含** `work-working` 两条（「正在处理这一步…」「手上还有活儿在跑哦~」）⇒ 「无气泡」不是缺文案。
2. **状态机已共用**：`pickBubble()` 按档查键、`shell-pet-work.js` 的 `applyTick()` 每 tick 调同一 `bubbleState` ⇒ 生成 / 工具两档**共用同一状态机**（不存在第二套）。
3. **真因 = 节流口径**：`bubbleState.lastAt` 是**跨档全局**计数器（`shell-pet-work.js:35`）⇒ 生成档进入时必弹（此时 `lastAt` 陈旧）⇒ 工具档在 **< 30 s** 内进入时被同一窗口压掉。
4. **量化（对 `pet-work-core.pickBubble` 真实实现跑合成时间线）**：思考 20 s / 工具 5 s 交替 ⇒ 30 min 内气泡**全为生成档**（工具档 0 条）；思考 45 s / 工具 30 s ⇒ 两档各 2 条。即：**工具档只在自身持续 ≳30 s 时**才可能被报到（短工具调用结构上不可见）。
5. **次要面**：`pickBubble` 在**让位 / 无窗口**时也推进 `lastAt`（`applyTick` 里状态机先于让位 return 执行）⇒ 被吞掉的那次同样占掉窗口。

**性质 = 语义面（需求口径）**：要交付「工具在跑 档可见」，必须把 `WORK_BUBBLE_MIN_GAP_MS` 的作用域由**跨档全局**改为**按档**；
而 US-22 的「两条工作气泡间隔 ≥ 30 s」字面支持**两种读法**（全局 / 同档），两种读法下 F3 的结论相反（不可交付 / 可交付）⇒ **按纪律停下打回，不自行选一种往下写**（设计档 O21 同步登记）。

**两条读法与后果（待裁）**：

- **读法 A（按档）**：每档各自 30 s ⇒ 工具档进入即可弹（最坏 = 30 s 内 2 条，每档 1 条）；实现 = `bubbleState.lastAt` 按档存 + 同步改 US-22 一句；**F3 可交付**（追加设计：§2.8.5 修订 + AC/TC 各一条）。
- **读法 B（全局，现行实现）**：任何两条工作气泡 ≥ 30 s ⇒ 短工具调用**结构上无法被报到**；**F3 无实现面**（文案已在位）⇒ 应作废或改为「登记既有形态」。

**建议 = A**（依据：US-22 的 purpose 句「用一句短话告诉我当前进展」要求**每档**进展可报；「同档一次 / 同档 ≥30 s」仍守住「不许刷屏」，最坏 2 条 / 30 s）。
**另**：§1.3 第 6 行的现状描述与实现不符（该行称「工具在跑 档无气泡；**生成 / 收尾有**」——实为：工具档**有**文案但被节流压掉；**收尾档无文案**（`WORK_QUOTES` 无 `work-done` 键，`DD-25` 设计为不弹））⇒ 事实订正见本设计者报告（§1 为 §1 作者段，本设计者不改写）。

### 2.6 验收回指与取证（逐条可机检；判据细目 = 设计档节号）

- **AC24（F1，US-21）**：① 符号级——`playIdleVariant()` 体内 `petBaseState` 命中 ≥1 且在 `setPetState` 之前；② 运行时（视频通道）——工作档在途的日志窗口内**无** `anim slot key=events.read|starry|scared|happy` 行；③ 静态——90 s / 40% 与 2.4 s 逐字未改。
- **AC21–AC22（F2，US-24）**：① 桩测——`groundBounds` 增量 == `FEET_INSET_DIP` == `FEET_ANCHOR_INSET_DIP + FEET_ALPHA_MARGIN_DIP` ∧ `FEET_ANCHOR_INSET_DIP === 270 − PET_FEET_Y`；② 符号级——飞行 bounds 与散步 y 上界**同取** `groundBounds`（两处命中）；
  ③ 运行时——落地后 `y + PET_FEET_Y == wa.y + wa.height`（±1 DIP）；④ 零回拉——落地后显示器事件 / 松手校正路径零 `geom-fix` 行、再起飞无上跳、散步段起点 y 不变。
- **取证工具**：开发期桩测 `node .thincoder/b26-pet-feel-stub.mjs`（末行 `pass/total PASS`；非零退出即失败）+ `BIGFISH_PET_DEBUG=1` 的 `pet-anim.log` / `pet-geometry.log` / `pet-physics.log` + 静态机检（`git diff --stat` 零 diff 面 + 行宽行数）。
- **不作判据**：文字 / 文风 / 观感（观感项归 §2.7 的用户实机面）；`tests/**` 本批不新增（门禁未建，归 B16）。

### 2.7 待实机项（设计层不得自裁，交用户目视）

1. **脚底是否已踩实 + 残差是否可感**：按本批口径补偿后，可见脚底与工作区底边仍可能有 **≤ ~4 DIP** 悬空（实测残差：视频 ≈4.2 DIP / PNG 动画帧 ≈3.6 DIP；见设计档 O-13）——**用户实机目视后裁定是否把 `FEET_ALPHA_MARGIN_DIP` 由 0 调到 4**（一处常量，可逆）。
2. **混合 DPI / 多屏**：本批不改取屏与尺寸纪律，但地面位置使窗口标称矩形**越出工作区下沿 26 DIP**（透明区）——实机复核：跨屏拖动 / 显示器事件后脚底仍贴地、位置不被拉回（与 T9 的实机面合并看）。
3. **工作档可见性观感**：F1 只闸**起步**；读面清档间隙（写节流 5 s + 规则 6 的 idle 期）内自主动作仍可出现（设计档 O20）——实机确认此残余是否仍影响「看得出在干活」；若仍影响 ⇒ 需「会话活跃」新口径（另裁）。

### 2.8 待决项决断（批次档 §1.7 U-1…U-4；口径与理由逐条）

| # | 决断 | 理由 |
|---|---|---|
| U-1 | **① 起动侧门**（`playIdleVariant()` 首行守卫加 `petBaseState() === 'idle'`） | 与散步起步门同判据形态、同常量来源（口径一致）；1 行、零新增依赖；保护全部调用方；**否决**共同门侧排除（新增双源 + 事实上不阻止起步）与「摘除小动作」（删除既有行为）。依据 = 设计档 §2.12.1 |
| U-2 | **① 锚点常数（26）**，并拆成 `FEET_ANCHOR_INSET_DIP` + `FEET_ALPHA_MARGIN_DIP(0)` | 与渲染层可见身体锚点**同源**（媒体盒 / 命中区 / `#pet{bottom:26px}` 同线）；取**声明身体盒**（alpha 安全父集）下沿 ⇒ 最坏残留悬空而**不越沉**；单一常量可逆。**否决**：池帧内容盒（等值）· 起飞时一次性校准（新 IPC + 渲染层测量）· 改 `#pet{bottom}`（碰渲染层 + 两通道脚底不同线）。依据 = 设计档 §2.1 J 组 / §2.2.13 / DD-17 |
| U-3 | **文案已有、节流口径待裁 ⇒ F3 打回** | 实测：文案已在位、状态机已共用；真因 = `lastAt` **跨档全局**。要交付须改口径（需求层）⇒ 见 §2.5（两读法 + 建议 A） |
| U-4 | 边界 = **是否由用户输入触发**：用户触发（点击 / 喂食 / 拖动）**保留**；自主**动作**（随机小动作）**让位**；自主**台词**（闲聊 90 s）**本批不改** | 前两条 = F1 的实现面。第三条依据 = US-21 明列「台词照旧」+ 保护闲聊 1.5 min 参数（改它属语义面）⇒ 登记为待裁（若用户裁定「闲聊也让位」，须同轮改 US-21 并加门） |

### 2.9 范围外（本批不做，逐条）

- **不动手感常数**（`restitution` 0.55 / 起飞门 `T` / `Q 弹` 曲线）——用户未提手感意见。
- **不改渲染层与几何层**：`pet.html` / `pet.js` / `pet-preload.js` / `pet-chain*.js` / `shell-pet-geometry.js` / `shell-pet-drag.js` / `assets/**`（F2 走 `groundBounds` 与散步同口径，**不选**改 CSS 的路）。
- **不改共同门名单与优先级表**（§2.8.4 逐字不变）；**不做**「夺回已在途的自主动作」。
- **不做**气泡节流口径变更（F3 打回）；**不做** 12 段未引用素材接入（归 D2 / B22）。
- **不碰**台账 / 地图 / 其它批次面 / 本档 §1 · §3 · §4 段；`shell-pet.js` 为热点档（T38）⇒ 本批独立一轮，不与他批并写。

### 2.10 修正轮 1 订正块（eng-designer；评审轮 1 + 主 agent 裁决落地；append-only）

> 本节为 §2 的唯一修订面：凡本节与 §2.1–§2.9 冲突处，**以本节为准**（§2.1–§2.9 原文不改写）。
> 写入轮 = 修正轮 1 **续做**（2026-09-18；上一轮同任务因基础设施错误中断，已落部分不重做，本节补齐未落部分）。

#### 2.10.1 F1 机制改向（🔴 #1；主 agent 裁决：不加门，改为消掉空窗）

- **原方案作废**（§2.3 F1 / §2.8 U-1 决断的「只加起步门」）：档位在途时 `petState` 已是档位 ⇒ 既有守卫先挡、该门零增量；而症状窗口（读面清档间隙）内 `petBaseState()` 恰为 `idle` ⇒ 该门恰好放行 ⇒ 效力不落在症状上。
- **改向 = 「档位保持（主）+ 补漏起步门」**：选中记录新鲜（`now − mtime ≤ WORK_STALE_MS`，既有 60 s）期间，工作模块**不清档、不落 idle**（`deriveGear` 新增**规则 7** 保持条，维持上一档位）⇒ `petState` 恒为档位 ⇒ 自主动作被**既有** `petState === 'idle'` 守卫挡住（`shell-pet.js:124` 主 agent 亲验；散步门 / 闲聊回调同理）。
- **补漏起步门保留**（`playIdleVariant()` 首行加 `petBaseState() === 'idle'`，与散步门同形同源）：**增量效力只覆盖 ≤1 s 重断言窗**（交互档定时器到点 → 下一 tick 重断言之前；`WORK_TICK_MS` = 1 s）——如实登记，不夸大。
- **实现落点订正**（原 §2.3 只写 `shell-pet.js` 一处 ⇒ 订正为两档）：`pet-work-core.js` 的 `deriveGear` +2 行（规则 7）；`shell-pet.js` 的 `playIdleVariant` 守卫 +1 条件；`shell-pet-work.js` 的下发 / 重断言两条**逐字不变**。
- **行数预算订正**：`pet-work-core.js` 158 → ≈162（F1 规则 7 + F3 节流）；`shell-pet-work.js` 264 → ≈272（F3 状态形状）；`shell-pet.js` 454 → ≈458 不变（F1 门 + F2 取值同档）。三档均 < 500。
- **语义代价（U 项 = §2.10.6 U-5）**：`WORK_STALE_MS` 口径把「会话开着但模型空闲」也算作让位期；若用户要更严（只在回合在途 / 工具在跑时让位）⇒ 需要回合级信号 ⇒ 必须先有 Harness 侧事件桥（= B22 面），本批不自作。
- **效力边界已按改向后重写**：`PET-ANIMATION.md` §2.12.3（残余三条 + 补漏门零增量面 + 拖动断言订正）。

#### 2.10.2 F3 纳入本批（🔵 #11；主 agent 代裁 2026-09-18：节流按档 + 全局下限 10 s）

- **§2.5 的打回就此收口**：读法 A（按档）**采纳** + 全局下限；读法 B（全局）**作废**；§2.2 的 F3 行与计数行按本节订正。
- **三方一致订正（取代 §2.2 计数行）**：本批条目 = **F1**（US-21 / `PET-ANIMATION.md` AC24）· **F2**（US-24 / `PET-MOVEMENT.md` AC21–AC22）· **F3**（US-22 / AC31）——**3 条在批，0 打回**；TC 8 条（TC-34–36 / TC-38–40 / TC-48–49）。
- **节流三条件（`PET-ANIMATION.md` §2.8.5）**：① 同档一次；② 同档两条 ≥ `WORK_BUBBLE_MIN_GAP_MS`（30000）；③ 跨档全局下限 ≥ `WORK_BUBBLE_GLOBAL_GAP_MS`（**10000**，新增常量单点）——最坏 = 30 s 内 2 条（间隔 ≥10 s）。
- **状态形状**：`bubbleState` 由单值 `lastAt` 拆为 `{ gear, shown, lastAtByGear, lastAtAll }`（`shell-pet-work.js` 持有）。
- **闲聊气泡同轮处置（O21 ⑦）**：闲聊**不纳入**工作气泡节流（两套计数器分离，闲聊无节流、走既有 4 s 显示）；让位期内闲聊不弹 = 既有 idle 守卫的自然结果（**非新增门**）。
- **US-22 口径行已同步修订**（`docs/requirements/PET.md` §三 US-22：同档 ≥30 s ∧ 跨档 ≥10 s + 修正理由）。

#### 2.10.3 逐条落点（评审 #1–#12 × 处置；「已落」= 上一轮设计者落档，本轮回读核验成立）

| # | 评审发现 | 处置与落点 |
|---|---|---|
| 🔴1 | F1 效力不落症状窗口 | 改向 = 档位保持 + 补漏门（本节 2.10.1；`PET-ANIMATION.md` §2.12 重写、DD-27/DD-37）——已落 |
| 🟡2 | O20 / §2.12.3 拖动断言不符 | O20 补「增量效力 = 仅 ≤1 s」；§2.12.3 订正（`shell-pet-drag.js:146-150` 核对成立）——已落 |
| 🟡3 | AC24② 不可机检 | 改可机检窗口口径 + 桩测假时钟判据（注 C；改前树必红）——已落 |
| 🟡4 | `groundBounds` 空值契约 | 补 `groundBounds(null) ⇒ null` 入 AC21①（`PET-MOVEMENT.md` §2.2.13）——已落 |
| 🟡5 | AC22① 假失败 | 加 `vy ≥ 0` 限定 + TC-39 写死输入——已落 |
| 🟡6 | 其它落地静止点不同线 | 新增 O-15（显式取舍 = 本批不动；实机目视 = 本节 2.10.4）——已落，指针本轮坐实 |
| 🟡7 | O-14 的 `bounds` 面 | 补条件分支 + 实机复核（§2.7-2）——已落 |
| 🟡8 | NFR-19 扫描基准漂移 | 补重跑要求（`PET-MOVEMENT.md` §2.2.13 末段）——已落 |
| 🟡9 | 300–500 行档立场 | 本节 2.10.5（本轮补落） |
| 🟡10 | §1.3 第 6 行与实现相抵 | 主 agent 已在 §1.3 落勘误行（2026-09-18 评审 🔴 勘误；本轮回读确认）——已落 |
| 🔵11 | F3 打回处置 + O21 尾注 | F3 纳入本批（本节 2.10.2）——已落 |
| 🔵12 | 桩测 270 字面值 | 注 A 补来源与「几何档不可装载」限制——已落 |

#### 2.10.4 实机目视项（承 §2.7；O-15 连带，本轮补落）

- §2.7-3 的残余已被 2.10.1 收窄：清档间隙只剩「无选中记录 / 记录陈旧 / 首个工作档前」三段（与今天同形）⇒ 改测这三段。
- 新增（O-15）：初始 / 找回落点与物理地面**不同线**（脚底高 ≈50 DIP：24 边距 + 26 锚点内缩）——用户目视后裁定是否要求同线（另批 / 一处常量；`shell-pet-geometry.js` 的 `petDefaultPos` 面，本批不动）。

#### 2.10.5 300–500 行档立场（🟡 #9；照 `PET-MOVEMENT.md` §2.3 末同形）

本批三个改动代码档全部 >300 行（`pet-physics-core.js` 334 · `shell-pet-physics.js` 323 · `shell-pet.js` 454，实测 as-of 2026-09-18）——均在「>300 行 ⇒ 主动拆分复核」档内；本批对它们**只增常量 / 纯函数 / 一处取值 / 一处守卫**、均**未跨 500 行档** ⇒ **本批不启动拆分**（拆分 = 结构变更，另批；存量大档的拆分复核不属本批范围）。

#### 2.10.6 U 项（留用户批准时确认，逐条）

| # | U 项 | 内容与回正路径 |
|---|---|---|
| U-5 | 会话空闲计让位期 | `WORK_STALE_MS` 口径固有含义（§2.10.1）；更严需 B22 事件桥 |
| U-6 | 收尾档可见时长 | `work-done` 由 3 s 变「至记录陈旧（≤60 s）或至下一档」（O19 改写）；两处可调 |
| U-7 | 待机 PNG 越沉 | `FEET_ALPHA_MARGIN_DIP` = 4 ⇒ 待机 PNG 落地越沉 ≈4 DIP（O-13；回正 = 改 0） |
| U-8 | O-14 条件例外 | 任务栏置顶 / 自动隐藏 ∧ 混合 DPI 重叠 ⇒ 骑线推离可能上推窗口（复核 = §2.7-2） |
| U-9 | 让位期闲聊不弹 | 既有 idle 守卫的自然结果；若要保留须改回调守卫（语义面，本批不做） |

#### 2.10.7 已落 / 未落分界与回读实测（续做轮，as-of 2026-09-18）

- **上一轮已落（本轮回读核验成立，不重做）**：`PET-ANIMATION.md` F1 改向 + F3 全部节次（§2.8.2 规则 7/8 · §2.8.5 · §2.12 · O19/O20/O21/O27 · AC24 + 注 C · AC31 · TC-48/49 · DD-27/DD-37）；`PET-MOVEMENT.md` F2 六条（含 `FEET_ALPHA_MARGIN_DIP` = 4 ⇒ `FEET_INSET_DIP` = 30）；`PET.md` US-21 / US-22 口径行。
- **本轮补落**：① `PET.md` NFR-21 计数 6 → **9 处**（B20 六处 + B26 三处；与裁决字面 8 差一处 = US-22 的 B26 修订注记在档，D3 计数随枚举走）；② 本节（§2.10 订正块）；③ 三档 B26 面 **9 行**超宽行改写（B16 行宽门禁 ≤300，语义零变更）；④ `PET-MOVEMENT.md` §2.2.13 落地等式的「等价表述」符号订正（可见脚底 = `y + PET_FEET_Y −` 残差）。
- **未落（他人段，报告不改）**：批次档 **§4**（主 agent 裁决段现为空——裁决内容未落档，待主 agent 补）；本档 §1.3 第 6 行勘误已由主 agent 落（本轮回读确认）。

## §3 设计评审（评审子代理）

<!-- 由评审子代理填 -->

---

### 轮次 1（评审子代理）

**证据核验摘要（一手核对，as-of 2026-09-18；代码面只读）**：F2 的锚点与方向**成立**——`PET_SIZE_DIP.h = 270`（`shell-pet-geometry.js:29`）· `PET_FEET_Y = 244`（`pet-chain-core.js:16`，已导出 `:193`）· `#pet { bottom: 26px; height: 200px }` / `#pet-wrap 250×270`（`pet.html:13-14` / `:33-36`）⇒ `FEET_INSET_DIP = 26` 同源、`maxY += 26` 方向正确（落地后 `y + PET_FEET_Y == wa.y + wa.height` 自洽）；「消费点两处」（`shell-pet-physics.js:119` 飞行 bounds、`shell-pet.js:306/315` 散步 y 归位）与现状代码一致，无第三处内联。F3 的**打回成立**（见 #11）。F1 的**门位选择**本身不动优先级表 / 共同门（`shell-pet-work.js:176-177` 零改动），但**其效力范围与立案前提不符**（见 #1）。

| # | Category | Severity | Issue | Suggestion |
|---|----------|----------|-------|------------|
| 1 | Requirements coverage | 🔴 | **F1 在症状发生的窗口里不可能生效**。`playIdleVariant()` 的既有守卫已要求 `petState === 'idle'`（`shell-pet.js:124`），其唯一调用方（闲聊回调）也先判 `petState === 'idle'`（`shell-pet.js:136-138`；唯一入口 `main.js:210`）；而工作档在途时工作模块每 tick 把 `petState` 置回档位（`shell-pet-work.js:208-223`）⇒ 自主动作**早已被挡住**。视频链同理只在 `idle` 档运转（`PET-ANIMATION.md:285`）⇒ 用户看到的「穿插其它模式的动作」只可能发生在档位**不在场**的时刻（读面清档 / 回合起始 ≤5 s 延迟 / 陈旧清档 ⇒ `petBaseState()` 已是 `'idle'`），而这正是 §2.12.3 / `PET-ANIMATION.md:802-804`（O20）自认封不住的那段。⇒ 新增那道门的增量效力只剩「交互档结束 → 重断言」的 ≤1 s（`shell-pet-work.js:219-222`，≈ 每 90 s 一次的闲聊触发命中率 1% 量级），对 R19 / 批次档 §1.1「让『她正在干活』看得出来」几无可观察效力；设计却把它当核心交付，只把残余留给 §2.7-3 的**实施后**目视裁决 | 实施前把 F1 拉回需求层（与 F3 同类处置）：① 若目标就是「看得出在干活」，门判据须换成在清档间隙仍非 idle 的更宽信号（O20 自己给出的候选 = 选中记录新鲜 `now − mtime ≤ WORK_STALE_MS`）——属需求层新口径，须用户裁定；② 若接受窄口径，则把 §1.1 / F1 目标与 AC24 的预期效力同源写明（明确「只关闭 ≤1 s 重断言间隙」）并修正 §1.3 第 2 行的前提读法。放行本项的**唯一**路径 = 用户在**批准前**明确接受「F1 = 窄口径修正」 |
| 2 | Clarity / 效力边界登记 | 🟡 | 核心审查面②部分不成立：O20 登记了「射程外」窗口，但**未登记 F1 买到了什么**（既有 `petState === 'idle'` 层已挡住档位在途期的自主动作、闲聊回调自身亦要求 idle）⇒ 读者会以为除清档间隙外 F1 已交付；且 §2.12.3（`PET-ANIMATION.md:836`）的「本门关闭的窗口 ① 拖动在途（`gateBlocked` 让位期、`lastGear` 未更新）」与代码不符——拖动在途且档位在途时 `petState` 仍 = 档位（`shell-pet-drag.js:150` 只复位 `walk-*`/`run-*`）⇒ 已挡；档位不在途时该门无对象可挡 | O20 / §2.12.3 补一句「F1 的**增量**效力 = 仅 ≤1 s 重断言间隙」；对 `shell-pet-drag.js:146-150` 一手核对后删去或订正 ① 的拖动窗口断言 |
| 3 | Acceptance criteria | 🟡 | AC24②（`PET-ANIMATION.md:938`）不可操作：「工作档在途的**日志窗口**」无法用日志界定——让位期（`shell-pet-work.js:207`）恰恰不写 `work gear` / `work reassert` 行；而档位在途期本就不可能出现小动作行（见 #1）⇒ 该判据在**改前的树上同样通过**，B26 的可判别证据只剩静态的 AC24① | 把窗口口径写成可机检形式并点名（以 `petBaseState()` 非 idle 的区间为准，或用「`work gear|reassert` 行 + 相邻行间隔 > 1.5 s 的让位间隙」），并补一条**能区分改前 / 改后**的运行时判据（例：构造「交互档结束 → ≤1 s 内闲聊触发」用例，或桩测注入假时钟） |
| 4 | Clarity（契约完备性） | 🟡 | `groundBounds` 的空值契约未写：设计给的形状是对 `bounds` 展开取 `maxY`（`PET-MOVEMENT.md:494`），而接入点之一**可能传 null**——飞行 bounds 由 `win && !win.isDestroyed() ? … : null` 派生（`shell-pet-physics.js:119`），G8 的 `no-bounds` 分支（`pet-physics-core.js:300` / §2.2.10 行 3）依赖该 null 继续成立；按字面实现对 null 展开即抛错。散步侧虽先判空（`shell-pet.js:307`），设计给出的接线字面（批次档 §2.3）亦未体现该守卫 | 在 §2.2.13 的纯函数契约补 `groundBounds(null) ⇒ null`（与 G8 同源），并纳入 AC21① 的桩测断言面 |
| 5 | Acceptance criteria | 🟡 | AC22① 不可判定（`PET-MOVEMENT.md:645`：「落地后原地再起飞，首帧 `phys-tick` 的 `y` 不减小」）——**向上抛**时首帧 `vy < 0` ⇒ y 合法减小（`shell-pet-physics.js:195-215` / `pet-physics-core.js:232-242`）⇒ 用例未限定方向 / 初速，会出假失败 | 给判据加条件（`vy ≥ 0` 的平抛 / 斜下抛用例，或改述为「`y` 不小于 `groundBounds(wa).maxY`」这种与抛向无关的表述），并在 TC-39 写死输入 |
| 6 | Requirements coverage / 登记完备性 | 🟡 | F2 的「地面单一口径」只在飞行 + 散步 y 归位两条路径成立，**其它落地静止点仍用未补偿口径**：`petDefaultPos()` = `bounds.maxY − 24`（`shell-pet-geometry.js:134`），供「存档不可见 / 损坏回落」（`:160-164`）与找回（`shell-pet.js:146-153`）使用 ⇒ 这些落点脚底比工作区底边高 ≈50 DIP（再加 alpha 残差），与落地「踩实」不同线；另有救援钳入 `petNearestVisiblePos` 用未补偿 `maxY`（`shell-pet-geometry.js:126`）。设计（含 O-13 / §2.7）只登记了 ≤4 DIP 的 alpha 残差 | 补一条观察项或显式取舍（「初始 / 找回落点保持 B03 的 24 DIP 边距，本批不动」），并纳入 §2.7 的实机目视面，避免用户读到「落地贴地、找回又悬空」 |
| 7 | Document ownership / 跨档指针 | 🟡 | O-14（`PET-MOVEMENT.md:608`）把例外登记为对 B03「**标称矩形 ⊆ 工作区**」的一处具名例外；但代码里 rect-inside 判据的唯一消费方是 `petRectInside()`（`shell-pet-geometry.js:250-254`）→ `petStraddleFix()` 的 `display.bounds`（`:286`），且该处越界是**条件性的**：当该屏 `workArea` 底边 == `bounds` 底边（任务栏置顶 / 自动隐藏）时窗口下沿越出 `bounds` 26 DIP，若与另一块 `scaleFactor` 不同的屏重叠 ⇒ `petStraddleFix` 上推窗口（`kind='straddle'` ⇒ `geom-fix`），正好抵消 F2 并使 AC22③/④ 在该配置下不成立；O-14 只对 `petIsVisible`（中心点）做了论证。（B03 档不在本次评审范围 ⇒ 其字面口径 **unverified**） | O-14 同时点明 `bounds` 面与该条件分支，并把它列入 §2.7-2 的多屏实机复核项（跨屏拖动 / 显示器事件后脚底仍贴地、零 `geom-fix`） |
| 8 | Numeric drift（跨档数值） | 🟡 | F2 把地面下移 26 DIP ⇒ `NFR-19` 两支判据的**扫描基准**（`PET.md:437`：≤4 s 74 例 / 4–5 s 44 例 / 上界 10 例；档界 800 px/s；档内最坏 3.33 s）是按旧地面重算的：慢落冲击速度约 `sqrt(v² + 2·1400·26)`（800 → ≈844 px/s，≈+5%）⇒ 边缘例跨档。设计（`PET-MOVEMENT.md:634` 的 AC11 行 / §2.7）未提此事 | 在 §2.2.13 或 AC11 行加一句：落地后扫描集与两档判据须按新地面**重跑**（或声明 26 DIP 影响可忽略并给出余量），避免收口时以旧基准判绿 |
| 9 | Affected-file size annotations | 🟡 | 批次档 §2.4 只回答 ≥480 / 500 档（「贴线档内无 ⇒ 无需拆分计划」），未覆盖本批三个改动代码档都在 **>300 行 → 主动拆分复核**档这一事实（实测：`pet-physics-core.js` 334 · `shell-pet-physics.js` 323 · `shell-pet.js` 454，全部 >300；本批改动不跨 500 档） | 照 `PET-MOVEMENT.md:527`「300–500 行档立场」同形补一句（三档只增常量 / 纯函数 / 一处取值，未跨 500 ⇒ 本批不启动拆分，拆分另批） |
| 10 | Doc-state（§1 与实现相抵） | 🟡 | §1.3 第 6 行「『工具在跑』档**无气泡**；**生成 / 收尾有**」与实测相抵（`pet-work-core.js:23-26`：`work-working` **有**两条文案、**无** `work-done` 键 ⇒ 收尾档设计为不弹）；设计者已在 §2.5 末段如实报告但无权改 §1（主 agent 段）⇒ 该行仍在档内误导立案前提 | 由主 agent 在 §1（或收口轮）订正该行事实（工具档 = 文案在位而被跨档节流压掉；收尾档 = 设计不弹），与 §2.5 / O21 同源。报告项，不影响交付判定 |
| 11 | Note（F3 打回处置） | 🔵 | F3 打回**成立**：US-22「两条工作气泡间隔 ≥ 30 s」（`PET.md:220`）字面支持全局 / 同档两读；实测真因 = `bubbleState.lastAt` 跨档共享（`shell-pet-work.js:35`）+ `pickBubble` 同 `state` 对象（`pet-work-core.js:135-147`），且 `applyTick` 把气泡状态机排在让位 / 无窗口 return **之前**（`shell-pet-work.js:196-207`）⇒ §2.5 第 4 / 5 点逐条核实无误 | 仅提示：建议 A（按档）把「任意两条 ≥30 s」降为「最坏 2 条 / 30 s」（§2.5 已如实写明），且与 `PET-ANIMATION.md:809`（O21 尾注「闲聊气泡共用同一元素」）需**同轮**处置 |
| 12 | Note（桩测断言面） | 🔵 | AC21① 的 `FEET_ANCHOR_INSET_DIP === 270 − PetChainCore.PET_FEET_Y` 中 `PET_FEET_Y` 可真实装载（`pet-chain-core.js:16` / `:193`），但被减数 **270** 无法从真实实现取（`PET_SIZE_DIP` 在 `shell-pet-geometry.js:29`，该档 `require('electron')`（`:7`）⇒ node 不可装载）⇒ 桩测只能写字面 `270`（弱镜像断言） | 桩测注明该字面值的来源与「几何档不可装载」限制，或改由可装载来源表达该等式，避免 `PET_SIZE_DIP` 变更后等式静默失效 |

**计数**：🔴 1 · 🟡 9 · 🔵 2 = 12（严格按 4 项核验面判：#1 = 核心面①、#2 = 核心面②、#6/#7 = 核心面③、#3 与 #5 = 核心面④、#11 = 核心面⑤）。
VERDICT: changes-required

## §4 评审裁决与实施启动（主 agent）

### 4.1 裁决表（轮次 1 · §3 全 12 条 · 2026-09-18）

| # | Action | Detail |
|---|---|---|
| 1 🔴 | **Fixed**（经修正轮 1 ✓） | **F1 改向**（不加门 ✗ → 消掉空窗 ✗）：工作档在「读面清档间隙」内**不落 idle**（判据 = 记录新鲜 `now − mtime ≤ WORK_STALE_MS`）⇒ `petState` 恒为档位 ⇒ 自主动作被**既有** `petState === 'idle'` 守卫挡住（`shell-pet.js:124`，主 agent 亲验）；**语义代价**（「会话开着但模型空闲」也算让位期）= U 项，留用户批准时确认（§2.10.6） |
| 2 🟡 | **Fixed** | O20 补「F1 增量效力 = 仅 ≤1 s 重断言窗」；§2.12.3 「拖动在途」那条与代码不符的断言已订正/删去（`shell-pet-drag.js:146-150` 一手核对） |
| 3 🟡 | **Fixed** | AC24② 改为可机检窗口口径 + 补**能区分改前/改后**的运行时判据（原判据在改前树上同样通过 ✗） |
| 4 🟡 | **Fixed** | `groundBounds(null) ⇒ null`（与 G8 `no-bounds` 同源）⇒ 入 AC21① 桩测断言面 |
| 5 🟡 | **Fixed** | AC22① 加条件（`vy ≥ 0`）+ TC-39 写死输入（向上抛时首帧 y 合法减小 ⇒ 原写法会假失败 ✗） |
| 6 🟡 | **Fixed** | 找回 / 存档回落落点未补偿（`petDefaultPos` = `maxY−24`）⇒ 新增 **O-15** 显式取舍（本批不动）+ 实机目视面 |
| 7 🟡 | **Fixed** | O-14 补 `bounds` 面 + 任务栏置顶/自动隐藏的条件分支（`petStraddleFix` 会上推 26 DIP ⇒ 正好抵消 F2）⇒ 列入多屏实机复核项 |
| 8 🟡 | **Fixed** | 地面下移 26 DIP ⇒ `NFR-19` 扫描集须按新地面**重跑**（+6% 冲击速度；余量 0.67 s 核算入档） |
| 9 🟡 | **Fixed** | 受影响文件表补「300–500 行档立场」（三档 334/323/454 均 >300，不跨 500 ⇒ 本批不启动拆分） |
| 10 🟡 | **Fixed**（主 agent 亲落） | §1.3 第 6 行事实勘误（`work-working` **有**文案 / `work-done` **无**键；真因 = 跨档节流）—— §1 = 主 agent 段 |
| 11 🔵 | **Fixed** | **F3 打回成立** ⇒ 主 agent 代裁：**F3 纳入本批** + 节流改**按档**（每档 ≥30 s）+ **全局下限 10 s**；与 O21 尾注（闲聊气泡共用元素）**同轮**处置 |
| 12 🔵 | **Fixed** | AC21① 桩测的 `270` 注明来源 + 「几何档 `require('electron')` 不可装载」限制 |

计数：🔴 1 · 🟡 9 · 🔵 2 = 12（全部收敛为 Fixed ✓）。

### 4.2 修正轮 1（派发与核验）

- **派发**：eng-designer 修正轮 1（承 §3 全 12 条 + 主 agent 代裁 F2/F3）⇒ 中途遇**基础设施错误**（推理内容回传 400）⇒ 换模型续做同任务 ⇒ 完成 ✓
- **主 agent 核验**（一手跑 ✗ 非采信自报 ✓）：
  - `npm run lint` ⇒ **PASS** checks=6 selftest=14/14（width PASS / lines PASS / dag PASS(0 环) / fanout PASS(frozen=6) / assembly PASS(13/13+2)）✓ —— 本轮压掉 3 档 **10 行**超宽 ⇒ 门禁由 FAIL 转 PASS ✓
  - **三方一致**：批次档 §2.10.2 = 设计档回指 = 需求档条目 ✓
  - **已落核验**：F1 改向（§2.8.2 规则 7/8 · §2.12 全重写 · DD-27/DD-37 · O19/O20/O27 · AC24+注C · TC-48）· F2 六条（`PET-MOVEMENT.md`）· F3（§2.8.5 三条件节流 · AC31 · TC-49 · O21 ①–⑦）✓
  - **代码档零 diff** ✓（修正轮只写文档）
  - 修正轮新增发现：§2.2.13「等价表述」代数符号订正（`+` ⇒ `−`）✓ · NFR-21 口径计数 = **9 处**（B20 六 + B26 三；与初判 8 的差异已在档内说明）✓

### 4.3 实施启动（**待用户批准**✗）

- **待批准项**：本批设计（F1 改向 / F2 落点口径 / F3 节流）+ **§2.10.6 的 U 项**（含 F1 新语义「会话开着但模型空闲是否算让位期」⇒ 用户可感面 ✗）
- **批准后**：派 eng-coder（持设计凭证）实施三件 —— ① F1（工作档让位时间信号）② F2（`FEET_INSET_DIP = 30` + `groundBounds`）③ F3（按档节流 + 全局下限）
- **实施后机检凭证**：B16 门禁（lint 六判据）+ `test:full` + `test:integration` + 实机目视（用户）

### 4.4 本批期间的实机发现（已单列，不阻断本批）

- **集成测试弹真通知** ✗（用户撞见「发现 Harness 新版本 v0.1.9」）⇒ 已由 B16 修偏（`BIGFISH_TEST_NO_NOTIFY`，默认关）✓ 非 B26 面 ✓
- **T43 登记**（文档面热点：`PET-ANIMATION.md` 多批共用 ⇒ 设计链串行 ✗）✓ 非本批面 ✓

---

## §5 实施记录（eng-coder）

<!-- 由 eng-coder 填 -->

---

## §6 验收核销（主 agent）

<!-- 由主 agent 填 -->
