# B12 —— 插件市场的安全与阻塞三项

> 六段制批次档（每段一位作者、append-only）：§1 主 agent · §2 eng-designer · §3 评审子代理 · §4 主 agent · §5 eng-coder · §6 主 agent。

## §1 批次立案（主 agent）

### 1.1 立案背景

接手代码面只读体检（子代理 2026-09-17）报出三项互相咬合的缺陷，**主 agent 已逐条亲读源码验证**（非采信报告）：

**① 🔴 路径穿越 ⇒ 任意目录递归删除**（`shell-plugins.js`）

| 行 | 事实 |
|---|---|
| `:312` | `bundledName = String(spec).replace(/^builtin:/, '')` —— 入参 `spec` **零校验** |
| `:313` | `bundledSource = path.join(bundledPluginsDir(), bundledName)` —— `path.join` **归一化 `..`** |
| `:314` | `if (fs.existsSync(bundledSource))` —— 逃逸路径（盘根/上级）必然存在 ⇒ **进门** |
| `:316-319` | `target = path.join(profileDir(), 'node_modules', bundledName)` → `fs.rmSync(target, {recursive, force})` → `fs.cpSync(bundledSource, target, {recursive})` |
| `:358-362` | `uninstallPlugin(pkgName)` **同形**（同样零校验 + 递归删） |

- **影响面**：`target` 可归一化至 **`<DSH_HOME>`（`~/.dsh`）或更高**——含 `sessions/` · `profiles/` · **`.credentials.yaml`** · `settings.yaml` ⇒ **递归删除用户全部会话与 API 凭据**；随后 `cpSync` 再把逃逸源整目录拷入。
- **可达链**：`market:install` IPC（`shell-ipc.js:25`）← 渲染层 `marketAPI.install`（`market-preload.js`）← **用户在插件市场点条目**（spec 来自**第三方注册表** `awesome-dsh-plugin.com`，实测 3727 条）。
- **修复面小**：同档**已有** `isPlainPackageName()`（`:99-106`）并被 `addBundle` / `marketEnable` 使用 ⇒ 两处各加一道门 + 归一化后越界校验。

**② 🔴 注册表数据经 `innerHTML` 注入（XSS）⇒ 直连 ①**

- `market.js:286-290`：`confirmModal(title, html)` → `body.innerHTML = html`。
- 调用点 `:296-302`：把 `${p.name}`（第三方注册表字段，**未消毒**）插值进该 HTML。
- ⇒ 恶意条目的 `name` 含 `<img src=x onerror=…>` 时，**用户点开即在该渲染进程执行脚本**；该进程持有 `marketAPI`（install / uninstall / restart / update）⇒ **无需额外动作即触达 ①**。

**③ 🔴 主线程同步阻塞（可感知卡顿的直接根因）**

- `shell-plugins.js:152-177`：循环体内**逐项**调用 `resolveInstalledName(spec)`（`:109-123` → `:111 listInstalledPlugins()` = `readdirSync` + 逐项 `statSync`）与 `isPluginInProfile`（→ `:52` 同步读 + `JSON.parse`）。
- 注册表实测 **3727 项** ⇒ 每次 `market:list` / `market:state` 约 **3.7k 轮同步 fs**，全部跑在 **Electron 主线程**（与桌宠 16 ms 移动循环、拖动 8 ms tick 争线程）。
- 同类先例：B07 刚退役的 `~/.dsh` 同步全树遍历（NFR-5）。

### 1.2 任务来源

- 用户 2026-09-17：「我的项目是后来接手的……**检查代码是否有优化改进空间**」+「**听你的，你可以有非常合理的安排**」。
- 承接：需求池 **R11**（接手项目规范化）第 0 步「只读体检」的**前三条结论**落地批。
- 台账：**T18**（穿越）· **T19**（阻塞）· **T20**（XSS）——本批承载。

### 1.3 范围

**做**：

1. **入参门与越界校验**（`shell-plugins.js`）：`installPlugin` / `uninstallPlugin` 对 `spec` / `pkgName` 走 `isPlainPackageName` 门；**并在 `path.join` 之后做归一化越界校验**（`target` 必须落在 `profileDir()/node_modules` 之下；`bundledSource` 必须落在 `bundledPluginsDir()` 之下）——白名单是主防线，越界校验是纵深防御（防未来形态变化）。
2. **市场弹窗文本化**（`market.js`）：把注入 `innerHTML` 的插值改为文本节点（或对插值转义）；**UI 外观与文案保持不变**。
3. **扫描提到循环外**（`shell-plugins.js` 的 `computePluginUpdates`）：单次扫描 + 复用；**判定语义与逐项结果逐字不变**。

**不做（边界）**：

- 不改 IPC 通道名 / 参数契约 / 返回值形态；
- 不改 `market:update-all` 的编排语义、不改注册表来源与回退链；
- 不引入**新依赖**、不新增构建步骤；
- 不做「注册表签名校验 / 宿主白名单」等更大机制（另议；体检已记录在更新链信任面）。

### 1.4 验收标准（初拟，供设计细化并给可机检判据）

| # | 验收标准（可机检 / 可桩测） |
|---|---|
| AC1 | `installPlugin('../../..')` / `uninstallPlugin('../../..')` ⇒ `ok:false`，且 **fs 零改动**（桩测断言目标未被删/拷） |
| AC2 | `isPlainPackageName` 门覆盖负例：`..` · `../..` · `/abs` · `C:\` · `github:` · `link:` · `file:` · `@scope/..`；正例：合法 npm 名 · `@scope/name` 放行 |
| AC3 | 越界校验独立有效：构造绕过白名单的入参 ⇒ `target` 不在 `node_modules` 之下时拒绝，**不执行任何 fs 写/删** |
| AC4 | 恶意 `name`（含 HTML/script 形态）在市场弹窗中**只以文本呈现**、不执行（桩测：断言无新增元素节点） |
| AC5 | `computePluginUpdates` 对 N 项注册表只做**常数次**目录扫描（计数器桩断言），且**逐项判定结果与改前一致**（同输入同输出对照） |
| AC6 | 零新依赖 · 改动 js 全部 `node --check` 绿 · 市场既有文案/交互不变 |

### 1.5 关联与时效

- 需求档：`docs/requirements/SHELL.md`（归属由 eng-designer 判定；US-7 附近为插件域）
- 设计档：`docs/design/SHELL-UX.md`（落点由设计判定；**不得修改 B07 已核销判据行**，B09/B10 正在同档追加，只做追加不重排）
- **时效**：用户尚未发布 v0.0.1（台账 T11）⇒ **建议本批在发版前完成**（发布即带该路径）

---

## §2 本批任务书（eng-designer）

### 2.1 本批条目（三方一致源——硬）

| # | 本批条目 | 需求档条目 | 设计档验收 | 交付面 |
|---|---|---|---|---|
| I1 | 入参三形态白名单门（`installPlugin` / `uninstallPlugin`，覆盖 `market:install` / `uninstall` / `update` / `update-all`） | `docs/requirements/SHELL.md` **US-14** | `docs/design/SHELL-UX.md` §3.1 **AC26 · AC27** | `shell-plugins.js` |
| I2 | `path.join` 之后越界校验（`target` ⊂ `profileDir()/node_modules`；`bundledSource` ⊂ `bundledPluginsDir()`；词法判定、不做 `realpath`） | **US-14** | **AC27** | `shell-plugins.js` |
| I3 | 拒绝消息形态（`{ok:false,message}`，**不含绝对路径**，只回显入参前 60 字符） | **US-14** | **AC31** | `shell-plugins.js` |
| I4 | 市场弹窗文本化（节点构造 + `innerHTML` 清零；外观与文案逐字不变） | **US-14** | **AC28** | `market.js` |
| I5 | 扫描提到循环外（单次请求一次快照 + 复用；逐项判定零回退） | **US-15** | **AC29** | `shell-plugins.js` + `shell-market.js` |
| I6 | 零回退面（零新依赖 / 语法绿 / 四文件零 diff / `market.js` ≤ 500 行 / IPC 契约零改动） | **US-14 · US-15 · NFR-3 · NFR-6 · NFR-7** | **AC30** | 全部改动面 |

> **三方一致**：§2 本批条目（I1–I6）= 设计档 **AC26–AC31** 回指条目 = 需求档 **US-14 / US-15**（并含 NFR-6 / NFR-7）。
> §1.4 的 AC1–AC6 → 设计 AC26–AC31 的映射：**AC1→AC26 · AC2→AC27 · AC3→AC27 · AC4→AC28 · AC5→AC29 · AC6→AC30**；**AC31** = 本批新增（拒绝消息形态——§1 交设计裁定项「拒绝时的错误消息与返回值形态」的落点，§1.4 未列）。
> 需求档归属 = `docs/requirements/SHELL.md`（判定见 §2.7）；设计档落点 = §2.1 选型 M（M-1…M-5）+ §2.2.14（机制节）+ §3.1 AC26–AC31 + §3.2 TC-68–TC-86 + §3.3 手段 13。

### 2.2 §1「交设计裁定项」逐项裁定

| # | §1 交裁定项 | 本批裁定 | 依据（设计档落点） |
|---|---|---|---|
| ① | 两层防御的形态：哪层是主防线 / 判据句 / 拒绝形态 / 是否抽公共 helper | **主防线 = 入参三形态白名单**（复用 `isPlainPackageName`；形态 = `builtin:`+纯包名 / 裸纯包名 / `github:owner/repo[#片段]` / `[scope/]name[@版]`）；**第二层 = `path.relative` 词法包含判定**（纵深，防形态演进）。（余文见 §2.9 注①） | §2.2.14「入参门判据句」/「越界校验判据句」/「两层关系」/「拒绝消息形态」；DD-41 · DD-42 · DD-44 |
| ① 附 | 白名单对真实数据的误拒面（本批风险点） | **3725 个推导标识实测零误拒**（含 1832 个 `github:`、491 个 scoped、**139 个 `github:…#path:/…` 子目录形态**）；注册表 `npm` 字段非纯包名条目 = 0；`version` 形态异常 = 0 | §2.2.14「零误拒实测」（设计者本轮亲跑；含一次修订教训——`#` 片段必须放开 `[A-Za-z0-9._:@/-]`，否则误拒 139 条） |
| ② | XSS 修复形态 + 同类 `innerHTML` 扫描 + 「外部数据 vs 常量」判据 | **结构面修**：`confirmModal(title, okLabel, ...parts)` 改收节点（`frag()` 收字符串/节点；字符串永走 `createTextNode`）（余文见 §2.9 注②） | §2.2.14「弹窗文本化判据句」+「外部数据 / 常量裁定表」；选型 M-3；DD-43；C39 |
| ③ | 阻塞修复形态 + 「判定结果与改前一致」对照判据 + 是否引入短期缓存 | **请求内单次快照 + 复用**（`scanProfile()` ⇒ `{names,nameSet,bundles,entries,versions}`，经**可选** `ctx` 尾参下传）；（余文见 §2.9 注③） | §2.2.14「扫描契约」+「等价性论证」（`names` 逐条等价 / `isPluginInProfile` 深度 ≤ 2 等价 / `installedPluginVersion` 记忆）；选型 M-4；DD-45 |
| ④ | AC + TC 细化（可机检 / 可桩测） | **AC26–AC31**（判据全文见 §3.1，枚举面见该表下「B12 判据细化」注①–③）+ **TC-68–TC-86**（正常 / 边界 / 异常；含穿越形态枚举负例、恶意 `name` 三形态、N=3727 计数对照） | §3.1 / §3.2 |
| ⑤ | 是否落回归测试 | **落** `tests/b12-plugin-guards.test.js`（`node --test`；**零第三方框架 / 零依赖**：`node:test` + `node:vm` + `node:fs`）；运行命令 = `node --test tests/b12-plugin-guards.test.js`；**不登记 `package.json`**（门禁接入属 T4）、**不入 `build.files`**。寿命口径 = ① 开发期工具（批次收口逐条判处置，默认退役——落批次档 §6） | 选型 M-5；DD-46；§3.3 手段 13 |
| ⑥ | 受影响文件清单 + 需求档条目 | 见 §2.5；需求档 = US-14 / US-15 + NFR-6 / NFR-7（**未改写任何既有已核销条目**；B07 已核销判据行零改动） | §2.3 B12 表 / §2.5 |

### 2.3 覆盖需求（US-14 / US-15 / NFR-6 / NFR-7 逐条 → 设计落点）

| 需求契约点 | 设计落点（`docs/design/SHELL-UX.md`） |
|---|---|
| US-14 入参门（三形态 + 覆盖四条 IPC 链 + 零误拒） | §2.2.14「入参门判据句」+ 零误拒实测 + §3.1 AC26·AC27 |
| US-14 越界校验（严格包含 / 词法 / 不 `realpath`） | §2.2.14「越界校验判据句」+ 两层关系蕴含论证 + §3.1 AC27 |
| US-14 弹窗文本化（`innerHTML` 清零 / 外观文案不变） | §2.2.14 弹窗判据句 + 外部数据/常量表 + §3.1 AC28 |
| US-14 拒绝消息形态（不含绝对路径） | §2.2.14「拒绝消息形态」+ §3.1 AC31 |
| US-15 扫描次数与 N 解耦 | §2.2.14「扫描契约」+ §3.1 AC29 |
| US-15 判定零回退（黄金样本 + 日志行不回退） | §2.2.14 等价性论证 +「取证锚点不回退」+ §3.1 AC29 |
| NFR-6 不可信输入面（不进路径拼接 / 不进 HTML 解析面） | §2.2.14 全节 + NFR-6 度量方式（静态 + 桩测 + 真机） |
| NFR-7 扫描开销解耦（计数器判据） | §2.2.14「扫描契约」+ NFR-7 度量方式（N=2 与 N=3727 计数相等） |

### 2.4 明确出批（本批不做）

- 不改 IPC 通道名 / 参数契约 / 返回字段（`shell-ipc.js` 零 diff）；不改注册表来源与回退链；不改更新判定语义（入选条件 / 版本比较 / `github:` 重装口径 / 日志行）；
- 不做**注册表签名校验 / 更新 URL 宿主白名单**（体检另记，属另批）；不做 `<a href>` 协议守卫与市场窗口 `will-navigate` / `setWindowOpenHandler` 守卫（登记 **O20**）；不改 `market.html`（含其 CSP 与文案）；
- 不改 `exchange.js`（常量源，判定见 §2.2 ②）；不改 `market-preload.js` / `market-update.js` / `plugins.json`；
- 不做 `realpath` / 符号链接感知（登记 **L-B12-1**）；不做异步化改造（选型 M-4 候选 4 已否决）；
- 不引新依赖、不新增构建步骤；`package.json` 的 `dependencies` 与 `build.files` 零 diff；
- **禁碰**：`docs/TODO.md` / `docs/README.md`（主 agent 写域）· `docs/design/AUTO-UPDATE.md` / `docs/requirements/UPDATE.md`（B08 面）；不代他批改任何档。

### 2.5 受影响文件（行数 = 换行符计数；行宽判据 = 不含行尾 CR）

| 文件 | 当前行数 | 改动点 | 末行数 |
|---|---|---|---|
| `shell-plugins.js` | 396 | 新增 `installSpecKind()` / `isInsideDir()` / `scanProfile()`；六个既有函数增可选 `ctx`（清单见 §2.2.14 注 S8）；`installPlugin`（`:311-322`）/ `uninstallPlugin`（`:358-365`）加入参门 + 越界校验；导出面 +3 | ≈461 → **实施期实测回填** |
| `shell-market.js` | 196 | `marketList`（`:80-91`）/ `marketState`（`:95-102`）各建一次快照并下传 | ≈198 |
| `market.js` | **500** | `el()` 变参 / 新增 `frag()` 与段落 helper / `confirmModal` 改收节点 / 三处调用点改节点构造 / 三处清空改 `textContent=''` | 净 **≤ 0** → **≤ 500（硬限——NFR-3）** |
| `tests/b12-plugin-guards.test.js`（新） | 0 | 三层夹具（守卫面 / XSS 面 / 扫描面） | ≈260（不入 `build.files`） |
| `docs/requirements/SHELL.md` | 250 | US-14 / US-15 / NFR-6 / NFR-7 / B12 批次目标与层级注 / 头部关联与条目区间 / 变更记录 2 行 | **299**（落档实测） |
| `docs/design/SHELL-UX.md` | 1349 | 回指表 +3 行 / 选型 M / §2.2.14 / 注 S8 + 锚点 B12 附注 / §2.3 B12 表 / DD-41…DD-47 / C36–C39 · O20–O23 · L-B12 / U-15 与追认块 / AC26–AC31 / TC-68–TC-86 / 手段 13 / 变更记录 | **1601**（落档实测） |

**行数 / 行宽实测（本角色落笔后）**：`docs/requirements/SHELL.md` = **299** 行、最宽 **276**、超 300 行数 = **0**；`docs/design/SHELL-UX.md` = **1601** 行、最宽 **300**、超 300 行数 = **0**（两档自指行已按实测回填——承 D6 / D7）。

### 2.6 验收判据（可机检清单——与设计档 AC26–AC31 同源，判据全文见 §3.1）

| 验收 | 判据（可机检） |
|---|---|
| AC26 | 穿越形态枚举（12 形）⇒ 全部 `{ok:false}`；两侧夹具 fs **零改动**（存在性 / 内容哈希 / manifest）；无子进程（计数 = 0） |
| AC27 | 白名单正负例（谓词组）放行 / 拒；`isInsideDir` 真值表（`nm/a` true · `nm/../..` false · `nm` 自身 false · `bundled/x` true · `bundled/../profiles` false）；两处 `path.join` 后各有一次包含判定调用（grep） |
| AC28 | 静态：`market.js` 全文 `innerHTML` = **0**；三处 `confirmModal(` 第二实参非字符串。桩测：`node:vm` + DOM 桩 ⇒ 恶意 `name` 三形态只产生文本节点。真机人工：注入恶意条目 ⇒ 弹窗只显文本 |
| AC29 | 计数器：同一夹具 N=2 与 N=3727 的 `readdirSync` / `statSync` / `readFileSync` **计数相等**；`node_modules` 的 `readdirSync` = 1 / 次调用。黄金样本：返回值逐字段 + `updaterLog` 行序列逐字相等 |
| AC30 | `package.json` 零 diff；改动文件 `node --check` 全绿；`market.html` / `market-preload.js` / `market-update.js` / `exchange.js` / `shell-ipc.js` 零 diff；`market.js` ≤ **500** 行 |
| AC31 | 两类拒绝消息匹配 `^无效的插件标识：` / `^插件标识越界，已拒绝：`；不含 `[A-Za-z]:[/\\]` 与三个基准目录取值 |

### 2.7 归属与判据句（指针——全文在设计档，本节不重述，D2）

- **需求档归属 = `docs/requirements/SHELL.md`**：判据 = ① §二 排除句明指「**dsh web 应用内部**内容页」，壳自有市场窗口（`market.html` / `shell-market.js` / `shell-plugins.js` 安装通道面）不在排除范围；② §二「跨批不得回退项」已含「市场 / 兑换屋窗口」；③ 模块面 = `shell-plugins.js` 为 B06 F6 拆分的 `shell-*.js` 之一（桌面壳层）。
  **未触发「归属不明 ⇒ 停下打回」分支**（对该排除句加**层级注**说明，原句不改——落 SHELL.md §二）。
- **三项修复的判据句**（原文见 `docs/design/SHELL-UX.md` §2.2.14）：① 入参门判据句（三形态 + 拒绝形态）；② 越界校验判据句（严格包含 + 词法判定 + 不 `realpath` 理由）；③ 弹窗文本化判据句（`innerHTML` 0 处 + `frag()` / `el()` 变参语义）；④ 扫描判据句（1 次扫描 / 与 N 无关 / 等价性论证）。
- **行为差异（有意变更）**：越过入参门的入参从「执行 fs / 起子进程」变为「拒绝」；未解析的 `github:` 卸载面收紧为拒绝（卸载面合法输入 = 已装纯包名）；同一请求内的重复读变为**一致快照**；新增两类拒绝消息（走既有失败 toast 渲染面，**无新用户面文案**）——四条全文见 §2.2.14「行为差异表」。

### 2.8 实施顺序（硬）与报告项

- **实施顺序（硬约束，顺序倒置即样本不可信）**：① **先在改前代码上**用同一夹具跑一次 `computePluginUpdates`，把返回值与 `updaterLog` 行序列冻结进 `tests/b12-plugin-guards.test.js` 的期望常量（黄金样本）；② 再改代码；③ 跑 `node --test tests/b12-plugin-guards.test.js` + 静态判据组。样本内容与夹具定义回填批次档 §5。
- **计数同步（D3）**：SHELL.md US **13 → 15** / NFR **5 → 7**；SHELL-UX.md 回指表 +3 行、选型 +1 组（M-1…M-5）、AC +6、TC +19、DD +7、C +4、O +4、U +1、注 S8、锚点附注 1 条。
- **报告项 O20**：`market.js:254-258` 把注册表字段 `p.url` 直接赋给 `<a href>`（点击路径已由 `preventDefault` + 主进程 `^https?://` 判据兜住，但中键 / 新窗与导航守卫未设）——与「更新 URL 宿主白名单」同族，**本批不改**。
- **报告项 O21**：`docs/design/AUTO-UPDATE.md` §2.2.5 / §3.1 AC10 与 `docs/requirements/UPDATE.md` US-7 的更新判定面在本批后新增**前置条件**（入参门）——该两档写权不在本角色（B08 面禁碰），提请主 agent 处置。
- **报告项 O22**：**内置插件「一键安装」路径当前不可达**——`normalizePlugin`（`market.js:99-103`）只从 `install` 字段提取 `add …` 之后的标识，而内置条目 `install` 被构造为字面量 `'builtin:' + n`（`market.js:451`）⇒ `installSpec` 为 `undefined`（设计者模拟实跑确认）⇒ 渲染面走「不可一键安装」分支（`:275` 的「一键安装」标签为死码）。
  **本批不改**（超范围；`installPlugin('builtin:<名>')` 经 IPC 仍可达——本批的门正是守这条 IPC 面）。
- **报告项 O23**：`shell-market.js:127` / `:131` 的 `console.log` 调试行在 `market:enable` 路径上各调一次 `listDisabledPlugins()` / `profileBundles()`（**为打日志而扫描**）——同属「主进程扫描开销」家族，本批不改。
- **观察项（CSP 事实注记）**：`market.html:5` 已设 CSP（`default-src 'self' data:`，无 `unsafe-inline`）⇒ 按规范内联事件处理器与 `javascript:` URL 应被阻断（**未实机实测**）；本批判据不依赖该结论（修的是**注入面**）——全文见 §2.2.14「事实注记」。
- **流程项**：本批对 `docs/requirements/SHELL.md` / `docs/design/SHELL-UX.md` **只做追加**，未重排、未改他人已落内容、**未改动 B07 已核销判据行**（逐条核对：所有落笔均为新增行 / 新增节 + 既有两个自指行的实测回填）。
- **本段写入状态**：§2 **已写入**（经 `batch_segment`，无路径参数；内容为上述 §2.1–§2.8）。

### 2.9 行宽压行（§2.2 表长单元格下沉 + §2.7 / §2.8 就地断行）

**本轮压行（形态修正——语义逐字不变；承「残留即示范」）**：上一轮本角色落笔后实测 §2 有 **5** 行超 300 字符（`:98` 433 / `:100` 453 / `:101` 372 / `:154` 306 / `:164` 313；判据 = 不含行尾 CR 的字符数；§1 全部 ≤ 300）。
本轮就地压行：三处表行把长单元格的余文下沉到本节（行内改留节内指针），两处条目行就地断为续行——内容逐字不删、无语义改动。

**实测（压行后）**：§2 段内最宽 **298**（`:103`，整档最宽同值）；§2 超 300 字符行数 = **0**；整档超 300 字符行数 = **0**（§3 段头无正文）；本档 **197** 行（换行符计数——与 §2.5 行数口径同）。

本节 = §2.2 表 ①②③ 行「本批裁定」单元格的**下沉余文**——行内保留段 + 本注逐字拼接 = 原单元格全文（零删减）：

**注①（§2.2 ① 行余文）**：**拒绝形态 = `{ok:false,message}`，不含绝对路径**。**抽两个纯函数 helper**：`installSpecKind(spec)`（形态门）+ `isInsideDir(child,base)`（包含判定），随 `module.exports` 导出（桩测机检用，无副作用）

**注②（§2.2 ② 行余文）**：，`el()` 改变参（单字符串 ⇒ 旧语义），清空面改 `textContent = ''` ⇒ **`market.js` 全文 `innerHTML` = 0 处**。**判据 = 来源是否在本仓源码内**：注册表字段 ⇒ 修（`market.js:296-302` / `:326-330` / `:354-358`）；`exchange.js:39-46` 插值源 = 主进程硬编码 `FOODS`（`shell-affinity.js:34-38`）+ 本地数字 ⇒ **常量源，不改**

**注③（§2.2 ③ 行余文）**：**不引入跨请求缓存**（失效面为零；快照生命周期 = 单次 IPC 调用）。**对照判据 = 黄金样本**：同一夹具下 `computePluginUpdates` 返回值逐字段相等 + `updaterLog` 行序列逐字相等

**就地断行（无下沉余文）**：§2.7 需求档归属行 / §2.8 报告项 O22 行 = 叙述性条目，压行 = 就地断为续行，全文逐字留在原位（本注不重述——D2）。

上一轮本节的 5 个「压行版」副本块已移除：①②③ 由上述逐字余文取代；归属行 / O22 两块为改写措辞的副本，其正文即 §2.7 / §2.8 的原位行。

### 2.10 设计评审修正轮 1（落点与生效口径同步；append——§2.1–§2.9 原文不动）

> 源 = 本档 §3 轮次 1（VERDICT = changes-required：🔴2 / 🟡5 / 🔵5）；主 agent 裁决 = **11 条 Dispatched、#12 判 Not an issue（不改）**。
> 本节 = 本批的**生效口径注记**（append-only 档的承先例做法——B07 / B09 同款）；设计档 / 需求档的落地处与下表一一对应，行号 = 本轮实测 as-of（2026-09-17）。

| 评审# | 落点（`docs/design/SHELL-UX.md` 行号；需求档单独标） | 生效口径 / 要点 |
|---|---|---|
| #1 🔴 | 判据句 `:1019-1023` · 行为差异表第 3 行 `:1087` · 拒绝消息形态 `:1044` · AC27 ④ `:1460` · AC31 `:1464` · 判据细化 ③ `:1470` · TC-76 `:1570` · 改动面 `:1095` | **白名单不缩窄**（US-14 三形态对两函数照旧）——改的是**动作前提**：`realName === null` 或 `!isPluginInProfile(realName)` ⇒ `{ok:false, message:'未安装或无法解析：…'}`；需求档未改契约句 |
| #2 🔴 | 手段 7 `:1596-1597` | 「本批不引入」加 **B06 口径**标 + **B12 例外注**（1 个仓内测试文件 `tests/b12-plugin-guards.test.js` = 手段 13 的落点）⇒ 全档对本批只保留一种读法 |
| #3 🟡 | 入参门「分支通路」条 `:1012-1014` · 行为差异表第 2 行 `:1086` · TC-87 `:1581` | 过门后 `bundledSource` 不存在 ⇒ **落回 npm / pnpm 面**（与改前 `existsSync` 假分支同径——判定者 `shell-plugins.js:314` / `:360`） |
| #4 🟡 | 形态 ② `:1010` · 逐形态论证 `:1037-1041` · 注① `:1466` · 注② `:1467` · TC-73 `:1567` | ② 的 `owner` / `repo` **显式排除 `..` 与全点段**；逐形态写明是否进入两处 `path.join`（①②③**均进第一处**；`target` 仅内置分支可达）；负例补 `github:` / `github:owner` / `github:../..` |
| #5 🟡 | §2.3 B12 表 `market.js` 行 `:1206` · 修正轮补正注 `:1216-1218` | 预算收敛为**单值「净 ≤ 0」**（原「−4 … +4」区间作废）；越限处置 = 实测 > 500 ⇒ **停手上报**（DD-47 已否决本批拆分）+ 第一优先削减序（`frag()` / 段落 helper 内联） |
| #6 🟡 | §2.3 B12 表下新增 **>300 软档 体量裁定** `:1220-1222` | `shell-plugins.js` 396 → ≈461：事实 / 理由（纯增量 + 单文件职责单一，`ctx` 快照跨档反增耦合）/ **消解路径 + 触发条件**（实测 > 480 或再获增量批 ⇒ 该批先做拆分复核） |
| #7 🟡 | 设计档头部 `:5-10` | **已由 B09 修正轮 1 同步**（本轮实测 = US-1…US-15 / NFR-1…NFR-7 + B09/B10/B12 指针）⇒ **只核对、跳过**（不改） |
| #8 🔵 | AC28 ① `:1461` · 手段 13 ① `:1612-1613` · `docs/requirements/SHELL.md:275-276`（NFR-6 度量方式） | 判据面补兄弟符号（`insertAdjacentHTML` / `outerHTML` / `document.write`）与 NFR-6 第二分句同宽；**需求档度量方式同步同宽 = 本轮唯一需求档改动**（现状实测三者各 0 处） |
| #9 🔵 | 手段 13 ② 新增「夹具加载顺序（硬）」注 `:1618-1619` | 包装 `node:child_process` 须在 `require('./shell-plugins.js')` **之前**（`spawn` 为加载时解构，`shell-plugins.js:10`）⇒ AC26「子进程计数 = 0」与 TC-87「`spawn` 计数 = 1」两子判据可达 |
| #10 🔵 | AC27 ③ `:1460` · AC27 判据细化 ② `:1470` | 结构判据 = 包含判定位于该分支内任何 fs 调用（`existsSync` / `rmSync` / `cpSync` / `mkdirSync`）**之前**——批次档 §1.4 AC3 的「不执行任何 fs 写/删」半句由此承重 |
| #11 🔵 | §2.3 B12 表 `tests` 行 `:1207` · 修正轮补正注 `:1216-1218` | 用例区间 `TC-68…TC-85` → **TC-68…TC-87**（原行笔误 + 本轮新增 TC-87） |
| #12 (—) | 不改 | 主 agent 已判 **Not an issue**（源码行数经主 agent 实测）；设计者本轮**独立复测同值**：`shell-plugins.js` **396** / `shell-market.js` **196** / `market.js` **500**（换行符计数） |

**本轮覆盖的三项口径更替（原文按 append-only 保留，生效口径如下——D2 / D7）**：

1. **§2.5 `market.js` 行**原记「净 **≤ 0**（−4 … +4）」与 §2.6 AC30 ④ 的 ≤500：生效口径 = **单值「净 ≤ 0」**；越限 ⇒ 停手上报（见 #5）。
2. **§2.7「行为差异」的卸载面条目**原记「未解析的 `github:` 卸载面收紧为拒绝（卸载面合法输入 = 已装纯包名）」：生效口径 = **形态仍过白名单**，被拒的是「解析不到已装对象」这一动作前提（见 #1）。
3. **§2.6 AC26 的「穿越形态枚举（12 形）」**：生效口径 = **13 形**（补 `github:../..`）；**AC31 的「两类拒绝消息」**：生效口径 = **三类**（补 `未安装或无法解析：…`）；**§2.8 计数同步行的 TC +19**：生效口径 = **+20**（TC-87）。

**计数与枚举同步（D3，逐条）**：AC 编号不变（AC26–AC31；AC27 增 ④ 子项、AC31 改三类）· TC **19 → 20**（TC-87）· 穿越枚举 **12 → 13 形** · 形态负例 **+3 形** · 拒绝消息 **2 → 3 类** · `market.js` 预算口径 **区间 → 单值**。

**两档实测（本轮落笔后；口径 = 不含行尾 CR 的字符数 / 换行符计数）**：`docs/design/SHELL-UX.md` = **1690** 行、最宽 **300**、超 300 行数 = **0**；`docs/requirements/SHELL.md` = **306** 行、最宽 **299**、超 300 行数 = **0**；本档 §2 段（含本节）= 最宽 **298**、超 300 行数 = **0**。两档自指行已按实测回填（承 D6）。

**本轮新发现（发现即报告——不阻断本批；处置权在主 agent）**：

- **F-B12-1（一致性面，未自行修）**：`docs/design/SHELL-UX.md` 的 **B12 受影响文件表块内**（`:1223` 一带，紧随 B12 的「行宽判据」条之后）残留 **1 行 B10 内容的「单函数体量核对」条**（`sumSessionTokens()` / `startAffinityWatcher()`——属 B10 块，位置错位）。
  **未修理由** = 该行为 B10 修正轮已落内容、且 B10 本轮并行在途（改它 = 动他人已落内容 + 与在途写者冲突）⇒ 随报告提请主 agent / B10 侧归位。
- **F-B12-2（本档自身，非设计者写域）**：本档 **§3 段 9 行超 300 字符**（`:197-208`，最宽 **633**，评审表的「问题」单元格）——§3 写权属评审子代理；本轮不改，提请主 agent 在下轮评审格式上约束（§1 / §2 均 0 行超宽）。

**本段写入状态**：§2 **已写入**（经 `batch_segment`，无路径参数；内容 = 上表 + 口径更替 3 条 + 计数同步 + 实测）。

**§2.10 补正（行宽——承「残留即示范」/ §2.9 压行先例；语义逐字不变）**：本节初稿的 **#1 🔴 行**实测 **320** 字符（超 300）⇒ 下述为**压行生效版**（原行按 append-only 保留，**以本行为准**）：

| #1 🔴（压行版） | 判据句 `:1019-1023` · 行为差异表第 3 行 `:1087` · 拒绝消息形态 `:1044` · AC27 ④ `:1460` · AC31 `:1464` · 判据细化 ③ `:1470` · TC-76 `:1570` · 改动面 `:1095` | **白名单不缩窄**（US-14 三形态对两函数照旧）——改的是**动作前提**：`realName === null` 或 `!isPluginInProfile(realName)` ⇒ `{ok:false, message:'未安装或无法解析：…'}`；需求档未改契约句 |

同轮补正两处指针：① **F-B12-1** 的错位行实测 = `docs/design/SHELL-UX.md:1223`；② **F-B12-2** 的 §3 段超宽行实测（本节 append 后行号）= `:235-246`（9 行，最宽 633）。本档 §2 段（含本节与补正）压行后最宽 **≤ 300**、超 300 行数 = **0**。

**§2.10 二次补正（一致性面·就地修 + 留痕——D7）**：本节 **#1 行已就地压行**（`:198`：**320 → 287** 字符）——判据 = 形态不统一属**一致性面** ⇒ 当场修正（承「残留即示范」），并在此留痕；上条补正的「原行按 append-only 保留」表述随之作废（该压行版即该行本身，语义逐字不变）。
本档 §2 段（含本节全部补正）现最宽 **292** 字符、超 300 行数 = **0**；§1 / §2 均合规（§3 段 9 行超宽 = 评审子代理写域，见 F-B12-2，不属本节处置面）。

### 2.11 设计评审修正轮 2（收尾轮——N1–N4 落点与生效口径；append——§2.1–§2.10 原文不动）

> 源 = 本档 §3 轮次 2（VERDICT = **pass**；第 1 轮 12 条 = 11 已修 + 1 维持 Not an issue；本轮新发现 N1–N4）；主 agent 裁决 = **4 条全部 Dispatched**。
> 本节 = 本轮**生效口径注记**（append-only 档的承先例做法——§2.10 同款）；落点行号 = 本轮实测 as-of（2026-09-17，设计档 **1782** 行 = 换行符计数）。

| 复核# | 落点（`docs/design/SHELL-UX.md`） | 生效口径 / 要点 |
|---|---|---|
| N1 🟡 | 行为差异表第 5 行 `:1118` | 「新增**两类**拒绝消息」→「新增**三类**拒绝消息（`无效的插件标识：` / `插件标识越界，已拒绝：` / `未安装或无法解析：`）」——与判据句 `:1075`「共三类」· AC31 `:1514`「三类」· AC27 判据细化 ③ `:1529` 同源（D3 计数同步） |
| N2 🟡 | 「分支通路」条 `:1038-1042`（3 → 5 行）· 越界校验判据句下新增「面标号与执行先后」注 `:1062` · 逐形态论证 ② 行 `:1068` · AC27 判据细化 ② `:1528` | 统一为**一条流程**（口径 = 「**计算面对全形态、动作面按 `kind` 分派**」）——余文见本节**注 N2-①**（逐字下沉）；①②③ 顺序与两条包含判定的位置由该注定死 ⇒ AC27 ③ 的结构判据不再靠读者猜 |
| N3 🟡 | AC28 ① `:1511` · TC-79 `:1632` · 新增 B12 判据细化 ④ `:1525` | 实参序号按**新签名** `confirmModal(title, okLabel, ...parts)` 更正：**节点构造实参 = `parts` 形参（第三实参起）**——原文按旧签名 `(title, html)` 记「第二实参」（= 实现者会验错对象）；细节下沉为判据细化 ④（一处定义 · AC28 ① 与 TC-79 两处回指——D2） |
| N4 🔵 | TC-70 `:1623` · TC-71 `:1624` | 输入集由 9 形列举改为「**注① 全量（13 形）**」指针式表述（`§3.1「B12 判据细化」`）——消除两处各自枚举的漂移面（D2 / D3） |

**注 N2-①（§2.11 N2 行余文——逐字下沉；与行内保留段拼接 = 原单元格全文）**：
① **计算面（全形态必经）**：`bundledName`（去 `builtin:` 前缀；非该形态 = 原样入参）⇒ 第一处 `path.join`（`bundledSource`）⇒ 紧随 `bundledSource` 面包含判定；
② **动作面按 `kind` 分派**：`kind === 'bundled'` 进内置分支，分支内按 `existsSync` 再分——源存在 ⇒ 内置拷贝面 / 源不存在 ⇒ 落回 npm / pnpm 面；`github` / `npm` ⇒ 直接落 npm / pnpm 面（**② 形态仍进第一处 `path.join`**）；
③ **第二处 `path.join`（`target`）仅内置动作面内可达** ⇒ 紧随 `target` 面包含判定 ⇒ 之后才是 `mkdirSync` / `rmSync` / `cpSync`。
判据句 ①② 明确为**按面标号**（① `target` / ② `bundledSource`），**执行先后** = ①②③（见设计档 `:1062`）。

**§2.6 / §2.7 的生效口径更替（原文按 append-only 保留——D2 / D7）**：

1. **§2.6 AC28 行**原记「三处 `confirmModal(` **第二实参**非字符串」：生效口径 = **节点构造实参 = `parts` 形参（第三实参起）**（与设计档 AC28 ① / TC-79 同指；见上表 N3）。
2. **§2.6 AC27 行**原记「两处 `path.join` 后各有一次包含判定调用（grep）」：生效口径补**执行先后** = 设计档 §2.2.14「分支通路」①②③（`bundledSource` 面判定先于 `target` 面判定）；原「该分支内」表述作废（见上表 N2 / 设计档 AC27 判据细化 ②）。
3. **§2.7 行为差异条目**原记「新增两类拒绝消息」→ 生效 **三类**（§2.10 已记口径更替 + 本轮 N1 同步设计档）。

**计数与枚举同步（D3，逐条）**：AC 编号不变（AC26–AC31）· TC 不变（TC-68…TC-87，共 20 例）· DD / C / O / U 均不变 · 穿越枚举 **13 形**（不变）· 拒绝消息 **三类**（本轮消掉其后一处残留）· **本轮无新增 / 无删除条目**。

**两档实测（本轮落笔后；口径 = 换行符计数 / 不含行尾 CR 的字符数）**：`docs/design/SHELL-UX.md` = **1782** 行（本轮 **1774 → 1782**，+8 = 分支通路条 3 → 5 行 / 判据句面标号注 +1 / 判据细化 ④ +1 / 变更记录 4 行）· 最宽 **300** · 超 300 行数 = **0** · EOL 全档 **CRLF**（原文两行 LF-only 随本轮写入归一）；
`docs/requirements/SHELL.md` **310**（本轮**未改**该档——本轮 4 条全落设计档 / 本档面）。本档（批次档）段边界 / 行宽实测见本节末补正。

**本轮新发现（发现即报告——未落笔，处置权在主 agent；承本轮边界「只落上表 4 条」）**：

- **F-B12-3（判据可达性 ⇒ 语义面 ⇒ 停下打回，未自行修）**：**AC31 的「越界校验」前缀（`插件标识越界，已拒绝：`）在当前白名单下无任何可达输入**——AC31（`:1514`）要求三条前缀「各至少一例命中」，第二条标注命中面 = TC-70 · TC-71；
  而 §2.2.14「两层关系」（`:1064`）自陈「三形态的段集 ⇒ `path.join` 结果**必在**基准之下 ⇒ 当前形态下第二层**不会**成为决定层」——两者互斥：白名单内不存在「过入参门但被包含判定拒」的形态（三形态段集均不含 `..` / 全点段 / 盘符 / 绝对路径）⇒
  TC-70 / TC-71 的 13 形输入**全体在入参门即被拒**，越界校验分支及其前缀消息**不可达**；TC-75（`:1628`）的期望随之只余一类可达。
  可选修法（请主 agent 裁定后另轮落笔）：① AC31 第二前缀的判据面改**函数级**（`isInsideDir` 真值表 = TC-74 已覆盖）+ 消息文本改由**静态核对**（形态在场）守，不要求行为命中；② 或新增一条「过入参门但越界」的构造输入（需动白名单语义 ⇒ 超本轮边界）。
- **F-B12-4（计数口径 ⇒ 一致性面 ⇒ 未落笔，随报告提请）**：§2.2.14「两层关系」（`:1064`）的「可机检面 = … + 调用点在场（`installPlugin` / `uninstallPlugin` **各 1 处**）」与 AC27 判据细化 ②（`:1528`）「两处 `path.join` 之后**各有一次**包含判定调用」计数不符——按后者，每个入口函数应有 **2 处**调用点（`bundledSource` 面 + `target` 面）。
  未自行修理由 = 本轮边界（只落上表 4 条）；**建议**：该行改「各 **2** 处（`bundledSource` 面 + `target` 面）」或改「各 ≥1 处（面见判据细化 ②）」。
- **已核对无缺陷（观测项，不改）**：TC-75（`:1628`）的「**两类**拒绝消息（穿越入参）」是**形态面**限定（穿越枚举涉及入参门 + 越界校验两类），与 AC31 的三类不矛盾（第三类属卸载面解析门 = TC-76）；N1 点名「核本档其余『两类』计数」经全文核对只此一处，语义正确。

**本段写入状态**：§2 **已写入**（经 `batch_segment`，无路径参数；内容 = 上表 + 生效口径 3 条 + 计数同步 + 两档实测 + 新发现 F-B12-3 / F-B12-4）。

**§2.11 补正（行宽 + 段边界实测；承「残留即示范」/ §2.9–§2.10 压行先例）**：本节初稿的 3 行超 300 字符 ⇒ 已**就地压行**（该 3 行即本轮新落行，压行后语义逐字不变）：① N2 行 **638 → 229**（余文逐字下沉为**注 N2-①**）；② 两档实测行 **310 → 227**（就地断为续行）；③ F-B12-3 行 **371 → 142**（就地断为 3 行）。

**本档段边界 / 行宽实测（本轮 append 后；口径 = 换行符计数 / 不含行尾 CR 的字符数）**：本档 = **346** 行（本轮 305 → 346，+41）；**§1 `:5-76`**（最宽 **268**，超 300 = **0**）· **§2 `:77-278`**（最宽 **298**，超 300 = **0**；§2.11 = `:238-278`）· **§3 `:279-341`**（超 300 = **12** 行——评审子代理写域，见 §2.10 F-B12-2）· **§4 `:342` / §5 `:344` / §6 `:346`**。

**§2.11 二次补正（段边界 as-of 更正——一致性面·就地修 + 留痕；D7）**：上条段边界为**其落笔前**实测值；本补正落笔后实测 = 本档 **352** 行 · **§1 `:5-76`** · **§2 `:77-284`** · **§3 `:285-347`**（超 300 = **12** 行，最宽 **633**——评审子代理写域，见 §2.10 F-B12-2）· **§4 `:348` / §5 `:350` / §6 `:352`**。

**本档（含本补正）行宽实测**：§1 / §2 超 300 = **0** 行（§2 段最宽 **298**）；§3 = **12** 行超宽（`:291-298` / `:302` / `:318` / `:336-337`——同上写域）；设计档同轮实测见 `docs/design/SHELL-UX.md` 变更记录（**1782** 行 / 最宽 300 / 超 300 = 0）。

**§2.11 三次补正（段边界数值更正；上条 +2 偏差 = 追加段自带空行）**：以本补正为**最后一条**实测 = 本档 **357** 行（§1 `:5-76` · §2 `:77-289` · §3 `:290-352` · §4 `:353` · §5 `:355` · §6 `:357`）；行宽 = §1 / §2 超 300 **0** 行（§2 段最宽 **298**）、§3 超 300 **12** 行（`:296-303` / `:307` / `:323` / `:341-342`——评审子代理写域）。

**§2.11 四次补正（收口——段边界改锚点式，免自指数值再漂）**：行宽（不随本条变化）= §1 / §2 超 300 **0** 行（§2 段最宽 **298**）、§3 超 300 **12** 行（评审子代理写域，见 §2.10 F-B12-2）；段边界 = §1 档首～`### 2.1` 前 · §2 `## §2 本批任务书（eng-designer）`～`## §3 设计评审（评审子代理）` 前 · §3 该题～`## §4 评审裁决与实施启动（主 agent）` 前 · §4 / §5 / §6 = 各自题行；前述两条的段边界数值以本条口径为准（D6 回读）。

**§2.11 五次补正（指针更正——一致性面·就地修 + 留痕；D7）**：N2 行的「逐形态论证 ② 行」as-of 行号 `:1067` → **`:1068`**（初稿错按「面标号注」落笔前实测取值）；已就地更正、语义不变；段边界口径仍以「四次补正」的锚点式为准。

### 2.12 设计评审修正轮 3（收尾轮 · 2 条：F-B12-3 / F-B12-4 落点与实测；append——§2.1–§2.11 原文不动）

> 源 = 本档 §2.11 报而不改的两条（F-B12-3 🔴 判据不可满足 · F-B12-4 🟡 计数口径不符）；主 agent 亲验两条为真并裁定「均 Dispatched」，修法取向由主 agent 给定（本轮不得改取向）。
> 落点全在 `docs/design/SHELL-UX.md` 的 B12 面（§2.2.14 / §3.1 AC26–AC31 / §3.2 TC-68–TC-87 / §3.3 手段 13）；段边界一律锚点式（免自指数值再漂）。

**F-B12-3（🔴 判据不可满足——AC31 第二条前缀改为函数级 + 静态面）**：

- （a）§3.1 **AC31 行**——改前：第二条前缀 `插件标识越界，已拒绝：…` 的命中面标注 = TC-70 · TC-71（白名单下不存在「过门但越界」的输入 ⇒ 该判据永不满足）；
- 改后 = **函数级 + 静态**面：TC-74（`isInsideDir` 真值表，回指不重述）+ 该前缀字符串在 `shell-plugins.js` 的**静态命中**（存在且走既有失败 toast 渲染面），行内并标注「运行时不可达」。
- （b）§2.2.14「两层关系」行之后**新增 1 条**「已知事实（不可达面）」：白名单在场 ⇒ 第二层拒绝分支**不可达**——保留为**纵深防御**（形态演进时守位）；**其可达性不得作为验收面**。
- （c）§3.1 表下**新增「AC31 判据细化」块（4 行）**：三前缀逐条「取证面 + 可达性」（① `无效的插件标识：…` 可达 / ② `插件标识越界，已拒绝：…` 运行时不可达 ⇒ 函数级 + 静态 / ③ `未安装或无法解析：…` 可达）+ 共同面（不含盘符形态与基准目录取值）。
- （c-补）该块并注明 `TC-75` 的「两类（穿越入参）」= **形态面限定**、与 AC31 三类不矛盾 ⇒ 本轮**逐字未动**（判读依据见设计者报告③）。
- （d）**未新增**「过门但越界」的构造输入（= 不给白名单开口）；本轮**零新增 AC / TC 编号**（AC 计数 31 · TC 计数 87 不变）。
- 派生（D3 计数与枚举同改）：§3.2 `TC-74` 行的「映射」列 `US-14 / AC27` → `US-14 / AC27·AC31`（该行真值表面即第二条前缀的取证面）。

**F-B12-4（🟡 计数口径不符——统一为「每函数 2 处 / 两函数共 4 处」）**：

- 落点 1 = §2.2.14「两层关系」行的调用点计数句（该句下沉为新增条「可机检面（修正轮 3 计数口径）」）：改前「调用点在场（`installPlugin` / `uninstallPlugin` **各 1 处**）」→
  改后「调用点计数 = **每函数 2 处**（`bundledSource` 面 + `target` 面），两函数共 **4** 处判定面」。
- 落点 2 = §3.1 AC27 判据细化 ②：改前「两处 `path.join` 之后各有一次包含判定调用」→ 改后「**每函数两处 `path.join`**（`bundledSource` 面 + `target` 面）**之后各一次包含判定调用**（grep）⇒ 两函数共 **4** 处判定面（`installPlugin` 2 + `uninstallPlugin` 2）」。
- 判读依据（以实现面实况为准）= grep 实测 `shell-plugins.js`：`installPlugin` 内 `path.join` 恰 **2** 处（`bundledSource` `:313` / `target` `:316`）、`uninstallPlugin` 内恰 **2** 处（`:359` / `:361`）⇒ **每函数 2 处、两函数共 4 处**。
- 口径不矛盾的理由：§2.2.14 判据句的「两处 `path.join`」= **按面**（`bundledSource` 面 / `target` 面，两函数各一组）⇒ 与「每函数 2 处」同源；AC27 细化 ② / §2.2.14 调用点计数 / AC31 取证面 三处互不矛盾（设计者自查②）。

**本轮实测（D6 回读核对）**：

- 设计档 `docs/design/SHELL-UX.md`：改前 **1785** → 改后 **1794**（换行符计数；+9 = 可机检面 / 已知事实 2 行 + AC31 判据细化 4 行 + 变更记录 3 行）；行宽 **0** 行超 300（最宽 300 = B06 旧行）；EOL 全档 CRLF（本轮无 LF-only 行）。
- 段边界（锚点式）：§2.2.14 = 「入参门判据句（主防线…）」起 ～ 下一个 `**` 机制段标题前 · AC26–AC31 = 自 `| AC26（B12） |` 行至 `| AC31（B12） |` 行 · AC31 细化块 = `> **AC31 判据细化（B12 修正轮 3` 起 4 行 · TC-68–TC-87 = `| TC-68（B12） |` ～ `| TC-87（B12） |` 行。
- 本档（批次档）：§1 / §2 超 300 = **0** 行；§3 = **12** 行超宽（评审子代理写域，见 §2.10 F-B12-2）——本轮未改该段；本轮追加段行宽见设计者报告④。

**新发现（单列不改笔——报而不改，待主 agent 裁定）**：

- **N5**：设计档 §3.3 手段 13 ①（B12 面）写「`grep -n "path.join" shell-plugins.js` 的**每处**后随包含判定」；grep 实测该档 `path.join` 命中 **26** 处（`profileDir()` / `bundledPluginsDir()` / `pnpm` 面等既有拼接点在内），仅 **4** 处为本批判定的守卫面。
- 判读：按字面**不可满足**（既有拼接点无需守卫）——属**判据与实现冲突**（语义面）⇒ 本轮未自行改笔；建议口径 = 「四处（两函数 × 两处面判定）后随包含判定，且先于其后的 fs 调用」，或按 §2.2.14 的面清单限定。

**§2.12 补正（D6 回读核对；数值口径 = 含本行自身——与既有补正先例同口径）**：上条实测的设计档数值应为**改前 1785 → 改后 1795**（换行符计数；**+10** = 可机检面 / 已知事实 2 行 + AC31 判据细化 4 行 + **本记录 4 行**）；本档（批次档）本轮实测 = **360 → 396** 行（**+36 = 本 §2.12 段 36 行**，含本补正行与分隔空行）、§2 段最宽 **298**、§1 / §2 超 300 = **0** 行、§3 超 300 = **12** 行（评审子代理写域，本轮未改）。

### 2.13 设计评审修正轮 4（收尾轮 · 单条：N5 落点与实测；append——§2.1–§2.12 原文不动）

> 源 = 本档 §2.12「新发现（单列不改笔）」的 **N5**；主 agent 亲验为真并裁定 = **修**（同类第二次：与 F-B12-3 同形——判据的「全量面」未随收紧面同步）；修法取向由主 agent 给定（本轮不得改取向）。
> 落点全在 `docs/design/SHELL-UX.md` 的 B12 面（§3.3 手段 13 ① + §四 变更记录）；段边界一律锚点式（免自指数值再漂）。

**N5（🔴 判据不可满足——手段 13 ① 改为面限定口径）**：

- （a）落点 = §3.3 手段 13 ① 判据句（改前单行 = `:1691`）；改前原文 =「`grep -n "path.join" shell-plugins.js` 的**每处**后随包含判定、**且该判定在该分支内任何 fs 调用之前**」。
- 改后 = 拆 3 行（`:1691`–`:1693`）——① 「**四处守卫面**（`installPlugin` 的 `bundledSource` 面 + `target` 面；`uninstallPlugin` 的同两面）**之后各随一次包含判定**
  （grep 面限定 = 这四处，as-of `shell-plugins.js:313` / `:316` / `:359` / `:361`），**且该判定先于其后的任何 fs 调用**」；
  ② 新增「**为何不能按全档判**」句（其余拼接点输入为常量段 / 既有解析值、**不以用户入参为输入** ⇒ 无注入点、无须包含判定；故不得按全档「每处」判读——实测全档 **26** 处 vs 守卫面 **4** 处）；
  ③ `installSpecKind\|isInsideDir\|scanProfile` 在场（实现 + 导出）+ `shell-ipc.js` 零 diff（逐字未动）。
- （b）实测依据（设计者亲跑，2026-09-17）：`shell-plugins.js` 的 `path.join` 共 **26** 处 = `:27` / `:31` / `:32` / `:36` / `:37` / `:42` / `:48` / `:64` / `:146` / `:191` / `:204` / `:213` /
  `:218` / `:222` / `:236` / `:242` / `:246` / `:278` / `:284` / `:292` / `:298` / `:302` / `:313` / `:316` / `:359` / `:361`；
  守卫面 = `installPlugin` 的 `:313` / `:316` + `uninstallPlugin` 的 `:359` / `:361` = **4** 处 ⇒ 改前「每处」按字面不可满足。
- （c）**零新增 AC / TC 编号**（AC 计数 31 · TC 计数 87 不变）；**未改白名单语义**；**未新增构造输入**；§2.2.14 / AC27 判据细化 ② / AC31 / TC 各行**逐字未动**。

**口径一致性自查（设计者——三处逐处结论；取证面 = 全档 grep「手段 13」「path.join」）**：

- **① §2.2.14 面清单**——`:1039`（计算面 ⇒ `bundledSource` 面判定）· `:1041`（第二处 ⇒ `target` 面判定）· `:1057`（越界校验判据句「两处 `path.join` 之后各做一次包含判定」）·
  `:1062`（面标号 ≠ 执行先后）· `:1065`（调用点计数 = 每函数 2 处 / 两函数共 4 处判定面）· `:1068`（两处 = `bundledSource` `:313`/`:359` 与 `target` `:316`/`:361`）；
  口径 = 按**面**（`bundledSource` / `target`）× 两函数 ⇒ 与手段 13 ①（改后）的「四处守卫面」**同源一致**（同一面清单的展开，行号逐字同源）。
- **② §3.1 AC27 判据细化 ②**（`:1530`）——每函数两处 `path.join` 之后各一次包含判定调用（grep）⇒ 两函数共 **4** 处判定面，且四处判定均先于其后的任何 fs 调用
  ⇒ 与手段 13 ① **两项逐字对应**（「4 处判定面」+「先于 fs 调用」）。
- **③ §3.3 手段 13 ①**（`:1691`–`:1693`，本轮改后）——四处守卫面各随一次包含判定 + 先于 fs 调用 + 其余拼接点不在判据面内 ⇒ 即本轮生效口径。
- **结论**：三处**无一相抵**；`:1057` 的「两处 `path.join`」= **按面**（每函数一组）⇒ 与「每函数 2 处 / 两函数共 4 处」的既有换算（`:1065`，源 = F-B12-4）同源，非矛盾项；
  全档复查 = 含 `path.join` 且含「每处」字样的行改后**仅 `:1692`**（本轮新增的**否定句**「不得按全档『每处』判读」）⇒ 无残留的全档字面口径。

**本轮实测（D6 回读核对）**：

- 设计档 `docs/design/SHELL-UX.md`：改前 **1795** → 改后 **1801**（换行符计数；+6 = 手段 13 ① 179 字单行 → 3 行（+2）+ 变更记录 4 行——含补正行）；行宽 **0** 行超 300（最宽 300 = B06 旧行；本轮新增行最宽 **274**）；EOL 全档 CRLF（本轮无 LF-only 行）。
- 段边界（锚点式）：手段 13 ① 段 = `13. **B12 判据（新增；` 起 ～ `② 桩测——加载器注入假 ` 前 · 变更记录本轮块 = `| 2026-09-17 | **B12 修正轮 4` 行 ～ `|  | 补正（2026-09-17，落档实测）：本轮改后本档 **1795 → 1801**` 行。
- 本档（批次档）：本轮追加段行宽见本节末（补正行）；§1 / §2 超 300 = **0** 行；§3 = **12** 行超宽（评审子代理写域，见 §2.10 F-B12-2）——本轮未改该段。

**新发现（单列不改笔——报而不改，待主 agent 裁定）**：

- **N6（观察项 · 不阻断本批）**：本轮派单硬约束列「单档 ≤500 行」，而 `docs/design/SHELL-UX.md` 实测 **1801** 行（B06 初版即 >500；历轮补正只报「行宽 ≤300」）。
  该行数上限与本档现行判据面不符（设计档判据 = 行宽 ≤300；500 行疑为代码档 / 模板档残留）——本轮**未动**，报请主 agent 裁定该上限的适用范围。

**§2.13 补正（D6 回读核对；数值口径 = 含本行自身——与既有补正先例同口径）**：本档（批次档）本轮实测 = **396 → 439** 行（**+43 = 本 §2.13 段 38 行 + 段首空行 1 + 本补正前空行 1 + 本补正块 3 行**）；
§2.13 段行宽最宽 **188**、超 300 = **0** 行；§1 / §2 超 300 = **0** 行；§3 = **12** 行超宽（评审子代理写域，本轮未改该段）；EOL 全档 LF（0 处 CRLF——与既有形态一致）；段边界 = §2 题 ～ `## §3 设计评审（评审子代理）` 前。
（留痕 · D7）本补正原落为单行 **311** 字 ⇒ 超 300 ⇒ **就地拆两行**（+ 本留痕行 = 3 行；一致性面·就地修，语义不变；本档行数口径随之由 **437** 更正为 **439**）。

### 2.14 B12 实施后收口轮（源 = 本档 §5 残余 3 / 4 / 6；append——§2.1–§2.13 原文不动）

> 源 = 本档 **§5.6 ⑤**（设计档 as-of 计数漂移 + 实测回填未落）· **§5.6 ① / §5.5 #2**（卸载面 `builtin:<名>` 假成功未登记）· **§5.6 ④**（台账承接）；
> 主 agent 亲验三条并裁定 **全部 Dispatched**（修法取向由主 agent 给定，本轮不得改取向）。
> 落点全在 `docs/design/SHELL-UX.md` 的 B12 面（§3.3 手段 13 ① / §2.2.14 已知限制 / §2.3 B12 表 / §四 变更记录）；**未动任何 `.js` / `tests/**` / 台账 / README / 地图 / 他批面**；段边界一律锚点式（免自指数值再漂）。

**① 说明性计数过期（26 → 21；判据句语义不动）**

- 落点 = §3.3 手段 13 ① 判据句内的**说明性计数**（改前 `:1713`；本轮改后该句在 `:1719`，新增注在 `:1721`）。**改前（原文逐字段）**：「（**故本判据不得按全档「每处」判读**——实测全档 **26** 处 vs 守卫面 **4** 处；面清单与执行先后同源 …）」。
- **改后**：该括号内数字改 **21**，并紧随一行新注——「**该计数只作 as-of 说明、不构成判据（B12 实施后收口轮）**：全档数字随实现重构而变（设计期 **26** → 实施后 **21**——实施期重构引入 `nodeModulesDir()` 等使既有拼接点去重），而判据按面限定、守卫面恒为 **4** 处 ⇒ 判据判读一律以「四处守卫面」为准、**不得援引全档计数**」。
- **判据句本身（面限定 + 四处守卫面 + 先于 fs 调用）逐字未动**；守卫面实测仍恰 **4** 处（设计者亲测 `shell-plugins.js`：`path.join` 于 `:361` / `:365` / `:412` / `:415`，包含判定于 `:362` / `:366` / `:413` / `:416`）。

**② §2.3 B12 表「实施期实测回填」完成（四行）**

- 表行（原地改 `末行数` 列；标记「实施期实测回填」→ **「实施期实测已回填」**）：`shell-plugins.js` **454**（396 → +58）· `shell-market.js` **198**（196 → +2）· `market.js` **500**（±0 = 改 20 / 删 20，贴 NFR-3 硬限零余量）· `tests/b12-plugin-guards.test.js` **491**（新建；`node --test` = 19/19 绿、退出码 0）。
- 表下新增 **3 行回填注**（`:1288-1290`）：记录四行实测值 + 同族一致性面同步——上条「>300 软档 体量裁定」条内落档预估「396 → ≈461」按实测 = **454**（偏差 −7；余量 **46** 行）；
  其**消解期触发条件（实施期实测 > 480）未触发** ⇒ 该条裁定结论（本批保持单文件、不拆分）不变，**条件句逐字未动**。

**③ 设计面缺口登记（只登记、不改行为、不扩范围）**

- 落点 = §2.2.14「**已知限制（本批不修，留档）**」新增 **L-B12-4**（`:1152-1155`，4 行）：**卸载面 `builtin:<名>` 假成功**——现象（`uninstallPlugin('builtin:<已装名>')` 返回 `{ok:true,'已卸载 builtin:<名>（pnpm 有警告，已清理注册）'}` 而实际未删未注销）+
  触发面（`builtin:` 前缀形态经 `market:uninstall` IPC = 边角入口）+ 根因（动作面用**原始入参**：`:412` / `:415` 拼接、`:421` `pnpm remove` 实参、`:424` 失败分支注销）+
  **与改前逐字同源（非本批回归）** + **为何本批不改**（改它即变更 `pnpm remove` 的实参语义，行为差异表未覆盖）+ **修法方向**（动作面改用解析门已得的 `realName`）+ **台账承接 = T32**（`docs/TODO.md`）。
- 判据（设计者亲读代码复核，与 §5 报告一致）：`installSpecKind('builtin:<名>')` ⇒ `'bundled'`（`shell-plugins.js:325`）；`resolveInstalledName` 去 `builtin:` 前缀（`:143`）⇒ 解析门可过（`:410`）；动作面用原始入参（`:412` / `:421` / `:424`）⇒ `existsSync(bundledSource)` 判假、落 pnpm 面后失败却仍回成功。

**本轮实测（D6 回读核对）**

- 设计档 `docs/design/SHELL-UX.md` = **1847**（改前 **1834**，+13 = §3.3 注 +1 / §2.2.14 L-B12-4 +4 / §2.3 回填注 +3 / §四 本记录 5 行——含补正行 / 行宽压行 +1）；行宽 **0** 行超 300（不含行尾 CR，最宽 300）；EOL 全档 **CRLF**（LF-only 行 = **0**）。
- 代码 / 测试四档（设计者亲测，换行符计数）：`shell-plugins.js` **454** · `shell-market.js` **198** · `market.js` **500** · `tests/b12-plugin-guards.test.js` **491**（与主 agent 复核值一致）。
- `path.join` 计数实测：全档 **21** 处（`shell-plugins.js`）= `:27` / `:31` / `:35` / `:36` / `:40` / `:41` / `:46` / `:52` / `:68` / `:128` / `:132` / `:182` / `:230` / `:281` / `:287` / `:301` / `:305` / `:361` / `:365` / `:412` / `:415` —— 守卫面 **4** 处（`:361` / `:365` / `:412` / `:415`）。
- **自查 grep「26」（两档逐处）**：设计档含 `**26**` 的**说明性计数**共 **4** 行 —— `:1721`（本轮新增 as-of 注，**标注为「设计期」**= 合规）/ `:1828`（历史行 = 修正轮 4 记录，**已被取代**）/ `:1842` + `:1845`（本轮的更正记录与取代说明）；
  其余 131 行命中为**日期 `2026`**（79 行）与**行号区间 / 版本号**（52 行，如 `:263-268` / `AC26` / `TC-26`）——**无残留的旧计数**；需求档 `docs/requirements/SHELL.md` 的 `**26**` 计数 = **0** 处（其 2 处命中为 `AC26` 编号引用）。
- **条目计数**：AC26–AC31 = **6** 行 · TC-68…TC-87 = **20** 行（**未新增 / 未删除**）；白名单语义 / 判据句 / 构造输入 / 四档 `.js` 与 `tests/**` **逐字未动**。

**新发现（单列不改笔——报而不改，待主 agent 裁定）**

- **F-B12-5（一致性面 · 行锚漂移）**：§3.3 手段 13 ①（`:1719`）与 §2.2.14「逐形态论证」（`:1088`）所记守卫面行锚 `shell-plugins.js:313` / `:316` / `:359` / `:361` 为**设计期 as-of 值**；实施后实测 = `path.join` 于 `:361` / `:365` / `:412` / `:415`、包含判定于 `:362` / `:366` / `:413` / `:416`。
  **未改理由** = 超本轮三条边界，且行锚漂移按既有口径（台账 T13 / T31：行号只作 as-of 参考）归 **B14 文档事实收口**——本轮只报。
- **F-B12-6（一致性面 · 文档自指行）**：§2.3 B12 表两处**文档自指行**的「末行数」为其**落档时点**实测（`docs/requirements/SHELL.md` **299** / 本档 **1601**），现值 = **319** / 本档 **1847**（本轮落笔后）。
  主 agent 派单 item 2 只列代码 / 测试四行 ⇒ 设计者**未自行扩面**；建议按既有先例（B09 / B10 各轮）在 §2.3 表下补「文档行数补正注」，或随 **B14** 收口。
- **F-B12-7（派单表述与实况不符——已按实况落笔）**：派单 item 1 称该说明性计数「设计档 §3.3 手段 13 ① **与 §2.2.14 的注记**」两处均写 26；实测**唯一落点 = §3.3 手段 13 ①**——
  §2.2.14 不含该计数（其面计数为「调用点 = 每函数 2 处 / 两函数共 **4** 处判定面」，`:1086`；全档计数只此一处 + §四 历史行）。本轮**只改唯一落点**，**未**在 §2.2.14 新造同源注（D2 单一权威源——不重述）。
- **观察项（不改）**：L-B12-4 与台账 **T32** 同源同指（T32 登记句已写「B12 已登记为已知限制」）⇒ 本轮使该指针**兑现**，未发现新增缺口；`market.js` 贴 500 硬限（零余量）属既有事实（AC30 ④），本轮**未**触碰任何 `.js`。

**本段写入状态**：§2 **已写入**（经 `batch_segment`，无路径参数；内容 = ①–③ + 实测 + 新发现 F-B12-5 / F-B12-6 / F-B12-7）。

## §3 设计评审（评审子代理）

### 轮次 1（评审子代理）

| # | 类别 | 严重度 | 问题 | 建议 |
|---|------|--------|------|------|
| 1 | Requirements | 🔴 | 卸载面入参门两处描述互斥：判据句把三形态白名单同时施加于两函数（`docs/design/SHELL-UX.md:982-985`，② `github:` 为白名单成员），而行为差异表第 3 行称未解析的 `github:` 卸载入参本批后 ⇒ `{ok:false}`，理由写作「该形态不在白名单内」（`docs/design/SHELL-UX.md:1045`），TC-76 同期望（`docs/design/SHELL-UX.md:1500`）；需求档 US-14 契约第 1 项亦把 `github:` 形态同时授予 `installPlugin` / `uninstallPlugin`（`docs/requirements/SHELL.md:210`）。该收紧的**实现机制**在判据句与 AC 中均无落点（批次档 §2.7 只记结论：`docs/batches/B12-market-security-blocking.md:157`）⇒ 按判据句实现则 TC-76 必不成立，按 TC-76 实现则引入需求未审的收窄面 | 二择一并同源落定：(a) 若确要收紧，把「卸载面合法输入 = 已装纯包名」写成显式条款（判据句 + AC + 需求档修订句，承 US-12 对 US-11 的显式修订先例）；(b) 若不收紧，回改行为差异表该行与 TC-76 期望值；两种情形都在 §2.5 冲突点表登记 |
| 2 | Acceptance criteria | 🔴 | 验证手段自相矛盾：§3.3 手段 7 写「**本批不引入**：测试框架 / `test` script / **新增 `tests/` 文件**」且无批次限定（`docs/design/SHELL-UX.md:1525`；同节 8–13 条均带批次标），而 手段 13 / DD-46 / §2.3 B12 表 / 追认块一致要求本批新增 `tests/b12-plugin-guards.test.js`（`docs/design/SHELL-UX.md:1537` / `:1215` / `:1156` / `:1351`；批次档 `docs/batches/B12-market-security-blocking.md:103`）——同一机制两种读法；AC29 的计数器与黄金样本判据依赖该文件（`docs/design/SHELL-UX.md:1400`），按手段 7 可被援引为「不落文件、只做静态判据」（该路径已被 M-5 候选 3 否决，`docs/design/SHELL-UX.md:277`） | 给手段 7 加批次限定或例外注（B06 口径 +「B12 例外 = 手段 13 的 1 个仓内测试文件」），使全档对本批是否新增 `tests/` 文件只有一种读法 |
| 3 | Clarity | 🟡 | 「内置分支的进入条件 = `kind === 'bundled'`」（`docs/design/SHELL-UX.md:987`）与本批「零回退」承重点未闭合：形态 ① 含**裸纯包名**（`:983`），而注册表实测「`npm` 字段非纯包名条目 = 0」（`:989`）⇒ 裸名条目同样落入 `kind==='bundled'`；设计未写明 `kind==='bundled'` 但 `bundledSource` 不存在时的通路（落回 npm/pnpm 面 or 拒绝）。「与改前 `existsSync` 结果面等价」的论据（`:987`）与「3725 形态零误拒」（`:988`）只覆盖**谓词**面、不覆盖**分支结果**面，故行为差异表第 2 行「走内置面或 pnpm 面 ⇒ 同」（`:1044`）当前不可核 | 判据句补一句分支通路规则（过门后源不存在时的去向），并把该态补入 TC（与 TC-68 / TC-72 并列），使 P2 零回退有可机检面 |
| 4 | Clarity | 🟡 | 形态 ② 的字符类与负例面不一致：`owner` / `repo` 匹配 `[A-Za-z0-9._-]+`（`docs/design/SHELL-UX.md:983`）允许 `..` 与全点段（`github:../..` 可整体匹配），而「不含 `..` 段」的约束只写在 `#<片段>` 上；负例枚举（注① `:1404` / 注② `:1405` / TC-70 `:1494` / TC-73 `:1497`）无畸形或含 `..` 的 `github:` 形（`github:` / `github:owner` / `github:../..`）。另「两层关系」的蕴含论证只对「纯包名」成立（`:1000`），未逐形态说明 ② 是否进入两处 `path.join`（本评审未核下游代码，只报判据面） | 对 ② 的 owner/repo 显式排除 `..` 与全点段（或说明其下游后果），并把畸形 / 含 `..` 的 `github:` 形补入负例枚举；蕴含论证逐形态写明是否进入两处 `path.join` |
| 5 | Affected-file size | 🟡 | `market.js` 的 delta 口径自相冲突：§2.3 B12 表「预计改动量 = 净 **≤ 0**（−4 … +4）」与「末行数（预估）= ≤ 500（硬限——NFR-3）」不可同时成立（区间上界 +4 ⇒ 504 即破线），而 DD-47 已否决本批拆分该档（`docs/design/SHELL-UX.md:1216`）⇒ 无越限处置路径（现状 = 500 行，`docs/design/SHELL-UX.md:1155`；批次档同口径 `docs/batches/B12-market-security-blocking.md:134`） | 把预算收敛为「净 ≤ 0」的单值约束并写明越限时的处置或触发动作，使 AC30 ④ / TC-85 的 ≤500 判据在预算层面可保证 |
| 6 | Affected-file size | 🟡 | `shell-plugins.js` 由 396 → ≈461（`docs/design/SHELL-UX.md:1153`；批次档 `docs/batches/B12-market-security-blocking.md:132`）越 300 软档且逼近 500 硬限，但本批未带该档的体量复核：§2.2.6 的「>300 行档结论」对该档的理由是「≈370 + R7『只搬不改』窗口期不做结构改造」（`docs/design/SHELL-UX.md:609`），该理由对 B12 增量（三个新 helper + 六个函数增参 + 两处入口加门）已不适用；同档有同类裁定先例（B07 `shell-backend.js` 322 的「>300 软档体量裁定」，`docs/design/SHELL-UX.md:1118`） | 在 §2.3 B12 表补一条该档体量裁定（单文件保留理由 + 后续增量触线阈值 / 拆分触发条件），与 B07 先例同形 |
| 7 | Doc state | 🟡 | 设计档头部关联面滞后于本档正文与需求档：`docs/design/SHELL-UX.md:5`「关联需求档 …（US-1…US-11、NFR-1…NFR-5）」与 `:6-7`（关联批次只列 B06 / B07）仍是 B07 期值，而本档正文已承载 US-14 / US-15 / NFR-6 / NFR-7（`:38-40`、§2.2.14），需求档侧对应行已同步为「US-1…US-15、NFR-1…NFR-7」并含 B09 / B10 / B12 批次指针（`docs/requirements/SHELL.md:5-10`）；§2.3 B12 表的修订清单未列该头部行（`docs/design/SHELL-UX.md:1158`） | 把头部行同步为 US-1…US-15 / NFR-1…NFR-7 并补 B09 / B10 / B12 批次指针（纯计数同步，承 D3） |
| 8 | Acceptance criteria | 🔵 | NFR-6 第二分句枚举三形态（`innerHTML` / `insertAdjacentHTML` / HTML 字符串模板进 DOM；`docs/requirements/SHELL.md:271`），而 AC28 ① 与 手段 13 ① 的静态判据只对 `innerHTML` 计数 = 0（`docs/design/SHELL-UX.md:1399` / `:1538`）⇒ 判据覆盖面窄于需求分句（机制面已由节点构造消除该类形态，属判据补面） | 把兄弟符号（`insertAdjacentHTML` / `outerHTML` / `document.write`）并入同一判据，或加一条「无 HTML 字符串进 DOM」的等价判据 |
| 9 | Acceptance criteria | 🔵 | AC26 的「无子进程（计数器 = 0）」（`docs/design/SHELL-UX.md:1397`）与 手段 13 ② 自述的「`spawn` 在加载时被解构」（`:1542`）相抵：被解构的引用在模块加载后包装 `child_process` 不生效 ⇒ 该子判据可达性取决于夹具在 require 前完成包装 / 替换，设计未写加载顺序 | 在手段 13 ② 写明夹具加载顺序（先包装 / 替换后 require）或改用预载注入，使该子判据可达 |
| 10 | Acceptance criteria | 🔵 | 批次档 AC3 的「不执行任何 fs 写/删」半句在 AC27 无对应判据（AC27 ② 为函数级真值表、③ 只判「两处 `path.join` 之后各有一次包含判定调用」，`docs/design/SHELL-UX.md:1398`），而判据句要求越界拒绝「在 `fs.existsSync` / `rmSync` / `cpSync` / `mkdirSync` 之前返回」（`:996`）——顺序性无静态断言 | 补一条结构性判据（包含判定位于该分支内任何 fs 调用之前），使批次 AC3 该半句有落点 |
| 11 | Doc state | 🔵 | §2.3 B12 表的用例区间写 `TC-68…TC-85`（`docs/design/SHELL-UX.md:1156`），而 §3.2 实际到 TC-86（`:1510`）、变更记录记 TC-68–TC-86（`:1598`）、批次档 §2.2 ④ 记 TC-68–TC-86（`docs/batches/B12-market-security-blocking.md:102`） | 同步为 TC-68…TC-86 |
| 12 | Verification（限制） | 🔵 | 本次评审范围内只有三份文档 ⇒ 受影响文件表的源码行数 / 行号 as-of 值（`shell-plugins.js` 396 / `shell-market.js` 196 / `market.js` 500——`docs/design/SHELL-UX.md:1153-1155`；批次档 §2.5 `docs/batches/B12-market-security-blocking.md:130-137`）与两档自报的行数 / 行宽实测（批次档 `:139`；设计档 `:1600`）**本次无法抽查** = `unverified`（不据此判缺陷） | 由实施期实测回填（设计已声明该口径）或由持源码的角色复核 |

**评审范围外**（不赋严重度）：批次档 §1.5「建议发版前完成」与台账 T18 / T19 / T20 的核销面属主 agent 写域，本轮不评。

VERDICT: changes-required

计数：🔴 2 / 🟡 5 / 🔵 5（共 12 项；🔴 = #1 卸载面入参门两处描述互斥且与 US-14 相抵、#2 手段 7 与 手段 13 对「本批是否新增 `tests/` 文件」互斥）。

### 轮次 2（评审子代理）

**轮次 2 核验（源 = 本档 §2.10；面 = 四档 fresh read：`docs/design/SHELL-UX.md`（1755 行）/ `docs/requirements/SHELL.md` / 本档 / `docs/TODO.md`；行号 = 本轮实测）**

**一、第 1 轮 12 条发现逐条核验**

| 原# | 严重度 | 结论 | 证据（本轮实测行号 / 引文要点） |
|---|--------|------|--------------------------------|
| 1 | 🔴 | **已修** | 判据句 `SHELL-UX.md:1025-1028`（三形态对两函数照旧）+ 新增**卸载面解析门判据句** `:1038-1044`（「白名单**不缩窄**……被拒的不是形态，而是『**解析不到已装对象**』这一动作前提」）；行为差异表第 3 行 `:1106`（改后 = 解析门命中 / 形态仍过白名单）；AC27 ④ `:1499` + 判据细化 ③ `:1516`；AC31「拒绝消息形态（三类）」`:1503`；TC-76 `:1616`；改动面 `:1114`。需求档 US-14 契约句（`docs/requirements/SHELL.md:212`）未改且不再相抵 |
| 2 | 🔴 | **已修** | 手段 7 `:1642` 加「（B06 口径）」限定 + 例外注 `:1643`「**B12 例外（修正轮 1 #2）**：本批**新增 1 个仓内测试文件** … 全档对本批只保留**一种读法**」 |
| 3 | 🟡 | **已修** | 新增**分支通路条** `:1031-1033`（「**源存在 ⇒ 内置拷贝面；源不存在 ⇒ 落回 npm / pnpm 面**」；判定者 = `existsSync(bundledSource)`）+ 行为差异表第 2 行 `:1105` + TC-87 `:1627` |
| 4 | 🟡 | **已修** | 形态 ② 判据句 `:1026`（「且**排除 `..` 与全点段**」）；**逐形态论证** `:1056-1061`；注① 穿越枚举 13 形 `:1510`；注② 负例 `:1511`（补 `github:` / `github:owner` / `github:../..`）；TC-73 `:1613` |
| 5 | 🟡 | **已修（相邻面残留见新 N1）** | §2.3 B12 表 `:1239`「**净 ≤ 0**（**单值约束**——修正轮 1 #5）」；越限处置补正注 `:1249-1250`（> 500 ⇒ 停手上报 + 削减序） |
| 6 | 🟡 | **已修** | 新增「**>300 软档 体量裁定（`shell-plugins.js` 396 → ≈461…）**」`:1253-1255`（事实 / 理由 / 消解路径「实测 > 480 或再获增量批次 ⇒ 该批先做拆分复核」） |
| 7 | 🟡 | **已修** | 头部 `:5`「（US-1…US-15、NFR-1…NFR-7）」+ `:6-10`（B06 / B07 / B09 / B10 / B12 批次指针） |
| 8 | 🔵 | **已修** | AC28 ① `:1500` 与 手段 13 ① `:1667`（`innerHTML` / `insertAdjacentHTML` / `outerHTML` / `document.write` 各 0 处）；需求档 NFR-6 度量方式 `docs/requirements/SHELL.md:275` 同宽 |
| 9 | 🔵 | **已修** | 手段 13 ② 新增「**夹具加载顺序（硬）**」注 `:1673-1674`（计数器须在 `require('./shell-plugins.js')` **之前**就位） |
| 10 | 🔵 | **已修** | AC27 ③ `:1499` + AC27 判据细化 ② `:1515`（包含判定位于该分支内任何 fs 调用**之前**） |
| 11 | 🔵 | **已修** | §2.3 B12 表 tests 行 `:1240` = 「§3.2 **TC-68…TC-87**」 |
| 12 | — | **Not an issue（维持）** | 主 agent 实测 + 设计者独立复测同值；本评审面无源码 ⇒ 不据此判缺陷（本次仍标 unverified） |

**二、本轮发现（新）**

| # | 类别 | 严重度 | 问题 | 建议 |
|---|------|--------|------|------|
| N1 | Doc state | 🟡 | 行为差异表第 5 行仍记「新增**两类**拒绝消息」（`SHELL-UX.md:1108`），与判据句「**共三类**拒绝消息」（`:1065`）、AC31「拒绝消息形态（**三类**）」（`:1503`）、判据细化 ③（`:1516`）不一致——#1 修正的相邻面未同步（本档其余面均已三类） | 该行同步为三类，并核本档其余「两类」计数 |
| N2 | Clarity | 🟡 | 修正轮 1 同轮新增的两条对「② 形态是否进入第一处 `path.join`」描述互斥：分支通路条写「`kind === 'bundled'` ⇒ **计算** `bundledSource = path.join(...)` …… `kind === 'github' \| 'npm'` ⇒ 同走 npm / pnpm 面」（`:1031`），逐形态论证写「② GitHub 形态：**进入第一处 `path.join`**（`bundledName` 原样携带 `github:owner/repo`）」（`:1058`）。因白名单已排除 ② 的 `..` / 全点段，两种读法对枚举输入**行为同值**（故不按机制级相左计），但 AC27 ③ 的「两处 `path.join` 之后各有一次包含判定（grep）」判据与延续关系论证依赖该通路的明确性 | 择一写明：`bundledSource` 计算点与 `kind` 分派的先后（或注明「计算面对全形态、动作面按 `kind`」） |
| N3 | Acceptance criteria | 🟡 | AC28 ① 与 TC-79 的「`confirmModal(` 三处调用的**第二实参**均为节点构造 / 无模板字符串实参」（`:1500` / `:1619`）与判据句的**新签名** `confirmModal(title, okLabel, ...parts)`（`:1070`）错位——第二实参 = `okLabel`（字符串），节点构造面自**第三实参**起（`parts`）。**非本轮修正引入**（批次档 §2.6 `:147` 原文即按旧签名 `(title, html)` 记「第二实参非字符串」；第 1 轮漏检） | 判据与 TC 的实参序号对齐新签名（第三实参起 / 全部 `parts`） |
| N4 | Acceptance criteria | 🔵 | AC26 输入集（注①）已扩至 **13 形**（`:1510`），而 TC-70 / TC-71 的枚举仍列 9 形（`:1610-1611`，缺 `..` / `a/../../b` / `link:../x` / `builtin:/abs` / `github:../..`）——桩测输入面窄于判据声明的输入集（#4 新增形未同步 TC） | TC-70 / TC-71 输入集与注① 对齐，或注明 = 注① 的代表性子集 |

**三、旁证面（本轮点名面）**：`docs/TODO.md` 条目 T18–T23（`:37-45`）与 B12 设计档 O20–O23 / 批次档 §2.8 落档一致（含 T27 对「AC30 = B12 零回退面 / AC23 = B10 调用面」的更正），未发现新增矛盾。

**四、第 2 轮结论**：第 1 轮 2 条 🔴 均已真落地；本轮**无新增 🔴**（N1–N4 全为 🟡/🔵，不阻断）。

**计数：🔴 0 / 🟡 3 / 🔵 1（新发现 4 项）；第 1 轮 12 项 = 11 已修 + 1 维持 Not an issue**

VERDICT: pass

## §4 评审裁决与实施启动（主 agent）

## §5 实施记录（eng-coder）

### 5.1 交付摘要（2026-09-17）

- **写域 4 档（实测行数 = 换行符计数）**：`shell-plugins.js` 396 → **454**（+58）· `shell-market.js` 196 → **198**（+2）· `market.js` 500 → **500**（改 20 行 / 删 20 行 = **净 0**，贴 500 硬限零余量）· `tests/b12-plugin-guards.test.js`（新建）**491** 行（不入 `build.files`、不登记 `package.json`）。
- **命令与结果**：`node --test tests/b12-plugin-guards.test.js` ⇒ **tests 19 / pass 19 / fail 0**（退出码 **0**）；`node --check` × 4 档全绿；`market.js` 全文 `innerHTML` / `insertAdjacentHTML` / `outerHTML` / `document.write` 各 **0** 处；`confirmModal(` = 4 处（1 定义 + 3 调用）。
- **零 diff 面（实测）**：`market.html` / `market-preload.js` / `market-update.js` / `exchange.js` / `shell-ipc.js` / `shell-pet-geometry.js` / `shell-pet-drag.js` 零 diff；`package.json` 的改动为**他人批次在飞**（`bundle:refresh` / `bundle:check` 两条 scripts），本批未碰（`dependencies` 仍 `{}`、`build.files` 无 `tests/`）。
- **行宽 / 行数**：4 档最宽 = 131 / 158 / 226 / 214（判据 ≤300）⇒ 超 300 行数 = 0；`market.js` = 500 ≤ 500 硬限；末行换行齐备；EOL 与各档改前一致（`shell-*.js` CRLF、`market.js` 与 tests LF）。
- **实施顺序（§2.8 硬约束）**：① 先在**改前**代码上（同一夹具）跑 `computePluginUpdates`，把返回值与 `updaterLog` 行序列冻结进测试常量（见 §5.2）；② 再改代码；③ 跑夹具 + 静态判据组。

### 5.2 黄金样本与夹具定义（§2.8① / 设计档 §3.3 手段 13 ③ 回填）

**夹具载体**：每用例 `fs.mkdtempSync` 独立临时目录（`after` 钩子清理）；`electron`（`{app:{isPackaged:false,getAppPath,getPath}}`）与 `./shell-backend.js`（`{dshHome:()=><root>/home}`）经 `Module._load` 拦截注入；子进程计数器同法在 `require('./shell-plugins.js')` **之前**就位（手段 13 ② 的加载顺序硬约束）。

**扫描面夹具**：`<root>/home/profiles/web/package.json` = `{name:'web', dependencies:{'dsh-pet':'1.0.0'}, dsh:{profile:{bundles:['dsh-pet']}}}`；`node_modules/` 下 `dsh-pet`（`version:1.0.0`）· `dsh-no-version`（**无** `version` 字段）· `@scope/pkg` · `.pnpm/`（点目录）· `.modules.yaml`（点文件）。
**注册表（N=3）**：`{name:'dsh-pet', npm:'dsh-pet', version:'9.9.9'}` · `{name:'nope', npm:'dsh-nope', version:'2.0.0'}`（未装）· `{name:'nv', npm:'dsh-no-version', version:'3.0.0'}`（装了但读不到版本）。

**黄金样本（改前实测冻结，逐字——`tests/b12-plugin-guards.test.js` 的 `GOLDEN_UPDATES` / `GOLDEN_LOG`）**：
- `updates` = `[{id:'dsh-pet', name:'dsh-pet', updateSpec:'dsh-pet@9.9.9', latestVersion:'9.9.9', installedVersion:'1.0.0'}]`
- `updaterLog` 行序列 = `['plugin update spec=dsh-no-version result=skip detail=no-installed-version']`

**计数器夹具（TC-81）**：同一夹具，N=2 = `[P_UPDATE, P_NOVER]`、N=3727 = 同两条循环填充；断言三计数相等 + `node_modules` 顶层 `readdirSync` = 1 / 次调用；另 TC-82 用「无 manifest / 空 `node_modules`」夹具断言不抛 + 返回空集。

**改前 ⨯ 改后逐面对照（仓外一次性探针，不入仓）**：夹具含 bundles 挂名、scoped、`@deepseek-ai/dsh`、点条目、散文件、`github:` 重装面、`link:`、缺版本、同/低/高版本、未装；13 条注册表 × 7 面对照面（`updates` / `log` / `installed` / `disabled` / `resolve` / `profiles` / `plain`）⇒ **差异面 0**（改前取 `git show HEAD:shell-plugins.js`）。
**注册表误拒探针**：本仓 `plugins.json`（35 条 = 12 `github:` + 23 裸名）⇒ `installSpecKind` **误拒 0**；设计期的**在线 3725 形态**实测值仓内不可复跑（记为设计期一次性证据，非本批判据）。

### 5.3 决策透明表（实施期裁定，逐条给理由）

| # | 事实 / 岔路 | 选择与理由 |
|---|---|---|
| 1 | 两处包含判定的 base 实参用 `path.join(profileDir(),'node_modules')` 会使命中判据「每函数两处 `path.join`」按 grep 变 3 处 | 新增 `nodeModulesDir()`（`shell-plugins.js:29-32`）作 base 单一来源 ⇒ 两函数各 **2** 处 `path.join` + 2 处 `isInsideDir`（grep 实测），判据字面与实现对齐；代价 +3 行（454 < 480 消解期阈值） |
| 2 | 裸纯包名归 `'bundled'` 还是 `'npm'` | `isPlainPackageName(spec)` ⇒ `'bundled'`；`github:` / `name@ver` 不进内置面（设计档 `:1037` 判据句）⇒ 内置分支条件 = `kind === 'bundled' && fs.existsSync(bundledSource)` |
| 3 | `frag()` / 段落 helper 落 `market.js` 的形态 | 各压成 1 行（`frag` / `para`），配 1 行组注释——500 行硬限下保住两个设计点名 helper（未走「内联」削减序） |
| 4 | `confirmModal` 的正文包裹 | `showModal(title, frag(...parts), okLabel)` ⇒ 正文直接子节点 = `P`/`CODE`/`B`（去掉旧 `el('div')` 空包裹；无类名无样式 ⇒ 外观等价，`m-body` 的 `textContent` 与改前逐字相同） |
| 5 | 非字符串 / 非节点实参（注册表字段类型异常） | `el` / `frag` 内统一 `p.nodeType ? p : createTextNode(String(p))` ⇒ 与改前 `textContent = value` 的强制语义一致（**评审修正轮 1 #1**，1 行内改，`market.js` 仍 500） |
| 6 | 卸载面动作面用「原始入参」还是「解析名」 | 沿用**原始入参**（与改前 `:359` / `:361` 逐字同源，零回退优先）⇒ `builtin:<名>` 形态的假成功面**报而不改**（见 §5.6 残余①） |
| 7 | 卸载面解析门的位置 | 入参门之后、两处 `path.join` 与动作面之前（判据句「通过入参门之后、**任何动作之前**」；两处面判定仍紧随各自 `path.join`） |
| 8 | XSS 面沙箱载入面 | 同时 `vm.runInContext` `market.js` + `market-update.js`（`market.html:205-206` 的同页第二只 `<script>`）⇒ `updOf` 用**真代码**而非手写桩，避免 sibling 契约漂移 |
| 9 | 桩测 DOM 桩的 `innerHTML` | 桩内保留**标记解析器**（回归哨兵：若实现回退到 `innerHTML`，注入出的元素节点会被断言捕获），而非把 `innerHTML` 做成空操作 |
| 10 | 弹窗面取证的「空过」风险 | `elementTags()` 对 `#fragment` **不登记但递归**，并加「取证面非空」自检断言（审计 D4 修复） |

### 5.4 审计与代码评审轮次与终态

- **轮 1 · 内部探索审计（只读子代理，实现 vs 设计偏离审计）**：VERDICT = **divergence-found**（🔴 0 / 🟡 4 / 🔵 4）⇒ 实修 2 条（D4 = 弹窗面断言空过；D3 = 计数口径与实现不符）· 转本段落档 1 条（D1/D2 = §5 与实测回填）· 余 4 条为观察面（真机/人工项、行宽判读面）随报告提请。
- **轮 2 · 内部 advisor 代码评审（同会话同步节点）**：VERDICT = **pass**（🔴 0 / 🟡 3 / 🔵 5）⇒ 裁决见 §5.5。
- **终态 = `clean`**：两轮独立评审均无 🔴；19/19 夹具绿；零 diff 面实测零 diff。

### 5.5 裁决表（轮 2 advisor 发现 → 处置）

| # | Action | Detail |
|---|---|---|
| 1 🟡 | Fixed | `el()` 非字符串实参走 `appendChild` 原值 ⇒ 注册表字段类型异常（`owner` / `description`）时真机 DOM 抛错、整页渲染落 catch。已改 `market.js:48` + `market.js:52`：`e.appendChild(p.nodeType ? p : document.createTextNode(String(p)))`（`frag` 同改），并补夹具断言（`tests/b12-plugin-guards.test.js:361-363`，`description:{}` / `owner:{}`）——`market.js` 行数不变（500） |
| 2 🟡 | Deferred | 卸载面动作名用原始入参 ⇒ `uninstallPlugin('builtin:<已装名>')` 返回 `{ok:true,'已卸载 builtin:<名>（pnpm 有警告，已清理注册）'}` 而实际未删未注销（`shell-plugins.js:412/415/421/424`）。**未改理由**：与改前逐字同源（零回退面），改它 = 变更 `pnpm remove` 的实参语义（设计档「行为差异表」未覆盖该面）⇒ 属设计面裁定，随报告提请主 agent（见 §5.6 残余①） |
| 3 🟡 | Fixed | 本节（§5）原为空 ⇒ 已写入（含黄金样本、夹具定义、实测行数、裁决与残余面） |
| 4 🔵 | Not an issue | 测试内 `confirmModal(` 计数判据（4 处）与 `slice(at, at+420)` 窗口：两者都是**响亮失败**而非静默通过，且计数本身编码 AC28 ④ 的「1 定义 + 3 调用点」判据（调用点增减应使夹具失败）——保留 |
| 5 🔵 | Fixed | 「文案逐字不变」无机检面 ⇒ 已加**文案冻结断言**（`tests/b12-plugin-guards.test.js:351-360`：三处弹窗的标题与正文 textContent 逐字等于改前 HTML 串的 textContent） |
| 6 🔵 | Deferred | `shell-market.js:107` 的 `'无效的插件标识'`（无冒号、不回显）与新门族 `'无效的插件标识：<入参前 60 字符>'` 命名不一致。**未改理由**：该行不属本批设计声明的改动面（§2.3 B12 表只列 `marketList` / `marketState`）⇒ 属另批或判据口径注 |
| 7 🔵 | Not an issue | 「`scanProfile().names` 含 bundles 是否与改前等价」的疑虑：已取改前源码实证 `git show HEAD:shell-plugins.js` ⇒ 改前 `listInstalledPlugins()` 首行即 `const names = new Set(profileBundles());` ⇒ 逐条等价（本批实现 `shell-plugins.js:119` 同形） |
| 8 🔵 | Deferred | `shell-market.js` 的 `return ({...})` 空包裹与三处注释缩进偏差为**改前形态** ⇒ 本批不顺手改（避免在零回退面上多动行） |

### 5.6 残余风险与人工/未覆盖项（如实；逐条给取证方式）

① **卸载面 `builtin:<名>` 假成功（承 §5.5 #2，未改）**：取证 = TC-76 只覆盖「解析不到已装对象」，`builtin:<名>` 形态无 TC；建议主 agent 裁定「卸载面动作名用解析名」或把该形态写成显式失败，并补 TC。
② **AC31 第二条前缀（`插件标识越界，已拒绝：…`）的取证方式**：白名单在场时该分支**运行时不可达**（设计档「已知事实（不可达面）」）⇒ 本批取证 = **函数级真值表**（TC-74，5 例）+ **静态命中**（`tests/…:170-176` 断言前缀串 + 三个符号在场；`shell-plugins.js` 内三前缀齐备）——**未**为构造命中而新增「过门但越界」输入。
③ **真机人工项未执行（非机检面）**：AC28 ③（注入恶意条目后开市场目视）· TC-83（`node_modules` 不可读人工构造）· TC-86（本地注册表注入恶意 `name` 的真机 DevTools 断言）——留给交付评审 / 用户真机回归。
④ **`package.json` 零 diff 判据的工作区实况**：本工作区有他人批次在飞改动（`scripts.refresh-dsh-bundle` / `bundle:check`），故 `git diff` 于该档非空；本批未写入该档（`dependencies` = `{}`、`build.files` 无 `tests/` 实测）——AC30 ① 按「本批未动」口径成立。
⑤ **设计档 as-of 数值漂移（报而不改——非本角色写域）**：`docs/design/SHELL-UX.md` §3.3 手段 13 ① 记「全档 **26** 处 `path.join`」⇒ 本批实施后实测 **21** 处（扫描面去重所致；判据本身**按面限定**成立：守卫面 4 处 + 顺序性成立）；§2.3 B12 表 `shell-plugins.js` 行「≈461 → 实施期实测回填」与 tests 行「≈260」的实测回填尚未落笔（实测 = **454** / **491**）。
⑥ **夹具时长**：`TC-82` 单例 ≈756 ms（>500 ms 慢测归册阈值）——仓内暂无慢测层机制（门禁 0/3，T4），全档运行 ≈1.2 s；若后续引入 `slow()` 归册，TC-82 为首个候选。
⑦ **既有面（本批不改、如实登记）**：`market.js:116` 的 `(p.name || p.npm || installSpec).replace(...)` 对**非字符串** `name` 会抛（改前同形，属 `normalizePlugin` 既有面）；O20（`market.js:262` `href` 无导航守卫）· O22（内置「一键安装」不可达）· O23（`shell-market.js:129/133` 为打日志而扫描）三报告项现状与设计档一致。

### 5.7 本段写入状态

§5 **已写入**（经 `batch_segment`，无路径参数；内容 = §5.1–§5.6 + 本行）。

## §6 验收核销（主 agent）

**验收依据** = 本档 §2.4 AC26–AC31 + 设计档 §3.1 逐条 + **主 agent 独立实测**（一手证据，不采信自报）。**安全批 ⇒ 闸门由主 agent 亲自跑。**

### 6.1 AC 逐条（证据 = 主 agent 亲跑 / 亲读）

| AC | 判据 | 证据（主 agent 一手） | 结论 |
|---|---|---|---|
| **AC26** | 入参三形态门 + 穿越枚举全拒 | **亲跑** `node --test tests/b12-plugin-guards.test.js` ⇒ **TC-70 / TC-71 绿**（13 形×两函数全 `{ok:false}` + **夹具全量快照零改动** + `spawn` 计数 0）；**亲读** `installSpecKind`（`shell-plugins.js:323-343`）：`builtin:` / `github:owner/repo[#frag]`（owner/repo 排除 `..` 与全点段）/ 裸纯包名⇒`bundled` 三形态 —— **与设计档 §2.2.14 `:1047` 判据句逐条对得上**（该句**无 `npm:` 形态**；`npm` 仅是 `name@ver` 的 *kind*） | ✅ |
| **AC27** | 包含判定四处守卫面 + 顺序 | **亲跑** TC-74 真值表绿；**亲 grep** `isInsideDir` 调用 = **恰 4 处**（`:362` `:366` `:413` `:416`）+ 定义 1，与「每函数 2 处 path.join 之后各一次判定」相符；`path.join` 全档 **21** 处（设计期 26 ⇒ 实施后 21，已回填） | ✅ |
| **AC28** | 注入面归零 | **亲 grep** `market.js`：`innerHTML` / `insertAdjacentHTML` / `outerHTML` / `document.write` **各 0** ✓；**亲跑** TC-77/78 绿（恶意 name/desc/owner/url 三形态 ⇒ 弹窗与卡片**只产生文本节点**）；**真机人工项未跑（见 6.3）** | ✅ |
| **AC29** | 单次快照 / 逐项判定零回退 | **亲跑** TC-81（N=2 与 N=3727 的 fs 计数相等）· AC29 快照复用绿 · TC-80 黄金样本绿（`updates` 与 `updaterLog` 行序列 = 改前冻结常量） | ✅ |
| **AC30** | 零回退面 | **亲读**：四档行数 = `shell-plugins.js` **454** · `shell-market.js` **198** · `market.js` **500**（**贴硬限、零余量**）· 新测试档 **491**；`node --check` ×4 绿；`shell-ipc.js` / `market.html` / `market-preload.js` / `market-update.js` / `exchange.js` 零 diff | ✅ |
| **AC31** | 拒绝消息三类 | TC-70/71（`无效的插件标识：…`）· TC-76（`未安装或无法解析：…`）绿；第二条前缀（`插件标识越界，已拒绝：…`）= **函数级 + 静态**（TC-74 真值表 + 前缀串静态命中）；**白名单未开口、未新增构造输入** ✓ | ✅ |

### 6.2 交付终态实测（主 agent 一手）

- **测试档**：`node --test tests/b12-plugin-guards.test.js` ⇒ **tests 19 / pass 19 / fail 0**，退出码 **0**（主 agent 亲跑）。
- **零回退**：`market.js` 净 0 改动（改 20 / 删 20）· `shell-plugins.js` +121/−63 · `shell-market.js` +8/−6 · 新档 491 行。
- **依赖 / 构建面**：`dependencies` 仍 `{}` · `build.files` 零改动（`tests/` **不入包**）✓。

### 6.3 残余与处置（不藏）

1. **AC28③ / TC-83 / TC-86 = 真机人工项**（真机弹窗目视 / `node_modules` 不可读构造 / DevTools 断言）⇒ **归用户真机回归**（本批未跑，如实标注）。
2. **L-B12-4 / T32（卸载面 `builtin:<名>` 假成功）**：**与改前逐字同源、非本批回归**；设计档已登记（现象 / 根因 / 修法方向 / 台账承接）⇒ 归 **B17** 与 T22 同轮。
3. **行锚 as-of 漂移**（F-B12-5：守卫面行锚 `:313/:316/:359/:361` → 现值；F-B12-6：§2.3 表文档自指行 299/1601 → 319/1847）⇒ 承 as-of 口径，归 **B14 文档事实收口**（与 T31 同类）。
4. **测试资产归册裁定（主 agent）**：`tests/b12-plugin-guards.test.js` = **集成资产（② 业务场景设立）**——断言全在业务可观察面（入参被拒 / 文件零改动 / 消息文案 / 节点面）+ 未被其他面覆盖 + 可稳定驱动 ⇒ **保留**（不归 ① 开发期退役）。
5. **慢测归册**：**TC-82 ≈749 ms**（> 500 ms 阈值）⇒ 登记为慢测层首个候选（归 **T26 / B16 门禁批**——仓内现无慢测层机制）。
6. **仓外探针退役**：`%TEMP%\b12-equivalence-probe.cjs` / `%TEMP%\b12-registry-probe.cjs` = 开发期一次性工具 ⇒ **退役**（不转集成资产；证据已入 §5，仓外无删除动作）。

### 6.4 台账

- **T18**（路径穿越）/ **T19**（主线程同步阻塞）/ **T20**（注册表字段 XSS）→ **已核销**（本节证据）；归档动作由主 agent 执行。。
