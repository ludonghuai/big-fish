# B28 —— 插件面小修（用户可见缺陷三处）

## §1 立案（主 agent）

### 1.1 目标（一句话）

修掉插件 / 后端面三处「点了没反应或假成功」的缺陷：一键安装不可达 · 卸载 `builtin:` 假成功 · 旧后端进程清理对 Windows 不生效。

### 1.2 任务来源

台账技术待办（均已实测、非本批引入、用户可见或静默失效）：**T22**（一键安装死码）· **T32**（卸载假成功）· **T30**（进程清理通配不匹配）；顺带 **T23**（插件市场面同族两项低风险卫生）。用户 2026-09-19「该立项的赶紧立项」。

### 1.3 现状实测（证据，as-of 2026-09-19）

- **T22**：`market.js:99-103` 的 `normalizePlugin` 只从 `install` 字段提 `add …` 形态，而内置条目 `install` = 字面量 `builtin:<name>` ⇒ `installSpec === undefined` ⇒ `market.js:275` 的「一键安装」为**死码** ✗（对照：`shell-plugins.js` 的 `builtin:` 分支**已实现** ✓）。
- **T32**：`shell-plugins.js:412` / `:415` / `:421` / `:424` 卸载动作面用**原始入参** ⇒ `uninstallPlugin('builtin:<名>')` 返回 `{ok:true, '已卸载 builtin:<名>…'}` 而**实际未删未注销** ✗（B12 交付报告 §5.6 残余 #4，主 agent 亲验）。
- **T30**（**实位订正**：B28 设计轮实测 `scripts/ensure-deps.js` 94 行全读**无此代码** ⇒ 死指针）：`shell-backend.js:188`（`cleanupStaleDsh`）的 PowerShell 清理通配对反斜杠路径不匹配 ⇒ 用户运行中的旧后端**未被清**（B08 §5.6 范围外注记实测）⇒ 依赖变更后旧后端可能仍占端口 / 跑旧代码。
- **T23**：`market.js` 两项低风险卫生（细节 = 台账 T23 行）。

### 1.4 范围（做 / 不做）

**做**：① T22（让「一键安装」走通 **或** 移除死码 —— 见 U-1）；② T32（动作面改用 `resolveInstalledName` 的解析结果）；③ T30（清理通配修正）；④ T23 两项。
**不做**：① **不夹带重构**（台账 T39 / T42 原文警示：不得在修复轮夹带 ✗）；② 不改插件面其它口径（B12 已收口面逐字不回退 ✗）；③ 不动 T18 / T19 / T20（已收口 ✓）。

### 1.5 硬约束

1. **行为修正须留取证行**（T42 家族教训：早退 / 异常分支在 debug 开关下至少一行）。
2. 每处修复 = **最小改动**；B12 已锁定守卫面（三形态白名单门 / 节点构造 / 单次快照）逐字不动。
3. 三道门 + lint；B12 写域冻结面机检零 diff。

### 1.6 待决项（须裁定后才能定稿设计）

- **U-1**：T22 修复方向——**A 补可达路径**（`normalizePlugin` 认 `builtin:` 形态 ⇒ 一键安装走通；保留用户可见能力）vs **B 移除死码**（UI 去掉该按钮）——推荐 **A**（最终由用户裁）。
- **U-2**：T30 是否同轮（跨面：脚本层 vs 插件层）——推荐同轮（同属「静默失效」家族）。

### 1.7 关联台账与需求档

台账 **T22 / T23 / T30 / T32** → 本批（销账条件 = 修复 + 机检 + 用户实机确认）；需求档：无新增（缺陷修复；行为口径以 B12 已收口面为准）。

### 1.8 交付物与排期

交付物 = 四处修复 + 行为面取证（before / after）+ 本档六段。**零在途冲突**（`shell-plugins.js` / `market.js` / `scripts/ensure-deps.js` 无在飞写者）⇒ 可立即派设计。

### 1.9 变更记录

| 日期 | 变更点 |
|---|---|
| 2026-09-19 | 建档（B28 立案）：T22 / T23 / T30 / T32 合批；U-1 修复方向待裁 |

---

## §2 本批任务书（eng-designer）

<!-- 由 eng-designer 填 -->

---

### 三方一致清单（硬）

本批条目 **I1–I5** = 设计档 `docs/design/SHELL-UX.md` §3.1 **AC32–AC36** 回指条目 = 需求档**无新增**（缺陷修复面——回指锚 = `docs/requirements/SHELL.md` **US-14 / US-15** / NFR-6 / NFR-7；行为口径以 B12 已收口面为准）。设计档 §3.1「B28 回指口径」与本清单同源。

| # | 条目 | 设计档承载 | 验收 |
|---|---|---|---|
| I1 | T22：内置条目「一键安装」补可达路径（U-1 裁 **A**——`normalizePlugin` 认 `builtin:` + `bundledNames` 目录过滤） | §2.1 N-1 · §2.2.15 ① · DD-51 | AC32 |
| I2 | T32：卸载面动作面改用解析门 `realName`（`builtin:` / `github:` 形态统一落法） | §2.1 N-4 豁免 · §2.2.15 ② · DD-52 | AC33 |
| I3 | T30：Windows 陈旧后端清理通配双形态 `-like`（**实位 = `shell-backend.js:188`**；台账/§1.3 指针错误见下「提请主 agent」） | §2.1 N-2 · §2.2.15 ③ · DD-53 | AC35 |
| I4 | T23：市场面卫生两项（① 「主页」链接不再直赋注册表 `url`；② 删两条「为打日志而扫描」的调试行） | §2.1 N-3 / N-5 · §2.2.15 ④⑤ · DD-54 / DD-55 | AC34 |
| I5 | 零回退 + 门禁三连（B12 锁定面逐字不动 + lint / test:full / test:integration 全绿） | §2.2.15 行为差异表 · C45 · DD-57 | AC36 |

### 实施面（含受影响文件与行数）

- `shell-plugins.js`（454 行）：`uninstallPlugin`（`:408-428`）动作面 6 行（`bundledSource` / `target` / `removeBundle`×3 / `pnpm remove` 实参）与 3 条成功消息由 `pkgName` → `realName`；**±0 行**。门 / 越界 / 解析门 / 消息前缀逐字不动（C45）。
- `market.js`（**500** 行）：`normalizePlugin` 删注释行 + 增 `builtin:` 识别分支（净 −1）；删 `link.href = p.url`（−1）⇒ **净 −2（498 行）**；实测 >500 ⇒ **停手上报**（§2.3 B28 表下注）。
- `shell-market.js`（198 行）：`bundledNames`（`:101`）单行替换为目录过滤；删两条调试行（`:129` / `:133`）⇒ **净 −2（196 行）**。
- `shell-backend.js`（323 行）：`:188` PowerShell 通配单行替换为双形态 `-like`（`*dsh/lib/bin.js*` + `*dsh\lib\bin.js*`）⇒ ±0。
- `tests/b28-plugin-fixes.test.js`（新，≈150 行）：T32 卸载夹具 + T22 vm 桩 + 静态 grep 断言（设计档 §3.3 手段 14）。
- **零 diff**：`shell-ipc.js` / `market.html` / `market-preload.js` / `market-update.js` / `package.json` / `tests/b12-plugin-guards.test.js`。

### 验收

- **AC32–AC36**（判定方式逐条 = 设计档 §3.1；机器面 = 桩测 + 静态 grep + 门禁三连；真机人工 = TC-96）。
- 销账条件（§1.7）= 修复 + 机检 + **用户实机确认**（TC-96：一键安装 → 重启生效 → 卸载走通；运行中旧后端被清）。

### 明确出批（不做）

- 不改 IPC 通道名 / 参数契约 / 返回字段；不改白名单 / 越界 / 解析门判据句与消息前缀（三前缀拒绝消息逐字不回退）。
- 不改 `installPlugin` 与更新链（`pluginUpdateSpecOf` 已 skip `builtin:`，本批不触）。
- 不拆 `shell-plugins.js`（B12 体量裁定消解期复核结论 = **不拆分**——±0 行变量面替换；拆分即「修复轮夹带重构」；重登口径 = §2.3 B28 表下注）。
- 不改 POSIX 清理侧（O26）；不做「已装未收录条目」的 `installSpec` 补全（O25，语义面超批）。

### 待用户追认（设计已按推荐值落全档；评审时可由用户改判）

- **U-1 → A**（补可达路径；B 否决理由 = 设计档 N-1）。
- **U-2 → 同轮**（T30 与 T22 / T32 / T23 同批实施）。

### 提请主 agent 处置（本角色写域外）

- **O24 死指针**：台账 T30 行 / 批次档 §1.3 / B08 §5.6 记「`scripts/ensure-deps.js` 的清理通配」——实测该档无此代码（94 行全读；唯一命中 = `shell-backend.js:188`）⇒ 请更正为 `shell-backend.js`。
- **O25**（语义面，建议登记台账或另批）：`market.js:455-464` 兜底条目 `installSpec` 恒 undefined ⇒ 「已安装」标签页过滤掉 AI 安装的未收录插件（与 `:452-453` 注释意图相抵）。
- 设计评审发起权在用户（评审时传 `batchDoc`）。

### 三方声明

- 需求档无新增（缺陷修复面）；设计档 = `docs/design/SHELL-UX.md`（§2.1 选型 N / §2.2.15 / §2.3 B28 表 / §2.4 DD-51…DD-57 / §2.5 C41–C47 + O24–O26 + L-B28 指针 / §2.6 U-16 / §3.1 AC32–AC36 / §3.2 TC-88–TC-96 / §3.3 手段 14 / 变更记录）。
- 本批条目（I1–I5）= AC32–AC36 回指 = US-14 / US-15（锚）——三方一致。

### 修正轮 1 修正记录（eng-designer；源 = §3 轮次 1 发现 #1–#9；主 agent 裁决落地）

**#1（🔴，裁决 = 路径①）**：静态 grep 面移出测试档——设计档 §3.3 手段 14 ① 改为「实施期亲 grep 取证行」（原始输出写批次档 §5、不落测试档内），TC-94 / AC32–AC35 静态项 / §2.2.15 取证行与改动面清单 / §2.3 B28 表两行同步；测试档只留行为面断言（T32 卸载夹具 + T22 vm 桩）。
理由 = 规范面（断言面纪律 / AC-B16-9）为未限定适用范围之明文（实位 `docs/design/REPO-CONVENTIONS.md:458` / `:653` / `:315`）；路径① 同时满足规范面与取证需求（承 B12「四符号各 0 亲 grep」先例 = B12 批次档 §5 AC28 收口行）。
自检 = 全文 grep「测试档内…读源码」零残留 ✓。

**#2（🟡）**：§2.3 B28 表下注补 `market.js` 体量裁定复核条（事实 = 500 贴线零余量 / 结论 = 本批不拆分（修复轮不夹带重构 + 净 −2 单值约束）/ 消解路径承 T2 路由 + 触发条件两条——对应 `REPO-CONVENTIONS.md:188` 贴线档必填面）。

**#3（🟡）**：收敛锚句 = 回指锚 US-14 / US-15（并含 NFR-6 / NFR-7）＋补适用锚 NFR-1（AC35 / T30 启动面）· NFR-3（AC36 / 门禁三连）——落 AC35/AC36 回指列 + 设计档 §3.1 B28 回指口径；本段即批次档侧同文承载（§2 三方一致清单按 append-only 不重写，生效口径以收敛锚句为准）。

**#4（🟡）**：U-15 行与 §2.6 B12 追认块 O22/O20 条补「已修（B28）」注。

**#5（🔵）**：`tests/b12-plugin-guards.test.js` 行数实测 = **492**（换行符计数，as-of 2026-09-19；B12 收口 491 / B16 面预计 493 均不采信）——§2.3 B12 表行 / 回填注 / DD-57 三处同步。
`docs/design/REPO-CONVENTIONS.md:436/:572` 的 491 同族面不在本批写域，随报告提请处置。

**#6（🔵）**：设计档 §四 变更记录补 B28 轮「补正（落档实测）」行 + §2.3 B28 表自指行按实测回填（**2207**，含 B30 同档并行面）。

**#7（🔵）**：`normalizePlugin` 行锚统一两口径——函数范围 = `market.js:100-135`（36 行；§2.3 B28 表下注定义）、本批改动行 = `:104-108`（§2.2.15 改动面清单定义；实测核对 market.js 源码 ✓）；§2.2.15 提字段行 `:100-108` 订正为 `:104-108`。

**#8（🔵）**：设计档头部关联批次补 B28 指针（注明需求档无新增故其头部不列）。

**#9（🔵）**：O24 / C46 表述收敛——残余更正面 = 台账 T30 行 / B08 §5.6 注记（批次档 §1.3 已含「实位订正」句）；本段 §2 I3 行的「台账/§1.3 指针错误见下」按同口径收敛（§1.3 已订正，残余面如上）。

**行锚漂移说明**：评审报告 §3 的三条规范面行锚因并行轮写入漂移（非内容错误）；实位 = `REPO-CONVENTIONS.md:572`（b12 491→493 行）/ `:579`（其拆分计划）/ `:188`（market.js 贴线纪律）/ `:418`（C 判据 ≥480）/ `:436`（C 现状）——本修正记录与设计档修正均按实位引用。

**§2 历史行取代说明**：本段「实施面」的 `tests/b28-plugin-fixes.test.js ≈150 行 / 静态 grep 断言（设计档 §3.3 手段 14）」与本段「三方一致清单 / 三方声明」的回指锚句按 append-only 保留，生效口径分别以本修正记录 #1 / #3 为准（权威面 = 设计档）。

**门禁与落档实测**：设计档 **2207 → 2222**（换行符计数；+15 = 内容修订 +9 / 变更记录 5 行 / 压行 +1）；行宽实测 **0** 行超 300（不含行尾 CR）；EOL 全档 CRLF；`docs/requirements/SHELL.md` 355（本轮未改该档）。
**lint = FAIL（19/20；SELFTEST-FAIL TC-B29-03）——违规面 = `scripts/gates/**` 的 B29 样本区零引用判据自证夹具（pkg-build+pkg-extra 实得 3 / ref=4），属别批在飞面**（B29-samples-guard 实施中；git status 实证 `scripts/gates/checks.js` / `run.js` / `selftest.js` 三档未提交改动）；本批写域两档不涉该面——如实标注。

### 设计档回填轮（eng-designer；源 = §5 实施记录「决策透明表」/「存疑项」+ 父侧裁定；三件）

**#1 偏差登记（越单改动 = 接受 + 登记）**：实施在 `shell-market.js:88`（`marketList` 的 `bundledNames`）加的同形目录过滤为**实施期补全**（设计改动面清单原只列 `:101`）——父侧裁定 = **接受 + 登记**；
理由 = 市场页初始加载走 `refresh() → api.list() → marketList`，只滤 `:101` ⇒ §1.3 实测的「README.txt 幻影卡片」在初始加载仍出现 + T22 落地后幻影条目还会获得可点击的「一键安装」（`builtin:README.txt`）；补全 = 履行 §1.3 需求。
落点 = 设计档 §2.2.15 改动面清单（补 `shell-market.js:88` + 实施期补全注）· §2.3 B28 表 `shell-market.js` 行（实测回填 **196**——含补全 ±0，与净 −2 预算一致）。

**#2 TC-92 口径修正（两子判据互斥）**：期望面两子判据（「`node_modules/dsh-x` 删除」与「`pnpm remove` 实参 = `dsh-x` + `spawn` 1」）在实现上互斥（内置分支删目录但零 spawn；pnpm 分支 spawn 1 而目录删除由真 pnpm 完成、桩内不删——`shell-plugins.js:421-428` 实核）——
父侧裁定 = **取实施口径**（动作面取证优先）：TC-92 期望改单一取值面（实参断言 + `spawn` 计数 1）＋注「删除面 = TC-91 覆盖 ✓」；落点 = 设计档 §3.2 TC-92 行。

**#3 实测值同步**：`tests/b28-plugin-fixes.test.js` 实测 = **288 行**（换行符计数 / 去尾空行口径；含尾空行口径 +1 = 289，as-of 2026-09-19）——落点 = 设计档 §2.2.15 改动面清单 + §2.3 B28 表（**0 → 288**）；本 §2 历史行（「≈150 行」）按 append-only 保留，生效口径 = **288**。

**附带一致性修正（报告项）**：§2.3 B28 表 tests 行用例区间 `TC-88…TC-95` → **`TC-88…TC-93`**（原区间含 TC-94 静态面 / TC-95 门禁面——均不落测试档，与同行「行为面断言 only」限定相抵；实测覆盖 = TC-88…TC-93，源 = §5 / 测试档档头）。

**存疑提请（待父侧裁；本轮未动）**：设计档 AC33 的 `github:owner/dsh-x` 子句「⇒ 同」——若按字面读含「profile 副本目录不存在」，则与本轮 TC-92 修正同族（桩内不可验：pnpm 分支不删目录，`shell-plugins.js:421-428`）；
本轮按父侧字面**只动 TC-92**、AC33 未动——如需同口径收口请裁（改法建议：该子句限定为「动作面收 `realName`」＋删除面回指 TC-91）。

**落档实测**：设计档 **2222 → 2230**（换行符计数；+8 = §2.2.15 补全注块 +3 / 变更记录 5 行；其余为等行数行内改述）；行宽实测 **0** 行超 300（不含行尾 CR）；EOL 全档 CRLF；**条目计数不变（I1–I5 / AC32–AC36 / TC-88…TC-96 / DD-51…DD-57 均不变）**。

### 设计档末微轮（eng-designer；源 = 回填轮报告未落 / 存疑项 + 父侧裁定；三件）

**#1 AC33 口径收口（与 TC-92 同族互斥）**：设计档 §3.1 AC33 的 `github:owner/dsh-x` 子句「⇒ 同」按字面读含「profile 副本目录不存在」⇒ 桩内不可验——父侧裁定 = 同 TC-92 口径：子句限定为**动作面**（`{ok:true}` + bundles 不含该名 + `pnpm remove` 实参 = 真名；`spawn` 计数 1 + args 断言）+ **删除面回指 TC-91** ✓（照回填轮建议改法）。
落点 = 设计档 §3.1 AC33 行（判定方式拆为两行——AC32 式；改后 L1840–L1841）。

**#2 实测回填标注**：§2.3 B28 表三行（`shell-plugins.js` **454** / `market.js` **498** / `shell-backend.js` **323**）末列补「**实施期实测已回填** as-of 2026-09-19」（数值不变 ✓；与 `shell-market.js` 行同式）。

**#3 ①表就近交代**：§2.2.15 ① 表「幻影条目」行修后格补半句指针——两处面 = `:101` + `:88`（`:88` 为实施期补全，见改动面清单下补全注）；取「补半句指针」案（父侧二选一）。

**落档实测**：设计档 **2230 → 2234**（换行符计数；+4 = AC33 行拆分 +1 / 变更记录 +3）；行宽实测 **0** 行超 300（不含行尾 CR）；EOL 全档 CRLF；`docs/requirements/SHELL.md` 355（本轮未改该档）；**条目计数不变（I1–I5 / AC32–AC36 / TC-88…TC-96 / DD-51…DD-57 均不变）**。

**存疑提请（本轮未动）**：§2.3 B28 表自指行与表下注仍记本档 **2207**（修正轮 1 as-of；后续增量以变更记录为指针）——父侧若需同步至现值请裁。

## §3 设计评审（评审子代理）

<!-- 由评审子代理填 -->

---

### 轮次 1（评审子代理）

| # | Category | Severity | Issue | Suggestion |
|---|----------|----------|-------|------------|
| 1 | 方法学合规（规范面相抵） | 🔴 | §3.3 手段 14 ① 规定静态 grep 落在**新建测试档内**——「测试档内以 `fs.readFileSync` 读源码断言」（`docs/design/SHELL-UX.md:2050`；断言对象 = `market.js` / `shell-market.js` / `shell-backend.js` 三个源码档）。规范面 `docs/design/REPO-CONVENTIONS.md:458`（断言面纪律：「**禁**「读非测试档 + 子串断言」式散文锚（NFR-A5）」）、`:653`（AC-B16-9：禁止形态 = 「新增档内『读 `docs/**` 或源码正文（`.js` / `.mjs` / `.html`）+ 子串断言』0 处……**命中即红**」）、`:315`（NFR-A5 度量方式句）对同一机制（新增档的断言形态）给出相抵规定；B28 全档未见该禁令的豁免/限定注（对照先例 = 本档 §3.3 手段 7 的 B12 例外注，`docs/design/SHELL-UX.md:2003-2004`：同类验证手段冲突以「批次限定 + 例外注」收口）。问题核心是承载面：静态 grep 可移由操作者/门禁面执行（B12 收口为「主 agent 亲 grep」），写成测试档内断言则落入规范面明文禁止形态。 | 实施前定论其一：① 静态 grep 面移出测试档（改由操作者或门禁面承载，承 B12「亲 grep」先例）；② 若判定该禁令为 B16 批内条、不追及后续批，则登记显式范围限定/例外注（承手段 7 的 B12 例外注先例）后再保留「与门禁同源面」表述。 |
| 2 | 受影响文件体量注解（贴线档拆分计划） | 🟡 | 本批触碰 `market.js`（500 → 498，净 −2，`docs/design/SHELL-UX.md:1493`）——该档为「≥480 贴线档」；规范面 `docs/design/REPO-CONVENTIONS.md:577`（拆分计划 = 贴线档必填面）与 `:582`（`market.js`「改它们时随批给」）⇒ 计划应随本批给出。§2.3 B28 表下注仅见「净 −2 单值约束 + 越限停手上报 + 第一削减序」（`docs/design/SHELL-UX.md:1500`），无拆分计划、亦无「本批不拆分 + 消解期」形式的体量裁定；对比同一批为 `shell-plugins.js` / `shell-backend.js` 均落了 >300 体量裁定复核（`docs/design/SHELL-UX.md:1502-1504`）。另有旧路由句「拆分裁决仍归 T2 评估面」（`:1670`），其与本规范句的关系未在 B28 段交代。 | 补一条 `market.js` 体量裁定/拆分计划条（结论可维持「修复轮不夹带重构 ⇒ 不拆分」，但给出拆分计划或消解期 + 触发条件）；或注明沿用 T2 路由的理由及其对本规范句的优先关系。 |
| 3 | 验收标准（三方一致 / 回指锚） | 🟡 | 回指锚两处口径不一：AC35 回指列 = `NFR-1（启动面；T30）`、AC36 = `NFR-3 / NFR-6`（`docs/design/SHELL-UX.md:1824` / `:1825`）；而批次档「三方一致清单（硬）」与「三方声明」均声明回指锚 = `US-14 / US-15`（＋ NFR-6 / NFR-7）（`docs/batches/B28-plugin-fixes.md:60` / `:105`），本档 §3.1 B28 回指口径同句（`docs/design/SHELL-UX.md:1862`）。 | 三处对齐（AC35 改列声明锚或补注适用条目；NFR-1 / NFR-3 若要保留则同步扩写批次档声明面），保持「三方一致（硬）」逐条可核。 |
| 4 | 文档归属（同机制两处口径） | 🟡 | B28 收口 B12 面时，O20 / O22 / O23 / L-B12-4 均已加「已修（B28）」注（`docs/design/SHELL-UX.md:1695` / `:1699` / `:1700`），但两处同族旧句未同步：U-15 行「「不可一键安装」标签保持现状（O22 的现存缺口不在本批处置）」（`:1726`）与 §2.6 B12 追认块「O22 / O20 …两条**本批不改**，提请另批裁定」（`:1752`）；B28 落地后档内对同一 UI 机制（内置条目卡片徽章）并存旧读法（U-16 已声明徽章消失，`:1727`）。 | 按本档既有惯例（C28 修订面注 / 「已修」注 / B30 边界句修订面先例）为上述两处补注。 |
| 5 | 文档卫生（计数漂移） | 🔵 | `tests/b12-plugin-guards.test.js` 行数三值不一：**491**（`docs/design/SHELL-UX.md:1468` / `:1485`；与 `docs/design/REPO-CONVENTIONS.md:436` / `:572` 同值）；B16 面预计 **493**（`docs/design/REPO-CONVENTIONS.md:572`「+2 行 → 493」）；DD-57 记「现 **492** 行」（`docs/design/SHELL-UX.md:1582`）。 | 按实测核一值并附 as-of（该值仅作「不可扩展」判断依据，三种取值下结论不变）。 |
| 6 | 文档卫生（回填未执行） | 🔵 | §2.3 B28 表自指行记本档 1862 → 「≈2020（预估；**落档实测回填**）」（`docs/design/SHELL-UX.md:1497`），但 §四 变更记录无 B28 轮「补正（落档实测）」条目（`:2185-2187`）——承诺的回填未执行。 | 补 B28 轮落档实测补正行，或注记该值由后续轮接管/作废。 |
| 7 | 清晰度（行锚三值） | 🔵 | 同一函数 `normalizePlugin` 行锚三值并存：`market.js:100-108`（`docs/design/SHELL-UX.md:1280`）、`:104-108`（`:1335`）、`:100-135`（`:1501`，并注「36 行」）。 | 统一为「函数范围 + 本批改动行」两个口径、各一处定义。 |
| 8 | 文档卫生（头部指针） | 🔵 | 设计档头部「关联批次」列 B06 / B07 / B09 / B10 / B12 / B30（`docs/design/SHELL-UX.md:6-11`）、未列 B28（本批设计面即在本文）；需求档头部同（`docs/requirements/SHELL.md:5-10`）。 | 设计档头部补 B28 指针（需求档若因「无新增条目」有意省略，可在设计档侧注记）。 |
| 9 | 文档卫生（指针更正面表述滞后） | 🔵 | O24 / C46 / 批次档 §2 的 I3 行均记「台账 / 批次档 §1.3 指针错误」待更正（`docs/design/SHELL-UX.md:1701` / `:1643`；`docs/batches/B28-plugin-fixes.md:66`），而 §1.3 现已含「**实位订正**」句并指向 `shell-backend.js:188`（`docs/batches/B28-plugin-fixes.md:17`）——残余更正面实际 = 台账 T30 / B08 §5.6。 | 收敛表述（注明 §1.3 已订正），避免主 agent 重复核对。 |

**VERDICT: changes-required**

**计数：🔴×1 / 🟡×3 / 🔵×5（共 9 条）**

### 轮次 2（评审子代理）

**复核轮 2（重跑）——九条修正落点速核**（评审面 = `docs/design/SHELL-UX.md` + `docs/batches/B28-plugin-fixes.md`；方法 = 两档本轮重读 + 定向 grep；行锚 = 本轮读取值 as-of）

| # | Orig# | File | Severity | Status | Notes |
|---|-------|------|----------|--------|-------|
| 1 | 1 | docs/design/SHELL-UX.md | 🔴 | Fixed | 手段 14 ①（L2068）=「静态取证 = 实施期「亲 grep」取证行（**不落测试档内**……原始输出行写进批次档 §5）」；TC-94（L1994）· AC32–AC35（L1836 / L1838 / L1839-1840 / L1841）· §2.2.15 改动面清单（L1349）/ 取证行（L1352）· §2.3 B28 表 tests 行（L1509「行为面断言 only——静态面移实施期亲 grep」）同步；全档 grep「测试档内」仅余否定式 3 处（L1836 / L1994 / L2068）+ 变更记录 1 处（L2216）——零残留 |
| 2 | 2 | docs/design/SHELL-UX.md | 🟡 | Fixed | L1519-1521 体量裁定复核三段在场：① 事实 = 500 贴线零余量；② 结论 = 本批不拆分；③ 消解路径 = 下次再获增量 ⇒ 拆分复核（**承 T2 路由**——归 `docs/TODO.md` T2 评估面；O3 / DD-47 同口径）+ 触发条件两条 |
| 3 | 3 | docs/design/SHELL-UX.md | 🟡 | Fixed | AC35（L1841）「NFR-1（启动面；T30——补适用锚，见 B28 回指口径）」· AC36（L1842）「NFR-3（门禁三连——补适用锚）/ NFR-6（零回退面）」· §3.1 口径（L1879-1880）「补适用锚 = NFR-1（AC35 / T30 启动面）· NFR-3（AC36 / 门禁三连）」——三处同文可核 |
| 4 | 4 | docs/design/SHELL-UX.md | 🟡 | Fixed | U-15（L1743）与 §2.6 B12 追认块（L1769）均补「**已修（B28；修正轮 1 #4）**」注 |
| 5 | 5 | docs/design/SHELL-UX.md | 🔵 | Fixed | 单值 = **492**（as-of 2026-09-19）落 §2.3 B12 表行（L1480）/ 回填注（L1498「行数核正……权威面 = §2.3 B12 表行」）/ DD-57（L1599）；491 / 493 按取代口径处理（L1497 历史行 + L1498） |
| 6 | 6 | docs/design/SHELL-UX.md | 🔵 | Fixed | 自指行回填（L1510 行 / L1512 注：2207，含 B30 同档并行面，as-of）+ 变更记录 B28 补正行（L2216–2221：2207 → 2222）；残差（非阻塞）= 行值取轮前 as-of 快照，与轮后末值 2222 的 +15 落差由 L1512「修正轮 1 增量见变更记录」注覆盖 |
| 7 | 7 | docs/design/SHELL-UX.md | 🔵 | Fixed | §2.2.15 提字段行（L1291）两口径同格：本批改动行 `market.js:104-108` / 函数范围 `:100-135`（与 §2.3 表下注 L1515 一致） |
| 8 | 8 | docs/design/SHELL-UX.md | 🔵 | Fixed | 头部 L11 补 B28 指针（「需求档无新增故其头部不列 B28——评审修正轮 1 #8」） |
| 9 | 9 | docs/design/SHELL-UX.md | 🔵 | Fixed | O24（L1718）/ C46（L1660）收敛为「§1.3 已含实位订正句；残余更正面 = 台账 T30 行 / B08 §5.6」 |
| — | 另核 | docs/batches/B28-plugin-fixes.md | — | 在场·一致 | §2 修正记录三段齐备（L107-128 修正记录 #1–#9 / L130 行锚漂移说明 / L134-135 门禁与落档实测），数字与设计档一致（2207 → 2222 · 0 行超 300 · EOL CRLF · SHELL.md 355）；lint FAIL（SELFTEST-FAIL TC-B29-03）= 别批在飞面（B29-samples-guard）如实标注——非本批缺陷 |

VERDICT: pass

计数：🔴×0 / 🟡×0 / 🔵×0（九条全数 Fixed；无新增 🔴）

另注（范围外）：`docs/design/REPO-CONVENTIONS.md` 的行锚（:653 / :188 / :458 / :315 及漂移说明所列 :572 / :579 / :418 / :436）不在本评审范围内，未逐条复核——批次档 §2「行锚漂移说明」已登记其实位。

## §4 评审裁决与实施启动（主 agent）
- **用户批准（2026-09-19 13:17，原话「批准」）**：批准 B28 设计实施（修正轮 1 已落地并经父侧核验 ✓）。**凭证链说明**：本批评审轮 1 = changes-required ⇒ 需**复核轮 2**（验证 §3 轮次 1 九条修正的落地）pass 后签发设计凭证 ⇒ 再派实施轮 ✓。
- **实施面（设计 §2.3 表）**：`shell-plugins.js`（T32 卸载面走解析结果）· `market.js`（T22 可达路径 + T23① href 守卫）· `shell-market.js`（T23② 扫描面）· `shell-backend.js`（T30 清理通配）+ 新增行为面测试档；静态取证 = **实施期亲 grep 取证行**（原始输出落 §5）✓；冻结面零 diff（不碰 pet 链 / `main.js` / `package.json`）✓。

<!-- 由主 agent 填 -->

---

## §5 实施记录（eng-coder）

<!-- 由 eng-coder 填 -->

---

### 交付摘要（四处 + 测试档；file:line = 实施后实测）

> 父侧压行注（2026-09-19）：本段 5 行超宽由主 agent 纯折行 / 指针化收窄（内容逐字、零语义；表格行按既有先例指针化）——批次已核销、不重开实施段；承 B30 / B29 §5 压行先例。

1. **T22**（`market.js`）：`normalizePlugin` `:105-106`——删注释行 + 增 `builtin:` 识别分支（并进既有正则行，净 −1）：
   `/^(builtin:[^\s]+)$|add\s+(github:[^\s]+|link:[^\s]+|[^\s]+)/` + `m[1].startsWith('builtin:')` 分支 ⇒ `installSpec === 'builtin:<名>'`；
   `add …` 分支匹配面与改前逐字同源（改前原文同形无锚）；渲染面零改动（`:268-283` 按钮链按 installSpec 生效）。
2. **T32**（`shell-plugins.js` `uninstallPlugin` `:408-429`）：动作面 6 行（`:412`/`:415`/`:418`/`:421`/`:424`/`:427`）+ 成功消息 3 条（`:419`/`:425`/`:428`）由 `pkgName` → `realName`（±0 行）；门/越界/解析门拒绝行（`:409`/`:411`/`:413`/`:416`）仍回显原始入参 `pkgName`（三前缀逐字不动）。
3. **T30**（`shell-backend.js:188`）：单行替换为双形态 `-like`——`($_.CommandLine -like '*dsh/lib/bin.js*' -or $_.CommandLine -like '*dsh\\lib\\bin.js*')`（源码面转义口径）；`node.exe` 过滤 / `Stop-Process` / POSIX 侧 `:192` 逐字不动（±0）。
4. **T23①**（`market.js`）：删 `link.href = p.url`（原 `:260`，−1）——「主页」`<a>` 无 `href`；左键 onclick 链逐字不动。**T23②**（`shell-market.js`）：删两条调试行（原 `:129`/`:133`，−2）；`bundledNames` `:101` 单行替换为 `statSync().isDirectory()` 目录过滤（±0）。
5. **测试档** `tests/b28-plugin-fixes.test.js`（新，**288 行**换行符计数；行为面断言 only——无「读源码正文 + 子串断言」）：TC-91/92/93（T32 卸载夹具）+ TC-88/89（vm 桩）+ TC-90（shell-market 直接调用面）。

### 决策透明表（超设计改动面清单的改动——如实披露）

| 文件:行 | 改动 | 理由 |
|---|---|---|
| `shell-market.js:88`（`marketList` 的 `bundledNames`） | 同形目录过滤（设计改动面清单只列 `:101`） | **设计缺口补全**：初始加载走 `marketList` ⇒ 只滤 `:101` 时「README.txt 幻影卡片」在初始加载仍出现 + 幻影条目会获得可点击「一键安装」⇒ TC-90 真实行为面不成立。补全 = 同形单行替换、±0 行（档 196 ✓）。明细 = 下存疑项 + 设计档 §2.2.15 补全注（已回填 ✓）。 |

### 审计与代码评审（内环轮次与终态）

- **分歧审计（explore，轮 1）**：**clean**——四处交付点逐条对照无分歧；4 条非阻塞观察（越单披露未落仓 / shell-backend 行数复测 / §5 未写 / 测试档行数回填）——本节即处理面（行数复测 = 323 ✓，与设计一致；「read 行号 324」为工具尾空行显示口径差，非漂移）。
- **代码评审（advisor，轮 1）**：**pass**——🔴0 / 🟡1（协调项 = 本 §5 未写，已由本节落地）/ 🔵3。
  裁决：① §5 取证与披露 → 已落（本节）；② 测试档 288 行回填 → 已落（本节 + 提请设计档 §2.3 收口回填）；
  ③ `shell-market.js:94/:118/:144` 孤儿注释（B12 期遗留，非本批面）→ Not an issue（最小改动纪律）；
  ④ `market.js:105` `add\s+` 分支无锚 → Not an issue（改前原文同形无锚，匹配面逐字同源）。**终态 = 收敛（clean / pass，零修正轮）**。

### 亲 grep 取证行（原始输出；手段 14 ① / TC-94——不落测试档内）

1. `grep "startsWith('builtin:')" market.js` → `D:\big-fish\market.js:106:     if (m && (m[1] && m[1].startsWith('builtin:') || m[2] && m[2].startsWith('github:'))) { installSpec = m[1] || m[2]; isGitHub = !!m[2]; }`
2. `grep "link\.href" market.js` → （**0 处**，无输出行）
3. `grep "market:enable input|market:enable added" shell-market.js` → （**0 处**，无输出行）
4. `grep -F "*dsh\\lib\\bin.js*" shell-backend.js` → `D:\big-fish\shell-backend.js:188:`
   `const script = "Get-CimInstance Win32_Process -Filter \"Name='node.exe'\" | Where-Object { ($_.CommandLine -like '*dsh/lib/bin.js*' -or $_.CommandLine -like '*dsh\\lib\\bin.js*') } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force -ErrorAction SilentlyContinue }";`
   （父侧压行注：原行 >300，折点 = `:188:` 后；两行拼接 = 原始输出行，零改动。）
5. `grep "bundledNames.*isDirectory" shell-market.js` → `shell-market.js:88` 与 `:101` 两行命中（原始行见实施日志）。

### 自证（原始结果行）

- `npm run lint` → `CHECK syntax PASS files=63` · `CHECK lines PASS near=3`（market.js 498 = 贴线提示）· `CHECK dag PASS (cycles=0)` ·
  `CHECK fanout PASS frozen=6` · `CHECK assembly PASS (13/13)` · `CHECK samples PASS refs=0 pkg=0` · `GATE lint FAIL checks=7 selftest=20/20`——
  红面 = width 2 条全部在别批在飞档（`B22` 1 行 · `B29` 2 行；`frozen=undefined` 新增拦截），本批写域五档零违例（如实标注，不代修）。
- `npm run test:full` → `GATE test:full PASS pass=51 fail=0 skipped=0 ms=1104`（45 既有 + 6 新档；b12 档零回退；无 SLOW-UNREGISTERED）。
- `npm run test:integration` → `GATE test:integration PASS scenarios=3 pass=3 fail=0`。
- 新档单跑 `node --test tests/b28-plugin-fixes.test.js` → `# pass 6 # fail 0`（逐用例 1–15 ms，全 <500 ms）。

### 行数与冻结核验（换行符计数口径）

- 行数：`market.js` **498**（净 −2 单值 ✓）/ `shell-plugins.js` **454**（±0 ✓）/ `shell-market.js` **196**（净 −2 ✓）/ `shell-backend.js` **323**（±0 ✓）/ `tests/b28-plugin-fixes.test.js` **288**（新档；设计预估 ≈120，实测回填——设计档 §2.3 B28 表收口轮请同步）。CRLF 逐档保全（四档 `\n` 计数 = `\r\n` 计数，无 LF 泄漏）。
- 冻结面：本批仅触碰上述五档；`tests/b12-plugin-guards.test.js` / `main.js` / `package.json` / `pet.js` 的 `git diff --numstat` **空** ✓；pet 链（`pet-chain*.js` / `shell-pet*.js`）与 `scripts/gates/**` 的工作区改动 = B22/B27/B29 在飞面（本批零触碰；父侧任务书已预期）。

### 存疑 / 未落项（如实标注）

1. **TC-92 设计行内部矛盾**：§3.2 TC-92 期望同时含「`node_modules/dsh-x` 删除」与「`pnpm remove` 实参 = `dsh-x`（spawn 1）」——两子判据在实现上互斥（内置分支删目录但零 spawn；pnpm 分支 spawn 1 但目录删除由真 pnpm 完成、桩内不删）。实施取**动作面取证优先**（args 断言 = T32 修前/修后差异的本质证据）；删除面由 TC-91 内置分支覆盖。提请父侧核该行口径。
2. **TC-96 真机人工面**：未执行（本角色无真机面）——不记已验；销账条件 = 用户实机确认。
3. lint 红面 = 别批在飞（B22/B29 批次档 width 新增拦截）——归因如上，未代修。


## §6 验收核销（主 agent）

**核销（2026-09-19）**——B28 全链 = 设计（SHELL-UX §2.1 选型 N + §2.2.15 + AC32–36 + TC-88–96）→ 评审轮 1 = changes-required（🔴1 / 🟡3 / 🔵5）→ 修正轮 1 全落 → **用户批准** → **复核轮 2 = pass** → 实施（四处修复 + 新测试档 `tests/b28-plugin-fixes.test.js`）→ 设计回填三轮（#73 / #74）→ **全仓 lint 首绿** → 本节核销。

**验收证据链**：

| 面 | 证据（可复核） |
|---|---|
| T22 一键安装（`market.js` **498** ✓ < 500） | `normalizePlugin` 认 `builtin:`（`:105-106` ✓ 父侧实读）；桩测 TC-88/89 ✓ |
| T32 卸载真名（`shell-plugins.js`） | 动作面 6 行 = `realName`（`:412` / `:415` / `:418` / `:421` / `:424` / `:427` ✓ 父侧实读）；TC-91/92/93 ✓ |
| T30 陈旧后端清理（`shell-backend.js:188`） | Windows 清理通配双形态 `-like` 在场 ✓（父侧实读） |
| T23 市场卫生 | `link.href` 归零（0 处 ✓ 父侧实测）+ 调试行删除 ✓ |
| 新测试档 | **288 行**（lint 口径；含尾空行 +1 = 289）· `node --test` 6/6 绿 ✓ · 自动收档（`test:full` 51 ✓） |
| 门禁 | **`lint PASS`**（checks=7 · selftest=20/20 · 全仓首绿 2026-09-19）· `test:full` 51 ✓ · `test:integration` 3 ✓ |
| 冻结面 | `main.js` / `package.json` / `tests/b12-plugin-guards.test.js` / `pet.js` 零 diff ✓ |

**偏差登记（D7）**：`shell-market.js:88` 实施期补全（`marketList` 同形目录过滤——父侧裁定接受 + 已回填设计档 ✓）；TC-92 口径修正 + TC 区间订正 + AC33 收口（#73 / #74 ✓）。

**测试面处置（① 寿命判）**：`tests/b28-plugin-fixes.test.js` = **转 ②③ 资产保留**（业务可观察行为断言 + 自动收档 + 稳定驱动三条件满足 ✓——本批唯一样例）。

**真机项**：TC-96 用户实机目视（安装弹窗 / 幻影卡片消失 / 卸载）—— 2026-09-19 市场检视通过 ✓。

**台账**：**T22 / T23 / T30 / T32 → 已核销**（逐条移入 `docs/TODO-archive.md`）✓；地图 B28 行 → 「已收口 + 核销」✓。