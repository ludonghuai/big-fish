# B30 —— 提醒面后续（重启误报修复 + 「等待确认」信号）

## §1 立案（主 agent）

### 1.1 目标（一句话）

修掉「每次重启 app 都会误报一次任务完成」（T40），并给「回合在等你确认」补上正确信号（R12）——两条都在提醒判定面。

### 1.2 任务来源

**T40**（用户 2026-09-18 实机误报，主 agent 三段实证）+ **R12**（B09 设计期 U-13 提出；用户 2026-09-17 裁定 A：不并入 B09，另立需求点）。两者同族：**提醒时机与真实状态脱钩**（T40 = 把后端启动写入误判成完成；R12 = 「该你确认」缺位）。

### 1.3 现状实测（证据，as-of 2026-09-19）

- **T40**：重启 app（12:00:51）⇒ 后端自身启动写入 `DSH_HOME`（`storages/session_projcache/sessions/session-f098….json` +
  `sessions/…/session.v3.jsonl.zstd` + `storages/workspace.json`，实测 mtime **12:01:24**）⇒ 提醒模块把「启动写入 + 静默 8 s」判成任务完成 ⇒ 误报 ✗。
  判据面：`shell-notify.js:277` 阈值 + `:164` / `:171` 门回退；`main.js:53` `IDLE_NOTIFY_MS = 8 * 1000`。
- **R12**：现判定「回合在途 ⇒ 不提醒」正确（避开语义错误的「已完成」；`SHELL-UX.md` §2.2.12 `L-B09-1`）；缺「该你确认了」信号 ⇒ 需在判据面扩展。
- 注：台账 R10 行所述阈值「30 s」与实现（8 s）不符 ⇒ 该**文字**同步归 B14 ✓（已立案）。

### 1.4 范围（做 / 不做）

**做**：① 重启窗口内的启动写入不构成忙信号（或记启动前基线——见 U-2）；② 「等待确认」信号（判定口径 + 文案 + 节流）；③ 两处判据句落 `SHELL-UX.md` §2.2.12；④ 需求条目（`SHELL.md` §三）由设计轮落。
**不做**：① 不重开 B09 已收口面（AC17–AC19 逐字不动 ✗）；② 不改 B26 已收口的提醒节流口径（气泡三条件）；③ 不代做 R10 的归档条件（秒表验收 —— 用户侧独立 ✓）。

### 1.5 硬约束

1. B09 行为面不回退；2. 判定门对 `DSH_HOME` **只读**口径不变；3. 新信号必须**可机检**（判据 + 取证行）；4. 三道门 + lint。

### 1.6 待决项（须裁定后才能定稿设计）

- **U-1**：R12 语义——检测到什么 ⇒ 提醒什么（「回合在途且等用户确认」的判据面）；**触发时机**（一次 vs 重复）与**与「完成」提醒的互斥 / 共存口径**。
- **U-2**：T40 实现口径——启动窗口屏蔽 vs 基线重置（记启动前 `lastBusyAt`）——设计者给候选 + 理由。

### 1.7 关联台账与需求档

台账 **T40 + R12** → 本批；需求档 = `docs/requirements/SHELL.md` §三（待落——由设计轮落）；同族：R10（已收口待验收）· T33（读面谓词，若同轮动该面一并核）。

### 1.8 交付物与排期

交付物 = 两处修复 + 需求条目 + 验收证据（重启 ×3 零误报 + 「等待确认」正常 / 边界 / 异常用例）。**可立即派设计**（`shell-notify.js` 面零在途冲突 ✓）。

### 1.9 变更记录

| 日期 | 变更点 |
|---|---|
| 2026-09-19 | 建档（B30 立案）：T40 + R12 合批（同族提醒判定面）；U-1 / U-2 待决 |

---

## §2 本批任务书（eng-designer）

<!-- 由 eng-designer 填 -->

---

### 2.1 任务目标与范围

把「每次重启误报一次任务完成」修掉（判定面历史封口），并给「回合长时间静默（很可能在等你确认）」补上提醒信号——两条都在提醒判定面（`docs/design/SHELL-UX.md` §2.2.12）。

- 方向与依据：本档 §1.3（T40 三段实证 + R12 缺口）；§1.6 待决 U-1 / U-2（设计者给候选 + 推荐，追认在评审 / 用户）。
- 需求条目：`docs/requirements/SHELL.md` **US-16**（T40 启动不误报）与 **US-17**（R12 等待确认）——新增；NFR-5 增 **B30 注记**（等待面零新增读面）。
- 设计落点：`docs/design/SHELL-UX.md` §2.1 **选型 O**（O-1 T40 口径 / O-2 R12 判据源）· §2.2.12（B30 判据面：判据句 ①② / 执行面 / 交互补行 / 修订面 / L-B30-1…3）· §2.3 **B30 受影响文件表** · §2.4 **DD-58…DD-61** ·
  §2.5 **C48–C51 / O27 / L-B30 指针** · §2.6 **U-17** 与 B30 追认块 · §3.1 **AC37–AC39** · §3.2 **TC-97…TC-106** · §3.3 手段 15 + B30 人工项。

### 2.2 本批条目（I1–I3；三方条目一致基准）

| # | 条目 | 回指 | 验收 |
|---|---|---|---|
| I1 | **启动零误报（T40）**：完成判定加「会话有回合历史」合取项（`lastStepStartSeq !== null`）——新建会话（判定行 init 全 null）⇒ 按 `open` 处置（不提醒）；四态枚举与诊断行不回退 | US-16（并显式收紧 US-12 的「回合已闭合」判据——US-12 原文不动） | AC37 |
| I2 | **「等待确认」信号（R12）**：verdict=open ∧ 有历史 ∧ 静默 ≥ `WAITING_NOTIFY_MS`(60000；钩子 `BIGFISH_WAITING_NOTIFY_MS`) ∧ 每回合一次 ⇒ 提醒一次（文案三串诚实口径）；与「完成」互斥；判定读零新增 | US-17 | AC38 |
| I3 | **零回退与门禁**：B09 已收口面（AC17–AC20 判据 / 完成文案三串 / 节流）逐字不回退；冻结面零 diff；`lint` / `test:full` / `test:integration` 全绿 | US-16、US-17（门禁面） | AC39 |

> 三方一致（硬）：**本档 §2 本批条目 I1–I3 = 设计档 §3.1 AC37–AC39 回指条目 = 需求档 US-16 / US-17**（并含对 US-12 判据面的显式收紧 + NFR-5 B30 注记）。映射：**I1 → AC37 · I2 → AC38 · I3 → AC39**。

### 2.3 判定规则（判据句，逐字；实施按此执行）

**判据句 ①（T40）**：规则⑤ 收紧——`done` ⇔ `openTurnStartSeq === null` ∧ **会话有回合历史**（`tb.val.lastStepStartSeq !== null`）；无历史（启动新建会话，判定行 init 全 null——`dsh-agent-loop/lib/index.js:1312-1317`）⇒ 按 `open` 同处置（不提醒）；四态语义与枚举不变，仅 `done` 判据收紧（方向 = 抑制）。

**判据句 ②（R12）**：静默达 `WAITING_NOTIFY_MS`（默认 60000；env 钩子 `BIGFISH_WAITING_NOTIFY_MS`，解析形承 `idleNotifyMs()`）∧ verdict = `open` ∧ 有历史（`lastStepStartSeq !== null`）∧ 本回合未提醒（`waitingNotifiedTurn !== tb.val.lastTurn`）⇒ 「等待确认」提醒一次
（文案三串：`Bigfish 可能正在等你确认` / `助手已静默片刻；若它没有在跑长任务，回来看看吧` / `等你确认哦！`）；与「完成」提醒互斥（verdict 单值）；判定读零新增（复用同一次 probe 结果——记忆扩两键 `lastStepStartSeq` / `lastTurn`）。

- **U-2（T40 口径）设计裁定 = 判定面历史封口**（选型 O-1）：误报根因 = 判定错误（fresh 会话被判 `done`），非忙信号错误——启动窗口屏蔽（候选 2）时序脆弱（实测启动写入持续 33 s，URL 捕获时点与末次写入无因果）；
  基线重置（候选 3）在 watch 路径须逐事件 `stat`（违背零 per-event stat 判据）。
- **U-1（R12 语义）推荐值（待用户追认）**：近似判据（回合在途 + 静默 ≥ 60 s）+ 诚实文案 + 每回合一次——勘察发现精确判据（approval/asked 等）为 **log-only**、投影缓存不可观测
  （`dsh-user-approval` README「Both are log-only」；`ask_user_question` 纯 waterfall 零投影行）；zstd 日志尾读不可行（Electron 33 = Node 20.18 无 zstd）；精确判据随 **B22 事件桥**升级（接缝 = 设计档 O27 / L-B30-1——建议主 agent 在 B22 立案面登记「R12 信号升级」为其验收子项）。
- **执行面**：`completionGate()` 返回 `{ verdict, lastStepStartSeq, lastTurn }`；处置面新增 `completionGateWaitingDue()`（纯算术、无 I/O）；5 s 回调内按序判定 `completionGateDue()` → `completionGateWaitingDue()`；`waitingNotifiedTurn` 随监视器生命周期重置、初值 `null`；判定读异步 / 防重叠 / 每静默窗至多一次判据不变（NFR-5）。

### 2.4 明确出批（不做）

- 不改阈值 8 s / 四态枚举 / 两条诊断行 / 完成文案三串 / 托盘开关语义（`notifyOnComplete` 口径逐字）；
- 不改 busy 判定面与回落机制；不新增依赖 / 不新增仓内文件（桩测 = 仓外一次性 `%TEMP%\b30-notify-stub.mjs`，承 b09-gate-stub.mjs 先例）；不改 Harness（`dsh-bundle/` 与 `~/.dsh` 只读）；
- 不做 zstd 日志尾读 / 事件桥（B22 前置）——R12 精确判据随 B22 升级（O27）；
- 不改 B26 已收口的气泡节流口径（同档一次 / ≥30 s / 跨档 ≥10 s）；不代 B14 同步台账 R10 文字（文字同步归 B14）。

**2.5 重派轮补记（2026-09-19；源 = 主 agent 重派「上轮 400 零落盘」——实测证伪，本轮为收尾核验轮）**：

- **盘面实测（D6 回读核对）**：上轮**并非零落盘**——§2 全段（§2.1–§2.4 + 三方一致）· 需求档 US-16 / US-17 + NFR-5 B30 注记 · 设计档 B30 全区段（选型 O / §2.2.12 B30 判据面 / §2.3 B30 表 / DD-58…DD-61 / C48–C51 + O27 / U-17 与追认块 / AC37–AC39 / TC-97…TC-106 / 手段 15 / 变更记录）**均已在盘且内容完整**；400 发生在收尾报告阶段，写入已全部完成。本轮**零重写**，只做核验与形态收口。
- **本轮唯一改动 = 行宽压行（一致性面 / 形态，零语义变更）**：lint 门禁报本批新档行宽超限 12 处，本批写域 11 处逐条拆分（设计档 8 行 + 批次档 §2 4 行：设计落点 / 判据句 ② / U-2 / U-1 行）——逐字保留、仅换行（AC 行承 AC32–AC35 两行拆分先例；手段条承手段 12 式 3 空格续行先例；留痕 = 设计档 §四「B30 设计轮重派收尾补正」）。B25 批档 3 行超宽 = **别批在飞面**，不越域代修（如实标注）。
- **自检结果**：三方一致成立（I1–I3 = AC37–AC39 = US-16 / US-17，映射句两档各在场）；B09 已收口面（AC17–AC20 行 / TC-46…TC-55 行 / 完成文案三串 / GATE_* 三常量）逐字在档零回退；行宽两档 0 行超 300（不含行尾 CR）；`npm run lint` 复跑 = B30 面全绿、残余 FAIL 仅 B25 在飞面。
- **观察项（不阻断本批，报主 agent）**：① §2.3 B30 表「当前行数」列 = 批前基线（SHELL.md 319 / 本档 2051），本设计轮已实际写入两档（实测 353 / 2193，均落「预估」区间内）——末行数回填归实施收口轮（承 B09/B10/B12 先例）；② R10 台账行阈值「30 s」文字与实现（8 s）不符——批次档 §1.3 已立案归 B14，本轮未处置；③ O27 建议主 agent 在 B22 立案面登记「R12 信号升级」为其验收子项（主 agent 写域）。

### 2.6 修正轮 1 记录（eng-designer，2026-09-19；append-only）

- **源**：§3 轮次 1 发现 #1–#7（🔴0 / 🟡3 / 🔵4，VERDICT = pass）——父侧裁决 = 七条全修；均为文档登记 / 口径面，语义零改动。
- **#1（🟡 注 S6 + 锚点清单登记面）**：`docs/design/SHELL-UX.md` 注 S6 增「B30 增量」三行（辅助函数 5 → 6 · 新增状态 `waitingNotifiedTurn` · 新增常量 `WAITING_NOTIFY_MS` · 记忆全键五键 · 注入面增量）；取证锚点清单增「B30 附注」（env 开关 + 零新增诊断行 + 文案三串；形态照 B07/B09 附注）。
- **#2（🟡 U-13 承接注）**：U-13 表行与 open 项行各补「**已承接（B30）**」注——近似判据版随 US-17 落（§2.2.12 B30 判据面 / AC38）；精确判据 → O27 / B22 事件桥。B09 原文不动。
- **#3（🟡 生效口径注）**：设计档 §2.2.12 执行面「判定只可能抑制提醒」句与需求档 NFR-5 B09 注记同句各补「除 US-17 的等待确认提醒外」（承 `:988` / C28 先例）；B09 原文不动。
- **#4（🔵 键数口径）**：`completionGateMemo` 键数三处统一——判据句 ② 尾注列全键五键 `{ lastBusyAt, verdict, staleSince, lastStepStartSeq, lastTurn }`（B09 三键 + 本批两键）；执行面「三键写入」→「返回三键写回（另两键按 B09 语义维护）」；
  `completionGateWaitingDue()`「读记忆四键」列全所指（`lastBusyAt` / `verdict` / `lastStepStartSeq` / `lastTurn`——`staleSince` 仅完成面 ③′ 上界用）；需求档 NFR-5 B30 注记「记忆扩两键」核对一致（无改动）。
- **#5（🔵 lastTurn 置位取证）**：选**取证行**（非桩测边界）——Harness 投影源码实证：`lastTurn` 由 **`turn/start`** 置位（与 `openTurnStartSeq` 同一条投影 apply、同一原子快照；`dsh-agent-loop/lib/index.js:1318-1324`）；`turn/end` 只清 `openTurnStartSeq`、不触 `lastTurn`；
  init = `0`（非 `null`，`:1312-1317`）⇒ 任一在途回合 `lastTurn` 已就位、`waitingNotifiedTurn`(null) ≠ `lastTurn` 首发恒真——首发提醒不被自身节流吞掉。同族措辞订正：设计档判据句 ① 与需求档 US-16 契约的「判定行 init 全 null」→「三个可空字段全 `null`、`lastTurn` = `0`」（判据本身 `lastStepStartSeq !== null` 不变）。
  **批次档 §2.3 判据句 ① 同句「init 全 null」按 §2 append-only 不改原文，本注为生效口径。**
- **#6（🔵 记录数字自洽）**：设计档变更记录重派轮行「本批写域 11 处」→ **12 处**（8+4=12，与 lint 报数一致）；「批次档 §2 压行后 112 → 116」→「+4 行（4 处拆分各 +1）」（原对数字不可复现）。**批次档 §2.5 同句按 append-only 不改原文，本注为生效口径。**
- **#7（🔵 C49 证据指针）**：C49「不触 B26 工作气泡面」补证据——节流三条件在 `pickBubble` 内（`pet-work-core.js:135-150`）、`petSay` 本体无节流；`PET-ANIMATION.md` §2.8.5「闲聊台词不纳入本节节流」；既有完成台词直呼 `petSay` 同族先例。
- **顺带形态修（报主 agent）**：设计档变更记录 B28 / B30 批次修订 / 重派轮三条续行尾部的空单元格「| |」→「|」（表格两列形态；残留即示范）。
- **自检**：三方一致不变（I1–I3 = AC37–AC39 = US-16 / US-17）；B09 已收口面逐字不动；两档行数 = 设计档 2207 / 需求档 355（换行符计数，落设计档变更记录补正行）；行宽与 lint 结果见交付报告。

### 2.7 收口轮登记面回填（eng-designer，2026-09-19；append-only）

- **源**：批次档 §5「未落 / 存疑」项 **1–2**（注 S6 计数与实装不符；AC39 ② 字面张力）+ 父侧派单三件。
- **落三件（只此三件；另含同块邻域数值同步——见下，均为登记面回填、不夹带语义）**：
  - ① **注 S6 B30 增量**（`docs/design/SHELL-UX.md:590`）：辅助函数 **5 → 6** → **5 → 7**——计数与枚举同改（D3）；两个新列名按实装：处置面 `completionGateWaitingDue()` + 发射面 `completionGateWaitingFire()`（实装 `shell-notify.js:219` / `:228`，设计者亲测）。
  - ② **§2.3 B30 表四行实测回填**（`:1530-1533`）：`shell-notify.js` **314 → 351**（+37）· `main.js` **249 → 261**（+12）· `docs/requirements/SHELL.md` **319 → 355** · 本档 **2051 → 2239**（含 B28 / B30 同档并行面——单批增量不可逐行分离）+ 表下回填注（`:1535`）。
  - ③ **AC39 ② 括注补「返回载荷形状」半句**（`:1853-1854`）：规则 ①–④ 条件 / 语义不变——diff 仅返回载荷形状（与执行面「返回三键」同指）；字面张力消解（§5.5 项 2 建议已落）。
- **同块邻域数值同步（如实登记——②「预估行按实测回填」的邻域一致性面，承 B12 收口轮先例；裁定结论零变）**：
  §2.3 B30 体量裁定条内 3 处——`:1537`「314 → ≈350」→「314 → 351（实施期实测；原估 ≈350）」；`:1538`「±约 35 行」→「+37 行（实施期实测）」；`:1539`「≈350 距 400 余量 50」→「351（实测）距 400 余量 49」。
- **自检（D6 回读核对）**：本档 **2234 → 2239**（换行符计数；+5 = 回填注 +1 / 变更记录 +4）；行宽 **0** 行超 300（不含行尾 CR）；EOL 全档 CRLF（lf-only = 0）；
  `docs/requirements/SHELL.md` **355**（本轮未改）；三方条目一致不变（I1–I3 = AC37–AC39 = US-16 / US-17）；B09 已收口面（AC17–AC20 / TC-46…TC-55 / 文案三串 / `GATE_*` 三常量）逐字未动。
- **门禁**：`npm run lint` 复跑 = **PASS**（`GATE lint PASS checks=7 selftest=20/20`，exit 0；含 `CHECK width PASS frozen=15 lines=93`——本批两档 0 超宽）。
  注：中途一次复跑曾现 `selftest-failed TC-12`（在飞批 B31 的编辑中间态：`baseline.json` 第三免检条目已落、自测期望未同步）——末次复跑已全绿，非本批面。
- **观察项（报主 agent，不阻断）**：① §2.2.12 执行面未点名 `completionGateWaitingFire()`（行为描述逐字覆盖实现；注 S6 已列名）——本轮未动（不夹带）；
  ② §2.3 B30 表 `shell-notify.js` 行「改动点」列枚举未含 `completionGateWaitingFire()`（该列为摘要面、非计数面）——本轮未动，是否同步请主 agent 裁定。

## §3 设计评审（评审子代理）

<!-- 由评审子代理填 -->

---

### 轮次 1（评审子代理）

**评审范围**：B30 面（`docs/design/SHELL-UX.md` §2.2.12 B30 判据面 / §2.1 选型 O / §2.3 B30 表 / §2.4 DD-58…61 / §2.5 C48–51·O27 / §2.6 U-17·追认块 / §3.1 AC37–39 / §3.2 TC-97…106 / §3.3 手段 15 + `docs/requirements/SHELL.md` US-16·US-17·NFR-5 注记 + 本档 §1–§2 I1–I3）。

**已独立复核（通过项）**：① 三方一致成立——I1–I3 ⇔ AC37–AC39 ⇔ US-16 / US-17（映射句两档各在场，回指口径 `SHELL-UX.md:1863` / 本档 §2.2）；② B09 已收口面在场（AC17–AC20 行 / TC-46…TC-55 行 / 完成文案三串 / `GATE_*` 三常量），规则⑤ 收紧以修订面显式登记（`:1025`）且未改 B09 原文；③ 行宽——评审 grep 实测三档（设计 / 本档 / 需求档）**0 行 ≥301 字符**，与「0 行超 300」自检一致；④ §2.3 B30 表含代码行的当前行数 + 预估 delta 与 >300 软档体量裁定复核（`shell-notify.js` 314 → ≈350；`main.js` 249 → ≈257）；文档行数抽检落「预估」区间（SHELL.md 319 → ≈365 实测 353；本档 2051 → ≈2230 实测 2193）。
**范围内不可核实（unverified）**：代码档行数（`shell-notify.js` 314 / `main.js` 249）与 Harness 侧取证行号——代码实现不在本轮评审范围，如实标注不臆断。

| # | Category | Severity | Issue | Suggestion |
|---|---|---|---|---|
| 1 | 一致性（D3 计数与枚举同改） | 🟡 | B30 增量未登记进 §2.2.6 两处登记面：① 注 S6（`:580-587`，shell-notify 函数/状态/注入面清单）未含 B30 新增（`completionGateWaitingDue` / `waitingNotifiedTurn` / `WAITING_NOTIFY_MS`）；② 取证锚点清单无「B30 附注」（现仅 B07/B09/B10/B12 四附注，`:623`/`:627`/`:633`/`:637`）——B07/B09 均把各自新增测试钩子登记于此。B09 设计轮的做法 = 「§2.2.6 增注 S6 + 锚点 B09 附注」（`:2111`）；B30 事实本身已在 `:1001`/`:1511` 等处落档，属一致性面缺项、非事实缺项。 | 建议按 B07/B09 先例补 S6 增量与「B30 附注」一行（env 开关 + 零新增日志行 + 文案面注）；若裁定随实施收口轮登记，请在该轮以显式指针落档。 |
| 2 | 文档状态（semantic dangling） | 🟡 | U-13 的 open 状态未随 B30 处置：§2.6 表行（`:1724`）与 open 项块（`:1732`）仍写「open——建议本批不做；待评审 / 用户裁定」，全档无「由 US-17 承接」注（对照 O20/O22/O23 `:1695`/`:1699`/`:1700` 与 L-B12-4 `:1268` 均有「已修（B28）」承接注）；R12 语义 = U-13 派生项（本档 §1.2），其精确判据仍留 O27（`:1705-1706`）。 | 建议在 U-13 两处各补一句承接注（近似判据版随 US-17 落；精确判据 → O27 / B22）；B09 原文可不动。 |
| 3 | 文档状态（生效口径注） | 🟡 | B09 时代「抑制型判定」两处陈述未随 B30 落生效口径注：① 设计档 §2.2.12 执行面「判定只可能**抑制**提醒，不新增提醒」（`:934`）；② 需求档 NFR-5 B09 注记「判定对通知只做**抑制**（不新增通知类型、不新增提醒面）」（`docs/requirements/SHELL.md:302`）。B30 明确新增提醒面（`:1004`/`:1013`、C48「新增等待面」`:1645`），同节边界句已获专门修订注（`:988`，承 C28 先例）——此二处未获同等待遇，单独阅读会读到与 B30 判据面相抵的绝对句（报告即修项，非阻断）。 | 建议按 `:988` 与 C28 / O16 先例，对两处各补一句生效口径注（「除 US-17 的等待确认提醒外」）；B09 原文可不动。 |
| 4 | 清晰度（键数口径） | 🔵 | `completionGateMemo` 键数三处不闭合：判据面「记忆扩两键」（原 3 键 ⇒ 5 键，`:1005`）；执行面「三键写入记忆」（`:1010`）；`completionGateWaitingDue()`「读记忆四键」（`:1011`）。条件由判据句 ② 唯一确定，故不致误实现。 | 建议统一键数 / 命名口径（列全键或写明「四键」所指），并核需求档 NFR-5 B30 注记同句一致。 |
| 5 | 证据（判据 ② 节流键） | 🔵 | 每回合节流以 `tb.val.lastTurn` 为键（`:1002`/`:1014`、DD-61 `:1587`），但全档未给 `lastTurn` **置位时点**的取证行（对照 `lastStepStartSeq` 有「仅由 `step/start` 置位」+ 行号，`:995`）。若 `lastTurn` 仅在 `turn/end` 置位，则**首个在途回合**的等待提醒会被自身节流吞掉（`waitingNotifiedTurn(null) !== lastTurn(null)` 为假）——恰是 R12 首发场景；桩测 TC-101（`:1983`）按「有历史」夹具构造，未必覆盖该取值面。 | 建议补一行取证（Harness 投影定义 `turn/start` 分支）或加一条桩测边界（首回合 + `lastTurn` 取值面）；亦可列入实施期核对点。 |
| 6 | 记录一致性（数字） | 🔵 | 重派轮记录数字不闭合 / 与盘面口径不符：① `:2191`「超限 **12** 处——本批写域 **11** 处（设计档 **8** 行 + 批次档 §2 **4** 行）」⇒ 8+4=12≠11（本档 §2.5 `:99` 同句）；② `:2192`「批次档 §2 压行后 **112 → 116**」——实测 §2 段 = 51–101 行（≈52 行）、本档全文 123 行，无法复现该对数字。实质面已独立复核（行宽 0 行超 300 为真），仅涉记录口径。 | 建议复核并更正 / 写明口径（如「压行 +4 行」「批次档全文 112 → 116（其后 §2.5 补记另增）」），使 12 / 11 与 8+4 自洽。 |
| 7 | 证据（C49 断言） | 🔵 | C49（`:1646`）断言「等待信号……不触 B26 工作气泡面」未附证据指针（B26 设计档节号或 `petSay` 调用面 file:line 皆未给，unverified）；若 B26 的「全局下限 10 s」在 `petSay` 调用面生效，等待提醒的桌宠台词存在受既有节流影响之可能面。 | 建议补证据指针，或将该面列入实施期核对（一行注即可）；非阻断。 |

**出范围备注（无级别）**：B25 批档 3 行超宽、台账 R10 行「30 s」文字（归 B14）、O27 的 B22 立案登记均为别批 / 主 agent 面，已如实标注，不在本轮复核结论内。

计数：🔴 0 / 🟡 3 / 🔵 4

VERDICT: pass

## §4 评审裁决与实施启动（主 agent）

- **评审轮 1（设计）**：**pass**——🔴0 / 🟡3 / 🔵4（发现表见 §3 轮次 1）；裁决（主 agent）：七条全修 ⇒ 修正轮 1 已落地并经父侧核验（落点：`docs/design/SHELL-UX.md` `:589-591` / `:644-646` / `:942` / `:1013` / `:1015-1016` / `:1656` / `:1734` / `:2201-2202`；`docs/requirements/SHELL.md` `:243` / `:302` / `:354`）✓。
- **用户批准（2026-09-19 12:16，原话「批准」）**：批准 B30 设计实施；**U-1 文案三串追认**（`Bigfish 可能正在等你确认` / `助手已静默片刻；若它没有在跑长任务，回来看看吧` / `等你确认哦！`）；U-2 = 设计裁定（T40 修法 = 完成判定加「会话有回合历史」合取项）✓。
- **实施排期**：**排 B27 收口之后**——本批实施要动 `main.js`（+≈8 行：`WAITING_NOTIFY_MS` 常量 + env 钩子），而 `main.js` 是 **B27 的 15 档零 diff 冻结面** ✗ ⇒ B27 链终（§6 核销 + 凭证消耗）前不派实施轮 ✓。
- **实施面（设计 §2.3 表）**：`shell-notify.js`（判据 ①/② + 等待状态机）· `main.js`（常量 + 测试钩子）· 仓外桩测载具（`%TEMP%`，不入仓）✓；零新增依赖 / 零新增读面 ✓。
- **收口轮·设计档登记面回填裁决（2026-09-19，微轮 #5 交付核验后）**：回填三件核验 ✓（注 S6 **5 → 7** + 枚举同改 ✓；§2.3 B30 表四行实测回填 ✓；AC39 ② 补「返回载荷形状」✓）；
  **其披露的「邻域 3 处数值同步」= 裁定接受** ✓（预估→实测的邻域一致性，承 B12 收口轮先例；结论零变）；
  **两项观察项裁定 = 均不补** ✓——① §2.2.12 执行面不点名 `completionGateWaitingFire()`（执行面 = 行为描述面，名称已在注 S6 登记 ✓，最小改动原则）；② §2.3 表「改动点」列枚举不补（该列 = 摘要面、非计数面，D3 不适用）✓。

---

## §5 实施记录（eng-coder）

<!-- 由 eng-coder 填 -->

---

### 5.1 实施摘要（eng-coder，2026-09-19）

**范围**：I1–I3 全落（判据句 ① T40 启动零误报 + 判据句 ② R12 等待确认 + 零回退与门禁）。**改动面 = 恰两档**：`shell-notify.js`（314 → **351** 行）· `main.js`（249 → **261** 行）；零新增文件（桩测 = 仓外 `%TEMP%\b30-notify-stub.mjs`）· 零新增依赖 · 零新增读面 · 零新增诊断行 · 零新增导出。

**落点（file:line，as-of 2026-09-19）**：

| 面 | 落点 | 内容 |
|---|---|---|
| shell-notify 注入 / init | `:28` · `:32` | `WAITING_NOTIFY_MS` 变量 + `init` 注入键（末键，B30 增） |
| 记忆 / 状态 | `:48-53` | `completionGateMemo` 扩两键（全键五键）· 新增 `waitingNotifiedTurn`（初值 null） |
| 判据句①（T40） | `:169` | 无历史（`lastStepStartSeq === null`）⇒ 按 `open` 同处置；规则⑤ 收紧落 `:170` |
| 返回三键 | `:149` / `:152` / `:161` / `:166-171` | `completionGate()` 全路径返回 `{ verdict, lastStepStartSeq, lastTurn }` |
| 等待面 | `:214-233` | `completionGateWaitingDue()`（纯算术 · 四合一）· `completionGateWaitingFire()`（notify + petSay + 置节流键） |
| probe 写回 | `:247-253` | 返回三键写回记忆（`lastBusyAt` / `staleSince` 按 B09 语义维护） |
| 生命周期 | `:281-282` | memo 五键重置 + `waitingNotifiedTurn = null`（与 memo 同处） |
| 5 s 回调 | `:318-325` | 完成面 → 等待面按序判定（互斥由 verdict 单值保证） |
| main.js | `:55-56` · `:68-76` · `:88` | `WAITING_NOTIFY_MS = 60 * 1000` · `waitingNotifyMs()`（env `BIGFISH_WAITING_NOTIFY_MS`，解析形承 `idleNotifyMs()`）· `init` 注入键 |

**冻结面（零回退）**：`isIgnoredPath` / `latestMtimeAsync` / watch 回调 / 回落路径 / `completionGateDue()` / 两条诊断行 / 完成文案三串 / `GATE_*` 三常量 / 四态枚举——逐字在场；`grep "Sync("` = 0 · `grep "process.platform"` = 0；`shell-tray.js` / `shell-settings.js` / `shell-mode.js` / `package.json` 未触碰（git diff 面为空）。

### 5.2 自证（六项，原始结果行）

1. **静态判据（手段 15 ①）**
   - `grep lastStepStartSeq|WAITING_NOTIFY_MS|waitingNotifiedTurn shell-notify.js` → 命中（`:49` / `:53` / `:121` / `:128` / `:149`…`:171` / `:222` / `:224` / `:232` / `:281` / `:282`）。
   - 文案三串逐字 → `:229` `Bigfish 可能正在等你确认` · `:230` `助手已静默片刻；若它没有在跑长任务，回来看看吧` · `:231` `等你确认哦！`。
   - `grep BIGFISH_WAITING_NOTIFY_MS main.js` → `:68` / `:70`。
   - `grep "Sync(" shell-notify.js` = **0 命中**；`grep "process.platform" shell-notify.js` = **0 命中**；四态标识符 + `GATE_*` 三常量照旧在场。
2. **桩测（手段 15 ②）**：`node "%TEMP%\b30-notify-stub.mjs"`（夹具根 `%TEMP%\b30-fix\<case>`、`B30_FIX` 可覆盖；逐例独立子进程）→ `RESULT: ALL GREEN cases=8 checks=27`（exit 0；例 = TC-99 / TC-100 / TC-101 / TC-102 / TC-103 / TC-104a / TC-104b / TC-104c）。
3. **门禁三连**：`npm run lint` → syntax PASS files=63 · lines PASS · width FAIL 的 3 行 VIOLATION **全部为别批在飞文档**（B27 / B28 / B29），本批两档 0 超宽；`npm run test:full` → **pass=51 fail=0**（ms=1127）；`npm run test:integration` → **scenarios=3 pass=3 fail=0**。
4. **零回退面**：`git diff --name-only -- shell-tray.js shell-settings.js shell-mode.js package.json` = 空；本批实际改动 = `shell-notify.js` + `main.js`。
5. **行数 / 形态实测**：`shell-notify.js` 314 → **351** · `main.js` 249 → **261**（均 ≤ 500）；两档 CRLF 全量（lf-only = 0）· 0 行 >300；`node --check` 两档 OK。
6. **设计符合性核对**：判据句① `:169`；判据句② 四条件 `:221-224`（lastBusyAt 记忆 / open+历史 / 节流键 / 静默阈值）；互斥 = 完成面先判（`:322`）→ 等待面（`:325`）；判定读零新增（等待面无 I/O、不自发 probe）。

### 5.3 决策透明表（实施期）

| # | 决策 | 理由 | 备选与否决 |
|---|---|---|---|
| 1 | 等待面调用点置于完成块**之后、`!notifiedForCycle` 守卫之外** | 落实执行面「`notifiedForCycle` 不参与（独立旗标）」；完成面仍先判 ⇒ 互斥优先不变 | 放进 `!notifiedForCycle` 块内（可达态虽等价，但与「独立旗标」句相抵） |
| 2 | 等待发射**单列函数** `completionGateWaitingFire()` | 与 B09 `completionGateFire()` 对称；执行面「发射 = notify() + petSay()…发射后置 waitingNotifiedTurn」逐字落 | 内联进 5 s 回调（形态不对称；且与注 S6 计数同向偏 —— 见 §5.5 ①） |
| 3 | `completionGate()` 返回**逐字面量**（未引 helper） | 注 S6 声明辅助函数数量（D3 计数口径）；新增通用 helper 会破计数量 | 加 `gateResult()` helper（超注 S6 声明面） |
| 4 | main.js 用**并列小函数** `waitingNotifyMs()`（未抽共享 env 解析 helper） | 设计「解析形承 `idleNotifyMs()`」⇒ 保 `idleNotifyMs()` 形态逐字不动（AC17 解析面按形核对） | 抽 `envMs(name, def)` 共享（改 B09 已收口面形态） |
| 5 | 桩内 TC-103 用 `node:vm` **提取 main.js 真实常量行 + 函数体求值** | env 解析面在组合根、无导出；该法真测源码（非子串断言），仓外一次性载具可接受 | 把解析逻辑复制进桩内（测不到产品代码） |
| 6 | TC-104 拆 a/b/c 三子例（各独立子进程） | 三态断言面不同（stale 抑制 / unavailable 降级落回 B09 面）；逐例独立进程避免诊断「至多一条」封口标记互相污染 | 单进程混跑（标记污染 ⇒「零条」可能空过） |

### 5.4 审计与代码评审轮次与终态

- **内嵌发散审计（read-only explore，1 轮）**：四类偏差 = PARTIAL 0 · SILENT-SIMPLIFICATION 0 · OUT-OF-LIST 0 · **DOC-DRIFT 1（🔵）**；语义级偏差 0（16 项逐条通过）；审计对「AC39 ② 字面张力」的独立判断 = 实现按执行面落成立（未找到更贴设计的落法）。
- **advisor 代码评审（1 轮）**：**VERDICT: pass**（🔴 0 / 🟡 0 / 🔵 2）——① 同 DOC-DRIFT（注 S6 计数登记面）；② AC38「≥」措辞 vs 实装严格 `>`（同 `completionGateDue()` 形、边界不可达）。
- **修复轮**：0 轮（两轮均无 🔴/🟡 ⇒ 无代码修正）。
- **终态 = clean**（两项 🔵 已裁决：① Deferred → 收口轮登记面回填；② Not an issue → 设计「同形」要求、边界不可达）。

### 5.5 未落 / 存疑项（如实登记，不写「无」）

1. **收口轮待办（登记面）**：注 S6（`SHELL-UX.md:590`）「辅助函数 5 → 6」与实装不符——本批实装新增两个（`completionGateWaitingDue()` `shell-notify.js:219` + `completionGateWaitingFire()` `shell-notify.js:228`），
   按 S6 自身口径应为 **5 → 7**（承 B09 先例「按实装补齐」）；另 §2.3 B30 表末行数回填（`shell-notify.js` 314 → **351** · `main.js` 249 → **261**）。审计与 advisor 两轮独立命中同项。
   （父侧压行注：本行由主 agent 纯折行，内容逐字、零语义。）
2. **AC39 ② 字面张力（已登记；审计独立判定实现成立）**：「`completionGate` 规则 ①–③③′④ 分支零 diff」与执行面「返回 `{ verdict, lastStepStartSeq, lastTurn }`」不能字面同时成立——实现按执行面落（规则条件 / 语义不变，diff 限于返回载荷形状 = 判据句② 尾注「记忆扩键」的管道）；建议收口轮在 AC39 ② 括注补「返回载荷形状」字样（建议、非必需）。
3. **main.js 增量 +12 行**（设计预估 +约 8；实测 249 → 261）：主因 = `waitingNotifyMs()` 按「承 `idleNotifyMs()` 形」并列（10 行 + JSDoc），未合并共享 helper（见 §5.3 #4）；无上限判据，如实登记。
4. **L-B30-3 残余面未构造**：后端启动改写既有会话记录（带历史 + mtime 更新）⇒ 判据句① 不覆盖——现状未观察到此形态；如实标注，不伪报。
5. **真机人工项未执行**：TC-97（重启 ×3 零误报）/ TC-98（正常完成不回退 8–13 s）/ TC-101 真机面（用户目视）= 台账 T40 / R12 销账条件，归真机轮（本轮机检面 = 静态 + 桩测 + 门禁三连）。
6. **TC-105（长工具调用静默亦提醒）**按设计标注为已知限制（L-B30-1），桩测未作缺陷断言。
7. **别批在飞面**：lint width FAIL 3 行（B27 / B28 / B29 批档）为在飞文档，未越域代修。

## §6 验收核销（主 agent）

**核销（2026-09-19）**——B30 全链 = 设计（评审轮 1 **pass**（🔴0 / 🟡3 / 🔵4）→ 修正轮 1 七条全落 → **用户批准**（U-1 文案三串追认 + U-2 设计裁定））
→ 实施（恰两档：`shell-notify.js` 314 → **351** · `main.js` 249 → **261**）→ 收口轮（登记面回填：注 S6 **5 → 7** · §2.3 B30 表四行实测 · AC39 ② 口径 ✓）
→ **用户实机验收**（2026-09-19：提醒面运行正常；「启动不再误报」= 用户重启后**未见弹窗**（自述「应该是没有了」——**软通过**，下次重启可再留意一眼））→ 本节核销。

**验收证据链**：

| 面 | 证据（可复核） |
|---|---|
| T40 启动零误报 | 判据句①：`done` 须「会话有回合历史」（`shell-notify.js:169-170` ✓）；桩测 TC-99 / TC-100 ✓；**用户重启实机未再见弹窗** ✓（软） |
| R12 等待确认 | 判据句②四条件 + `WAITING_NOTIFY_MS = 60000` + env 钩子（`main.js:55-56` / `:68-76` / `:88` ✓）；文案三串逐字（`shell-notify.js:229-231` ✓）；桩测 TC-101 / 102 / 103 ✓ |
| 互斥与关闭态 | TC-104a/b/c ✓（verdict 单值互斥；`notifyOnComplete=false` ⇒ 两面零提醒 ✓） |
| 零回退 | B09 已收口面逐字不动 ✓（diff 仅返回载荷形状 + 新增面）；冻结面零 diff ✓ |
| 门禁 | `lint PASS` ✓ · `test:full` 51 ✓ · `test:integration` 3 ✓；桩测 ALL GREEN cases=8 checks=27 ✓ |

**测试面处置（① 寿命判）**：仓外一次性桩载具（`%TEMP%\b30-notify-stub.mjs`）= **退役**（不入仓、随本轮结束 ✓）；无新增单元桩 ✓。

**派生登记**：无（收口轮三项已折账 ✓——注 S6 / §2.3 表 / AC39 ② 均已回填）。

**台账**：**T40 / R12 → 已核销**（逐条移入 `docs/TODO-archive.md`）✓；地图 B30 行 → 「已收口 + 核销」✓。
