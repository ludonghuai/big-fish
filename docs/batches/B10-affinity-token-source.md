# B10 —— 好感度「真实消耗」读面修复

> 六段制批次档（每段一位作者、append-only）：§1 主 agent · §2 eng-designer · §3 评审子代理 · §4 主 agent · §5 eng-coder · §6 主 agent。

## §1 批次立案（主 agent）

### 1.1 立案背景

本仓「基于真实 token 消耗累积好感度」的功能**长期静默失效**：`shell-affinity.js:70` 读 `<DSH_HOME>/storages/session_projcache.json`，该文件在**当前活跃 harness（0.1.5-rc.1）下不存在** ⇒ `readFileSync` 抛错被 `catch { return null }` 吞掉，好感度不因真实消耗增长（喂食加成不受影响）。

### 1.2 任务来源

- 台账技术待办 **T14**（`docs/TODO.md` 技术组）。
- 只读勘察已闭环（子代理，2026-09-17 12:15–12:40；结论见 §1.3），**判定 = 路径/形状写错、数据面存在且可修**，功能无需重定义或移除。
- 用户 2026-09-17 授权：「**你安排，把我们的工作全部继续推进起来**」——由主 agent 排期立批。

### 1.3 勘察结论（证据面，供设计直接引用）

| # | 事实 | 证据 |
|---|---|---|
| ① | 写盘者 = `dsh-session-projection-cache`；`turn/end` **必写**，另有 200 事件 / 5000 ms 节流 | 0.1.5：`.pnpm\@deepseek-ai+dsh-session-pr_*\node_modules\@deepseek-ai\dsh-session-projection-cache\lib\index.js:263-268` · `:290-322` |
| ② | **0.1.5 = per-record 目录布局**：`<DSH_HOME>/storages/session_projcache/sessions/<sessionId>.json`，域版本 **7**，`layout: per-record` | 0.1.5 `…dsh-session-projection-cache\lib\index.js:89-101`（域声明）；`…dsh-storage-json\lib\index.js:575`（布局分发）· `:312-313` · `:464`（`<dir>/<table>/<key>.json`） |
| ③ | **0.1.0-rc.6 = 单文件**：`<DSH_HOME>/storages/session_projcache.json`，无 per-record 支持 | `dsh-bundle\node_modules\@deepseek-ai\dsh-storage-json\lib\index.js:262`；同包 `README.md:5` |
| ④ | 字段形状（两版一致）：`totals` 四桶 + `last.buckets`；per-record 路径 = `record.rows.tokenUsage.val.totals`，旧布局 = `tables.sessions[*].rows.tokenUsage.val.totals`（细节见 §2 / 设计档 §2.2.13） | 0.1.5 token-meter `lib/index.js:415-448` · `:361-366`；bundle 同包 `:241-269` |
| ⑤ | 本机活证据：`~/.dsh/storages/session_projcache/sessions/session-f80ce725-….json`（7827 B，`version:7`）含 `totals = {uncachedInputTokens:9666, outputTokens:2434, cacheReadTokens:37504, cacheWriteTokens:0}` | 子代理实测（2026-09-17） |
| ⑥ | `*.json.bak.<stamp>` 类文件（`invalidRecords:"backup-and-skip"` 产物）名称不以 `.json` 结尾 ⇒ 按 `*.json` 枚举天然排除 | 0.1.5 `…dsh-storage-json\lib\index.js:370-397` |
| ⑦ | **范围外注记（勘察提请）**：`shell-affinity.js:78` 只累加 `uncachedInputTokens + outputTokens`，未计 `cacheReadTokens` / `cacheWriteTokens`；harness 计费口径 = billed input = uncached + cacheRead + cacheWrite | `shell-affinity.js:78`；`dsh-bundle\node_modules\@deepseek-ai\dsh-client-ui-conversation\README.md:43` |

### 1.4 范围（含边界）

**做**：`shell-affinity.js` 的读面与解析——**双形态兼容**（0.1.5 per-record 目录优先 → 旧单文件兜底），保持既有失败语义（取不到即 null、不抛、不影响喂食路径）。

**不做（边界）**：

- **不改 harness**、不改 `~/.dsh` 任何内容（只读）；
- 不新增依赖 / 不新增文件；
- 不改喂食 / 兑换 / 好感度 UI 交互（除非计费口径裁定连带需要，见 §1.5）。

### 1.5 待决点（其一需用户裁定）

| # | 待决 | 状态 |
|---|---|---|
| ① | **计费口径**：好感度按 `uncachedInput + output`（现状）还是按 harness 计费口径 `uncached + cacheRead + cacheWrite + output`（勘察建议） | **已裁定 = B（对齐 harness 计费口径）** —— 用户 2026-09-17 12:19 裁定；依据：与 harness 同源，且本机样本 `cacheRead 37504 ≫ uncachedInput 9666` ⇒ 现口径会大幅低估 prompt 侧消耗 |
| ② | 多会话聚合口径：目录下多个会话文件如何累计（全量累加？按 workspace？仅当前会话？） | 交设计裁定，须给判据 |
| ③ | 旧布局兜底是否保留、保留到何时（消解路径） | 交设计裁定 |

### 1.6 验收标准（初拟，供设计细化并回指需求条目）

| # | 验收标准（可机检） |
|---|---|
| AC1 | 0.1.5 per-record 布局下能读出 `totals` 四桶（真实文件 / 构造夹具皆可，须给命令与期望） |
| AC2 | 旧单文件布局（0.1.0-rc.6 兜底场景）仍可读，两形态切换不抛错 |
| AC3 | 失败语义不回退：路径缺失 / JSON 损坏 / `*.bak` 文件在场 ⇒ 返回 null 且**不影响喂食路径** |
| AC4 | 计费口径按 §1.5 ① 的裁定落地，且给出可机检的累加判据 |

### 1.7 关联

- 需求档：`docs/requirements/SHELL.md`（SHELL 板块）或 `docs/requirements/PET.md`（桌宠板块）——**归属由 eng-designer 判定**（好感度域属谁，须给依据）
- 设计档：`docs/design/SHELL-UX.md`（或 PET 系设计档，同上判定）
- 台账：T14（本批承载）

---

## §2 本批任务书（eng-designer）

### 2.1 本批条目（三方一致源——硬）

| # | 本批条目 | 需求档条目 | 设计档验收 | 交付面 |
|---|---|---|---|---|
| I1 | 好感度数据面读口：读面双形态（per-record 目录优先 / 旧单文件兜底）+ 计费口径 B + 全量聚合 + 水位语义 + 失败语义不回退 | `docs/requirements/SHELL.md` **US-13** | `docs/design/SHELL-UX.md` §3.1 **AC21–AC25** | `shell-affinity.js`（读面）+ `shell-backend.js`（导出面 +1 行）+ 上述两文档 |

> **三方一致**：§2 本批条目（I1）= 设计档 AC21–AC25 回指条目 = 需求档 **US-13**。批次档 §1.6 的 AC1–AC4 → 设计 AC21–AC24（**AC1→AC21 · AC2→AC22 · AC3→AC23 · AC4→AC24**）；**AC25 = §1.5 ②（多会话聚合口径）的裁定面**（§1.6 未列，本批新增）。

### 2.2 §1.5 三项待决 —— 逐项裁定

| # | §1.5 待决 | 本批裁定 | 依据 |
|---|---|---|---|
| ① 计费口径 | **B** = `uncachedInput + cacheRead + cacheWrite + output`（**只读 `totals`，不读 `last.buckets`**；缺桶按 0） | 用户 2026-09-17 12:19 裁定 + §1.3 ⑦；设计 = §2.2.13「累加判据句」/ DD-36 | 判据句与样本算例：`9666 + 37504 + 0 + 2434 = 49604` |
| ② 多会话聚合口径 | **全量累加**（目录下全部可解析记录求和；**不按 workspace / 不按会话过滤**）；单条损坏只跳过该条 | 与旧实现同源（旧面 `tables.sessions[*]` 即全量）+ 水位单调性最好 | 选型 L-2 / DD-35 / DD-37 |
| ③ 旧布局兜底 | **带消解期的条件性保留**（至 **B08 收口点**；到期条件 = 内置 bundle ≥ 0.1.5 ∧ 活跃副本无 < 0.1.5） | 内置 Harness = `0.1.0-rc.6`（单文件布局）⇒ 兜底是**当前出厂路径的活跃形态**，非历史包袱 | DD-39 / US-13「消解期」节 |

**增量语义（`§2` 交代项）**：读面返回**幂等水位**（不写盘、不持状态）；好感度累计值由调用方按**水位差**累加并持久化（`affinity.json`）⇒ **重启不重复计入**（基线每次启动重置 + `usage` 已持久化）。水位下降（会话记录被清理）⇒ 只重置基线、**不扣**好感；读不到任何有效记录 ⇒ 返回 **`null`（不是 0）**——`0` 会把基线降到 0，使读数回升时此前已计入的消耗被**重复计入**。

### 2.3 覆盖需求（US-13 契约逐条 → 设计落点）

| US-13 契约点 | 设计落点（`docs/design/SHELL-UX.md`） |
|---|---|
| 读面双形态 + 形态判别以磁盘事实为准 | §2.2.13「读面判据」①–④ |
| 计费口径 B（四桶、只读 `totals`） | §2.2.13「累加判据句」+ 样本算例 |
| 聚合口径 = 全量累加；逐条容错（可解析记录数 ≥ 1 即可用） | §2.2.13 容错表 + §2.1 选型 L-2 |
| 水位语义（幂等 / 重启不重复计入） | §2.2.13「增量语义」 |
| 失败语义不回退（`null`、不抛、不影响喂食与兑换） | §2.2.13 容错表 + §3.1 AC23 |
| 读面开销（只读单层目录 `sessions/*.json`、不做递归） | §2.2.13「读面开销」+ L-B10-1 |
| 消解期（保留至 B08 收口点 + 到期条件） | §2.2.13 边界 + DD-39 + §2.6 B10 追认块 |

### 2.4 明确出批（本批不做）

- 不改 Harness（`~/.dsh` / `dsh-bundle/` / 更新副本**只读**）；不新增依赖 / **不新增文件**（桩测脚本待裁定，见 §2.8）；
- 不改喂食 / 兑换 / 好感度 UI 与常量（`AFFINITY_RATE` / `EXCHANGE_RATE` / `LEVEL_THRESHOLDS` / `FOODS`）；不改 10 s 周期与广播节奏；
- 不做目录递归；不按 workspace / 会话过滤；不做异步化改造（登记 L-B10-1 与再议触发）；
- 不代 B08 改 bundle 版本；**不代 B09 改判定面**（`shell-notify.js` 零改动——§2.2.12 逐字保留）。

### 2.5 受影响文件（行数 = 换行符计数；行宽判据 = 不含行尾 CR）

| 文件 | 当前行数 | 改动点 | 末行数 |
|---|---|---|---|
| `shell-affinity.js` | 305 | `sumSessionTokens()` 重写（`:68-83`）+ 头注释 / JSDoc 口径句（`:25-30` / `:67`）+ 档内常量与诊断标记 + 导出面 +1（`:292-305`） | ≈351 → **实施期实测回填** |
| `shell-backend.js` | 322 | `module.exports` 增 `writeDiag`（`:311-322`，1 行；函数体不动） | 323 |
| `docs/requirements/SHELL.md` | 217 | US-13 + §一 B10 批次目标 + §二 B10 条目与层级注 + 头部关联批次 / 条目区间 + 变更记录 2 行 | **250**（落档实测） |
| `docs/design/SHELL-UX.md` | **1282**（B10 落笔前实测；本档 = B09/B10 并行写入） | 选型 L / §2.2.13 / 注 S7 + 锚点 B10 附注 / B10 表 / DD-34…DD-40 / C32–C35 / O17–O19 / U-14 与追认块 / AC21–AC25 / TC-56–TC-67 / 手段 12 / 变更记录 | **1349**（落档实测） |

### 2.6 验收判据（可机检清单——与设计档 AC21–AC25 同源）

| 验收 | 判据（可机检） |
|---|---|
| AC21 | 夹具 1 条记录（四桶 = 9666 / 2434 / 37504 / 0）⇒ 返回值 **49604**；静态：目录段在场 + 导出在场 + `recursive` 0 处 |
| AC22 | 旧布局夹具 ⇒ 旧面值；目录空 / 全损坏 ⇒ 走旧面；两形态并存 ⇒ 只读目录（返回值 ≠ 两形态之和）；消解期登记在场 |
| AC23 | 两形态均缺 / 不可读 / 全损坏 ⇒ **`null`（非 0）**且不抛；调用面零 diff；诊断行 1 条（不可用）/ 0 条（正常） |
| AC24 | 四桶标识符逐条在场；`last.buckets` 与 `totals` 不同值夹具 ⇒ 取 `totals`；样本 9666+37504+0+2434 = **49604** |
| AC25 | 3 条记录夹具 ⇒ 三条之和（不按会话 / workspace 过滤）；幂等（连续两次相等）；空目录 ⇒ `null`；预置 `usage > 0` ⇒ 启动后 `usage` 不变（重启不重复计入） |

### 2.7 归属判定（需求档 = **SHELL**；PET 否决）—— 依据

1. **台账归属档**（主 agent 写域）已归 SHELL 板块：`docs/TODO.md:32` 的「归属档」列 = `docs/design/SHELL-UX.md`（SHELL 板块）。
2. **设计档已预先登记**本项为 SHELL 板块在途项：`docs/design/SHELL-UX.md` §2.3 B09 表注（`shell-affinity.js`「其读面失效 = 技术待办 T14，归 **B10**」）与 §2.5 C31。
3. **模块面**：`shell-affinity.js` 是 B06 F6 拆分的 15 个 `shell-*.js` 之一（桌面壳层模块）；其读面 = **壳读 Harness 落盘物**（壳↔Harness 对接面），与 SHELL 板块 §一 的 B07 延伸同层。
4. **PET 板块现有条目面** = 拖拽跟手（B01）+ 多屏几何与可见性（B03）（`docs/requirements/PET.md:24-26`）——全部是「桌宠窗口在桌面上的可用性与几何」；token 读面与几何无关。
5. 两档 §二 均持「桌宠其余机制（好感度…）的功能性需求…本档不代其立需求」（`docs/requirements/SHELL.md:45` / `docs/requirements/PET.md:33`）——本批**不为好感度机制补立功能需求**；US-13 登记的是**壳侧数据面读口**（形态兼容 + 计费口径），并在 SHELL.md §二 该句下加**层级注**（原句不改）。

⇒ 归属 = `docs/requirements/SHELL.md`；**未触发**「若判定归 PET ⇒ 停下报告」分支。

### 2.8 计数 / 口径补正与报告项

- **计数同步（D3）**：SHELL.md US **12 → 13**（NFR 计数不变 = 5）；SHELL-UX.md 回指表 +1 行；AC +5 / TC +12 / DD +7 / C +4 / O +3 / U +1；头部「关联设计档」条目区间同步为 US-1…US-13。
- **行宽实测**：两文档 **0** 行超 300（不含行尾 CR 口径；SHELL-UX.md 改后最宽 300 = B06 旧行）。
- **行数补正注（并行写入）**：SHELL-UX.md 为 B09 / B10 **两批并行写入**——本批**可分离面** = 1282 → 1349（**+67**）；总增量 1165 → 1349（+184）含 B09 同档在途改动，两批增量**不可逐行分离**（口径已落设计档 §2.3「行数口径注（B10）」）。
- **报告项 O18**：`docs/requirements/PET.md:214` 有 1 行 = 321 字符（超 300）——该档不在本角色写域，随报告提请主 agent 处置。
- **报告项 O19**：PET.md 侧是否需同款「层级注」= 主 agent 写域裁定项（本批不改）。
- **报告项（流程）**：B10 与 B09 **同档并行写入**——若 B09 设计评审在途，本批改动会使评审对象变化（**D5 冻结窗口**）；提请主 agent 协调两批入场次序。
- **待裁定**：桩测脚本（落 `.thincoder/`，gitignore；承 B03 `b03-pet-calibrate-stub.mjs` / B09 手段 11 ③ 先例）是否采用——本批边界声明「零新增文件」，采用须经主 agent 裁定（设计档 §3.3 手段 12 ③）。
- **本段写入状态**：§2 **已写入**（经 `batch_segment`，无路径参数；内容为上述 §2.1–§2.8）。

### 2.9 B10 修正轮 1 补正注（11 条；条文权威面 = 设计档 / 需求档）

**源** = 本档 §3 轮次 1 发现 #1–#11；**主 agent 裁决 = 11 条全部 Dispatched**；**修正轮边界** = 只落这 11 条直接导出的修正，不夹带新语义。

| 原# | 落点（file:line） | 要点 |
|---|---|---|
| 1 | `docs/design/SHELL-UX.md` §2.2.13「消解期（旧布局兜底）」· §2.6 B10 追认块 · §2.1 L-1 取舍列 · §3.1 AC22 | 消解期句落档（保留至 B08 收口点 / 到期条件 = 内置 bundle ≥ 0.1.5 ∧ 活跃副本无 < 0.1.5 / 条件不成立 ⇒ 顺延并重新登记，不得默认永久）；DD-39 补进追认块；两处悬空指针（L-1 / AC22）收口 |
| 2 | `docs/design/SHELL-UX.md` §3.3 手段 12 ②/③（+ §2.3 B10 表表下「零新增文件」注） | 载具裁定 = **仓外一次性开发期脚本**（不入仓）；可复跑命令 + 夹具定义 + 驱动方式（假 `electron` / `DSH_HOME` / 定时器桩手动驱动 tick）落档，使 AC21–AC25 的「全机检」名副其实 |
| 3 | `docs/design/SHELL-UX.md` 注 S7 · §2.2.13 增量语义 + 本批改动面 + 诊断行 · §2.6 追认块 DD-40 · `docs/requirements/SHELL.md` US-13「增量语义（水位）」 | 副作用口径统一为「**返回值幂等**；诊断行为 = 每进程至多一条、非持久状态」；诊断发射点定死 = `sumSessionTokens()` 函数内（判据 ④ 分支）；`shell-affinity` 导出面 +1 补进追认块 |
| 4 | `docs/design/SHELL-UX.md` §2.3 B10 表下（两行 >300 软档 体量裁定 + 单函数核对行归位） | `shell-affinity.js` 305 → ≈351 裁定（纯增量 / 职责未增 / 消解路径 = 逼近 400 或再获增量批次时做拆分复核）；`shell-backend.js` 322 → 323 的 B07 触发判定 = **不触发**（导出档内既有函数、职责未增、323 距 400 尚远）；错位行由 B12 表段末移回本表下 |
| 5 | `docs/design/SHELL-UX.md` §2.2.13 已知限制 **L-B10-4** · 容错表 `totals` 缺失行 · §2.5 O17 | L-B10-4 登记（可解析记录 ≥ 1 但四桶全缺 ⇒ 静默归零；本批不扩诊断条件 + 给触发条件与另批候选修法）；O17 的断言限定为「**路径 / 解析失效**」类形态变更 |
| 6 | `docs/design/SHELL-UX.md` §3.3 手段 12 ① | grep 计数口径改「**实现 / 导出各 ≥ 1 处**；**调用点不计入**」（档内 `:118` / `:127` 两处调用点不构成计数） |
| 7 | `docs/design/SHELL-UX.md` §3.2 **TC-67A**（新增） · §3.1 AC25 · §2.2.13 增量语义 | 边界用例：高水位 → 清掉记录 ⇒ `affinity.usage` / `wallet` **不减**（不扣好感）、基线重置 |
| 8 | `docs/design/SHELL-UX.md` §2.2.13 诊断行（行形加 `dir=<yes\|no>`）· §3.2 TC-67 · §3.1 AC23 | 行内加区分字段 = 目录 `sessions/` 存在性（区分「全新安装尚无数据」与「读面失效」，防长期同文脱敏） |
| 9 | `docs/design/SHELL-UX.md` §2.2.13 判据 ④ + 增量语义「`null` 分支」· §2.4 DD-38 · 已知限制 **L-B10-5** · §3.1 AC23 | `null` 分支的调用方语义明写（不累加 / 基线置 `null` / 本进程累加暂停）⇒ AC23 可独立核查；DD-38 措辞按实现更正（「基线保持不变」→「**不触发 0 回退**」） |
| 10 | `docs/design/SHELL-UX.md` §2.3 B10 表 SHELL-UX 行 + 行数口径注 | delta 口径统一为**可分离面单值 +67**（基线 = 1282，与「末行数」同基线）；「+184（1165 → 1349）」保留为跨批合计口径、**不作本表 delta** |
| 11 | 无改动（核对跳过） | 设计档头部区间与关联批次已由 B09 修正轮 1 同步（US-1…US-15 / NFR-1…NFR-7 + B09/B10/B12 指针）——本轮实测核对 = **已同步，跳过** |

**另（形态统一——一致性面，随报告披露）**：① `SHELL-UX.md` §2.5 补 **L-B10-1…L-B10-5 指针行**（原缺，形态与 B07 / B09 / B12 三条对齐——D2）；② §2.2.13 读面开销的样本档大小改为「随会话增长而变」口径（落笔 7827 B / 本轮实测 7191 B）。

**本轮实测（换行符计数；行宽 = 不含行尾 CR）**：`docs/design/SHELL-UX.md` **1690 → 1731**（+41；**0** 行超 300，最宽 300）；`docs/requirements/SHELL.md` **306 → 308**（+2；**0** 行超 300，最宽 299）。§2.3 各批文件表内的自指行保留 as-of 快照（1282 / 1349；217 → 250），本轮实测值落 §2.3 表下「文档行数补正注（B10 修正轮 1）」。

**新发现（语义面 → 提请主 agent 裁定，本角色未自决）**：

- **N-1（与实现冲突；由 #9 的「明写该分支」直接派生）**：DD-38 原文「`null` 使基线**保持不变**（安全侧）」与实现不符——`shell-affinity.js:125` 无条件执行 `lastTokenSum = sum`，`sum === null` 时基线被**置 `null`**；
  而 tick 守卫（`:128`）要求 `lastTokenSum !== null`，且该变量只在守卫内被重新赋值 ⇒ **启动期读数失败则该进程内累加永久暂停**（读数事后恢复也不重建基线）。
  本轮：按实现更正措辞 + 登记 **L-B10-5**（方向 = 保守：不重复计入；代价 = 该进程内漏计）。
  **是否改代码（候选修法 = 守卫改为「首次读到非 `null` 时重建基线」）= 语义面变更，未自行落地**——提请裁定（若采纳 = 改动面超出本批「调用面零 diff」口径，须另立条目 / 另批）。
- **N-2（一致性面；已就位）**：`docs/requirements/SHELL.md` US-13 原句「同一磁盘状态重复调用同值、**不写盘**、不持状态」与设计侧诊断行（写 console / `bigfish.log`）相抵 ⇒ 已按 #3 收敛为「同一磁盘状态重复调用**同值**——返回值幂等；**不写 `affinity.json`**、不持持久状态」（**未修订契约句语义面**）。

**本段写入状态**：§2.9 **已写入**（经 `batch_segment`，无路径参数）。

### 2.10 行宽收口与发现即报告（B10 修正轮 1 收尾）

- **形态修正（本段自持面，D6 回读核对）**：§2.9 的 **N-1 条目**首版为单行 **412** 字符（超 300）⇒ 已**原地重排为 4 行**（内容逐字不变、仅换行位置变），经直接 `edit` 落于本段（本段作者 = eng-designer，未触碰他段）。**本段现测**：§2.9 + §2.10 全部行 **0** 行超 300。
- **发现即报告（本角色禁改面）**：本档 **§3 表 5 行**实测 **359–487** 字符（L185–L189，评审子代理段）⇒ 该段不在本角色写域、**未改**；提请主 agent / 评审侧处置（压行或改写判据细化面）。**本档整档行宽自述口径**：仅本角色段（§2）为 0 行超宽；整档 >300 行数 = **5**（全在 §3）——承 B09 §2.9 同款口径（设计者只报不改）。
- **另（事实面，as-of 口径）**：`~/.dsh/storages/session_projcache/sessions/` 本机实测 **1** 档 / **7191 B**（B10 落笔时记 7827 B——档大小随会话增长而变，设计档 §2.2.13「读面开销」已改为变动口径）；形态字段（`version:7` / `record.rows.tokenUsage.val.totals` 四桶）与 §1.3 ④ 一致（本轮复读活样本，2026-09-17）。
- **本段写入状态**：§2.10 **已写入**（经 `batch_segment`，无路径参数）。

### 2.12 B10 修正轮 2（范围扩展轮——台账 T27 并入本批）落点记录

**源** = 用户 2026-09-17 裁定「把你上一轮报的 N-1 并进 B10 一并修掉」；**派单** = 8 条（下表逐条落点）；
**修正轮边界** = 只落这 8 条直接导出的修订，**不夹带新语义**（未动 `affinity.json` 结构 / 兑换逻辑 / 桌宠文案 / 既有常量 / 10 s 周期）。

**本批合并用户裁定 T27**：台账 `docs/TODO.md:47` 的 T27（好感度累加在「启动期读数 `null`」后本进程永久暂停；候选修法 = tick 内
`lastTokenSum === null && s2 !== null ⇒ lastTokenSum = s2`，**只重建基线、不计 delta**）由本批承载并落定；**T27 的状态推进与台账勾销归主 agent**（本角色不触碰台账与他段）。

| 派单# | 落点（file:line） | 要点 |
|---|---|---|
| 1 | `docs/design/SHELL-UX.md:964-966`（§2.2.13「增量语义（水位）」） | 新增**基线重建**条（`lastTokenSum === null ∧ s2 !== null` ⇒ 只重建基线、不计 delta）；**为何不计 delta** = 暂停期消耗不补算（防跨进程 / 跨会话的跳变式重复计入）；残余含启动期 `usage === 0` 的「一次性计入历史」面（`:120-124` 不补触发） |
| 2 | `docs/design/SHELL-UX.md:1000-1003`（§2.2.13 已知限制 **L-B10-5**） | 由「本进程累加永久暂停（留档待另批）」改写为「**本批已修**（基线重建 + DD-50）+ 残余 = 暂停期消耗不补算」；证据链（`:125` 无条件赋值 · `:128` 守卫 · `:131` / `:141` 守卫内赋值）与触发场景（新装 / 无记录 / 缓存清空后首启）逐条保留；判据指针 = AC23 + TC-67B |
| 3 | `docs/design/SHELL-UX.md:1495`（§3.1 **AC23**）+ `:1505-1508`（新增「AC23 判据细化」注）+ `:1497`（**AC25** 同步） | 调用面口径 = 「除 `startAffinityWatcher` 内新增的基线重建分支外零改动」；**可机检双判据**：机检①（**优先，行为面**）= 桩测 **TC-67B**；机检②（辅，形态面）= `git diff` 调用面 hunk 白名单（仅该分支） |
| 4 | `docs/design/SHELL-UX.md:1209`（§2.3 B10 表行）+ `:1220-1223`（单函数体量核对 / >300 软档裁定） | `shell-affinity.js` 预估 ≈351 → **≈355**（delta +约 64 / −约 14，含重建分支 ≈4 行）；末行数仍标**「实施期实测回填」**；`startAffinityWatcher()` 32 → **≈36** 行；职责未增句补「调用面改动 = 1 个条件分支」 |
| 5 | `docs/design/SHELL-UX.md:1310` + `:1312-1314`（§2.4 **DD-50** 与表下注）+ `:1297`（**DD-38** 更正） | DD-50 三项 = 为何修 / 为何不补算 delta / 为何改调用面口径（表下注承 ① ② ③）；DD-38 的 `null` 调用方语义按新规则更正并注明「修正轮 2 更正」 |
| 6 | `docs/design/SHELL-UX.md:1607`（§3.2 **TC-67B** 新增，紧随 TC-67A） | 夹具 = 启动期读面不可用（空目录 + 无旧面文件）⇒ `startAffinityWatcher()`（基线置 `null`）⇒ 注入记录（读面转非 `null`）⇒ 驱动 tick；断言 = 首个非 `null` tick **只重建基线** + 后续 tick 新消耗**计入**（= **累加恢复**） |
| 7 | `docs/requirements/SHELL.md:309-310`（变更记录 2 行） | US-13 **契约面逐字未改** ⇒ 只留一轮注记（判定与理由见下「需求档说明」） |
| 8 | 本档 §2.12（本段）+ `docs/design/SHELL-UX.md:1750-1753`（变更记录 4 行） | 落点记录 + 变更留痕；**含「本批合并用户裁定 T27」一行**（本段第 2 段） |

**三方条目一致（本轮不变）**：批次档 §2 本批条目 **I1（US-13）** = 设计档回指条目 **AC21–AC25** = 需求档 **US-13**——条目集未变（无新增 AC 编号 / 无新增 US·NFR）。

**计数同步（D3）**：AC 21–25 不变（本轮改 AC23 / AC25 的判据措辞）；TC-56…TC-67 + TC-67A + **TC-67B**（+1）；DD-34…DD-40 + **DD-50**（+1）；需求档 US 13 / NFR 5 不变。

**新发现（发现即报告——逐条）**：

- **N-3（与实现冲突；已按实现更正 + 提请追认）**：TC-67A 原期望「清掉记录 ⇒ 基线重置为空档」**不成立**——`:128` 守卫要求 `s2 !== null`，而 `lastTokenSum` 只在守卫内被赋值（`:131` / `:141`）⇒
  `s2 === null` 的 tick 不进任何分支、**基线保持原值**（既非空档、非清零；同进程内只有启动时 `:125` 会把基线置 `null`）。已按实现更正三处：TC-67A 期望列（`:1605`）+ AC25 同句（`:1497`）+ §2.2.13「`null` 分支」（`:962-963`）。
- **编号枚举（已更正）**：派单建议的 **DD-41** 已被 **B12** 占用（DD-41…DD-47）；DD-48 / DD-49 为 B09 修正轮 1 ⇒ 本批新决策取 **DD-50**（枚举一致，承 D3）。
- **指针悬空（提请主 agent——台账归其写域）**：派单与台账 `docs/TODO.md:47` 记「设计修订落 §2.2.13 + **AC30** 调用面口径」——**AC30 实为 B12 的「零回退面」**（不含调用面判据）；
  「调用面零 diff」口径的真实持有者 = **AC23（B10）** ⇒ 修订已落 AC23 + 其判据细化注；祈台账 T27 行的同名指针同步更正。
- **需求档说明（派单 #7 的「否则」分支）**：US-13 契约句逐字未改——基线重建是既有「按水位差累加」机制在 `null` 恢复态的延伸（基线设定本身不计 delta 的机制既有 = `:125`），**不构成契约语义变更**；故只在变更记录留一轮注记。

**行宽与行数实测（换行符计数；行宽 = 不含行尾 CR）**：`docs/design/SHELL-UX.md` **1731 → 1754**（+23；**0** 行超 300，最宽 300 = B06 旧行）；
`docs/requirements/SHELL.md` **308 → 310**（+2；0 行超 300，最宽 299）。

**本段写入状态**：§2.12 **已写入**（经 `batch_segment`，无路径参数）。

**当次 § 段边界（本轮落笔后实测；供主 agent 核豁免面判定）**：§1 = **L5** · §2 = **L64**（本段 §2.12 = **L182–L218**）· **§3 = L219** · §4 = L244 · §5 = L246 · §6 = L248（本档共 **248** 行）。
本角色段（§2）行宽实测：**0** 行超 300（本段落最宽 242）；**本档 >300 行数 = 5**，全在 **§3**（L229–L233，359–487 字符——评审子代理段，不在本角色写域，随报告提请）。

**段边界（as-of 口径；本行自身不计）**：§1 = **L5** · §2 = **L64**（本段 §2.12 = **L182–L221**）· **§3 = L222** · §4 = L247 · §5 = L249 · §6 = L251（本档 **251** 行；本行落笔后各绝对值 +1）。

### 2.13 B10 修正轮 3（收尾轮）落点记录

**源** = 本档 §3「### 轮次 2（评审子代理）」发现 #1–#7（VERDICT = **pass**，🔴0 / 🟡3 / 🔵4）；**派单** = 主 agent 裁决「**7 条全部 Dispatched**」（🟡 = 实现前必修的判据可达性项 / 🔵 = 文档卫生）；
**修正轮边界** = 只落这 7 条直接导出的修订，**不夹带新语义**（未动累加 / 重基线既有语义、未动 `affinity.json` 结构、未动任何常量值、未动需求档）。

| 派单# | 落点（file:line；行号 = 本次落笔后实测） | 要点 |
|---|---|---|
| 1 | `SHELL-UX.md` §2.2.13「基线重建」条（`:967-970` 新增 4 行）+ `:963` + `:1004` / `:1006` + `:1317`（DD-50 注）+ AC23 判据细化③（`:1512`） | **落位 = 既有守卫行 `:128` 之前、形态 = 新增 1 行**；守卫行与既有累加 / 重基线块**不改** ⇒ 死码面消除；守卫**合取**形态四处统一；hunk 白名单写明「守卫行改动不在白名单内 ⇒ 出现即停手上报」；否决两形落档 |
| 2 | `:1500`（AC23 行）+ `:1514`（细化⑤ 新增）+ `:1611`（TC-67①）+ `:1674`（手段 12 ② 新增行） | 诊断行断言**逐例进程隔离**（`affinityDiagLogged` = 每进程至多一条）⇒ `dir=no` / `dir=yes` 各起独立进程、「正常态 0 条」须在**新进程**内评估（否则空过） |
| 3 | `:1668-1671`（手段 12 ② 载具水位断言行 + 新增 3 行「键 / 符号对应」） | `init` 键 = 设计档声明面 + 实现面逐字核对（`main.js:66` 同形）；`petSay` / `setPetState` = **`pet` 对象成员**（非顶层键）⇒ 写明对应关系；视图符号取**导出名** `handleAffinityView()`（`:262`，内部转发 `affinityView()`） |
| 4 | `:1215` / `:1216`（§2.3 B10 表两处自指行） | 枚举同步（+ `DD-50` / + `TC-67B`）；需求档变更记录计数改为「落笔 2 行 + as-of 累计 B10 相关 **3 条 / 6 行**」（实测口径——见「新发现」N-1） |
| 5 | 本档 §2.10 段边界自述（`:222`）+ §2.12 item 5 指针（`:196`） | 追加补正注（**不改历史行**）：段边界按实测更正 + DD-38 指针更正——见下「段边界与指针补正注（派单 #5）」 |
| 6 | `:1355`（§2.5 C32） | 样本档大小改**变动口径 + as-of**（7827 B = B10 落笔时；口径指向 §2.2.13「读面开销」） |
| 7 | `:970`（§2.2.13「基线重建」条末） | 补**需求侧回指注**（需求侧登记 = `docs/requirements/SHELL.md` 变更记录；本批裁定机制延伸、**契约面逐字不动**）——**需求档本轮未改**（写域硬约束） |

**#1 的守卫处置措辞（原文摘；判据可达性面）**：

- §2.2.13「落位与守卫处置」：「分支落 **tick 回调内、既有守卫行（`:128`）之前**，形态 = **新增 1 行**（`if (lastTokenSum === null && s2 !== null) lastTokenSum = s2;`）——**守卫行与既有累加 / 重基线块逐字不动**。」
- 「三类处置由此各归一处：① `s2 === null` ⇒ 两处条件皆不成立 ⇒ 基线保持原值；② `lastTokenSum === null ∧ s2 !== null` ⇒ 该行只设基线，随后守卫放行而 `s2 > lastTokenSum` / `s2 < lastTokenSum` 皆假 ⇒ **不计 delta**；③ 基线非 `null` ⇒ 该行不触发，既有逻辑逐句不变。」
- 「**否决的落位 = 分支置于守卫之后（或守卫内）**：守卫为合取（`:128`）⇒ `lastTokenSum === null` 的 tick 在守卫处即早退，分支成**死码**；**另一否决形 = 「守卫拆为两句 + 早退」**：早退会跳过 `:145` 的每次广播 ⇒ 违「广播节奏不动」口径（`:974`）。」
- AC23 判据细化③（原文）：「白名单面 = `startAffinityWatcher` 内的基线重建分支，形态 = 新增 1 行（落于既有守卫行 `:128` 之前；见 §2.2.13「落位与守卫处置」）；**守卫行 `:128` 与既有累加 / 重基线块的处置 = 不改 ⇒ 其任何改动均不在白名单内（出现即停手上报）**；该分支以外的调用面 hunk = 0。」

**#1 的四处表述一致性自查（结论：四处可同时成立）**：① `:963`（`null` 分支）= 守卫为**合取** `s2 !== null && lastTokenSum !== null`；② `:1004`（L-B10-5）同款合取 + 补「修复前 `lastTokenSum` 只在守卫内被重新赋值（`:131` / `:141`）」；
③ 本档 `:168`（N-1，修正轮 1）与 `:207`（N-3，修正轮 2）为**原症状时点**的历史记述（各自单独看均成立：当时守卫确为合取、`lastTokenSum` 确只在守卫内赋值）——修正轮 3 的落位使二者结论（「进程内累加永久暂停」/「`s2 === null` 基线保持原值」）在修后**仍成立**；
④ 其部分措辞（只提 `s2 !== null` 或只提 `lastTokenSum !== null`）由设计档的**合取全形**统领——本段为补正注，**不改 `:168` / `:207` 原文**。

**段边界与指针补正注（派单 #5；追加一行、不改历史行）**：

- **段边界实测（as-of 2026-09-17 本段落笔前；口径 = `grep -n '^## §'` 当次输出）**：§1 = **L5** · §2 = **L64**（§2.12 = L182–L221）· **§3 = L224** · §4 = **L268** · §5 = **L270** · §6 = **L272**（本档共 **272** 行）。
  §2.10 `:222` 的自述「本行落笔后各绝对值 +1」**按实测需 +2**（§3 = L224 而非 L222）——成因 = 其后的 §3 追加文本（轮次 1 / 轮次 2，共 43 行）使下移量超过自述；**行号只作 as-of 参考**（D4），以当次实测为准。
- **>300 行分布实测**：本档 **10** 行超 300（不含行尾 CR，最宽 614）——**全在 §3**（轮次 1：L234–L238；轮次 2：L251 / L253 / L257 / L258 / L259）；`§2.10 `:220` 所记「L229–L233（5 行）」已被本轮实测取代（现 = 10 行 / L234–L259）。
  §3 不在本角色写域 ⇒ **只报不改**，随报告提请主 agent / 评审侧处置。
- **指针更正（DD-38）**：本档 §2.12 item 5（`:196`）记 `SHELL-UX.md:1297`（DD-38）——评审轮次 2 实测 = `:1298`；修正轮 3 落笔后实测 = **`:1303`**（其上新增 5 行：§2.2.13 重建条 4 行 + §2.3 表下补正注 1 行）。

**三方条目一致（本轮不变）**：批次档 §2 本批条目 **I1（US-13）** = 设计档回指条目 **AC21–AC25** = 需求档 **US-13**——条目集未变（无新增 AC / US / NFR；TC / DD 计数不变）。

**计数同步（D3）**：AC21–AC25 不变；TC-56…TC-67 + TC-67A + TC-67B（不变，本轮只改判据措辞）；DD-34…DD-40 + DD-50（不变）；US 13 / NFR 5 不变。设计档 §2.3 B10 表两处自指行的**枚举**已同步（`:1215` / `:1216`）。

**行数与行宽实测（换行符计数；行宽 = 不含行尾 CR）**：`docs/design/SHELL-UX.md` **1754 → 1768**（修正轮 3 **+14**——含 §2.3 表下补正注与 §四 变更记录补正行）；**0** 行超 300（最宽 300 = B06 旧行）；
`docs/requirements/SHELL.md` **310**（本轮**未改**该档——裁定契约面不动；0 行超 300 / 最宽 299）。**本档（批次档）段边界**见上条补正注（本段自身 append 会使其后移——as-of 口径）。

**新发现（发现即报告——逐条，本角色未自决 / 未改他段）**：

- **N-1（计数口径；已按实测落 §2.3 表）**：评审轮次 2 #4 记需求档「变更记录**现 4 行**」，本角色实测 B10 相关 = **3 条 / 6 行**（落笔 2 + 修正轮 1 2 + 修正轮 2 2）⇒ 设计档按实测口径落「落笔 2 行 + 累计 3 条 / 6 行」；若主 agent 采另一口径，请一并同步 §2.3 表（`:1215`）。
- **N-2（枚举面；设计档 §2.6 B10 追认块）**：该块 L-B10 枚举 = 「L-B10-1 / L-B10-2 / L-B10-4 / L-B10-5」——**缺 L-B10-3**；因该处未作计数声明（未称「五条」），判断为**有意**（L-B10-3 = 安全前提，判据面由判据 ④ 与 DD-38 承载）。本轮未改，提请评审裁定是否需补登。
- **N-3（报告项；§3 段行宽）**：批次档 §3 实测 **10** 行超 300（L234–L259）——评审子代理段，不在本角色写域 ⇒ 只报不改（与上「段边界与指针补正注」第二条同源）。
- **N-4（载具实现面；供 §5 实施期核对）**：`shell-affinity.js` 的 `init(deps)` 实际键 = `getPetWindow` / `pet` / `setQuitting` / `APP_NAME`（`main.js:66` 亲读），而设计档 §2.2.6 该行所列注入面写作 `petSay` / `setPetState`——两者关系已按「`pet` 对象成员」写明（§3.3 手段 12 ②）；**§2.2.6 该行原文本轮未改**（B06 面措辞；如需统一请另批裁定）。

**本段写入状态**：§2.13 **已写入**（经 `batch_segment`，无路径参数）。

**段边界补正（派单 #5 追加；本行之前实测）**：§1 = **L5** · §2 = **L64**（§2.13 = L224–L273）· **§3 = L274** · §4 = **L318** · §5 = **L320** · §6 = **L322**（本档共 **322** 行）；**>300 行 = 10**，全在 §3（L284–L288 / L301 / L303 / L307–L309）；
本行 append 后 §3 / §4 / §5 / §6 各自后移（后移量 = 本行起的追加行数；D4：行号只作 as-of 参考，以当次 `grep -n '^## §'` 实测为准）。
**本段行宽自查（D6 回读核对）**：本补正行首版为单行 **306** 字符（超 300）⇒ 已**原地重排为 2 行**（内容逐字不变、仅换行位置变）——本段（§2）现测 **0** 行超 300（本档 >300 行全在 §3：评审子代理段，不在本角色写域）。

### 2.14 B10 修正轮 4（实施后收口轮）落点记录

**源** = 本档 §5（eng-coder 交付摘要 + 残余风险）的**残余 #1 / #2 / #3 / #7**；**派单** = 主 agent 逐条亲验后裁定「**4 条全部 Dispatched**」；
**修正轮边界** = 只落这 4 条直接导出的修订，**不夹带新语义**（未动计费口径 / 累加 / 重基线 / `affinity.json` 结构 / 任何常量值 / 任何 `.js` 代码——代码改动按要求归下一轮 eng-coder）。

| 派单# | 落点（file:line；行号 = 本轮落笔后实测） | 改前 → 改后 |
|---|---|---|
| 1 | `SHELL-UX.md:1518` · `:1553-1555` · `:1690` | 判据「无目录递归」→「**读面内零递归**」（AC21 行 + 手段 12 ① + 新增「AC21 判据细化」；冻结函数内 3 处 `recursive` 不作判据） |
| 2 | `SHELL-UX.md:931-932` · `:957` · `:960-962` · `:1013` · `:1519` · `:1642` | **旧面可用判据**（可解析 ≠ 可用 ⇒ 该形态不可用 ⇒ `null`，**不得返回 `0`**）+ 容错表行 + 表下注 + L-B10-4 对照条 + **TC-67C** |
| 3 | `SHELL-UX.md:1024-1026` · `:1324` · `SHELL.md:197-198` · `:311-312` | US-13 括注按实况更新（`0.1.5-rc.1`）+ 两分句状态 + DD-39 理由格更正 + 需求档变更记录 +1 条 |
| 4 | `SHELL-UX.md:1231-1232` · `:1244` · `:1234` · `:1255-1256` · `:1818-1823` | 末行数 **354** / **323**（标记改**已回填**）+ 体量裁定 ≈355 → 354 + 自指行枚举（+ **TC-67C**） |

**逐条（改前 → 改后）**：

- **#1**：AC21 行原文「+ 导出在场 + **无目录递归**」；手段 12 ① 原文「`recursive` **0** 处（无目录递归）」。改后 = **读面内零递归**（结构面 `fs.readdirSync(dir)` **不带任何选项**，as-of `shell-affinity.js:113`
  + 行为面佐证 = 载具 `static` / `read` 用例）；新增加细三条——其中② = **全档 `recursive` 计数不作判据**（档内 3 处既有 `recursive` 全在**冻结函数**内 `:63` / `:268` / `:300`，本批禁改）。
- **#2**：新增**旧面可用判据** = 文件可解析 **且** `tables.sessions` 含至少一条**有效会话条目**（有 `rows.tokenUsage.val.totals` 可取）⇒ 可用；缺失 / 空 / 无有效条目 ⇒
  **该形态不可用** ⇒ 走下一形态（⇒ 判据 ④ 的 `null`）——**不得返回 `0`**；理由（注内）= `0` 会把水位降到 0 ⇒ 读数回升时**已计入的消耗被重复计入**（DD-38 的原理由，L-B10-2 同族）；
  对照条 = **L-B10-4**（目录面「记录 ≥ 1 但四桶全缺」）的**旧面**面。**实施侧改动（`legacySessionTokens()`，as-of `shell-affinity.js:90-103`）归下一轮 eng-coder**（本批批准清单外的改动）。
- **#3**：需求档括注「内置 Harness = `0.1.0-rc.6`，用单文件布局」→「内置 Harness 现为 **`0.1.5-rc.1`（per-record 目录布局）**」
  （实测 `dsh-bundle/node_modules/@deepseek-ai/dsh/package.json:4`，B08 钉版后）；设计侧记明 = 第一分句（内置 bundle ≥ 0.1.5——判据记为「出厂路径产出 per-record 布局」）**已满足** /
  第二分句（活跃副本枚举无 < 0.1.5）**不可从仓内验证** ⇒ 兜底与消解期**保留**、到期核对**顺延至下次发版复核**；DD-39 理由格按实况更正（原「兜底**当前仍是出厂路径的活跃形态**」）。
- **#4**：§2.3 B10 表两行末行数「**实施期实测回填**」→ **354**（`shell-affinity.js`）/ **323**（`shell-backend.js`）——标记改为**已回填**（换行符计数、实施后实测）；
  体量裁定行估值 ≈355 → **354**；本档自指行枚举同步（+ **TC-67C**，D3 计数与枚举同改）；§四 变更记录 6 行。

**三方条目一致（本轮不变）**：批次档 §2 条目 **I1（US-13）** = 设计档回指条目 **AC21–AC25** = 需求档 **US-13**——条目集未变（无新增 AC / US / NFR）。

**计数同步（D3）**：AC21–AC25 不变；**TC +1**（**TC-67C**——B10 TC 块 14 → **15**：TC-56…TC-67 + TC-67A·TC-67B + TC-67C）；DD-34…DD-40 + DD-50 不变；US 13 / NFR 5 不变。

**自查（派单三项）**：

- ① **#1 改后三处一致**：AC21 行 + §3.3 手段 12 ① + 载具断言口径（载具由手段 12 ① 承重）——设计档全文已**无**「全档 `recursive` 0 处」活口径（机检：`recursive` 命中 2 行，均为**面限定**表述）；档内 3 处既有 `recursive` 位置实测 = `shell-affinity.js:63` / `:268` / `:300`（与细化②逐条一致）。
- ② **#2 指针不矛盾**：新判据（旧面可用判据）与 **DD-38**（返回 `null` 非 `0`）/ **L-B10-2**（水位回升可能重复计入）/ **L-B10-4**（目录面记录 ≥ 1 但四桶全缺 ⇒ 静默归零）互不矛盾——三者危险值同为 `0`、安全侧同为 `null`；L-B10-4 条末已明写「旧面对照条」。
- ③ **#3 改后同源**：需求档 US-13 消解期（`SHELL.md:197-198`）与设计档 §2.2.13 消解期（`SHELL-UX.md:1015-1026`）同源——同括注（`0.1.5-rc.1`）/ 同两分句状态（第一已满足 · 第二不可仓内验证）/ 同顺延口（下次发版复核）；**到期条件 / 保留期 / 顺延重登三句在需求档逐字未动**。

**行数与行宽实测（换行符计数；行宽 = 不含行尾 CR；as-of = 本段落笔前）**：

- `docs/design/SHELL-UX.md` **1801 → 1824**（修正轮 4 **+23**）；**0** 行超 300（最宽 **300** = B06 旧行）；EOL 全档 CRLF。
- `docs/requirements/SHELL.md` **310 → 312**（**+2** = 消解期现状行改述 + §五 变更记录 **+1 条 / 2 行**）；**0** 行超 300（最宽 **299**）。
- §2 本段行宽：**0** 行超 300（落笔前逐行机检）。

**新发现（单列不改笔——提请主 agent 裁定）**：

- **N-1（判据字面口径；由 #3 直接派生）**：到期条件句写「内置 bundle **≥ 0.1.5**」，而现行出厂 bundle = **`0.1.5-rc.1`**——按 semver 严格比较，预发布版 `0.1.5-rc.1` **<** `0.1.5` ⇒ 该分句**按字面尚未成立**。本轮按主 agent 裁定记「**已满足**」并把判据明写为「出厂路径产出 per-record 布局」（该读法下成立）。建议下次发版复核时把判据句改为「≥ `0.1.5-rc.1`（或任意产出 per-record 布局的版本）」或明写预发布版纳入——**判据句语义面，本角色未自决**。
- **N-2（同族过期事实；本轮只加 as-of 说明）**：§2.1 L-1 候选 1 / 候选 2（`SHELL-UX.md:211-212`）与 §2.4 DD-34（`:1317`）的理由格仍引「出厂 `0.1.0-rc.6`（单文件布局）」——本轮**未改其原文**（否决理由 / 决策理由的语义面），仅在消解期加「**同族 as-of 说明**」（`:1026`）标明其为 B10 落笔时点事实、随 B08 钉版过期、双形态结论不受影响。**是否逐处加 as-of 标注 = 提请裁定**。
- **N-3（写域外·只报不改）**：本档 §3 与 §5 段实测 **12** 行超 300（as-of 本段落笔前：§3 = L288–L292 / L305 / L307 / L311–L313；§5 = L329 / L364，359–614 字符）——两段均不在本角色写域（§3 = 评审子代理段 / §5 = eng-coder 段）⇒ 只报不改。

**本段写入状态**：§2.14 **已写入**（经 `batch_segment`，无路径参数）。

**本段落笔后收尾（行宽重排 + 段边界实测；as-of 2026-09-17）**：

- **形态修正（本段自持面，D6 回读核对）**：本段落点表首版的 4 行「改前 → 改后」单元格实测 **322–549** 字符（超 300）⇒ 已按「**表行只留摘要 + 细目下沉为 `- **#n**：…` 分条多行**」重排（内容逐字保留、仅换行位置与归属层变；承 §2.10 同款处置），经直接 `edit` 落于本段（本段作者 = eng-designer，未触碰他段）。
- **本段现测（D6 回读）**：**§2 全段（L64–L326）0 行超 300**（本段最宽 **299**）；本档整档 >300 行数 = **12**，全在 **§3** 段（L337–L341 / L354 / L356 / L360–L362）与 **§5** 段（L378 / L413）——两段均不在本角色写域 ⇒ **只报不改**。
- **段边界实测（口径 = `grep -n '^## §'` 当次输出）**：§1 = **L5** · §2 = **L64**（本段 §2.14 = **L278–L326**）· **§3 = L327** · §4 = **L371** · §5 = **L373** · §6 = **L418**（本档共 **418** 行）；本次 append 使其后各段号后移（后移量 = 本次追加行数；D4：行号只作 as-of 参考）。

### 2.15 B10 修正轮 5（单条 · 判据字面口径收口）落点记录

**源** = 本档 §2.14「新发现 N-1」（报而不改——主 agent 复核后裁定 = **修**；同类第 5 次：判据的**字面**与**语义**不一致 ⇒ 字面永假，而文档记「已满足」）；**派单** = 单条；
**修正轮边界** = 只落该条直接导出的修订（第一分句**字面**改写 + 同源面字面改写 + 留痕），**不夹带新语义**（保留 / 顺延 / 到期核对三句语义、条目计数、任何 `.js` 代码、`affinity.json` 结构 / 常量均未动）。

**事实（改前）**：需求档 `SHELL.md:198` 的消解期**第一分句字面** = 「内置 bundle ≥ 0.1.5」，而出厂 bundle 实测 = **`0.1.5-rc.1`**
（`dsh-bundle/node_modules/@deepseek-ai/dsh/package.json:4`，本轮回读实测）——判据 = semver 2.0.0 §11「Precedence」阶 4：**预发布标识使版本低于同号正式版** ⇒ `0.1.5-rc.1` **<** `0.1.5` ⇒ 原字面**按字面尚未成立**，与 §2.14 所记「已满足」相抵（N-1 原文）。

| 派单# | 落点（file:line；行号 = 本轮落笔后实测） | 改前 → 改后（摘要） |
|---|---|---|
| 1 | `SHELL.md:198`（第一分句 · 复核句） · `:199`（第一分句 · 到期条件句） | 字面「内置 bundle ≥ 0.1.5」→「内置 bundle ≥ `0.1.5-rc.1`（或任意产出 per-record 布局的版本）」（两处同口径） |
| 2 | `SHELL-UX.md:1022`（§2.2.13 到期条件句） · `:1024`（修正轮 4 复核句） | 同上字面（同源同步；结论「已满足」不变） |
| 3 | `SHELL.md:201` · `SHELL-UX.md:1026-1027` | 各 +1 条「修正轮 5 字面口径收口」说明（改前字面 + semver 理由 + 判据语义未变） |
| 4 | `SHELL.md:315-316` · `SHELL-UX.md:1826-1828` | 两档变更记录各 +1 条（含「落档实测」补正行——行数 / 行宽） |

**逐条（改前 → 改后 全文）**：

- **#1**：`SHELL.md:198` 改前 = 「⇒ 到期条件第一分句（内置 bundle ≥ 0.1.5——判据记为「出厂路径产出 per-record 布局」）**已满足**，第二分句（活跃副本枚举中无 < 0.1.5）**不可从仓内验证** ⇒ 兜底与消解期**保留**（…）、到期核对顺延至**下次发版复核**；到期条件 = 内置 bundle ≥ 0.1.5（出厂路径不再产生旧布局）**且**活跃副本枚举中无 < 0.1.5 版本 ⇒ 由主 agent 在 B08 收口时核对并裁定剔除兜底分支；」
  改后 = 同句**仅换第一分句字面**（两处），并按 ≤300 字符重排为 2 行（`:198` / `:199`）——其余文字逐字未动。
- **#2**：`SHELL-UX.md:1022` 改前「到期条件 = 内置 bundle **≥ 0.1.5**（出厂路径不再产生旧布局）」→ 改后「到期条件 = 内置 bundle **≥ `0.1.5-rc.1`**（或任意产出 per-record 布局的版本；即出厂路径不再产生旧布局）」；
  `:1024` 同款字面（修正轮 4 复核句的第一分句括注）。
- **#3**：两档新增说明条——`SHELL.md:201`（「原第一分句字面「内置 bundle ≥ 0.1.5」按 semver 严格比较对预发布版 `0.1.5-rc.1` 恒假 ⇒ 改写为字面可满足的…；**判据语义未变**」）+ `SHELL-UX.md:1026-1027`（同旨 + 逐字同源句指针）。
- **#4**：`SHELL.md` §五 +1 条 / 2 行；`SHELL-UX.md` §四 +1 条 / 3 行（含补正行）。

**自查（两档全档 grep `≥ 0.1.5` / `0.1.5` 逐处；处置判径 = 「是否活判据 → 改 / 不改」）**：

| 命中（file:line） | 面 | 活判据？ | 处置 |
|---|---|---|---|
| `SHELL.md:198` · `:199` | US-13 消解期第一分句（复核句 + 到期条件句） | **是** | **本轮改**（新字面 = ≥ `0.1.5-rc.1`（或任意产出 per-record 布局的版本）） |
| `SHELL-UX.md:1022` · `:1024` | §2.2.13 到期条件句 / 修正轮 4 复核句 | **是** | **本轮改**（同源同口径） |
| `SHELL-UX.md:1326` · `:1468` | DD-39 决策表 / §2.6 B10 追认块 | **是**（复述到期条件字面；`:1326` 并写「第一分句已满足」） | **不改**——写域外（本轮写域 = §2.2.13 + §四）⇒ 见 **N-1** |
| `SHELL.md:201` · `:315` · `SHELL-UX.md:1826` | 本轮说明条 / 变更记录行 | 否（引用改前字面作留痕） | 保留（改前字面必须留痕） |
| `SHELL.md:302` · `SHELL-UX.md:1823` | 历史变更记录（B10 立案 / 修正轮 4 条） | 否（历史记录） | **不改**（不回改历史） |
| `SHELL.md:24` · `:44` · `:137` · `:139` · `:296` | US-9 / B07 面（Harness ≥ 0.1.5 会话鉴权） | 否（**另一条判据**：鉴权引入版本） | 不改 ⇒ 见 **N-3**（观察项） |
| `SHELL-UX.md:135` · `:141` · `:198` · `:211` · `:213` · `:656` · `:830` · `:835` · `:925` · `:1377` · `:1422` | 事实 / 选型 / 决策理由（0.1.5 或 0.1.5-rc.1 = 版本事实） | 否（非判据字面） | 不改 |
| 批次档 `:80`（§1） · `:149` · `:298` · `:321` · `:343` | 立案表 / 落点表 / N-1 原文 / §3 评审段 | 否（§1 = 主 agent 段历史立案；余为他段记录） | 不改（写域外；§2.15 即本轮记录） |

**同源核对（需求档 ↔ 设计档 消解期逐句）**：保留期（B08 收口点）✓ · 第一分句**新字面逐字相同** ✓ · 第二分句（活跃副本枚举中无 < 0.1.5）✓ · 保留理由（活跃副本不可仓内枚举）✓ · 顺延重登（条件不成立 ⇒ 顺延并重新登记，不得默认永久）✓ · 到期核对口（主 agent 在 B08 收口时核对并裁定）✓；
**无「字面为假而记已满足」的活口径残留**（两档 grep 逐处见上表——残留仅 `SHELL-UX.md:1326` / `:1468`，写域外，见 N-1）。

**行数与行宽实测（换行符计数；行宽 = 不含行尾 CR；as-of 本轮落笔后）**：

- `docs/requirements/SHELL.md` **312 → 316**（+4 = 消解期同源行 198 → 2 行 + 「修正轮 5」注 1 行 + §五 +1 条 / 2 行）；**0** 行超 300（最宽 **299**）。
- `docs/design/SHELL-UX.md` **1824 → 1829**（+5 = §2.2.13「修正轮 5」说明条 2 行 + §四 变更记录 3 行——含补正行）；**0** 行超 300（最宽 **300** = B06 旧行）；EOL 全档 CRLF。
- 回读核对（D6）：四处落点（`SHELL.md:198-201` · `:315-316` · `SHELL-UX.md:1022` · `:1024-1027` · `:1826-1828`）逐处回读在场，与写入内容一致。

**新发现（单列不改笔——提请主 agent 裁定）**：

- **N-1（同类活口径残留 · 写域外；同一缺陷类的剩余两处）**：`SHELL-UX.md:1326`（DD-39 决策表：左格「到期条件 = 内置 bundle ≥ 0.1.5 ∧ 活跃副本无 < 0.1.5」+ 理由格「第一分句**已满足**」⇒ **字面与结论相抵**未消）与 `:1468`（§2.6 B10 追认块 DD-39 条——复述同字面）——
  本轮写域 = 「§2.2.13 消解期同源句 + §四 变更记录」（派单原文）⇒ **未动**；建议修法（与 §2.2.13 同口径）：两处字面改「≥ `0.1.5-rc.1`（或任意产出 per-record 布局的版本）」，理由 / 结论不动。
- **N-2（同族 · 第二分句的字面风险；派单明文逐字不动）**：第二分句字面「活跃副本枚举中**无 < 0.1.5**」——严格 semver 下 `0.1.5-rc.1` **<** `0.1.5` ⇒ 若用户机上的活跃副本为 `0.1.5-rc.1`（已产出 per-record 布局），该分句按字面**判为不成立**（兜底保留——方向保守，非误报）。
  与 N-1 同族（**字面 vs 语义**）；派单明文「其余四句逐字不动」⇒ 本轮未动，提请下次发版复核（B08 收口点面）一并裁定字面口径（候选 = 「无产出旧布局的版本」）。
- **N-3（观察项；非本批判据）**：`SHELL.md:24` · `:44` · `:137` · `:139` · `:296` 的「Harness ≥ 0.1.5」= **US-9 / B07 面**的另一条判据（web 会话鉴权引入版本）——同样面对「活跃 / 出厂 = `0.1.5-rc.1`」的 semver 字面问题，但其验收面（AC9 / TC-27）以**具体版本事实**取证，未出现「字面为假而记已满足」。**不属本轮写域**，仅登记观察。
- **N-4（写域外 · 只报）**：批次档 `:80`（§1 立案表）与 `:149`（§2.14 落点表）仍含旧字面——§1 = 主 agent 段历史立案记录 / §2.14 = 上一段记录 ⇒ 均不改（本段 §2.15 即本轮记录，不代改他段）。

**三方条目一致（本轮不变）**：批次档 §2 条目 **I1（US-13）** = 设计档回指条目 **AC21–AC25** = 需求档 **US-13**——条目集未变（无新增 AC / US / NFR / TC / DD）。
**计数同步（D3）**：US 13 / NFR 5 不变；AC21–AC25 / TC-56…TC-67C / DD-34…DD-40·DD-50 / C / O 均不变（本轮为**字面**修订，不改条目与判据面）。

**本段写入状态**：§2.15 **已写入**（经 `batch_segment`，无路径参数）。

### 2.16 B10 修正轮 6（判据字面口径 · 家族全清）落点记录

**源** = 本档 §2.15「新发现 **N-1**（DD-39 决策表行 / §2.6 追认块复述同字面）与 **N-2**（第二分句字面风险）」——主 agent **逐条复核后裁定「均 Dispatched」**，并**代列全该判据家族的全部剩余出现处 + 授权扩写域**（上轮因派单写域受限未动）；
**派单** = 3 条落点面（DD-39 决策表行 · §2.6 追认块 · 「第二分句字面」家族全清）；
**修正轮边界** = 只落「两分句统一为**行为面**口径」直接导出的修订 + 同族逐处同步 + 留痕，**不夹带新语义**（结论 / 保留期 / 顺延重登 / 到期核对四口语义逐字未动；条目计数与 AC21–AC25 / TC-56…TC-67C / DD / C / O 均未动；**本轮不新增 TC**；未碰任何 `.js`、`affinity.json` 结构 / 常量）。

**事实（改前）**：到期条件**第二分句**字面 = 「活跃副本枚举中无 `<` 0.1.5」——而出厂 / 活跃副本实测 = **`0.1.5-rc.1`**（`dsh-bundle/node_modules/@deepseek-ai/dsh/package.json:4`，B08 钉版后实测 2026-09-17）
⇒ 判据 = semver 2.0.0 §11「Precedence」阶 4：**预发布标识使版本低于同号正式版** ⇒ `0.1.5-rc.1` **<** `0.1.5` ⇒ **产出 per-record 布局的预发布副本被字面计入「< 0.1.5」**——与第一分句（修正轮 5 已消解）**同型**：**以版本号比较代替「是否产出旧布局」**。

| 派单# | 落点（file:line；行号 = 本轮落笔后实测） | 改前 → 改后（摘要） |
|---|---|---|
| 1 | `SHELL-UX.md:1022`（§2.2.13 到期条件句）· `:1025`（修正轮 4 复核句第二分句） | 第二分句字面「活跃副本枚举中**无 `<` 0.1.5**」→「**无产出旧布局的版本**」，并补判据括注（= 逐副本「是否产出 per-record 布局」，**不以版本号比较为判据**） |
| 2 | `SHELL-UX.md:1028-1029`（新增「修正轮 6」说明条 2 行）· `:1023`（可机检口径行） | 说明条 = 改前字面 + semver 理由 + 新字面 + 「同口径（行为面）、版本号只作举例」；`:1023` 可机检口径「版本枚举」→「**活跃副本枚举 + 逐副本形态判定**」 |
| 3 | `SHELL-UX.md:1328`（§2.4 **DD-39 决策表行**）· `:1470`（§2.6 **B10 追认块** DD-39 条） | 左格 / 正文「内置 bundle ≥ 0.1.5 ∧ 活跃副本无 `<` 0.1.5」→「内置 bundle **产出 per-record 布局** ∧ 活跃副本枚举中**无产出旧布局的版本**」；理由格「到期条件可机检（版本枚举）」→「（副本枚举 + 形态判定）」 |
| 4 | `SHELL.md:198`（第二分句 · 复核句）· `:199`（第二分句 · 到期条件句）· `:202`（新增「修正轮 6」注 1 行） | 同源同口径字面改写（与设计档逐目同旨）+ 说明条（改前字面 + semver 理由 + 判据语义未变） |
| 5 | `SHELL.md:318-319` · `SHELL-UX.md:1831-1833` | 两档变更记录各 +1 条（含「落档实测」补正行——行数 / 行宽 / EOL） |

**自查（两档各跑全档 grep：`≥ 0.1.5` · `< 0.1.5` · `0.1.5-rc.1` 逐处；处置判径 = 「是否活判据 → 改 / 不改」）**：

| 命中（file:line） | 面 | 活判据？ | 处置 |
|---|---|---|---|
| `SHELL-UX.md:1022` · `:1023` · `:1025` · `:1028` | §2.2.13 到期条件句 / 可机检口径行 / 复核句第二分句 / 本轮说明条 | **是**（`:1028` 引用改前字面作留痕） | **本轮改**（新口径 = 行为面；`:1023` 改机检口径） |
| `SHELL-UX.md:1328` · `:1470` | §2.4 DD-39 决策表行 / §2.6 追认块 DD-39 条 | **是** | **本轮改**（上轮 N-1 的两处，写域本轮已授权） |
| `SHELL.md:198` · `:199` · `:202` | US-13 消解期第二分句（复核句 + 到期条件句）+ 本轮说明条 | **是** | **本轮改**（同源同口径） |
| `SHELL-UX.md:1024` · `:1027` · `SHELL.md:197` · `:201` | 修正轮 4 复核句第一分句 / 修正轮 5 说明条「现字面」行 / 消解期现状行 / 修正轮 5 注 | 是（**已在修正轮 5 改为新口径**） | 不改（回读核对 = 已同口径：≥ `0.1.5-rc.1`（或任意产出 per-record 布局的版本）） |
| `SHELL-UX.md:1026` · `:1825` · `:1828` · `:1831` · `SHELL.md:201` · `:303` · `:314` · `:316` · `:318` | 各轮说明条 / 变更记录（引用**改前**字面作留痕；`:303` = B10 立案历史条） | 否（历史留痕） | **不回改**（历史留痕必须保留原字面） |
| `SHELL-UX.md:141` · `:656` · `:830` · `:925` · `:1424` · `SHELL.md:185` | 事实 / 选型 / 决策理由（`0.1.5` 或 `0.1.5-rc.1` = **版本事实**，非判据字面） | 否 | 不改 |
| `SHELL.md:24` · `:44` · `:137` · `:139` · `:297` · `SHELL-UX.md:1510` · `:1603` | US-9 / B07 面（Harness ≥ 0.1.5 会话鉴权）与 AC9 / TC-27 | 否（**另一条判据**） | **不改**（主 agent 已裁定 Not an issue；见 N-5） |

**同源核对（需求档 ↔ 设计档 消解期逐目）**：保留期（B08 收口点）✓ · 第一分句（≥ `0.1.5-rc.1`（或任意产出 per-record 布局的版本））✓ · **第二分句新字面两档同旨**（活跃副本枚举中**无产出旧布局的版本**）✓ · 保留理由（活跃副本不可仓内枚举）✓ · 顺延重登（条件不成立 ⇒ 顺延并重新登记，不得默认永久）✓ · 到期核对口（主 agent 在 B08 收口时核对并裁定）✓；
**0 活判据残留**：两档全档 grep 逐处见上表——剩余命中面 = 历史留痕 / 版本事实 / 他面判据，**均非本批活判据**。

**行数与行宽实测（换行符计数；行宽 = 不含行尾 CR；as-of 本轮落笔后）**：

- `docs/requirements/SHELL.md` **316 → 319**（+3 = US-13 内「修正轮 6」注 1 行 + §五 +1 条 / 2 行）；**0** 行超 300（最宽 **299** = `:306` 既有行）。
- `docs/design/SHELL-UX.md` **1829 → 1834**（+5 = §2.2.13「修正轮 6」说明条 2 行 + §四 +1 条 / 3 行——含补正行）；**0** 行超 300（最宽 **300** = `:1238` B06 旧行；本轮改后 DD-39 行 = **298**）。
- EOL：两档**全 CRLF**（LF-only = **0**——不做归一，形态统一属 B11 面）。回读核对（D6）：五处落点逐处回读在场，与写入内容一致。

**新发现（单列不改笔——提请主 agent 裁定）**：

- **N-5（观察项 · 他面 · 承上轮 N-3；主 agent 已裁定 Not an issue ⇒ 本轮未动）**：`SHELL.md:24` · `:44` · `:137` · `:139` · `:297` 与 `SHELL-UX.md:1510` · `:1603` 的「Harness ≥ 0.1.5」= **US-9 / B07 面**的另一条判据（web 会话鉴权引入版本）——同样面对「活跃 / 出厂 = `0.1.5-rc.1`」的 semver 字面问题，但其验收面（AC9 / TC-27）以**具体版本事实**取证，无「字面为假而记已满足」形态。
- **N-6（写域外 · 只报）**：历史记录内的 **as-of 引用**随本轮 +2 行漂移——`SHELL-UX.md:1829`（修正轮 5 记录）写「新增「修正轮 5」说明条 2 行（改后 `:1026-1027` 区）」，现实际为 `:1026-1027`（第一分句说明条）与 `:1028-1029`（本轮新增）；`SHELL.md:317`（修正轮 5 记录）写「保留期 / **第二分句** / 顺延重登 / 到期核对四句逐字未动」= 描述**当轮**范围（当轮第二分句确未动）。
  二者均为**历史段内表述**（D4：行号只作 as-of 参考）⇒ **不回改**，仅登记备查。
- **N-7（写域外 · 只报 · 承上轮 N-4）**：`SHELL.md:303`（§五「B10 批次追加」历史条）与 `SHELL-UX.md:1825`（修正轮 4 条）、批次档 §1 / §2.15 内仍含旧字面（`内置 bundle ≥ 0.1.5 ∧ 活跃副本无 < 0.1.5`）——**历史变更记录 / 他段记录** ⇒ 按派单「历史留痕不回改」保留；本段 §2.16 即本轮记录，不代改他段。

**三方条目一致（本轮不变）**：批次档 §2 条目 **I1（US-13）** = 设计档回指条目 **AC21–AC25** = 需求档 **US-13**——条目集未变（无新增 AC / US / NFR / TC / DD）。
**计数同步（D3）**：US 13 / NFR 5 不变；AC21–AC25 / TC-56…TC-67C / DD-34…DD-40·DD-50 / C / O 均不变（本轮为**字面口径**修订，不改条目与判据面；**未新增 TC**）。

**本段写入状态**：§2.16 **已写入**（经 `batch_segment`，无路径参数）。

## §3 设计评审（评审子代理）

### 轮次 1（评审子代理）

设计评审（B10：好感度「真实消耗」读面修复）——评审对象 = 批次档 §1/§2 + `docs/design/SHELL-UX.md` §2.2.13 / 选型 L-1·L-2 / DD-34…DD-40 / AC21–AC25 / TC-56–TC-67 / 手段 12 + `docs/requirements/SHELL.md` US-13。
证据声明：本轮亲读三档全文 + 机检行宽（`.{301,}` 不含行尾 CR 于三档均 **0** 命中，与设计 :1144 的「0 行超 300」一致）；`.js` / `docs/TODO.md` 不在本评审声明范围 ⇒ 其行号 / 行数引用（`shell-affinity.js:68-83` · 305 → ≈351 · `shell-backend.js:286-290` / `:311-322` · `docs/TODO.md:32`）本轮 **unverified**。
项目规范档未在本轮声明（以 Project Guide + 评审判据为准，已在报告标注该限制）。

| # | Category | Severity | Issue | Suggestion |
|---|----------|----------|-------|------------|
| 1 | Requirements / Clarity | 🟡 | US-13 的「消解期」契约点在设计里无落点：§2.2.13 全文无「消解期 / 到期条件」句（全文 grep 仅命中 `SHELL-UX.md:208`·`:1208`·`:1393`），§2.6 B10 追认块（`:1337-1342`）只列 DD-35 / DD-37 / DD-40 / L-B10-1·2；而批次档 `:94` 与 AC22（`:1393`）都断言其落点 = 「§2.2.13 边界 + DD-39 + §2.6 追认块」，`§2.1 L-1`（`:208`）更直指「§2.2.13 消解期」这一不存在的节 ⇒ 两条判据 / 指针自指面与文中实际内容不符 | 二选一收口：把消解期句（保留至 B08 收口点 / 到期条件 = 内置 bundle ≥ 0.1.5 ∧ 活跃副本无 < 0.1.5 / 不成立 ⇒ 顺延并重登，不得默认永久）写入 §2.2.13 边界，或把指针改为只指 DD-39；并把 DD-39 补进 §2.6 B10 追认块（它是对 B08 的前向承诺） |
| 2 | Acceptance criteria | 🟡 | AC21–AC25 标「全机检（夹具 + 桩电子）」，但其载具仍未定：桩测脚本是否采用待主 agent 裁定（§3.3 手段 12 ③ `:1536` + 批次档 `:140`，因本批边界声明「零新增文件」）；批次档 §1.6 AC1（`:51`）明文要求「须给命令与期望」，设计只给机制与期望值（49604）未给可复跑命令；AC25 / TC-66 另需驱动 `startAffinityWatcher()` 的 10 s 周期，载具如何驱动（假定时器 / 手动调用 `:116-147`）未说明 | 协调项（非缺陷）：请主 agent 就载具（`.thincoder/` 脚本 or 仓外临时脚本）给裁定；裁定后把可复跑命令与夹具定义写入 §3.3 手段 12，使「全机检」不依赖未决项 |
| 3 | Clarity / Consistency | 🟡 | `sumSessionTokens` 在同一档内被三处描述为「无副作用 / 不写盘、不持状态」（`:520`·`:942`·`:931`），但同节规定它在「两形态均不可用」时经 `backend.writeDiag` 写 console + `bigfish.log`（`:944-953`）并持档内标记 `affinityDiagLogged`（`:942`）⇒ 同一机制两处描述不一致；实现者需自行决定诊断发射点（函数内 vs 调用方），且以「无副作用」为由导出的测试缝在 AC23 / TC-62 / TC-67① 这类夹具上会**真的落盘并写主机日志** | 统一措辞为「返回值幂等；诊断行为 = 每进程至多一次、无持久状态」，并明确发射点；同时把 `shell-affinity` 的导出面 +1（测试缝）补进 §2.6 B10 追认块——现追认块只登记 DD-40 的 `shell-backend` 导出面，而 `:561` 锚点附注记的是「导出面 2 项」 |
| 4 | Affected-file size annotations | 🟡 | 结构档判据：`shell-affinity.js` 305 → ≈351 越 300 软档（§2.3 B10 表 `:1139`），但本批只有一行单函数核对 + 「NFR-3 的 500 行硬限亦无影响」（`:1164`，且该行错位落在 B12 表段末），缺 B07 同款「>300 软档 体量裁定（事实 / 理由 / 消解路径）」（对照 `:1118`）；`shell-backend.js` 322 → 323 亦越 300，而 `:1118` 的消解路径明文「新增独立职责（新增导出面 / 新增落盘机制）⇒ 另批做拆分复核」——本批新增 `writeDiag` 导出（`:1140`）却未登记该触发判定 | 按 `:1118` 形式补两行裁定（`shell-affinity` 的越档事实 / 理由 / 消解路径；`shell-backend` 的「导出既有函数、职责未增、323 < 400 ⇒ 不触发复核」判定），并把 `:1164` 移回 B10 表下 |
| 5 | Acceptance criteria / 可观测性 | 🟡 | 残留静默路径未登记：判据② 与容错表（`:926`）把「`totals` 缺失」当合法（该记录贡献 0 且**仍计为可解析记录**）⇒ 若换代只改桶名 / 层级（文件仍可 parse），生效读面会返回 **0**（非 `null`、且不满足诊断条件「两形态均不可用」`:950`）⇒ 调用方按 0 重基线，症状与本次事故同型（静默不增长）；而 O17（`:1298-1299`）断言「形态若再变…本批侧有诊断行与 `null` 安全侧」——对「可解析但无桶」类形态变更不成立 | 登记为 L-B10-4（「可解析记录 ≥ 1 但四桶全缺 ⇒ 归零静默」）或把诊断条件扩到该态；并把 O17 的断言限定为「路径 / 解析失效」类形态变更 |
| 6 | Clarity | 🔵 | 手段 12 ①（`:1534`）的 `grep -n "sumSessionTokens" shell-affinity.js`（实现 + 导出各 1 处）与设计自述的调用面不相容（`:931-934`：调用方按水位差累加 ⇒ 调用点在档内） ⇒ 期望计数会落空 | 改为「实现 / 导出各 ≥ 1 处；调用点不计入」 |
| 7 | Acceptance criteria | 🔵 | 「水位下降（会话记录被清理 / 备份跳过）⇒ 只重置基线、不扣好感」是 US-13 显式契约面（§2.2.13 增量语义 `:935`），TC-56…TC-67 无用例覆盖该态 | 补一条边界用例（先高水位 → 清掉记录 → 断言 `usage` 不减、基线重置） |
| 8 | Clarity | 🔵 | 诊断行条件（两形态均不可用）不区分「全新安装尚无任何会话数据」与「读面失效」——两者都会每进程发一条同文行（`:947`）⇒ 长期脱敏风险 | 行内加可区分字段（如目录是否存在），或在实现期注明该口径 |
| 9 | Clarity | 🔵 | AC23 只判「调用面零 diff」，而「`null` ⇒ 基线不变」是本设计的安全前提（L-B10-3 `:963`）；设计仅以行号引用既有实现（`:935`） | 在 §2.2.13 明写「读面返回 `null` ⇒ 调用方早退、基线保持」，使其可独立核查 |
| 10 | Numeric annotations | 🔵 | §2.3 B10 表 `:1142` 的同列两个数值不同基线：「预计改动量 +约 170 / −0」vs「末行数 1349」（1349 − 1282 = +67），与同档其它批次的（+193 / +252）单一口径形式不一致 | 统一口径（把可分离面 +67 写进「预计改动量」列，或注明 +约 170 的基线） |
| 11 | Doc hygiene | 🔵 | 本档头部未随本批（及 B09 / B12）同步：`:5`「关联需求档…（US-1…US-11、NFR-1…NFR-5）」、`:6-7`「关联批次」仅列 B06 / B07——而 B10 已把 US-13 写进 §一 回指表与 §3.1 回指口径 | 由收口批次（B12）或主 agent 同步为 US-1…US-15、NFR-1…NFR-7 并补 B09 / B10 / B12 关联批次指针 |

计数：🔴 0 / 🟡 5 / 🔵 6。
VERDICT: pass

### 轮次 2（评审子代理）

设计评审（B10 第 2 轮复审 + 新增范围面复核）——评审对象 = 批次档 §2.9（修正轮 1 十一条）+ §2.10 / §2.12（范围扩展轮八条落点）+ `docs/design/SHELL-UX.md` §2.1 L-1·L-2 / §2.2.13 / §2.2.6 注 S7 + B10 附注 / §2.3 B10 表 / §2.4 DD-34…DD-40 + DD-38 更正 + DD-50 / §2.5 C32–C35·O17–O19·L-B10 指针 / §2.6 U-14 与 B10 追认块 / §3.1 AC21–AC25 + 「AC23 判据细化」 / §3.2 TC-56–TC-67 + TC-67A + TC-67B / §3.3 手段 12 / 变更记录 + `docs/requirements/SHELL.md` US-13 与变更记录。

证据声明：三档全文亲读。**11 条修正 + 8 条范围扩展落点逐条实测在场，无一条落空**（消解期句 / 载具裁定 + 可复跑命令 + 夹具定义 + 驱动方式 / 副作用口径统一与发射点定死 / 两行 >300 体量裁定 + 单函数核对 / L-B10-4 与容错表行与 O17 限定 / grep 计数口径 / TC-67A / 诊断行 `dir=` 字段 / `null` 分支 + L-B10-5 / delta 单值口径 / 头部区间——后者实测已由 B09 修正轮 1 同步；基线重建条 / L-B10-5 改写 / AC23 + 判据细化 + AC25 / §2.3 表行与裁定 / DD-50 + DD-38 更正 / TC-67B / 需求档变更记录 2 行 / 设计档变更记录 4 行）。`.js` 文件与 `docs/TODO.md` 不在本轮声明范围 ⇒ 其中行号 / 行数 / 符号（`shell-affinity.js:68-83`·`:116-147`·`:125`·`:128`·`:292-305` / `shell-backend.js:311-322` / `docs/TODO.md:47`）本轮 **unverified**；批次档 §2/§3 的行宽自述未逐行复测（未做字符宽度测量）。项目规范档未在本轮声明 —— 以 Project Guide + 本评审判据为准（该限制已标注）。

| # | Category | Severity | Issue | Suggestion |
|---|----------|----------|-------|------------|
| 1 | Clarity / 判据可达性 | 🟡 | 新增的**基线重建分支**与既有 tick 守卫的关系未定死：设计 `SHELL-UX.md:963` 记「`:128` 守卫要求 `s2 !== null`」，而同档 `:1000`（L-B10-5 证据链）与批次档 `:168`（N-1）记「tick 守卫（`:128`）要求 `lastTokenSum !== null`」，批次档 `:207`（N-3）又按「守卫要求 `s2 !== null`」立论——若守卫为合取，`lastTokenSum === null` 的 tick 在守卫处即早退 ⇒ 字面照落的重建分支会成**死码**（TC-67B 才暴露）；且「改守卫行」是否算 AC23 判据细化③（`:1507`）的「该分支」hunk 白名单未说明 | 在 §2.2.13「基线重建」条与 AC23 判据细化③明写守卫处置（分支置于守卫之前 / 守卫拆分），并把该行改动纳入 hunk 白名单口径（不改语义） |
| 2 | Acceptance criteria / 载具 | 🟡 | 载具（§3.3 手段 12 ②，`:1656-1665`）为**单脚本单进程**，而诊断标记 `affinityDiagLogged` 口径 = 「每进程至多一条」（`:982`）⇒ 同一脚本内先跑失败态即把门关死：TC-67①（`:1605`）的 `dir=no` / `dir=yes` 两态无法各自命中，AC23「正常态 ⇒ 0 条」（`:1495`）也会**空过**（0 条来自标记已置位而非读面正常） | §3.3 手段 12 ② 注明逐例进程隔离（或清 `require.cache` + 复位档内标记），并把「0 条」断言限定在**新进程**内 |
| 3 | Clarity / 判据可达性 | 🟡 | 载具驱动契约的符号名与设计自身声明面对不上：`:1662` 写 `init({ getPetWindow: () => null, pet: <桩>, setQuitting(){}, APP_NAME })` + `handleAffinityView()`，而 §2.2.6（`:488`）声明的本档注入面 = `petSay` / `setPetState` / `getPetWindow` / `setQuitting`；调用面枚举（`:967` / `:1354` / `:1428`）用 `affinityView`（`.js` 不在本轮范围 ⇒ 二者孰为准 unverified） | 载具写法改用与 §2.2.6 同名键（`petSay` / `setPetState` / …）与同一符号名，或注明键 / 符号对应关系 |
| 4 | Doc hygiene | 🔵 | §2.3 B10 表两处自指行的**枚举**未随修正轮 2 同步：`:1212` 列 `DD-34…DD-40` / `TC-56–TC-67 + TC-67A`（缺 **DD-50** / **TC-67B**）、`:1211` 记需求档「变更记录 2 行」（现 4 行）——行数补正注（`:1230-1231`）已给新值，枚举按 D3 同改原则应同步 | 由本批或收口批次同步枚举（纯记账，无设计语义） |
| 5 | Doc hygiene / 指针 | 🔵 | 批次档落点指针两处不精确：§2.12 item 5（`:196`）记 `SHELL-UX.md:1297`（DD-38），实际 DD-38 = `:1298`；§2.10 `:222` 段边界自述「本行落笔后各绝对值 +1」，按本档实读需 **+2**（§3 = L224 / §4 = L249 / §5 = L251 / §6 = L253，共 253 行；`:220` 记 >300 行在 L229–L233，现为 L234–L238） | 主 agent 核 §3 豁免面时以实测边界为准（无设计语义） |
| 6 | Doc hygiene / 口径一致 | 🔵 | §2.5 C32（`:1351`）仍记样本档 **7827 B**（未标 as-of），而 §2.2.13「读面开销」（`:989`）已改为「随会话增长而变（落笔 7827 / 修正轮 1 实测 7191）」⇒ 同一事实两处口径不一 | C32 补 as-of 标注，或改指 §2.2.13 的变动口径 |
| 7 | Requirements / Document ownership | 🔵 | US-13 契约面「逐字未改」是修正轮 2 的有意裁定（批次档 `:212`），但**基线重建**落在调用面语义上：需求档 US-13 只写「既有「水位下降只重置基线」的重基线语义不变」（`SHELL.md:195`），未列「启动期 `null` ⇒ 重建基线」这一新态（现只存在于需求档变更记录 `:309-310`） | 收口 / 下一轮决定是否在 US-13 该句补一句设计回指注（不改契约语义） |

计数：🔴 0 / 🟡 3 / 🔵 4。
VERDICT: pass

## §4 评审裁决与实施启动（主 agent）

## §5 实施记录（eng-coder）

### 5.1 交付摘要（2026-09-17）

- **交付面**（实改 2 档、**零新增仓内文件**）：`shell-affinity.js` **305 → 354** 行 · `shell-backend.js` **322 → 323** 行；`git diff --numstat` = `57/8` · `1/0`。
- **落点（`shell-affinity.js`）**：① 头注释口径句（`:27-29`）与读面 JSDoc 同步为**口径 B + 双形态**；② 档内常量 `TOKEN_BUCKETS` + 一次性诊断标记 `affinityDiagLogged`（`:68-72`）；③ 读面重写（`:74-130`：`bucketSum` / `recordTotals` / `legacySessionTokens` / `sumSessionTokens`，含四步判据、逐条容错、判据④诊断发射）；④ **调用面基线重建分支 = 新增 1 行**（`:175`，落于既有守卫 `:176` 之前；守卫与既有累加 / 重基线块**逐字不动**）；⑤ 导出面 +1（`:345` `sumSessionTokens`）。
- **落点（`shell-backend.js`）**：`module.exports` 增 `writeDiag` **1 行**（`:314`；函数体 `:61-64` 既有——B09 复用同一行，**先落者建立**）。
- **载具**（仓外一次性开发期脚本，不入仓）：`%TEMP%\b10-affinity-stub.mjs`；命令 `node "%TEMP%\b10-affinity-stub.mjs"` ⇒ **7 用例 / 66 条断言全绿 / 退出码 0**（逐例独立进程，诊断三态各自新进程）；夹具根 `%TEMP%\b10-fix`（退出时清理）。
- **静态门**：`node --check shell-affinity.js` / `shell-backend.js` 全绿；行宽（不含行尾 CR）**0** 行超 300（最宽 143 / 228）；单档 354 ≤ 500；工作区 EOL 为 CRLF（仓 `.editorconfig` 声明 lf / 索引 LF，未引入混合）。
- **回归面**：`git diff --name-only` 中 `shell-pet-geometry.js` / `shell-pet-drag.js` **零 diff**（几何层未触碰）。

### 5.2 决策透明表

| # | 决策点 | 采用 | 依据 / 理由 | 备选与否决 |
|---|---|---|---|---|
| 1 | 基线重建分支落位 | 落于既有守卫 `:176` **之前**、形态 = 新增 1 行（行尾注释保持单行形态） | 设计档 §2.2.13「落位与守卫处置」；守卫为**合取** ⇒ 置其后成死码 | 守卫之后 / 守卫内（死码）；守卫拆句早退（跳过每 tick 广播 `:193`）——均按设计档否决 |
| 2 | 诊断发射点 | `sumSessionTokens()` **函数内**判据④分支，经 `backend.writeDiag` | 设计档「诊断行」：判定与诊断一一对应；`require('./shell-backend.js')` 边既有 ⇒ 零新增依赖 | 放调用方（与判定不对应）；console-only（打包版不可见） |
| 3 | `dir=` 取值 | `fs.existsSync(dir)`（**目录存在性**） | 设计档「`dir=` 字段」= 区分「全新安装尚无数据」与「读面失效」 | 以 `parseable>0` 取值（与设计语义不符） |
| 4 | 四桶实现形态 | 常量数组 `TOKEN_BUCKETS` + `bucketSum()` 循环求和 | AC24 静态判据要求四桶标识符逐条在场；口径句单点便于换代同步 | 内联四个 `\|\| 0` 相加（换代时需改多处、易漂移） |
| 5 | 旧面实现 | 独立函数 `legacySessionTokens()`（原逻辑语义保留、仅口径 B 化） | 「只搬不改」+ 口径 B 落地；判据③ 分支自足可读 | 与目录面合并进单函数（判据③ 与容错面混杂） |
| 6 | 旧面无有效会话时返回 `0`（非 `null`） | **保持现状语义**（未改） | **设计只定义「旧面也不可用 ⇒ null」（判据④），未定义旧面「可用判据」**；改动 = 语义面 ⇒ 未自决，随报告提请（见 5.4） | 返回 `null`（与判据④ 的自陈理由更一致）——须设计侧裁定后另行落地 |
| 7 | 注释路径写法 | 写**字面访问链** `<文件对象>.record.rows.tokenUsage.val.totals` | 本缺陷成因即路径/形状写错，简写留歧义；纯形态零语义 | 沿设计档简写 `record.rows….totals`（评审 🔵 提出，已改） |

### 5.3 审计与代码评审轮次与终态

- **内部偏离审计**（`explore` 只读子代理，**1 轮**）：**无 🔴 / 无 🟡 代码面偏离**；「越出批准文件清单的改动」= **无**；2 项 🔵（手段 12① 的 `recursive` 字面判据 · §2.3 表「实施期实测回填」未填）。
- **内部代码评审**（advisor `code`，**1 轮**）：**VERDICT = pass**（🔴 0 / 🟡 3 / 🔵 3）；评审侧独立复核了活样本嵌套与四桶值（`…sessions/session-f80ce725-….json` ⇒ 49604）与读面非递归。
- **裁决与 fix round（1 轮，已收敛）**：
  - 🔵「JSDoc 路径与代码访问链不同形」⇒ **Fixed**：`shell-affinity.js:81` 注释改为字面链（语义零变化，载具复跑 66/0）。
  - 🔵「诊断发射无防御」⇒ **Not an issue**（证据：`shell-backend.js:61-64` 的 `writeDiag` 为全函数——`console.log` 与 `logStream.write` 各在判空 / try 内，任意字符串入参不抛；且该导出与本批同批同仓 ⇒ 设想面不可达）。
  - 🟡 三条（US-13「内置 Harness = `0.1.0-rc.6`」与仓库现状相抵 · 手段 12① 的 `recursive` 字面判据 · 旧面可用性判据未定义）⇒ **均属文档 / 语义面，不在本角色写域**，随报告上交（未自决、未改代码）。
  - **终态 = `clean`**（0 🔴 未决；2 项 🔵 已收敛为 Fixed / Not an issue；🟡 三条为报告项）。
- **fix round 边界**：仅 1 处注释改写（`:81`），无其它修正；未夹带新语义。

### 5.4 残余风险 / 未覆盖项（如实）

- **AC21 静态判据「`recursive` 0 处」按字面不可达**：全档实测 **3 处**，均为本批**受冻结的既有**调用（`:63` `fs.mkdirSync(…, { recursive: true })` · `:268` / `:300` `fs.rmSync(…, { recursive: true })`）；**读面零递归**由结构面（`:113` `fs.readdirSync(dir)` 不带选项）+ 行为面（载具 `static` / `read` 用例）双重佐证。判据写法归设计层（手段 12① 与 AC21 行）。
- **「旧面可用性判据」未定义**：`legacySessionTokens()` 于旧文件可解析但无有效会话（`tables.sessions` 缺失 / 空）时返回 **`0`**——与 L-B10-4（四桶全缺 ⇒ 静默归零）同族，而 `0` 是设计自陈的危险值（`0` 会把水位降到 0）。请设计侧裁定：补「旧面可用判据」或登记 L-B10-4 的旧面对照条。
- **真机人工项未覆盖**（设计档 §3.3「B10 人工项」）：好感度条 / 兑换屋余额按口径 B 的**增长观感**；TC-62 的「目录权限不可读」需 ACL 手工构造（Windows）——本轮未构造，**不伪报机检**。
- **AC23 判据细化③ 的 `git diff` hunk 白名单（机检②）** 未由本角色复跑（实现者不引 diff 作自证；形态面已核：调用面新增行 = 1、落位正确、守卫与既有块未出现在 + / − 面）。
- **并发在途改动（非本角色所改，如实披露）**：实施期内工作区出现 `package.json`（+`bundle:refresh` / `bundle:check`；mtime 本地 14:25:48）与 `dsh-bundle/package.json` + `dsh-bundle/package-lock.json`（`@deepseek-ai/dsh` `0.1.0-rc.6` → `0.1.5-rc.1`；mtime 本地 14:29:22）的改动——本角色仅两次写（14:21:24 `shell-backend.js` / 14:29:45 `shell-affinity.js`），**未触碰上述三档**。该并发改动使 US-13「当前内置 Harness = `0.1.0-rc.6`」句与仓库现状相抵，并关系到消解期到期条件第一分句（内置 bundle ≥ 0.1.5）——提请主 agent 归口核对（疑似 B08 面）。
- **§2.3 B10 表末行数「实施期实测回填」**：实测 = `shell-affinity.js` **354** · `shell-backend.js` **323**（回填归设计层写权）。

**本段写入状态**：§5 **已写入**（经 `batch_segment`，无路径参数）。

## §6 验收核销（主 agent）

**验收依据** = 本档 §2.4 AC21–AC25 + 设计档 §3.1 逐条 + **主 agent 独立实测**（一手证据，不采信自报）。

### 6.1 AC 逐条（证据 = 主 agent 亲跑 / 亲读）

| AC | 判据 | 证据（主 agent 一手） | 结论 |
|---|---|---|---|
| AC21 | per-record 读面生效（目录段 + 导出 + **读面零递归**） | 载具 `static` **16** 断言亲跑通过；亲读 `fs.readdirSync(dir)` 不带选项（读面内零递归；全档 3 处既有 `recursive` 在冻结函数内、判据按面限定） | ✅ |
| AC22 | 旧面兜底 + 并存判据 | 载具 TC-59（目录空 ⇒ 旧面 100）· TC-64（目录缺 ⇒ 旧面 25）· TC-60（并存 ⇒ 目录面专读、**≠ 两形态之和 6000**——防双计） | ✅ |
| AC23 | 失败语义不回退 + `null` 分支 + 调用面 | TC-62（**两形态均缺 ⇒ `null`，`null ≠ 0`**）· 诊断行两态各 1 条 / 正常态 0 条；**AC23③ 亲读 `git diff`**：`+ if (lastTokenSum === null && s2 !== null) …` 一行落于守卫之前，**守卫行 `if (s2 !== null && lastTokenSum !== null) {` 出现在未改上下文面**（不在 `+`/`−` 面） | ✅ |
| AC24 | 计费口径 B | 四桶标识符逐条在场 · TC-65（`last.buckets ≠ totals` ⇒ 取 `totals`）· TC-56 样本 `9666+37504+0+2434 = 49604` | ✅ |
| AC25 | 聚合口径 + 水位语义 | TC-58（三条之和 51104）· TC-66（幂等 / 启动不重复计入）· TC-67A（水位转 `null` 不扣好感、不改基线 ⇒ 恢复后只累加 1000 ⇒ 8000）· **TC-67B（启动期 `null` ⇒ 首个非 `null` tick 只重建基线 ⇒ 后续累加恢复 4000）** | ✅ |
| **TC-67C**（修正轮 4–6 派生） | 旧面可解析但**无有效会话条目** ⇒ `null`（非 `0`） | 载具 `tc-67c` **11 断言亲跑通过**（四构造全 ⇒ `null`）+ **正对照**（有效条目 ⇒ 15 · 混合 ⇒ 30）——**证不误拒可用态**；亲读 `shell-affinity.js:103` `return valid >= 1 ? sum : null;` | ✅ |

### 6.2 交付终态实测（主 agent 一手）

- `shell-affinity.js` **356** 行（305 → 354 → 356）· 超宽 **0** · 最长 143 · `node --check` 通过 · 文件头标准形未破。
- `shell-backend.js` **323** 行（322 → 323：`module.exports` +1 行 `writeDiag`）。
- 载具汇总 **77 / 0**（8 用例，逐例独立进程）——**主 agent 亲跑**。
- 既有 `tests/`（开发期工具、非仓门禁）：26 pass / 0 fail（旁证）。
- 几何面：本批写域仅 `shell-affinity.js` / `shell-backend.js`，**未触** `shell-pet-geometry.js` / `shell-pet-drag.js`。

### 6.3 残余与处置（不藏）

1. **旧面「有效条目在场但 `totals = {}`」仍返回 `0`**（**L-B10-4 同族**）：设计明示本批不扩诊断条件 / 不重新分类 ⇒ **保留**；该态下 L-B10-2 的重复计入面仍在（**窄面、非真实态**）。
2. **组合态未单断言**（目录在但空 / 全损坏 + 旧面无效）：路径同一（`parseable === 0` ⇒ 旧面 ⇒ `null`），两侧已各自覆盖。
3. **真机（打包版）未复跑**：载具为假 electron 桩 + 夹具盘；配对真目录用例（TC-57）仍绿。
4. **载具退役裁定（主 agent）**：`%TEMP%\b10-affinity-stub.mjs` = **开发期工具 ⇒ 退役**（不转集成资产）——本批验收已闭环，该载具**无 CI 落点**（仓门禁现状 0/3），保留 = 维护存量测试库存；其业务可观察面由本节实测记录承载；载具在**仓外**（`%TEMP%`）⇒ 无仓内删除动作。
5. **设计档 §2.3 行数回填（354 → 356）**：归设计层（登记在案）。

### 6.4 台账

- **T14**（好感度读面疑长期失效）→ **已核销**（本批修复 + 本节证据）。
- **T27**（累加在启动期 `null` 后本进程永久暂停）→ **已核销**（基线重建分支落地 + TC-67B 亲跑）。
- 归档动作（移入 `docs/TODO-archive.md` + 活文件计数同步）由主 agent 在核销轮执行。
