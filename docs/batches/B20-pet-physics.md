# B20 批次档 —— 桌宠物理手感 + 位移仲裁层（外部项目移植 B 面 · 含 O10 合并）

> 六段制 · append-only · 一段一作者：§1 主 agent / §2 eng-designer / §3 设计评审 / §4 主 agent / §5 eng-coder / §6 主 agent。
> 本档 = **R15（外部项目能力移植）的 B 面** + **O10 裁定面**（链接管位移 ⇒ 与 B 面合并立批）。
> 序 = A 动画链（B18 已收口）→ D1 工作状态（B19 设计就绪待评审）→ **B 物理（本批）** → D2 → E → C。

## §1 立案（主 agent）

### 1.1 目标（一句话）

给桌宠装上**物理手感**：拖拽跟手具备阻尼弹簧质感、松手后按重力抛物落地并**碰壁 / 触地反弹**、落地受摩擦逐渐停下、撞击瞬间有 **Q 弹挤压**——且**不破坏**既有拖拽跟手与几何纪律。

### 1.2 范围（做 / 不做）

**做**：
- **物理量参数化**（承样本规格）：`gravity` / `restitution` / `groundFriction` / `ceilingBounce` / `throwPower`（弹簧系数 K/C + 甩抛初速与软上限）。
- **甩抛与落地**：松手速度 → 抛物线 → 碰壁 / 触地反弹 → 摩擦衰减 → 静止（回到既有停泊纪律）。
- **Q 弹挤压**：撞击瞬间的形变反馈（渲染面，不写位置）。
- **位移仲裁层**（**本批核心**，承 O10）：把既有三个位移来源（**拖拽** / **散步** / **物理**）收进**单写者**仲裁——明确优先级、互斥与交接（含 O10 的「链是否需要接管位移」的落点判定）。
- **开关与默认值**：物理面可关（默认值待裁定，见 1.5）。

**不做（本批出批）**：
- 多实例 / pet pack（E 面）· 余额/碎碎念/对话（D2 面）· 素材生成链（C 面）· 工作状态联动（D1 面，B19 独立批）。
- **不改**：`shell-pet-geometry.js` / `shell-pet-drag.js`（**几何冻结面**，硬门禁）· `assets/**` 素材本体 · `pet-chain-core.js` / `pet-chain.js`（B18 链引擎）· B03 的停泊/尺寸纪律与 13 轮修正结论。

### 1.3 现状实测（勘察，2026-09-18）

| 面 | 实测事实 | 证据 |
|---|---|---|
| **样本物理规格** | `physics`：`gravity 1400`（px/s²，0 = 无重力）· `restitution 0.78`（0~1，墙/地共用）· `groundFriction 2.5`（/s）· `ceilingBounce true`（顶是否反弹）· `throwPower`（弹簧跟手 K/C + 甩抛初速与软上限 3600 整体 ×p） | `samples/dsh-pet/dsh-pet/assets/config.jsonc:80-94`（逐项注释） |
| **样本实现（参考）** | `src/shared/physics.ts` · `src/shared/motion.ts`（**TypeScript**；本仓为 CJS JS ⇒ 按规格重写或适配，**不得引入 TS 工具链**） | 文件在场（路径见上）；许可 = 代码 MIT（**须署名**，三处署名已在位）/ 素材禁商用（用户裁定本仓不商用） |
| **既有位移写者（三处）** | ① **拖拽**：`shell-pet-drag.js` 每 **8 ms** tick 读光标 → 推导目标 → `setPosition`（同目标去重；**不做工作区钳制**）② **散步**：`shell-pet.js:260-274` `scheduleWander()` / `doWander()`（定时器驱动）③ **物理**：本批新增（尚不存在） | `shell-pet-drag.js:21-26` / `:56` · `shell-pet.js:260-274` |
| **⚠️ 几何纪律（硬冲突）** | `shell-pet-geometry.js` 头注释明写：位置/尺寸校正**「只在离散停泊事件调用（松手 / 散步段末 / 显示器事件 / 找回 / 退出前），不得在 tick 内调用」** | `shell-pet-geometry.js:170` 原句（另有 `:26` 尺寸写入纪律 = `setBounds` 尺寸专用形态） |
| **B03 的回归面** | 几何面 13 轮修正（跨屏/混合 DPI 停泊与尺寸）；其 dev 桩 `.thincoder/b03-pet-calibrate-stub.mjs` **已损坏**（T29：硬绑 `main.js`、0 断言执行）⇒ 现由**几何面零 diff** 承担该回归面 | 台账 T9（三屏实机未验）/ T29（桩退役路径）· B18 §1.4-2 |
| **O10 的裁定** | 「位移接管（`weights.move`）= **与 B 面合并**」⇒ 本批须先**定位移仲裁层**再判链是否接管位移；依据 = `docs/batches/B18-pet-animation-chain.md` §1.9 | 台账 R15 行（用户 2026-09-17 裁定）· B18 §1.9 |

### 1.4 硬约束（不得偏离）

1. **几何冻结**：`shell-pet-geometry.js` / `shell-pet-drag.js` **不得出现在 `git diff`**（硬门禁；承 B18 同款）。
2. **位移仲裁必须显式成文**：新物理写者与既有两写者的**优先级 / 互斥 / 交接**必须逐条落档（含「谁在什么时刻拥有位置写权」的状态机），并**证明不与几何档的「离散停泊」纪律相抵**——若物理需要在 tick 内写位置，必须由仲裁层**独占该写权**且几何校正仍在离散点发生。
3. **既有手感零回退**：拖拽跟手的既有观感（B03 调定）不得变差；物理只在**松手后**接管（拖拽中仍按既有 8 ms 跟手）。
4. **零新依赖**：不引入 npm 包（自写积分/碰撞）；不改 `dependencies`；新增渲染档须同步 `build.files`。
5. **默认行为保守**：物理对手感的影响是**用户可感知变更** ⇒ 默认值须显式裁定（见 1.5 U-1）。
6. **行宽 ≤300（不含行尾 CR）· 代码档 ≤500 行 · 文件头标准形**。
7. **许可面沿用 B18**：代码 MIT 须署名（已在位，不得改写）；素材禁商用。

### 1.5 待决项（须裁定后才能定稿设计）

| # | 待决 | 备选 | 影响面 |
|---|---|---|---|
| U-1 | **默认开关** | ① 默认**开**（手感是"活感"核心）② 默认关（保守，同 D1 的 U-1） | 首次体验 / 与既有手感的关系 |
| U-2 | **参数取值** | ① 照搬样本（1400 / 0.78 / 2.5 / 3600）② 本仓调（如桌面尺寸下更"轻") | 手感观感（需实机感受） |
| U-3 | **实现路径** | ① 按样本规格**重写**（JS、无 TS）② 逐行适配样本 `physics.ts`（MIT，须署名） | 工作量 / 许可与维护面 |
| U-4 | **散步去留** | ① 物理接管后**去掉** `doWander` 的位移（只留链的节奏）② **保留**散步（物理只管拖拽后） | 与 B18 链 `pet-chain-move` 的接线 |
| U-5 | **拖动跟手是否改弹簧** | ① **不改**（既有 8 ms 跟手保持）② 改为阻尼弹簧（手感更"重"但有破坏 B03 结论的风险） | ⚠️ 与 1.4-3 硬约束直接相关 |
| U-6 | **O10 是否本批接管位移** | ① 只建仲裁层、**链仍不接管**（`weights.move` 保持 0）② 本批即让链接管 | 与 B18/B19 的 `weights.move` 口径 |

### 1.6 关联台账与需求档

- **台账**：`docs/TODO.md` §一 **R15**（外部项目能力移植）——本批 = 其 **B 面** + **O10** 合并面；**T9**（三屏实机）、**T29**（B03 桩退役）与本批的回归面相关。
- **需求档落点**：`docs/requirements/PET.md`（§三 US / §四 NFR）——由 eng-designer 起草（与 US-15…19、US-20…23 同档续编）。
- **设计档落点**：`docs/design/PET-MULTIMONITOR.md`（几何/停泊主题档，B03 主题）＋ `docs/design/PET-ANIMATION.md`（链与位移主题档，B18 主题）——**落点由设计者按「一主题一档」判定并说明**（位移仲裁层属二者交叉，须先跑 B14 的文档地图检查）。

### 1.7 变更记录

| 日期 | 变更点 |
|---|---|
| 2026-09-18 | 建档（B20 立案）：目标 / 范围 / 勘察实测（样本规格 + 既有三写者 + **几何纪律硬冲突** + O10 裁定面）/ 硬约束七条 / 待决 U-1…U-6。依据 = R15 的 B 面 + O10 合并裁定 + 用户 2026-09-17「都做，你安排次序」/ 2026-09-18「继续执行」。 |

---

## §2 本批任务书（eng-designer）

<!-- 由 eng-designer 填 -->

---

### 2.1 本批覆盖的需求条目（三方同源之一）

需求档 = `docs/requirements/PET.md`（新增 5 条 US + 5 条 NFR，另 4 处修订 / 限定注记）；设计档 = `docs/design/PET-MOVEMENT.md`（新建，主题档）。

| 需求条目 | 一句话 | 设计档验收标准 |
|---|---|---|
| US-24 甩抛与飞行 | 松手初速 → 抛物线 → 碰壁 / 触地反弹 → 摩擦衰减 → 静止 | AC1、AC2、AC3 |
| US-25 撞击形变反馈（Q 弹） | 落地瞬间「压扁—回弹」，力度随冲击速度 | AC4 |
| US-26 位移单写者仲裁 | 拖拽 > 物理 > 散步；单写者 + 交接 + 互斥 | AC5、AC6、AC7 |
| US-27 物理开关与默认值 | 托盘 checkbox + `settings.json` 顶层布尔；默认值待裁 | AC8 |
| US-28 停下即停泊（含降级面） | 停下 = 离散停泊；物理不可用 ⇒ 回退今天 | AC9、AC10 |
| NFR-18 物理写入纪律与开销 | 每 tick ≤1 次写 + 同目标去重 + tick 内零落盘零尺寸 + 关闭态零开销 | AC10 |
| NFR-19 静止收敛与有界 | ≤4 s 静止 + 只停一次 + 5 s 硬上界 + 轨迹界内 | AC11 |
| NFR-20 可测性 | 纯函数核心 + 双环境导出 + 桩测装载真实实现 | AC12 |
| NFR-21 零回退与规范 | 冻结面零 diff + 零新依赖 + 行宽行数文件头 + 署名 | AC13 |
| NFR-22 与既有机制的交互 | O10 不接管；B19 / B03 口径零改动 | AC14 |
| 修订注记（US-3 / US-12 / NFR-1 / NFR-2） | 松手后由物理接管（B01 期条文的显式语义修订，**待用户追认 = U-7**）；物理关闭态逐字照旧 | AC9、AC13 |

### 2.2 明确出批（本批不做，不写代码）

- 阻尼弹簧拖拽（U-5 ①；冻结面）· 跨屏飞行（O-1）· 多宠物碰撞（`petCollision`）· 用户可调参数（只给开关）· 飞行中位置钳制 · 撞墙 / 撞顶形变 · 点击 / 喂食挤压；
- **任何**对 `shell-pet-geometry.js` / `shell-pet-drag.js` / `pet-chain.js` / `pet-chain-core.js` / `assets/**` 的改动；
- B03 / B18 / B19 各档面（只读）· 本批不新增 `tests/**` 档 · T29（B03 桩损坏）不修。

### 2.3 实现写域（file 级权威表；超声明按实披露）

| # | 文件 | 动作 | 说明（细节 = 设计档 §2.2 / §2.3） |
|---|---|---|---|
| 1 | `pet-physics-core.js` | 新建 | 纯函数核心（参数表 / trimTrail / estimateReleaseVelocity / throwStep / 挤压曲线 / decideOwnership / armGate）；**零 electron / 零 fs / 零定时器**；双环境导出 |
| 2 | `shell-pet-physics.js` | 新建 | 域实现（采样 / 飞行循环 16 ms / armGate 消费 / 停泊收口 / 日志 / IPC → 挤压）；只读几何既有 helper |
| 3 | `shell-pet.js` | 改 | `doWander` 守卫加 `|| physics.isFlying()`（1 处）；`init` 注入点；`destroyPetWindow` / `clearPetTimers` 路径停物理 |
| 4 | `main.js` | 改 | require + `physics.init(deps)`（模块接线区）+ 本模块三条 `screen.on`（**`whenReady` 内、几何三条之前**；E7，见 §2.8）+ `before-quit` 停点 |
| 5 | `shell-ipc.js` | 改 | 增注册本模块自有的 `pet-drag-start` / `pet-drag-end` 监听器（**不改既有绑定行**） |
| 6 | `shell-tray.js` | 改 | 「设置」子菜单 +1 checkbox（标签 = `open-1` 待定，实施按推荐 ①）+ setter |
| 7 | `shell-settings.js` | 改 | `DEFAULT_SETTINGS` + `petPhysicsEnabled`（默认值 = U-1 裁定；未裁前按推荐 `false`） |
| 8 | `pet.html` | 改 | 插入 `#pet-squash` 包裹层（包 `#pet-stage` + `#pet`）+ keyframes + reduce-motion 分支 |
| 9 | `pet.js` | 改 | `onPhysicsSquash` → 逐帧 `squashScale` 播放（220 ms） |
| 10 | `pet-preload.js` | 改 | 暴露 `onPhysicsSquash(cb)` |
| 11 | `package.json` | 改 | `build.files` 增列两个新源档（白名单制，**必需**） |
| 12 | `.thincoder/b20-pet-physics-stub.mjs` | 新建 | 开发期桩测（gitignored 非交付物）；装载真实核心，末行 `pass/total PASS`，`total ≥ 40` |

**零 diff 面（机检，硬门禁）**：`shell-pet-geometry.js`（437 行）· `shell-pet-drag.js`（239）· `pet-chain.js`（240）· `pet-chain-core.js`（198）· `assets/**` · `package.json` 的 `dependencies` / `devDependencies` 段。
**贴线档**：本批写域内无 ≥480 行档（最大 `shell-pet.js` 426 → ≈434）⇒ 无需拆分计划。

### 2.4 验收标准（AC → 取证；判据详见设计档 §3.1）

| AC | 判据摘要 | 取证 |
|---|---|---|
| AC1 | 轨迹界内 + 反弹系数 0.78±0.02 + 摩擦衰减 + ≤4 s 静止 | 桩测 |
| AC2 | `ceilingBounce` 真 / 假两态（夹回反弹 / 越界不夹不弹） | 桩测 |
| AC3 | 估速五类 null 分支 + 平滑甩动方向与量级带 + 软上限 ≤3600×p | 桩测（扫描） |
| AC4 | `landingSquash` 单调不增（0.8…0.55）+ `squashScale` 端点 / 上界（≤1.12）+ 渲染面在场 | 桩测 + 静态（+ 目视） |
| AC5 | `decideOwnership` 8 组合 + tick 内「先判 `getPetDrag` 后写」+ 抢占后零写入 | 桩测 + 静态 + 日志面 |
| AC6 | armGate G1–G9 逐条置假 ⇒ 逐条 reason + 零写入；全通过 ⇒ `phys-arm` 的 `pos` == `drag-end` 的 `pos` | 桩测 + 日志面 |
| AC7 | tick 体内零 `petSavePos` / `petCalibrateSize` / `getSize`；`clearInterval` 先于停泊序列；停泊点 `geom-fix reason=physics-rest` 零行、`pos-save` ≤1 行、尺寸型 `geom-fix` ≤1 行 | 静态 + 日志面 |
| AC8 | 托盘项在场且取值一致 + `settings.json` 键落盘读回 + 默认值 == 裁定值 + 关闭零日志 | 静态 + 实机 |
| AC9 | 降级 10 行逐行行为与日志 reason 一致；关闭态与今天逐位一致 | 桩测 + 静态 + 日志面 |
| AC10 | 每 tick `setPosition` ≤1 且同目标去重；飞行期取屏 0 次；关闭态零定时器 | 静态 + 桩测 |
| AC11 | 8 方向 × {500,1200,2400,3600} × 4 起点 ⇒ 全部 ≤4 s `atRest`；`gravity=0` ⇒ 5 s 上界收口；最坏值记 §5 | 桩测（扫描） |
| AC12 | 核心档零 `electron` / 零 `fs` + 双环境导出 + 桩测末行 PASS（`total ≥ 40`） | 命令亲跑 |
| AC13 | 四档 + `assets/**` 零 diff；依赖段零 diff；`build.files` +2；行宽 / 行数 / 文件头 | 命令 + 静态 |
| AC14 | `pool.json`（`weights.move = 0`）· `pet-chain*.js` · `PET-MULTIMONITOR.md` 零 diff | 静态 |

用例表：TC-1…TC-29（正常 / 边界 / 异常；含撞击 / 反弹 / 静止收敛与「与拖拽 / 散步的交接」两类专项）= 设计档 §3.2。

### 2.5 实施前置（启动前必须闭合）

1. **待用户裁定（不得由实施者自裁）**：U-1 默认开关（推荐 ② 默认关）· U-2 参数取值（推荐 ① 照搬样本）· U-3 实现路径（推荐 ① 按规格重写）· U-4 散步去留（推荐 ② 保留）· U-5 拖动跟手（推荐 ① 不改，② 与硬门禁相抵）· U-6 O10 接管（推荐 ① 不接管）。
2. **本设计新增的三项（源 = 设计期勘察，须一并裁定）**：**U-7** US-3 / NFR-2 的语义追认（推荐 ① 追认——否则「松手后接管」不成立、本批不成立）· **U-8** 飞行边界口径（推荐 ① 当前屏工作区）· **U-9** reduced-motion 下关闭挤压（推荐 ① 关闭）。
3. **open（UI / 交互，实施前定案）**：`open-1` 托盘项标签文案（推荐 ①「甩抛物理手感」）· `open-2` 专注模式处置（推荐 ① 置灰）· `open-3` 撞墙形变（本批不做）。
4. **设计评审**：发起权在用户（本批设计就绪待评审）；评审在途不改本档与被审设计档（D5）。
5. **实施前置技术项**：`package.json` 的 `build.files` 必须先登记两个新源档（否则打包缺档）；桩测须能装载真实核心（零 electron）；基线 = 上一批收口点的四档零 diff。
6. **回归面**：本批回归判据 = **几何面零 diff**（B03 dev 桩损坏 = T29，本批不修；O-7）。

### 2.6 三方条目一致声明

**批次档 §2 本批条目（US-24…US-28 / NFR-18…NFR-22）= 设计档 `docs/design/PET-MOVEMENT.md` §3.1 验收标准回指的条目 = 需求档 `docs/requirements/PET.md` §三 / §四 的条目**——三处同源，计数一致：

- US：**5** 条（24…28）· NFR：**5** 条（18…22）· AC：**14** 条（AC1…AC14）· TC：**29** 条（TC-1…TC-29）· DD：**14** 条（DD-1…DD-14）· 选型组：**7** 组（A–G）· U：**9** 项（U-1…U-9）· open：**3** 项 · 观察项：**10** 项（O-1…O-10）。
- 需求档计数：US 23 → **28**；NFR 17 → **22**（含本批 4 处修订 / 限定注记，类别已在需求档变更记录标明）。

### 2.7 本段修正记录（eng-designer）

| 日期 | 修正点 |
|---|---|
| 2026-09-18 | 建档（本批任务书）：覆盖条目 / 出批 / 实现写域 12 项 / 零 diff 面 / AC1–AC14 / 前置（含**本设计新增的 U-7…U-9** 与 open-1…open-3）/ 三方一致声明与计数。 |

#### 2.8 实施细则（口径补充，2026-09-18）

- **`main.js` 的调用点（E7 约束）**：本模块的三条 `screen.on`（`display-added` / `display-removed` / `display-metrics-changed`）**必须在 `whenReady` 内、几何三条 `screen.on` 紧邻之前**注册——`screen` 模块只能 `app` ready 之后使用（证据 E7，见 `docs/design/PET-MULTIMONITOR.md` §2.2）。
- `physics.init(deps)` 本身可在模块接线区调用（只保存 deps + 注册 `ipcMain` 监听器，不触 `screen`）；判据 = 监听器执行顺序（日志 `phys-stop reason=display-change` 早于 `geom tag=display`）。
- 本细则为对 §2.3 第 4 行与设计档 §2.2.1 / §2.2.2 交接 ⑥ / §2.2.10 第 5 行的**同源收窄**（非语义变更）；设计档已同批更新。

#### 2.9 修正轮：起飞阈值口径（2026-09-18——**本段现行权威值**）

> **位次声明**：§2 为 append-only 段（前文行不可回改）⇒ **§2.1 / §2.4 / §2.5 / §2.6 中与本节冲突的计数与判据摘要以本节为准**（D3「计数与枚举同改」由本节完整重述承担）。
> **依据** = 用户裁定 2026-09-18 原话：「我觉得可以判断一下用户的鼠标移动速度，很慢的话就急停，很快的话就扔出去，有可能实现吗？…之前的约束有必要调整哦」。
> **本轮范围 = 只此一件**（阈值判别进物理模型与契约）；**零夹带**：飞行边界（U-8）· Q 弹面（U-9 / open-3）· 拖动跟手（U-5 已裁「不改」）· O10 接管口径（U-6）**均未动**。

**一、写域（取代 §2.3 的结论；表体不变）**：**12 项零增零减**（阈值 = 核心档内**一个常量** + **一个门函数**，不新增文件、不出批文件）。§2.2「明确出批」不变。

**二、覆盖条目与 AC 回指（取代 §2.1 相应行的 AC 列）**：

| 需求条目 | 一句话 | 设计档验收标准（回指） |
|---|---|---|
| US-24 甩抛与飞行 | 释放初速 ≥ 阈值 `T` ⇒ 起飞（抛物线 → 碰壁 / 触地反弹 → 摩擦衰减 → 静止）；`v < T` ⇒ 不起飞（缓放即停） | AC1、AC2、AC3、**AC15、AC16、AC17、AC18** |
| US-26 位移单写者仲裁 | 拖拽 > 物理 > 散步；交接② 以「`v ≥ T`」为条件 | AC5、AC6、AC7、**AC15** |
| 修订 / 限定注记（**5 处** = US-3 / US-12 / NFR-1 / NFR-2 + **US-26 口径②**） | **物理开启且释放速度 ≥ `T`** 时由物理接管；`v < T` 时逐字照旧（**待用户追认 = U-7**）；物理关闭态逐字照旧 | AC9、AC13、**AC15** |

（US-25 / US-27 / US-28 / NFR-18…NFR-22 的回指**不变** = §2.1 原行。）

**三、AC 增量与判据修正（取代 §2.4 相应行）**：

| AC | 判据摘要 |
|---|---|
| AC3（改） | 估速 **四类** null 分支（空 / 过期 / 太短 / 纯抖动；**低速不再判 null**）+ 平滑甩动方向与量级带 + 软上限 ≤ 3600×p |
| AC6（改） | armGate **G1–G10**（10 条）逐条置假 ⇒ 逐条 reason + 零写入；全通过 ⇒ `phys-arm` 的 `pos` == `drag-end` 的 `pos` |
| AC15（新） | `v < T` ⇒ `phys-stop reason=below-threshold` 在场 + 物理面零写入 + `drag-end` 后位置至多 1 次变动（**US-3 / NFR-2 逐字照旧**） |
| AC16（新） | `v ≥ T` ⇒ `phys-arm` + 按设计档 §2.2.4 步进（反弹 0.78±0.02 / 摩擦 / ≤4 s 静止 / 界内） |
| AC17（新） | 阈值常量**定义处恰 1 处**（机检 = 定义命中数 == 1）+ 量纲 px/s + 默认值 == 设计档 §2.2.3 表值 + 「待实机标定 / 标定路径 / 到期条件」在场 |
| AC18（新） | 边界两支：`v = T` ⇒ 起飞（**闭区间**）；`v = T − 1 px/s` ⇒ 不起飞（`below-threshold`） |

用例表：**TC-1…TC-34**（新增 TC-30 缓放 / TC-31 快甩 / TC-32 常量定义扫描 / TC-33 恰在阈上 / TC-34 恰在阈下；TC-10 / TC-14 / TC-19 判据同步改）。

**四、前置（取代 §2.5 第 1、2 项的 U 列表）**：U-1…U-6 不变（推荐不变）；**U-7 重写** = 「**缓放即停 / 快甩抛出**」阈值口径的追认（依据 = 用户 2026-09-18 原话；推荐 ① 追认）；U-8 / U-9 不变；**U-10（新增）** = 阈值 `T` 的默认取值与标定（推荐 ① 照搬样本值 500 px/s + 「待实机标定」+ 到期条件）；open-1…open-3 不变。

**五、阈值 `T`（判据面；权威句 = 设计档 §2.2.12）**：量纲 = px/s；默认值 = **500**（逐字照搬样本 `DEAD_ZONE_SPEED`，承 U-2 ①）；判据 = `v ≥ T`（**闭区间**；`v` = 抛掷所用初速的幅值，已含软上限与 `throwPower`）；**本仓无实机标定数据**（T9 未验）⇒ 声明「**可调单一常量 + 待实机标定**」，到期条件 = 用户实机感受一次（缓放不飞 / 中速不飞 / 快甩飞）后的裁定轮。

**六、计数与枚举（取代 §2.6 全段）**：

- US：**5** 条（24…28）· NFR：**5** 条（18…22）· AC：**18** 条（AC1…AC18）。
- TC：**34** 条（TC-1…TC-34）· DD：**15** 条（DD-1…DD-15；**DD-15 = 阈值口径**）· 选型组：**8** 组（A–H；**H 组 = 阈值口径三候选**）。
- U：**10** 项（U-1…U-10）· open：**3** 项 · 观察项：**11** 项（O-1…O-11；**O-11 = 样本注释与实现不符**）。
- armGate：**10** 条（G1–G10）· `phys-stop` reason 词表：**13** 个（含新增 `below-threshold`）。
- 需求档计数：US 23 → **28**；NFR 17 → **22**；本批修订 / 限定注记 **5** 处（4 → 5，新增 US-26）。

**七、三方一致（同源声明）**：本节条目 = 设计档 `docs/design/PET-MOVEMENT.md` §3.1（AC1–AC18）回指的条目 = 需求档 `docs/requirements/PET.md` §三 / §四（US-24…US-28 含 US-24 起飞门口径 / NFR-18…NFR-22 + 5 处注记）——三处同源，计数一致。

**八、风险 / 新发现（报告项；不阻断本批）**：

1. 阈值 `T` 的默认值**无本仓实机标定数据**（T9 未验）⇒ 已按「可调单一常量 + 待实机标定 + 到期条件」成文（U-10）。
2. **样本注释与实现不符**：样本注释（`samples/dsh-pet/dsh-pet/src/shared/physics.ts:212-213`）称死区随 `throwPower` 同比例缩放，实现（`:263-264`）却把固定常量与已乘 `p` 的速度比较 ⇒ 本仓按**实现口径**，登记 **O-11**。
3. **超出五条落点的第 6 处改动（已披露）**：需求档 US-26 口径②（「正常松手 ⇒ 物理接管飞行」）与阈值口径**直接相抵** ⇒ 以同款限定收窄（`v < T` ⇒ 物理不介入），否则三方条目一致不成立；①③④ 逐字未动。**若主 agent 不采纳，可单点回退该子条。**
4. 需求档 US-12（`:139`）/ NFR-1（`:277`）两处旧注记**未改**（口径仍为「松手**之后**的运动由 US-24 管辖」/「物理接管期改按 NFR-18 判」）——在阈值口径下**逐字仍真**（均以「存在接管期」为条件），仅不如新注记具体；是否收紧请主 agent 裁定。

**九、本段修正记录（追加行）**：

| 日期 | 修正点 |
|---|---|
| 2026-09-18 | **修正轮（起飞阈值口径）**：新增本节（位次声明 / 写域不变 / 覆盖表与 AC 回指 / AC15–AC18 摘要 / U-7 重写与 U-10 新增 / 阈值 `T` 判据面 / 计数全量重述 / 三方一致 / 风险四条）。设计档同步落 §2.2.12 · 选型 H 组 · DD-15 · O-11 · U-7/U-10 · AC15–AC18 · TC-30–TC-34；需求档同步落 US-3 / US-24 / US-26 / NFR-2 四处 + 变更记录。 |

#### 2.10 修正轮补正：起飞判据量（2026-09-18——**本段现行权威值**）

> **依据** = 主 agent 追加裁定 2026-09-18（用户已委托）——用户问「看速度好，还是看加速度好」⇒ 裁定：**用速度，不用加速度**。
> **位次**：§2 为 append-only 段 ⇒ §2.9 的 AC / TC / 计数与本节冲突处以**本节为准**。
> **范围**：只钉死「判据用哪个量」（+ 静默规则）；阈值 `T` 的取值 / 量纲 / 标定口径**不变**（= §2.9 五）。

**一、判据量（权威句 = 设计档 §2.2.12 / DD-16）**：

- **主判据 = 速度**：门量 `v` = 松手前 `GATE_WINDOW_MS`（**120 ms**）采样窗内的**峰值分段速度**（分段合并口径 = `SEG_MIN_DT_MS`）。
- **静默放置规则**：同一 120 ms 窗内**端点位移 ≤ `QUIET_SPAN_MAX_DIP`（5 px）** ⇒ 一律判**放置**（不起飞，`phys-stop reason=quiet-dwell`）。
- **加速度不作判据**：不得单独翻转门判据；样本的 `ACCEL_REF` / `ACCEL_GAIN_MAX` **只作用于飞行初速**（扔多远），不进「扔没扔」。
- 依据四条（逐条）：① 甩抛本体是动量（`m·v`，一阶量）；② 8 ms 采样（`shell-pet-drag.js:23`）下二阶差分放大量化抖动 ⇒ 误判「甩」（假阳性）；③ 放手前用户通常**先减速** ⇒ 末瞬加速度常为负 ⇒ 以加速度判据会**判反**；④ **零额外数据面**（复用既有拖动采样）。

**二、候选否决（= 选型 I 组，设计档 §2.1）**：① 速度 · 窗口峰值 + 静默规则 = **选定**；② 速度 · 端点均值×0.5 + 峰值×0.5（样本 `estimateReleaseVelocity` 口径）= **否决**（放置已由静默规则单独处置，两套叠加会把「快甩后收手」误判为放置）；③ 速度 · 末瞬单点 = **否决**（单点量化噪声）+ ④ **加速度作主判据** = **否决**（上面四条依据）。

**三、写域（仍不变）**：**12 项零增零减**（门量 / 静默判据 = 核心档内两个纯函数；常量 **+2**：`GATE_WINDOW_MS` / `QUIET_SPAN_MAX_DIP`）。§2.2「明确出批」不变。

**四、AC / TC（取代 §2.9 三、与 §2.4）**：

| AC | 判据摘要 |
|---|---|
| AC15（改） | 门量（120 ms 窗峰值速度）< `T` ⇒ `below-threshold` + 物理面零写入 + `drag-end` 后位置至多 1 次变动 |
| AC18（改） | 门量 `v = T` ⇒ 起飞（**闭区间**）；`v = T − 1 px/s` ⇒ 不起飞 |
| AC19（新） | **静默放置**：窗内端点位移 ≤ 5 px ⇒ 不起飞（`reason=quiet-dwell`）+ 零写入 + ≤200ms 静止；与「位移 > 5 px 且 `v < T`」两支**可区分** |
| AC20（新） | **判据量 = 速度**（判别力断言）：同一门量、末段加速度 **+ / −** 两例 ⇒ 门判据结果**相同**；门函数入参 = 标量速度（体内零加速度项）；`ACCEL_*` 常量行标「不进判据」 |

用例表：**TC-1…TC-37**（新增 TC-35 静默放置 / TC-36 两支可区分 / TC-37 加速度不进判据；TC-10 改为「门判据**不乘 `p`**」）。

**五、计数与枚举（取代 §2.9 六）**：

- US：**5** 条（24…28）· NFR：**5** 条（18…22）· AC：**20** 条（AC1…AC20）· TC：**37** 条（TC-1…TC-37）。
- DD：**16** 条（DD-1…DD-16；**DD-16 = 判据量 = 速度**）· 选型组：**9** 组（A–I；**I 组 = 起飞判据量四候选**）。
- U：**10** 项（U-1…U-10）· open：**3** 项 · 观察项：**11** 项（O-1…O-11）。
- armGate：**10** 条（G1–G10）· `phys-stop` reason 词表：**14** 个（`below-threshold` / **`quiet-dwell`**）。
- 需求档计数：US 23 → **28**；NFR 17 → **22**；本批修订 / 限定注记 **5** 处（US-3 / US-12 / NFR-1 / NFR-2 / US-26）。

**六、三方一致（同源声明）**：本节条目 = 设计档 `docs/design/PET-MOVEMENT.md` §3.1（**AC1–AC20**）回指的条目 = 需求档 `docs/requirements/PET.md` §三 US-24 的起飞门口径（含**判据量 = 速度** / **静默放置**两条）+ §四 NFR-18…NFR-22 + 5 处注记。

**七、风险 / 新发现（报告项；不阻断本批）**：

1. **门量口径使 `T` 的刻度整体下移**：门量由「加权估计量」改为「窗内峰值速度」（峰值 ≥ 加权量）⇒ 同一 `T` 下起飞门槛比样本口径**偏低**。`T` 本就是**待实机标定**（U-10）⇒ 标定时须按**新门量**标定，**不得**把样本的 500 当作已标定值直接沿用。
2. `QUIET_SPAN_MAX_DIP = 5 px` 取自既有「位移 > 5px 判为拖动」口径（`pet.js:121`）——属**同源借用**，其实机合适值同样**待标定**。
3. 样本 `estimateReleaseVelocity` 的 `PEAK_WEIGHT` / `ACCEL_REF` / `ACCEL_GAIN_MAX` 仍作用于**飞行初速**（AC3 的 ±60% 带不变）；**门量 ≠ 初速**是有意设计（两道判据各答一问），实施者**不得**把两者合一。

#### 2.11 修正轮：设计评审轮 1 处置（2026-09-18——**本段现行权威值**）

> **依据** = 批次档 §3「轮次 1」发现表（🔴2 / 🟡6 / 🔵7，共 15 条）+ 主 agent 逐条核验与裁决。
> **位次声明**：§2 为 append-only 段 ⇒ **§2.1 / §2.3 / §2.4 / §2.5 / §2.6 / §2.9 / §2.10 中与本节冲突的摘要与计数以本节为准**（D3「计数与枚举同改」由本节完整重述承担）。
> **写域**：`docs/design/PET-MOVEMENT.md` · `docs/requirements/PET.md`（本批相关行）· 本节。**零夹带**：参数默认值（U-2 = 照搬样本）· U-6 / U-8 / U-9 结论 · 出批项 · 写域 12 项文件清单**均未动**；地图 / 台账面归主 agent（本设计者只提交建议行）。

**一、🔴 两条**

1. **门量口径（§2.9 五）**：该行括注「`v` = 抛掷所用初速的幅值，**已含软上限与 `throwPower`**」**作废**。**门量 `v` = 松手前 120 ms 采样窗内的峰值原始速度**（标量 px/s）；`throwPower` **只放大飞行初速、不进门**。**§2.9 五的 `v` 定义以 §2.10 一 为准**（同源权威句 = 设计档 §2.2.12 / DD-16）。§2.9 五 的其余执行细节（量纲 / 默认 500 / 闭区间 / 待实机标定 / 到期条件）**不变**；设计档口径正确、未改。
   - **机制披露**：§2 为 append-only ⇒ 该行**无法原地改写**，故以本行明文**作废 + 取代**落地。若主 agent 要求物理删除该行错句，需在 §4 处置（或授权破坏 append-only）。
2. **「≤4 s 静止」不可达 ⇒ 三合一收口（**不改参数**）**：选定口径与实测数据见「二」；**AC1④ / AC11 / AC16 / TC-1 / TC-7 / TC-27 同源改**（见「三」）；**NFR-19 重写**（见「四」）。

**二、NFR-19 的选定口径与理由（供主 agent 呈报用户）**

1. **主判据（必然收口）**：任意可达初速（`|v| ≤ 3600×p`）+ 任意屏内起点 ⇒ **≤ 5 s**（`PHYS_MAX_FLIGHT_MS`）内**收口**（停机 → 停泊序列）；`timeout` 到点 ⇒ **强制落地**（`y = maxY` + 速度归零 → 停泊序列）——**不得停在半空**。
2. **「≤ 4 s 静止」= 限定档**：判据 = **首触竖直速度 `|vy_imp| ≤ 450 px/s`**（⇒ `atRest` ≤ 4 s）。一阶模型 = 竖直反弹链 ≈ `|vy_imp|/(g·(1−e)/2)` + 首段下落 ≤ `|vy_imp|/g` + 接地摩擦尾 ≈ `ln(|vx|/REST_VX)/groundFriction`。
3. **为什么不采「水平 / 向下为主」这个字面口径**：静止时长由**首触竖直速度**（抛掷高度 + 竖直初速的合成）决定，**不由初速方向决定**——所采纳参数下（`g 1400` / `e 0.78`）中屏高度的自由落体冲击即 ≈ **1008 px/s**（链 ≈ 6.5 s）⇒ 按方向分档的「水平」支同样远超声称。
4. **实测数据**（本设计者按 §2.2.4 步进契约 + 16 ms tick 亲算；单屏工作区 1920×1040 DIP）：
   - 128 例扫描集（AC11 口径）：**16 例 ≤ 4 s / 112 例走 5 s 硬上界**；
   - 稠密扫描 **2592 例**：945 例（36%）≤ 4 s，其余走硬上界；`rest > 4 s` 例的**最小**首触竖直速度 = **565 px/s**（= 判据 450 的裕度）；`|vy_imp| ≤ 450` 的全部例**最坏 3.31 s**（裕度 0.69 s）；
   - 门槛最低的抛掷（水平 500 px/s、中屏起点）首触竖直速度即 **1008 px/s** ⇒ 链 ≈ 6.5 s ⇒ 走硬上界。
5. **结论（必须让用户看到）**：照搬样本参数下，**「≤ 4 s」只覆盖「近地 + 缓落」这一窄档**（扫描集 ≈ 1/8）；**5 s 硬上界（含强制落地的一次可见位移）是常规路径**，不是兜底。若用户要「抛出去很快停住」的手感，动的是**参数或模型**：`restitution` 降到 ≈ **0.55** ⇒ 中屏典型抛掷（首触 1008）≈ **3.9 s** 内静止（`e = 0.6` ⇒ 4.34 s；`e = 0.5` ⇒ 3.62 s）——属 **U-2 参数面**，需用户实机感受后裁定；本批按裁定**不改参数**，只如实登记（见「八」）。

**三、批次档侧判据摘要（取代 §2.4 与 §2.9 三 / §2.10 四 的相应行）**

| AC | 现行判据摘要（可机检） |
|---|---|
| AC1 | 轨迹界内 + 反弹 0.78±0.02 + 摩擦衰减 + 静止收敛按**两档口径**（首触 `\|vy_imp\|` ≤ 450 ⇒ ≤4 s；其余 ⇒ ≤5 s 且**收口在地面**） |
| AC4 | `landingSquash` 单调不增 + `squashScale` 端点 / 上界 ≤1.12 + 渲染面在场；**④ 曲线定义恰 1 处**（渲染层按名引用）；**⑤ 贴地起飞不产生伪挤压** |
| AC6 | armGate **11** 条（G1–**G11**）逐条置假 ⇒ 逐条 reason + 零写入；全通过 ⇒ `phys-arm` 的 `pos` == `drag-end` 的 `pos` |
| AC9 | **10 条降级行 + 1 条主流分支行**（共 11 行）逐行行为与日志 reason 一致；关闭态与今天逐位一致 |
| AC10 | 每 tick `setPosition` ≤1 + 同目标去重 + 飞行期取屏 0 次 + 关闭态零定时器；**④ 拖动期 CPU 与拖动循环同一次 10 s 采样**（落 NFR-1 的 <5% 预算） |
| AC11 | 扫描集 **128 例**：① 恒界内 + 全部 ≤5 s **收口**且收口后无 `wrote=1`；② `timeout` 例**收口在地面**；③ 首触 `\|vy_imp\| ≤ 450 px/s` 档 ⇒ ≤4 s `atRest`；④ `gravity = 0` ⇒ 收口在地面；⑤ 超 4 s 占比 / 最坏值记 §5 |
| AC16 | `v ≥ T` ⇒ `phys-arm` + 按 §2.2.4 步进；**静止收敛按 AC11 两档口径** |

其余 AC（2 / 3 / 5 / 7 / 8 / 12–15 / 17–20）**判据不变**。

**四、需求档侧改动（逐条）**

1. **NFR-19 重写**：标题「静止收敛与有界」→「**静止收口与有界**」；「必然停下 ≤4 s」→「**必然收口 ≤5 s 且收口在地面**」；「≤4 s」→ **近地软着陆档条件式判据**（首触竖直速度 ≤450 px/s；含一阶模型）；硬上界行为 = **强制落地**；度量方式改**两支扫描**。
2. **NFR-8 carve-out 修订注记**（#7）：`build.files` 仅允许增列本批两个新源档（+2 行），并写明**消解 / 到期条件**；度量方式同源改（依赖段零 diff + `build.files` 仅 +2）。
3. **NFR-21 计数**：注记枚举由「四处」改 **6 处**（US-3 / US-12 / NFR-1 / NFR-2 / US-26 / **NFR-8**），并写明 NFR-8 的注记与物理开关无关。
4. **NFR-18 开销判据**：补**拖动期**（16 ms 采样器运行中）的增量落 NFR-1 的 <5% 预算，且与拖动循环**同一次 10 s 采样**。
5. **US-12（#15 收紧）**：注记后补子条——「松手之后的运动」以**物理开启且释放速度 ≥ `T`** 为条件；`v < T` 时逐字照旧。
6. **NFR-1（#15 收紧）**：口径限定的「物理接管期」补同条件；`v < T` 时本子条逐字照旧。
7. **US-3 / NFR-2 连带指针同源**：「静止时刻」→「**收口时刻**（近地档 ≤4 s；其余档 ≤5 s 且收口在地面）」。

**五、逐条同源表（取代 §2.9 二 的逐条行）**

| 需求条目 | 设计档验收标准（回指） |
|---|---|
| US-24 甩抛与飞行 | AC1、AC2、AC3、AC15、AC16、AC17、AC18、**AC19、AC20** |
| US-26 位移单写者仲裁 | AC5、AC6、AC7、AC15 |
| 修订 / 限定注记（**6 处** = US-3 / US-12 / NFR-1 / NFR-2 / US-26 / **NFR-8**） | AC9、AC13、AC15 |

US-25 / US-27 / US-28 / NFR-18…NFR-22 的回指**不变**（= §2.1 原行 + §2.9 二 的修正行）。

**六、计数与枚举（取代 §2.9 六 / §2.10 五）**

- US **5** 条（24…28）· NFR **5** 条（18…22）· AC **20** 条（AC1…AC20）· TC **37** 条（TC-1…TC-37）· DD **16** 条 · 选型 **9** 组（A–I）· U **10** 项 · open **3** 项 · 观察项 **11** 项（O-1…O-11）。
- **armGate：11 条（G1–G11；G11 = 静默放置）** · `phys-stop` reason 词表：**14** 个（与 G1–G11 对应——G7 占 4 个、G11 占 1 个）。
- 需求档计数：US 23 → **28**；NFR 17 → **22**；本批修订 / 限定注记 **6 处**（4 → 5 → **6**，新增 **NFR-8 的 carve-out**）。
- 新增编号：**G11**（静默支显式编号，#4）；**无**新增 AC / TC / DD / U / open / O 编号。

**七、三方一致（同源声明）**：本节条目 = 设计档 `docs/design/PET-MOVEMENT.md` §3.1（AC1–AC20）回指的条目 = 需求档 `docs/requirements/PET.md` §三（US-24 含起飞门与静默放置口径 / US-26 口径②）+ §四（NFR-18…NFR-22 + 6 处注记）。

**八、风险 / 新发现（报告项；不阻断本批）**

1. **「≤4 s」只覆盖窄档、5 s 硬上界成为常规路径**（数据见「二」）⇒ 需用户裁定 `restitution`（或增设接地阻尼项）；本批按裁定不改参数。
2. **强制落地的一次可见位移跳**：`timeout` 到点时若窗口在空中 ⇒ 落地 = 一次可见位移（现为常规路径）⇒ 与 1 同根（参数面）。
3. **TC-1（「正常」用例）在照搬参数下走硬上界档**（首触竖直速度 ≈ 1078 px/s）——用例期望已同源改；若用户改 `restitution`，该判定随参数重算。
4. **§2.9 五 的错句无法原地删除**（append-only）⇒ 见「一、1」的机制披露。
5. **文档面残留（不阻断）**：`docs/README.md` 的 PET-MOVEMENT / PET 两行落后两轮修正（设计档 O-4）——地图面归主 agent。
6. **NFR-13（B18 面）的「NFR-1…NFR-8 全部保持成立」**：其 NFR-8 部分因本批 carve-out 与现行 NFR-8 版本措辞不符（B18 期该断言为真）——建议行：主 agent 裁定是否补同源注记（本设计者未改 B18 面文字）。

**九、本段修正记录（追加行）**

| 日期 | 修正点 |
|---|---|
| 2026-09-18 | **修正轮（设计评审轮 1 处置）**：新增本节（位次声明 / 🔴2 处置 / NFR-19 选定口径与实测数据 / 批次侧判据摘要 7 行 / 需求档改动 7 项 / 逐条同源表 / 计数全量重述 / 三方一致 / 风险六条）。设计档 + 需求档同批落地（两档变更记录各一行）。 |

#### 2.12 修正轮：设计评审轮 2 处置 + 参数裁定 B（2026-09-18——**本段现行权威值**）

> **依据** = 批次档 §3「轮次 2」发现表（🔴1 / 🟡2，共 3 条）+ 主 agent 逐条独立复核（三条全部成立）+ **用户 2026-09-18 裁定**（原话「**先做成 B 吧，不行再调整手感**」）。
> **位次声明**：§2 为 append-only 段 ⇒ **§2.1 / §2.3 / §2.4 / §2.5 / §2.6 / §2.9 / §2.10 / §2.11 中与本节冲突的摘要与计数以本节为准**（D3「计数与枚举同改」由本节完整重述承担）。
> **写域**：`docs/design/PET-MOVEMENT.md` · `docs/requirements/PET.md`（仅本批相关行）· 本节。行号只作 as-of 参考（D4）。
> **零夹带**（逐条自查）：不改静默门 G11 / 起飞归一化 / 曲线单一来源 / O-10 结论 · 不改 U-6 / U-8 / U-9 · 不动 `gravity` / `groundFriction` / `ceilingBounce` / `TAKEOFF_MIN_SPEED` · 不动写域 12 项文件清单 · 不动 §2.2「明确出批」· 地图 / 台账面归主 agent（本设计者只提交建议行）。

**一、🔴 #16 处置：TC-31 改可判定 + 顺查发现第二处旧期望**

1. **TC-31 改写**（设计档 TC-31 行）：补**起点**（屏中央，窗口左上角 `(835, 385)`，单屏工作区 1920×1040）+ **方向**（水平向右）⇒ 首触竖直速度可判（= **1008 px/s**）；期望按**同一两档口径**：> 档界 800 ⇒ **非档内支** ⇒ ≤5 s 且**收口在地面**（e = 0.55 实测 `rest ≈ 3.36 s`、`reason=atRest`）；AC 列 = AC16、AC11。
2. **顺查发现第二处旧期望（本轮新增的同源修正，非新发现）**：**TC-1** 原写「首触 ≈1078 ⇒ 走 5 s 硬上界（`reason=timeout`、`snap=1`）」——该期望**只对 e = 0.78 成立**；参数改 0.55 后同例实测 **3.81 s 收敛**（`reason=atRest`、`snap=0`）⇒ 同源改（这正是轮 2 #16 建议的「核对 §3.2 全表无第二处旧期望」的产物）。
3. **§3.2 全表核扫结果**：除 TC-31 / TC-1 外**无第三处** `≤4 s` 旧期望；TC-27（5 s 上界用例）**仍成立**（e = 0.55 下仍有 10/128 例触上界，见二.4）。

**二、参数裁定 B 五件（用户裁定 → 落档）**

1. **参数表**：`docs/design/PET-MOVEMENT.md` §2.2.3 的 `restitution` 行 0.78 → **0.55**（其余四项不动），标「**本仓调（用户 2026-09-18 裁定 B）** · **待实机标定**（到期条件 = 用户实机感受一次后的裁定轮）· 可逆变 = 本表一处常量」；同源改 §2.2.3 的「U-2 口径」行与 §2.5.2 的 U-2 行（由「① 照搬」改 **② 本仓调（已裁定）**）。
2. **「≤4 s 档」的界重推**（推导逐字，见本节「三」+ 下条）：**450 → 800 px/s**（放宽 **78%**）。
3. **同源同步**（逐处）：设计档 §1.1 NFR-19 行 / §3.1 AC1②④ / AC2 / AC11③⑤ / AC16 / §3.2 TC-2 / TC-3 / TC-5 / TC-1 / TC-31 / §3.3 限制 6 + 变更记录一行；需求档 NFR-19 三行（档界 / 度量 / 参数前提）+ 变更记录一行。
4. **与样本差异登记**：新增 **O-12**（设计档 §2.5.3）——本仓 `restitution` **0.55** vs 样本 `assets/config.jsonc:92` 的 **0.78**；**理由** = 手感裁定 B（玩偶感：首触后**可感弹跳 1–2 下**——实测弹跳高度 **115 / 33 / 10 / 2 px**；典型抛掷 **3.0–3.8 s** 停）；**可逆性 = 单常量、一处**；样本为只读参考（R14）不改。
5. **本节**（§2.11 之后的追加节）：改动清单 + 裁定依据 + 重算数据（即本 §2.12）。

**三、扫描口径钉死（上轮未记录 ⇒ 本轮起为唯一口径，重算依据）**

- **边界**：单屏工作区 **1920×1040 DIP**；窗口 250×270 ⇒ 窗口左上角取值区间 `x ∈ [0, 1670]`、`y ∈ [0, 770]`（窗口矩形口径，承 §2.2.4）。
- **4 起点**（窗口左上角）：① 屏中央 `(835, 385)` ② 近地（贴下缘）`(835, 770)` ③ 近顶（贴上缘）`(835, 0)` ④ 左缘中高 `(0, 385)`；密扫另含 `(0, 0)`。
- **8 方向**：水平 / 竖直 / 两条 45° 对角线（±x / ±y 全组合，已归一化）；**初速档** = `{500, 1200, 2400, 3600}` px/s（= AC11 的 128 例扫描集）；密扫 = 100…3600 步 50（另有步 25 / 步 10 复核）。
- **步进契约** = §2.2.4 逐字（16 ms tick、semi-implicit Euler、`MAX_STEP_DT 0.05`、`REST_VY 40` / `REST_VX 15`、`ceilingBounce true`）；**`atRest`** = 同契约式；`5 s` 到点按 §2.2.6 强制落地。

**四、e = 0.55 的档界推导与重算数据（评审取证面）**

**（1）一阶模型（本轮把上轮的「未截断几何级数」改为截断口径）**

- 竖直反弹链：第 k 次反弹的上升速度 = `e^k·|vy_imp|` ⇒ 该次滞空 = `2·e^k·|vy_imp|/g`；链止于 `e^K·|vy_imp| < REST_VY(40)` ⇒ 链 ≈ `(2·|vy_imp|/g)·Σ_{k=1..K} e^k`，闭式上界 `(2·|vy_imp|/g)·e/(1−e)`。
- **上轮 `|vy_imp|/(g(1−e)/2)` = `(2|vy_imp|/g)·Σ_{k≥0}e^k` = 未截断口径** ⇒ 对 1008 px/s 给出 6.5 s，而截断口径（K=12）给出 **4.85 s** —— 本轮实测与截断口径逐点一致（见（3）模型验证）。
- 首段下落 ≤ `|vy_imp|/g`；接地摩擦尾 ≈ `ln(|vx|/REST_VX)/(−ln(1−F·dt))·dt`（F = 2.5/s、dt = 16 ms ⇒ 每步 ×0.96，且只在接地步衰减）。
- **数值（`|vy_imp|` = 1008 px/s，中屏典型）**：e = 0.78 ⇒ K = 12、`Σ` = 3.37 ⇒ 链 1.44×3.37 = **4.85 s** ⇒ 加首段下落（0.72 s）与摩擦尾 ⇒ **触 5 s 上界**；e = 0.55 ⇒ K = 5、`Σ` = 1.16 ⇒ 链 1.44×1.16 = **1.67 s** ⇒ 全链 **3.0–3.8 s**。

**（2）档界（= 使档内**全部**例 `rest ≤ 4 s` 的 `|vy_imp|` 上界）**

| 密扫粒度 | 例数 | 档内（`|vy_imp| ≤ 800`）最坏 `rest` | `rest > 4 s` 例的最小 `|vy_imp|` |
|---|---|---|---|
| 步 50 | 2880 | 3.26 s | 1023 px/s |
| 步 25 | 5760 | 3.26 s | 1018 px/s |
| 步 10 | 14400 | **3.33 s** | **999 px/s** |

⇒ 判据裕度 **0.67 s**（4 − 3.33）；取整档界 = **800 px/s**（与上轮同法：上轮取 450、实测分界 565–596，比值 0.80；本轮 800/1000 = 0.80）。
⇒ **e 由 0.78 降至 0.55 ⇒ 链缩短 2.4–2.9× ⇒ 档界由 450 放宽至 800 px/s（+78%）**。

**（3）模型验证（同模型对上 §2.11 的三项原值）**

| 项 | §2.11 记录 | 本轮重算 | 结论 |
|---|---|---|---|
| 中屏水平 500 的首触 `|vy_imp|` | **1008 px/s**（二.4） | **1008** | 逐字一致 |
| TC-1（1200,−400）首触 | **1078 px/s**（八.3） | **1078** | 逐字一致 |
| e = 0.78 的 128 例「走 5 s 上界」数 | **112 例**（二.4） | **112** | 逐字一致 |

**（4）重算数据（AC11 / NFR-19 同源）**

| 口径 | e = 0.78（上轮） | **e = 0.55（裁定 B，现行）** |
|---|---|---|
| 128 例：≤ 4 s | 14（上轮记 16） | **74** |
| 128 例：4 s < rest ≤ 5 s | 2 | 44 |
| 128 例：走 5 s 上界 | 112 | **10**（恒为 45° 斜下快甩 v ≥ 2400） |
| 稠密（步 50，2304 例）≤ 4 s 占比 | 11.0%（上轮记 36%，**构成不同**） | **57.6%** |
| `rest > 4 s` 例的最小首触 `|vy_imp|` | 596 px/s | **999–1023 px/s** |
| 档界 / 档内最坏 `rest` | 450 / 2.98–3.31 s | **800 / 3.26–3.33 s** |
| 中屏水平 500（首触 1008） | 5 s 上界 | **3.02 s 收敛** |
| TC-1 例（首触 1078） | 5 s 上界 | **3.81 s 收敛** |
| TC-31 例（水平 1200，首触 1008） | 5 s 上界 | **3.36 s 收敛** |

- **5 s 硬上界仍可达**（10/128 例；如屏中央竖直向上 3600 ⇒ 首触 2387、rest 4.43 s 仍收敛）⇒ **强制落地收口不得撤**（承 §2.11 二.1）。
- **弹跳可感性**（O-12 的「1–2 下」依据）：中屏典型抛掷的首触后弹跳高度序列 = **115 / 33 / 10 / 2 px** ⇒ 可感弹跳 1–2 下，其余为 <10 px 余振。

**五、🟡 #17 / #18 处置（需求档面）**

1. **#17 逐条回指补全**：需求档 US-24 验收指针 `AC1、AC2、AC3` → **加 AC15–AC20**（= 设计档 §1.1 的现行集合）；US-26 指针 `AC5、AC6、AC7` → **加 AC15**。三方逐条集合自此同源（设计档 §1.1 ↔ 需求档指针 ↔ 本批 §2.12 六）。
2. **#18 NFR-13 同源注记**：需求档 NFR-13 的「US-1…US-14 与 NFR-1…NFR-8 全部保持成立」补**口径澄清注记**——按 **B18 期口径**读；NFR-8 面**以 NFR-8 的 B20 carve-out 注记为准**（消解 / 到期条件同 NFR-8）。**该注记 = 口径澄清（非 carve-out）⇒ 本批「修订 / 限定注记」计数仍为 6 处**（US-3 / US-12 / NFR-1 / NFR-2 / US-26 / NFR-8）。

**六、计数与枚举（取代 §2.9 六 / §2.10 五 / §2.11 六）**

- US **5** 条（24…28）· NFR **5** 条（18…22）· AC **20** 条（AC1…AC20）· TC **37** 条（TC-1…TC-37；本轮**改判据不改编号**）· DD **16** 条 · 选型 **9** 组（A–I）· U **10** 项 · open **3** 项 · **观察项 12 项（O-1…O-12；新增 O-12）**。
- armGate **11** 条（G1–G11）· `phys-stop` reason 词表 **14** 个 · 参数表 **5** 项（结构不变，仅 `restitution` 值变）· 本仓新增常量 **6** 个（不变）· 档界 **1** 个（450 → **800 px/s**）。
- 需求档计数：US 23 → **28**；NFR 17 → **22**；本批修订 / 限定注记 **6 处**（NFR-13 的注记不计入）。
- 写域 **12 项零增零减**（§2.3 表体不变；§2.9 一 / §2.10 三 的「零增零减」声明继续有效）。

**七、三方一致（同源声明）**

本节条目 = 设计档 `docs/design/PET-MOVEMENT.md` §3.1（AC1–AC20）回指的条目 = 需求档 `docs/requirements/PET.md` §三（US-24 起飞门口径 / US-26 口径②）+ §四（NFR-18…NFR-22 + 6 处注记）——**三处同源，计数一致**（US 5 / NFR 5 / AC 20 / TC 37）。

**八、风险 / 新发现（报告项；不阻断本批）**

1. **上轮扫描构成未记录**（§2.11 二.4 只记结果）：同模型重算下「128 例 ≤4 s」= 14（上轮 16）、稠密占比 = 11.0%（上轮 36%，8 方向 × 4 速/档 × 4 起点的构成不同）⇒ **起点集与构成已在本节三钉死**，后续一律以本节口径重算；模型本身已由（3）三项逐字对上。
2. **档界是「参数 + 口径」的函数**，非普适常量：改 `restitution` / 步进契约 / 起点集都须按本节三的同一仿真重算（AC11③ 与 NFR-19 的数值随之变）。
3. **5 s 上界仍是常规路径的一部分**（10/128 例为 45° 斜下快甩）⇒ §2.2.6 的强制落地（一次可见位移）保留；观感面待实机。
4. **`restitution = 0.55` 无本仓实机标定**（T9 未验）⇒ 已按「可调单一常量 + 待实机标定 + 到期条件」成文（O-12 / §2.2.3）；用户实机感受后如需回调，只改一处常量 + 同源重算本节（4）。
5. **文档面残留（不阻断）**：`docs/README.md` §二 的两行仍落后（设计档 O-4）——地图面归主 agent。
6. **`docs/design/PET-MULTIMONITOR.md` §2.3.2 调用时机表未含 B20 两行**（O-3）——仍为建议行，本轮未动（跨档写权归主 agent 裁定）。

**九、本段修正记录（追加行）**

| 日期 | 修正点 |
|---|---|
| 2026-09-18 | **修正轮（设计评审轮 2 处置 + 参数裁定 B）**：新增本节（位次声明 / 零夹带自查 / #16 的 TC-31 改写与 TC-1 同源改与全表核扫 / 参数裁定五件 / 扫描口径钉死 / e = 0.55 档界推导与重算数据 / #17 与 #18 处置 / 计数全量重述 / 三方一致 / 风险六条）。设计档与需求档同批落地（两档变更记录各一行）。 |

**十、实施口径补注（追加；面向上游实施）**

- §2.2.3 的 `restitution` 表值 = **0.55**（「≈」只表示该值属**待实机标定档**，不是不确定值）⇒ AC17③ / TC-32 的机检判据 = 仓内常量 **`== 0.55`**（不得写成「约 0.55」）；标定轮若调值，须同源改 §2.2.3 表值 + NFR-19 两支判据 + 本节（4）的两张表。
- 本节（三）的扫描口径（边界 / 4 起点 / 8 方向 / 步进契约）是 AC11 与 NFR-19 重算的**唯一依据**：实施期桩测的扫描集须与之一致，否则档界判据不可比。

## §3 设计评审（评审子代理）

<!-- 由评审子代理填 -->

---

### 轮次 1（评审子代理）

轮次 1（设计评审 · 2026-09-18）— 评审对象：`docs/design/PET-MOVEMENT.md` + `docs/requirements/PET.md` 的 US-24…US-28 / NFR-18…NFR-22 与四条修订注记 + 本档 §2（含 §2.8–§2.10）。结论 = **changes-required**（🔴2 / 🟡6 / 🔵7）。

范围与方法注：不读 git diff、不读范围外源档 ⇒ §2.3 的既有档行数（`shell-pet.js` 426 等）**未抽检**（标注为 unverified）；`physics.ts:*` / `pet.ts:*` / `pet.js:121` / `shell-pet-drag.js:*` 的行号引文**未核实**（仅核内部一致性）；本仓无独立规范档，方法学按 AGENTS.md（写权矩阵 / 四步流程 / 三方同源）。

| # | Category | Severity | Issue | Suggestion |
|---|----------|----------|-------|------------|
| 1 | Document ownership | 🔴 | 起飞门量同一机制两处口径相抵：本档 `:204`（§2.9 五，标「本段现行权威值」，且 §2.10 声明「阈值 T 的…判据口径**不变**（= §2.9 五）」）写「`v` = 抛掷所用初速的幅值，**已含软上限与 `throwPower`**」；设计档 `:444` 写「`v`…**不是** `estimateReleaseVelocity` 的幅值（那是飞行初速）」、`:449` 写「门判据在**未增益的原始速度**上比较（`p` **不进门**）」。本档 §2.10 一（`:237`）与 TC-10（`:254`）已按设计档口径，但 §2.10 的取代声明（`:232`）只覆盖判据量，未覆盖该句 ⇒ 「现行权威值」段仍在教实现者相反做法。 | 父侧一行对齐：把本档 `:204` 的括注改为 §2.2.12 口径（门量 = 120 ms 窗峰值原始速度，`p` 不进门），或加一句「本节五的 `v` 定义由 §2.10 一取代」。设计档无需改。 |
| 2 | Acceptance criteria / Feasibility | 🔴 | 「≤4 s 静止」与所采纳参数（U-2 ① 照搬样本）+ 设计档自己的步进契约不自洽：按设计档 `:329-336` + `:290-311`，竖直向抛掷要等反弹链衰减到 `REST_VY = 40`，链耗时 ≈ `2·v₀·Σ0.78ⁿ/1400` = **800 px/s ≈ 5.2 s、1200 px/s ≈ 7.8 s**（撞顶夹断只会缩短，不足以回到 4 s），另有 `vx` 落地摩擦衰减最多 2.2 s。后果：① AC11（`:571`）的扫描集「8 方向 × {500,1200,2400,3600} × 4 起点 ⇒ 全部 ≤4 s `atRest`」无法通过，AC1④（`:561`）/ AC16（`:576`）/ TC-1（`:586`）同源承载该期望；② 5 s 硬上界（`:319` / `:410`）因此被**常规操作**触达（≥ ~800 px/s 的向上快甩），`reason=timeout` 走 §2.2.6 停泊 ⇒ 窗口被**停在空中**（可见缺陷，与需求档 `:224` / `:263` 的「落地后在摩擦下逐渐停下 / 停下即停泊」相抵）。 | 按所采纳常量重推上界并显式二选一：① 放宽静止判据（提高触地归零阈值 / 加接地阻尼项）并把与样本的差异登记；② 收窄断言（「≤4 s 适用于水平、向下为主的初速；竖直向由 5 s 上界收口」）并**同改三处**：需求档 NFR-19（`:416-420`）措辞、AC11 的扫描集、AC1④/TC-1；③ 把 `gravity` / `restitution` 归入「本仓调」（U-2 ②）。无论哪支，AC11 与 NFR-19 必须给出同一套可测集合。 |
| 3 | Clarity / 三方同源 | 🟡 | 逐条回指表未随最近两轮修正更新，本档「三方一致」声明不可逐条机检：设计档 `:37`（§1.1）US-26 行只列 AC5/6/7，而 `:575`（AC15 自身行）回指 US-26；本档 `:183`（§2.9 二）US-24 行只列 AC1/2/3 + AC15–AC18，而 `:265`（§2.10 六）声明按 AC1–AC20 一致。 | 父侧刷新三处逐条行（设计档 §1.1 的 US-24 / US-26；本档 §2.9 二的 US-24），或加一行声明这些行由 §2.10 计数取代。 |
| 4 | Clarity / Requirements coverage | 🟡 | 静默放置规则没有进入 armGate 枚举：设计档 `:251-264` 恰列 G1–G10（G10 = 阈值，`:264`），而 `:434` 把 §2.2.12 的判据块标为「= armGate 的 G10」、`:438` 另定义 `isQuietPlacement`；`:430` 的 reason 词表已含 `quiet-dwell`（14 个），AC6（`:566`）只断「armGate **10** 条…逐条置假」。只读 §2.2.2 的实现者可能漏掉该支，且无门级 AC 覆盖它。 | 在 §2.2.2 的门表里显式给出静默支的位次（如 G11 或「G10 的两支之一」），并让 AC6 或 AC19 的判据集合覆盖它，使「10 门 / 14 reason」一处可推。 |
| 5 | Clarity / 可维护性 | 🟡 | 挤压曲线的跨进程单一来源未定：设计档 `:376` 让 `pet.js`（渲染层）逐帧用 `squashScale(u, depth)` 与 `SQ_DURATION_MS`（220 ms）驱动，但两符号是核心档（主进程）常量（`:309-310`），受影响文件表（`:466-468`）只有 `pet.js` +12 / `pet-preload.js` +3，没有共享档或「主进程带曲线过 IPC」的路径。若在 `pet.js` 复制一份，会与桩测断言的纯函数（AC4，`:564`）静默漂移，且无 AC 管。 | 指明渲染层如何取得曲线（双环境共享档，或主进程直接下发曲线 / 时长），并加一条 AC / 静态判据：渲染层的常量不得构成第二处定义。 |
| 6 | Design gap（边界与首步钳入） | 🟡 | 起飞点可合法落在「起飞边界」之外，而设计按「不会」假设：设计档 `:339` 把飞行边界定为窗口矩形口径的工作区、`:333-336` 把 `x/y` 钳入该区间、`:510`/`:368` 断言「起飞点在起飞时的工作区内（冻结面 settle 保证）⇒ 轨迹不出界」。但需求档 `:73`（US-2）明确「允许一部分拖到屏幕外」，B03 的 settle 只保证**中心点**可见（需求档 `:126`）⇒ 贴下缘 / 任务栏边松手是常规路径，首个积分步会跳一次并把竖直向判成 `landed = true`（`§2.2.7` 触发伪 Q 弹）。本档 O-10（设计档 `:550`）只登记了「骑线推离」这一窄因并判「理论可达性低」。 | 明确并落档起飞归一化：要么在起飞时（首个 tick 前）一次钳入并把位移登记为已知限制（`prevGrounded` 同步取初值），要么把边界语义改为「中心点式」与 NFR-5 对齐；随后更正 O-10 的可达性判断。 |
| 7 | Requirements / Document ownership | 🟡 | 需求档 `:336`（NFR-8）要求 `build.files` **保持不变**，而本批 NFR-21（`:433`）与设计档 `:469` 要往 `build.files` 增列两个新源档；NFR-21 同时声明「US-1…US-23 与 NFR-1…NFR-17 保持成立」且只列四处修订注记 + US-26。 | 为 NFR-8 补同形 carve-out 或修订注记（B18 对其 package.json 子句用过的写法），使 NFR-21 的不回退声明字面为真。 |
| 8 | Methodology / Document ownership | 🟡 | 设计档 `:24` 自述「本设计者的越界例外，**承批次档 §1.6 授权**」，但 §1.6（本档 `:58-62`）无该授权句（只列台账 / 需求档 / 设计档落点），而地图写权按 AGENTS.md 属主 agent；且所写行立即已失真（见文末范围外注）。 | 父侧收口时显式追认该越界，或把地图行的维护移回主 agent（设计档自身 O-4 已是同型处置）。 |
| 9 | Requirements coverage（取证面） | 🔵 | 需求档 `:267`（US-28）要求降级各路径「各留 1 行诊断」，而设计档 §2.2.10 第 9 行（`:411`，退出/销毁）的取证只写「静态（三处停点）+ 无飞行态残留」，未列日志行（词表 `:426` 已含 `quit` / `destroyed`）。 | 第 9 行取证补该诊断行，或声明退出路径豁免「各留 1 行」。 |
| 10 | Clarity | 🔵 | `prevGrounded` 初值未定（设计档 `:374` 只写「上一帧未贴地」），而常见静止位形即窗口贴工作区底边：此位形下水平 / 向下轻甩，首步即触地且 `landed = true` ⇒ 松手瞬间多一次伪挤压。 | 成文初值口径（如 `s0.y == bounds.maxY` 时首帧视为已贴地），使首次挤压必为真落地。 |
| 11 | Clarity（数字漂移） | 🔵 | 设计档 `:582` 节标题写「用例表（TC-1…**TC-34**）」，表内实为 TC-37，§四 `:647` 记「TC 34 → 37」。 | 同步标题（或标题不写计数）。 |
| 12 | Clarity（计数口径） | 🔵 | AC9 判据写「降级 **10** 行（§2.2.10）逐行…」（设计档 `:569`），而 §2.2.10 现有 **11** 行（`:399-413`），第 11 行自标「主流分支，**非降级**」；本档 §2.4 同写「10 行」。 | AC9 与本档 §2.4 改为显式「10 条降级 + 1 条主流分支」。 |
| 13 | Acceptance criteria（开销面） | 🔵 | 新增的 16 ms 光标采样器**在拖动期**运行（设计档 `:318` / `:345`），落在需求档 `:277`（NFR-1）「拖动期间主进程 CPU 增幅 < 5%」预算内，但 AC10（`:570`）/ NFR-18 取证只覆盖飞行期与关闭态。 | 把拖动期增量并入 NFR-1 的同一 CPU 取证行，或声明与拖动循环同批度量。 |
| 14 | Affected-file size annotations | 🔵 | `shell-pet.js` 426 → ≈434（设计档 `:461` / `:476`）已在 300 行「主动拆分复核」档之上；「贴线档（≥480 行）：本批写域内**无**…⇒ 无需拆分计划」只讨论了 ≥480 口径（本档 `:119-120` 同）。 | 补一行 300–500 带的立场（如「既有 >300 行档本批仅小幅增长、不跨档 ⇒ 不启动拆分」），使判据被显式回应。 |
| 15 | Doc-state（协调项） | 🔵 | US-12（需求档 `:141`）/ NFR-1（`:283`）两处旧注记未按阈值口径收紧（本档 §2.9 八-4 `:221` 已登记待裁）；两处条文仅以「存在接管期」为条件才逐字为真。 | 父侧随本轮一并裁定（收紧或记录裁决），以免三方口径宽窄不一。 |

**范围外注（不设严重度）**：`docs/README.md` §二 的 `docs/design/PET-MOVEMENT.md` 行仍写「AC1–AC14 + TC-1…TC-29 / U-1…U-9 与 O-1…O-10」「待评审（2026-09-18 落档：B20 设计草稿）」，落后两轮修正（现为 AC1–AC20 / TC-1…TC-37 / DD-1…DD-16 / 选型 A–I / U-1…U-10 / O-1…O-11）；同表 `docs/requirements/PET.md` 行亦未同步（设计档 O-4 `:544` 已登记）。两行均属主 agent 档面、在本次评审目标之外，仅作协调报告。

**计数**：🔴 2 / 🟡 6 / 🔵 7（合计 15）。两条 🔴 均非口味问题：#1 = 同一机制在本档「现行权威值」段与设计档权威句两处相反；#2 = 设计自身模型与参数无法满足其验收断言，且失败路径用户可见（空中强制停泊）。

VERDICT: changes-required

### 轮次 2（评审子代理）

轮次 2（设计评审 · 2026-09-18）— 复核对象：轮次 1 的 15 条（🔴2 / 🟡6 / 🔵7）在 `docs/batches/B20-pet-physics.md` §2.11 + `docs/design/PET-MOVEMENT.md` + `docs/requirements/PET.md` 的落地情况。结论 = **changes-required**（🔴 1 残余 / 🟡 2 / 🔵 0；15 条中 14 条已验证 Fixed，1 条部分残余）。

范围与方法注：三档全文重读（本轮无 git、无范围外档读取）；`physics.ts:*` / `pet.js:121` / `samples/**` 引文仍**未核实**（范围外）；本档 §2.4 / §2.6 / §2.9 / §2.10 的旧计数与判据行经 §2.11 `:276` 的位次声明覆盖，不计为残余。

| # | Orig# | File | Severity | Status | Notes |
|---|-------|------|----------|--------|-------|
| 1 | 1 | docs/batches/B20-pet-physics.md:281 | — | Fixed | §2.11 一.1 明文作废 §2.9 五 的括注：「该行括注…**作废**。**门量 `v` = 松手前 120 ms 采样窗内的峰值原始速度**（标量 px/s）；`throwPower` **只放大飞行初速、不进门**」，并声明 §2.9 五 的 `v` 定义以 §2.10 一 为准；§2.11 `:276` 位次声明的取代清单已含 §2.9 ⇒ 机制面两处相反的口径已收口（:204 的错句按 append-only 无法物理删除，已按轮 1 建议的「加取代声明」路径落地并披露）。设计档 §2.2.12 `:459`/`:464` 与 §2.2.3 `:295` 口径一致（`p` 不进门）。 |
| 2 | 2 | docs/requirements/PET.md:419-424 · docs/design/PET-MOVEMENT.md:587 | — | Fixed（核心） | NFR-19 已重写为「必然收口 ≤5 s 且收口在地面（不得停在半空）」+「≤4 s 仅限近地软着陆档（首触 `|vy_imp| ≤ 450 px/s`，实测最坏 3.31 s、分界 ≈565 px/s）」；`:320` 的 `PHYS_MAX_FLIGHT_MS` 行为改为**强制落地**；`:373`（§2.2.6）落地机制成文；AC1 `:577` / AC11 `:587` / AC16 `:592` / TC-1 `:602` / TC-7 `:608` / TC-27 `:628` 同源改。残余 = TC-31（见 #16）。 |
| 3 | 3 | docs/design/PET-MOVEMENT.md:35/37 · docs/batches/B20-pet-physics.md:324 | — | Fixed | 设计 §1.1 的 US-24 行现列「AC1、AC2、AC3、AC15、AC16、AC17、AC18、AC19、AC20」、US-26 行列「AC5、AC6、AC7、AC15」；本档 §2.11 五 `:320-328` 逐条同源表明文「取代 §2.9 二 的逐条行」。残余 = 需求档逐 US 指针（见 #17）。 |
| 4 | 4 | docs/design/PET-MOVEMENT.md:251/:265 · docs/batches/B20-pet-physics.md:333 | — | Fixed | armGate 现为 **11** 条（G1–G10 + **G11 = 静默放置**），AC6 `:582` / TC-19 `:620` 已同步为 G1–G11；本档 §2.11 六 `:333` 写明「11 条（G1–G11）· reason 词表 14 个（G7 占 4 个、G11 占 1 个）」⇒ 11 门 ↔ 14 reason 一处可推。 |
| 5 | 5 | docs/design/PET-MOVEMENT.md:388-390 | — | Fixed | §2.2.7 增「曲线的跨进程单一来源（本轮补）」：唯一处定义 = 核心档，渲染层经 `<script src="pet-physics-core.js">` + `window.PetPhysicsCore` 按名取用；AC4④ `:580`「曲线定义恰 1 处」；受影响文件表 `:481`/`:482` 同步（pet.html +15 / pet.js +13）。 |
| 6 | 6 | docs/design/PET-MOVEMENT.md:354-356/:379/:385/:526/:566 | — | Fixed | §2.2.5 第 7 条成文起飞归一化（建 `s0` 后一次性钳入 + `prevGrounded` 初值 = 钳入后 `s0.y >= maxY`）；§2.2.6 不变量 `:379` 与 §2.5.1 #6 `:526` 改为「收口点必在区间内，**不依赖起飞点**」；O-10 `:566` 重标为「**常规路径**」已知限制；AC4⑤ `:580`「贴地起飞不产生伪挤压」。 |
| 7 | 7 | docs/requirements/PET.md:338-340/:436 | — | Fixed | NFR-8 增 carve-out 修订注记（仅允许增列本批两个新源档 +2 行，含消解 / 到期条件与度量方式）；NFR-21 `:436` 的注记枚举改为 **6 处**（含 NFR-8）；本档 §2.11 四.2/四.3 与六 `:334` 同源。 |
| 8 | 8 | docs/design/PET-MOVEMENT.md:25 | — | Fixed | §零「授权口径（本轮修正，#8）」：本档**不再自述「承批次档 §1.6 授权」**，越界落笔的追认权归主 agent（批次档 §4），地图面归主 agent、本设计者只提交建议行；O-4 `:560` 同源。 |
| 9 | 9 | docs/design/PET-MOVEMENT.md:425 | — | Fixed | §2.2.10 第 9 行取证补「**诊断行 `phys-rest reason=destroyed` / `reason=quit` 各 1 行**」；§2.2.6 `:374`「窗口不在场的收口（本轮补）…仍**各留 1 行**」。 |
| 10 | 10 | docs/design/PET-MOVEMENT.md:356/:385 | — | Fixed | `prevGrounded` 初值口径成文于 §2.2.5 第 7 条与 §2.2.7。 |
| 11 | 11 | docs/design/PET-MOVEMENT.md:598 | — | Fixed | §3.2 标题现为「用例表（TC-1…TC-37）」，表内至 TC-37，§四 `:664` 记「TC 34 → 37」一致。 |
| 12 | 12 | docs/design/PET-MOVEMENT.md:585 · docs/batches/B20-pet-physics.md:303 | — | Fixed | AC9 现写「**10 条降级行 + 1 条主流分支行**（共 11 行）」；本档 §2.11 三 `:303` 同源。 |
| 13 | 13 | docs/design/PET-MOVEMENT.md:586 · docs/requirements/PET.md:413 | — | Fixed | AC10④ 补「**拖动期**（16 ms 光标采样器运行中）的开销落在 NFR-1 的 <5% 预算内，且与拖动循环**同一次 10 s 采样**度量」；NFR-18 同源。 |
| 14 | 14 | docs/design/PET-MOVEMENT.md:492 | — | Fixed | §2.3 增「300–500 行档立场（本轮补，#14）」一行。 |
| 15 | 15 | docs/requirements/PET.md:142/:284 · docs/batches/B20-pet-physics.md:316-317 | — | Fixed | US-12 与 NFR-1 两处旧注记按阈值口径**收紧**（补「物理开启且释放速度 ≥ `T`」条件 + `v < T` 逐字照旧）。 |
| 16 | (新·#2 残余) | docs/design/PET-MOVEMENT.md:632 | 🔴 | Unfixed（残余） | 用例表 TC-31 仍留旧期望：「| TC-31 | 正常 | 快甩：估计 `v = 1200 px/s` | `phys-arm` ⇒ 抛物线 ⇒ 反弹 ⇒ 摩擦衰减 ⇒ **≤4 s `atRest`** | AC16 |」——与它自己回指的 AC16 `:592`「**静止收敛按 AC11 的两档口径**（近地档 ≤4 s；其余 ⇒ ≤5 s 且**收口在地面**）」相抵，也与本档 §2.11 二.4 的实测相抵（`:293`「门槛最低的抛掷（水平 500 px/s、中屏起点）首触竖直速度即 **1008 px/s** ⇒ 链 ≈ 6.5 s ⇒ 走硬上界」；128 例扫描仅 16 例 ≤4 s）。该行未标注起点 / 方向 ⇒ 期望要么不可判（欠定）、要么在常规读法下**必然不通过**（竖直向 1200 px/s 首触 ≈1200 > 450 ⇒ 应走 5 s 档）。轮 1 处置的 `:283` 清单只列 AC1④ / AC11 / AC16 / TC-1 / TC-7 / TC-27，TC-31 被漏扫。 | 父侧把 TC-31 的期望改写到同一两档口径（并写明起点 / 方向，使 `|vy_imp|` 可判），或明确标其为「近地软启动」档用例；同时核对 §3.2 全表无第二处旧期望。 |
| 17 | (新) | docs/requirements/PET.md:223/:243 | 🟡 | New（三档逐条回指未同步） | 需求档的逐 US 验收指针仍停在旧集合：「### US-24 甩抛与飞行（验收：`docs/design/PET-MOVEMENT.md` §3.1 **AC1、AC2、AC3**）」与「### US-26 位移单写者仲裁（验收：…§3.1 **AC5、AC6、AC7**）」，而设计 §1.1 `:35`/`:37` 与本档 §2.11 五 已把 US-24 映射到 AC1/2/3 + AC15–AC20、US-26 映射到 AC5/6/7 + AC15 ⇒ 三方同源在「逐条」层面仍有一处宽窄不一。 | 父侧把 US-24 / US-26 的验收指针补到现行集合（或加一行声明：逐条集合以 `docs/design/PET-MOVEMENT.md` §1.1 为准）。属文档面，不阻断。 |
| 18 | (新) | docs/requirements/PET.md:371 | 🟡 | New（协调项） | NFR-13（B18 面）仍写「**既有语义零回退**：US-1…US-14 与 NFR-1…NFR-8 全部保持成立…**零新依赖**：…（仅 `build.files` 增列新增源档）」——NFR-8 本轮已开 `build.files` carve-out（`:338`）⇒ 「NFR-1…NFR-8 全部保持成立」的字面断言不再为真（B18 期该断言为真）。设计者已按报告项登记（本档 §2.11 八.6 `:346`）未改 B18 面文字。 | 父侧裁定是否给 NFR-13 补同源注记（或声明 NFR-13 的该句按 B18 期口径读）。属跨批文字协调，不阻断。 |

**计数**：🔴 1（#16，= 轮 1 #2 的残余）· 🟡 2（#17 / #18，均为文档面、按 R7e 不阻断）· 🔵 0 · 轮 1 的 15 条中 14 条验证 Fixed。

VERDICT: changes-required

### 轮次 3（评审子代理）

轮次 3（设计评审 · 2026-09-18）— 复核对象 = 轮次 2 的 4 项修正声明（评审对象声明范围内的严格验证；本轮不猎新问题）。**结论 = pass**（🔴 0 / 🟡 0 / 🔵 1 附注）。

方法注：三档全文重读（本轮无 git、未读范围外档）；`physics.ts:*` / `pet.js:121` / `samples/**` 引文仍**未核实**（范围外）；本档 §2.1–§2.11 的旧值与旧计数经 §2.12 `:357` 的位次声明（取代清单已含 §2.11）覆盖，按 append-only 既定路径不计为残余。

| # | 声明 | 验证点 | 状态 | 证据（本轮 fresh read 引文） |
|---|---|---|---|---|
| ① | 🔴#16 TC-31 改可判定 + TC-1 同源改 | TC-31 补起点 / 方向 / 两档期望；TC-1 由「走 5 s 硬上界」改判收敛（e = 0.55 下 3.81 s） | **Fixed** | `PET-MOVEMENT.md:633`「方向 = **水平向右**、门量 `v = 1200 px/s`」＋「② 首触竖直速度 `1008 px/s` > 档界 800 ⇒ **非档内支** ⇒ ≤5 s 且**收口在地面**」；`:603`「（e = 0.55 实测 `rest ≈ 3.81 s`、`reason=atRest`、`snap=0`）」；回指 AC16 `:593`「近地软着陆档 = 首触 `\|vy_imp\| ≤ 800 px/s` ⇒ ≤4 s；其余 ⇒ ≤5 s 且**收口在地面**」 |
| ② | 🟡#17 US-24 / US-26 验收指针补全 | 需求档逐 US 指针 = 设计 §1.1 现行集合 | **Fixed** | `PET.md:223`「### US-24 甩抛与飞行（验收：`docs/design/PET-MOVEMENT.md` §3.1 AC1、AC2、AC3、AC15、AC16、AC17、AC18、AC19、AC20）」；`PET.md:243`「### US-26 位移单写者仲裁（验收：`docs/design/PET-MOVEMENT.md` §3.1 AC5、AC6、AC7、AC15）」——与设计 `:35`/`:37` 及本档 `:324-325` 同源 |
| ③ | 🟡#18 NFR-13 同源注记（不计入 6 处） | 注记在场 + 计数三处一致 | **Fixed** | `PET.md:372`「**同源注记（2026-09-18；B20 评审轮 2 #18）**」＋「本注记 = **口径澄清**，**不计入本批「6 处修订 / 限定注记」**」；`PET.md:438` 六处枚举仍为 US-3 / US-12 / NFR-1 / NFR-2 / US-26 / NFR-8；本档 `:430`「本批「修订 / 限定注记」计数仍为 6 处」 |
| ④a | 参数裁定 B · 参数表值 | 0.55 + 来源 + 待标定 + 差异登记 | **Fixed** | `PET-MOVEMENT.md:292`「\| `restitution` \| —（0~1） \| **0.55**（登记作 ≈ 0.55） \|」；`:568` O-12「本仓 `restitution` **0.55**（登记作 ≈ 0.55）vs 样本 `assets/config.jsonc:92` 的 **0.78**」；`:325` U-2 = ② 本仓调（已裁定 2026-09-18） |
| ④b | 档界 450 → 800 px/s | 设计 §1.1 / AC1④ / AC11③ / AC16 与需求档 NFR-19 四处同值 | **Fixed** | `PET-MOVEMENT.md:41`「近地软着陆档（首触竖直速度 ≤ 800 px/s）≤ 4 s + 只停一次 + 5 s 硬上界**收口在地面**」；`:588` AC11③；`:593` AC16；`PET.md:421`「判据 = **首触竖直速度 `\|vy_imp\| ≤ 800 px/s`** ⇒ 起抛后 **≤ 4 s** 内 `atRest`（`e = 0.55` 实测档内最坏 3.33 s；分界 ≈ 1000 px/s）」 |
| ④c | 128 例重算 + §2.12 追加节 | 74 / 44 / 10 三处同源；扫描口径钉死；计数重述 | **Fixed** | `PET-MOVEMENT.md:588` AC11⑤「基准 ≤4 s 74 · 4–5 s 44 · 上界 10」＝ `PET.md:425` ＝ 本档 `:414-416`；本档 `:375-380`（1920×1040 / 4 起点 / 8 方向 / {500,1200,2400,3600} / 16 ms tick）；`:382-425` 推导与两张重算表；`:434-435`（AC 20 / TC 37 / 观察项 12 / 档界 1 个 450→800） |
| ④d | 四个值自洽（参数表 / AC11③ / NFR-19 / TC-1 / TC-31 ↔ §2.12 重算数） | 逐值对表 | **Fixed** | 0.55 = `:292`；800 = `:41`/`:578`/`:588`/`:593`/`PET.md:421`；档内最坏 3.33 s = `:397`；74-44-10 = `:414-416`；TC-1 3.81 s = `:421`；TC-31 3.36 s = `:422`；首触 1008 / 1078 = `:420-421` —— **逐项一致** |
| 🔵 | 附注（不阻断；不参与 ①②③④ 判定） | §2.12 四(2) 与 四(4) 的「步 50」稠密例数不一致（2880 vs 2304；差 = 是否含第 5 起点 `(0,0)`） | Note | `:395`「\| 步 50 \| 2880 \| 3.26 s \| 1023 px/s \|」 vs `:417`「\| 稠密（步 50，2304 例）≤ 4 s 占比 \| 11.0%（上轮记 36%，**构成不同**） \| **57.6%** \|」——该两行不承载 AC11 / NFR-19 的承重数（承重数 = 128 例集的 74 / 44 / 10），仅建议补一句起点数口径 |

**计数**：🔴 0 · 🟡 0 · 🔵 1（附注）· 轮次 2 的 4 项声明（含 ④ 的四个值自洽）**全部验证 Fixed**。

VERDICT: pass

## §4 评审裁决与实施启动（主 agent）

### 4.1 实机发现与修偏轮（2026-09-18）

**D-1（实施偏差 —— 用户实机发现，非设计问题）**

- **用户报告（2026-09-18 12:03）**：「我看到菜单项目了，但是没有效果」——托盘「甩抛物理手感」可勾选、勾上后甩桌宠无任何反应。
- **主 agent 三段实证（非推测）**：
  1. `main.js` 组合根接线区（`:75-88`）—— 其余 **11 个域模块逐一有 `init({...})`**（`notifier` / `backend` / `geometry` / `drag` / `pet` / `affinity` / `mode` / `plugins` / `update` / `win` / `tray`），**唯独 `physics` 无调用**（`:80-82` 仅为三行注释 ✗）。
  2. 模块专属日志 `userData/pet-physics.log` **不存在** ✗（⇒ 模块从未运行）。
  3. `settings.json` 的 `petPhysicsEnabled = **true**` ✓（⇒ 托盘链路与落盘正常 ⇒ 故障点唯一）。
- **根因** = **`physics.init(deps)` 漏调用** ⇒ 模块收不到 5 个依赖（`getPetWindow` / `getPetDrag` / `getMoveTimer` / `settings` / `setPetState`，实读点 `shell-pet-physics.js:32-38`）⇒ `ipcMain` 监听未注册 ⇒ 采样器不启动 ⇒ 甩抛无反应。
- **修法**（不涉设计变更 —— 设计档 §2.7 本就要求该接线，属实施遗漏 ✓）：按 §2.7 注入面表，在组合根接线区补 `physics.init({...})`（与其余 11 模块同处）。
- **复验判据**：① 带 `BIGFISH_PET_DEBUG=1` 启动 ⇒ `userData/pet-physics.log` 出现 `phys-*` 行；② 实机推抛 ⇒ 起飞 / 弹跳 / 停泊 / 逐次挤压可见；③ 关闭开关 ⇒ 零日志、零定时器（AC8④）。
- **流程缺口（归台账 T37）**：本批 AC 集覆盖了「托盘有该项 / 设置落盘 / 两处 IPC 监听在场」，但**未覆盖「组合根是否真的调了 init」** ⇒ 组装面机检缺失（12 个域模块中任一漏调，现有 AC 全绿而功能全失 ✗）。

### 4.2 修偏轮 2（真因定位——2026-09-18；用户实机验收通过）

- **现象**（用户 2026-09-18）：勾选「甩抛物理手感」后拖拽正常、**甩出无任何反应**，且 `userData/pet-physics.log` **不存在**。
- **主 agent 外部定位（逐项排除）**：拖拽事件到达 app ✓ · 调试开关生效 ✓ · 接线在位 ✓（`main.js:96`）· 开关为真 ✓（`settings.json`）· 绑定在跑 ✓ ⇒ 仅剩三类：处理器未真正调用 / 进去就早退 / reason 面。
- **真因（eng-coder 离屏复现实证）**：`handleTrailEnd(reasonRaw)` 为**单参签名** ✗，而 `ipcMain` 派发形态 = `(event, reason)` ⇒ 处理器把 **IpcMainEvent 对象**绑进 `reasonRaw` ⇒ 归一化正则对 `[object Object]` 恒假 ⇒ 静默清轨迹早退（零日志零异常）—— 与现象完全吻合。**反证**：同通道冻结面 `drag.handlePetDragEnd(_e, reason)` 双参 ⇒ 拖拽侧一直正常。
- **性质**：**非设计面缺口**（设计档 §2.2.1 数据流本就规定 `pet-drag-end(reason)` 直达本模块）⇒ 属实施签名缺陷，无需重新评审。
- **修复**（双参签名 · `typeof` 归一 + 非法分支产 `phys-stop reason=bad-reason` · 增设 `phys-trail-start seq=<n>` · 补 G6 前置耦合注释）详见 §5。
- **验收**：用户实机 + 机器取证双证，见 §6.1。

---

## §5 实施记录（eng-coder）

### 5.0 续做轮与验收补齐（eng-coder，2026-09-18）

**背景**：上一轮 eng-coder 会话进程重启死亡——代码已全部落盘、未出交付报告、未跑自审与内部评审。本轮 = 在既有实现上完成验证链（不重写已验证成果）。

#### 5.1 交付面（11 档 git 面 + 2 档 gitignored 工具）

| # | 文件 | 动作 | 要点 |
|---|---|---|---|
| 1 | `pet-physics-core.js` | 新建（334 行） | 纯函数核心：PHYSICS 参数表（restitution 0.55 = 裁定 B）/ 常量表 / trimTrail / gatePeakSpeed / isQuietPlacement / canTakeOff / estimateReleaseVelocity（四类 null）/ throwStep（landed 标志）/ landingSquash / squashScale / decideOwnership / armGate（G1–G11）；双环境导出；零 require / 零 fs / 零定时器 |
| 2 | `shell-pet-physics.js` | 新建（309 行） | 域实现：采样器（16 ms）/ evaluateArm（setImmediate）/ armFlight（起飞归一化 + prevGrounded 初值）/ flightTick（抢占首行 + 同目标去重）/ stopFlight（停泊序列 + timeout 强制落地 snap）/ handlePhysicsToggle / registerScreenStops / quit；日志 `pet-physics.log`（BIGFISH_PET_DEBUG=1） |
| 3 | `shell-pet.js` | 改（+16/−1） | doWander 守卫 `|| physics.isFlying()`（1 处）/ physics.init 注入点（shell-pet.js:29-35）/ destroyPetWindow:221 + clearPetTimers:247 停点 |
| 4 | `main.js` | 改（+8） | require:38 / 接线注释区 / registerScreenStops:179（whenReady 内、几何三条 screen.on **之前**，E7）/ before-quit:210 停点 |
| 5 | `shell-ipc.js` | 改（+6） | pet-drag-start / pet-drag-end 两条本模块自有监听（:26-27，注册点唯一；既有绑定行零改动） |
| 6 | `shell-tray.js` | 改（+11） | 「甩抛物理手感」checkbox（open-1 ①；open-2 ① 专注模式置灰）+ setPetPhysics（落盘 + handlePhysicsToggle + rebuild） |
| 7 | `shell-settings.js` | 改（+1） | `petPhysicsEnabled: false`（U-1 ② 默认关） |
| 8 | `pet.html` | 改（+19/−6） | `#pet-squash` 包裹层（包 #pet-stage + #pet）+ reduce-motion 分支 + `<script src="pet-physics-core.js">`（曲线单一来源） |
| 9 | `pet.js` | 改（+22） | onPhysicsSquash → playSquash（rAF 逐帧 scaleY；曲线/时长取 window.PetPhysicsCore 按名引用） |
| 10 | `pet-preload.js` | 改（+1） | 暴露 `onPhysicsSquash`（通道 `pet:physics-squash`，O-5 规则句形态） |
| 11 | `package.json` | 改（+2） | `build.files` 增列两个新源档；dependencies 零 diff |
| 12 | `.thincoder/b20-pet-physics-stub.mjs` | 新建（389 行，gitignored） | 开发期桩测（112 断言，装载真实核心）；另有开发期自检工具 `b20-static-checks.cjs`（64 断言）/ `b20-smoke-load.cjs`（同 gitignored 区，非交付物） |

#### 5.2 本轮补齐的改动（相对上一轮落盘形态；其余全部为上一轮已存在成果，零重写）

| # | 位置 | 改动 | 理由 |
|---|---|---|---|
| 1 | shell-pet-physics.js:186-189 | flightTick 首行 drag-preempt 分支补 `phys-stop reason=drag-preempt` 取证行（先于 stopFlight） | §2.2.2 交接② / §2.2.10 行 6 的字面判据「drag-preempt 之后无 phys-tick wrote=1」——原实现只有 phys-rest 行，phys-stop 行缺失 |
| 2 | shell-pet-physics.js:121-128 | evaluateArm 的 G4 分支不再换词（`drag-active` 照抄词表，删去对 drag-preempt 的误映射） | §2.2.11 词表 14 个中两者并存（drag-active = G4 未飞面 / drag-preempt = 抢占面）；原实现把 G4 的 reason 改写成 drag-preempt ⇒ 词表失真 |
| 3 | shell-pet-physics.js:277-279 | handlePhysicsToggle(false) 补 `stopTrailSampler()` | 评审 🔵：拖动中关闭时采样器残留至 drag-end 自停——AC8④/TC-23「零定时器」严格化（1 行） |
| 4 | shell-pet-physics.js:298 / :274 注释 | quit() 与显式停点注释按实际行为改写（quit 在窗口存活时步骤 3 照常执行——对将死窗口无害） | 评审 🔵：注释与行为不符（行为本身正确不动） |

#### 5.3 验证实测（亲跑输出）

- 桩测：`node .thincoder/b20-pet-physics-stub.mjs` ⇒ **112/112 PASS**
- 静态机检：`node .thincoder/b20-static-checks.cjs` ⇒ **64/64 PASS**
- 装载烟测：`node .thincoder/b20-smoke-load.cjs` ⇒ 导出面 8 符号 / 核心导出 35 符号 / restitution 0.55 / T 500
- `node --check`：main / pet / pet-preload / pet-physics-core / shell-pet / shell-pet-physics / shell-ipc / shell-settings / shell-tray / 桩测 ⇒ 全绿
- 零 diff 面：`git status --porcelain` 恰 11 档（写域内）；`git diff --stat` 对 shell-pet-geometry.js / shell-pet-drag.js / pet-chain.js / pet-chain-core.js / assets/** / docs/** ⇒ **空输出**；`pool.json` weights.move = 0
- 行宽/行数：11 档 + 桩测全部 ≤300 宽 / ≤500 行（最大 shell-pet.js 438 行）；文件头三判据（新档 2 + stub）全过
- 静态门：squashScale / SQ_DURATION_MS / TAKEOFF_MIN_SPEED / estimateReleaseVelocity / throwStep / decideOwnership / armGate / landingSquash 全仓**定义数 == 1**（均在 pet-physics-core.js）；pet.js 按名引用 window.PetPhysicsCore
- 飞行期取屏 0 次：petCurrentDisplay 仅 :105（arm 路径）+ 注释 :6；flightTick 体内零 petSavePos / 零 petCalibrateSize / 零 getSize
- 顺序门：getPetDrag 判定(185) < drag-preempt 日志(186) < setPosition(201)；clearInterval(246) < petSettlePos(259)；main.js registerScreenStops(179) < 几何三条 screen.on(180-182)

#### 5.4 偏离审计（内部 explore 子代理，1 轮）——结论 DEVIATIONS（无 🔴）

已披露偏离 A/B/C 成色核验**全部通过**（各有设计档其他节明文支持）：
- **A**（G11 先于 G10 评估）：§2.2.12「一律判放置」语义要求静默支先行；TC-35/TC-36 两支可区分断言在场。
- **B**（dock 集合 {atRest, timeout, toggle, error}）：§2.2.8 / §2.2.10 行 7 / TC-24 的明文要求（toggle/error 走停泊序列）——§2.2.6 步骤 2 字面集合属设计档未同步面，**建议行**：设计者下轮补齐措辞。
- **C**（drag-preempt 两行并存）：两行均在 §2.2.11 词表内，运行时判据由首行自停保证。

另 3 条 🔵 字面级差异：physics.init 落点在 shell-pet.js（经 main.js 间接，行为等价）；quit 在窗口存活时步骤 3 照常执行（无害）；pendingRelease 未物化快照（G4 兜底等价）。**建议行**（文档面，归主 agent/设计者）：A/B 的裁决记录 + 设计档 §2.2.6 dock 措辞同步。

#### 5.5 内部代码评审（内部子代理，1 轮）——结论 pass（无 🔴）

10 个重点判面（dt 漂移 / timeout 双写 / lastApplied 基准 / sessionSeq 竞窗 / G3 幂等 / stale 分支 / atRest 第二支 / quit 路径 / 注册顺序 / #pet-squash 插层）9 项确认无误 + 1 项为规格级观察项。处置：#3（toggle 停采样器）已修；#4（quit 注释）已修；#1（atRest 第二支「贴墙即停」与样本 physics.ts:310 逐字同形）= 规格继承，不改代码，**登记为实机标定轮观察项**；#5（snap 写不查去重，幂等）/#6（reduce-motion 下 rAF 空转，U-9 由 CSS 分支承担）/#7（getCursorScreenPoint 无 try/catch，与冻结面既有同款同形）= 登记，不阻断。

#### 5.6 残余风险与未覆盖面（如实标注）

1. **观感类无机器判据**（Q 弹好不好看 / 手感轻重 / 弹跳可感性）= 人工目视项——本会话无实机（T9 未验）。
2. **实机链路**（拖动→甩→飞→停泊 + 日志面 `phys-stop/phys-arm/phys-rest` 行为 + 跨屏拖动）待用户环境亲跑；判据已由设计档 §3.3 限制节登记。
3. **AC11⑤ 分布**（≤4s 74 / 4–5s 44 / 上界 10）：桩测按 ±2 容差断言通过（实跑值在 72–76 区间内为绿）；与批次档 §2.12 重算数同源。
4. **真机手感面不在本轮闭环内** ⇒ restitution 0.55 / T 500 / 档界 800 均为「待实机标定」值（U-10 / O-12，到期条件已落档）。
5. AC10④ 拖动期 CPU 增量（NFR-1 <5% 预算）需实机 10 s 采样——同 2 待用户环境。

### 5.7 修偏轮 2（第 2 次实机修偏，2026-09-18）

**用户现象**：勾选「甩抛物理手感」后拖拽正常、**甩出无任何反应**（主 agent 已定位：`pet-drag.log` 有行 ✓ · `pet-physics.log` 不存在 ✗ · `PET_DRAG_DEBUG` / `BIGFISH_PET_DEBUG` 同条命令生效 ✓ · `main.js:96` 真实 `physics.init` ✓ · `shell-ipc.js:26-27` 绑定在位 ✓ · `settings.json` `petPhysicsEnabled=true` ✓）。

**真因（离屏 Electron 复现钉死，探针输出 `probe arg1=IpcMainEvent arg2="pointerup"`）**：`ipcRenderer.send(ch, reason)` 到主进程 `ipcMain` 监听器的派发形态 = **(event, reason) 双参**，而 `shell-pet-physics.js` 原 `handleTrailEnd(reasonRaw)` **单参签名**把 IpcMainEvent 对象绑进 `reasonRaw` ⇒ regex `.test(event)` 强转 `"[object Object]"` 恒假 ⇒ G6 归一化必拒 ⇒ **静默清轨迹早退，零日志零异常**。冻结面同通道处理器 `drag.handlePetDragEnd(_e, reason)`（`shell-pet-drag.js:176`）双参所以拖拽正常——恰好反证根因。(a) 注册断与 (b) settings undefined 均被排除（register 不抛、market/affinity 绑定完好、init 注入 5 键完整）。**非设计面缺口**——设计档 §2.2.1 数据流本就规定 `pet-drag-end(reason)` 到达本模块，属实施签名缺陷。

**改动（git 面 = 仅 `shell-pet-physics.js` +18/−4，其余零 diff）**：

| # | 位置 | 改前 → 改后 |
|---|---|---|
| 1 | `shell-pet-physics.js:94-99` | `function handleTrailEnd(reasonRaw)` → `function handleTrailEnd(_event, reasonRaw)`（根因修复，与冻结面同通道同形）+ JSDoc 记录根因与复现证据 |
| 2 | `shell-pet-physics.js:102-109` | `reasonRaw` 直接进 regex → 先 `typeof` 归一（非字符串 ⇒ `''`）；非法分支从静默零日志 → `phys-stop reason=bad-reason pos=…` 取证行（词表 14 个的既有项，本轮前无产出行） |
| 3 | `shell-pet-physics.js:73-75` | handleTrailStart 加 `phys-trail-start seq=<n>` 诊断行（置于 G1 早退之后 ⇒ 关闭态零日志保持 AC8④/TC-23）——下次「甩了没反应」不必外部推断 |
| 4 | `shell-pet-physics.js:127` | `reasonOk: true` 补耦合注释（G6 前置执法点在 handleTrailEnd；删前置须同步传归一化结果） |

**验证实测（亲跑）**：① `node --check shell-pet-physics.js` ✓；② `node .thincoder/b20-pet-physics-stub.mjs` ⇒ **112/112 PASS** ✓；③ `node .thincoder/b20-static-checks.cjs` ⇒ **64/64 PASS** ✓；④ 离屏复现（`BIGFISH_PET_DEBUG=1 npx electron .thincoder/b20-repro-trail.cjs`）⇒ 修复前 `log-lines 0`（复现用户症状）→ 修复后 `phys-trail-start → phys-arm vel=(-2080,0) trail=8 → phys-land/phys-squash ×8 → phys-tick → phys-rest reason=atRest flight=1892`（全链路）+ bogus reason ⇒ `phys-stop reason=bad-reason` 恰 1 行 ✓；⑤ 冻结面 `git diff -- shell-pet-geometry.js shell-pet-drag.js pet-chain.js pet-chain-core.js assets` ⇒ 空输出 ✓；⑥ 行数 323 ≤500 / 行宽 ≤300 零超 ✓。

**实机验收判据（供主 agent 复验）**：`BIGFISH_PET_DEBUG=1` 启动 → 拖拽桌宠快甩松手 → `%APPDATA%\Bigfish\pet-physics.log` 应出现 `phys-trail-start seq=…` + `phys-arm vel=…`（快甩）或 `phys-stop reason=…`（缓放/静默）+ `phys-rest reason=atRest`；窗口应可见飞行/弹跳/挤压动画。

**内部审计与评审**：explore 偏离审计 1 轮 ⇒ DEVIATIONS（代码语义零偏离、无 🔴；3 条 🔵 全在文档面）+ 内部代码评审 1 轮 ⇒ **pass**（🟡1/🔵2，#2/#3 已修落注释，#1 Deferred 见上报项）。终态 = **clean**。

**上报项（写权在父侧/设计者，本段披露）**：① `phys-trail-start` 新 tag 未在设计档 §2.2.11 tag 表（写权 eng-designer）——建议补一行 tag 定义；② 修偏轮 2 的 §4 条目（D-2）未落（§4 写权主 agent）；③ 本轮开发期工装 `.thincoder/b20-repro-trail.cjs`（离屏复现，gitignored 非交付物，同 §5.1 表 12 行先例披露形态）。

**范围外注记（未触碰）**：工作树中 `docs/TODO.md` / `docs/batches/B16-test-gates.md` / `dsh-bundle/package.json` + `package-lock.json` 4 档为既有他批改动，非本轮所改。

## §6 验收核销（主 agent）

### 6.1 验收结论（2026-09-18）：**通过 ✓**

**人可观察面（用户实机）**：用户确认「非常好的设计，**我已经能运用重力模式了**」⇒ 飞行 / 弹跳 / 落地挤压均可见 ✓。

**机器取证（主 agent 亲读 `userData/pet-physics.log` 实测）**：

| 事件 | 计数 | 判据 |
|---|---|---|
| `phys-trail-start` | 12 | 处理器被调用 ✓ |
| `phys-arm` | 10 | 起飞（快甩过门）✓ |
| `phys-tick` | 95 | 飞行推进 + 写窗 ✓ |
| `phys-land` / `phys-squash` | 107 / 107 | 落地 + 挤压 ✓ |
| `phys-rest` | 10 | 静止停泊 ✓（末条 `reason=atRest flight=4529 steps=172`）|
| `phys-stop` | 3 | **未过门槛被正确拒** ✓（门非来者不拒）|

- **修偏轮合计**：D-1（`physics.init` 漏接线）+ D-2（`handleTrailEnd` 单参签名）；**两次均为实机发现、非设计问题** ⇒ 未触发设计重审。
- **父侧形态规范（已披露，两处标题）**：① §5 内子代理所写重复标题 `### §5 实施记录（eng-coder）——续做 + 补齐验收轮` ⇒ 规范为 `### 5.0 续做轮与验收补齐（eng-coder，2026-09-18）`（**它是 5.1–5.6 的父节**；首版误编为兄弟节号，本轮已纠正为父节号）；② `### §5.7 修偏轮 2` 的标题前缀 `§` 与同段兄弟节形态不一致 ⇒ 去掉 `§`。**两处仅改标题行，内容零改** ✓。
- **残余（不阻断核销）**：✅ **已闭环** —— 设计档 §2.2.11 事件词表已补 `phys-trail-start`（**新事件**，表数据行 7 → **8**）+ `bad-reason` **产出路径**标注（reason 词表仍 **14** 条，未新增词）+ §四 变更记录 1 行；`docs/design/PET-MOVEMENT.md` 668 → **673** 行，行宽 0 超。
- **归档判定**：本批**可归档** ✓（用户实机 + 机器取证双证）。

### 6.2 台账联动

- 本批派生条目 **T37**（组装面机检缺失）已登记，处置挂 **B16**（实施中 ✓）；无其它派生条。
- 本批无关联需求池条目（R 类）⇒ 无需求池勾销面。
