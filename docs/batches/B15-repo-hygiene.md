# B15 批次档 —— 仓库卫生（结构整理：EOL 固化 / 散档归位 / 垃圾清点）

> 六段制 · **一段一作者（append-only）**：§1 / §4 / §6 = 主 agent · §2 = eng-designer · §3 = 评审子代理 · §5 = eng-coder。
> 本档只登记**本仓**范围；需求档落点 = **无**（工程/流程类需求，依 B11 先例）；设计档落点 = `docs/design/REPO-CONVENTIONS.md`（B11 已立，本批**修订**该档，不新建）。

---

## §1 立案（主 agent）

### 1.1 目标（一句话）

**把仓库里的「杂物」收干净 —— 让后来者不在噪音里找东西，且这次整理绝不改变任何运行时行为。**

### 1.2 任务来源

- 归属：**R11（接手项目规范化）② 文档补齐 / ③ 代码体检** 的**机械面**。
- 用户 2026-09-18 裁定 **A**（结构调整 = **目录 / 文件结构要整理**）⇒ 机械面 = 本批；认知面（把结构讲清楚）= **B25**。
- 已登记的具体条目：**T24**（EOL 固化）· **T34**（散档归位）· **T35**（垃圾目录清点）· **T36**（不搬立场）。

### 1.3 现状实测（证据，2026-09-18）

| # | 实测 | 证据 |
|---|---|---|
| 1 | 根目录**自研档平铺 49 档**；**8 档 `probe-*` + 5 档构建脚本**（`afterPack.js` / `download-electron.js` / `download-node.js` / `make-icons.js` / `make-latest.js`）混在运行时档里 | 实测根目录清单；台账 **T34** |
| 2 | **`build.files` = 43 条扁平路径**（非目录通配）⇒ 发行包白名单是逐条列出的 | 实测 `package.json` `build.files[0..41]`；台账 **T36** |
| 3 | **`build.files` 内零 probe 条目** ⇒ 8 档探针**不进发行包** ⇒ 可安全搬迁 | 实测（同上） |
| 4 | 存在 `测试-更新功能/`（**中文名 + 仅 1 项**，用途未入档） | 实测；台账 **T35** |
| 5 | **EOL 口径只依赖本机 `core.autocrlf`**，仓内无 `.gitattributes` | 台账 **T24** |
| 6 | `probe-pet-media.js` **硬绑 `main.js`**（搬迁必连带改） | 台账 **T29** |

### 1.4 范围（做 / 不做）

**做**：

1. **EOL 固化**（T24）：新增 `.gitattributes`，口径 = `* text=auto eol=lf`（与 `.editorconfig` 的 LF 声明同源）。
2. **散档归位**（T34）：8 档 `probe-*` + 5 档构建脚本归位到 **`scripts/`（已存在）或其子目录**；连带改：① 各档内部相对 `require` 的**深度** ② `probe-pet-media.js` → `main.js` 的硬绑（T29） ③ 各档**文件头的设计档指针**（R11 ② 的「未入档」由此一并解决）。
3. **垃圾清点**（T35）：`测试-更新功能/` 清点后三选一 → **删 / 归位 `tests/` / 归档**（在 §2 写明选择与依据）。
4. **迁移后机械守卫**（防回潮）：① `build.files` **仍为零 probe 条目** ② 全仓 `require` 面**零残留**（旧路径 grep = 0 命中）③ `npm start` 能起、既有桩测全绿。

**不做**：

- **不搬 `build.files` 内那 43 档**（T36 立场）：搬迁需连带改 43 条白名单 + 全部相对 require + 预载/渲染页路径 + `__dirname` 假设 ⇒ **收益仅观感、风险是发版面** ⇒ 本仓**接受「根目录平铺」**这一形态。
- **不改任何运行时行为**；**不改依赖**；**不删任何被引用的档**。
- **不做文档搬迁**（文档体系落点归其地图，D4 指针纪律）；**不写架构文档**（→ **B25**）。

### 1.5 硬约束（不得偏离）

1. **零行为变更**（纯位置 / 元数据）：判据 = 应用能起（`npm start`）+ 既有桩测全绿 + 项目门禁（lint → test:full → test:integration，就位后）全绿。
2. **不动 43 档白名单面**：`build.files` 的**条目集合**在本批前后逐条相同（顺序可变，集合不可变）。
3. **每次移动 = 全量核对**：移动后逐档 grep 旧路径（引用面 = 0 命中）；被移动档的相对引用深度全部修正。
4. **`.gitattributes` 只声明不「批量重写」**：本批只**声明口径**；如需重归一化（`git add --renormalize`）⇒ 单独列为一步并**事先报告**（可能产生大 diff）。
5. **零删除风险**：删除类动作（T35 若选「删」）须**先报告 + 用户确认**（不可逆）。
6. **行宽 ≤300 / 单档 ≤500 / 文件头标准形**（`docs/CONVENTIONS.md`）。
7. **排期**：**B19 / B20 实施交付之后**（两批在飞时移动文件 = 让它们的验收面全部失真）。

### 1.6 待决项（须裁定后才能定稿设计）

| # | 待决 | 备选 | 关键判据 |
|---|---|---|---|
| U-1 | **落点** | ① `scripts/` + `scripts/probes/`（**推荐**：`scripts/` 已存在）② `tools/` | 与既有 `scripts/` 的口径一致（该目录现有 `ensure-deps.js` 等）；避免新造顶层目录 |
| U-2 | **`afterPack.js` 能否移动** | ① 能（若 `build.afterPack` 未指向它）② 不能（指向则需同步改配置） | **需先核 electron-builder 的接线**（§2 勘察项）——这是本批唯一的「配置面」风险点 |
| U-3 | **垃圾目录处置** | 删 / 归位 `tests/` / 归档 | 清点其内容来源（若含用户数据 ⇒ 不删） |
| U-4 | **是否顺带做 T13**（行宽债 65 行） / **T25**（文件头迁移 20 档） | ① 只做本批四件（**推荐**：避免批次臃肿）② 一并清 | 本批性质 = 「位置与元数据」；T13/T25 是「内容改写」⇒ 宜独立或并入 B14/B17 |
| U-5 | **命名约定** | 归位后是否统一前缀（如 `probe-*` → 保持原名） | 保持原名（减少认知变更；文件名已是自解释的） |

### 1.7 关联台账与需求档

- **台账**：`docs/TODO.md` §一 **R11**（②③ 的机械面）；§二 **T24 / T34 / T35 / T36**（本批四件）+ **T29**（探针硬绑，属连带面）。
- **需求档**：**无**（工程/流程类需求，依 B11 先例）。
- **设计档**：`docs/design/REPO-CONVENTIONS.md`（B11 落档；本批 = **修订**——补「目录与归位约定 + EOL 固化」节）。

### 1.8 交付物与排期

- **交付物**：① `.gitattributes` ② 归位后的 13 档 + 连带 require/文件头修订 ③ 垃圾目录处置 ④ 机械守卫（零残留 grep + 桩测 + 能起）⑤ 地图 / 台账同步（主 agent）。
- **排期**：**B19 / B20 交付之后**（与 **B25** 同窗或其后；B25 是「讲清楚」，本批是「摆整齐」）。
- **预估**：设计面 = 1 轮设计 + 1 轮评审；实施面 = 位置移动 + 少量文件头/require 修订（**无行为改动** ⇒ 风险低，但**移动类批次必须全量核对**）。

### 1.9 变更记录

| 日期 | 变更点 |
|---|---|
| 2026-09-18 | 建档（B15 立案）：目标 / 来源（用户裁定 A + R11 ②③）/ 六条现状实测 / 四件范围与四条不做 / 七条硬约束 / 待决 U-1…U-5 / 关联台账与排期。依据 = 用户 2026-09-18「你自己设计」授权。 |

---

## §2 本批任务书（eng-designer）

<!-- 由 eng-designer 填 -->

---

### 2.1 本批条目（三方同源 = §1.4「做」四条 + T36 立场）

> 设计 = `docs/design/REPO-CONVENTIONS.md` **附 B（B15 面）**——需求层 B.1 / 设计层 B.2 / 测试层 B.3；本段 = 任务书摘要，逐条验收判据以附 B §B.3.1 为准。

| # | 条目 | 回指 |
|---|---|---|
| F1 | **EOL 固化**：新增根 `.gitattributes` = `* text=auto eol=lf`；只声明、不重归一化（如需 ⇒ 先报） | §1.4-1 · 台账 T24 |
| F2 | **散档归位（11 档）**：8 档 `probe-*` → `scripts/probes/`；`afterPack.js` / `make-icons.js` / `make-latest.js` → `scripts/`；连带 = require 深度 / `ROOT`·`__dirname` 基准 / 头指针（标准形）/ `package.json` 三行（`:22` / `:23` / `:44`） | §1.4-2 · 台账 T34 |
| F3 | **`测试-更新功能/` 清点**：**实测不在盘**（as-of 2026-09-20 双查）⇒ 无处置对象；`.gitignore:22` 条目保留为预防性声明；台账核销建议（主 agent/用户） | §1.4-3 · 台账 T35 |
| F4 | **迁移机械守卫**：`build.files` 集合零动 + 旧路径零残留 + 三绿 + 实机（pack / 探针） | §1.4-4 |
| F5 | **文档同步面**：6 档活文档（命令串 / 位置陈述 / 入口表 / §六 §八 §1.1）+ 本设计档附 B | 设计附 B §B.2.3 |
| — | **不做**：白名单内 49 档（T36）；`debug-pet.cmd` / `setup-linux.sh` / `install-desktop-entry.sh`（另裁）；`--renormalize`；门禁扩面；台账 / 地图 / `CHANGELOG.md`（主 agent 面） | §1.4「不做」+ T36 |

### 2.2 交付物

① `.gitattributes`；② 11 档归位 + 内部修正；③ `package.json` 三行；④ 6 档活文档同步；⑤ 机检 + 实机取证（AC-B15-1…11）。

### 2.3 受影响文件（写权标注；行数 / Δ 明细 = 附 B §B.2.3）

| 面 | 文件 | 写权 |
|---|---|---|
| 新建 | `.gitattributes` | eng-coder |
| 迁移 + 修正 | 8 档探针 → `scripts/probes/`；`afterPack.js` / `make-icons.js` / `make-latest.js` → `scripts/` | eng-coder |
| 配置 | `package.json` 三行（行数不变；`build.files` 零动） | eng-coder |
| 文档同步 | `docs/CONVENTIONS.md` · `docs/design/ARCHITECTURE.md` · `PET-ANIMATION.md` · `PET-MULTIMONITOR.md` · `PET-MOVEMENT.md` · `AUTO-UPDATE.md` | eng-designer |
| 台账 / 地图 / `CHANGELOG.md` | 状态 + T25 计数 20 → 10 + B15 行 + 条目 | 主 agent |

### 2.4 验收标准（逐条 = 附 B §B.3.1 的 AC-B15-1…11；此处为摘要）

- **AC-B15-1**（F1）：`.gitattributes` 在场 + `git status` 空 + 索引面 `i/crlf` 恒 0；
- **AC-B15-2**（F2）：11 条 rename + 旧路径无 + 根非包散档 26 → 15；
- **AC-B15-3**（F2）：11 档 `node --check` + require 解析扫描 0 未解析 + `ROOT` / 基准断言 + make-latest 沙箱正负例；
- **AC-B15-4**（F2 / T36）：三行新值 + `build.files` 排序集零 diff；
- **AC-B15-5**（F4）：G2 引用面 0 + G4 活文档跑法 0；
- **AC-B15-6**（F2）：`npm run pack` 含 `[afterPack] applied icon + version to`（拒收 skipping）；
- **AC-B15-7**（F4）：探针 2 档实跑（`probe-pet-media` 耦合面 + `probe-displays` 零依赖面）；
- **AC-B15-8**（F5）：6 档同步点落地（§六 固化条 / §八 新路径 + 8 档 / §2.6 跑法 8 命中 / 前瞻注改「已落地」/ 命令串更新 / 变更记录各 1 行）；
- **AC-B15-9**（全局）：运行时面零 diff + 三绿 + 冒烟；
- **AC-B15-10**（F3）：订正登记 + 核销建议行；
- **AC-B15-11**（T36）：白名单 49 档 + 其余散档零改动。

### 2.5 排期与前置

- **实施 = B35 收口之后**（`package.json` 为 B35 零 diff 冻结面——B35 §2.2；§1.5-7）；开工前核 `git status` 干净、无在飞批次占用 `package.json`。
- 避让：另一会话写域（市场 / 桌宠 / physics 面）不在本批改动面；文档同步轮（第 ⑦ 步）须在各受影响设计档无在飞评审窗口时进行（D5）。

### 2.6 实施步序（= 附 B §B.3.3）

① `.gitattributes` → ② `git mv` ×11 → ③ 逐档修正（附 B §B.2.2 ② 表 + 证据注）→ ④ `package.json` 三行 → ⑤ 机检守卫 G1–G5 + 注入 / 移除双向实测 → ⑥ 实机（pack + 探针 2 档）→ ⑦ 文档同步轮（eng-designer）→ ⑧ 主 agent 收口（台账 / 地图 / CHANGELOG / §6）。
提交 = 1–2 次均可（信息含 `B15`；§七「一次提交一件事」）。

### 2.7 建议行（主 agent；本设计者不落）

- 台账：T24 / T34 / T35 状态推进（T35 = 核销建议，证据 = 实测不在盘）；**T25 计数 20 → 10**（B15 迁移 10 档：7 档探针 + `afterPack` + `make-icons` + `make-latest`）；T29 已订正注。
- 新发现登记（建议）：**`make-icons.js` 依赖 `sharp` 不在依赖树**（`package-lock.json` 零命中、`node_modules` 无）⇒ `npm run icons` 现即不可跑（非 B15 引入；B15 该档验证 = 静态面）——建议立技术待办或用户裁定。
- 计数对账族（建议归 T31 / B14）：`CONVENTIONS.md` §1.1 / §零 三分计数过期（实测码档 68；`probe-pet-media.js` 未被三清单收录）；`ARCHITECTURE.md` §2.7 记 `build.files` 46 条 vs 实测 49 条。
- 地图：B15 行状态 + 「8 + 3」口径；`REPO-CONVENTIONS.md` 行可补「后附 B16 / B29 / B15 三面」注（可选）。
- `CHANGELOG.md`：B15 条目（收口轮）。

### 2.8 评审关注点（供 §3 用）

- 五组选型对比（附 B §B.2.1）采纳与否；DD-B15-1…10；**U-4 裁定面**：T13 / T25 未搬迁面明确留外（本批只做搬迁档 10 档的头迁移——T25 计数同步见 §2.7）；**U-2 裁定面**：`afterPack.js` 迁移 + `npm run pack` 实机复核（配置面唯一风险点）。
- open ②–⑥（附 B §B.2.7）逐条是否需在本批处理。

### 2.9 变更记录

| 日期 | 变更点 |
|---|---|
| 2026-09-20 | §2 落档（设计轮）：条目 F1–F5 + 交付物 + 受影响文件（写权）+ AC 回指 + 排期前置 + 步序 + 建议行 + 评审关注点；设计档 = `docs/design/REPO-CONVENTIONS.md` 附 B（B15 面）。 |

### 2.10 修正轮 1 记录（eng-designer，2026-09-20；append-only）

- **源**：评审轮 1（§3 轮次 1：🔴0 / 🟡3 / 🔵6 = 9 条）+ 父侧裁决——#1–#8 = Fixed（本轮已落）；#9 = Not an issue（`docs/CONVENTIONS.md:175-176` §八 措辞现状即可 ⇒ 零动作，不改 §八 结构）。
- **#1（300 档结论）**：设计档 `:1084` 补句——`probe-pet-media.js` 397 行、本批仅 `git mv` + 注释 / 基准级改动（Δ ±1）、职责单一 ⇒ 不拆分（照附 A-续 `:824` 先例形态）。
- **#2（`git status` 空态）**：三处统一为「除 `.gitattributes` 自身暂存条目外为空」——设计档 `:924`（NFR-B15-7）/ `:1045`（§B.2.2 ⑤）/ `:1131`（AC-B15-1）。
- **#3（排期口径）**：设计档 `:925`（NFR-B15-8）改以 `docs/batches/B35-pet-gallery-ui.md` §2.2 为准（§1.5-7 仅作下限，注明其文本含 B19 / B20 前置、已被 B35 面取代）。
- **#3 建议行增补**（主 agent 面；本设计者不落）：地图 B15 行 / 台账 T34 行 → 排期口径同步「B35 收口之后」。
- **#4（白名单计数族）**：设计档 `:1121`（④）/ `:1123`（⑥）增列——地图 B15 行（`docs/README.md:49`）与本档 §1.3-2（`:25`）记 43 ⇒ 实测 49（族口径 = 43 / 46 → 49）。
- **#5（AC-B15-4 计数口径）**：设计档 `:1134` 补「行数不变（按 `\n` 计数 = 144）」。
- **#6（探针行数对位）**：设计档 `:1056` 改按档名对位（8 档逐名 + 行数；映射注 = `docs/design/ARCHITECTURE.md` §2.6）。
- **#7（配置面确定性）**：设计档 `:955` 补解析链实读（`.` 前缀走显式 `path.resolve`；非 `.` 前缀先 `require.resolve` 失败后回落）⇒ `build.afterPack` 新值改取 `./scripts/afterPack.js`（`:1020` ③ 表同步；`:1014` ③ 标题注同步）。
- **#8（`package.json:62` 缩进）**：纳入本批——设计档 `:1023`（③ 注）/ `:1058`（B.2.3）/ `:1167`（B.3.3 ④）/ `:1122`（B.2.7 ⑤ 状态改写）四处落点。
- **§2 存量段注**：本档 §2.3 / §2.6 的对应增量（探针行数 = 同值改呈现层；④ 步 = 三行 + `:62` 缩进修正）以设计档 §B.2.3 / §B.3.3 为准（§2 历史段不改写——append-only）。
- **边界**：T36 立场 / §1.4 做·不做 / U-1…U-5 裁定 / 五组选型结论 / DD-B15-1…10 编号零改动；未夹带新语义 / 范围 / 排期结论（#9 免动）。
- **D6 回读 + 门禁**：设计档 8 条发现对应落点（共 18 处行级修订，含为守 ≤300 行宽的回修 1 处——`:1167`）+ 变更记录 1 行（`:1198`）已逐处回读核实；`npm run lint` = **PASS（checks=7 · selftest=20/20）**。（行号 as-of 2026-09-20，只作定位参考——D4。）

- **补注（同轮，如实标注）**：完稿复跑 `npm run lint` = **FAIL**（`VIOLATION lines pet-unlock-core.js :: lines=524 limit=500`）——该档**非本批写域**（git status 显示为另一会话在改的在飞面）⇒ 如实标注、不代修；上一行的 PASS 回执 = 本设计档改毕时点实测（两连跑，均 checks=7 · selftest=20/20；本批两档文件在 width 判据面零违规——REPO-CONVENTIONS.md 实测 0 行 >300）。

## §3 设计评审（评审子代理）

<!-- 由评审子代理填 -->

---

### 轮次 1（评审子代理）

设计评审（对象 = `docs/design/REPO-CONVENTIONS.md` 附 B（B15 面）+ `docs/batches/B15-repo-hygiene.md` §2；范围文档 = 附 B · 附 A / 附 A-续（活契约）· `docs/CONVENTIONS.md` · `docs/README.md`）。核查 = 逐条对读 + 行锚 / 计数 spot-check。

计数：🔴 0 / 🟡 3 / 🔵 6。

| # | Category | Severity | Issue | Suggestion |
|---|----------|----------|-------|------------|
| 1 | 受影响文件面（判据 8） | 🟡 | 归位档中 `probe-pet-media.js` = **397 行**（实测 1..397；`docs/design/ARCHITECTURE.md:300` 同值 ✓）跨「>300 行」档，但 11 档的唯一形态结论 = `docs/design/REPO-CONVENTIONS.md:1081`「贴线档 / 超限档拆分计划：无（11 档均 <500 行；本批不改 ≥480 档）」——只覆盖 480/500 档，未给 300 档评估句（同档先例 = 附 A-续 `:824` 的「300 档结论」） | 补一句 300 档结论（照 `:824` 形态）：本批 = `git mv` + 注释 / 基准级改动（Δ ±1）、职责单一（单档探针）⇒ 不拆分 |
| 2 | 清晰度 / 验收一致性 | 🟡 | 同一判据两处措辞可分叉：NFR-B15-7 `:924` 与 B.2.2 ⑤ `:1043` 写 `git status --porcelain`「空」/「应空」，AC-B15-1 `:1128` 写「空（**除该档自身暂存**）」、TC-B15-01 `:1144` 写「仅该档 1 条（`A`）」；`git add .gitattributes` 后状态必含该档一条 `A` ⇒ 按前两处字面执行会误判红 | 三处统一为「除 `.gitattributes` 自身暂存条目外为空」，或 NFR-B15-7 改指 AC-B15-1 |
| 3 | 跨档一致性（排期口径） | 🟡 | 附 B NFR-B15-8 `:925` 与 `docs/batches/B15-repo-hygiene.md:135` 定「实施 = **B35 收口之后**」；而立案档 §1.5-7 `B15-repo-hygiene.md:54` 记「**B19 / B20** 实施交付之后」、地图 B15 行 `docs/README.md:49` 与台账 T34 `docs/TODO.md:43` 同行——两套前置并存；且 NFR-B15-8 把更严闸门写作「§1.5-7 + B35 §2.2」，而 §1.5-7 文本不含 B35；建议行（B.2.7 ① `:1115`）未含排期句同步 | ① 引用改为以 `docs/batches/B35-pet-gallery-ui.md` §2.2 为准（§1.5-7 作下限）；② 建议行补「地图 B15 行 / 台账 T34 排期口径 → B35 收口之后」 |
| 4 | 计数口径（跨档） | 🔵 | `build.files` 现状 **49** 条（实测 `package.json:46-94`），地图 B15 行 `docs/README.md:49` 与立案档 §1.3-2 `B15-repo-hygiene.md:25` 仍记 **43**；设计只点了 ARCHITECTURE 的 **46**（B.2.7 ④ `:1118`）与地图「8 + 5」（⑥ `:1120`） | ④/⑥ 增列两处 43 → 49（或建议行写「白名单计数族：43 / 46 → 49」） |
| 5 | 判据口径 | 🔵 | AC-B15-4 `:1131`「行数 **144** 不变」未写计数口径：该档末行为空行（`\n` 计数 = 144 · 非空行 = 143），两读并存易误红 | 判据补口径（「按 `\n` 计数 = 144」）或改写「三行在位替换 ⇒ 行数不变」 |
| 6 | 受影响文件表呈现（判据 8） | 🔵 | 探针 8 档行数以**未映射档名**的多重集给出（`:1054`：55 / 77 / 397 / 70 / 56 / 117 / 53 / 68），逐档契约表该 7 行填「—」（`:1006`）⇒ 逐档核对须回 ARCHITECTURE §2.6 | 行数按档名对位（或注明映射见 `docs/design/ARCHITECTURE.md:293-300`）；本轮已逐值核对一致，非数值错误 |
| 7 | 配置面确定性 | 🔵 | 组 2 证据注 `:954` 结论成立（实读 `node_modules/app-builder-lib/out/util/resolve.js:30-53`：`.` 前缀走 `path.resolve(p)`；非 `.` 前缀先 `require.resolve` 失败后回落 `path.resolve`）——现选值 `"scripts/afterPack.js"` 依赖**回落分支** | 实施可取 `./scripts/afterPack.js`（走显式 `path.resolve` 分支）；或证据注补回落链一句 |
| 8 | 评审关注点（批次档 §2.8） | 🔵 | 需求覆盖（F1–F5 ↔ AC-B15-1…11 ↔ TC-B15-01…14 ↔ G1–G7 ↔ 台账 T24 / T34 / T35 / T36 / T29）逐条可追、无缺口；五组选型（B.2.1）/ DD-B15-1…10 / U-2 / U-4 核对**无阻塞**——U-2 风险点已由 AC-B15-6 实机 + TC-B15-11 拒收静默分支封口（`build/rcedit-x64.exe` + `build/icon.ico` 实测在场 ⇒ 正例可达；门禁宽基线只含 `.md`、移动不触「陈腐即红」）；open ② 已由台账 T63 `docs/TODO.md:37` 承载；③/④ 留建议行得当 | 唯一动作建议：open ⑤（`package.json:62` 缩进 4 空格 vs 邻居 6）**随批一行修正**——本批恰在该档动笔 |
| 9 | Document ownership | 🔵 | 核对通过：EOL 口径唯一详述处仍 = `docs/CONVENTIONS.md` §六（`:145-157`；其「建议另批加 `.gitattributes`」句已列入 AC-B15-8 ✓）、归位布局详述处 = 附 B B.2.2（NFR-B15-4 ✓）、不新建档（DD-B15-10，与附 A / A-续 同形）、地图零新增行 ✓ | 轻微重叠：`docs/CONVENTIONS.md:175-176` 将同时承载「位置 + 8 档计数」与附 B 布局契约 ⇒ 建议用「新路径 + 计数 + 回指附 B」措辞（AC-B15-8 已近此形态） |

已 spot-check 通过（无发现）：`package.json` 三行行锚 `:22` / `:23` / `:44` 逐字相符 ✓ · `build.files` 49 条零 probe ✓ · 根非包散档 26 → 15 口径自洽（实测 70 跟踪顶层 − 44 白名单顶层 = 26）✓ · 11 档行数（55 / 77 / 397 / 70 / 56 / 117 / 53 / 68 / 39 / 72 / 93）✓ · `__dirname` 行锚（`afterPack:17-18` · `make-icons:11-13` · `make-latest:41/51/69/79` · `probe-pet-media:31`）✓ · require 面（`probe-pet-media:22` + `:138-141` 五处；其余 7 档零相对依赖）✓ · 文档同步行锚（ARCHITECTURE `:287` / `:293-300` / `:302-307` / `:344-346` / `:392` / `:394` / `:397` · PET-ANIMATION 六处 · PET-MOVEMENT `:698` · PET-MULTIMONITOR `:237` / `:900` · AUTO-UPDATE `:507` / `:518` / `:866` / `:944-945`）✓ · `.gitignore:22` ✓ · 附 B 新增文无 >300 字符行 ✓ · 全仓 `require` / 跑法面旧路径残留 = 0（仅名称级引用）✓。

VERDICT: pass

## §4 评审裁决与实施启动（主 agent）

**评审轮 1 裁决（2026-09-20；发现表 = §3，9 条：🔴0 / 🟡3 / 🔵6）**：

| # | Action | Detail |
|---|--------|--------|
| 1 | Fixed | 🟡 300 档结论补落（设计档 `:1084`）——`probe-pet-media.js` 397 行、本批仅 `git mv` + 注释 / 基准级改动、职责单一 ⇒ 不拆分 ✓ |
| 2 | Fixed | 🟡 `git status` 空态三处统一为「除 `.gitattributes` 自身暂存条目外为空」（`:924` / `:1045` / `:1131`）✓ |
| 3 | Fixed | 🟡 排期引用改以 `docs/batches/B35-pet-gallery-ui.md` §2.2 为准（`:925`，§1.5-7 作下限）+ 建议行增补（§2.10）✓ |
| 4 | Fixed | 🔵 白名单计数族 43 / 46 → **49**（`:1121` / `:1123`）✓ |
| 5 | Fixed | 🔵 AC-B15-4 补计数口径「按 `\n` 计数 = 144」（`:1134`）✓ |
| 6 | Fixed | 🔵 探针 8 档行数按档名对位（`:1056`）✓ |
| 7 | Fixed | 🔵 `build.afterPack` 新值改取 `./scripts/afterPack.js` + 解析链实读证据（`:955` / `:1020`）✓ |
| 8 | Fixed | 🔵 `package.json:62` 缩进修正纳入本批（`:1023` / `:1058` / `:1122` / `:1167`）✓ |
| 9 | Not an issue | 🔵 `docs/CONVENTIONS.md:175-176` §八 措辞现状即合规（AC-B15-8 已近目标形态）⇒ 零动作 ✓ |

**修正轮 1 落地核验（主 agent，2026-09-20）**：8 条落点逐处复读核实 ✓（设计档 18 处行级修订 + 变更记录 `:1198`；批次档 §2.10 记录 ✓；D6 回读在案 ✓）。
**跨批观察（非本批面，如实登记）**：完稿复跑 `npm run lint` = FAIL —— `pet-unlock-core.js` **524 行 / 上限 500**（另一会话在飞面；本批未触碰、不代修 ✗）⇒ 交该批处置；**B35 实施前须重测该档基线**（其设计记 345 → 470，现基线已变）。

**用户批准（2026-09-20，原话「批准 B15」）** ✓——批准对象 = 附 B 设计定稿 + 修正轮 1 后状态（9 条裁决全收口：8 Fixed / 1 Not an issue）✓；**实施排 B35 收口之后**（以 `docs/batches/B35-pet-gallery-ui.md` §2.2 为准）——实施启动时补记派工与 §5。

---

## §5 实施记录（eng-coder）

<!-- 由 eng-coder 填 -->

---

### §5 实施记录（eng-coder，2026-09-20）

**交付形态**：新建 1 档 + `git mv` 11 档 + 连带修正 11 档 + `package.json` 三行与 `:62` 缩进；步序 ①–⑥ 全走（⑦ 文档同步 / ⑧ 台账收口 = 其他角色，未触碰）。改动已 `git add` 暂存（`A .gitattributes` + `R ×11` + `M package.json`，恰 13 档）；**未提交**（§2.6 的提交形态含 ⑦ 文档面，时机归父侧统筹）。
工作区除本批 13 档外零改动（运行时面 / `build.files` 49 档 / `tests/**` / `scripts/gates/**` 零 diff）。

**逐档落点（行数 as-of 2026-09-20 实测）**：

| 档 | 动作 | 行数 |
|---|---|---|
| `.gitattributes`（新） | `* text=auto eol=lf` + 两条声明注（= 附 B §B.2.2 ⑤ 契约逐字） | 新建 3 行 |
| `scripts/probes/probe-pet-media.js` | `:12` 跑法行；`:22` + `:138-141` require → `'../../…'`；`:31` `ROOT = path.join(__dirname, '..', '..')` | 397 → 397 |
| `scripts/probes/probe-displays.js` | 头重排标准形 + 跑法新路径（指针 §2.2） | 55 → 57 |
| 6 档几何探针（displays2 / size-readback / straddle-size / position-accuracy / resizable-setsize / settle-scan） | 同上；settle-scan 指针 §2.5 | 各 +3 |
| `scripts/afterPack.js` | `:19-20` `'..'` 基准；JSDoc 补指针（ARCHITECTURE §2.7 面⑦） | 39 → 41 |
| `scripts/make-icons.js` | `:15-17` `'..'` 基准；头重排 + 指针 + 用法行 | 72 → 76 |
| `scripts/make-latest.js` | `:41/:51/:69/:79` `'..'` 基准；指针 → `docs/design/AUTO-UPDATE.md §2.2.8`；用法行全路径 | 93 → 93 |
| `package.json` | `:22` / `:23` / `:44` 三行 + `:62` 缩进 4 → 6 | 行数不变 |

**头指针归属（7 档几何探针，按实证归属取节）**：6 档 → `docs/design/PET-MULTIMONITOR.md` §2.2（各档读数表所在：Q1–Q4 / Q5 / Q6 / Q7 / 位置侧 A–C）；`probe-settle-scan.js` → §2.5（§2.2 无其读数表；该档落点校正读数归属 §2.5 决策面 DD-2 / DD-14 / DD-17）。
批次号字段口径 = 该头形态的确立批（10 档标 `B15`）；`probe-pet-media.js` 保留 `（B18；…）` 指针、不追改批号（设计 §B.2.2 ②「保留指针」口径；评审轮 1 #7 同源）。

**机检与实机（原始读数）**：

- **G1** `build.files` 排序集前后零 diff（49 条、零 probe 条目）；**G2** `git grep -nE "require\(['\"]\./(probe-|afterPack|make-icons|make-latest)"` = **0**。
- **G3** 11 档字面量相对 require 5 条全解析；全仓 68 档 .js/.mjs、115 条相对 require，未解析 17 处**全部**位于 `scripts/gates/**` 的注释 / 自证夹具字符串（B16 面、本批零触碰）。
- **G4** 活文档面 27 命中（分类与处置见下）；注入 / 移除双向实测 = 27 → 注入 28 → 清除 27（文档回净、零 diff）。
- **G5** 三绿：`GATE lint PASS checks=7 selftest=20/20` · `GATE test:full PASS pass=62 fail=0 skipped=0 ms=1143` · `GATE test:integration PASS scenarios=3 pass=3 fail=0`（S1 = 真启动，冒烟证据）。
- **G6** `npm run pack` 退 0，stdout 含 `[afterPack] applied icon + version to D:\big-fish\dist\win-unpacked\Bigfish.exe`（无 skipping 分支）。
- **G7** 探针实跑：`scripts/probes/probe-displays.js` 正常读数退 0；`scripts/probes/probe-pet-media.js` **O5 pass** 退 0（`window alpha(825x475 px, sf=1.2500)` / 播放推进 / DOM 读数齐）。
- **TC 双向**：TC-05 / 06 注入回红（`UNRESOLVED scripts/probes/probe-pet-media.js:22` / `ROOT=…scripts\probes miss=3`）→ 恢复绿；TC-08 夹具 +1 报红、真实档零 diff；TC-03 临时 clone 索引注入 CRLF ⇒ `i/crlf` 命中；TC-01 / 02 / 04 / 07 读数在案。
- **AC-B15-3 ④**：make-latest 沙箱正例退 0（sha256 与实算一致）+ 负例退 1；真仓 `latest.json` 零触碰；make-icons 静态基准过（`sharp` 缺依赖 ⇒ 不实跑，见下）。

**如实声明 / 存疑（提请父侧裁定）**：

1. **G4 = 27 命中**：22 处 = 6 档活文档同步面（⑦ eng-designer：CONVENTIONS `:176`；ARCHITECTURE `:287` + 8 行；AUTO-UPDATE 5 处；PET-ANIMATION 6 处；PET-MOVEMENT `:698`）；5 处 = `docs/design/REPO-CONVENTIONS.md` 自身定义 / 契约行（`:1018` / `:1019` / `:1032` / `:1135` / `:1155`）——**判据自指 ⇒ 按字面「= 0」不可达成**。
建议随 ⑦ 或设计微修补「判据自身定义行豁免」口径（先例 = 附 A-续 E① 守卫自身豁免）。
2. **AC-B15-2 计数口径**：「根非包散档 26 → 15」按逐字公式实测 = **26 → 16**（70 − 11 + 1 `.gitattributes` = 60 顶层；60 − 44 = 16）——存量面 = 15（原 26 中 11 档离根），+1 = F1 新增档自身。
3. **AC-B15-4 行数口径**：`package.json` `\n` 字符数 = 143（read / 编辑器口径 144 行；末行空行），前后同值——设计记「按 `\n` 计数 = 144」与实测差 1（疑为 split 口径）；「行数不变」结论成立。
4. **探针 0x0 首发抖动（一次性）**：probe-pet-media 首次实跑 `capturePage` 返 0x0 ⇒ 该次 O5 判 fail；独立最小复现（原版无改动 capture 测试：首捕 0x0 / 次捕正常）+ 旧版探针原位对照（pass）+ 新版复跑 ×2（pass）⇒ 判为**会话级捕屏抖动，非 B15 引入**（本批不触 `capturePage` 路径）。
5. **Δ 与设计预估差异（零行为面）**：make-icons +4（设计 ≈+3）；make-latest ±0（设计 ≈+2）；`.gitattributes` 3 行（设计 §B.2.3 记「≈ 6 行」——实现取 §B.2.2 ⑤ 契约逐字为准）。
6. **未做面**：`--renormalize`（无触发面：索引 `i/crlf|i/mixed|i/none` 双向 = 0）；提交；⑦ / ⑧ 全部面。
7. **T35**：`测试-更新功能/` 实测不在盘（`git ls-files` + 工作区双查零命中，as-of 2026-09-20）⇒ 无处置动作；`.gitignore:22` 条目保留为预防性声明（DD-B15-6）；核销建议行归主 agent。

**交付前内部审计与代码评审（终态）**：

- **偏离审计（explore，read-only，1 轮）**：1 条 🟡 = §5 未落笔（**本段落笔 = 闭环**）；写域 / 逐档修正 / 头形态 / 陈述一致性 / 如实声明五维逐条核过（其独立复核：11 档 `node --check` 全过、行数 Δ 与 §B.2.3 预计带一致）。观察 2 条：指针精度合规；`PET-MOVEMENT.md` mtime 异常 = 本批 TC-B15-09 注入-清除净零操作（`git diff` 实测为空）——均已解释。
- **代码评审（advisor，1 轮）**：VERDICT **pass**；发现 1 条 🔵（批次号标法两存）→ 已由本段「头指针归属」句落定口径；范围外注记 2 条（G4 判据自指 / `.gitattributes` 估数漂移）= 已登记于「如实声明」1 / 5。
- 无 fix round 新增（审计 🟡 由本段落笔闭环；评审 🔵 以口径落档闭环）。

**评审发现裁决表（advisor 轮 1）**：

| # | Action | Detail |
|---|---|---|
| 1 | Fixed | 🔵 批次号标法 → 本段「头指针归属」句落定口径（批次号 = 头形态确立批；`probe-pet-media.js` 保留 B18 不追改） |

（父侧压行注：本 §5 的 3 行超宽（`:239` / `:254` / `:269`，as-of）由主 agent **打标代修**——逐字保留、仅换行、零语义；成因 = 本段落笔于 coder 门禁复跑之后，其 PASS 行对 §5 为 stale；折后门禁复绿。）

## §6 验收核销（主 agent）

<!-- 由主 agent 填 -->
