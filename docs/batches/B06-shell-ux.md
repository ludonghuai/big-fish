# 批次档 B06 — 桌面壳 UX 整合（启动 · 托盘 · 桌宠交互 · 更新门禁 · main.js 拆分）

> 本文件按六段制组织，每段只有一位作者（append-only，段不重叠）：
> §1 主 agent · §2 eng-designer · §3 设计评审 · §4 主 agent · §5 eng-coder · §6 主 agent

---

## §1 批次立案（主 agent）

### 1.1 批次信息

| 项 | 值 |
|---|---|
| 批次号 | B06 |
| 主题 | 桌面壳 UX 整合——启动形态 / 桌宠左右键语义 / 新手向导删除 / 托盘菜单重排 / 更新门禁口径 / `main.js` 拆分 |
| 立案日期 | 2026-09-16 |
| 验收目标平台 | Windows（macOS / Linux 不得回归） |
| 需求档 | `docs/requirements/SHELL.md`（**新建板块「桌面壳」**；由 eng-designer 落档） |
| 设计档 | `docs/design/SHELL-UX.md`（**新建**；由 eng-designer 落档） |
| 关联技术待办 | T1（桌宠右键语义）· T2（`main.js` 拆分）——两者并入本批 |
| 前置批次 | B01–B05（B05 已核销；**B03 在途**——其手上持有 `main.js` 未提交改动，见 §1.7） |

### 1.2 任务来源（用户原话，2026-09-16）

> 「我们接下来整合项目启动的问题，现在启动项目就会自动出现浏览器和harness项目，太冗余重复了；右下角的托盘右键不好用，为什么更新会显示只有安装版可以使用？不要做这个显示可以吗？；我们需要彻底整合一下，否则不符合用户习惯，把切换模式放在二级菜单不好用」

> 「右键大鲸鱼还会同时出现harness和喂养界面，非常不符合逻辑」

> 「新手启动界面能不能直接删掉？太废物了，因为harness里面就有api入口吧？」

> 「托盘不加，有harness的设置就可以了；托盘右键不好用是设计问题，应该出现哪些，顺序如何，不符合人类视觉习惯；左键打开harness，右键打开喂养怎么样？；导向窗用不着；dev下面不能使用同样的功能吗？；main要拆分，但是你要拆的合理」

> 「app不自更新，不可能比我开发还快；harness为什么还是旧的？你觉得有必要替换成新的吗？」

> 「安装版的肯定是要留的，否则用户没法更新；要换新版的，你可以自由安排任务顺序；保留自更新，你的建议可以，开工」

### 1.3 已确认的需求结论（用户拍板 + 主 agent 裁定，逐条）

| # | 结论 | 来源 |
|---|---|---|
| C1 | **桌宠左键 = 打开/聚焦 harness 主界面；右键 = 打开兑换屋（喂养）**；两条链互不串（现状一次右键同时触发两条） | 用户原话（§1.2 第 4 段） |
| C2 | **删除新手向导窗**（`welcome.*` 三文件 + 全部触点）；理由 = 它不配置任何东西，只把用户指去 harness 的「设置 ▸ 模型 / API Key」；托盘**不加**「设置 API Key」项 | 用户原话（§1.2 第 3、4 段）+ `welcome.html:90-99` 原文 |
| C3 | **托盘菜单重排**：按「使用频率 + 人类视觉习惯」排（高频在上、设置归一组、危险操作隔离）；**「模式」提为一级项**（不再藏二级菜单） | 用户原话（§1.2 第 1、4 段） |
| C4 | **启动形态：不自动弹出 harness 主界面**（只出桌宠 + 托盘；主界面按需打开） | 用户原话（「太冗余重复了」）+ 主 agent 裁定 R2 |
| C5 | **更新门禁**：dev 下不显示/不弹「只在安装版可用」；harness 检查与更新在 dev 放行（且 dev 读活跃指针使其真生效）；**安装版保留 App 自更新** | 用户原话（§1.2 第 1、4、6 段） |
| C6 | **`main.js` 拆分**：必须拆，且「拆得合理」（高内聚低耦合、不引依赖、行为零回退） | 用户原话（§1.2 第 4 段） |
| C7 | **harness bundle 升级**（0.1.0-rc.6 → 0.1.5-rc.1）：要做，但**另立 B07**，排在依赖脚本那摊收口之后 | 用户原话（§1.2 第 5、6 段）+ 主 agent 裁定 R6 |

### 1.4 主 agent 的技术裁定

- **R1 落点**：本批属**新板块「桌面壳（SHELL）」**——启动形态 / 托盘 / 窗口 / 桌宠交互入口 / 更新门禁同属"壳"的用户面。`docs/requirements/SHELL.md` + `docs/design/SHELL-UX.md` 由 eng-designer 新建；`docs/README.md` 地图登记由主 agent 收口时补。
- **R2 启动形态（待用户在设计评审时追认）**：冷启动 = **桌宠（鲸鱼模式）+ 托盘**；主窗口**不自动显示**。**注意**：后端（`startDsh()`）仍须照常启动（本批只改"是否显示窗口"，不改"是否启动后端"）。
- **R3 桌宠左键语义**：**只开不隐**——点击桌宠一律"显示 + 聚焦"主界面，不做 toggle（避免"想打开却把它藏了"的误触）；隐藏主界面走托盘项。
- **R4 dev 门禁**：dev 下（a）托盘不出现 App 自更新入口、不弹「只在安装版可用」；（b）harness 检查 + 更新放行；（c）dev 的 `dshBinPath()` 改读活跃指针（与打包分支同口径），使 harness 更新在 dev 可验证。安装版行为不变。
- **R5 托盘草案作为设计基线**（eng-designer 可优化但须给理由）：一级 ≤ 12 项；分组 = 高频动作 → 外观与模式 → 功能与设置 → 危险操作（「高级」子菜单）→ 退出；「新手向导」项随 C2 删除。
- **R6 顺序裁定**：**B06（本批）先做**；**harness bundle 升级另立 B07**（前置 = 依赖脚本 / 锁文件那摊收口；须冒烟：dev + 打包各跑一次 + 插件装/卸一轮；保持精确钉版本，不放开 `^`）。
- **R7 拆分边界**：`pet.*`（B01/B03 纪律）· `updater.js` / `harness-store.js`（B02/B04/B05 纪律）**只搬不改**——拆分为纯结构迁移，任何行为改动都按回归处理。

### 1.5 验收标准

| # | 标准 | 判定方式 |
|---|---|---|
| AC1 | 桌宠**左键** → 主界面显示并聚焦（不 toggle）；**右键** → 只开兑换屋，主界面显示状态不变 | 真机 + 静态（`pet.js` 按键判断在场、两条 IPC 链互斥） |
| AC2 | 冷启动（whale）：无向导窗、主界面**不自动显示**、托盘在场、桌宠在场；左键/托盘可打开主界面 | 真机（隔离 `--user-data-dir` + 隔离 `DSH_HOME`） |
| AC3 | 向导删除彻底：全仓 0 处 `createWelcomeWindow` / `onboardingDone` / `welcome-open-url` / `welcome-done`；三文件不存在；`build.files` 无残留 | `grep` + 文件存在性断言 + 打包清单核对 |
| AC4 | 托盘：一级项 ≤ 设计档规定数；「模式」为一级 radio；危险操作在「高级」子菜单；排序与设计档逐条一致 | 真机目视 + 条目逐条核对 |
| AC5 | dev 门禁：托盘无 App 自更新项、无「只在安装版可用」弹窗；harness 检查可用；dev 的 `dshBinPath()` 有指针时走指针 | dev 实跑 + 静态核对（门禁分支与路径分支） |
| AC6 | 安装版回归：App 自更新入口与流程不变（沿用 B02/B05 判据，不新增） | 代码路径对照（真机发布门另计） |
| AC7 | 拆分：按设计档方案落地；拆分后每文件 < 500 行；`node --check` 全绿；托盘/桌宠/窗口/市场/插件/更新**零回退** | 行数实测 + `node --check` + 真机回归清单 |

### 1.6 已勘察的事实（主 agent 亲读，2026-09-16）

| # | 事实 | 证据 |
|---|---|---|
| 1 | 桌宠点击链：`pet-clicked` → `toggleMainWindow()`（开/隐主窗）+ 台词 + 开心动画 | `main.js:2660-2667` |
| 2 | 桌宠右键链：`pet-right-clicked` → `openExchangeWindow()` + 台词 | `main.js:2673-2678` |
| 3 | 按键判断缺失：渲染层 `pointerdown` / `pointerup` 不判 `button`，右键松开同样走 `clicked()`；右键正经语义在 `contextmenu` | `pet.js:98` / `:117` / `:125` |
| 4 | 托盘菜单 = **17 顶层项 + 4 分隔线**（含「模式」「Windows 右键菜单」两个子菜单）；「模式」是 radio 子菜单 | `main.js:1889-1930`（模式子菜单 `:1906-1912`） |
| 5 | 新手向导触点：定义 `main.js:616`；托盘入口 `:1893`；启动调用 `:2550`；IPC `:2580-2588`；默认值 `:81`；三文件 `welcome.html` / `welcome.js` / `welcome-preload.js` | 逐处亲读 |
| 6 | 向导不配置任何东西：第 3 步原文即"去主界面「设置 ▸ 模型 / API Key」填 Key" | `welcome.html:90-99` |
| 7 | 更新门禁（dev）：手动检查弹「只在安装版可用」+ 日志；自动检查**静默跳过** | `main.js:567-576` / `:578-584` |
| 8 | `dshBinPath()` 双态：打包分支读活跃指针（`userData/dsh-update/versions`）；dev 分支固定 `dsh-bundle/` | `main.js:131-140` |
| 9 | 启动序列：`loadSettings → startDsh → createWindow → createTray → … → scheduleUpdateChecks → maybeShowModeDialog → 桌宠 → launchAtLogin → 向导 → handleOpenArg` | `main.js:2462-2552` |
| 10 | `main.js` 现 **2831** 内容行（远超 500 行硬上限；T2 存量债） | 实测（文件末 `}` 在 `:2831`） |
| 11 | harness bundle 钉死 `0.1.0-rc.6`；注册表最新 `0.1.5-rc.1`（2026-09-10 发布）⇒ 落后 5 个 rc | `dsh-bundle/package.json:7`；registry 实查 |
| 12 | 用户 settings 现状：`onboardingDone:false`（故每次启动都弹向导）、`mode:"whale"`、`modeChosen:true` | `%APPDATA%\bigfish\settings.json` |

### 1.7 既有实现约束

- **写域冲突（硬）**：`main.js` 现被 **B03 会话**持有未提交改动（B03 批次档 §6.7 明文「两侧不得同时改 `main.js`」）。**B06 实施前必须错开**（B03 收口或停其会话）；设计阶段可并行（只读）。
- **拆分不得改行为**：`pet.*` · `updater.js` / `harness-store.js` · 更新链路语义（B05 纪律）——**只搬不改**。
- **打包清单同步**：删除 `welcome.*` 三文件、新增拆分模块后，`package.json` 的 `build.files` 必须同步。
- **零新依赖**；**不引构建步骤**（保持"直接跑源码"可运行）。
- **双态覆盖**：dev 与安装版差异按 R4 收口后，两态都须可解释、可复现。
- **不改 harness 内部代码**（`dsh-bundle/` 内容不动——升级另批 B07）。
- **既有能力不得回退**：托盘常驻、单实例、`window-all-closed` 不退出、全局快捷键、点击穿透、市场/兑换屋窗口。

### 1.8 交付与核验路径

需求档 + 设计档（eng-designer，含托盘结构与拆分方案对比）→ 主 agent 内容核验 → **用户发起设计评审** → 逐条裁决 → 用户批准 → eng-coder 实施（写域按设计档；须与 B03 错开）→ 主 agent 真机复测（AC1–AC7）→ 本档 §6 核销（含 T1 / T2 归档）。

### 1.9 立案后更正记录

| 日期 | 更正 |
|---|---|
| 2026-09-16 | 立案。**备注**：C4（启动形态）、R3（左键只开不隐）、R5（托盘草案）为主 agent 裁定，用户未逐字确认——已在设计档落为可审条目，用户可在设计评审时改判。**harness 升级**（C7）经用户授权「自由安排任务顺序」后定为**另批 B07**（§1.4 R6）。 |

---

## §2 本批任务书（eng-designer）

> **打标（主 agent 代录）**：本节由主 agent **逐字代录** eng-designer 的提交稿——该子代理的 `batch_segment({segment:"§2"})` 两次被工具骨架保护拒绝（本档缺 `## §2` 段头，骨架须由记录创建者预置；§1.12）。主 agent 已补骨架并逐字代录，**内容未作任何改写**（含计数、指针、表值）。
> 质量分级：⚠️ 代录段（非子代理自写）。

### 2.1 批次范围与三方条目一致（计数）

**本批 = B06 桌面壳 UX 整合（F1–F6）+ 无用文件清理（F7，主 agent 裁定并入本批）**。

- 条目计数：功能用户故事 **8** 条（US-1…US-8）+ 非功能标准 **4** 条（NFR-1…NFR-4）= **12 条**；验收 **8** 条（AC1…AC7 取自本档 §1.5；**AC8 = F7 追加验收**，权威判定面 = `docs/design/SHELL-UX.md` §3.1）。
- 三链同源：**本 §2 条目 = 设计档 §3.1 回指条目 = `docs/requirements/SHELL.md` 条目**（US-1…US-8、NFR-1…NFR-4；计数 8 + 4）。

| 条目 | 一句话 | 验收 |
|---|---|---|
| US-1 | 桌宠左键 = 打开 / 聚焦主界面（只开不隐） | AC1 |
| US-2 | 桌宠右键 = 只开兑换屋（两链互不串） | AC1 |
| US-3 | 删除新手向导（含首启无 Key 路径交代） | AC3 |
| US-4 | 托盘菜单重排（一级 11 项 / 模式提级 / 兑换屋可达 / 危险隔离） | AC4 |
| US-5 | 冷启动 = 桌宠 + 托盘（主界面不自动显示） | AC2 |
| US-6 | 更新门禁口径（dev 无 App 面；Harness 放行；安装版不变） | AC5、AC6 |
| US-7 | `main.js` 拆分（15 模块；每文件 <500 行；零回退） | AC7 |
| US-8 | 无用文件清理（F7：白名单精确删除） | AC8 |
| NFR-1 | 启动与首启可用性（无自动可见窗口；首启 ≤2 步） | AC2、AC3 |
| NFR-2 | 兼容性（Windows 验收；macOS/Linux 不回归） | — |
| NFR-3 | 可维护性（拆分口径 + 取证锚点不丢） | AC7 |
| NFR-4 | 双态可解释性（dev 与安装版差异只允许两处） | AC5、AC6 |

> 待用户追认项（设计已定，用户可在设计评审时改判；详见本档 §1.9）：US-1 的「只开不隐」（R3）、US-5 的启动形态（C4/R2）、US-4 的结构（R5 基线 + 设计档 DD-3/DD-4 两处优化）。

### 2.2 明确出批（不做）

1. harness bundle 升级（`0.1.0-rc.6` → 新版）——另批 B07（本档 §1.4 R6）；
2. B02 / B03 / B04 / B05 已交付面语义（除 US-6 明示的 dev 门禁修订面）；
3. macOS / Linux 专项验证（只要求既有行为不回退）；
4. 全仓未引用文件扫描式大清理（F7 边界 = 白名单制）；
5. `market.js` 拆分（500 行贴线未超；T2 评估面）；
6. 模式选择弹窗与 dev 提示家族非更新面成员（卸载 / Windows 右键菜单安装）——保持现状。

### 2.3 受影响文件（实施者写域；逐文件行号与预估增量 = 设计档 §2.3）

| 文件 | 当前 | 动作 |
|---|---|---|
| `main.js` | 2831 行 | F1/F2/F3/F4/F5 改动 + F6 拆出全部域（余组合根 ≈300 行） |
| `pet.js` | 185 行 | F1 左键守卫（`pointerdown` / `pointerup` / 自愈起拖；+4/−2） |
| `welcome.html` / `welcome.js` / `welcome-preload.js` | 104 / 23 / 7 行 | **删除** |
| `shell-*.js` × 15（新） | 0 | F6 新增（清单与职责 = 设计档 §2.2.6） |
| `package.json` | 116 行 | F2 删 `build.files` 三行；F6 增 15 个 `shell-*.js` |
| `README.md`（根） | 162 行 | F2 向导行删除 / 特性行更新；F6 目录段同步 |
| `使用说明.txt` | 53 行 | F2 首次使用段改写（不再指向向导） |
| `remove-pet-bg.js` / `update-pet-frames.js` | 120 / 76 行 | **删除**（F7） |
| 根 `background.jpg` | 395,696 B | **删除**（F7：与 `assets/background.jpg` 逐字节重复） |
| `assets/pet/` 9 张旧贴图 | 426,157 B 合计 | **删除**（F7；保留 `idle.png`） |

### 2.4 验收判据（机检面）

AC1…AC7 = 本档 §1.5 的判定方式（设计档 §3.1 细化）；AC8 = 追加（设计档 §3.1）。要点：

> **主 agent 同步（2026-09-16）**：评审修正轮 1 后，AC 的**范围 / 排除 / 限定**以 `docs/design/SHELL-UX.md` §3.1（`:554-562`）为**权威口径**；下列为要点摘要，不一致时以设计档为准。

- **AC1**：静态（`pet.js` 左键守卫 3 处；`pet-clicked` 走 `showMainWindow`；右键链唯一入口 = `contextmenu`）+ 真机（左开不隐 / 右只开兑换屋）。
- **AC2**：隔离 `--user-data-dir` + 隔离 `DSH_HOME` 冷启动 → 无向导、主界面不自动显示、托盘 + 桌宠在场。
- **AC3**：**五符号**（含 `welcomeWindow`）grep 0 处（排除 `docs/` / `.test-*` / `dsh-bundle`）+ 三文件不存在 + `build.files` 无残留。（主 agent 同步 2026-09-16：修正轮 1 后口径）
- **AC4**：托盘结构逐条对照（设计档 §2.2.3 表；一级 11 项 / 4 分隔线 / radio 条件 / 子菜单成员）+ 专注模式兑换屋可达。
- **AC5**：dev 实跑（无 dev 弹窗；`update gate … face=app skipped=dev` 行 + harness 检查行在场）+ 静态门禁分支。
- **AC6**：安装版代码路径对照（沿用 B02/B05 判据，不新增）。
- **AC7**：每文件 <500 行（`find /c /v ""`）+ `node --check` 全绿 + 锚点 grep 清单 + `dependencies` 零 diff + 真机回归清单。
- **AC8**：F7 四组删除（不存在 + 引用 0 处，grep 范围排除 `docs/` / `.test-*` / `dsh-bundle`）+ 保留面在场（`assets/pet/idle.png` / `assets/pet-new/**` / `probe-*.js` ×7 / `debug-pet.cmd`）+ 真机冒烟。

> F7 删除清单与判据（coder 逐条对照）：设计档 §2.2.7。删除前预检 = 对四项（组）逐条重跑 0 引用核对；任一发现引用 → **停删并回报**。

### 2.5 实施前置与顺序

- **前置（硬）**：`main.js` 写域与 B03 错开——B03 收口或停其会话后再动 `main.js`（本档 §1.7）。
- **顺序**：P0 行为变更逐项提交——① F1 按键守卫与点击语义 → ② F2 向导删除 → ③ F3 托盘重排（含模式弹窗文案） → ④ F4 启动形态 → ⑤ F5 门禁 → ⑥ F7 无用文件清理（独立提交，纯删除）；P1 拆分 F6（L0→L3 八步逐层提交；P1 起点 = 纯迁移 diff 基线）；P2 收尾全回归（设计档 §2.2.6 迁移表 + §3.2 TC-18…TC-23）。
- 每步 `node --check` + 对应 AC 核对；每步可独立回滚（每项 / 每层一提交）。

### 2.6 边界与纪律（实施者）

- 只动 §2.3 清单内文件；**不动** `docs/TODO.md` / `docs/README.md` / 其他批次档 / 其他设计档；**不动** `.npmrc` / `scripts/` / `package-lock*.json`（另一会话在途）。
- F7 = 白名单精确删除；保留面（`assets/pet/idle.png` / `assets/pet-new/**` / `probe-*.js` ×7 / `debug-pet.cmd`）不得触碰。
- F6 = 纯迁移：零回退口径 = 允许「引用限定 / 转发访问器 / 样板」三类改动面，禁止「执行顺序 / 条件 / 常量 / 字符串 / 日志时点」五类语义面变更（设计档 §2.2.6）。
- 契约语义（设计档 / 需求档）实施中不得自行改动——发现设计缺口 → 停工回报主 agent。

### 2.7 文档落点状态（本角色交付）

- `docs/requirements/SHELL.md`（新建，143 行）· `docs/design/SHELL-UX.md`（新建，568 行）——本 §2 同批落档。
- 跨档修订注记已落：`docs/requirements/PET.md`（US-5 边界 / §二 + 变更记录）· `docs/requirements/UPDATE.md`（US-1 边界 / US-5 / US-6 边界 / NFR-3 + 变更记录）。
- 待主 agent 收口面：`docs/README.md` 地图登记；台账 R5 / R6 / T1 / T2 状态推进；`docs/design/AUTO-UPDATE.md` §2.2.7 / §2.2.9 同步（观察项 O1）；`THIRD-PARTY-NOTICES.md:53-54` 陈旧提及（观察项 O7）。

---

## §3 设计评审（评审子代理）

---

### 轮次 1（评审子代理）

（评审对象：`docs/design/SHELL-UX.md` + `docs/requirements/SHELL.md` + `docs/batches/B06-shell-ux.md`（§1/§2）；范围内另含 `docs/requirements/PET.md` / `docs/requirements/UPDATE.md` / `docs/design/AUTO-UPDATE.md`。限制：未提供 AGENTS.md / 项目标准档，方法论合规按评审标准 + 文档地图 + 范围内文档判定；按范围约定未读代码文件，行数抽查以范围内各档交叉一致性为准。）

| # | 分类 | 严重度 | 问题 | 建议 |
|---|------|--------|------|------|
| 1 | 文档归属 | 🔴 | **同一机制（dev 更新门禁 / `dshBinPath()` dev 分支 / gate 日志行）两处描述不同且未收口**：`docs/design/AUTO-UPDATE.md` 仍为旧口径——`:41`（C5「dev 模式不检查（`!app.isPackaged` 返回，现状保留）」）、`:48`（C12「dev 分支不变」）、`:245`（「dev 分支逐字不变」）、`:474`（「dev 模式；托盘手动点击时弹提示『更新检查只在安装版可用』」）、`:517`（gate 行 `skipped=dev` 整面）、`:632`（C5 行）、`:639`（C12 行）、`:652`（L5「Harness 更新不可用于 dev 模式」）、`:684`（U-12 dev 提示）；`docs/design/SHELL-UX.md` §2.2.5（`:250-298`）为相反口径（App 面静默跳过 / Harness 放行 / 无弹窗 / 新 gate 行 / dev 指针优先）。设计已登记 O1（`:471`）但范围只含 §2.2.7 / §2.2.9 + B02 §3.1 AC7（`:455`），未覆盖 §2.2.1 / §2.5 C5·C12 / L5 / U-12；文档地图亦记「同步待收口」。F5 实施者按 AUTO-UPDATE.md 会得到与设计相反的指令。 | 本轮通过前由父侧文档层完成 AUTO-UPDATE.md 同步：范围扩到 `:41`/`:48`/`:245`/`:474`/`:517`/`:632`/`:639`/`:652`/`:684`，权威口径指向 `docs/requirements/SHELL.md` US-6 与 SHELL-UX §2.2.5；同步落档后再放行实施。 |
| 2 | 需求覆盖 / 步骤一致性 | 🟡 | F2 触点清单遗漏 `createWindow()` `ready-to-show` 内的「向导窗显示」行（只出现在 F4 的「与向导窗显示两行整段移除」`:142`；C7 亦记「向导窗同刻被拉起」`:39`）；按 P0 顺序（`:361`：② F2 先于 ④ F4），F2 提交删除 `main.js:63` `welcomeWindow` 变量（`:222`）后仍留其引用 ⇒ 冷启动 ready-to-show 处理器抛 ReferenceError，与「每步可独立回滚 / 每步核对」相抵；且 AC3 四符号 grep 集（`:507`）不含 `welcomeWindow`，机检发现不了该悬空引用。 | 把该行并入 F2 删除清单（或把 F4 前移 / 与 F2 同一步完成 createWindow 两处改动）；AC3 grep 集补 `welcomeWindow`。 |
| 3 | 验收判据 / 机检范围 | 🟡 | 三处机检范围界定不足：① AC3（`:507`）写作「范围 = 全仓，排除 `.test-userdata/` 与 `dsh-bundle/node_modules/`」——设计档自身含四符号（`:158`/`:221`/`:223`/`:225`/`:226`/`:231`/`:474`）、批次档含 `onboardingDone`（`B06-shell-ux.md:65`/`:86`）⇒ 按字面执行不可能通过；② AC5（`:509`）与 §3.3 ①⑦（`:546`）的 `grep "更新检查只在安装版可用" = 0` 会命中设计档（`:278`/`:546`）与 AUTO-UPDATE.md（`:474`/`:684`）；③ AC7（`:511`）「全部 js ≤500」未界定文件集（`dsh-bundle/`、`node_modules/`、`probe-*.js` 是否计入）。 | 三处判据补范围 / 排除声明（grep 排除 `docs/`；行数实测给文件清单或排除规则），与 AC8 的「排除 `docs/` / `.test-*` / `dsh-bundle`」口径对齐。 |
| 4 | 验收判据 | 🟡 | AC8（`:512`）与 TC-21（`:538`）的「`package.json` scripts / `build.files` / 依赖零 diff」与同档 §2.3 的 F2（删 `:41-43` 三行）/ F6（`build.files` +15，`:398`）相抵——按字面在批末执行必失败；§2.2.7 判据③（`:383`「删除项本就不在其中」）已是限定口径，AC 行未同步。 | 限定为「F7 提交自身不引入 `package.json` 改动」，或注明排除 F2 / F6 的预期 diff。 |
| 5 | 受影响文件体量 | 🟡 | 15 个新模块未逐档给行数（`:397` 只给合计「+约 3100 / 单档 35–400」），且 >300 档缺 proactive split review：按 §2.2.6 表来源段估算 `shell-plugins`（`:316`，2007-2364 除 restartBackend）≈350+、`shell-pet-geometry`（`:311`，785-1113）≈330，均已越 300 行档（500 硬上限未越）；设计未记录这两档的拆分审查结论，也未说明被搬移函数是否存在单函数 >300 行。 | 在 §2.3 / §2.2.6 补逐档预估行数 + >300 档结论（纯搬移不拆分 → 留档体量裁定 / 或给拆分方案），并核对被搬移函数的单函数体量。 |
| 6 | 兼容性（NFR-2） | 🟡 | L3（`:465`）记录 macOS `activate`（Dock 点击）不再主动显示主界面，与 `SHELL.md:119`「macOS / Linux 既有行为不得回退」相抵；NFR-2 的例外只声明 US-5 / US-4 的跨平台有意变更（`SHELL.md:120`），未涵盖 activate 路径；该差异亦不在「待用户追认项」（`:493`）内。 | 二选一：① activate 统一改经 `showMainWindow()`（用户主动显示请求）；② 在 SHELL.md NFR-2 例外 / 追认清单明示该项为有意变更。 |
| 7 | 纯迁移覆盖 | 🔵 | §2.2.6 来源段映射存在未覆盖行区间（72 / 114 / 245 / 259 / 316 / 331 / 604 / 698 / 721 / 781-784 / 1189 / 1773 / 1947-1950 / 1997-1998 / 2365 / 2384 / 2449-2452 / 2578 / 2831，合计约 27 行）；若含代码行，纯迁移口径下内容去处不明（是否全为空行 / 注释本轮无法核实——unverified）。 | 补段覆盖核对（或把空行 / 注释归入相邻模块），并在 P2 以「源行覆盖清单」机检。 |
| 8 | 需求回指 | 🔵 | §3.1 的「回指需求」列未含 NFR-1 / NFR-2（AC2 行只列「US-5、US-3」`:506`），与 `SHELL.md:111`「NFR-1 …（验收：AC2、AC3）」不完全对齐（NFR-2 度量另落 §3.3，`:546`）。 | 行内补 NFR 回指，保持三链同源可核。 |
| 9 | 计数漂移 | 🔵 | 跨档行数漂移：`main.js` 记 2831（`:393`、`B06-shell-ux.md:84`/`:152`）而 AUTO-UPDATE.md §2.3 记 2827（`:543`）；`package.json` 记 116（`:398`）而 AUTO-UPDATE.md 记 113（`:549`）。 | as-of 口径可容忍；建议 §2.3 表口径注明差异来源（测量时点 / B03 在途改动），实施起点重测并回填批次档 §5。 |
| 10 | 清晰性 | 🔵 | §2.2.2 显示时机表把「第二实例 / `--open`」标为「不变（统一改经 `showMainWindow()`）」（`:155`/`:156`），而 `showMainWindow()` 含 `isMinimized() → restore()`（`:143`）——若既有路径不含 restore 则属行为微增（与「不变」标注不符，本轮无法核实现状实现——unverified）；TC 表亦无这两条路径的回归用例。 | 注明 restore 是否为既有语义（若有差异按 US-5 边界「不改显示路径」核对）；TC 表补第二实例 / `--open` 目视项。 |
| 11 | 限制项归属 | 🔵 | L1（`:463`）自称「留待用户 / 评审裁量」，但 §2.6 追认清单（`:493`）未收录；TC 表也无「dev 手动检查、无更新 → 静默」用例（TC-15 只覆盖有更新场景，`:532`）。 | 把 L1 并入追认清单，或补一条「已接受行为」用例。 |
| 12 | UI 分组 | 🔵 | 「找回鲸鱼娘」降入「高级 ▸」（与「重置插件配置 / 彻底恢复出厂 / 卸载」同组，`:191`；差异表 `:201`）——救援入口与破坏性操作同组；PET US-14 的「托盘菜单有入口」语义未变，但 DD-3 的「与打开窗口类动作同族」理由未适用于该项。 | 评估移入功能组，或在 §2.6 / §2.4 记录该取舍理由。 |
| 13 | 记录质量 | 🔵 | 批次档 §2 为代录段（`B06-shell-ux.md:112`/`:113`：子代理写入两次被工具骨架保护拒绝，主 agent 逐字代录并打 ⚠️ 标）——根因 = §1–§6 骨架未在立案时预置。 | 无设计改动需求；后续批次立案时预置六段骨架，并在 §4 记录该质量分级。 |
| 14 | 模块归属 | 🔵 | `petCalibrateSize` 归属表述两处并存：注 S2 列入 `shell-pet-drag`（`:326`），geometry 行写「尺寸校准支撑」（`:311`）——若 geometry 侧函数（如 `handleDisplayChange`）需调用该校准，其依赖声明（注入面仅 `getPetWindow` / `getPetDrag`）不足以支撑（调用点本轮无法核实——unverified）；PET NFR-8 的「单一实现」口径需保持。 | 明确 `petCalibrateSize()` 的归属模块与跨模块访问方式（必要时补注入项），保持单一实现。 |

VERDICT: changes-required

发现计数：14（🔴 1 / 🟡 5 / 🔵 8）

### 轮次 2（评审子代理）

**轮次 2（复评）范围**：只验 §4 裁决表 13 项修正落地 + 显见新问题；逐档重读六档全文（`docs/design/SHELL-UX.md` / `docs/requirements/SHELL.md` / `docs/batches/B06-shell-ux.md` / `docs/requirements/PET.md` / `docs/requirements/UPDATE.md` / `docs/design/AUTO-UPDATE.md`）；未读代码文件（范围约定），代码级断言按设计档自述记。

**裁决项落地核验（逐条）**

| # | 裁决项 | 状态 | 证据（本轮重读） |
|---|---|---|---|
| 1 | AUTO-UPDATE 九 loci 注记 + §2.3 as-of | ✅ 已落地 | 九处：`:42`/`:49`（C5/C12 事实行，注记 `:53-54`）· `:249`+`:250` · `:479`+`:480` · `:523`（gate 行就地换新形态）· `:640`/`:647`+`:654` · `:662`（L5 改写）· `:694`+`:699-700`；行数口径注 `:532`；§2.3 as-of 注 `:567`；变更记录 `:855-856` |
| 2 | F2 补 ready-to-show 向导块 + AC3 五符号 | ✅ 已落地 | `SHELL-UX.md:226`（归 F2 + ReferenceError 动机）· `:142`（F2 先行摘除）· `:556`/`:600`（五符号含 `welcomeWindow`）· `:578`（TC-10） |
| 3 | AC3/AC5/AC7/§3.3 范围与排除 | ✅ 已落地 | `:556`（排除 `docs/` / `.test-*` / `dsh-bundle`）· `:558`（范围=代码/配置，排除 `docs/`）· `:600` · `:560`（AC7 文件集=全仓 .js 含 probe/scripts/tests） |
| 4 | AC8/TC-21 零 diff 限 F7 自身 | ✅ 已落地 | `:561` + `:589`（F2/F6 预期 diff 除外） |
| 5 | 逐档预估 + >300 档结论 + 单函数体量 | ✅ 已落地 | `:385-404`（表，合计 ≈2920）· `:406-410`（四档结论）· `:411`（单函数 <300）· `:442`（§2.3 指向 §2.2.6） |
| 6 | macOS `activate` 改经 `showMainWindow()` + NFR-2 例外 | ✅ 已落地 | `SHELL-UX.md:157`/`:513`（L3 改写）/`:594`（TC-26）；`SHELL.md:120` |
| 7 | 来源段覆盖核对 + P2 机检 | ✅ 已落地 | `:380-383`（29 行 = 15 空 + 11 注释 + 3 代码；tray 段 `:322` 同步为 1889-1948；`:2831` 归组合根）· `:373`（P2 源行覆盖清单） |
| 8 | AC 行补 NFR 回指 | ✅ 已落地 | `:555`（AC2 含 NFR-1/NFR-2）· `:556`/`:557` · 回指口径注 `:563` |
| 9 | as-of 注（SHELL-UX + AUTO-UPDATE） | ✅ 已落地 | `SHELL-UX.md:463` / `:507`；`AUTO-UPDATE.md:567` |
| 10 | `restore()` 口径注明 + TC 目视项 | ✅ 已落地 | `SHELL-UX.md:160`（口径注）· `:155-156` · TC-24/25/26 `:592-594` |
| 11 | L1 并入追认清单 | ✅ 已落地 | `:511` + `:542`（「L1…『已接受行为』」） |
| 12 | 找回鲸鱼娘保留「高级 ▸」+ 留档理由 | ✅ 已落地 | `:484`（DD-16）+ `:540`（U-10）+ `:193` / `:203` |
| 13 | Not an issue（维持） | ✅ 无异动 | `B06-shell-ux.md:112-113`（⚠️ 代录标在场） |
| 14 | `petCalibrateSize` 归属与跨模块访问 | ✅ 已落地 | `:337-341`（注 S4）· `:314-315`（表）· `:328-331`（注 S2） |

**本轮新发现（剩余问题）**

| # | File | Severity | Status | Notes | 建议 |
|---|---|---|---|---|---|
| 15 | docs/design/AUTO-UPDATE.md | 🟡 | New：修订覆盖外残留（判定面） | §3.1 AC7 行 `:720`「dev 模式 → 无任何检查行」未随 B06 注记刷新（B06 后 dev 产出 gate 行 + harness 检查行——与 `SHELL-UX.md` §2.2.5 / AC5 相抵）；O1（`SHELL-UX.md:519`）已把该判定面覆盖登记给主 agent，但措辞点名 `B02` 批次档 §3.1 AC7，未含本档 §3.1 AC7 行；§一 索引 `:23` / `:31`（「dev 不检查」「dev 短路」）同族，已有节内注记 `:53-54`。不影响实施口径（实施以 `:480` 注记 + SHELL-UX §2.2.5 为准） | 收口时把本档 §3.1 AC7 行（及 §一 索引短语）并入 O1 范围加注，或注明判定面由 SHELL-UX AC5 取代 |
| 16 | docs/design/SHELL-UX.md + docs/design/AUTO-UPDATE.md | 🔵 | New：修正轮后数字/口径未同源刷新 | ①「落档实测」行数仍为修正前值：`SHELL-UX.md:447`（568）· `:446`（143）· `AUTO-UPDATE.md:549`（839）——现况 624 / 144 / 856（批次档 §4 核验已记）；② 选型 E `SHELL-UX.md:94`「净增 ≈600 行」与刷新后逐档预估（合计 ≈2920，`:442`）不同源（按现口径 ≈+410）；③ §2.2.6 逐档来源段之和 2856 + 主档保留段 196 + 未覆盖 29 = 3081 > main.js 2831（约 +250，疑因 `shell-ipc` 行「`2579-2830`（改写）」与各域 IPC 处理器段重叠）。均不影响 <500 结论 | 收口时回填终态行数（B04/B05 先例）；②③ 注明口径（净增公式 / 来源段是否互斥） |

**计数**：本轮新发现 2（🔴 0 / 🟡 1 / 🔵 1）；裁决表 14 项复核完毕（13 项修正全部落地 + 1 项 Not an issue 维持）；上一轮 🔴（同机制两处描述不同）已消解。

VERDICT: pass

## §4 评审裁决与实施启动（主 agent）

### 轮次 1 裁决（主 agent，2026-09-16）

评审发现 14 项（🔴 1 / 🟡 5 / 🔵 8，见 §3 轮次 1）→ 逐条裁决如下；13 项派 **eng-designer 修正轮**（同批次同链，只落本表直接导出项、不夹带新语义），1 项判 Not an issue。

| # | Action | Detail |
|---|--------|--------|
| 1 | Dispatched | `docs/design/AUTO-UPDATE.md` 同步扩围：加日期化 B06 口径修订注记（`:41`/`:48`/`:245`/`:474`/`:517`/`:632`/`:639`/`:652`/`:684`；权威口径 → `docs/requirements/SHELL.md` US-6 + `SHELL-UX.md` §2.2.5）；§2.3 补 as-of 行数注（2831 / 116，差异源 = 测量时点 / B03 在途） |
| 2 | Dispatched | `SHELL-UX.md` §2.2.4 F2 删除清单补 `createWindow()` `ready-to-show` 的「向导窗显示」行（`:142` 归属改 F2）；AC3（`:507`）grep 集补 `welcomeWindow` |
| 3 | Dispatched | AC3（`:507`）/ AC5（`:509`）/ §3.3 ①⑦（`:546`）/ AC7（`:511`）补范围与排除声明（grep 排除 `docs/`；AC7 给文件清单或排除规则），与 AC8（`:512`）口径对齐 |
| 4 | Dispatched | AC8（`:512`）/ TC-21（`:538`）的 `package.json` 零 diff 限定为「F7 提交自身」，注明 F2 / F6 预期 diff |
| 5 | Dispatched | §2.3（`:397`）/ §2.2.6 补 15 档逐档行数预估 + `>300` 档结论（`shell-plugins` `:316`、`shell-pet-geometry` `:311`）+ 被搬移函数单函数体量核对 |
| 6 | Dispatched | 采用方案 ①：macOS `activate` 统一改经 `showMainWindow()`（零回退）；改 §2.2.2 显示时机表 + L3（`:465`）+ TC 表；NFR-2 例外句按需补（`SHELL.md:120`） |
| 7 | Dispatched | §2.2.6 补来源段覆盖核对（空行 / 注释归属）+ P2「源行覆盖清单」机检 |
| 8 | Dispatched | §3.1 AC 行补 NFR-1 / NFR-2 回指（AC2 `:506` 一带） |
| 9 | Dispatched | §2.3（`:393`/`:398`）加口径注；§2.5 注明实施起点重测并回填 §5 |
| 10 | Dispatched | §2.2.2（`:155`/`:156`）注明 `restore()` 是否为既有语义（只读核对 `main.js` 后落笔）+ TC 补第二实例 / `--open` 目视项 |
| 11 | Dispatched | L1（`:463`）并入 §2.6 追认清单（`:493`） |
| 12 | Dispatched | 主 agent 裁定：**保留在「高级 ▸」**（低频救援动作）；在 §2.6 / §2.4 记录取舍理由（`:191`/`:201`） |
| 13 | Not an issue | §2 代录已带 ⚠️ 质量分级标（`:112`/`:113`），根因（六段骨架）已在立案后修正；评审自身亦标「无设计改动需求」——无文档改动 |
| 14 | Dispatched | §2.2.6 明确 `petCalibrateSize()` 归属与跨模块访问方式（保持 PET NFR-8 单一实现），必要时补注入项声明 |

**处置说明**：

- 修正轮边界 = 上表 13 项；落地后由主 agent 逐条核验（file:line）→ 进**第 2 轮复评**（只验本节裁决表 + 显见新问题）。
- **本批未放行**：🔴 #1 未落地前不签发设计凭证、不派 eng-coder；§1.7 的 B03 写域冲突另为实施前置。

### 修正轮 1 落地核验（主 agent，2026-09-16）

eng-designer 修正轮（id=6）报 13 项全落 → 主 agent 脚本核验 + 回读：

| 核验面 | 结果 |
|---|---|
| 行数（内容行口径，末行换行不计） | ✅ 与报告一致（`SHELL-UX.md` 622 → 小修后 **624**；`SHELL.md` 144；`AUTO-UPDATE.md` 856） |
| #2 §2.2.4 补 `ready-to-show` 行；AC3（`:555`）含 `welcomeWindow` + 排除 `docs/` | ✅ |
| #3 AC3 / AC5 / AC7 / §3.3①⑦ 范围与排除声明 | ✅（AC7 文件集明确 = 全仓 `.js` 含 `probe-*.js`/`scripts/`/`tests/`，排除 `dsh-bundle/`、`node_modules/`、`.test-*`，唯一超顶者 `main.js`） |
| #5 逐档预估表（合计 ≈2920）+ `>300` 档结论 + 单函数体量（全 <300，最大 `doWander` ≈90） | ✅ |
| #6 `activate` 统一经 `showMainWindow()` + `SHELL.md:120` NFR-2 例外句 | ✅ |
| #7 来源段覆盖核对（实核 29 行 = 15 空 + 11 注释 + 3 代码；`1947-1948` 归 `shell-tray`，`:2831` 归组合根） | ✅ |
| #9 as-of 注（B06 实测 2831 / 116；历史值 2827 / 113 保留不改写） | ✅ |
| #10 `restore()` = 既有语义（第二实例 `main.js:2458`；`--open` 无 `restore()` = 等价收敛） | ✅ |
| #12 DD-16 + U-10（「找回鲸鱼娘」保留「高级 ▸」的取舍理由） | ✅ |
| #14 注 S4（`petCalibrateSize()` 归 `shell-pet-geometry.js`；无新注入项） | ✅ |
| #1 `AUTO-UPDATE.md` 9 loci + 变更记录（B06 提及 11 次；新 gate 形态在场） | ✅ |
| **行宽 ≤300**（口径 = 内容字符，剥离行尾 CR） | ✅ 二次小修（id=7）后复扫：三档 **0 行超宽**（`SHELL-UX.md:314` 301→**182**；`AUTO-UPDATE.md:771` 301→**293**；AUTO 档只动该行，他会话在途 hunk 未触碰） |

**附加同步（主 agent）**：本档 §2.4 的 AC3 摘要同步为「五符号（含 `welcomeWindow`）+ 排除 `docs/`」，并在 §2.4 头部加「权威口径 = 设计档 §3.1」指针（§2 为代录段，故本次同步由主 agent 打标执行）。

**口径备注**：① 两档为 CRLF，「301」= 内容 300 + 行尾 CR 1——修复后双口径均 ≤300；② 本档 §3 评审发现表含 6 行 >300 字符（`:211`/`:212`/`:213`/`:215`/`:220`/`:224`，最长 784）——≤300 规范的适用面 = 需求层 / 设计层文档（`docs/requirements/` / `docs/design/`），批次档为工作记录不在其内；且 §3 为评审子代理段，主 agent 不改写其内容。

### 第 2 轮复评结果（主 agent 记录，2026-09-16）

- **VERDICT: pass**（🔴 0 / 🟡 1 / 🔵 1——两项均为新增低级别项，见 §3 轮次 2）。
- 复核面：§4 裁决表 **14 项全部收敛**（13 项 Dispatched → 已落地并经核验 = Fixed；1 项 Not an issue 维持）；上一轮 🔴（同一机制两处描述不同且未收口）**已消解**（九处 loci 加日期化注记 / 就地改写 + 权威口径指针 + §2.2.9 gate 行枚举换新 + §2.3 as-of 注）。
- **设计凭证已签发**（凭证值不落档——运行态，见会话流）。
- **引用格式复核（主 agent）**：宿主校验器对本轮 §3 的若干「行号 + 片段」引用报格式不匹配（引用带省略号 / 截断）；主 agent 逐条以**整行 / 连续子串**复读核对，**实质结论全部为真**：
  - `docs/design/SHELL-UX.md:94`：「15 个新文件 + 接线样板（净增 ≈600 行）；`build.files` 同步 15 项」 ✓
  - `docs/design/SHELL-UX.md:442`：「+约 2920（15 档合计；单档预估 30–395——**逐档行数预估与 >300 档结论见 §2.2.6**）」 ✓
  - `docs/design/SHELL-UX.md:446` / `:447`：「**143**（落档实测）」/「**568**（落档实测）」 ✓（现况 144 / 624）
  - `docs/design/AUTO-UPDATE.md:549`：「**839**（实测内容行口径，含两轮修正）」 ✓（现况 856）
  - `docs/design/AUTO-UPDATE.md:720`：「dev 模式 → 无任何检查行」 ✓（B06 后 dev 产出 gate 行 + harness 检查行 ⇒ 与 SHELL-UX AC5 相抵；判定面残留）
- **收口项**（不阻断批准，实施后收口时处理）：§3 轮次 2 新发现两项——① AUTO-UPDATE §3.1 AC7 判定面残留 → 并入 O1；② 修正轮后行数与「净增」口径未刷新 → 回填终态行数。
- **下一步 = 用户批准**（链终消费与实现启动在批准后）。

### 用户批准（2026-09-16，会话内）

- 用户裁定：「可以的123我都同意，全部批注（批准）」——**设计批准生效**。
  - 追认：① 桌宠左键 = 只开不隐；② 冷启动 = 只出桌宠 + 托盘；③ 托盘结构 = 11 一级项（模式提级 / 兑换屋一级补位 / 危险操作入「高级 ▸」）。
  - 修正轮的两项主 agent 裁定（macOS `activate` 改经 `showMainWindow()`；「找回鲸鱼娘」保留「高级 ▸」）未被反对。
- **实施启动**：P0（F1–F5 + F7）派 eng-coder；P1（F6 拆分）与 P2 回归另轮（同批同链、同凭证）。
- **实施前置核对（主 agent）**：`main.js` 已由他会话提交（`7f4258b`，工作区干净 + mtime 静默）——§1.7 的「与 B03 错开」前置**已满足**；⚠️ 同目录仍有活跃会话（pid 5760）在改 `README.md` / `使用说明.txt` / `AUTO-UPDATE.md` / `latest.json`（其发版链条工作面）——本批对这些档只做**定点编辑**，不得回写整档 / 触碰他方改动区；发现冲突即停工回报。

### P0 交付核验与真机目视验收（主 agent + 用户，2026-09-16）

| 面 | 结果 |
|---|---|
| 六个提交 | ✅ `e05c834`(F1) / `82aa26e`(F2) / `cc0b49f`(F3) / `59b1ba7`(F4) / `37f614d`(F5) / `2cf1289`(F7)——事故提交 `47cbfad` 已回滚干净（未入历史） |
| AC5（真机实测，两轮隔离实例） | ✅ `updater.log`：`update gate reason=startup face=app skipped=dev` + `update check reason=startup type=harness result=update-available latest=0.1.5-rc.1 current=0.1.0-rc.6`——dev 下 App 面静默、Harness 面照常 |
| AC3 真机面 | ✅ CDP 目标仅 = `pet.html` + harness 主窗（**无向导窗**）；启动日志无向导痕迹；五符号静态 0 命中 |
| AC2 真机面 | ✅ 后端就绪 → 建窗 → 桌宠在场（`petPos` 回写生效）；`createWindow()` 无自动显示路径（静态 + 目视） |
| AC1 / AC4 目视 | ✅ **用户目视通过**（2026-09-16 22:43「可以的」）——左键只开不隐 / 右键只开兑换屋 / 托盘 11 项 + 模式一级 |
| AC8 清理面 | ✅ 12 文件删除到位、保留面在场；隔离实例起停正常（删除后冒烟） |
| 他会话在途改动 | ✅ 完好（README 的 276MB→157MB、node-runtime 打包说明原样未动） |

**主 agent 裁决**：托盘 #5 标签保留「更换背景…」（Windows 省略号惯例 = 弹出对话框；设计主表口径）——§5 5.2 #3 待裁定项关闭。
**O1 残留**（`AUTO-UPDATE.md` §3.1 AC7 判定面 + §一 索引）：该档正被他会话在途改动，**暂缓**，收口时统一处理。
**范围外发现（已立条）**：启动自动拉起系统浏览器 = dsh CLI 默认行为（`dsh web: opening the default browser; pass --no-open to disable`；日志 + 截图实证）——非本批范围；用户裁定「单独立条，以后做」→ 台账 **R7**。

### 设计档实施期形态注（eng-designer，2026-09-16）

eng-designer 极小修正落地（写域 = `docs/design/SHELL-UX.md`，3 处 hunk，回读核验 ✅）：

- `:143`（§2.2.2）零窗口建窗分支补「经 `ready-to-show` 首帧就绪后才 `show` + `focus`」口径（实现 = `main.js:732-733`；不改语义）；
- `:554`（§3.1 AC1）机检措辞改按实现形态（早退式守卫 `e.button !== 0` ×2 + `(e.buttons & 1) !== 0` 位掩码 ×1）；
- `:625`（§四 变更记录）追加「实施期形态注（P0 落地后）」。
- 档行数 624 → **625**；全档 0 行 >300 字符。

**收口轮待办清单（已登记，随 §6 一并处理）**：

1. `SHELL-UX.md:56`（§2.1 选型 A 描述）残留字面「判 `button === 0`」→ 与 AC1 措辞对齐（描述面，非机检面）；
2. §3 轮次 2 新发现 #1（`AUTO-UPDATE.md` §3.1 AC7 判定面 + §一 索引）→ 并入 O1 处理（**须待该档他会话在途收口**）；
3. §3 轮次 2 新发现 #2（行数与「净增」口径回填：`SHELL-UX.md` / `SHELL.md` / `AUTO-UPDATE.md` 三档终态）；
4. 本档 §5/§6 之间的分隔线补齐（形态）；
5. 凭证链终消费（`consume-design`，本批收口时）。

---

## §5 实施记录（eng-coder）

---

### 5.1 交付摘要（P0 = F1–F5 + F7；F6 拆分属下一轮）

全部落地，六步各一提交（逐项可独立回滚）；每步 `node --check` 全绿：

| 步 | 提交 | 内容 |
|---|---|---|
| ① F1 | `e05c834` | `pet.js` 左键守卫三处（早退式 ×2 + `(e.buttons & 1)` ×1）；`main.js` 新增 `showMainWindow()`；`pet-clicked` 改「只开不隐」 |
| ② F2 | `82aa26e` | 向导全触点 + 三文件 + `build.files` 摘三行 + 随包文档（README.md / 使用说明.txt）定点同步（7 档） |
| ③ F3 | `cc0b49f` | 托盘重排（11 一级项 / 4 分隔线 / 模式提级 radio / 设置·高级子菜单）+ 模式弹窗文案 |
| ④ F4 | `59b1ba7` | 建窗不自动显示；第二实例 / `--open` / macOS `activate` 收敛于 `showMainWindow()` |
| ⑤ F5 | `37f614d` | dev App 面静默跳过（gate 行新形态；旧整面 `skipped=dev` 退役）+ `dshBinPath()` dev 指针优先 |
| ⑥ F7 | `2cf1289` | 4 组白名单 12 文件纯删除 |

**行数回填（设计 §2.5 指定动作）**：实施起点实测 `main.js` **2831** / `package.json` **116**（与设计 as-of 一致，无漂移）；终态 `main.js` **2789** / `pet.js` **189** / `package.json` **113**。

**AC 机检（AC1–AC6 + AC8）**：全部通过——AC1（守卫 2+1 在场、`pet-clicked`→`showMainWindow`、右键链唯一入口 = `contextmenu`）；AC2 静态（`createWindow` 无 `ready-to-show→show`；`show:false` / `close→hide` / `closed→null` / `will-navigate` / `did-finish-load` 保留）；AC3（五符号 0 处〔排除 `docs/` / `.test-*` / `dsh-bundle`〕+ 三文件不存在 + `build.files` 无残留）；AC4（25 步顺序断言 / 4 分隔线 / 2 radio / 两子菜单逐条）；AC5（旧文案 0 处；`face=app skipped=dev` 仅 `:569`/`:578`；`skipped=toggle-off|in-flight` 保留）；AC6（代码形态与 §2.2.5 伪码逐条一致）；AC8（12 文件不存在 + 0 引用〔唯一命中 `main.js:1745` = 指向保留的 `assets/background.jpg`〕+ 保留面全员在场 + 删除前 sha256 逐字节一致）。**真机面**（AC1/AC2/AC4/AC5 互动与日志实跑、AC6 发布门）归主 agent 复测。

### 5.2 决策透明与事故披露

| # | 事项 | 设计原文 | 落地 | 理由 |
|---|---|---|---|---|
| 1 | `showMainWindow()` 引入步骤 | 设计记于 F4（§2.2.2） | 随 **F1** 引入，F4 完成接线收敛 | F1 的 `pet-clicked` 必须调用它；延至 F4 会在 F1–F4 间留下引用悬空（中间提交不可运行），与「每步可独立回滚」相抵。终态与设计一致 |
| 2 | `showMainWindow()` 零窗口分支形态 | 仅写「不存在则 `createWindow()`」 | 建窗分支追加 `once('ready-to-show')` 等待后显示 | 保「零窗口时建窗后显示」（L3 / TC-26）且不露未加载空窗；已存在分支按设计 `restore/show/focus` 不变 |
| 3 | 托盘 #5 标签 | 主表 `更换背景…`；差异表标「不变」 | 按主表取 `更换背景…` | 表内不对称（#5 有省略号 / #6 无）符合「打开对话框」约定；如需回退为 `更换背景` 请主 agent 裁定 |
| 4 | F2 提交事故与重建 | — | 首提 `47cbfad` 因 git 工具「无路径 commit = `add -A`」混入他会话/主侧 7 档在途改动 → `git reset --mixed` 回 F1 后以 **index-only** 重建 = `82aa26e`（恰 7 档） | 保「每步提交可独立回滚 + 不触碰他方改动区」；README / 使用说明提交版 = HEAD + 本批定点编辑（不含他方 hunk），他方 hunk 完整留存于工作区 |

**形态注（供机检口径）**：① `ready-to-show` 在 `main.js` 仅 1 处（`showMainWindow` 建窗分支），`createWindow()` 内确无（AC2 静态判据面）；② 左键守卫为早退式（`e.button !== 0` ×2 + `e.buttons & 1` ×1），AC1 机检按该形态 grep。

### 5.3 审计与代码评审轮次与终态

- **内部 explore 分歧审计（1 轮）**：终态 **DEVIATIONS**（3 项，全为文档面；**0 未披露偏离 / 0 简化 / 0 超清单**）——D1 §5 未写（本段落地）；D2 建窗分支形态注（见 5.2 #2）；D3 引入步骤映射（见 5.2 #1）。
- **内部 advisor 代码评审（1 轮）**：终态 **pass**（🔴 0；🟡 1 + 🔵 3）——🟡 = §5 落档（本段）；🔵 = 建窗分支显示时点不对称（Deferred：修需引入首帧标志，将偏离 §2.2.2 双分支口径；已记形态注，交主 agent 真机复测裁量）；🔵 = 设计档 §2.2.2 未记该形态（提请主 agent 收口补句，写权不在本角色）；🔵 = AC1 机检措辞 vs 早退式实现（按实现形态执行，实测 2+1）。
- **修正轮**：无代码修正轮（评审未要求代码改动；唯一 🟡 由本段落档收敛）。

### 5.4 本轮交付表（任务书本轮条目）

| # | Status | Requirement | 证据 |
|---|---|---|---|
| 1 | ✅ Done | F1 桌宠左键 = 只开不隐 / 右键 = 只开兑换屋（US-1/US-2） | `e05c834`；`pet.js:99/114/117`；`main.js:2622` |
| 2 | ✅ Done | F2 删除新手向导（US-3） | `82aa26e`；五符号 0 处 / 三文件 / `build.files` / 随包文档 |
| 3 | ✅ Done | F3 托盘重排（US-4） | `cc0b49f`；11 项 + 4 分隔线逐条 |
| 4 | ✅ Done | F4 冷启动形态（US-5） | `59b1ba7`；显示入口收敛 |
| 5 | ✅ Done | F5 更新门禁（US-6） | `37f614d`；gate 新形态 + dev 指针优先 |
| 6 | ✅ Done | F7 无用文件清理（US-8） | `2cf1289`；12 文件 + 预检 0 引用 |
| 7 | ⏭ 出批 | F6 `main.js` 拆分（US-7 / AC7） | 任务书明确出批——下一轮，本轮未实施 |

## §6 验收核销（主 agent）

### 6.1 核销结论（F6 拆分面）

- **F6 代码已落地**：九层提交 `b25b6e6`…`1384769` + 收尾 `2ca9ef8` / `20dd78c`；终态 `main.js` **204 行** + **15 个 `shell-*.js`**（最大 `shell-pet-geometry.js` **437** < 500 硬限）。
- **⚠️ 记录缺口——如实披露（不代写）**：本档 **§5（eng-coder 段）未含 F6 的实施记录**；§5.4 交付表第 7 行仍写「F6 ⏭ 出批——本轮未实施」（`:420`），与代码实况不符。段权属 eng-coder，**主 agent 不代写**——F6 的实施证据改由 **git 提交历史 + B07 §6 的机检与真机复测**承载；该缺口在此登记为**已知记录债**（不追溯补写；若后续需补，须由 eng-coder 另起一轮并走新的设计凭证）。
- **F6 机检（B07 轮复测）**：IPC 面拆分前后一致（`ipcMain.on` **11** / `handle` **12**）；15 档全部 < 500 行；`node --check` 全仓 **40/40** 绿；未绑定命名空间引用全扫 **0**（带阳性对照）。
- **F6 遗留缺陷**：`shell-mode.js:131/141` 调用未绑定 `notifier`（技术待办 T12）**已由 B07 修复**并经真机确认（托盘两菜单项正常）。

### 6.2 台账核销（本批面）

- **R5 / R6 → 已核销**：P0 六步 + F6 拆分均落地；真机目视（用户 2026-09-16 22:43）+ B07 轮机检与真机复测。
- **T1 / T2 → 已核销**：T1 由 AC1 真机确认（左右键语义）；T2 由 F6 终态实测确认（`main.js` 2831 → 204 + 15 模块）。
- 以上逐条移入 `docs/TODO-archive.md`。

### 6.3 残留与消解（不阻断）

| 残留 | 消解 |
|---|---|
| O1 跨档残留（`AUTO-UPDATE.md` §3.1 的 AC7 / TC-14 dev 判定面） | **已由 B07 设计修订收口** |
| O2 / O6 / O7（文档指针与体量债） | T13（已收口部分）与 T16 承载 |
| F6 记录缺口（§6.1） | 登记为**记录债**；补写需 eng-coder 另轮 + 新凭证 |
| `market.js` **500** 行贴线（未超 500 硬限） | 维持现状（R5 明确出批项） |

### 6.4 本批边界说明

本档 **§1–§6 至此全落**。§6 的核销依据中，「F6 拆分」一面依赖 B07 轮的机检与真机复测（承 §6.1 的缺口披露）；其余（P0 = F1–F5 + F7）依据本档 §4 的真机目视验收记录。
