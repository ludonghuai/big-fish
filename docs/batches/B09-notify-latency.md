# B09 —— 任务完成提醒的及时性

> 六段制批次档（每段一位作者、append-only）：§1 主 agent · §2 eng-designer · §3 评审子代理 · §4 主 agent · §5 eng-coder · §6 主 agent。

## §1 批次立案（主 agent）

### 1.1 立案背景

「Bigfish 任务已完成」提醒**迟滞太久**：用户体感为「早就做完了结果才发消息」。机制（主 agent 读码核实）：

- 判据 = **末次写入后空闲 `IDLE_NOTIFY_MS`**（`main.js:49` `30 * 1000`）⇒ 提醒时刻 = 末次写入 + 30 s（另有 5 s 周期定时器，实测落在 30–35 s）；
- 忙信号 = `~/.dsh` 下**非** `profiles/` · `node_modules` 的写入（`shell-notify.js` `isIgnoredPath`）——**与「真实完成」无因果**，只是「最近有没有磁盘活动」。

⇒ 提醒时刻与真实完成时刻**脱钩**，且固定多出 30 s。

### 1.2 任务来源

- 用户 2026-09-17 真机验收原话：「**通知间隔的时间太长了，早就做完了结果才发消息**」。
- 台账需求池 **R10**（本批承载）。
- **注意**：B07 的需求条目 US-11 明文锁定「通知文案 / 阈值（`IDLE_NOTIFY_MS = 30 s`）/ 托盘开关语义**不改**」⇒ 本项属**新需求点**，本批为独立批次，不改 B07 已核销的任何判据。

### 1.3 前置事实（主 agent 2026-09-17 实测，供设计直接引用）

| # | 事实 | 证据 |
|---|---|---|
| ① | **忙信号有效**（不必换信号源）：`~/.dsh/sessions/` 与 `~/.dsh/storages/` 在任务期间持续写入，且都不在忽略面内 | 2026-09-17 11:37 实测两目录 mtime 持续更新；`shell-notify.js:44-54`（`isIgnoredPath` 只忽略 `profiles` / `node_modules`） |
| ② | **「从后端输出识别回合结束」当前不可行**：后端启动后**不再输出任何行**（最后两次启动之间仅两行） | `%APPDATA%\bigfish\bigfish.log` 全档比对（`start backend` 行之间无其他输出） |
| ③ | 方向 = **缩短空闲阈值**（30 s → 8 s 量级），取值与误报边界由设计定 | 主 agent 2026-09-17 定向（用户已授权主 agent 定方向）；R10 台账行 |
| ④ | 钉版 `0.1.0-rc.6` 与活跃 `0.1.5-rc.1` 的后端行为差异不影响本批（本批只看 `~/.dsh` 写入面） | B08 §1.3 |

### 1.4 范围（含边界）

**做**：`shell-notify.js` 的**空闲阈值与判定**（可含 `main.js` 的常量注入）；阈值取值依据（误报边界）写成可验收判据。

**不做（边界）**：

- 不改通知**文案**、不改**托盘开关语义**、不改「关闭⇒重开不补发」语义、不改回落/`fs.watch` 机制（B07 已核销面**不动**）；
- 不引入 harness 侧改动（② 已排除：需改 harness ⇒ 出批）；
- 不新增依赖、不新增文件。

### 1.5 待决点（交给设计）

- **取值**：8 s 的中心值？还是「短阈值 + 二次判定」？——须给出**误报边界**：任务中途的静默窗口（如长工具调用、等待模型）不得触发「已完成」提醒；设计需说明在何种活动模式下会误报、以及如何界定可接受。
- **可配性**：是否随 env 暴露（B07 已有 `BIGFISH_WEB_URL_WAIT_MS` 先例，测试钩子口径）。
- **`notifyOnComplete` 关闭态**与阈值变更的交互（不得回退既有语义）。

### 1.6 验收标准（初拟，供设计细化并回指需求条目）

| # | 验收标准（可机检 / 可真机） |
|---|---|
| AC1 | 新阈值生效且可由测试钩子观测（取值以设计为准，须写明默认值与边界） |
| AC2 | **误报边界**：忙碌期内的静默窗口不产生「已完成」提醒（设计给出的判据可机检或桩测） |
| AC3 | 通知语义**不回退**：一个活动周期一次提醒 · 开关关闭⇒零提醒 · 关闭期活动⇒重开不补发 · 回落路径同语义 |
| AC4 | 真机体感：任务完成后**显著更快**收到提醒（用户判；须给出可对照的量化口径，如「末次写入 → 提醒」的实测区间） |

### 1.7 关联

- 需求档：`docs/requirements/SHELL.md`（新增条目——编号由 eng-designer 落，如 US-12 + NFR 补充）
- 设计档：`docs/design/SHELL-UX.md`（新增小节；**不得修改 B07 已核销的 §2.2.9 / §2.2.10 判据**，只做追加与回指）
- 台账：R10（本批承载）

---

## §2 本批任务书（eng-designer）

### 2.1 任务目标与范围

把「任务完成」提醒从「末次写入后空等 30 s」升级为「**回合（turn）结束 + 静默 8 s**」——不误报、可降级、不回退既有语义。

- 方向与依据：本档 §1.3（方向 = 缩短空闲阈值；换信号源不可行）+ §1.5 待决点（取值 / 误报边界 / 可配性 / 与既有语义交互）。
- 需求条目：`docs/requirements/SHELL.md` **US-12**（新增；并显式修订 US-11 的「阈值不改 / 空闲达阈值即通知」两处——**US-11 原文不动**）+ §四 **NFR-5 B09 注记**（判定读的 I/O 口径）。
- 设计落点：`docs/design/SHELL-UX.md` §2.1 **选型 K**（K-1 阈值取值 / K-2 判定源）·
  §2.2.12（判定面：实测证据 / 判定源六事实 / 判定前提 / 四态规则 / 执行面 / 降级与诊断行 / 交互表 / 修订面 / 已知限制）·
  §2.2.6 注 S6 + 锚点 **B09 附注** · §2.3 **B09 受影响文件表** · §2.4 **DD-29…DD-33** ·
  §2.5 **C27–C31 / O15·O16 / L-B09-1…4** · §2.6 **U-13** 与 B09 追认块 · §3.1 **AC17–AC20** ·
  §3.2 **TC-46–TC-55** · §3.3 手段 11 + B09 人工项。

### 2.2 本批条目（I1–I4；三方条目一致基准）

| # | 条目 | 回指 | 验收 |
|---|---|---|---|
| I1 | **阈值面**：`IDLE_NOTIFY_MS` 30 s → **8 s**（= Harness 投影缓存写后阈值 5000 ms + 3000 ms 余量）；测试钩子 `BIGFISH_IDLE_NOTIFY_MS`（默认 8000，承 `BIGFISH_WEB_URL_WAIT_MS` 先例）；判定源不可用时的**降级口径**（退回 30 s + 一条诊断行） | US-12 | AC17 |
| I2 | **判定面**：触发条件增「**回合已闭合**」合取项（读 Harness 会话投影缓存 `record.rows.turnBoundary.val.openTurnStartSeq`） | US-12 | AC18 |
| I3 | **语义不回退**：一个活动周期一次提醒 / `notifyOnComplete=false` 零提醒 / 关闭期活动重开不补发 / 回落路径同判定面 / 通知文案三串逐字不变 | US-11（口径修订见 US-12） | AC19 |
| I4 | **真机时延量化**：末次写入（= 投影缓存记录 mtime，强制落盘点）→ 提醒可见 **8–13 s**（5 s 周期对齐；现状对照 30–35 s） | US-12 | AC20 |

> 三方一致（硬）：**本档 §2 本批条目 I1–I4 = 设计档 §3.1 AC17–AC20 回指条目 = 需求档 US-12**（+ NFR-5 B09 注记 / 对 US-11 的显式修订面）。
> 本档 §1.6 的 AC1–AC4 ↔ 设计 AC17–AC20：**AC1 → AC17 · AC2 → AC18 · AC3 → AC19 · AC4 → AC20**。

### 2.3 判定规则（判据句，逐字；实施按此执行）

> 静默达 `IDLE_NOTIFY_MS` 后，判定**一次性**执行（异步）：
> ① 取 `<dshHome>/sessions/**` 下 mtime 最大的会话日志 `L` 与 `<dshHome>/storages/session_projcache/sessions/*.json` 下 mtime 最大的记录 `C`；
> ② `C` 缺失 / 不可解析 / 无 `record.rows.turnBoundary` / `rows.turnBoundary.ver !== 2` ⇒ **降级**（阈值改用 `IDLE_NOTIFY_FALLBACK_MS` = 30 s = 现状语义）；
> ③ `mtime(C) + 1000 ms < mtime(L)` ⇒ **本轮不提醒**（快照落后于会话日志）；
> ④ `rows.turnBoundary.val.openTurnStartSeq !== null` ⇒ **回合在途 ⇒ 不提醒**；
> ⑤ 否则 ⇒ **通知一次**（既有通知语句逐字保留）。

- **误报边界（本批核心判据）**：设计者实测（本机真实会话事件日志，解 zstd 帧取事件时间戳）单次 `tool/call → tool/result` 静默
  **21.9 s**、其间 `~/.dsh` 零写入 ⇒ **只缩阈值必然误报**（30 s 阈值下 >30 s 的工具调用 / 构建同样误报）；故完成判据必须带 Harness 自己落盘的
  `turn/end` 标记——该档**每个 `turn/end` 立即无条件落盘**（`@deepseek-ai/dsh-session-projection-cache/lib/index.js:290-321`；写入阈值 `writeEveryEvents=200` / `writeIntervalMs=5000`，源 = `@deepseek-ai/dsh-base/cordis.patch.yml`）。
- **承重前提**：`IDLE_NOTIFY_MS`(8000) **必须大于** `writeIntervalMs`(5000)——否则判定会读到尚未折叠最近事件的快照；前提失效 ⇒ 规则③ 兜底（方向 = 抑制）+ 规则④ 静态判据把门（设计档 §2.2.12）。
- **执行面**：判定仅在「静默 ≥ 阈值且本轮未通知」时触发，结果按 `lastBusyAt` 记忆（每静默窗至多一次读，任何写入自动失效重判）；异步 + 防重叠，5 s 回调内无同步 I/O（NFR-5 判据不变）。

### 2.4 明确出批（不做）

- 不改通知文案 / 不改托盘开关语义 / 不新增通知类型（「等待你的确认」= 设计档 open 项 U-13）；
- 不改 busy 判定面（`isIgnoredPath` 任一段落谓词）与回落机制；不新增依赖；**不新增文件**（桩测脚本 = 可选手段，若采用须经主 agent 裁定，落 gitignore 面）；
- 不改 Harness（`dsh-bundle/` 与 `dsh-update/versions/**` 只读）；不代 **B10** 修 `shell-affinity.js` 读面（技术待办 T14）。

### 2.5 受影响文件（预估；实施期实测回填本档 §5）

| 文件 | 当前行数 | 改动 | 预计 | 末行数 |
|---|---|---|---|---|
| `shell-notify.js` | 163 | 新增 `completionGate()` + 记忆 / 在途 / 诊断三标记（`:13-24` 注入与状态区）+ tick 判定分支（`:132` 条件处：短阈值 + 判定两步）+ 头注释（`:86-96`）口径 | +约 70 / −约 6 | ≈227 |
| `main.js` | 204 | `:49` 阈值 30 s → 8 s + 新增 `IDLE_NOTIFY_FALLBACK_MS`(30 s) + env 解析 `idleNotifyMs()`（承 `shell-backend.js:66-74` 形）+ `:61` 注入面 +1 键 | +约 12 / −约 2 | ≈214 |
| `docs/requirements/SHELL.md` | 196 | US-12 / NFR-5 注记 / §二 范围 / 头部关联指针 / 变更记录 2 行 | +21 / −0（实测） | **217**（落档实测） |
| `docs/design/SHELL-UX.md` | 972 | 本档全部 B09 追加（落点见 §2.1） | +193 / −0（实测） | **1165**（落档实测） |

不触碰（明确）：`shell-tray.js`（175，`setNotify` 面不变）/ `shell-settings.js`（60，`notifyOnComplete` 默认值不变）/ `shell-affinity.js`（305，读面失效归 B10）/ 其余 `shell-*.js` / `package.json`（`build.files` 与 `dependencies` 均不动）/ `docs/TODO.md` 与 `docs/README.md`（主 agent 写域）。

### 2.6 验收与用例（细化见设计档 §3.1 / §3.2）

- **AC17**（阈值与判定面 + 降级面）：静态常量关系 `IDLE_NOTIFY_MS`(8000) > 5000、注入两键、判定函数单一实现（含 `ver !== 2` 守卫）、无 `*Sync(` / 无 `process.platform`；降级面 = 隔离 `DSH_HOME` ⇒ 30 s 面 + 诊断行（`grep "completion gate unavailable"`）。
- **AC18**（误报边界）：真机 ≥15 s 工具调用（或长构建）⇒ 静默期内**零**提醒、回合结束后一次提醒；规则③（快照落后）/ 规则④（回合在途）分别构造 ⇒ 均不提醒。
- **AC19**（语义不回退）：开关关闭零提醒 / 关闭期活动重开不补发 / 一周期一次 / 回落路径同判定面 / 文案三串逐字；静态 = 既有守卫行与清忙态调用零 diff。
- **AC20**（真机时延）：末次写入 → 提醒可见 **8–13 s**（现状对照 30–35 s）；用户判「显著更快」。
- 用例 **TC-46…TC-55**（正常 / 边界 / 错误：env 钩子、隔离 `DSH_HOME` 降级、快照落后与回合在途两态、开关与不补发、回落路径、多会话、静态判据组）。
- **实施期硬复核点**（任一不成立 ⇒ **停下报告**，不在实施期自行放宽规则）：① 回合结束时缓存记录确实落盘且 `openTurnStartSeq === null`；② 静默期中快照不落后于会话日志（规则③ 不误伤）；③ shipped composition 的 `writeIntervalMs` = 5000。

### 2.7 需用户 / 评审裁定

- **open 项 U-13**：「等待你的确认」类提醒是否设立（本批不做——新增通知类型 / 新文案超批次边界）。
- **待追认项**（设计已定，可改判）：**DD-29**（阈值 8 s）/ **DD-30·DD-31**（判定源 = Harness 会话投影缓存；不可用 ⇒ 退回 30 s + 一条诊断行）/ **DD-33·L-B09-4**（mtime 兜底与承重前提）/ **L-B09-1**（等待用户确认期间不再收到提醒——现状会发一条语义错误的「已完成」）。

### 2.8 行数补正注（本任务书自指；D3 计数与枚举同改）

本角色本批改后实测行数（换行符计数）：`docs/requirements/SHELL.md` **217**（原 196，+21）、`docs/design/SHELL-UX.md` **1165**（原 972，+193）——设计档 §2.3 B09 表两处自指行已按该实测值回填；两档全文行宽 ≤ 300 字符（不含行尾 CR；改后实测最宽 270 / 300）。

### 2.9 行宽压行（形态修正 · 实测）

**改动（本角色本轮就地改写本段两行；同轮新加行的行宽属格式修正）**：

- §2.1「设计落点」条目（改前行 `:72`）**351** 字符 → **5** 行（最长 89；`§2.2.12（判定面：…）` 一条整段不再掐断）；
- §2.3「误报边界」条目（改前行 `:95`）**386** 字符 → **3** 行（最长 209）。
- 手段 = 就地续行（续行缩进 2 空格）；断点 = 原文中的**单个空格**（原文无空格处不断行）⇒ 断行不吞字、不加字。内容一字不删、无语义改动、条目集合不变（I1–I4 = 4 条）。

**逐字保全（机检，源 = 改前文本）**：两块「各行去首缩进后以单空格相接」**逐字等于**原文（`===` 断言 true）；
非空白字符投影逐字相等；`sha256(重建) === sha256(原文)`（`:72` → `ddd0159…`，`:95` → `bb1c7cf…`）。
除这两块外，全档行序列与改前逐行相同（139 行对 139 行）。

**实测（压行后）**：§2 段内最大行宽 **248**（`:120`）；§1 段内最大行宽 **193**（`:26`，**无**超宽行）；整档 >300 字符行数 = **0**（判据 = 不含行尾 CR 的字符数）。本档行数（换行符计数）：压行后 **146**，含本注自身 **160**（本注 = `:140–153`）。

### 2.10 B09 修正轮 1 补正注（9 条；条文权威面 = 设计档 / 需求档）

源 = 本档 §3 轮次 1 发现 #1–#9（主 agent 裁决 = 全部 Dispatched）。本注只记「§2.3 判据句逐字面」的生效修订与落点；§2.3 原文按 append-only 保留（生效口径以本注 + 设计档为准）。

- **#1（🔴）判定面定稿 = 四态**（`open` / `stale` / `unavailable` / `done`）：「快照落后于会话日志」= `stale` ⇒ **本轮不提醒**（**非降级**、不改阈值）；降级仅限 `unavailable`（记录缺失 / 形态不符 / 解析失败）；
  `stale` 与降级**各发一条诊断行**（同形、各一条/监视器生命周期）；持续 `stale` ≥ 30 s ⇒ 按 `unavailable` 处置（抑制上界）。落点 = 需求档 `docs/requirements/SHELL.md:173`（US-12 契约句）、设计档 `docs/design/SHELL-UX.md` §2.2.12 规则③·③′ / §2.4 DD-49 / §3.1 AC18 / §3.2 TC-48。
- **#2** 降级面判据取证化：两条诊断行经 `backend.writeDiag`（console + `bigfish.log` 双写）⇒ AC17 / TC-50 的 `grep` 目标 = `bigfish.log`（console 仅辅证 / 桩测面）。
  落点 = 设计档 §2.2.12「降级与可观测性」/ §2.2.6 注 S6（新增 `require('./shell-backend.js')`；同层 L1 + 无环 ⇒ 合 §2.2.6 规则 1）/ §2.5 C40 / **锚点 B09 附注（诊断行 1 → 2 条）** / §3.1 AC17 / §3.2 TC-50 / §3.3 手段 11。
- **#3** 规则① 取档谓词**具名化**：`L` = `<dshHome>/sessions/<项目段>/<会话段>/session.v3.jsonl.zstd`（深度 = 3 段 + 文件名写死；**无 `**` 无界写法、不做目录递归遍历**）；读次数上界按「会话段数（含项目段数）」计。
- **#4** 承重前提**源码补证**（设计档 §2.2.12 事实 #7）：写后阈值 = **write-behind 节流 + 尾部追写**（每次写入取当前实时状态快照 ⇒ 不丢事件）；残余失效面登记 **L-B09-5** + 规则③′ 抑制上界 `GATE_STALE_MAX_MS`(30000)。
- **#5** 单函数体量核对按实测重写：`startCompletionWatcher()` 现 **43** 行（`:97-139`）→ 预估 ≈60；`completionGate()` ≈40（原记 163 → 178 为口径错误）。
- **#6** 设计档头部回指区间与关联批次同步（US-1…US-15、NFR-1…NFR-7；B06 / B07 / B09 / B10 / B12 指针）。
- **#7** 常量符号名统一 = **`GATE_FRESH_TOLERANCE_MS`**（注 S6 与规则③ 同名；静态判据按同一名核对）。
- **#8** 需求档 NFR-5 标题验收指针 = **「AC3、AC17」**（`docs/requirements/SHELL.md:261`）。
- **#9** TC-49 补注：钩子置 `0` 仅供**驱动四态**，不得用于时延 / 误报面断言（阈值 ≤ 写后阈值 5000 ms，故意违前提）。

**§2.3 判据句修订面（逐字）**：生效规则块 = 设计档 §2.2.12「判定规则」（①③ 改写 + 新增 ③′，②④⑤ 逐字不变）。§2.3 内的 `**` 无界谓词、`FRESH_TOLERANCE_MS` 符号名、诊断行单条计数三处**作废**。

**实测（本轮落档）**：`docs/design/SHELL-UX.md` 1601 → **1649**、`docs/requirements/SHELL.md` 299 → **303**（换行符计数）；两档行宽 ≤ 300 字符（不含行尾 CR；最宽 **300** / **299**，超宽 **0** 行）。
实施面口径：`shell-notify.js` 70 → **78** / 227 → **235**；新增 `shell-backend.js` 行（`writeDiag` 导出行 = 与 B10 §2.2.13 / DD-40 **同一行**——两批共用、先落者建立，不得重复添加）。

**发现即报告（本角色禁改面）**：本档 §3 表 7 行实测 **362–783 字符**（评审者段，本角色不改）⇒ 本档 §2.9「整档 >300 字符行数 = 0」的自述在 §3 落笔后**不再成立**——请评审侧 / 主 agent 处置（压行或改写该自述）。

### 2.11 B09 修正轮 2 补正注（1 条；条文权威面 = 设计档）

源 = 本档 §3 **轮次 2 发现 N1**（🟡）；主 agent 裁决 = **Dispatched**（**文档自洽项——不改任何语义**）。本注只记落点；§2.3 原文按 append-only 保留（生效口径以设计档 §2.2.12 为准）。行号 = as-of 2026-09-17（B10 / B12 同档并行写入，行号仅作 as-of 参考——D4）。

**修法（唯一 1 条）**：设计档 §2.2.12「判定前提」块的兜底句由「前提失效（配置被改 / 形态换代）⇒ 规则② 兜底（方向 = 抑制）」**拆写为两路**：

- ① **形态换代**（域 v7 / 行 `ver` ≠ 2——同 L-B09-3）⇒ **规则②** 降级（= `unavailable`：阈值退回 30 s + 降级行）；
- ② **配置被改**（`writeIntervalMs` ≥ 阈值）⇒ **规则③** 抑制（方向 = 安全侧：`stale` ⇒ 本轮不提醒），持续超 `GATE_STALE_MAX_MS`(30000) 未转 `done` ⇒ 由 **③′** 转降级（退回 30 s + 降级行）。

**落点与改前改后（逐字）**：

- `docs/design/SHELL-UX.md:846–849`（改前 = 同档 `:846` 单行）——改前：`> 该前提使「静默 + 快照回合态」可作完成判据；前提失效（配置被改 / 形态换代）⇒ 规则② 兜底（方向 = 抑制，安全侧）+ 实施期真机复核（TC-46）；登记为 **L-B09-4**。`
  → 改后 = `:846`（引句 + 「按成因分两路兜底」）+ `:847` ① + `:848` ② + `:849`（实施期真机复核 TC-46 把门 + L-B09-4 登记）。
- `docs/design/SHELL-UX.md:917`（L-B09-4）——改后补句 = 「持续超 `GATE_STALE_MAX_MS`(30000) 未转 `done` 则由 **③′** 转降级（退回 30 s + 降级行）」（原只写规则③）。

**一致性自查（裁定判为一致的三处既有条文，逐条未改）**：

- 设计档 DD-33（现 `:1301`）「兜底仅在前提失效时生效，方向 = 抑制（安全侧）」——所指 = **mtime 新鲜度兜底（规则③）** ⇒ 与新 `:846` 的 ② 路同向；① 路（判据读不出 ⇒ 规则② 降级）不在 DD-33 的覆盖面内 ⇒ 无相抵。
- 本档 §2.3 `:102`「前提失效 ⇒ 规则③ 兜底（方向 = 抑制）+ 规则④ 静态判据把门」——指 ② 路 ⇒ 一致。
- 设计档 `:198`（K-2 候选 1 取舍）「换代即降级——§2.2.12 规则②」——指 ① 路 ⇒ 一致。

**改后未改面（硬约束核对）**：规则 ①–⑤ 与 ③′ 的实质行为**零改动**（只拆兜底句的归属指引）；未新增判据 / 未改计数 / 未新增条目（本批条目 I1–I4 = 4 条不变；AC17–AC20 不变；DD / TC 计数不变）；需求档本轮未改。

**实测（本轮落档）**：`docs/design/SHELL-UX.md` **1768 → 1774**（换行符计数；+6 = 判定前提块 1 → 4 行 / 设计档变更记录 3 行）；`docs/requirements/SHELL.md` **310**（本轮未改该档）；设计档行宽 ≤ 300 字符（不含行尾 CR；实测最宽 **300**、超宽 **0** 行）。

**发现即报告（本角色禁改面 / 本轮现存）**：

- 本档 §3 段 >300 字符行 = **15** 行（353–783 字符；分布 = 轮次 1 表 6 行 + 轮次 1 已核验行 1 + 轮次 2 表 6 行 + N1 行 1 + 轮次 2 已核验行 1）——§3 = 评审子代理写域，本角色不改；由此 §2.9「整档 >300 字符行数 = 0」的自述在 §3 落笔后不再成立（§2.10 已报，本轮计数由 7 行增至 15 行）⇒ 请评审侧 / 主 agent 处置（压行或改写该自述）。§1 / §2 段实测 **0** 行超宽。
- 设计档 §2.1 K-1 表**被否决候选 2（5 s）**行（`docs/design/SHELL-UX.md:184`）写「5 s ≤ 写后阈值 ⇒ 判定可能读到尚未追上日志的快照（**规则②兜底** ⇒ 白白推迟，收益被吃掉）」——按本轮修正后的归属，「参数 / 配置导致快照落后」一路属**规则③**（抑制）而非规则②（降级）；该行**不在本裁定修法（`:846` / `:914`）范围内** ⇒ 报而不改，请主 agent 裁定是否并入下一轮。

### 2.12 B09 修正轮 3 补正注（1 条 + 全档归属自查；条文权威面 = 设计档）

源 = 本档 §2.11「发现即报告」末条（**报而不改**项：设计档 `:184` K-1 被否决候选 2 的归属句）；主 agent 复核裁定 = **修**（Dispatched）。本注只记落点与自查；§2.3 原文按 append-only 保留（生效口径以设计档 §2.2.12 为准）。行号 = as-of 2026-09-17（B10 / B12 同档并行写入，行号仅作 as-of 参考——D4）。

**修法（唯一 1 条）**：设计档 `docs/design/SHELL-UX.md:184`（§2.1 K-1「阈值取值」表**被否决候选 2「5 s」**的判据句）——「规则②兜底」→ 与 `:848`（两路兜底之 ②「**配置被改**」）一致的**规则③** 口径（抑制；持续超 `GATE_STALE_MAX_MS`(30000) 由 **③′** 转降级）。候选取值 / 结论（否决）与其余判据文字**均不改**。

**改前改后（逐字，`:184`）**：

- 改前：`…但 5 s **≤** 写后阈值 ⇒ 判定可能读到尚未追上日志的快照（规则②兜底 ⇒ 白白推迟，收益被吃掉）；…`
- 改后：`…但 5 s **≤** 写后阈值 ⇒ 判定可能读到尚未追上日志的快照（**规则③** 兜底（抑制；持续超 `GATE_STALE_MAX_MS`(30000) 由 **③′** 转降级）⇒ 白白推迟，收益被吃掉）；…`

**全档归属自查（`规则②` / `规则③` / `规则③′` 逐处；判据 = `:846–849` 分路：形态换代 ⇒ 规则②；参数 / 配置 ⇒ 规则③ / ③′）**：

| # | 行号（设计档） | 该行所指的成因 ⇒ 兜底路 | 与 `:846–849` 一致？ |
|---|---|---|---|
| 1 | `:184`（**本轮修正**） | 阈值 ≤ 写后阈值 = **参数 / 配置** ⇒ **规则③** 抑制（+ ③′ 上界） | ✔（改前写「规则②」= ✘，已修） |
| 2 | `:198`（K-2 候选 1 取舍） | 形态换代（换代即降级）⇒ 规则② | ✔ |
| 3 | `:564`（§2.2.6 锚点 B09 附注） | 诊断行 1 = 规则② / ③′ 的**降级**面 | ✔ |
| 4 | `:565`（同上） | 诊断行 2 = 规则③（`stale`） | ✔ |
| 5 | `:847`（判定前提 ①） | 形态换代 ⇒ 规则② | ✔（权威句） |
| 6 | `:848`（判定前提 ②） | 参数 / 配置 ⇒ 规则③ + ③′ | ✔（权威句） |
| 7 | `:850`（残余失效面） | 写路径 fail-soft ⇒ 持续 `stale` ⇒ 规则③′ 上界 | ✔ |
| 8 | `:873`（降级与可观测性） | 降级 = 规则② 与 ③′ 的处置 | ✔ |
| 9 | `:885`（实施期复核点 ②） | 快照不落后 ⇒ 规则③ 不误伤 | ✔ |
| 10 | `:915`（L-B09-2） | 多会话 mtime 对不齐 ⇒ 规则③ / ④ | ✔ |
| 11 | `:916`（L-B09-3） | 形态换代 ⇒ `unavailable`（规则② 收口） | ✔ |
| 12 | `:917`（L-B09-4） | 判定前提失效（参数 / 配置）⇒ 规则③ + ③′ | ✔ |
| 13 | `:918`（L-B09-5） | 写路径 fail-soft ⇒ 规则③ 连续 `stale` + ③′ | ✔ |
| 14 | `:1320`（DD-49） | 持续 `stale` ≥ 30 s ⇒ 规则③′ 抑制上界 | ✔ |
| 15 | `:1382`（§2.5 L-B09 指针行） | L-B09-5 缓解 = 规则③′ | ✔ |
| 16 | `:1407`（T14 / B10 读面） | 读面布局换代 ⇒ 规则② 封口 | ✔ |
| 17 | `:1501`（AC18） | 规则④（在途）/ ③（落后）/ ③′（上界） | ✔ |
| 18 | `:1599`（TC-48） | 规则④ / ③ / ③′ | ✔ |
| 19 | `:1713`（§3.3 人工项） | TC-48 / TC-53 = 规则③·④ 构造 | ✔ |
| 20 | `:1754`（变更记录·修正轮 1） | 规则③′ 抑制上界 | ✔ |
| 21 | `:1775`（变更记录·修正轮 2） | ① 形态换代 ⇒ 规则② | ✔ |
| 22 | `:1776`（同上） | ② 配置 ⇒ 规则③ + ③′ | ✔ |

- 计数口径：`≤ :1781` 面上含 ② / ③ 族符号的行 = **22** 行（本轮改后；`:184` 为修正项，其余 **21** 行均一致）；`规则①` 行（`:1753` 取档谓词具名化）**不属** ② / ③ 族，不计入。行号 as-of 本轮修稿后（`≤ :1781` 段不受本轮变更记录追加影响）。

**改后未改面（硬约束核对）**：规则 ①–⑤ 与 ③′ 的实质行为**零改动**；K-1 表任何候选的取值 / 结论**零改动**；未新增判据 / 未改计数（本批条目 I1–I4、AC17–AC20、DD / TC 计数均不变）；需求档本轮未改。**附带留痕**：设计档 §四 变更记录增本轮回行 **3** 行（与修正轮 1 / 2 同形）——若主 agent 判其超本轮写域，可即刻回退。

**实测（本轮落档）**：`docs/design/SHELL-UX.md` **1782 → 1785** 行（换行符计数；+3 = 变更记录 3 行，`:184` 为等行数单行替换）；行宽 **0** 行超 300（不含行尾 CR；最宽 **300** = `:1490` B06 旧行）；`:184` 改后行宽 **198**（改前 146）；EOL 全档 **1785** 行皆 CRLF。`docs/requirements/SHELL.md` **310** 行（本轮未改该档）。

**发现即报告（本轮报而不改项，单列）**：

- `:1600` / `:1663`（TC-49 注）：「钩子置 `0` 使阈值 ≤ 写后阈值 5000 ms ⇒ 故意违反判定前提」——成因属**参数 / 配置**类（即 ③ / ③′ 面），但行内**未声明规则归属**（只写「故意违反判定前提」）⇒ 与 `:846–849` **无相抵**，未改。若要显式标注（例「该态即规则③ 面」）= **新增判据文字**，超本轮边界 ⇒ 请主 agent 裁定是否另轮并入。
- `:1300`（DD-29 取舍列）：「5 s（阈值 ≤ 写后阈值，判定前提相抵）」——同上（无规则归属）⇒ 无相抵，未改。
- `:1304`（DD-33）：「兜底仅在前提失效时生效，方向 = 抑制（安全侧）」——该行的口径裁定已由本档 §2.11 给出（所指 = mtime 新鲜度兜底（规则③）⇒ 与 `:846` 的 ② 路同向；① 路不在其覆盖面内）⇒ 本轮**沿用不重开**。
- 工具侧观察（非文档缺陷）：`insert_after` 首次写入设计档变更记录行时产生 **2 行 LF-only**（`:1782–1783`），经 `edit` 重写后归一为 CRLF —— `insert_after` 与 `edit` 的 EOL 处理不一致，落笔他档时须事后核 EOL（本轮已核：0 行 LF-only）。
- 计数自述仍不成立项（承 §2.10 / §2.11）：本档 §2.9「整档 >300 字符行数 = 0」因 §3 段存在超宽行而不成立（§3 = 评审子代理写域，本角色不改）；**本轮新增 §2 段 0 行超宽**。

### 2.13 B09 实施后收口轮（6 条；条文权威面 = 设计档 `docs/design/SHELL-UX.md`）

源 = 本档 §5 残余 3 / 4 + advisor 裁决 #1 / #7；主 agent 裁决 = 全部 Dispatched。**只回填实测与补枚举**——不新增语义 / 不新增判据 / 不新增 TC / 不改任何计数 / 不触碰 `GATE_*` 三常量与四态语义。
行号 = as-of 2026-09-17（B10 / B12 同档并行写入，行号仅作 as-of 参考——D4）。实施已完成（本角色**未碰任何 `.js`**）。

**六条落点与改前改后（逐字）**：

1. **行数回填 + 体量裁定**（§2.3 B09 表）
   - `:1221`（`shell-notify.js` 行）——改前：`| +约 78 / −约 6 | ≈235 → **实施期实测回填** |`；
     改后：`… | ≈235 → **308**（**实施期实测已回填**，+73；越 300 软档——**体量裁定见下**；< 500 硬限） |`
   - `:1223`（`main.js` 行）——改前：`| +约 12 / −约 2 | ≈214 → **实施期实测回填** |`；
     改后：`… | ≈214 → **217**（**实施期实测已回填**，+3） |`
   - `:1233–1235` 新增 **>300 软档 体量裁定** 3 行（形式承 B07 `shell-backend.js` 先例）：
     ① 事实 = 越 300 软档但**未触 400 消解线**（NFR-3 的 500 硬限亦无影响）；
     ② 理由 = **纯增量、职责未增**（档内既有域「完成提醒」不变；无新文件 / 无新依赖 / 无新落盘机制——诊断行复用 B07 / B10 既有 `writeDiag` 面）+ 最长函数实测 = `startCompletionWatcher()` **47** 行（`:238-284`）· `completionGate()` **42** 行（`:117-158`）⇒ **无 >300 函数面**；
     ③ 消解路径 = **保持单文件、不拆分**——触发条件（**消解期**）= 该档**逼近 400**（> 380）或再获增量批次 ⇒ 该批立案时先做拆分复核（候选切面 = busy 记法与判定面 vs 通知发送面）。

2. **§2.2.6 注 S6 补函数清单**（`:520–521`，新增 2 行）：辅助函数 **5** 名逐名 + 职责一行（**均不外导出**）——
   `completionGateProbe()`（判定准入：异步 + 防重叠 + 按 `lastBusyAt` 记忆）/ `completionGateDue()`（处置面：纯算术，四态 ⇒ 本轮是否提醒）/
   `completionGateFire()`（提醒一次：既有通知语句逐字保留）/ `completionGateLogDiag()`（降级诊断行：规则② / ③′）/ `completionGateLogStale()`（`stale` 诊断行：规则③）。
   （D2 只列名与职责一行；判定规则仍单点在 §2.2.12——不重述。）

3. **§2.2.12 规则② 成因枚举按实装补齐**（`:859` 改写 + `:860` 新增 1 行）：枚举尾部补「**无 `val`**（`!tb.val`）」与「**`L` 读不出**（`sessions/` 下无可读会话段 ⇒ `logMtime === null`）」⇒ `unavailable`（降级）。
   补注写明：实现 = `shell-notify.js:136`（`L` 读不出）/ `:153`（无 `val`）；与「降级与可观测性」条的「读失败不抛出」**同向**（读不出即判定源不可用、方向保守）⇒ **非新增判据**（不新增规则、四态语义与全部计数不变）。

4. **§2.2.12 读次数上界补精度**（`:870–871`，原单行 324 字符 ⇒ 压为 2 行）：在「1 次 `projcache/sessions/` 列表」与「1 次记录 `readFile`」之间补「**每记录 1 次 `stat`**（per-record ⇒ 与记录数同阶；实现 = `shell-notify.js:141-147`）」。

5. **§3.3 手段 11 ③ 载具落点按实况更正**（`:1707–1709`）：原记「落 `.thincoder/`（gitignore）面」→ 实落**仓外** `%TEMP%\b09-gate-stub.mjs`（承 B10 §3.3 手段 12 ② 先例；本批「零新增文件」保持）；
   补**可复跑命令** `node "%TEMP%\b09-gate-stub.mjs"` + 夹具根 `%TEMP%\b09-fix\<case>` / `B09_FIX` 可覆盖 + 退出码语义（**0 = 全绿**、非 0 = 有断言失败；规模 = 12 用例 / 80 断言，源 = §5）。

6. **载具时序敏感性登记（本轮新增项）**（同处 `:1710`，新增 **执行约束** 行）：该载具多条断言为**墙钟时间窗**（8–13 s / 30–36 s）⇒ **并发负载下时序断言不可采信**（主 agent 实测：并发 2 个子代理时 4 条 FAIL；空闲单跑同载具全绿）——**复跑须在空闲机器上单跑、与并发任务隔离**（**判据前提，非缺陷**）。

**附带一致性修正（本角色当场修，逐条报告；越权即可回退）**：

- **注 S6 常量枚举补齐**（`:519`）：除 3 个 `GATE_*` 外，实装另有 `SESSION_LOG_NAME`(`session.v3.jsonl.zstd`) / `PROJCACHE_SEGMENTS`(`['storages','session_projcache','sessions']`)（`shell-notify.js:38-39`）——原注只列 3 个 = **计数与枚举不符**（D3）。
- **§2.3 B09 表函数级预估按实测校正**（`:1232–1234`）：`startCompletionWatcher()` 预估 ≈60 → **实测 47**；`completionGate()` 预估 ≈40 → **实测 42**（设计者亲读 `shell-notify.js:238-284` / `:117-158`）。
- **B09 修正轮 1 补正注的旧末值加限定**（`:1237`）：「末行数 227 → **235**」补注 = **该轮落档预估**、**实施后收口轮实测终值 = 308**（权威面 = 本表「末行数」列）——防 stale 读数（承 B12 实施后收口轮「同族一致性面同步」先例）。

**自查（机检，改后实测）**：

- **AC17–AC20 判据句逐字未动**：`:1534` / AC18 / AC19 / AC20 四行与改前逐字相等（`AC1x（B09）` 行计数 = **4**）；**TC-46…TC-55 = 10 行**未增未删未改；DD / C / O 计数、本批条目 I1–I4、需求档 US-12 均未触碰（**本轮未改需求档**）。
- **行宽**：设计档 **0** 行 > 300 字符（不含行尾 CR；实测最宽 **300** = B06 旧行）；**EOL 全档 CRLF**（LF-only **0** 行）。
- **行数**（换行符计数）：设计档 **1847 → 1862**（+15 = 注 S6 +2 / 规则② +1 / 读次数 +1 / 单函数核对 +2 / 体量裁定 +3 / 手段 11 ③ +2 / 变更记录 3 行 + 补正行）；`docs/requirements/SHELL.md` **319**（本轮未改）。
- **旧行数残留 grep**：`≈235` / `≈214` 仅出现在「预估 → 实测」对照位（表内 2 处 / 体量裁定 1 处 / 变更记录 1 处）+ `:1237` 的**已限定**历史预估；**无任何处把 235 / 214 当末值**。

**发现即报告（报而不改，单列）**：

- §2.3 B09 表的 `docs/design/SHELL-UX.md` 行（`:1224`）仍记「末行数 **1165**（落档实测）」——为 B09 落档 as-of 快照，现远滞后（本档已 1862；B10 / B12 轮同样各自保留 as-of 行）⇒ 本轮不改（超六条改动面；as-of 约定由 §2.3「行数口径注」承载），请主 agent 裁定是否另轮统一。
- 工具侧观察（非文档缺陷）：`edit` 批量写入设计档后 EOL 全 CRLF（本轮 0 行 LF-only）；但**先写超宽行再压缩**会留中间态超宽（本轮实测 `:870` = 324 / 单函数核对 = 396 → 已按「断点 = 单个空格」压为 2 / 3 行）⇒ 落笔他档宜先算宽度再写。
- 本档 §2.10 记「`shell-notify.js` 70 → **78** / 227 → **235**」——同为历史记录（append-only），本条即其收口注。

## §3 设计评审（评审子代理）

### 轮次 1（评审子代理）

设计评审（B09 面：批次档 §1+§2 · 设计档 §2.2.12 / 选型 K-1 / C27–C31 / AC17–AC20 / TC-46–TC-55 / 手段 11 · 需求档 US-12 + NFR-5 注记）

| # | 类别 | 级别 | 问题 | 建议 |
|---|------|------|------|------|
| 1 | Document ownership（需求↔设计机制级相抵） | 🔴 | 「快照落后」的处置两份文档写法相反：`docs/requirements/SHELL.md:173`（US-12「降级」句）把「快照落后于会话日志」列为**判定源不可用**四成因之一，后果 = **退回 30 s 阈值 + 一条诊断行**；设计 `docs/design/SHELL-UX.md:838` 规则③ 却单列为 `stale` ⇒ **本轮不提醒**（不改阈值、不发诊断行），且 `SHELL-UX.md:851` 明写「降级 = 规则② 的 `unavailable` 分支」（把 stale 排除在降级外）。批次档 `docs/batches/B09-notify-latency.md:95` 与设计用例 `SHELL-UX.md:1472`（TC-48「均不提醒」）同设计侧口径 ⇒ 需求唯一权威源与设计/批次三方相抵，且无任何 C / DD / O 条目登记该分歧。用户可见后果不同：需求口径下 stale 仍在 30 s 得到提醒且有诊断行；设计口径下可能**整轮无提醒且无任何信号** | 二选一并**显式登记**：①（建议；与 R7b「设计 > 需求」一致）把 `SHELL.md:173` 成因枚举中的「快照落后于会话日志」移出「降级」句、改为独立状态句（stale ⇒ 本轮抑制、不误报）——理由 = 按需求字面执行时，「日志已前进而快照未追上」的静默窗会在 30 s 触发**误报**，与本条自身的 AC18 误报边界相抵；② 或把规则③ 改为降级触发（须同时评估该误报面）。任选其一后需同步 `SHELL-UX.md:1472`（TC-48）与批次档 §2.3 的「判据句，逐字」快照（§2 为 append-only ⇒ 以补正注同步） |
| 2 | Acceptance criteria（判据可达性） | 🟡 | 降级面的机检判据**无可 grep 的目标**：批次档 `B09:124`（AC17）与设计 `SHELL-UX.md:1474`（TC-50）均写 `grep "completion gate unavailable"`，但 `SHELL-UX.md:858` 明定该行「发射面 = `console.log`」且 `SHELL-UX.md:557`（锚点 B09 附注）只登记「新增 console 行 1 条」⇒ **不落 `bigfish.log`**。B07 的同类缺口已由 DD-27（`SHELL-UX.md:1196`）用 `writeDiag` 双写解决、B10 复用同一落盘面（`:1209` DD-40）；本批反向选 console-only 却保留 grep 断言 ⇒ 打包版 console 不可见、判据落空 | ① 让该行经既有 `writeDiag(line)`（console + `bigfish.log`）发射——须核对 `shell-notify.js → shell-backend.js` 的新 require 边是否合 §2.2.6 依赖规则（同层、无环），并在锚点附注同步登记；或 ② 保留 console-only 但把判据句点名捕获面（dev 终端 stdout 重定向 / 桩测 `console.log` 桩断言），使 AC17 / TC-50 可达 |
| 3 | Clarity（判定源枚举谓词） | 🟡 | 规则① 的取档谓词不足以实施且与 NFR-5 自述相抵：`SHELL-UX.md:836`（= 批次档 `B09:93`）写「`<dshHome>/sessions/**` 下 mtime 最大的**会话日志**」，但（a）`**` 是**无界深度**写法，而 `SHELL-UX.md:846` 自述「≈ 2 次目录列表（`sessions/` 两层…）…**不做目录递归遍历**」、`docs/requirements/SHELL.md:265`（NFR-5 B09 注记）亦以「不做目录递归遍历」为判据句；（b）「会话日志」的**文件名谓词**未给（`session.v3.jsonl.zstd` 只在 `SHELL-UX.md:190` 的否决候选里出现）⇒ 取到的 `L` 未必是日志档，mtime 比较失真 | 在 §2.2.12 规则① 内钉死枚举面（深度 = 已知布局几层 + 文件名模式），使 NFR-5 注记的「不做目录递归遍历 / 按会话数计」可机检；并注明「2 次目录列表」所依赖的目录数假设（多工程目录下的实际列表次数） |
| 4 | Feasibility / 已知限制 | 🟡 | 抑制方向的失效面未登记：`SHELL-UX.md:830` 的**承重前提**「任一事件后 ≤ 5000 ms 内必有缓存写入」由事实 #4（`:824`，`writeEveryEvents: 200` / `writeIntervalMs: 5000`）推出，但事实表未说明该参数是「**尾部追写**」还是「**节流（只限频、不补写）**」——若为后者，末批事件可能永不折叠 ⇒ 规则③（`:838`）长期判 `stale` ⇒ 结合 `:844` 的记忆键（`completionGateMemo.lastBusyAt` 不变即不重读）与 `:852` 的「诊断行只在 `unavailable` 发」，结果是**永不提醒且无任何信号**（用户可见功能静默失效）。现有 L-B09-4（`:890`）只覆盖「**误报**」方向；兜底 = 实施期复核点②③（`:859` / `B09:129`）「任一不成立 ⇒ 停下报告」 | 补一条已知限制（如 L-B09-5）点名「前提失效 ⇒ 持续 stale ⇒ 永不提醒 + 无诊断行」，并给最小缓解：或让诊断行/降级封口覆盖持续 stale（与 #1 取法② 一致），或给 stale 设抑制上界（连续 N 个静默窗仍 stale ⇒ 按 `unavailable` 处置）；同时把「尾部追写 vs 节流」写成复核点② 的**明文前置** |
| 5 | 受影响文件标注（函数级体量核对） | 🟡 | `SHELL-UX.md:1133`（B09 表下单函数体量核对行）写「`completionGate()` 预估 ≈40 行、`startCompletionWatcher()` **163 → ≈178 行**」——163 是**本档当前行数**（同表 `:1126`：`shell-notify.js` 当前 163），且 178 与同表末行数 ≈227 不符；B07 表的同函数记录为 **36 → 43 行**（`:1117`）⇒ 该行把「文件行数」混入「函数行数」（结论「均 <<300」不受影响，但函数级读数不可核） | 按实测/预估重填函数级两个数（如 `startCompletionWatcher()` 43 → ≈58）并注明口径（函数体起止行） |
| 6 | Document ownership / 文档状态 | 🟡 | 设计档头部回指区间未随本批同步：`SHELL-UX.md:5` 仍写「关联需求档：`docs/requirements/SHELL.md`（**US-1…US-11、NFR-1…NFR-5**）」，而需求档 `SHELL.md:10` 已写「回指本档 **US-1…US-15、NFR-1…NFR-7**」，本档 §一 回指表亦已含 US-12（`:35-36`）与 US-13…US-15（`:37-40`）⇒ 自相矛盾；本批变更记录（`:1588-1591`）只登记了需求档侧同步 | 由父侧文档层把该行同步为实际覆盖（至少 US-1…US-12；含后批则 US-1…US-15、NFR-1…NFR-7）；R7a 型「报告不改」项，不阻塞 |
| 7 | Clarity（符号命名） | 🔵 | 同一常量两个名字：`SHELL-UX.md:515`（注 S6）记 `GATE_FRESH_TOLERANCE_MS`(1000)，`SHELL-UX.md:838`（规则③）写 `FRESH_TOLERANCE_MS(1000)` | 两处统一为一个符号名（静态判据与实现按同一名字核对） |
| 8 | 回指指针（需求档 NFR-5） | 🔵 | NFR-5 标题的验收指针未随 B09 注记更新：`SHELL.md:259` 仍为「（验收：AC3）」，而设计把该注记判据落在 AC17（`SHELL-UX.md:36` / `:1388`，AC17 行末明确「NFR-5（B09 注记）」） | NFR-5 注记内补「验收：AC17」，或标题指针改为「验收：AC3、AC17」 |
| 9 | Note（测试钩子边界） | 🔵 | `SHELL-UX.md:1473`（TC-49）以 `BIGFISH_IDLE_NOTIFY_MS=0` 作构造手段，但该值使阈值 ≤ 写后阈值 5000 ms ⇒ **故意违反**承重前提（`:830` / `:874`），存在读到未折叠快照的可能（规则③/④ 仍会抑制，但构造结论易混淆） | 在 TC-49 或执行面补一句：钩子置 0 仅用于**驱动四态**，不得用于时延 / 误报面断言 |

本轮已核验（机检，只读）：三档 >300 字符行数 = **0**（判据 = `^[^\r\n]{301,}`，不含行尾 CR）⇒ 批次档 §2.8 / §2.9 的压行与行宽自述成立；三方条目一致（批次档 §2.2 I1–I4 = 设计 AC17–AC20 = 需求 US-12）逐条对上；受影响文件表算术自洽（163+70−6=227 / 204+12−2=214 / 196+21=217 / 972+193=1165）。未核验（越出评审面，标注为 `unverified`）：Harness 源码事实（`writeIntervalMs`=5000、`turn/end` 强制落盘、域 v7 / 行 ver=2）、设计者实测（21.9 s 工具静默、19 帧、单档 7.8 KB）、以及各代码档行数。

计数：🔴 1 · 🟡 5 · 🔵 3（共 9）

VERDICT: changes-required

### 轮次 2（评审子代理）

**轮次 2 复审（只验轮 1 九条；轮次衰减）——逐条实测核验表**

| 轮 1# | 级别 | 落点（本轮 read 现态） | 状态 | 证据（逐字，本轮 read / grep 输出） |
|---|---|---|---|---|
| 1 | 🔴 需求↔设计「快照落后」机制级相抵 | `docs/requirements/SHELL.md` US-12 / 设计 §2.2.12 | **Fixed** | `SHELL.md:173`：`- **判定四态**：判定一次性给出 \`open\` / \`stale\` / \`unavailable\` / \`done\` 之一——\`done\` ⇒ 提醒一次；**\`open\`（回合在途）与 \`stale\`（快照落后于会话日志）⇒ 本轮不提醒**（保守：宁可推迟，不误报；**非降级态**、不改阈值）；` · `SHELL.md:174`：「\`unavailable\`（记录缺失 / 形态不符 / 解析失败）⇒ **降级**：退回 30 s 阈值（= 现状语义）；\`stale\` 与降级**各发一条诊断行**（同形；每次监视器生命周期各至多一条）——「不提醒」不得无信号；」 · `SHELL.md:175`：「持续 \`stale\`（同一 \`stale\` 状态延续 ≥ 30 s）⇒ 按 \`unavailable\` 处置」 · 设计 `SHELL-UX.md:855`（规则③ `⇒ \`stale\`——**快照落后于会话日志 ⇒ 本轮不提醒**…+ 一条 \`stale\` 诊断行`）/ `:856`（规则③′ 抑制上界）/ `:870`（降级 = 规则② 与 ③′）/ DD-49 `:1309` / AC18②③ `:1490` / TC-48 `:1586` 全部同口径；变更记录 `SHELL.md:303-304` 留档原文改写 |
| 2 | 🟡 降级面判据无可 grep 目标 | §2.2.12「降级与可观测性」/ 注 S6 / 锚点 B09 附注 / C40 / AC17 / TC-50 / 手段 11 | **Fixed** | `SHELL-UX.md:878`：`- 发射面 = **\`backend.writeDiag(line)\`**（console + \`bigfish.log\` 双写——承 §2.2.8 / DD-27 / DD-40 的既有**单一诊断落盘面**；\`logStream\` 不可用时只到 console）。` · `:879`：「**修正轮 1 #2**：原设计为 console-only，而打包版 console 不可见 ⇒ AC17 / TC-50 的 \`grep\` 判据无可达目标；改走 \`writeDiag\` 使判据落 \`bigfish.log\`` · TC-50 `:1588`：「**取证面 = \`bigfish.log\`**（\`grep "completion gate unavailable"\`；经 \`writeDiag\` 双写，console 仅辅证 / 桩测面——修正轮 1 #2）」 · 手段 11② `:1651`：「诊断行取证面 = \`bigfish.log\`（\`grep "completion gate unavailable"\` / \`grep "completion gate stale"\`——经 \`backend.writeDiag\` 双写）」 · DD-48 `:1308` / C40 `:1359` / 锚点 B09 附注 `:563-567`（诊断行 1 → 2 条） |
| 3 | 🟡 规则① 取档谓词欠定 + 与「不做目录递归」相抵 | 规则① / 读次数上界 / NFR-5 注记 | **Fixed** | `SHELL-UX.md:852`：`> ① 取**会话日志** \`L\` = \`<dshHome>/sessions/<项目段>/<会话段>/session.v3.jsonl.zstd\` 中 mtime 最大者（**具名谓词**：深度 = 3 段、文件名写死 = \`session.v3.jsonl.zstd\`；实测布局；**无 \`**\` 无界写法、不做目录递归遍历**）` · `:864`：「读次数上界（**具名谓词口径**）= 1 次 \`sessions/\` 列表 + 每项目段 1 次列表 + 每会话段 1 次 \`stat\`（固定文件名）+ …**按会话段数计（含项目段数），不按文件数计、不做目录递归遍历**…目录数假设 = 实测 **1** 个项目段（多工程 ⇒ 列表次数 = 1 + 项目段数…）」 · `SHELL.md:267`（NFR-5 B09 注记）同口径未变 |
| 4 | 🟡 抑制方向失效面未登记 | 事实 #7 / 判定前提 / 规则③′ / L-B09-5 / 复核点 | **Fixed** | `SHELL-UX.md:836` 新增事实 #7：「**写后阈值的语义（修正轮 1 #4 补证）**：write-behind 节流 + **尾部追写**（不丢事件）；写失败 fail-soft」 · `:847`：「**已然成立的残余失效面（修正轮 1 #4）**：写路径本身 fail-soft（事实 #7）——…⇒ 持续 \`stale\`；本批以**规则③′ 的抑制上界**（≥ 30 s ⇒ 按 \`unavailable\` 处置 = 退回 30 s 阈值 + 降级行）兜住「长期静默抑制」，登记为 **L-B09-5**」 · `:882` 复核点② 明文前置 = 尾部追写 · `:915` L-B09-5 全文（含残余 = 上界生效前窗口内提醒被推迟） |
| 5 | 🟡 单函数体量读数错（163 → 178） | §2.3 B09 表下核对行 | **Fixed** | `SHELL-UX.md:1195`：`> 单函数体量核对（**修正轮 1 #5**；口径 = 函数体起止行）：\`shell-notify.js:97-139\` \`startCompletionWatcher()\` 现 **43** 行（实施期实测口径同 B07 表 \` :1117 \`）→ …预估 **≈60** 行；\`completionGate()\` 为新增函数，预估 **≈40** 行；两者均 **<<300**…` · `:1196` 明记原记 163 → 178 为口径错误（已核 B07 表 `:1178` 记同函数 36 → 实施期实测 43 行，一致） |
| 6 | 🟡 设计档头部回指区间滞后 | 设计档头部 | **Fixed** | `SHELL-UX.md:5`：`> 关联需求档：\`docs/requirements/SHELL.md\`（US-1…US-15、NFR-1…NFR-7）`（`:6-10` 关联批次含 B06 / B07 / B09 / B10 / B12）；变更记录 `:1738` ⑥ 登记 |
| 7 | 🔵 同一常量两名 | 注 S6 / 规则③ / 执行面 / AC17 | **Fixed** | `SHELL-UX.md:518`（注 S6）「档内常量 \`GATE_FRESH_TOLERANCE_MS\`(1000) / \`GATE_TURN_BOUNDARY_VER\`(2) /」· `:855`（规则③）「\`mtime(C) + GATE_FRESH_TOLERANCE_MS(1000) < mtime(L)\`」· `:866`「常量 \`GATE_FRESH_TOLERANCE_MS\`(1000) / \`GATE_TURN_BOUNDARY_VER\`(2) / \`GATE_STALE_MAX_MS\`(30000)」· `:1526`（AC17③）「**符号名与 §2.2.6 注 S6 同名**，修正轮 1 #7」 |
| 8 | 🔵 NFR-5 标题验收指针未同步 | 需求档 NFR-5 标题 | **Fixed** | `SHELL.md:261`：`### NFR-5 主进程响应性（空闲检测域；验收：AC3、AC17）`（`:304` 变更记录登记同） |
| 9 | 🔵 TC-49 钩子边界未注 | TC-49 / 手段 11② | **Fixed** | `SHELL-UX.md:1587`（TC-49）：「**注（修正轮 1 #9）**：钩子置 \`0\` 使阈值 ≤ 写后阈值 5000 ms ⇒ **故意违反判定前提**（§2.2.12）——仅用于**驱动四态**，不得用于时延 / 误报面断言」 · `:1650`（手段 11②）同款注 |

**剩余问题（本轮新见，轮 1 表外）**

| # | 级别 | 落点 | 问题 | 建议 |
|---|---|---|---|---|
| N1 | 🟡 | `SHELL-UX.md:846`（判定前提块） | 「前提失效」的兜底规则在四处给了两个答案：`:846`「该前提使…；前提失效（配置被改 / 形态换代）⇒ **规则②** 兜底（方向 = 抑制，安全侧）+ 实施期真机复核（TC-46）；登记为 **L-B09-4**。」 vs `:914`（L-B09-4）「…⇒ 判定可能误报——缓解 = **规则③** 兜底（方向 = 抑制）+ 实施期复核点③ + 诊断行」、`:1293`（DD-33）「兜底仅在前提失效时生效，方向 = 抑制（安全侧）」、批次档 `B09:102`「前提失效 ⇒ **规则③** 兜底（方向 = 抑制）+ 规则④ 静态判据把门」。而规则② = `unavailable ⇒ 降级`（`:854` / `:870`：阈值改 30 s、可发提醒）——方向**不是**抑制；且「配置被改」一路的兜底应为规则③（持续超 `GATE_STALE_MAX_MS` 由 ③′ 转降级），`:914` 亦未补 ③′。该态非运行期分支（前提失效由实施期复核点①–③ 把门）⇒ 实现面无歧义，属文档自洽项 | 把 `:846` 的兜底句拆写为两条（形态换代 ⇒ 规则② 降级；配置被改 ⇒ 规则③ 抑制，持续超上界由 ③′ 转降级），并让 L-B09-4（`:914`）补上 ③′（报告级，不阻塞） |

**本轮已核验（机检，只读）**：三档 >300 字符行数按轮 1 判据复核仍为 0（不含行尾 CR）；三方条目一致（批次档 §2.2 I1–I4 = 设计 AC17–AC20 = 需求 US-12 + NFR-5 注记）逐条对上；受影响文件表算术自洽（163+78−6=235 / 204+12−2=214）；设计 `SHELL-UX.md:1195` 的函数级读数与 B07 表 `:1178` 互证。`unverified`（越出评审面）：Harness 源码事实（`writeIntervalMs`=5000 · `turn/end` 强制落盘 · 域 v7 / 行 `ver`=2 · 事实 #7 尾部追写）、设计者实测（21.9 s / 19 帧 / 7.8 KB）、各代码档现行行数——均按「设计者亲测」采信但不背书。

计数：🔴 0 · 🟡 1 · 🔵 0（轮 1 九条 = 全部 Fixed；轮 1 计数 🔴1 / 🟡5 / 🔵3）

VERDICT: pass

## §4 评审裁决与实施启动（主 agent）

## §5 实施记录（eng-coder）

> 本节由 eng-coder 自写（一段一作者，未经父侧转述）。日期 2026-09-17。

### 5.1 交付摘要（改了什么 / 为什么）

- `main.js:49-52` —— 阈值面：`IDLE_NOTIFY_MS = 8 * 1000`（= 投影缓存写后阈值 5000 ms + 3000 ms 余量；8000 > 5000 = 判定承重前提）+ 新增 `IDLE_NOTIFY_FALLBACK_MS = 30 * 1000`（判定源不可用时的降级阈值 = 现状语义）。
- `main.js:54-62` —— env 钩子 `idleNotifyMs(defaultMs = IDLE_NOTIFY_MS)`（承 `shell-backend.js:66-74` `webUrlWaitMs()` 形：空 / 非有限 / 负数 ⇒ 取默认 8000；`0` 可过——TC-49 需要）。
- `main.js:74` —— 注入面两键：`notifier.init({ …, IDLE_NOTIFY_MS: idleNotifyMs(), IDLE_NOTIFY_FALLBACK_MS })`。
- `shell-notify.js:15` —— 新增 `require('./shell-backend.js')`（取 `writeDiag` 作诊断落盘面；同层 L1 + 无环）。
- `shell-notify.js:34-43` —— 判定面常量与状态：`GATE_FRESH_TOLERANCE_MS`(1000) / `GATE_TURN_BOUNDARY_VER`(2) / `GATE_STALE_MAX_MS`(30000) / `SESSION_LOG_NAME` / `PROJCACHE_SEGMENTS`；`completionGateMemo` / `completionGateProbeRunning` / `completionGateDiagLogged` / `completionGateStaleLogged`（符号名与设计档 §2.2.6 注 S6 同名）。
- `shell-notify.js:108-158` —— `completionGate()`：规则① 具名谓词取 L（`sessions/<项目段>/<会话段>/session.v3.jsonl.zstd`，深度 3 段 + 文件名写死，无 `**`、不做目录递归）+ C（`storages/session_projcache/sessions/*.json` 中 mtime 最大者）⇒ 四态 `done` / `open` / `stale` / `unavailable`；全程 `fs.promises`，全包 try/catch。
- `shell-notify.js:160-217` —— 处置面：`completionGateLogDiag()`（降级行：规则② / ③′，每生命周期至多一条）/ `completionGateLogStale()`（stale 行：规则③，每生命周期至多一条）/ `completionGateFire()`（既有通知语句逐字保留）/ `completionGateDue()`（纯算术：done ⇒ 提醒一次；unavailable 与超上界的 stale ⇒ 退回 30 s；open / stale ⇒ 本轮不提醒）/ `completionGateProbe()`（异步 + 防重叠，结果按发起时 `lastBusyAt` 记忆；监视器已停 ⇒ 不消费在途结果）。
- `shell-notify.js:275-280` —— tick 判定分支：静默 > `IDLE_NOTIFY_MS` 且本轮未通知 ⇒ 「每静默窗至多一次读 + 处置收口」两步（5 s 回调内仍无同步 I/O）。
- `shell-notify.js:239-241` —— 判定面随监视器生命周期重置（诊断行「每生命周期至多一条」的口径）。
- `shell-notify.js:245-248` —— 头注释补判定面口径。
- **未触碰**：`shell-backend.js`（B10 的 `writeDiag` 导出行 `:314` 已在场，按设计「两批共用、先落者建立」**未重复添加**）· `shell-tray.js` / `shell-settings.js` / `shell-pet-geometry.js` / `shell-pet-drag.js` / `package.json` / `docs/**`。

### 5.2 实测行数（回填 §2.5 表；口径 = 换行符计数）

| 文件 | §2.5 预估末行数 | 实施期实测 | 差 |
|---|---|---|---|
| `shell-notify.js` | ≈235 | **307** | +72（判定面 JSDoc / 常量注释为主；行为面零超设计） |
| `main.js` | ≈214 | **218** | +4 |

两档行宽 ≤ 300（实测最宽 131 / 188，超宽 0 行）；均 < 500 硬限。`shell-notify.js` 越 300 软档 —— 设计档 §2.3 该行列的「实施期实测回填」缺口见 §5.5 上报项。

### 5.3 验收证据（AC17–AC20 + TC-46…TC-55）

**载具（两件）**：① **仓外一次性桩测** `%TEMP%\b09-gate-stub.mjs`（承 B10 §3.3 手段 12② 的仓外裁定先例；不入仓 / 不登记 `package.json` / 不进 `build.files`；可复跑 `node "%TEMP%\b09-gate-stub.mjs"`，夹具根 `%TEMP%\b09-fix\<case>`、`B09_FIX` 可覆盖，退出码 0 = 全绿；逐例独立子进程——诊断标记按生命周期至多一条）；② 真机静态 / diff / 日志面。

**结果：12 用例 / 80 断言，`ALL CASES GREEN`**。关键实测（原文）：

- `done`（TC-46 等价夹具驱动）：`末次写入 → 提醒 ∈ (8, 13] s（实测 9.22 s）`、`提醒次数 = 1`、标题/正文/桌宠台词逐字、正向态零诊断行、+6 s 无重复。另两例同族：`回合结束 → 提醒 12.44 s`（规则④ 构造）、`重开新周期 10.24 s`（TC-51）、`多会话闭合后 8.42 s`（TC-53）——四个实测点均在设计窗口 8–13 s 内。
- `open`（规则④ / AC18② / TC-47 构造）：`回合在途 ⇒ 静默期内零提醒（规则④）` + `open 非降级态 ⇒ 无诊断行`。
- `stale`（规则③ + ③′ / TC-48）：`快照落后 ⇒ 静默期内零提醒（规则③）` + `stale 诊断行恰一条` + `持续 stale ≥ 30 s ⇒ 按 unavailable 处置（规则③′）` + `降级面提醒时延 > 30 s（实测 39.25 s）`。
- `unavailable` / `noRecords` / `corrupt` / `verMismatch` / `logUnreadable`（规则② / TC-50）：降级行逐字 `[bigfish] completion gate unavailable; falling back to idle threshold 30000ms`、恰一条且多 tick 不重复；`判定源不可用 ⇒ 8 s 阈值不再触发`；降级面提醒 `实测 34.23 s`（= 现状 30–35 s 语义）。
- `switch`（TC-51 / AC19）：`notifyOnComplete=false ⇒ 零提醒` + `关闭期活动 ⇒ 重开不补发`。
- `fallbackPath`（TC-52）：`回落路径首扫只建基线 ⇒ 不误报` + `回落路径判定面同样生效 ⇒ 提醒一次`。
- `static`（TC-55 / §3.3 手段 11①，33 断言）：四态标识符 / `GATE_*` 三常量 / `ver !== 2` 守卫 / `require('./shell-backend.js')` 边 / `backend.writeDiag` 两条发射点 / `*Sync(` = 0 / `process.platform` = 0 / 判定段无 `recursive` / 取档列表 = 3（按段计）/ `IDLE_NOTIFY_MS(8000) > 5000` / 注入两键 / 文案三串各一处 / 开关守卫行与清忙态行逐字 / 钩子取值 `'abc'`·`'-1'`·`''` ⇒ 8000、`'0'` ⇒ 0、`'8500'` ⇒ 8500。

**实施期硬复核点 ①②③（设计档 §2.2.12「执行面」）——三条全部成立，未触发停手报告**：

| # | 复核点 | 实测证据 |
|---|---|---|
| ① | 回合结束时缓存记录确实落盘且 `openTurnStartSeq === null` | 本机 `~/.dsh/storages/session_projcache/sessions/session-f80ce725….json`（7827 B）实测 `version: 7` · `rows.turnBoundary.ver: 2` · `openTurnStartSeq: null`；mtime `2026-09-17T03:38:34.008Z` |
| ② | 静默期中快照不落后于会话日志（规则③ 不误伤） | 同批实测：会话档 `…/session.v3.jsonl.zstd` mtime `03:38:34.006Z` ≤ 记录 `03:38:34.008Z`（先 flush 后 put——设计事实 #5 同序） |
| ③ | shipped composition 的 `writeIntervalMs` = 5000 | `dsh-bundle/node_modules/@deepseek-ai/dsh-base/cordis.patch.yml:165-166` = `writeEveryEvents: 200` / `writeIntervalMs: 5000` |

**诊断行落盘面（AC17② 取证面）**：两条 gate 行均经 `backend.writeDiag()` 发射（`shell-notify.js:164` / `:171`），`writeDiag` = console + `bigfish.log` 双写（`shell-backend.js:61-64`，`logStream` 指向 `userData/bigfish.log`，`shell-backend.js:215-217`）；本机真实 `%APPDATA%\bigfish\bigfish.log` 已含同函数发射的 `[bigfish] backend web url captured port=… token=yes` 3 行 ⇒ 生产面落盘成立（B09 两条 gate 行需新版应用真机运行后方可见）。

### 5.4 残余风险 / 未覆盖项（如实）

1. **`grep "completion gate unavailable" bigfish.log` 的真机面未在本批实施期执行**：需以新版代码真机启动一次应用（应用启动会走 `cleanupStaleDsh()` 杀同类 dsh 后端进程，实施期不擅自触发）。本批以「同一 `writeDiag` 函数的发射行已在生产日志」+ 桩测 console 面等价取证；真机运行回归留父侧 / 用户（与 AC20 的真机体感同批）。
2. **TC-46 / TC-47 / AC20 的「真机任务 + 秒表」面**：本批以夹具驱动等价取证（真实 5 s 周期 + 真实阈值 8000 / 30000，非钩子置 0 的时延面）；用户判「显著更快」留人工。
3. **桩测驱动方式**：忙态用注入访问器 `setLastBusyAt()`（watch 回调写入的同一状态变量）驱动，不依赖 OS watch 投递；回落路径例以 monkeypatch `fs.watch` 抛错进入。`IDLE_NOTIFY_MS=0` 仅用于驱动「会话日志读不出 / 损坏 / 形态换代」三例的诊断行，**不作时延 / 误报面断言**（TC-49 注）。
4. **一处实现超出设计字面（已评审认可、报而不改）**：`L` 读不出（`shell-notify.js:136`）与 `!tb.val`（`:153`）⇒ `unavailable`；设计规则② 只枚举 C 侧成因。方向保守（降级 = 现状语义，属 US-12「形态不符 / 解析失败」面）。
5. **设计档回填 / 同步项（eng-designer 写域，本角色不改，随交付上报）**：§2.3 B09 表末行数回填（≈235 → 实测 307；`main.js` ≈214 → 218）· §2.2.6 注 S6 函数清单补 5 名辅助函数（`completionGateProbe` / `completionGateDue` / `completionGateFire` / `completionGateLogDiag` / `completionGateLogStale`；均不外导出）· §2.2.12 规则② 成因枚举补「L 读不出」一句 · 读次数上界句补「每记录 1 次 `stat`（per-record ⇒ 与会话段数同阶）」· §3.3 手段 11③ 的桩测落点口径（`.thincoder/` vs 实落 `%TEMP%`）。

### 5.5 审计与代码评审轮次、终态

- **内部偏离审计**（explore 子代理，只读）：D1–D6（全部文档同步项 + 一处证据缺口）；结论 = 行为面与 AC17–AC20 代码侧判据**零偏离**、无越清单改动、无静默简化。修正轮 1：D2（阈值符号与设计字面判据对齐 → `IDLE_NOTIFY_MS = 8 * 1000` 保持字面常量、解析结果在注入点求值）、D6（补 `logUnreadable` 用例）。
- **advisor 代码评审**（sync，本会话）：`VERDICT: pass` —— 1 🟡（L 读不出分支的设计条文缺口，报而不改）+ 6 🔵。修正轮 2 已落：在途判定在监视器停止后不再消费（`shell-notify.js:209-210`，恢复 B07「停后不提醒」不变式）· 桩测时延断言上界 +600 ms 抖动余量 · TC-49 非法值面补行为证据（从 `main.js` 源码取真实函数体求值）· 本节 5.2 / 5.3 的回填与载具登记。余 2 项（设计档写域）见 §5.4 第 5 条。
- **终态：converged（clean）** —— 修正轮 2 后复跑桩测 12 例 / 80 断言全绿；工作树改动仅 `main.js` + `shell-notify.js`（`git diff --name-only` 其余条目属 B10 / B12 / B18 并行批次）。

### 5.6 §5.2 行数口径补正（自检：计数自述与口径对齐）

§5.2 表内两数误用「read 工具行号总数」口径（= 换行符计数 + 1，末行空行计入）。按本批统一口径（`docs/design/SHELL-UX.md` §2.3 行数口径注 = **换行符计数**；`\n` 计数、CRLF 按 1 计）实测终版为：

| 文件 | §5.2 原记（错） | 口径正确值（换行符计数） | §2.5 预估 | 差 |
|---|---|---|---|---|
| `shell-notify.js` | 307 | **308** | ≈235 | +73 |
| `main.js` | 218 | **217** | ≈214 | +3 |

两档 EOL 全 CRLF（LF-only 0 行）、行宽 ≤300（最宽 131 / 188，>300 行 = 0）、均 < 500 硬限 —— 上述形态判据不受本次补正影响。§5.4 第 5 条的设计档回填数字以此表为准。

## §6 验收核销（主 agent）

**验收依据** = 本档 §2.4 AC17–AC20 + 设计档 §3.1 逐条 + **主 agent 独立实测**（一手证据，不采信自报）。
**声明（前置，诚实面）**：本批主判据之一（AC20 时延量化）**在本机不可得可复现证据**——详 6.3；下方逐条按取得的一手证据如实记（**不将不可验证项记为已验**）。

### 6.1 AC 逐条（证据 = 主 agent 亲跑 / 亲读）

| AC | 判据 | 证据（主 agent 一手） | 结论 |
|---|---|---|---|
| **AC17** | 阈值 30 s → 8 s + env 钩子 + 降级回 30 s | **亲读** `main.js:51` `IDLE_NOTIFY_MS = 8 * 1000` / `:52` `IDLE_NOTIFY_FALLBACK_MS = 30 * 1000`；**亲跑**载具 `static` 组 **33 / 33 绿**（含钩子 5 值 `abc`/`-1`/`''` ⇒ 8000 · `0` ⇒ 0 · `8500` ⇒ 8500） | ✅ |
| **AC18** | 判定面（回合闭合合取项 + 四态 + ③′ 上界） | **亲跑**：`open` 组 5/5 绿（回合在途 ⇒ 静默期零提醒 + 回合结束 ⇒ 一次提醒）· `stale` 组 6/6 绿（快照落后 ⇒ 抑制 + stale 诊断行恰一条 + 持续 ≥30 s 按 unavailable 处置）· `noRecords`/`corrupt`/`verMismatch`/`logUnreadable` 各 3/3、2/2 绿；**亲 grep** 四态标识符与 `GATE_*` 三常量齐 ✓ | ✅ |
| **AC19** | 语义不回退（一周期一次 / 开关 / 不补发 / 回落路径 / 文案逐字） | **亲跑**：`switch` 组的开关与不补发两条绿 · `fallbackPath` 组 4/4 绿（首扫只建基线不误报 + 回落路径判定面生效）· 文案三串逐字断言在 `static` 组绿；**亲 grep** `*Sync(` = 0 · `process.platform` = 0 ✓ | ✅（**时延面除外**，见 AC20） |
| **AC20** | **真机时延量化**（末次写入 → 提醒落在设计窗内） | **不可得可复现证据**：载具在并发负载下 `done` 例 **19.23 s**（窗 ≤ 13）· `unavailable` 例 **39.25 s**（窗 ≤ 36）· `switch` 例**未触发**（两次运行**数值一致** ⇒ 非随机抖动）；而 `multiSession` 例 **8.43 s 通过**（与 eng-coder 空闲时报的 8.42 一致）⇒ 判读 = **相位/负载敏感**（失效例各多等一个 5 s 投影写周期），非实现缺陷、亦非随机噪声 | ⚠️ **未验证 ⇒ 归真机人工项** |

### 6.2 交付终态实测（主 agent 一手）

- `shell-notify.js` **308** 行 · `main.js` **217** 行（≤ 500 硬限）；设计档 §2.3 已回填 + 越 300 软档体量裁定段已落。
- 禁触面：`shell-backend.js` / `shell-tray.js` / `shell-settings.js` / `shell-pet-geometry.js` / `shell-pet-drag.js` / `package.json` 零改动（单边面）。
- 本批写域 = `main.js` + `shell-notify.js`（2 档）。

### 6.3 残余与处置（不藏）

1. **AC20 真机时延量化未完成**（= **发布门/真机项**）：需新版应用真机启动一次，秒表测「末次写入 → 提醒」并目视判「显著更快」；同批附带 `grep "completion gate unavailable" bigfish.log` 的真机面校验。**这是本批唯一未闭环的验收面**（设计早已列为人工项）。
2. **载具时序断言的可采信边界**：8–13 s / 30–36 s 窗在并发负载或不利相位下**整体后移一个 5 s 周期**（主 agent 两次隔离尝试均复现同值）⇒ 设计档已登记执行约束（复跑须空闲单跑）；**但相位敏感性超出该约束能覆盖的范围** ⇒ 本批**不以该载具的时延断言作 AC20 的证据**（其余不变量仍有效）。
3. **规则② 成因枚举已补**（`L` 读不出 / 无 `val` ⇒ `unavailable`，方向保守、与改前同源）——设计档已同步。
4. **载具退役裁定（主 agent）**：`%TEMP%\b09-gate-stub.mjs` = **开发期工具 ⇒ 退役**（不转集成资产）——理由同 B10：本批验收已闭环（除真机项）、无 CI 落点、保留 = 维护存量测试库存；**其不变量断言的价值已由本节证据承载**。

### 6.4 台账

- **R10**（任务完成提醒迟滞）→ 状态改为 **待核销**（**不归档**：AC20 真机量化未跑）；归档条件 = 用户真机秒表验收通过。
