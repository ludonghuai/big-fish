# B21 批次档 —— 动作选择规则：交互 / 个人动作分离 + 丝滑衔接 + 逃跑槽修复

> 六段制 · append-only · 一段一作者：§1 主 agent / §2 eng-designer / §3 设计评审 / §4 主 agent / §5 eng-coder / §6 主 agent。
> 本档 = **B18（动画链，A 面）的真机反馈修复批**：B18 收口后用户实机目视发现两处不通过（见 `docs/batches/B18-pet-animation-chain.md` §6.6）+ 提出一个新分类需求。
> 依据：B18 的设计凭证已消费（锚#5）⇒ 本批必须**走完整四步 + 重新评审**。

## §1 立案（主 agent）

### 1.1 目标（一句话）

让桌宠的动作**该来的来、该走的走**：她自己演的动作（**个人动作**）与你触发的动作（**交互动作**）**分工明确、互不打断错乱**，动作之间**衔接丝滑**（不再"一顿一顿"），并且**拖到屏幕边不再乱切成跑步**。

### 1.2 任务来源（用户原话，2026-09-18）

> 「动作直接会有切换的间隔，是因为没有一个动作开始与结束的状态让它丝滑链接；是的，是有交互的；动作非常丰富；但是**拖到边界的时候触发逃跑动作不太对，动作没正确切换**，我们要考虑一下鼠标点击的时候是否需要切换到交互动作，**区分交互动作与个人动作**，交互动作就比如**拖到边框逃跑，喂养**等等，个人动作是**它自己演示**的」

### 1.3 现状实测（勘察，2026-09-18）

| 面 | 实测事实 | 证据 |
|---|---|---|
| **问题①：切换有"间隔感"** | 19 次切换的 `shown` 间隔全部 ≈ 段长（min 254 / max 10052 ms，**超段长 0 次**）⇒ 技术上**无空白帧**（`ended→shown` 仅 16–29 ms）⇒ 观感成因 = **硬切 + 剪辑自带起收势 + 180 ms 淡入过短**（**无段间重叠**） | 主 agent 亲读 `%APPDATA%\bigfish\pet-anim.log`（B18 §6.6 已落档）；双缓冲机制见 `pet-chain.js`（两 `<video>` + `.is-front` 交叉淡入） |
| **问题②：`moves.run` 槽高频连切** | 切换 #15–#18 间隔 = **2952 / 635 / 1060 / 816 ms**；日志两行 `anim slot key=moves.run pick=原地左转奔跑 exclude=原地左转奔跑` ⇒ **该槽仅 1 段**，`exclude` 排除自己后仍只能选它 ⇒ 反复同段 + 高频打断 | 同上日志；池结构 = `assets/pet-anim/pool.json` 的 `moves.run`（1 段）· `moves.walk`（3 段） |
| **池与分类现状** | `weights {idle:10, turn:5, move:0}`；动作组 = `idle`(3) / `turn`(1) / `moves.walk`(3) / `moves.run`(1)；分类 = 小动作(18) / 玩耍(27) / 吃什么(12) / 时节(21) / 文字(2)；事件档 = happy / eat / sleep / read / starry / scared | `assets/pet-anim/pool.json`（71 行） |
| **交互触发面现状** | 点击 → `events.happy`（避开连播）· 喂食 → `events.eat` · 走路 → `moves.walk` · 跑步 → `moves.run` · 睡眠 → `events.sleep`（`loop=1`）· 拖拽 → **无专门档**（用户预期有「逃跑」类） | B18 §2.2.3 / §5.6（`slot-*` 日志）；样本侧有「被鼠标拖拽悬空反馈」段（池内有此素材名） |
| **素材面** | `assets/pet-anim/webm/` 106 段（**只读**）；池引用 91 段 / 未引用 15 段（工作状态 5 → B19；余额 6 / 碎碎念 3 → D2；拖拽 1 → **本批候选**） | B18 §5.4 |
| **既有机制（不得推翻）** | 双缓冲无空白帧 · 池校验 V1–V6 · 三级回落 · `prefers-reduced-motion` 四项 · 事件档避重（`nextInSlot` 契约保留） | `docs/design/PET-ANIMATION.md`（B18 段） |

### 1.4 硬约束（不得偏离）

1. **几何层零 diff**：`shell-pet-geometry.js` / `shell-pet-drag.js` 不得出现在 `git diff`。
2. **素材本体只读**：`assets/pet-anim/webm/**` 不得增删改（本批只改**池数据**与**选择规则**）。
3. **零空白帧不得回退**：B18 的 AC4 机检面（切换全 `readyState=4`、无禁用形态）必须保持全绿——**"衔接丝滑"不得以牺牲"无空白帧"换取**。
4. **PNG 通道零回退** + **零新依赖** + 新增渲染档须登记 `build.files`。
5. **默认行为保守**：衔接参数与交互档的引入若改变既有观感 ⇒ 默认值须显式裁定（1.5 U 项）。
6. **行宽 ≤300（不含行尾 CR）· 代码档 ≤500 行 · 文件头标准形**。
7. **许可面沿用 B18**（素材禁商用 + 三处署名在位，不得改写）。

### 1.5 待决项（须裁定后才能定稿设计）

| # | 待决 | 备选 | 影响面 |
|---|---|---|---|
| U-1 | **点击是否切交互动作** | ① **切**（点即有反应，推荐）② 不切（个人动作不被打断） | 交互/个人分类的核心语义 |
| U-2 | **打断规则** | ① 交互动作**可打断**个人动作 ② 个人动作演完再切（延迟响应） | 手感 vs 连贯 |
| U-3 | **拖到边框的"逃跑"档** | ① 新立 `drag` 类交互档（引用池内未引用的「被鼠标拖拽悬空反馈」）② 复用一个既有档 | 素材利用 + 语义准确 |
| U-4 | **衔接方式** | ① **段间重叠交叉淡入**（提前 N ms 起下一段，双缓冲已具备）② 加长淡入（180 ms → 更大）③ 段尾对齐（挑"起势/收势"匹配的段） | 观感（需实机调参） |
| U-5 | **单段槽的处置**（问题②的根治） | ① `exclude` 失效时**改用基槽**（如 run → walk）② 扩该槽段数（选段 = 数据面，但素材受限）③ 该槽**加冷却**（最小驻留时长） | 数值与规则 |
| U-6 | **个人动作的最小驻留** | ① 引入**最小播放时长**（如 ≥3 s 才允许被打断）② 不加 | 与 U-2 联动 |

### 1.6 关联台账与需求档

- **台账**：`docs/TODO.md` §一 **R15**（外部项目能力移植）——本批 = A 面（B18）的**修复批**，收口后在其触发列登记；**B18 §6.6** 的两处"不通过"由本批修复后撤销。
- **需求档落点**：`docs/requirements/PET.md`（§三 US / §四 NFR）——由 eng-designer 起草（续编 US-29…；**交互/个人分类**为本批新增需求条目）。
- **设计档落点**：`docs/design/PET-ANIMATION.md`（链与动作选择主题档，B18 主题）——**由设计者按「一主题一档」判定并写明理由**（预期为续写该档；若判定需新档须给四候选否决理由）。

### 1.7 变更记录

| 日期 | 变更点 |
|---|---|
| 2026-09-18 | 建档（B21 立案）：目标 / 任务来源（用户原话）/ 勘察实测（两处不通过 + 池与触发面现状）/ 硬约束七条 / 待决 U-1…U-6。依据 = B18 §6.6 的真机目视结论 + 用户 2026-09-18 原话。 |

---

## §2 本批任务书（eng-designer）

<!-- 由 eng-designer 填 -->

---

### 2.1 本批覆盖的需求条目（三方同源之一）

**条目 = 4**（US-29…US-31 = 3 条 · NFR-23 = 1 条）；需求档 = `docs/requirements/PET.md`（本批已落档：§三 / §四）；设计档 = `docs/design/PET-ANIMATION.md` 的 **B21 面**（**预定节号** = §2.13 / §3.7 / §3.8）。
另含 **US-7 修订注记**（需求档已落 · 类别 = 语义变更 · 属**修订**非新条目 ⇒ 不计入条目数）。

| # | 需求条目（`docs/requirements/PET.md`） | 设计档节（预定号） | 验收 |
|---|---|---|---|
| 1 | US-29 交互动作与个人动作分离 | §2.13.3 分类契约 · §2.13.6 交互档 | AC25 · AC26 |
| 2 | US-30 动作衔接丝滑 | §2.13.4 衔接机制 | AC27 · AC28 |
| 3 | US-31 拖到屏幕边界的逃跑动作 | §2.13.5 选段修正 · §2.13.6 逃跑档 | AC29 |
| 4 | NFR-23 衔接的可标定与零回退 | §2.13.4 常量表 · §2.13.8 | AC27 · AC30 |

- **计数**：条目 **4** · AC **6**（AC25–AC30）· TC **11**（TC-37–TC-47）· DD **9**（DD-28–DD-36）· C **12**（C45–C56）· O **5**（O22–O26，末位编号，承既有编号规则、不重排既有号）。
- **声明（硬）**：**本表 = 设计档 §3.7 的 AC 回指 = 需求档条目**——三处逐条对应、同增同减；advisor 八维 #1（需求覆盖）与 #6（范围）以这份清单判定。
- ⚠ **落盘状态（如实披露）**：设计档的 B21 面**尚未落盘**（该档正被 **B26 的设计评审冻结** · D5「评审在途不改被审文档」）⇒ 本表节号与 AC / TC / DD / C / O 号均为**预定号**；放行后由本角色**一次性写入并回读核对**，届时以落盘原文为准。

### 2.2 实施面（改什么 · 每个面一句话 + 证据）

| 面 | 落点 | 关键点（判据见设计档 B21 面） |
|---|---|---|
| **衔接**（US-30） | `pet-chain.js`（段末决策）+ `pet-chain-core.js`（常量 + 纯判定） | 段末决策**抽为一个决策函数、保留两个触发点**（`ended` / **段末前 N ms**）；旧段退场仍**晚于**新段就绪（AC4 三禁止形态不变）；旧段 `pause()` 由「就绪回调内」**推迟到淡出窗末**（不早于 `readyState ≥ 2` ⇒ 判据②仍真） |
| **分类**（US-29） | 设计档 §2.13.3 分类表（**文档面**，**不加池键**——本档既有原则「没有消费者的参数不入 schema」，避免死配置） | 交互档 = `happy` / `eat` / `drag` / `escape`；个人动作 = 自由链（待机 / 转向 / 分类）；**状态档**（`sleep` / `work-*` / `walk-*`·`run-*`）**不在二元内**；运行时**不引入来源通道**（既有链规则 1 / 3 已承载优先级与打断） |
| **拖动档**（US-29） | `pet.js`（`beginDrag` / `clearDragState` 对称上报）→ `pet-chain.js`（`setDragging` + 让位守卫） | **不碰** `shell-pet-drag.js` / `shell-pet-geometry.js`（拖动档走**渲染层手势面**；拖动期主进程档位让位，防「起拖瞬间被 `setPetState('idle')` 顶掉」） |
| **逃跑档**（US-31） | `shell-pet.js` 的 `doWander`（消费既有 `petForceRun` 信号 → 档名 `escape-left` / `escape-right`）+ `pet-chain.js`（映射 + `loop=true`） | ❗ 逃脱**触发阈值与方向语义零改动**（US-7 触发面不回退）；`doWander` 的**折返序列与 idle 守卫不动**（属 B03 校准面） |
| **选段修正**（U-5 根治） | `pet-chain.js`（`switchTo` 入口的**唯一前置判定**） | R1 **同段不重播**（只对 `loop=true` 持续档：`moves.*` / `sleep` / `drag` / `escape`）；R2 **仅镜像变化 ⇒ 只改 `transform`，不重载**（一次性反馈档保持「每次触发都重播」= 反馈语义） |
| **池数据** | `assets/pet-anim/pool.json` | 新增 `events.drag`（引用池内**未引用**段「被鼠标拖拽悬空反馈」）/ `events.escape`（引用既有跑步段）；**素材本体零增删改**；`POOL_KEYS` 与 V1–V6 谓词**零改动**（走 `events` 的既有自由键扩展面 ⇒ 新键自动被 V5 覆盖） |

### 2.3 受影响文件（实现写域；file 级权威表）

行数口径 = **换行符计数**（本角色本轮亲测，as-of 2026-09-18）。

| # | 文件 | 当前行数 | 预计增量 | 预算末值 | 备注 |
|---|---|---|---|---|---|
| 1 | `pet-chain.js` | 240 | +25 ~ +45 | ≤ 285 | 全批最重：衔接触发 / 两新档映射 / R1·R2 / 拖动让位 / 日志字段 |
| 2 | `pet-chain-core.js` | 198 | +15 ~ +30 | ≤ 228 | 新常量 + 纯判定函数（**可被桩测装载真实实现**） |
| 3 | `pet.js` | 236 | +3 ~ +8 | ≤ 244 | 拖动起止对称上报；`FRAMES` / 帧表 / 200px·140px 口径**零改动** |
| 4 | `shell-pet.js` | 454 | +2 ~ +6 | ≤ 460 | `doWander` 内捕获 `petForceRun` → 逃跑档名（其余逐字不动） |
| 5 | `assets/pet-anim/pool.json` | 74 | +2 ~ +8 | ≤ 82（数据档） | 两个 `events` 新键；引用段 94 → **95**（未引用 12 → 11） |
| 6 | `probe-pet-media.js`（开发期 · 不入包 · 仓内资产） | 345 | +40 ~ +90 | ≤ 435（硬限 500） | 叠化窗两路播放 / `overlap=` 读数 / 拖动档 / 逃跑档取证 |
| 7 | `.thincoder/b21-pet-selection-stub.mjs`（开发期 · 不入包） | **新建** | 150 ~ 250 | — | 纯判定函数桩测（跑真实实现，不写镜像断言） |

**零改动面（机检 = 零 diff）**：`shell-pet-geometry.js` · `shell-pet-drag.js` · `pet-preload.js` · `pet.html` · `package.json`（依赖段与 `build.files` **均零 diff**）· `assets/pet-anim/webm/**` · `assets/pet-new/**`；
（续）`shell-pet-physics.js` · `pet-physics-core.js` · `shell-pet-work.js` · `pet-work-core.js` · `THIRD-PARTY-NOTICES.md` · `README.md` · `版本说明.txt`。
（`pet.html` 零 diff 的理由：叠化**复用**既有 `.pet-media { transition: opacity .18s }` 与 `.is-front` ⇒ 无 CSS 变更；`package.json` 零 diff 的理由：本批**零新源档**。）
台账 / 地图 / 变更记录 / checklist = **父侧写域**，本批实施者不碰（承写权矩阵）。
规范：行宽 ≤300（不含行尾 CR）· 单档 ≤500 行（最大预算末值 = `shell-pet.js` 460 ✓）· 文件头标准形（`docs/CONVENTIONS.md` §一）。

### 2.4 验收标准（AC → 取证）

判据**全文** = 设计档 §3.7（放行后落盘）；本表只给回指与取证命令 / 手段（D2）。

| AC | 回指需求 | 取证命令 / 手段 |
|---|---|---|
| AC25 | US-29 | 分类表在场（设计档 §2.13.3）+ 池数据核对（`events.drag` / `events.escape` 在场且 `parsePool` `ok=1`，V1–V6 全过）+ 日志：拖动期 `anim slot key=events.drag` · 逃生 `anim switch … reason=slot-escape` |
| AC26 | US-29 | 交互档在途 ⇒ 该段内**无** `anim chain` 行；点击从个人动作切入 ⇒ `anim switch reason=slot-happy` 且 `shown − t0 ≤ 300 ms`（**无容差**，与 NFR-9 同口径） |
| AC27 | US-30 · NFR-23 | 换段行带 `overlap=<ms>`；链自主段→段 `overlap ∈ [N, N+250]`；段末前触发占比 ≥ **0.9**；探针在叠化窗内两路 `paused=false`、窗外恒 1 路 |
| AC28 | US-30 | **AC4 三条禁止形态零命中 + 换段行 `readyState ≥ 2`**（零空白帧不回退）+ `ended → shown ≤ 300 ms` |
| AC29 | US-31 | 逃生路径档名 = `escape-*`；一次逃跑内 `same=1` 在场且**零重播**；折返 `anim hold reason=mirror`（不重载）；`reason=slot-idle` 不产生可见换段 |
| AC30 | NFR-23 | 零改动面逐档 `git diff --stat` 空 · `dependencies` / `build.files` 零 diff · 素材零 diff · 常量单点（一处定义一处消费）· 行宽 / 行数机检 |

- 验收依据 = 上表机检证据 + **人工项**（衔接观感 / 拖动档观感 / 逃跑观感，如实标注为待用户实机目视）；本批**不建仓级门禁**（现状 = `AGENTS.md` §三 0/3）。

### 2.5 明确出批（本批不做）

越界即打回（实施者遇到下列面向 ⇒ **停下上报主 agent**，不自行扩面）。

| # | 出批项 | 归属 | 依据（指针） |
|---|---|---|---|
| 1 | 调整散步 / 位移节奏或折返序列以匹配素材段长 | 另批（散步状态机面） | 需求档 §二 不做项 · 设计档 §2.13 观察项 O22 |
| 2 | 改自主小动作的触发源 / 节拍 | **B26 面**（F1） | 需求档 §二 不做项 · US-29 边界 |
| 3 | 素材生成 / 转码 / 补跑步类段（`moves.run` 单段的**素材面**消解） | C 面（B24） | 设计档 §2.13 观察项 O25 |
| 4 | 物理手感与位移写权（B20 面） | B20（已收口） | 需求档 §二 · 设计档 §2.13 冲突核对 C50 |
| 5 | 工作状态六档 / 事件桥（B22）与工作档气泡（B26 F3） | B22 / B26 | 需求档 §二 · B19 面 |
| 6 | 几何层任何改动（两档逐字不动） | 冻结 | 需求档 NFR-23 · 批次档 §1.4-1 |

### 2.6 实施前置与排期

1. **本批尚未批准实施** ✗——顺序 = ① 放行后 B21 面落盘 → ② **用户发起**设计评审（发起权在用户；本角色不自发起）→ ③ 评审修正轮落地并核验 → ④ **用户明确批准** → ⑤ 签发设计凭证后派 eng-coder；
2. **待实机项（初值 + 标定路径 + 到期条件；承 B20 `restitution` 先例）**：

   | 项 | 初值 | 标定路径 | 到期条件 |
   |---|---|---|---|
   | 衔接提前量 `PET_OVERLAP_MS` | **1200 ms**（依据 = 立案档 §1.6 的关联地图行「段间重叠 ~1–2 s」+ 素材段长实测 10.04 s） | **单一常量** + 探针 `overlap=` 读数 + 用户实机目视 | 本批 §6 验收时以实机目视定值；**未定值前 1200 ms = 临时值** |
   | `events.drag` 悬空反馈段 | 引用「被鼠标拖拽悬空反馈」（池内未引用 · 1 段） | 用户实机目视（拖动中观感） | 同 §6 |
   | `events.escape` 逃跑段 | 引用既有跑步段（**素材面无专门逃跑段**，故档位面独立、段同源） | 用户实机目视（拖到边界松手） | 同 §6 |
3. **写域互斥面**：本批与在飞的 **B26**（`PET-ANIMATION.md` 冻结窗 · `PET.md` 的 US-21 / US-24 口径）**不重叠**（本批 = 新节号 / 新 AC·TC·O 号）；与 **B16**（`scripts/gates/**`）无文件冲突；与本仓既有 `.js` 面无第三方在飞写者（`git status` 实测：`docs/batches/B26-pet-feel.md` 为唯一在改档）。
4. **池数据前置**：两新键加定后必须 `parsePool` `ok=1`；新键为**单段** ⇒ 链按 §2.2.3「单候选 ⇒ `loop=true`」分支处理（`drag` / `escape` 均**持续档**，与 R1 同源）。

### 2.7 三方条目一致声明

- **计数**：条目 **4** · AC **6** · TC **11** · DD **9** · C **12** · O **5**；本轮实测复核 = 需求档 §三 B21 段 **3** 条 + §四 **1** 条（设计档计数待落盘后同轮核对）。
- **声明（硬）**：**批次档 §2.1 表 = 设计档 §3.7 的 AC 回指 = 需求档条目**——三处逐条对应、同增同减。
- **范围判定**：§2.5 的 **6** 项出批 = 需求档 §二 不做项 + 设计档 §2.13 出批 / 冲突核对条目逐条对应。
- 引用形态 = `文档:节`（D4，行号只作 as-of 参考）；设计内容一律**只引用不重述**（D2）。

### 2.8 B21 面落盘声明与编号核对（续节；eng-designer · 2026-09-18 落笔轮）

- **落盘完成**：B21 面已一次性写入 `docs/design/PET-ANIMATION.md`——§2.13（2.13.1–2.13.11：落点判定与四组选型对比 / 常量表 / 分类契约 / 衔接机制 / 选段修正 R1·R2 / 两个交互档 drag·escape / 池数据面 / 零改动面与降级面 + 冲突核对 C45–C56 / 观察项 O22–O26 / U-1…U-6 决断对照表 / DD-28…DD-36）
  · §3.7（AC25–AC30 + 注 E / 注 F + 人工项分列）· §3.8（TC-37–TC-47）· §四变更记录两行；同步：档头标题与回指 / 关联批次行 · §1.1 回指表四行 · §2.11 O27 核销行。
  D6 回读核对通过（逐节回读，见交付报告）。
- **编号回读核对**：实际号与 §2.1 预定号**逐条一致**——AC25–AC30 · TC-37–TC-47 · DD-28–DD-36 · O22–O26 · C45–C56；与既有号（AC1–24+31 / TC-1–36+48–49 / DD-1–27+37 / O1–21+27 / C1–44）零冲突 ⇒ 无需订正 §2.1。
- **§2.4 判据一处订正（判据方向，非语义）**：AC27 的「链自主段→段 `overlap ∈ [N, N+250]`」按定时器物理订正为 **`[N − 250, N]`**（JS 定时器只晚不早 ⇒ 实测 overlap = N − 回调延迟 ≤ N；+250 容差的方向与物理不符）——设计档 §3.7 注 E 以 `[N − 250, N]` 为准。
- **前向指针处置**：`docs/requirements/PET.md` 的 B21 条目（US-29…US-31 / NFR-23）与 US-7 修订注记经本轮逐条核对**不含「待落盘」类注记** ⇒ 需求档本轮零改动；设计档 O27 的「归 B21 面落笔时处理」已加核销行关闭。
- **三方一致**：条目 4（US-29…US-31 + NFR-23）= §3.7 的 AC 回指（AC25/26→US-29 · AC27/28→US-30 · AC29→US-31 · AC27/30→NFR-23）= 需求档条目——三处逐条对应 ✓。
- **门禁**：`npm run lint` 亲跑通过（`GATE lint PASS checks=6 selftest=14/14`）；落盘面自检发现 3 处超宽行（>300 字符）已当场修正后复跑通过（详见交付报告）。

### 2.9 B21 设计评审修正轮 1 落笔记录（续节；eng-designer · 2026-09-18 修正轮）

- **范围**：仅落评审轮 1 的 9 条裁决（F-1…F-9），零新语义 / 零新范围；落点 = 设计档 / 需求档 / 本档 §2（本续节）。
- **F-1（🔴 #1；Fixed）**：交互段退场口径 =「不被中途顶掉、含段末衔接退场」——落点：设计档 §2.13.3 口径句（`PET-ANIMATION.md:962`）· §2.13.4 事件段不豁免句（`:972`）· **AC26 ① 重写**（`:1269` + 判据细目 = **注 G**（`:1291`）：违规面 = 非本档 `slot-*` 零命中、合法面 = 链自主换段集合的段末决策面、可区分改前 / 改后树）· **TC-39**（`:1312` 同口径订正）· 需求档 US-29 口径行（`PET.md:300`）+ 变更记录（`:554`）。
  **读法判定**：该读法 = 澄清（起演仍恒立即、目标句与边界句未改）⇒ 未改变语义，未打回。
- **F-2（🔴 #2；Fixed）**：B18 面规范同步——§2.2.4 规则 3（`:294` 第三触发 (c) + `:295` 拖动窗旁路注）· §2.2.5 步骤 4（`:312`：`pause()` 推迟到淡出窗末）· §2.2.5 序列头注（`:307`）· C45（`:1058`）/ C47（`:1060`）表述同步。**同步清单增补**：§2.8 落盘同步清单（档头 / §1.1 / O27）自本修正轮起**增补 §2.2.4 / §2.2.5 / C45 / C47**（本续节即登记）。
- **F-3（🟡 #3；Fixed）**：淡出窗末 pause 回调失效守卫（`PET-ANIMATION.md:1000`，§2.13.4 竞态清单）。
- **F-4（🟡 #4；Fixed）**：「交互档」术语桥接——§2.13.3（`:955`）+ C51（`:1064`）；两套机制均不变。
- **F-5（🟡 #5；Fixed）**：受影响文件表体例注（`PET-ANIMATION.md:894`，§2.13 头部——批次档 §2.3 为 file 级权威表 = 承 B26 形态的既有裁定）。
- **F-6（🔵 #6；Fixed）**：注 E 双向容差（`:1278`）+「或 0」归属（`:1280`）+ AC27 ④ 采样口径（`:1270` 行 + `:1282` 探针句——窗后稳态判据 + ended 兜底行豁免）。
- **F-7（🔵 #7；Not an issue——基准说明行 = 本行）**：§2.3 行数基准 = 本角色 2026-09-18 亲测，**与现树一致**——`pet.js` 236 · `pet-chain.js` 240 · `pet-chain-core.js` 198 · `probe-pet-media.js` 345 · `pool.json` 74 均一致；
  `shell-pet.js` §2.3 记 454、现树 455 = **+1 归 B26 实施**（B26 §5.0.6 已登记）⇒ 基准无漂移。设计档 §1.3.5 的 211 / 426 = **2026-09-17（B19 期）历史值**，不作本批基准。
- **F-8（🔵 #8；Fixed）**：§2.2.3 计数口径注（`:268`）+ 单候选分支重启注（`:271`）+ §1.3.1 前向注（`:59`）；C47 括注改为「新增档位在批次面独立成表」（`:1060`）。
- **F-9（🔵 #9；Fixed）**：`PET_OVERLAP_MS` 依据指针订正 = **`docs/README.md` 文档地图 B21 行**（设计档 `:941`；该句实际位于 `docs/README.md:52`）。**本档 §2.6 第 2.6 项同款指针**（`:151`）为 append-only 段的既有行——**以本注记订正，不改既有行**（依据 = 文档地图（`docs/README.md`）B21 行）。
- **§2.4 回指表口径注记**：AC26 行「该段内无 `anim chain` 行」与 AC27 行「[N, N+250] / 窗外恒 1 路」字面随设计档 §3.7 订正**作废**——判据全文以设计档 §3.7（注 E / 注 G）为准（§2.4 只给回指与取证面，D2）。
- **三方一致核对**：条目 4 · AC 6（AC25–AC30）· TC 11（TC-37–TC-47）· DD 9 · C 12 · O 5——本轮**编号零变动**、只改内容与判据文 ✓。
- **门禁**：`npm run lint` 亲跑通过（最后一次写入之后；`GATE lint PASS checks=6 selftest=14/14`）；设计档（1345 行）/ 需求档（555 行）零超宽行（>300）实测 ✓。

### 2.10 B21 设计修正轮 2 落笔记录（续节；eng-designer · 2026-09-18 修正轮）

- **范围**：仅落复核轮（§3 轮次 2）的 2 条新发现（F-10 / F-11），零新语义 / 零新范围；落点 = 设计档 / 本档 §2（本续节）。需求档与代码面零改动。
- **F-10（🟡 #10；Fixed）**：注 G 违规面 / 合法面在「交互→交互切换」上的自相矛盾收口——落点：设计档 §3.7 注 G 违规面（`PET-ANIMATION.md:1294`：驱动面收窄为「idle / 个人档」+ 显式排除交互→交互合法切换）· 合法面（`:1295`：「只允许」集并入交互→交互合法切换——换段行同属交互档 ⇒ 区间延续、不构成违规）；区间口径（`:1293`）与「可区分改前 / 改后树」（`:1296`）两条不变。
  **三条合读自洽核对**（区间口径 / 违规面 / 合法面）：区间内换段合法 ⇔ 链自主换段集合 ∨ 交互→交互用户触发切换；违规面 = idle / 个人档驱动零命中（用户触发已移出驱动清单）；交互→交互在两面均显式合法 ⇒ 与 `PET.md:311`「用户触发的动作引起的切换仍然立即发生」无相抵 ✓。
- **F-11（🟡 #11；Fixed）**：TC-44 / TC-46 随 AC27②/④ 与注 E 同步——落点：设计档 §3.8 TC-44（`PET-ANIMATION.md:1317`：带 = `[N − 250, N + 250]`）· TC-46（`:1319`：「窗外」→「窗后稳态（ended 兜底行豁免）」）——与 AC27 ②④（`:1270`）/ 注 E（`:1278` / `:1282`）逐字一致。
- **三方一致核对**：条目 4 · AC 6（AC25–AC30）· TC 11（TC-37–TC-47）· DD 9 · C 12 · O 5——本轮**编号零变动**、只改判据措辞与字面 ✓。
- **门禁**：`npm run lint` 亲跑通过（最后一次写入之后；`GATE lint PASS checks=6 selftest=14/14`）；改动行零超宽（≤300 字符）实测 ✓。

### 2.11 B21 设计修正轮 3 落笔记录（续节；eng-designer · 2026-09-18 修正轮）

- **范围**：仅落修正轮 3 的 1 项（TC-39 字面与注 G 收窄对齐），零新语义 / 零新范围；落点 = 设计档 / 本档 §2（本续节）。需求档与代码面零改动。
- **修正内容（Fixed）**：TC-39 预期输出「（非本档 `slot-*` 零命中）」→「由 idle / 个人档驱动的换段行零命中——交互→交互的合法切换**不在违规面**、**区间延续**、不构成违规；细目 = 注 G」——与注 G（`PET-ANIMATION.md:1294` / `:1295`）同源：违规面 = idle / 个人档驱动；交互→交互合法切换（`slot-escape` 入逃生档、交互中再次点击 / 喂食等用户触发切换）不在违规面、区间延续、不构成违规。
  TC-39 其余字面（`slot-happy` ≤300 ms · `pre-end` / `anim chain` 不违规 · 1.6 s 回 idle）不变。
- **修正依据**：修正轮 2 自报观察项 1（TC-39 字面漂移）经主 agent 判定现在扫掉——TC-39 = B21 验收轮要跑的用例，其过宽字面会在「happy 段窗口内交互→交互切换」（验收时再次点击 / 喂食）时造成假红；观察项 2（§2.4 回指表旧字面）已由 §2.9 声明作废 ⇒ 不动。
- **三方一致核对**：条目 4 · AC 6（AC25–AC30）· TC 11（TC-37–TC-47）· DD 9 · C 12 · O 5——本轮**编号零变动**、只改 TC-39 一处字面 ✓。
- **门禁**：`npm run lint` 亲跑通过（`GATE lint PASS checks=6 selftest=14/14`）；改动行零超宽（TC-39 行 295 · 变更记录行 283，≤300）实测 ✓。

## §3 设计评审（评审子代理）

<!-- 由评审子代理填 -->

---

### 轮次 1（评审子代理）

**评审对象**：B21 面（`docs/design/PET-ANIMATION.md` §2.13 / §3.7 / §3.8 · `docs/requirements/PET.md` US-29…US-31 / NFR-23 / US-7 注记 · 本档 §1 / §2 / §2.8）。范围 = 三档文档全文；代码面未读（按评审纪律）——涉代码事实按文档声明或标 unverified。

| # | Category | Severity | Issue | Suggestion |
|---|----------|----------|-------|------------|
| 1 | Acceptance criteria / 需求口径一致性 | 🔴 | **AC26 ①（`PET-ANIMATION.md:1261`）与 TC-39（`:1296`）的「交互段段播期间无 `anim chain` 行」与 §2.13.4 预触发机制（`:965`「仅 `loop=false` 段」即武装到 happy / eat 事件段）相抵**：预触发在段末前触发 `decideNext`（掷骰 ⇒ `anim chain` 行；`:963-966`、`:969-986` 时序图），且 `event-end` 被计入「链自主换段」集合（`:966`）⇒ 合规实现的正常路径（点击 → happy 播完 → 回链；`:1296`）在该段播期间必然出现 `anim chain` 行 ⇒ AC26 ① 字面不可通过；这同时牵动 US-29「交互动作在途期间不换段」与 US-30 叠化的口径取舍（叠化起点落在交互段尾部）。 | 二选一写定并同源修订：① 回指需求口径澄清「不换段」= 不被个人动作**中途顶掉**、**不含**段末衔接退场（同时订正 AC26 ① 判据面，如改判 `anim switch` 无中途打断 / 以段末为界）；② 或 §2.13.4 明写预触发对「不由链自主驱动的档位段」豁免（并写清事件段退场 overlap 语义）。建议连带复核 `event-end` 在该口径下的归属。 |
| 2 | Document ownership / 机制级不一致 | 🔴 | **B18 面规范文本未随 B21 同步（同一机制两处描述不一致）**：§2.2.4 规则 3「段切换的**唯一两个触发**」（`:291`）未含新增的段末前预触发（§2.13.4 `:965`）；§2.2.5 步骤 4 的旧段 `pause()` 写在换前台时点（`:308`，且 `:303` 自述「实现与静态核对均按此判定」），与 §2.13.4「`pause()` 推迟到淡出窗末」（`:976-986`）、DD-31（`:1089`）、AC27 ④ / AC28 ④（`:1262-1263`）相抵；另 §2.13.6 的拖动期 `setSlot` 让位（`:1014`）旁路了规则 3(b) 而未在 §2.2.4 留注。C45（`:1050`）/C47（`:1052`）的「衔接仍走同一切换序列 / 规则表不改」与上述变更并存，按 §2.2.5 字面实施会与 AC27 ④（叠化窗两路在播）相抵。 | 就地同步 §2.2.4 规则 3（触发枚举 + 拖动窗旁路注）与 §2.2.5 步骤 4（pause 时点），或加「B21 起以 §2.13.4 为准」的显式指针行；并把该两处列入落盘同步清单（§2.8 现清单只有档头 / §1.1 / O27）。建议顺带订正 C45 / C47 的表述。 |
| 3 | 边界 / 竞态 | 🟡 | **淡出窗末的旧段 `pause()` 回调未写失效守卫**：回调在 +F（180 ms）后触发（`:979-980`）；窗内若发生新换段（快速连点 / 起拖）且复用同一 back 元素，回调可能暂停已被复用的元素 ⇒ 新前台被冻结（画面停住）。`:988-992` 的竞态清单只覆盖预载作废与 ended 竞态，`:986` 只规定「唯一发生点」。 | 在 §2.13.4 竞态清单补一条：回调执行前校验 `gen` / 该元素仍承载旧段名（或在每次换段时取消挂起的 pause 回调）；可顺带定一条判据（AC28 / 探针面）。 |
| 4 | Document ownership / 术语桥接 | 🟡 | **「交互档」一词跨节集合不一致**：§2.8.4（`:636`）/ §2.12.2 的工作档让位名单 = `happy` / `eat` / `read` / `starry` / `scared`；§2.13.3 按触发面把 `read` / `starry` / `scared`（自主面）归入**个人动作**（`:946`、`:949`）；C51（`:1056`）未做桥接 ⇒ 读者需自行拼合「自主 read 是否仍让位工作档」的结论。 | 在 §2.13.3 或 C51 补一句口径桥接（两处所指集合不同：§2.8.4 = 可覆盖工作档的**档位集合**、§2.13.3 = 按**触发面**的分类；两套机制均不变）。 |
| 5 | Methodology / 落点 | 🟡 | **受影响文件全清单落在批次档 §2.3（`B21:100-108`），设计档仅指针（`:889` 引 D2）**：与 `AGENTS.md` §二「设计：…受影响文件全清单…→ 落成设计档」的字面及 B18（设计档 §2.3）/ B19（§2.9）先例不一致（受影响文件维度的检查面因此需跨档读取）。 | 若 D2 系既有裁定 ⇒ 在设计档该指针处注明裁定来源行；否则把表补入设计档（或在 §2.13 内给行数与增量摘要）。表内容本身完整（改动的 5 个源档 + 池数据 + 2 个开发期档均有行数与增量），无须重做。 |
| 6 | Acceptance criteria / 判据口径 | 🔵 | **AC27 的带与采样口径两处过硬**：① `[N − 250, N]` 的单向论证（`:1270`）只覆盖「定时器只晚不早」；段长**估计**误差另一向可使实测 `overlap > N`（`overlap` 按估计结束算，`:1269`）⇒ 带上界无余量；250 ms 又为单次硬容差（③ 才给 0.9 比例口径）。② ④「窗外恒 1 路 `paused=false`」（`:1262`、`:1273`）与 ended 兜底路径相抵：该路径下 [D, D+L] 内旧段已 ended（paused=true）、新段未就绪 ⇒ 0 路在播。 | ① 容差写双向或注明估计误差的处置（并把「或 0」行的归属写清）；② ④ 明确以叠化窗为界（「窗外」= 叠化窗结束后的稳态）或对 ended 行豁免采样。 |
| 7 | 受影响文件 / 数值抽查 | 🔵 | **行数基准抽查存疑（未能核验）**：批次档 §2.3（`:104-105`）记 `pet.js` 236 / `shell-pet.js` 454（称 2026-09-18 亲测），设计档 §1.3.5（`:122`）实测（2026-09-17）为 211 / 426 ⇒ 差 +25 / +28，超出设计档自身载明的 B19 预算（§2.9 `:746-747`：+8~+14 / +4~+7）与 B26 增量之和；评审范围不含代码档 ⇒ 无法核验。即便取增量上限，预算末值仍远低于 500 ⇒ 无档位风险。 | 重测核对或注明口径 / 时点差异（避免行数预算的基准漂移）；如系 B19 / B26 实施超预算所致，建议同源登记。 |
| 8 | 一致性 / 计数口径 | 🔵 | **档位计数与映射表口径未含新档位**：§2.2.3 标题「11 档全部落位」（`:255`）与 §1.3.1 的「11 档词表」（`:59`）未含 `escape-*`（新增 pet-state 值）与 `drag`（链内交互档）；§2.2.3 的「单候选分支在本批不适用」注（`:268`）自 B21 起因 `drag` / `escape` 单段档而重启（设计正文已按该分支处理，`:1021`、`:1042`）；C47（`:1052`）「映射表不改（新增档位 = 映射表增行）」两半措辞相拉扯。 | 补指针 / 注记（承 B19 §2.8.3 独立表先例）：在 §2.2.3 或 §2.13.6 加一行「B21 起新增 drag / escape-* ⇒ 单候选分支启用」；C47 括注改为「新增档位在批次面独立成表」。 |
| 9 | 引用精度 | 🔵 | **`PET_OVERLAP_MS` 初值的依据指针落点不准**：批次档 §2.6（`:151`）与设计档 §2.13.2（`:936`）均引「立案档 §1.6 / 批次档 §2.6 的关联地图行『段间重叠 ~1–2 s』」，但该句不在 §1.6（`:49-53`）或 §2.6 内——实际位于文档地图（`docs/README.md` 的 B21 行）。 | 订正指针（指向地图行）或把该依据句补入相应节，保证待标定初值可溯源。 |

**计数**：🔴 2 · 🟡 3 · 🔵 4（共 9 项）。

VERDICT: changes-required

### 轮次 2（评审子代理）

**复核轮（逐条验证 §3 轮次 1 的 9 条发现 + 复扫新问题）**：范围 = 三档文档全文（本轮全部回读）；代码面未读（按评审纪律）——涉代码事实按文档声明或标 unverified。

| # | Orig# | Severity | Status | Evidence（本轮回读原文，节选）/ Notes |
|---|-------|----------|--------|------------------------------------------|
| 1 | 原 🔴#1 | ✅ 无遗留 | **Fixed** | AC26 ① 重写 + 注 G + 事件段不豁免句 + TC-39 + 需求档同源：`PET-ANIMATION.md:1269`「① 交互档在途（`drag` / `escape` / `happy` / `eat` 段播期间）⇒ 该段区间内**无中途顶掉**换段（判据细目 = 注 G）」；`:1295`「**合法面（段末衔接退场）**：区间内的换段只允许**链自主换段集合**（§2.13.4：`reason ∈ {pre-end, ended, event-end, slot-rotate}`）——`pre-end` 须落在段末前 `PET_OVERLAP_MS` 内；`anim chain` 行（链决策行）在场**不构成**违规」；`:972`「**事件段不豁免（F-1 口径）**：预触发武装到**全部 `loop=false` 段**——含 `happy` / `eat` 等事件段」；`:962` 口径句；`PET.md:300` 收窄句在场。残余（措辞面）= 新 #10。 |
| 2 | 原 🔴#2 | ✅ 无遗留 | **Fixed** | `:294`「**段切换的三个触发（B21 起；B18 期 = 两个）**：(a) `once` 段 `ended`；(b) 语义档位**变化**且新档 ≠ 当前档；(c) **段末前 `PET_OVERLAP_MS` 预触发**（B21 新增…）」+ `:295` 旁路注；`:307` 头部注 + `:312`「旧段 `pause()` **推迟到淡出窗末**（B21 起…就绪回调内**不再** `pause()`）」；`:1058` C45 / `:1060` C47 同步；同步清单增补 = 批次档 §2.9 F-2。 |
| 3 | 原 🟡#3 | ✅ | **Fixed** | `:1000`「**淡出窗末 `pause()` 回调的失效守卫（F-3）**：窗内发生新换段（快速连点 / 起拖）且复用同一 back 元素时，回调执行前须校验 `gen` 与「该元素仍承载旧段名」（或换段时**取消挂起的 pause 回调**）——防「暂停了新前台 ⇒ 画面冻结」」。 |
| 4 | 原 🟡#4 | ✅ | **Fixed** | `:955`「**「交互档」一词的跨节口径桥接（F-4；两处所指集合不同、两套机制均不变）**：…共同门的让位名单（…）是**档位集合**…本表的分类是**按触发面**的分类…两套口径互不引用、互不改写」；`:1064` C51 同步。 |
| 5 | 原 🟡#5 | ✅（advisory） | **Fixed** | `:894`「**体例注（F-5）**：受影响文件全清单以**批次档 §2.3 为 file 级权威表**是本批的既有裁定形态——与 **B26**（批次档 §2 落表、设计档只给指针）同形…本批承 B26 形态 ⇒「设计档内无全清单」非裸缺口」。注：B26 形态本身超评审范围，未核验。 |
| 6 | 原 🔵#6 | ⚠ 残余见 #11 | **Fixed（主判据面）** | `:1270` AC27②「链自主换段行 `overlap ∈ [N − 250, N + 250]` 或 0」+ ④「**窗后稳态**恒 1 路（ended 兜底行豁免）」；`:1278`「因此带 = **[N − 250, N + 250]**」+ `:1279` 单向字面作废声明；`:1280`「或 0」归属；`:1282` 探针采样豁免段。 |
| 7 | 原 🔵#7 | ✅ | **Not an issue**（基准说明行已落） | 批次档 `:185-186`「§2.3 行数基准 = 本角色 2026-09-18 亲测，**与现树一致**——`pet.js` 236 · `pet-chain.js` 240 · `pet-chain-core.js` 198 · `probe-pet-media.js` 345 · `pool.json` 74 均一致；`shell-pet.js` §2.3 记 454、现树 455 = **+1 归 B26 实施**…设计档 §1.3.5 的 211 / 426 = **2026-09-17（B19 期）历史值**」。代码面数值超范围（unverified）。 |
| 8 | 原 🔵#8 | ✅ | **Fixed** | `:268`「**计数口径注（B21；承 B19 §2.8.3 的「新增档位批次面独立成表」先例）**…」+ `:271`「**B21 起该分支重启**：`drag` / `escape` 两个新交互档各 1 段 ⇒ 单候选 ⇒ `loop=true`」+ `:59` 前向注；`:1060` C47「**新增档位在批次面独立成表**」。 |
| 9 | 原 🔵#9 | ✅ | **Fixed** | `:941`「依据 = `docs/README.md` 文档地图 B21 行「段间重叠 ~1–2 s」+ 素材段长实测 10.04 s」；批次档旧行以 §2.9 F-9 注记订正（`:188`，append-only 不改既有行）。 |
| 10 | (new) | 🟡 | **New（判据口径 · 非阻断）** | 注 G 在「交互→交互切换」上自相矛盾/可能误伤**合法**切换：违规面含「**用户触发驱动**的换段行零命中」（`:1294`），合法面又写「区间内的换段**只允许**链自主换段集合」（`:1295`），而区间口径（`:1293`「到其后第一条**非交互档**换段行」）使 `drag → escape`（松手入逃生档）/ `happy → eat`（交互中喂食）/ `escape → happy`（逃跑中点击）落在区间内 ⇒ 按字面判违规，与 `PET.md:311`「用户触发的动作引起的切换**仍然立即发生**」相抵。TC-37–TC-47 无该场景 ⇒ 非阻断。建议：违规面收窄为「idle / 个人档驱动」+ 明示「交互→交互切换延续区间且不构成违规」。 |
| 11 | (new) | 🟡 | **New（订正漏改 · 非阻断）** | `:1317` TC-44 仍写「链自主换段 `overlap ∈ [N − 250, N]` 或 0」、`:1319` TC-46 仍写「窗外恒 1 路 `paused=false`」——未随 AC27②/④ 与注 E（`:1270` / `:1278` / `:1282`）的**双向带 / 窗后稳态 + ended 豁免**同步；注 E 又声明「以本档为准」（`:1279`）⇒ 同档两版字面。建议就地订正 TC-44 / TC-46（或加「以注 E 为准」指针）。 |

**计数**：本轮新发现 = 🔴 0 · 🟡 2 · 🔵 0；原 9 项收敛 = Fixed 8 · Not an issue 1 · Unfixed 0。

VERDICT: pass

## §4 评审裁决与实施启动（主 agent）

<!-- 由主 agent 填 -->

---

## §5 实施记录（eng-coder）

<!-- 由 eng-coder 填 -->

---

## §6 验收核销（主 agent）

<!-- 由主 agent 填 -->
