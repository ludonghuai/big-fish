# 桌面壳需求档 — SHELL

> 归属板块：桌面壳（启动形态 / 托盘 / 窗口 / 桌宠交互入口 / 更新门禁）
> 落点：`docs/requirements/SHELL.md`（本档为桌面壳需求唯一权威源）
> 关联批次：`docs/batches/B06-shell-ux.md`（§1.3 需求结论 C1–C7、§1.4 技术裁定 R1–R7、§1.5 验收 AC1–AC7）·
> `docs/batches/B07-harness-auth-compat.md`（§1.3 需求结论 C1–C6、§1.4 技术裁定 R1–R6、§1.5 验收 AC1–AC7）·
> `docs/batches/B09-notify-latency.md`（§1.3 前置事实、§1.5 待决点、§1.6 验收 AC1–AC4）·
> `docs/batches/B10-affinity-token-source.md`（§1.3 勘察结论、§1.5 待决点、§1.6 验收 AC1–AC4）·
> `docs/batches/B12-market-security-blocking.md`（§1.3 范围、§1.4 验收 AC1–AC6）·
> `docs/batches/B30-notify-followup.md`（§1.3 两处实测证据、§1.6 待决 U-1·U-2）
> 关联设计档：`docs/design/SHELL-UX.md`（回指本档 US-1…US-17、NFR-1…NFR-7）

---

## 一、总目标

本档是桌面壳板块（Bigfish 桌面端「壳」层：启动形态 / 托盘 / 窗口 / 桌宠交互入口 / 更新门禁）需求的唯一权威源。**板块级总目标**：

让使用桌面端（验收目标平台为 Windows）的用户，在「壳」这一层获得安静、清晰、符合直觉的使用体验——**启动安静**（冷启动只出桌宠与托盘，不自动弹出任何窗口）、**入口清晰**（桌宠左键打开主界面、右键打开兑换屋，两条链互不串；托盘菜单按使用频率与视觉习惯组织、所有窗口入口可达）、**无冗余步骤**（删除不配置任何东西的新手向导）、**双态一致**（dev 与安装版的更新门禁口径可解释、可复现）；并且壳代码**结构可控**（`main.js` 按域拆分，修改任一域不必通读全档）。

**B06 批次的任务目标（原文保留）**：整合项目启动与桌面壳交互——启动形态 / 桌宠左右键语义 / 新手向导删除 / 托盘菜单重排 / 更新门禁口径 / `main.js` 拆分。

**追加工作项（F7，2026-09-16 主 agent 裁定；依据 = 用户指示「删 1-5」）**：无用文件清理——删除跑不起来的一次性脚本、逐字节重复的背景图与 0 引用的旧贴图（保留面清单见 US-8）。

**B07 批次的任务目标（2026-09-17）**：Harness ≥ 0.1.5 会话鉴权兼容（主窗口 URL）· 后端不拉起系统浏览器 ·
空闲检测不阻塞主进程 · F6 拆分遗留缺陷修复 · B06 收尾核销。

**板块级总目标的 B07 延伸**：壳与 Harness 的对接**以进程输出为契约**——版本差异（是否带 `?token=`）不写死在壳里，
靠读后端打印的地址适配；壳的主进程**不因周期性检测而停顿**——陪伴功能不得成为卡顿源。

**B10 批次的任务目标（2026-09-17）**：修复「基于真实 token 消耗累积好感度」的**壳侧数据面读口**——按 Harness 当前 per-record 布局读取会话投影缓存
（旧单文件布局兜底），并把计费口径对齐 Harness（`uncachedInput + cacheRead + cacheWrite + output`，用户 2026-09-17 裁定）。

**B12 批次的任务目标（2026-09-17）**：插件市场窗口的安全与响应性三项——安装 / 卸载入参门与越界校验（封路径穿越）、市场弹窗文本化（封注入面）、已装插件更新扫描提到循环外（封主线程同步阻塞）。

**板块级总目标的 B12 延伸**：壳自有的市场窗口（`market.html` / `market.js` / `shell-market.js` / `shell-plugins.js`）面对的是**第三方注册表数据**——该数据一律视为不可信输入：既不进文件系统路径拼接，也不进 HTML 解析面；并且市场操作的目录扫描开销与注册表规模**解耦**，市场不得成为主进程卡顿源。

---

## 二、范围

本档按批次登记桌面壳需求：

- **B06（桌面壳 UX 整合）**：US-1…US-8、NFR-1…NFR-4——七项：启动形态 / 桌宠左右键语义 / 向导删除 / 托盘重排 / 更新门禁口径 / `main.js` 拆分 / 无用文件清理（F7 追加项）；
- **B07（Harness 鉴权兼容与壳收尾）**：US-9…US-11、NFR-5——三项：主窗口会话鉴权兼容（Harness ≥ 0.1.5）/ 后端不拉起系统默认浏览器 / 空闲检测不阻塞主进程；并收口 US-7 的拆分遗留缺陷（技术待办 T12）与 `docs/design/AUTO-UPDATE.md` §3.1 判定面对齐（技术待办 T13）；
- **B09（任务完成提醒及时性）**：US-12——一项：完成提醒的及时性与误报边界（空闲阈值 30 s → 8 s + 「回合已闭合」判定 + 判定源不可用时的降级）；并新增 NFR-5 注记（判定读的 I/O 口径）；
- **B10（好感度真实消耗读面）**：US-13——一项：好感度数据面读口（per-record 目录优先 + 旧单文件兜底 + 计费口径 B + 全量聚合与水位语义 + 失败语义不回退）；
- **B12（插件市场安全与阻塞三项）**：US-14…US-15、NFR-6…NFR-7——两项：插件安装面安全（入参三形态门 + 越界校验 + 弹窗文本化）/ 市场扫描开销与注册表规模解耦；
- **B30（提醒面后续）**：US-16…US-17——两项：启动不误报「任务完成」（完成判定加「会话有回合历史」合取项）/ 回合长时间静默的「等待确认」提醒（近似判据 + 诚实文案）；
- **跨批不得回退项**：托盘常驻、单实例、`window-all-closed` 不退出、全局快捷键、桌宠穿透与拖动（B01/B03）、市场 / 兑换屋窗口、更新链路既有语义（除 US-6 明示的 dev 门禁口径修订面）、完成提醒语义（一个活动周期一次提醒 / `notifyOnComplete=false` 零提醒 / 关闭期活动重开不补发）、**插件安装面的入参门与越界校验（US-14）**——后续批次均不得回退。

明确不包含（本档不为其补造需求条文；若要做须另立批次）：

- **harness bundle 升级**（`0.1.0-rc.6` → 新版，技术待办 T10）——原注「归批（B07）」已顺延 **B08**（B07 被本收尾批占用；`docs/batches/B07-harness-auth-compat.md` §1.1），本档不为其立条文；
- **B02 / B03 / B04 / B05 已交付面的语义**——除 US-6 明示的 dev 门禁口径修订外，逐条保持；
- **macOS / Linux 的专项验证**——只要求既有行为不回退；
- 主界面 / 市场 / 兑换屋**内容页**（dsh web 应用内部）的功能需求；
  - **注（B12，2026-09-17）**：本句排除的是 **dsh web 应用内部**的内容页；**壳自有的市场窗口**（`market.html` 与 `shell-market.js` / `shell-plugins.js` 的安装通道面）不在排除范围内——
    US-14 / US-15 登记的是该壳侧窗口与安装通道（判定法与 US-13 的层级注同款：按「壳侧面 vs dsh 应用内部面」分层，不按功能名称归属）。
- 桌宠其余机制（好感度、兑换屋、散步、睡眠、闲聊）的功能性需求——它们目前只存在于代码中，本档不代其立需求；
  - **注（B10，2026-09-17）**：本句仍有效——B10 **不为**好感度机制补立功能需求（等级 / 兑换 / 喂食 / 文案均不立条）；US-13 登记的是**壳侧数据面读口**
    （壳读 Harness 落盘物的形态兼容与计费口径），属本档板块面（壳↔Harness 对接，见 §一 板块级总目标的 B07 延伸）。
- **模式选择弹窗**（首次安装 / 版本更新后弹一次）与 **dev 提示家族中非更新面成员**（卸载、Windows 右键菜单安装）的既有行为——保持现状，本批只动更新检查面。

---

## 三、功能用户故事

格式：**作为一个 [角色]，我想要 [功能]，以便 [目的]**。
本条验收回指 `docs/batches/B06-shell-ux.md` §1.5 的 AC1…AC7（编号取自该节）。

### US-1 桌宠左键 = 打开 / 聚焦主界面（只开不隐）（验收：AC1）

作为一个使用桌宠的用户，我想要左键点击鲸鱼娘时主界面**显示并聚焦**（而不是隐藏已打开的主界面），以便「想打开」的点击永远不会把界面藏起来。

- 背景：现状为显隐切换（toggle）——主界面已打开时点击桌宠会把它隐藏。
- 边界（不做）：不做显隐切换；不改动点击 / 拖动的判定阈值（位移 > 5px 判为拖动，承 `docs/requirements/PET.md` US-5）；点击后的台词与开心动画保持。
- 备注：本条「只开不隐」为主 agent 裁定 R3，待用户在设计评审时追认（`docs/batches/B06-shell-ux.md` §1.9）。

### US-2 桌宠右键 = 只开兑换屋（与左键链互不串）（验收：AC1）

作为一个使用桌宠的用户，我想要右键点击鲸鱼娘时只打开兑换屋、主界面的显示状态保持不变，以便一次右键不再同时冒出主界面与兑换屋两个窗口。

- 背景：现状右键松开同时触发左键链（一次右键同时出现主界面与兑换屋——既有语义缺口，`docs/design/PET-DRAG.md` §2.5 观察项 F2 / 技术待办 T1）。
- 边界（不做）：不改兑换屋窗口自身内容与定位策略（跟随桌宠的既有逻辑保持原样；专注模式下无桌宠时窗口落系统默认位置）。

### US-3 删除新手向导（验收：AC3）

作为一个使用桌面端的用户，我想要启动时不再出现「新手向导」窗，以便不被一个不配置任何东西、只把用户指去别处照做的窗口打扰。

- 依据：向导第 3 步原文即「把 Key 填进软件自带的设置」；harness 自带模型 / API Key 设置与首启引导（证据见设计档 §2.2.4）。
- 首启无 Key 路径（交代）：冷启动只出桌宠 + 托盘（承 US-5）→ 点击桌宠或托盘项打开主界面 → 按 harness 自带「设置 ▸ 模型 / API Key」完成配置。
- 边界（不做）：不重造任何替代向导 / 弹窗；托盘不新增「设置 API Key」项（用户裁定，`docs/batches/B06-shell-ux.md` §1.3 C2）。

### US-4 托盘菜单重排（验收：AC4）

作为一个使用托盘的用户，我想要托盘右键菜单按使用频率与视觉习惯组织——高频动作在前、模式提为一级、设置项归一组、危险操作隔离在「高级」子菜单、退出收尾——以便右键菜单一眼可用、符合视觉习惯。

- 可达性（硬要求）：专注模式下桌宠被销毁（桌宠右键链不可用）⇒「鲸鱼娘兑换屋」必须在托盘菜单有入口（一级项）。
- 边界（不做）：一级项 ≤ 12；不删任何既有能力（含 Windows 右键菜单安装 / 卸载、自动检查更新、任务完成通知、开机自启、找回鲸鱼娘）；不改托盘图标左键行为（保持主界面显隐切换）。

### US-5 冷启动形态 = 桌宠 + 托盘（主界面不自动显示）（验收：AC2）

作为一个使用桌面端的用户，我想要冷启动时只出现桌宠与托盘、主界面不自动弹出，以便启动不再被多余的窗口打扰——主界面按需打开（桌宠左键 / 托盘项 / 快捷键）。

- 边界（不做）：不改「是否启动后端」（后端照常启动，只改「是否显示窗口」）；不改「关闭主界面 = 隐到托盘」的既有语义；不改 `--open` / 第二实例 / 全局快捷键的显示路径。
- 备注：本条（C4 / R2 裁定）为主 agent 裁定，待用户在设计评审时追认（`docs/batches/B06-shell-ux.md` §1.9）。

### US-6 更新门禁口径（dev / 安装版双态）（验收：AC5、AC6）

作为一个在本地运行 Bigfish 的用户，我想要 dev 下不再出现「只在安装版可用」这类限制显示、并且 Harness 的检查与更新在 dev 同样可用（且真生效），同时安装版保留 App 自更新，以便两态行为一致、可解释、可复现。

- 口径（本条为权威口径；`docs/requirements/UPDATE.md` 中「dev 不检查 / 不更新」的旧条文已按本口径修订注记）：
  - **App 自更新面**：仅安装版可用。dev 下不执行、不显示、不弹窗（无任何 App 面的可见物）；相应跳过记日志。
  - **Harness 面**：dev 与安装版同口径放行——检查（手动 + 自动）、下载、安装、激活、回滚照常；dev 的 `dshBinPath()` 与打包分支同口径（活跃指针优先，兜底各自出厂副本），使 dev 的 harness 更新真生效。
  - **安装版行为不变**（`docs/batches/B06-shell-ux.md` §1.5 AC6）。
- 边界（不做）：不改 harness 检查的既有节奏（启动一次 + 6 小时轮询 + 托盘手动）与自动检查开关语义；不改门禁的 in-flight 守卫；不新增「只在安装版可用」类提示的替代文案。

### US-7 `main.js` 拆分（验收：AC7）

作为一个维护者，我想要 `main.js` 按功能域拆分为高内聚模块（拆分后每文件 < 500 行），以便修改任一域不必通读 2831 行、单文件体量债清零。

- 约束（口径随设计档）：纯结构迁移——行为零回退（`pet.*` / `updater.js` / `harness-store.js` 只搬不改）；零新依赖；不引构建步骤（保持「直接跑源码」可运行）；既有取证行 / 日志口径不丢（B01/B03 桌宠日志、B02/B04/B05 更新日志与 console 行）。
- 边界（不做）：不动 `market.js`（500 行贴线未超）；不顺手做任何行为变更；不引入构建器 / ESM 改造；不改 `package.json` 的依赖段。
- **B07 补注（2026-09-17；技术待办 T12）**：F6 拆分遗留两处未绑定引用——托盘「更换背景…」「恢复默认背景」所经的 `shell-mode.js` 调用了该档从未 require 的 `notifier`（该档只注入 `notify`）：
  `chooseBackground` 的异常被 `try/catch` 吞掉（只留一条假错误日志）、`resetBackground` 直接抛未捕获异常。属本条「行为零回退」口径下的缺口——B07 修复（走注入面 `notify`），并做同类未绑定引用全扫（期望 0 处）。
  验收 = `docs/batches/B07-harness-auth-compat.md` §1.5 AC5（设计档 AC14）。

### US-8 无用文件清理（验收：AC8；F7 追加项）

作为一个维护者，我想要删除仓库里跑不起来、无引用、重复或已被取代的文件，以便仓库不带「看似可用实则无效」的残留（残留即示范）。

- 清理面（白名单制；判定依据 = 主 agent 逐条核实，2026-09-16）：`remove-pet-bg.js` / `update-pet-frames.js`（全仓 0 引用、依赖 `sharp` 未安装跑不起来）；根 `background.jpg`（与 `assets/background.jpg` 逐字节重复，唯一读取面只读后者）；`assets/pet/` 中 9 张旧贴图（`pet.js` 逐文件核对 0 引用）。
- 保留（明确不动）：`assets/pet/idle.png`（待机帧仍在使用）· `assets/pet-new/**`（现行贴图）· `probe-*.js`（7 个）与 `debug-pet.cmd`（B03 实机验收待用）· `.npmrc` / `scripts/ensure-deps.js` / `package-lock*.json`（另一会话在途）。
- 边界（不做）：不做全仓未引用扫描式大清理；不动打包 / 构建配置（`package.json` scripts、`build.files`、依赖零改动）。

### US-9 主窗口能打开 Harness 的会话鉴权 UI（验收：AC1、AC6）

作为一个使用桌面端的用户，我想要主窗口打开后直接显示 Harness 的对话界面（而不是一行 401 提示文字），并且在 Harness 换代（≥ 0.1.5 引入 web 会话鉴权）后仍然如此，以便「打开主界面」这一动作永远真的能打开界面。

- 背景：Harness ≥ 0.1.5 的 web 服务给浏览器侧加了会话鉴权——启动时打印带一次性令牌的地址（`dsh web: http://127.0.0.1:<port>/?token=…`）；
  必须先访问该地址换取签名 Cookie（绑定 host:port）才能拿到 UI；直接访问裸地址一律 401 + 文本 `dsh web authentication required; reopen the URL printed by dsh web.`（取证见设计档 §2.2.8）。
- 契约（做什么）：主窗口加载地址以后端启动输出行 `dsh web: <url>` 为**唯一来源**（回环主机 + 当前端口校验通过才采用）；后端重启（新端口 + 新令牌）后主窗口自动跟进新地址；地址未捕获时回落裸地址**并在日志留一条可检索的诊断行**（不静默白屏）；地址晚到（宽限耗尽后才命中）时同样自动跟进——已按裸地址加载的主窗口补一次加载到命中地址，使该次会话自行回到对话界面（不要求用户重启应用）。
- 边界（不做）：不改 Harness 内部代码（`dsh-bundle/` 与 `userData/dsh-update/versions/**` 只读）；不关闭 / 绕过鉴权（Harness 无此开关）；不自行推算或伪造令牌 / Cookie；不新增 Harness 未支持的启动参数；不把令牌值写入任何文档或新增日志行。
- 兼容性：旧版 Harness（`0.1.0-rc.6`，打印不带令牌的裸地址）下行为与既有版本一致（捕获行在、回落面不变）。
- 备注：本条代码已由他在途会话先行落地并经用户实机确认可用（`docs/batches/B07-harness-auth-compat.md` §1.3 C6 / §1.6 #11）——B07 为**设计补档 + 评审追认**，不重写实现。

### US-10 后端不再拉起系统默认浏览器（验收：AC2、AC6）

作为一个使用桌面端的用户，我想要 Bigfish 启动后端时不再额外弹出系统默认浏览器窗口（壳自带主界面就是入口），以便启动不被多出来的浏览器打扰。

- 依据：需求池 R7 转正（用户 2026-09-17 明确同意并入本批；原 2026-09-16「单独立条，以后做」撤销）。
- 契约（做什么）：后端以 `--no-open` 启动（`dsh --profile web` 的既有选项）——启动日志不再出现 `dsh web: opening the default browser; pass --no-open to disable`，实际无新浏览器窗口 / 标签页。该选项**只影响是否拉起浏览器**，不影响地址打印（US-9 的捕获链依赖该行继续在场）。
- 边界（不做）：不改 Harness 其余启动参数语义；不改主窗口 / 托盘 / 快捷键的打开路径；不为「打开浏览器」另设替代入口。
- 取证面（硬）：后端 stdout/stderr 改为管道后**必须双写 `bigfish.log`**——B04 / B05 的验收取证面（`harness activate …` / `harness install phase=…` 行）依赖该档，不得因本批改动丢失。

### US-11 空闲检测不阻塞主进程（验收：AC3、AC4）

作为一个使用桌宠的用户，我想要「任务完成」提醒的探测不拖慢主进程（拖动桌宠、点托盘、开窗口不出现周期性顿挫），并且提醒语义与原来一致，以便陪伴功能只在真的完成任务时轻轻提醒一次。

- 背景：旧实现每 5 s 在主进程同步递归遍历整个 `~/.dsh`（本机实测 **20,857 文件**，其中 `pnpm-store/` 占 20,849）⇒ 每轮阻塞 ≈2 s，表现为周期性卡顿。
- 契约（做什么）：Windows 走 `fs.watch(recursive)` 事件推送（OS 级监听，零遍历）；其余平台走异步扫描回落（不阻塞）；5 s 定时器回调只做空闲判定的纯算术（无文件系统 I/O）；「任务完成」语义保持——后端写入期不误报、空闲达阈值后通知**一次**、`notifyOnComplete=false` 时零通知（且关闭期间的活动不在重新开启时补报）。
- busy 判定面（口径）：`~/.dsh` 下**路径任一段落**为 `profiles` 或 `node_modules` 的写入不计忙（= 旧实现的 skip 语义原样保留）；其余写入计忙。
- 边界（不做）：不改通知文案 / 阈值（`IDLE_NOTIFY_MS` = 30 s）/ 托盘开关语义；不新增依赖（含 `chokidar`）；不新增常驻日志（诊断行只在监视器注册失败或运行期异常时发）。

### US-12 完成提醒的及时性与误报边界（验收：AC17、AC18、AC19、AC20）

作为一个使用桌宠的用户，我想要「任务完成」提醒在助手回合真正结束后的数秒内到达——而不是固定空等 30 秒——并且任务进行中的静默（长工具调用 / 等待模型 / 网络停顿）不会被误当成「已完成」，以便提醒既快又准。

- 背景（2026-09-17 主 agent 实测 + 设计者复核）：提醒时刻 = 末次写入 + `IDLE_NOTIFY_MS`（30 s）+ 5 s 周期对齐 ⇒ 实测 30–35 s，与真实完成时刻脱钩（`docs/batches/B09-notify-latency.md` §1.1）；
  且忙信号只看 `~/.dsh` 下的写入——工具在用户工程目录里干活（长构建 / 安装 / 长命令）期间**完全没有**忙信号，静默窗口与「已完成」在现状判据下不可区分。
- 契约（做什么）：
  - **阈值**：`IDLE_NOTIFY_MS` = 8 s（现状 30 s）；有效时延 = 8–13 s（5 s 周期对齐）；`BIGFISH_IDLE_NOTIFY_MS` 为测试钩子（默认 8000；承 `BIGFISH_WEB_URL_WAIT_MS` 先例）。
  - **判定面**：触发条件由「静默达阈值」升级为「静默达阈值 **∧ 回合已闭合**」——「回合已闭合」读自 Harness 自己落盘的会话投影缓存（`~/.dsh/storages/session_projcache/sessions/<会话 ID>.json` 的 `record.rows.turnBoundary.val.openTurnStartSeq === null`）；该档在**每个 `turn/end` 强制落盘**（Harness 源码取证见设计档 §2.2.12）。
  - **判定四态**：判定一次性给出 `open` / `stale` / `unavailable` / `done` 之一——`done` ⇒ 提醒一次；**`open`（回合在途）与 `stale`（快照落后于会话日志）⇒ 本轮不提醒**（保守：宁可推迟，不误报；**非降级态**、不改阈值）；
    `unavailable`（记录缺失 / 形态不符 / 解析失败）⇒ **降级**：退回 30 s 阈值（= 现状语义）；`stale` 与降级**各发一条诊断行**（同形；每次监视器生命周期各至多一条）——「不提醒」不得无信号；
    持续 `stale`（同一 `stale` 状态延续 ≥ 30 s）⇒ 按 `unavailable` 处置（防「长期抑制且无信号」）。
- 契约修订（对 US-11 的显式修订面；US-11 其余语义逐条不变）：US-11 的「阈值（`IDLE_NOTIFY_MS` = 30 s）不改」与「空闲达阈值后通知**一次**」两处由本条修订——阈值面 30 s → 8 s、触发条件面增「回合已闭合」合取项；
  US-11 的 `fs.watch` 主路径 / 异步回落 / 5 s 定时器纯算术 / 写入期不误报 / 一周期一次 / `notifyOnComplete=false` 零通知 / 关闭期活动重开不补发 / busy 判定面（任一段落谓词）**逐条保持**。
- 边界（不做）：不改通知文案 / 不改托盘开关语义 / 不新增其他通知类型（「等待你的确认」类文案 = 设计档 open 项 U-13）/ 不改 busy 判定面 / 不新增依赖与文件 / 不改 Harness（`dsh-bundle/` 与更新副本只读）/ 不新增用户面提示。
- 验收：AC17（阈值与判定面 + 降级面）、AC18（误报边界）、AC19（既有语义不回退）、AC20（真机时延量化）——判据细化见 `docs/design/SHELL-UX.md` §2.2.12 / §3.1。

### US-13 好感度按真实消耗累积（数据面读口）（验收：AC21、AC22、AC23、AC24、AC25）

作为一个使用桌宠的用户，我想要好感度真的随我实际用掉的 token 增长（而不是长期停在原地），并且在 Harness 换代（存储布局变化）后仍然如此，以便「陪伴随时间积累」这件事不被一次布局变化悄悄作废。

- 背景：现状读 `<DSH_HOME>/storages/session_projcache.json`（0.1.0 期的单聚合文件），活跃 Harness（0.1.5-rc.1）已改为 **per-record 目录布局**
  （`<DSH_HOME>/storages/session_projcache/sessions/<会话 ID>.json`）⇒ 读失败被 `catch { return null }` 吞掉，好感度不因真实消耗增长（喂食加成不受影响）；
  依据 = 批次档 `docs/batches/B10-affinity-token-source.md` §1.3（B07 观察项 O15 / 技术待办 T14）。
- 契约（做什么）：
  - **读面双形态**：per-record 目录**优先**、旧单文件**兜底**；形态判别以**磁盘事实**为准（不嗅探 Harness 版本）。目录下「可解析记录数 ≥ 1」⇒ **只读目录**
    （不叠加旧文件——防同一批消耗被计两次）；否则读旧单文件；两者皆不可用 ⇒ 返回 `null`（**不是 0**——`0` 会把水位降到 0，使此前已计入的消耗在读数回升时被重复计入）。
  - **计费口径 = B**（用户 2026-09-17 12:19 裁定）：每条记录取 `record.rows.tokenUsage.val.totals`，逐桶按
    `uncachedInputTokens + cacheReadTokens + cacheWriteTokens + outputTokens` 求和；**只读 `totals`，不读 `last.buckets`**（后者是最近一步的分桶、其数值已含在 `totals` 内）；缺失桶按 0。
  - **聚合口径 = 全量累加**：目录下**全部**可解析记录求和；不按 workspace / 不按会话过滤；单条记录损坏只跳过该条（不整面失效）。
  - **增量语义（水位）**：读面返回**幂等水位**（同一磁盘状态重复调用**同值**——返回值幂等；**不写 `affinity.json`、不持持久状态**）；好感度累计值由调用方按水位差累加并持久化 ⇒ **重启不重复计入**。
  - **失败语义不回退**：路径缺失 / 目录不可读 / JSON 损坏 ⇒ 返回 `null`、不抛错、不影响喂食与兑换路径（既有「水位下降只重置基线」的重基线语义不变）。
  - **读面开销**：只读单层目录 `sessions/*.json`（**不做目录递归**）；同步读与现状同口径（量级上界与再议触发条件见设计档 §2.2.13）。
- **消解期（旧布局兜底）**：兜底**保留至 B08（内置 bundle 版本收口）收口点**——内置 Harness 现为 `0.1.5-rc.1`（**per-record 目录布局**；`dsh-bundle/node_modules/@deepseek-ai/dsh/package.json:4`，B08 钉版后实测 2026-09-17；原文该括注记 `0.1.0-rc.6`，已过期——修正轮 4 按实况更新）
  ⇒ 到期条件第一分句（内置 bundle ≥ `0.1.5-rc.1`（或任意产出 per-record 布局的版本）——判据记为「出厂路径产出 per-record 布局」）**已满足**，第二分句（活跃副本枚举中**无产出旧布局的版本**——判据 = 逐副本「是否产出 per-record 布局」）**不可从仓内验证**
  ⇒ 兜底与消解期**保留**（保留理由由「当前出厂路径的活跃形态」改为「活跃副本不可仓内枚举」）、到期核对顺延至**下次发版复核**；到期条件 = 内置 bundle ≥ `0.1.5-rc.1`（或任意产出 per-record 布局的版本；即出厂路径不再产生旧布局）**且**活跃副本枚举中**无产出旧布局的版本**（判据 = 逐副本「是否产出 per-record 布局」，与第一分句同口径；版本号只作举例）⇒ 由主 agent 在 B08 收口时核对并裁定剔除兜底分支；
  条件不成立 ⇒ 顺延并**重新登记到期条件**（不得默认永久）。
  （修正轮 5 字面口径收口：原第一分句字面「内置 bundle ≥ 0.1.5」按 semver 严格比较对预发布版 `0.1.5-rc.1` 恒假 ⇒ 改写为字面可满足的「≥ `0.1.5-rc.1`（或任意产出 per-record 布局的版本）」；**判据语义未变**。）
  （修正轮 6 字面口径收口：原第二分句字面「活跃副本枚举中无 < 0.1.5」按 semver 严格比较会把产出 per-record 布局的预发布副本（如 `0.1.5-rc.1`）计入「< 0.1.5」⇒ 与第一分句同型（以版本号比较代替「是否产出旧布局」）；现改为行为面字面「无产出旧布局的版本」，两分句同口径、版本号只作举例；**判据语义未变**。）
- 边界（不做）：不改 Harness（`~/.dsh` / `dsh-bundle/` / 更新副本**只读**）；不改喂食 / 兑换 / 好感度 UI 与既有常量（`AFFINITY_RATE` / `EXCHANGE_RATE` / `LEVEL_THRESHOLDS` / `FOODS`）；
  不改 10 s 检测周期与广播节奏；不新增依赖 / 不新增源文件；不做目录递归；不按 workspace / 会话过滤；不做异步化改造；不代 B08 改 bundle 版本。
- 备注：好感度属**桌宠机制**，但本条登记的是**壳侧数据面读口**（壳读 Harness 落盘物）——归属依据 = 台账 T14 的归属档（`docs/TODO.md:32`）+ 模块面（`shell-affinity.js` 为 B06 拆分的 `shell-*.js` 之一）
  + 设计档既登记（`docs/design/SHELL-UX.md` §2.5 C31 / 观察项 O15）；B10 不代桌宠板块补立好感度机制的功能需求（§二 注）。

### US-14 插件安装面不接受越界入参、注册表字段不执行（验收：AC26、AC27、AC28）

作为一个使用插件市场的用户，我想要即使第三方注册表里的条目被恶意构造（路径穿越标识 / HTML 字段），我的 `~/.dsh`（配置、会话、API 凭据）也不会被删除或覆盖、页面上也不会执行任何脚本，以便「浏览市场并点开一个条目」这一个动作永远不会触碰市场之外的文件系统。

- 背景：`installPlugin` / `uninstallPlugin` 对入参零校验即以 `path.join` 归一化 `..` 后递归删 / 拷（`shell-plugins.js:312-319` / `:358-362`）——`target` 可归一化至 `<DSH_HOME>`（含 `sessions/` 与 `.credentials.yaml`）；
  市场弹窗把注册表字段插值进 `innerHTML`（`market.js:286-290` / `:296-302`），而该渲染进程持有 `marketAPI` ⇒ 恶意条目被点开即直连前一项（批次档 `docs/batches/B12-market-security-blocking.md` §1.1 ①②）。
- 契约（做什么）：
  - **入参门（主防线）**：`installPlugin` / `uninstallPlugin` 只接受三种形态——① `builtin:<纯包名>` 或裸 `<纯包名>`；② `github:<owner>/<repo>[#<片段>]`；③ `[<scope>/]<纯包名>[@<版本>]`；其中「纯包名」的判据 = **既有** `isPlainPackageName()`。
    其它形态（`..` · 绝对路径 · 盘符 · 反斜杠 · UNC · `file:` · `link:` · `git+` · `@scope/..` 等）⇒ `{ ok:false, message }`，**不触文件系统、不起子进程**；该门同时覆盖 `market:install` / `market:uninstall` / `market:update` / `market:update-all`（后两者经 `installPlugin` 同一入口）——注册表实测 **3725** 个推导标识零误拒（设计档 §2.2.14）。
  - **越界校验（纵深防御）**：两处 `path.join` 之后各做一次包含判定——`target` 必须**严格落在** `profileDir()/node_modules` 之下、`bundledSource` 必须**严格落在** `bundledPluginsDir()` 之下（词法归一化判定；不做 `realpath`——pnpm 的链接布局会误伤合法安装且路径不存在时抛错）。
  - **弹窗文本化**：市场弹窗正文由 DOM 节点构造（插值一律走 `textContent`），该文件**全部 `innerHTML` 使用面清零**；UI 外观与文案逐字不变。
  - **拒绝消息形态**：`{ ok:false, message }`——消息**不含任何绝对路径**（不出现 `profileDir()` / `dshHome()` / `bundledPluginsDir()` 的解析值），只回显入参标识的前 60 字符。
- 边界（不做）：不做「注册表签名校验 / 更新 URL 宿主白名单 / 链接 href 协议白名单」等更大机制（体检已另记，属另批）；不改 IPC 通道名 / 参数契约 / 返回值形态；不改 `market:update-all` 编排语义、不改注册表来源与回退链；不做市场窗口的 `will-navigate` / `setWindowOpenHandler` 守卫（登记为观察项）。
- 备注（② 的执行面，据实标注）：市场页既有 CSP（`market.html:5`：`default-src 'self' data:`，无 `unsafe-inline`）按规范应阻断内联事件处理器与 `javascript:` URL——**该结论未实机实测**；但**注入面本身成立**（任意标记注入 ⇒ UI 欺骗 / 伪造按钮）⇒ 本条的判据 = **注入面消失**，与 CSP 状态无关。

### US-15 市场扫描开销与注册表规模解耦（验收：AC29、AC30）

作为一个使用桌宠的用户，我想要打开插件市场或刷新其状态时主进程不被数千轮同步目录扫描卡住，以便市场操作不再拖慢桌宠动画与拖动。

- 背景：`computePluginUpdates` 在**循环体内逐项**调用 `listInstalledPlugins()`（`readdirSync` + 逐项 `statSync`）与同步读 profile（`shell-plugins.js:152-177` / `:109-123` / `:52`）；注册表实测 **3727** 项 ⇒ 每次 `market:list` / `market:state` 约 3.7k 轮同步 fs，全部跑在 Electron 主线程（批次档 §1.1 ③）。
- 契约（做什么）：`market:list` / `market:state` 的文件系统**扫描次数与注册表条目数 N 无关**——同一已装状态下 N=2 与 N=3727 的调用计数相等；实现面 = **单次请求内一次扫描 + 快照复用**（快照 = `node_modules` 顶层条目 + bundles + 已装名单），快照生命周期 = **单次调用**（不引入跨请求缓存）。
- 判定零回退（硬）：`computePluginUpdates` 对同一夹具的**逐项判定结果与改前逐字一致**（更新清单逐字段相等 + `plugin update …` 日志行序列逐字相等）；对照基准 = 实施前用改前实现在同一夹具上的实测输出（黄金样本，落批次档 §5）。
- 边界（不做）：不改更新判定语义（入选条件 / 版本比较口径 / `github:` 重装口径 / 日志行格式）；不改 IPC 契约；不引入持久化缓存或定时刷新；不做异步化改造（超本批）；不改 `plugins.json` 与注册表来源。
- 备注：无新增用户可见面（本条可见收益 = 「不卡」）。

### US-16 启动不误报「任务完成」（验收：AC37）

作为一个使用桌宠的用户，我想要重启 Bigfish 后不会收到一条虚假的「任务完成」提醒，以便每次打开应用都从安静开始。

- 背景（2026-09-18 用户实机 + 主 agent 三段实证；技术待办 T40）：后端启动会在 `~/.dsh` 写入自己的会话落盘物
  （新建会话投影记录 `session-f098….json` + 会话日志 `session.v3.jsonl.zstd` + `storages/workspace.json`，实测 mtime 12:01:24）
  ⇒ 提醒模块把「启动写入 + 静默 8 s」判成任务完成 ⇒ 每次重启误报一次。
- 契约（做什么）：「回合已闭合」判据收紧为「**回合已闭合 ∧ 会话有回合历史**」——历史 = 会话投影记录的
  `rows.turnBoundary.val.lastStepStartSeq !== null`（该字段只在回合内的 `step` 事件置位；新建会话的判定行 init：三个可空字段全 `null`、`lastTurn` = `0`）
  ⇒ 从未开始过回合的新建会话不判完成、不提醒（按「回合在途」同处置）；有回合历史的正常完成 ⇒ 提醒一次（既有行为逐字不变）。
- 边界（不做）：不改阈值（8 s）/ 判定四态枚举与两条诊断行 / 既有通知文案；不改 busy 判定面与回落机制；不改 Harness（`~/.dsh` / `dsh-bundle/` 只读）。
- 验收：AC37（启动零误报 + 判定面历史封口）——判据细化见 `docs/design/SHELL-UX.md` §2.2.12 / §3.1。

### US-17 回合长时间静默时的「等待确认」提醒（验收：AC38）

作为一个使用桌宠的用户，我想要助手回合在途且长时间静默（很可能是在等我确认）时收到一条提醒，以便不用时不时回来查看「它是不是在等我」。

- 背景：现判定「回合在途 ⇒ 不提醒」正确（避免语义错误的「已完成」——设计档 L-B09-1），但等待用户确认期间（提问 / 审批）
  用户收不到任何信号（B09 设计期 U-13 / 需求池 R12）。
- 契约（做什么）：静默达 `WAITING_NOTIFY_MS`（默认 **60 s**；测试钩子 `BIGFISH_WAITING_NOTIFY_MS`，承 `BIGFISH_IDLE_NOTIFY_MS` 先例）
  ∧ 判定为「回合在途」∧ 会话有回合历史 ⇒ 提醒**一次**；文案三串：标题 `Bigfish 可能正在等你确认`、
  正文 `助手已静默片刻；若它没有在跑长任务，回来看看吧`、桌宠台词 `等你确认哦！`（**诚实口径**——见边界）；
  每回合至多一次；与「完成」提醒**互斥**（同一判定的不同态）；`notifyOnComplete=false` 时不提醒；
  判定读**零新增**（复用完成判定的同一次读）。
- 边界（不做）：不改「完成」提醒的阈值与文案；不加新读面 / 新依赖 / 新文件；不改 Harness；
  **不区分**「长工具调用」与「等待用户」——两者在壳可读面不可区分（审批事件为 log-only；已知限制 L-B30-1），
  文案按诚实口径声明；精确判据随事件桥批次（B22）升级（观察项 O27）。
- 验收：AC38（等待确认信号）——判据细化见 `docs/design/SHELL-UX.md` §2.2.12 / §3.1。

---

## 四、非功能标准

### NFR-1 启动与首启可用性（验收：AC2、AC3）

- 冷启动（whale 模式）序列中，除桌宠窗与托盘外**不得出现任何自动可见的窗口**（向导窗与主界面均不自动显示）。
- 首启可达性：无 Key 用户从冷启动到达「模型 / API Key」配置入口的路径 ≤ 2 步（打开主界面 → 设置），且每一步都有可达入口（桌宠左键 / 托盘项）。
- 度量方式：真机窗口枚举 + 显示路径清单静态核对（设计档 §2.2.2）；首启路径真机走查。

### NFR-2 兼容性

- Windows 为验收与验证目标平台；macOS / Linux 既有行为不得回退。
- 本批的启动形态（US-5）与托盘结构（US-4）变更为**跨平台有意变更**（纯 Electron 共有 API、无平台分支），不属于「回退面」；macOS `activate`（Dock 点击）统一改经 `showMainWindow()`（用户主动显示请求：显示 + 聚焦；零窗口时建窗后显示）——**有意变更**，同样不属于「回退面」；`activate` 之外的 macOS / Linux 既有行为不得回退。
- 既有 win32 分支（穿透开关、Windows 右键菜单）保持原样。
- **B07 注记（2026-09-17）**：本批新增的后端输出捕获 / 双写 / `--no-open` 路径**零平台分支**（纯 Node 流 API + Harness 既有选项）；空闲检测在非 Windows 平台走**异步扫描回落路径**（不因平台分支回归、不阻塞）。
- 度量方式：评审逐条核对设计档 §2.3 受影响文件表与 §2.5 冲突点核对表；`grep` 平台分支计数不变（新增代码零平台分支）。

### NFR-3 可维护性（拆分口径；验收：AC7）

- 拆分后**全仓 JavaScript 单文件 ≤ 500 行**（含既有文件；现状超顶者仅 `main.js` 一处 2831 行，拆分后清零）。
- 零新依赖（`dependencies` 保持空数组）、不新增构建步骤（源码直接可运行）。
- 既有取证锚点不丢：`pet-drag.log` / `pet-geometry.log`（B01/B03）、`updater.log`（B02/B04/B05，含 15 条主格式行与变体）、`market.log` / `exchange.log` / `bigfish.log`、console 行、env 开关（`BIGFISH_PET_DEBUG` 等）——字符串与发射点逐条保持；拆分自身不新增 / 不删除日志（US-6 新增的 `update gate … face=app skipped=dev` 为唯一新增行）。
- **B07 注记（2026-09-17）**：`bigfish.log` 的取证面契约扩展为「后端 stdout/stderr 双写 + 壳侧启动横幅行 + 本批新增的地址捕获 / 未捕获诊断行」（设计档 §2.2.9）；`dependencies` 保持空数组、不引构建步骤；本批**零新增文件** ⇒ `package.json` 的 `build.files` 不动。
- 度量方式：行数实测（换行符计数口径，定义见 `docs/design/SHELL-UX.md` §2.3 行数口径注）+ `node --check` 全绿 + 锚点清单逐条 `grep`（设计档 §3.3）+ `package.json` 依赖段零 diff。

### NFR-4 双态可解释性（验收：AC5、AC6）

- dev 与安装版的差异**只允许**两处：① App 自更新面（仅安装版）；② 出厂副本兜底路径（dev = `dsh-bundle/`，安装版 = `resourcesPath/dsh`）。除这两处外两态同口径。
- 度量方式：门禁分支与路径分支静态核对（设计档 §2.2.5）；dev 实跑对照 `updater.log` 行。

### NFR-5 主进程响应性（空闲检测域；验收：AC3、AC17）

- 5 s 周期回调路径内**不得出现任何同步文件系统调用**——判据 = 探测模块全文无 `*Sync(` 调用面（死代码同判：不留同步遍历实现，残留即示范）。
- 探测开销与 `~/.dsh` 规模解耦：Windows 主路径为 OS 级事件推送（不为每个文件做 `stat`）；回落路径为异步扫描（不阻塞事件循环）。
- 度量方式：静态判据（`grep` 于 `shell-notify.js`：`*Sync(` = 0 处）+ 真机（本机 `~/.dsh` 20,857 文件下，桌宠拖动 / 托盘点击 / 开关窗口无可感周期性顿挫；旧实现对照 = 每 5 s ≈2 s 阻塞）。
- 适用范围：Windows 为验收平台；非 Windows 走异步回落路径，同样满足「无同步调用」判据（不要求事件推送语义）。
- **B09 注记（2026-09-17）**：「回合已闭合」判定读**每静默窗至多一次、且只在静默达阈值时触发**（结果按末次写入时刻记忆，任何新写入即失效重判）——只读 1 个会话投影缓存记录（单档，含读前 `stat`）+ 会话档 mtime 比较（按会话数计，非按文件数计），不做目录递归遍历；
  判据「全档无 `*Sync(`」「5 s 回调体内无同步文件系统调用」「零平台分支」三条不变；判定对通知只做**抑制**（不新增通知类型、不新增提醒面——**B30 生效口径注**：自 B30 起「除 US-17 的等待确认提醒外」，B09 原文不动；承设计档 `SHELL-UX.md` §2.2.12 `:988` 先例）。
- **B30 注记（2026-09-19）**：等待确认面**零新增读面**——复用完成判定的同一次 probe 结果（记忆扩两键 `lastStepStartSeq` / `lastTurn`），
  读次数上界与「每静默窗至多一次」口径不变；「无 `*Sync(`」「5 s 回调体内无同步文件系统调用」判据照旧。

### NFR-6 不可信输入面（安全；验收：AC26、AC27、AC28）

- 第三方注册表字段（`name` / `npm` / `install` / `url` / `version` 与一切未在 schema 内声明的字段）在本板上**一律视为不可信**：
  ① 不得参与文件系统路径拼接（唯一例外 = 经 US-14 三形态门与包含判定双重校验之后的包名）；② 不得进入 HTML 解析面（`innerHTML` / `insertAdjacentHTML` / HTML 字符串模板进 DOM）。
- 破坏面界定义（判据）：`installPlugin` / `uninstallPlugin` 的删除与拷贝面**只能**落在 `profileDir()/node_modules` 与 `bundledPluginsDir()` 之下——任何一次删除 / 拷贝的目标不在该范围内即视为缺陷。
- 度量方式：静态判据（`market.js` 全文 `innerHTML` / `insertAdjacentHTML` / `outerHTML` / `document.write` 各 **0** 处——与第二分句的三形态逐条同宽（修正轮 1 #8）；
  `shell-plugins.js` 两处 `path.join` 之后各有一次包含判定调用，且该判定在该分支内任何 fs 调用之前）+ 桩测（穿越形态枚举负例 ⇔ `ok:false` 且两侧夹具文件系统零改动；恶意 `name` ⇔ 只产生文本节点）+ 真机目视（恶意条目的弹窗无脚本执行、无新增元素节点）。
- 适用范围：Windows / macOS / Linux 同口径（本批改动零平台分支）；只覆盖壳侧市场窗口与安装通道面，不覆盖 dsh web 应用内部页（§二 注）。

### NFR-7 市场扫描开销与注册表规模解耦（验收：AC29）

- `market:list` / `market:state` 的**扫描次数**（`readdirSync` 计数）与注册表条目数 N 无关；与已装规模的关系 = 单次 `readdir` + 每顶层条目一次 `stat`（与改前同口径，但**只执行一次**而非 N 次）。
- 度量方式：计数器桩（同一夹具下 N=2 与 N=3727 的 `readdirSync` / `statSync` / `readFileSync` 计数**相等**；`node_modules` 的 `readdirSync` 计数 = 1 / 次调用）+ 真机（3727 项目录下打开市场 / 刷新状态无可感顿挫）。
- 适用范围：三平台同口径（零平台分支）。

---

## 五、变更记录

| 日期 | 变更点 |
|---|---|
| 2026-09-16 | 初版：新建桌面壳板块需求档（B06：US-1…US-7、NFR-1…NFR-4），覆盖启动形态 / 桌宠左右键语义 / 向导删除 / 托盘重排 / 更新门禁口径 / `main.js` 拆分六项；验收编号回指 `docs/batches/B06-shell-ux.md` §1.5 AC1…AC7。 |
| 2026-09-16 | 追加 **US-8 无用文件清理**（F7，主 agent 裁定并入本批；依据 = 用户指示「删 1-5」）——白名单精确删除（2 脚本 + 根重复图 + 9 张旧贴图）；保留 `assets/pet/idle.png` / `assets/pet-new/**` / `probe-*.js` / `debug-pet.cmd`；验收 = AC8（机检）。 |
| 2026-09-16 | **评审修正轮 1**（源 = `docs/batches/B06-shell-ux.md` §3 轮次 1 发现 #6；类别 = 口径补充）：§四 NFR-2 补 macOS `activate` 有意变更句（Dock 点击改经 `showMainWindow()`——用户主动显示请求；设计面 = `docs/design/SHELL-UX.md` §2.2.2 / L3）。US / NFR 计数不变（8 / 4）。 |
| 2026-09-17 | **B07 批次追加**：新增 **US-9 主窗口会话鉴权兼容**（Harness ≥ 0.1.5 的 `?token=` 地址换取会话 Cookie）/ **US-10 后端不拉起系统默认浏览器**（需求池 R7 转正）/ **US-11 空闲检测不阻塞主进程**（含 busy 判定面口径）； |
|  | 并新增 **NFR-5 主进程响应性**；US-7 加 T12 补注；NFR-2 / NFR-3 加 B07 注记；§二 范围同步（B07 条目 + T10 顺延 B08 的指针更正）。**条目计数：US 8 → 11、NFR 4 → 5**。 |
| 2026-09-17 | **B07 修正轮 1**（源 = `docs/batches/B07-harness-auth-compat.md` §3 轮次 1 发现 #5；主 agent 裁决 = 修）：US-9 契约补「地址晚到（宽限耗尽后命中）时同样自动跟进」——设计面 = `docs/design/SHELL-UX.md` §2.2.8「晚命中回收」/ DD-28。条目计数不变（US 11 / NFR 5）；同轮：NFR-3 度量方式行口径措辞统一（`find /c /v ""` → 换行符计数，形式修正无语义变化）。 |
| 2026-09-17 | **B09 批次追加**：新增 **US-12 完成提醒的及时性与误报边界**（阈值 30 s → 8 s + 「回合已闭合」判定 + 判定源不可用时的 30 s 降级）；并**显式登记对 US-11 的修订面**（阈值面与触发条件面——US-11 原文不动，其余语义逐条保持）； |
|  | 新增 **NFR-5 注记**（判定读的 I/O 口径）；§二 范围同步（B09 条目 + 跨批不得回退项补「完成提醒语义」）。**条目计数：US 11 → 12**；NFR 编号不变（5），NFR-5 增 1 条注记。 |
| 2026-09-17 | **B10 批次追加**：新增 **US-13 好感度按真实消耗累积（数据面读口）**——读面双形态（per-record 目录优先 / 旧单文件兜底）· 计费口径 = B（对齐 Harness：`uncachedInput + cacheRead + cacheWrite + output`，用户 2026-09-17 裁定）· 全量聚合 + 水位语义（重启不重复计入）· 失败语义不回退（`null`、不抛）； |
|  | 并登记旧布局兜底的**消解期**（保留至 B08 收口点，到期条件 = 内置 bundle ≥ 0.1.5 ∧ 活跃副本无 < 0.1.5）；☞ §一 补 B10 批次目标、§二 范围补 B10 条目 + 对「好感度机制功能需求不立条」句加**层级注**（原句不改）。**条目计数：US 12 → 13**；NFR 编号与计数不变（5）。头部「关联设计档」的条目区间同步为 **US-1…US-13**（计数同步，承 D3）。 |
| 2026-09-17 | **B12 批次追加**：新增 **US-14 插件安装面不接受越界入参、注册表字段不执行**（入参三形态门复用 `isPlainPackageName` + `path.join` 后越界校验 + 弹窗文本化）与 **US-15 市场扫描开销与注册表规模解耦**（单次请求内一次扫描 + 快照复用；逐项判定零回退）； |
|  | 并新增 **NFR-6 不可信输入面**（第三方注册表字段不进路径拼接 / 不进 HTML 解析面）与 **NFR-7 市场扫描开销与注册表规模解耦**；§一 补 B12 批次目标、§二 范围补 B12 条目 + 跨批不得回退项补「插件安装面的入参门与越界校验」+ 「市场内容页」排除句加**层级注**（原句不改，判定法与 US-13 层级注同款）。**条目计数：US 13 → 15、NFR 5 → 7**；头部「关联设计档」的条目区间同步为 **US-1…US-15、NFR-1…NFR-7**（计数同步，承 D3）。 |
| 2026-09-17 | **B09 修正轮 1**（源 = `docs/batches/B09-notify-latency.md` §3 轮次 1 发现 #1🔴 / #8；裁决 = 全部 Dispatched）：① US-12 的「降级」句改写为**判定四态**（`open` / `stale` / `unavailable` / `done`）——`stale` **移出降级枚举**（= 本轮不提醒，同设计规则③），降级仅限 `unavailable`；`stale` 与降级**各发一条诊断行**，持续 `stale` ≥ 30 s ⇒ 按 `unavailable` 处置； |
|  | ② NFR-5 标题验收指针 `（验收：AC3）` → **`（验收：AC3、AC17）`**（B09 注记的判据落在 AC17）。**条目计数不变（US 12 / NFR 5）**；设计面同步 = `docs/design/SHELL-UX.md` §2.2.12 / DD-49 / §3.1 AC17·AC18 / §3.2 TC-48。 |
| 2026-09-17 | **B12 修正轮 1**（源 = `docs/batches/B12-market-security-blocking.md` §3 轮次 1 发现 #8🔵 / #10🔵；裁决 = Dispatched）：**NFR-6 度量方式**的静态判据面扩宽为与第二分句逐条同宽——`market.js` 全文 `innerHTML` / `insertAdjacentHTML` / `outerHTML` / `document.write` 各 **0** 处；并补明包含判定与 fs 调用的**顺序**要求（#10 的判据面）； |
|  | 同轮同步：设计档 AC28 ① / §3.3 手段 13 ①。**条目计数不变（US 15 / NFR 7）**；**未修订任何契约句**（US-14 卸载面解析门只改实现机制与设计判据——白名单对两函数的三形态授权、拒绝消息形态逐字不变）。 |
| 2026-09-17 | **B10 修正轮 1**（源 = `docs/batches/B10-affinity-token-source.md` §3 轮次 1 发现 #3；裁决 = Dispatched）：US-13「增量语义（水位）」的措辞收敛为**返回值幂等**——「同一磁盘状态重复调用**同值**、**不写 `affinity.json`**、不持持久状态」（原「不写盘」与设计侧诊断行（每进程至多一条、非持久状态）相抵；修正轮 1 #3）；
|  | **条目计数不变（US 13 / NFR 5）**；**未修订任何契约句的语义面**（读面形态 / 计费口径 / 消解期 / 失败语义逐字不变）；设计面同步 = `docs/design/SHELL-UX.md` §2.2.13「增量语义（水位）」/「诊断行」+ DD-38。 |
| 2026-09-17 | **B10 修正轮 2（范围扩展轮——台账 T27 并入本批；用户 2026-09-17 裁定）**：**契约面逐字未改**（US-13 的读面形态 / 计费口径 / 聚合口径 / 增量语义 / 失败语义 / 消解期 / 边界全部不变）；本轮修订全落设计侧 = |
|  | `docs/design/SHELL-UX.md` §2.2.13 新增**基线重建**（启动期读数为 `null` ⇒ 首次读到非 `null` 水位时**只重建基线、不计 delta**；残余 = 暂停期消耗不补算）+ L-B10-5 改写为「**本批已修**」+ DD-38 更正 + 新增 **DD-50** + AC23 调用面口径（「零 diff」→「除该分支外零改动」；判据 = **TC-67B** 优先）+ **TC-67A 更正** + **TC-67B 新增**。**条目计数不变（US 13 / NFR 5）**。 |
| 2026-09-17 | **B10 修正轮 4（实施后收口轮——源 = 批次档 §5 残余 #3；主 agent 裁决 = Dispatched）**：US-13「消解期（旧布局兜底）」的**现状括注**按实况更新——内置 Harness `0.1.0-rc.6`（单文件布局）→ **`0.1.5-rc.1`（per-record 目录布局）**（`dsh-bundle/node_modules/@deepseek-ai/dsh/package.json:4`，B08 钉版后实测）； |
|  | 并把兜底的**保留理由**由「当前出厂路径的活跃形态」改为**「活跃副本枚举不可从仓内验证」**（第一分句已满足 / 第二分句不可仓内验证 ⇒ 兜底与消解期保留，到期核对顺延至下次发版复核）。**到期条件 / 保留期 / 顺延重登三句逐字未动**；**条目计数不变（US 13 / NFR 5）**；设计面同步 = `docs/design/SHELL-UX.md` §2.2.13「消解期（旧布局兜底）」+ DD-39。 |
| 2026-09-17 | **B10 修正轮 5（单条 · 判据字面口径收口——源 = 批次档 §2.14 报而不改的 N-1；主 agent 复核裁定 = 修）**：US-13「消解期（旧布局兜底）」的**到期条件第一分句字面**由「内置 bundle ≥ 0.1.5」改写为「≥ `0.1.5-rc.1`（或任意产出 per-record 布局的版本）」——按 semver 严格比较，预发布版 `0.1.5-rc.1` < `0.1.5` ⇒ 原字面**自身永假**（与「已满足」的结论相抵）； |
|  | **判据语义未变**（该分句要表达的就是「出厂路径已切 per-record 布局」），保留期 / 第二分句 / 顺延重登 / 到期核对四句逐字未动。**条目计数不变（US 13 / NFR 5）**；设计面同步 = `docs/design/SHELL-UX.md` §2.2.13「消解期（旧布局兜底）」（含新增「修正轮 5」说明条）。 |
| 2026-09-17 | **B10 修正轮 6（判据字面口径 · 家族全清——源 = 批次档 §2.14 报而不改的 N-2 + 主 agent 列全的家族剩余面；主 agent 复核裁定 = 修）**：US-13「消解期（旧布局兜底）」的**到期条件第二分句字面**由「活跃副本枚举中无 < 0.1.5」改写为「活跃副本枚举中**无产出旧布局的版本**」（判据 = 逐副本「是否产出 per-record 布局」）——预发布版 `0.1.5-rc.1` 按 semver 严格比较 < `0.1.5` ⇒ 原字面把产出新布局的预发布副本误计为「< 0.1.5」（与第一分句同型的口径错误）；
|  | **两分句自此统一为行为面口径**（版本号只作举例）。**判据语义 / 保留期 / 顺延重登 / 到期核对口径均未变**。**条目计数不变（US 13 / NFR 5）**；设计面同步 = `docs/design/SHELL-UX.md` §2.2.13「消解期（旧布局兜底）」（含新增「修正轮 6」说明条）+ DD-39 决策表行 + §2.6 追认块。 |
| 2026-09-19 | **B30 批次追加**：新增 **US-16 启动不误报「任务完成」**（T40：完成判定加「会话有回合历史」合取项——新建会话不再判完成）与 **US-17 回合长时间静默时的「等待确认」提醒**（R12：近似判据 + 诚实文案 + 每回合一次）；§二 范围同步（B30 条目）；NFR-5 增 B30 注记（等待面零新增读面）；头部「关联批次 / 关联设计档」同步（B30 指针 + US-1…US-17 区间）。**条目计数：US 15 → 17**；NFR 编号与计数不变（7）。 |
| 2026-09-19 | **B30 修正轮 1**（源 = `docs/batches/B30-notify-followup.md` §3 轮次 1 发现 #3 / #4；评审 VERDICT = pass）：NFR-5 B09 注记「判定对通知只做抑制」句补 **B30 生效口径注**（自 B30 起 = 「除 US-17 的等待确认提醒外」；B09 原文不动——承设计档 `SHELL-UX.md` §2.2.12 `:988` 先例）； |
|  | NFR-5 B30 注记「记忆扩两键」与设计档 `completionGateMemo` 全键五键口径核对一致（无改动）。**条目计数不变（US 17 / NFR 7）**。 |
