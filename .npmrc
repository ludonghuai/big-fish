; 仓库的 package-lock.json 里 resolved 指向 registry.npmmirror.com（国内下载快），
; 而 npm 12 起 allow-remote 默认为 none：只有与「当前配置的 registry 同主机」的
; tarball 才允许下载。若使用者用的是别的源（比如默认的 registry.npmjs.org），
; 安装会直接以 EALLOWREMOTE 失败。
; 下面这行让 npm 把锁文件里的 resolved 主机改写成使用者自己配置的 registry，
; 于是无论用哪个源都能装（tarball 也从使用者自己的源下载，不再依赖 npmmirror 可达）。
replace-registry-host=always
