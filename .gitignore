node_modules/
dist/
electron-dist/
node-runtime/
.npm-cache/
.electron-cache/
.electron-builder-cache/
.dsh-home/
.dsh-home-packaged/
*.log
.DS_Store
probe-electron.js
download-electron.js
download-node.js

# 框架目录（checklist / memory / sessions / 临时开发工具）——非交付物
.thincoder/

# 测试临时产物（隔离 userData / DSH_HOME 的测试目录）——非交付物
.test-userdata*/
.test-dsh-home/
测试-更新功能/

# 外部项目样本 / 移植参考区——非交付物：不被任何代码引用 · 不参与构建 · 不打进发行版
# （build.files / extraResources 为显式白名单，不登记即排除；第三方代码不入仓历史——见 docs/README.md §一 落点行 / 台账 R14）
samples/
